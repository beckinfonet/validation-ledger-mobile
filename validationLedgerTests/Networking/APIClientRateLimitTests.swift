// validationLedgerTests/Networking/APIClientRateLimitTests.swift
// Wave 0 stub — Plan 05 fills in red→green cycles for AUTH-02, D-02.
// Per 03-PATTERNS.md flagged convention: unit tests use Swift Testing (`import Testing`),
// NOT XCTest. XCUITests stay on XCTest in a separate target.

import Testing
import Foundation
@testable import validationLedger

@Suite("APIClient — 429 + Retry-After parsing (AUTH-02, D-02)")
struct APIClientRateLimitTests {
    @Test(.disabled("Wave 2 Plan 05 implements"))
    func placeholder() {
        // Implementation lands in Plan 05.
    }
}
