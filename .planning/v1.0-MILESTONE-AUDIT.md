---
milestone: v1.0
milestone_name: M1 Foundation
audited: 2026-05-18T23:55:00Z
status: tech_debt
re_audit:
  previous_audit: 2026-05-18T19:15:00Z
  previous_status: gaps_found
  gaps_closed:
    - "DEV-04 — App Attest now fires on first OTP verify (Phase 6 Plan 02; OTPViewModel STEP 5 rewrite)"
    - "CI-03 — retroactive 04-VERIFICATION.md now exists (Phase 6 Plan 04)"
    - "Phase 4 unverified — 04-VERIFICATION.md produced (status human_needed, score 3/3)"
    - "Integration BLOCKER — OTPViewModel now captures /device/register response and writes trustTier (Phase 6 Plans 01+03)"
    - "Integration LOW (KYC-05) — Profile-entry 'Continue' CTA onVerified now wired (Phase 6 Plan 03 D6-10)"
    - "WARNING-1 — KYCStatusViewModel.refreshCachedKYCStatus refreshes Keychain kycStatus after GET /kyc/status (Phase 6 Plan 03)"
    - "WARNING-2 — AppSession.trustTier didSet posts .trustTierDidChange; AppCoordinator re-renders LimitedTrustBanner (Phase 6 Plan 03)"
  gaps_remaining: []
scores:
  requirements: 67/67
  phases: 6/6
  integration: 8/8
  flows: 8/8
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 01-foundational-conventions-scaffolding
    items:
      - "Nyquist: 01-VALIDATION.md is an unfilled planning-time draft (status draft, nyquist_compliant false, wave_0_complete false) — PARTIAL"
      - "CR-02b: PIIScrubber DL regex over-eager — matches transaction IDs like TX1234567 (over-redaction, warning-level)"
      - "CR-03: DevMenuViewController force-unwrap Row(rawValue:)! at line 68 (DEBUG-only)"
      - "check-coverage.sh integer truncation — 69.99% logs as 69 (cosmetic; gate math correct)"
      - "pre-commit.sh silent 20-file cap on staged files linted"
      - "8 open HUMAN-UAT scenarios (01-HUMAN-UAT.md) — simulator launch, DevMenu, Keychain wipe, PII end-to-end, CI PR trigger, device CI, role swap, .ipa extraction"
  - phase: 02-networking-contract-device-keys
    items:
      - "Nyquist: no 02-VALIDATION.md — MISSING"
      - "CR-01: APIClient.send default extension default-arm force-unwraps HTTPURLResponse(...)! (reachable only by non-URLSession conformers)"
      - "CR-04: noReleasePlaceholders test is DEBUG-skipped; no Release-config CI job — PHASE2-TODO SPKI placeholders could ship unguarded (M5 pre-submission hygiene)"
      - "WR-02: DeviceFingerprint.resolveInstallUUID swallows Keychain errors via try? — before-first-unlock window can regenerate install UUID"
      - "IN-02: SoftwareKeyStore (sim) returns 64-byte compact ECDSA; SecureEnclaveKeyStore (device) returns DER X9.62 — wire-format differs sim vs device"
      - "3 open HUMAN-UAT scenarios (02-HUMAN-UAT.md) — NetworkConfig toggle, SE round-trip on device, refuse-launch on device"
  - phase: 03-otp-auth-role-shell-session-the-fixed-phase-1-goal
    items:
      - "Swift 6 concurrency warnings — LogField Hashable conformance + logger.info from nonisolated deinit (Info; not blocking in Swift 5 mode; pre-existing from Phase 1)"
      - "SHELL-01..04 absent from 03-11 SUMMARY requirements-completed frontmatter — explicitly SATISFIED in 03-VERIFICATION.md requirements table (frontmatter-hygiene only)"
      - "4 open HUMAN-UAT scenarios (03-VERIFICATION.md) — cold-boot biometric prompt, >5min-background re-prompt, biometric re-enrollment, SE authorizationKey ACL inspection on logout"
  - phase: 04-app-attest-physical-device-ci-hardening
    items:
      - "deferred-items.md #1: pre-existing MockURLProtocol fixture-registry leak — likely resolved (Phase 5 reports 367 / Phase 6 reports 391 simulator tests, 0 failures); treat as a closed test-infra item, confirm if it recurs"
      - "5 open HUMAN-UAT scenarios (04-HUMAN-UAT.md) — LimitedTrustBanner: iPhone landscape safe-area, gesture non-dismiss, iPad portrait/landscape/Split View, yellow-tone legibility"
  - phase: 05-kyc-capture-upload-pipeline
    items:
      - "05-REVIEW CR-01: AppContainer.kycTestSeed static never cleared after consumption (DEBUG-only, non-blocking; --fix recommended before the seam is extended)"
      - "05-REVIEW WR-01..05: DEBUG test-seam warnings (implicit fixture source, throwaway probe container, over-scoped biometric-lock suppression, simulator hard-fail vs XCTSkip, inconsistent seedMidUpload state)"
      - "CameraPermissionViewController exists but is never presented — denied camera permission shows inline copy instead of the blocking screen (product decision pending)"
      - "2 open HUMAN-UAT scenarios (05-VERIFICATION.md) — SC-4 background-upload completion under real OS suspension, Test-10 deliberate AVCaptureSessionRuntimeError injection"
  - phase: 06-close-gap-dev-04-app-attest-at-first-login-trusttier-consume
    items:
      - "06-REVIEW CR-01: retryRegister() re-issues POST /auth/otp/verify with an already-consumed OTP code — .registerFailed retry path is unreliable in production (pre-existing Phase 3 design, NOT introduced by Phase 6; advisory robustness fix)"
      - "Dead code: UITabBarController.wrapWithLimitedTrustBanner(trustTier:) (RoleCoordinator.swift:97-101) — the Phase 4 banner-attach API, zero call sites after Phase 6 replaced it with AppCoordinator-owned LimitedTrustBannerContainerViewController (maintenance hazard)"
      - "2 open HUMAN-UAT scenarios (06-VERIFICATION.md) — LimitedTrustBanner in-place re-render on mid-session trustTier mutation, Profile-entry KYC 'Continue' CTA pop/dismiss behavior"
nyquist:
  compliant_phases: ["3", "4", "5", "6"]
  partial_phases: ["1"]
  missing_phases: ["2"]
  overall: "gaps"
open_artifacts:
  verification_human_needed: 6
  uat_human_items_total: 24
  debug_sessions: 4
  todos: 1
---

# v1.0 "M1 Foundation" — Milestone Audit (Re-Audit)

**Audited:** 2026-05-18
**Status:** ⚡ `tech_debt`
**Project:** Validation Ledger — iOS Client

## Summary

This is a **re-audit**. The prior audit (2026-05-18T19:15:00Z) returned
`gaps_found` with 2 critical blockers — Phase 4 was never verified, and DEV-04
(App Attest at first login) was unwired. Phase 6 was inserted to close them.

**All prior gaps are closed.** Phase 6 (4/4 plans, `06-VERIFICATION.md` score
9/9) wired App Attest into the first-login `/device/register` path, captured the
discarded response into `AppSession.trustTier`, and produced the retroactive
`04-VERIFICATION.md`. The `gsd-integration-checker` re-trace confirms all four
prior findings (1 BLOCKER, 1 LOW, 2 WARNINGs) are genuinely closed in source.

The milestone now has **6/6 phases verified, 67/67 requirements satisfied, 8/8
E2E flows wired, 0 critical blockers, 0 unsatisfied requirements**.

It does **not** score `passed` because of a real tail of accumulated deferred
work — Nyquist validation gaps on Phases 1 and 2, ~24 physical-device HUMAN-UAT
scenarios that were never executed, and the normal "30% infrastructure tax"
debt residue. None of it blocks the close; all of it should be reviewed before
`/gsd-complete-milestone` records it as accepted debt.

| Score | Value | Note |
|-------|-------|------|
| Requirements | 67/67 satisfied | DEV-04 + CI-03 closed by Phase 6 |
| Phases verified | 6/6 | Phase 4 + Phase 6 VERIFICATION.md now present |
| Integration | 8/8 flows wired | all 4 prior findings closed |
| E2E flows | 8/8 complete | trustTier flow + Profile-CTA flow restored |
| Nyquist | gaps | P1 partial, P2 missing (discovery only) |

## What Phase 6 Closed (verified against source)

Phase 6 was the closure phase for the prior audit's gaps. Each is re-verified:

| Prior finding | Severity | Closure | Source evidence |
|---------------|----------|---------|-----------------|
| Phase 4 never verified — no `04-VERIFICATION.md` | BLOCKER | CLOSED | `04-VERIFICATION.md` exists (163 lines, `status: human_needed`, `score: 3/3`), produced by Phase 6 Plan 04 |
| DEV-04 — App Attest not invoked at first OTP login | BLOCKER | CLOSED | `OTPViewModel.verify()` STEP 5 (L249-401) fires `generateKeyIfNeeded → GET /device/challenge → attestKey → POST /device/register`; `grep 'attestationStatus: .unsupported'` → 0 |
| OTP-verify → `/device/register` → `AppSession.trustTier` (response discarded) | BLOCKER | CLOSED | `OTPViewModel.swift:286` `writeTrustTier(registerResponse.trustTier)`; `AppContainer.swift:421-422` seeds `AppSession` from Keychain |
| Profile → KYC status "Continue" CTA (dead button) | LOW (KYC-05) | CLOSED | `AppContainer.swift:189-198` wires `viewModel.onVerified` (pop/dismiss) |
| WARNING-1 — `kycStatus` Keychain never refreshed after `GET /kyc/status` | warning | CLOSED | `KYCStatusViewModel.swift:155-186` `refreshCachedKYCStatus()` writes `.kycStatus` after successful fetch |
| WARNING-2 — `LimitedTrustBanner` cannot re-render on heartbeat `trustTier` mutation | warning | CLOSED | `AppSession.swift:72-79` `didSet` posts `.trustTierDidChange`; `AppCoordinator.swift:151-167` observer → `bannerContainer.update(trustTier:)` |

## Requirements Coverage (3-source cross-reference)

`VERIFICATION.md` × `SUMMARY` frontmatter × `REQUIREMENTS.md` traceability, all
67 REQ-IDs:

| Phase | Reqs | Satisfied | Notes |
|-------|------|-----------|-------|
| 1 | 26 | 26 | `01-VERIFICATION.md`: all 26 satisfied. `status: human_needed` (8 UAT items). |
| 2 | 10 | 10 | `02-VERIFICATION.md`: all 10 satisfied. `status: human_needed` (3 UAT items). |
| 3 | 18 | 18 | `03-VERIFICATION.md`: all 18 satisfied. SHELL-01..04 absent from the 03-11 SUMMARY frontmatter but explicitly SATISFIED in the VERIFICATION requirements table — frontmatter-hygiene only. |
| 4 | 2 | 2 | DEV-04 + CI-03 — both `partial` in the prior audit, now SATISFIED. `04-VERIFICATION.md` score 3/3; corroborated by `06-VERIFICATION.md`. |
| 5 | 11 | 11 | `05-VERIFICATION.md`: all 11 satisfied, `gaps: []`. `status: human_needed` (2 UAT items). |
| 6 | (DEV-04, CI-03) | — | Closure phase; re-covers Phase 4's two REQ-IDs, adds none. `06-VERIFICATION.md` score 9/9. |

**67/67 satisfied. 0 partial. 0 unsatisfied. 0 orphaned** — every REQ-ID in the
traceability table is claimed by ≥1 plan and verified by its phase's
`VERIFICATION.md`. The FAIL gate does not fire.

**Documentation drift (not a gap):** the `REQUIREMENTS.md` traceability table is
stale — Phase 1/2/4 rows read "Pending", Phase 3 rows read "Validated (code)"
with 18 unchecked `[ ]` boxes — despite every phase being complete and verified.
The table has never been updated by any phase since 2026-04-20. The
`[x]` checkboxes on the requirement *text* lines are the authoritative status.
`/gsd-complete-milestone` rewrites this table at archive time regardless.

## Integration Findings (cross-phase wiring)

Spawned `gsd-integration-checker` for a full source re-trace. **8 of 8 E2E flows
wired end-to-end**, **67/67 requirements wired or self-contained**, **0 broken
flows, 0 unprotected routes, 0 missing connections.**

| Flow | Status |
|------|--------|
| A. App launch → `SessionRestoreProbe` → route to `.auth`/`.role`/`.kyc`/lock | WIRED |
| B. Phone entry → OTP verify → STEP 5 App Attest → trustTier captured → role shell | WIRED |
| C. OTP verify → `/device/register` → `AppSession.trustTier` → `LimitedTrustBanner` render/re-render | WIRED |
| D. Role shell → Profile tab → KYC status screen → "Continue" CTA | WIRED |
| E. KYC capture → `KYCUploader` chunked upload → resume after force-quit → KYC status | WIRED |
| F. Logout → Keychain + SE ACL teardown → return to phone entry | WIRED |
| G. 401 response → `Auth401ResponseInterceptor` → auto-logout | WIRED |
| H. Cold-boot with non-verified `kycStatus` → hard gate into KYC flow (fail-closed) | WIRED |

All 10 networking endpoints are consumed. `SensitiveActionService` has zero M1
call sites — intentional per AUTH-06.

**One non-blocking observation (WARNING — dead code):** the integration checker
found `UITabBarController.wrapWithLimitedTrustBanner(trustTier:)`
(`RoleCoordinator.swift:97-101`) — the Phase 4 banner-attach API — now has zero
call sites. Phase 6 replaced it with an `AppCoordinator`-owned
`LimitedTrustBannerContainerViewController`. The orphaned method compiles and is
semantically correct but is dead production code (maintenance hazard, no runtime
impact). Tracked in tech debt above.

**One non-blocking observation (INFO — accepted by design):** the 24h heartbeat
(`SceneDelegate.performHeartbeatIfNeeded:636`) mutates `AppSession.trustTier`
in-memory but does **not** persist it to Keychain — the Phase 6 D6-01 research
scoped the Keychain write to the first-login path only. On process restart
`AppContainer.init` reads the OTP-era Keychain value; the cold-boot heartbeat
then updates in-memory state and re-renders the banner via the `didSet`
observer. The brief OTP-era-tier window is accepted per D6-01's "over-show is
preferable to ever missing it" design.

## Nyquist Validation Coverage

| Phase | VALIDATION.md | nyquist_compliant | Status | Action |
|-------|---------------|-------------------|--------|--------|
| 1 | exists (`draft`) | false | PARTIAL | `/gsd-validate-phase 1` |
| 2 | **missing** | — | MISSING | `/gsd-validate-phase 2` |
| 3 | exists (`planned`) | true | COMPLIANT | stale status field only |
| 4 | exists (`approved`) | true | COMPLIANT | — |
| 5 | exists (`approved`) | true | COMPLIANT | — |
| 6 | exists (`validated`) | true | COMPLIANT | — |

Phase 1's `01-VALIDATION.md` is an unfilled planning-time draft
(`wave_0_complete: false`). Phase 2 has no `VALIDATION.md` at all. Nyquist
discovery is advisory — it does **not** force `gaps_found` — but both phases
should be run through `/gsd-validate-phase` before or shortly after milestone
close. Phase 6 added `06-VALIDATION.md` (`validated`) — the one improvement over
the prior audit's Nyquist picture.

## Open Artifacts

- **6 `VERIFICATION.md` `human_needed`** — all six phases. Every phase has a
  complete, code-verified report; each carries physical-device HUMAN-UAT items
  that are observation tasks, not code gaps.
- **~24 physical-device HUMAN-UAT scenarios** never executed — P1: 8, P2: 3,
  P3: 4, P4: 5, P5: 2, P6: 2 (P6 item 3 re-points to P4's 5 banner items). These
  are biometric prompts, Secure Enclave ACL inspection, banner orientation /
  iPad / ambient-light checks, CI PR-trigger and device-runner first runs, and
  `.ipa` extraction — all requiring real hardware or CI infrastructure.
- **4 debug sessions** — `front-camera-preview-black`, `kyc-flow-device-audit`,
  `kyc-force-quit-camera-stuck`, `kyc-status-screen-load-error`. Most are
  resolved/diagnosed but never formally closed.
- **1 todo** — `device-ci-biometric-infra` (Device CI Face-ID hang — a locked
  runner iPhone surfaces as a ~35-min "cancelled" timeout, not a code failure).

## Tech Debt

See the `tech_debt:` block in the frontmatter — aggregated from all six
`VERIFICATION.md` files, the Phase 4 `deferred-items.md`, and `05-REVIEW.md` /
`06-REVIEW.md`. None of it blocks the milestone; it is the budgeted "30%
infrastructure tax" residue plus DEBUG-only test-seam warnings and one piece of
dead code. Notable advisory items:

- **06-REVIEW CR-01** — `retryRegister()` re-issues `POST /auth/otp/verify` with
  an already-consumed OTP code, so the `.registerFailed` retry path is
  unreliable against a real backend. Pre-existing Phase 3 design, not introduced
  by Phase 6. Worth a robustness pass before M2 backend integration.
- **`wrapWithLimitedTrustBanner` dead code** — delete in a cleanup pass.
- **05-REVIEW CR-01** — `AppContainer.kycTestSeed` static is never cleared;
  `--fix` recommended before the test seam is extended.

## Recommendation

Status is **`tech_debt`** — all requirements met, all phases verified, all flows
wired, no critical blockers. The two blockers from the prior audit are closed
and re-verified. What remains is accumulated deferred work that needs an
explicit accept-or-address decision.

**A. Complete the milestone** — accept the debt; `/gsd-complete-milestone`
records it in `MILESTONES.md`. Defensible: M1 is a TestFlight-only internal
foundation milestone, the HUMAN-UAT items are observation tasks against verified
code, and the Nyquist gaps are advisory.

```
/gsd-complete-milestone v1.0
```

**B. Close the Nyquist gaps first** — they may close retroactively without a new
phase:

```
/gsd-validate-phase 1
/gsd-validate-phase 2
```

**C. Plan a cleanup phase** — only if you want the tech-debt tail addressed
before close (orphaned `wrapWithLimitedTrustBanner`, 06-REVIEW CR-01 retry path,
05-REVIEW CR-01 seam reset, the dead `CameraPermissionViewController` product
decision):

```
/gsd-phase --insert 7 "Address v1.0 tech debt: dead code, retry-path robustness, test-seam hygiene"
/gsd-discuss-phase 7
/gsd-plan-phase 7
/gsd-execute-phase 7
```

The ~24 physical-device HUMAN-UAT scenarios are a separate, parallel track —
they need real hardware + an active self-hosted runner, not a code phase.

Either way, `/gsd-complete-milestone` accepts this audit as the gate.

---
*Re-audit generated by `/gsd-audit-milestone` — 3-source requirements cross-reference + `gsd-integration-checker` cross-phase wiring re-trace. Supersedes the 2026-05-18T19:15:00Z `gaps_found` audit.*
