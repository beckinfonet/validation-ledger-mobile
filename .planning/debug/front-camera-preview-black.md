---
status: awaiting_human_verify
trigger: "Front-camera preview does NOT open in the KYC face-capture screen on a physical iPhone — preview area is solid black."
created: 2026-05-17T00:00:00Z
updated: 2026-05-17T17:05:00Z
---

## Current Focus

ROUND 5 — the black-preview bug is RESOLVED (round 4, commit 13a1b7c — camera renders
on-device, confirmed by the tester). On-device verification exposed TWO further bugs in
the KYC face-capture flow; both are tracked in this same session for coherence.

reasoning_checkpoint (BUG A — FaceCaptureViewController view is 320×480):
  hypothesis: "The app's `UIWindow` / scene is sized to the legacy 320×480 canvas because the app declares NO launch screen. The hand-written `validationLedger/App/Info.plist` (used because `INFOPLIST_FILE` is set + `GENERATE_INFOPLIST_FILE = NO` in project.pbxproj) contains a `UIApplicationSceneManifest` but NO `UILaunchScreen` key and no `UILaunchStoryboardName` — and there is no LaunchScreen.storyboard anywhere in the project. iOS treats an app with no launch screen as a legacy app and runs it in a compatibility canvas of 320×480 — the `UIWindowScene` never receives the modern full-device geometry, so `UIWindow(windowScene:)` (correct code) inherits a 320×480 bounds, and every VC pushed into it (incl. FaceCaptureViewController) is 320×480 on a 393×852 device."
  confirming_evidence:
    - "`validationLedger/App/Info.plist` — full file read: keys present are CFBundle*, LSRequiresIPhoneOS, UIApplicationSupportsMultipleScenes, UISupportedInterfaceOrientations(~ipad), UIApplicationSceneManifest, NSLocation/FaceID/Camera usage descriptions, BGTaskScheduler*, UIBackgroundModes, NSAppTransportSecurity. There is NO `UILaunchScreen` and NO `UILaunchStoryboardName`."
    - "`grep -rn -i 'launchscreen|UILaunchStoryboard|LaunchScreen' validationLedger.xcodeproj/project.pbxproj validationLedger/` and `find ... -name '*.storyboard'` both return ZERO results — the project ships no launch screen of any kind."
    - "`project.pbxproj`: app target has `GENERATE_INFOPLIST_FILE = NO` + `INFOPLIST_FILE = validationLedger/App/Info.plist` — so the hand-written plist is authoritative; Xcode does not inject a default `UILaunchScreen` dict."
    - "On-device `kyc_camera event=final_state` from 13a1b7c: `view.bounds={{0,0},{320,480}}` on a Dynamic-Island iPhone (~393×852). `view.window=true navController=true` — the VC IS correctly in a window inside a nav controller. SceneDelegate uses `UIWindow(windowScene:)` and `window.rootViewController = …` (both correct). The only thing that downsizes the whole window to 320×480 is the missing-launch-screen legacy compat mode."
  falsification_test: "Add a `UILaunchScreen` dict to Info.plist. If the next on-device run's `final_state` shows `view.bounds` ≈ {393,852} (full screen) and `previewView.bounds` height grows from ~154pt to a large value → confirmed. If `view.bounds` is still {320,480} → the launch screen is not the cause and the window/scene sizing must be probed elsewhere."
  fix_rationale: "An empty `UILaunchScreen` dict (`<key>UILaunchScreen</key><dict/>`) opts the app into the modern launch path. iOS then sizes the `UIWindowScene` to the real device geometry; `UIWindow(windowScene:)` inherits full-device bounds and every VC fills the screen on all iPhone + iPad sizes. This is a one-key Info.plist fix — it touches NO Swift code, so the auth/OTP screens (same `presentRoot` path) are unaffected except they ALSO become full-screen (which is the correct, desired behaviour they were silently missing)."
  blind_spots: "A unit test cannot exercise the OS launch-canvas behaviour (the test host bypasses it). Mitigation: add an Info.plist key-presence test (mirrors the existing CameraUsageDescriptionTests / BiometricServiceTests plist-assertion pattern) so a future edit cannot silently drop the key. The full-screen result itself still needs on-device confirmation via the `final_state` log."

reasoning_checkpoint (BUG B — auto-fire gate never receives camera frames):
  hypothesis: "The D-04 auto-fire never triggers because `VisionFaceQualityGate.process(...)` is never called — the `AVCaptureVideoDataOutput` + sample-buffer delegate that was supposed to feed it was never implemented. `CameraSession.configureSessionInputs` adds ONLY an `AVCapturePhotoOutput` (`outputs=1` confirmed on-device). With no video-data-output there is no `CMSampleBuffer` stream; `VisionFaceQualityGate.signals()` returns an `AsyncStream` whose continuation never `.yield`s; `FaceCaptureViewModel.observeGateSignals()` awaits a stream that emits nothing; `SteadyHoldTracker` never sees a `.pass`; `fireCapture()` is never called; the cue stays on the initial `.adjusting(nil)` 'Center your face'."
  confirming_evidence:
    - "Repo-wide grep: ZERO `AVCaptureVideoDataOutput` instantiations and ZERO `setSampleBufferDelegate` calls in `validationLedger/` source."
    - "`FaceQualityGate.swift` lines 17-18 / 189 / 200: comments SAY 'plan 05 feeds `process(sampleBuffer:)` from the AVFoundation delegate' / 'Called from the `AVCaptureVideoDataOutputSampleBufferDelegate` on device' — that delegate path was never written. `process` exists with signature `process(pixelBuffer:orientation:)` and has zero callers."
    - "On-device log: session committed with `outputs=1` — the lone `AVCapturePhotoOutput`."
    - "`FaceCaptureViewModel.observeGateSignals()` consumes `faceQualityGate.signals()`; the auto-fire chain `gate .pass → SteadyHoldTracker dwell → fireCapture() → performCapture() → .captured → presentPreview()` is sound — but it never starts because the stream never emits."
  falsification_test: "Wire an `AVCaptureVideoDataOutput` whose sample-buffer delegate calls `VisionFaceQualityGate.process(sampleBuffer:)`. If the on-device cue progresses past 'Center your face' (→ 'Hold still' → photo fires → Use/Retake preview) when a face is centered and held steady → confirmed. If it still never fires, the break is downstream (Vision orientation, stream wiring, or SteadyHold)."
  fix_rationale: "Add a `videoDataOutput: AVCaptureVideoDataOutput` to `AVFoundationCameraSession`, attached in `configureSessionInputs` alongside the photo output, with a dedicated serial delegate queue. Add a `videoFrameHandler` closure the VM sets to forward each `CMSampleBuffer` to the gate. The gate gets a new `process(sampleBuffer:)` entry point that extracts the `CVPixelBuffer` and computes the correct `CGImagePropertyOrientation` for the front camera in portrait (`.leftMirrored`) so Vision face detection actually works. This addresses the root cause — the missing frame source — not a symptom."
  blind_spots: "Live frames cannot run on the simulator (RESEARCH Pitfall 1) — the auto-fire end-to-end still needs on-device confirmation. The simulator path (`isCameraAvailable == false`) must stay a no-op: the video-data-output is added inside `configureSessionInputs`, which is only reached after the `isCameraAvailable` guard. Front-camera portrait orientation for Vision is `.leftMirrored` — if wrong, face detection silently fails; this is the highest-risk detail and is locked with a unit test on the orientation helper."

OLD reasoning_checkpoint (black-preview, round 4 — RESOLVED):
  hypothesis: "ROUND 4. The round-3 layout fix DID resolve the Auto Layout constraint conflict (device console for 93386af no longer prints `Unable to simultaneously satisfy constraints`). But the preview is STILL black. Two hypotheses remain and MUST be disambiguated by a post-settle state snapshot, not guessed: (H-layout) the VC's view / `previewContainer` still never reaches a real non-zero size on-device — the round-3 hugging-priority fix is simulator-verified but the simulator test FORCES a known host size, so it cannot catch a real presentation/sizing failure; the device log only ever shows ONE `viewDidLayoutSubviews` pass, at {0,0}, fired BEFORE the async session start — we never see whether a later pass occurs at a real size. (H-camera) the layout is fine post-startup but the camera produces no frames. The structural root cause regardless of which is true: the preview is hosted by manually adding an `AVCaptureVideoPreviewLayer` as a sublayer of a plain `UIView` and syncing `.frame` in `viewDidLayoutSubviews` — a fragile pattern with an inherent 0×0 race window. The idiomatic fix (`layerClass` override) makes the preview layer the view's BACKING layer so it ALWAYS equals the view bounds with zero manual sync and zero race."
  confirming_evidence:
    - "Device console for 93386af: the round-2 `Unable to simultaneously satisfy constraints` line is GONE — round 3's hugging-priority layout fix resolved the constraint conflict. The chrome (nav bar, instruction + cue labels) all render."
    - "Device console for 93386af: `face_vc.viewDidLayoutSubviews previewContainer.bounds={{0,0},{0,0}} previewLayer.frame={{0,0},{0,0}} inHierarchy=true connectionExists=false` — but this is logged exactly ONCE, BEFORE `start_authorized_session.begin`. The first layout pass naturally precedes the async session start; whether a LATER pass fires at a real size is UNKNOWN from this capture. THIS is the core ambiguity round 4 must remove."
    - "Device console: camera session reports healthy — `permission_resolved status=rawValue 3` (.authorized), `device_discovered name=Front Camera position=2`, `input_added inputs=1`, `configure_inputs.committed inputs=1 outputs=1`, `session_start.after_startRunning isRunning=true`."
    - "`FigCaptureSourceRemote err=-17281` fires twice during `configureSessionInputs`. WebSearch (Apple Developer Forums threads 808736 / 810894 / 808479) confirms this is BENIGN internal AVFoundation/CoreMedia log noise on iOS 17+/26 — frames are still delivered, the camera still works; it is not caused by app code and does not need a configuration change. It is NOT the black-preview cause."
  falsification_test: "The new one-shot `kyc_camera event=final_state` log (emitted ~2s after startAuthorizedSession completes, on the main actor) is the disambiguator. If it shows `previewContainer.bounds` / `previewLayer.frame` height ~0 → H-layout confirmed (layout still broken). If it shows a real non-zero size AND `connectionActive=true` AND `session.isRunning=true` but the preview is still black → H-camera confirmed. If the `layerClass` fix lands and the snapshot shows a real non-zero `view.bounds`/preview layer frame and the preview renders → fixed."
  fix_rationale: "(1) Replace the manual-sublayer pattern with a `CameraPreviewView: UIView` whose `override class var layerClass: AnyClass { AVCaptureVideoPreviewLayer.self }` — the preview layer IS the view's backing layer, so it always matches `view.bounds` with zero manual frame-sync and zero 0×0 race. The session is wired into that backing layer. Applied to BOTH face + vehicle capture VCs; the oval guide moves to a sibling overlay view. This permanently eliminates the manual-frame-sync failure mode whichever hypothesis is true. (2) Add the definitive `final_state` snapshot log to remove the layout-vs-camera ambiguity for the next device run. (3) `err=-17281` needs NO fix — confirmed benign. (4) Coordinator path confirmed sound: `.kyc` phase installs the `KYCCoordinator`'s `UINavigationController` directly as `window.rootViewController`; the face VC is a full-screen pushed VC — it DOES get real-size layout passes."
  blind_spots: "Live camera frames cannot be exercised on the simulator (RESEARCH Pitfall 1) — the final on-device run is still required. The `layerClass` fix removes the manual-sync race but if H-layout is the real cause (the VC's view itself never reaches a real size) the `layerClass` view inherits that zero size too — which is exactly why the `final_state` snapshot also captures `view.bounds`, `view.window != nil`, and `navigationController != nil` so a residual sizing failure is unambiguous. If the snapshot shows everything sized + connected + running and the preview is still black, the next round must look at the capture-device format / `activeFormat` negotiation, not layout."

## Symptoms

expected: "Front-camera live preview renders in the KYC face-capture screen on a physical iPhone after granting camera permission."
actual: "Face-capture screen chrome renders correctly but the preview area is solid black — no live front-camera feed."
errors: "None reported — no crash, no error state. Silent black preview."
reproduction: "Fresh install, DEBUG container (event=debug), route to kyc.carrier, tap Get started, grant the camera permission prompt, reach the Verify identity screen."
started: "Phase 05 device verification — first time the live camera path ran on a physical device."

## Eliminated

- hypothesis: "[DISPROVEN ROOT CAUSE — round 2] A 3:4 aspect-ratio constraint on `previewContainer` gives the host view a definite non-zero height (commit 873becd)."
  evidence: "Round-3 on-device console (commit 873becd) prints `Unable to simultaneously satisfy constraints` and `Will attempt to recover by breaking constraint <NSLayoutConstraint kyc-face-preview.height == 1.33333*kyc-face-preview.width>` — that IS the round-2 aspect constraint. The host `UIStackView` is pinned to the safe area on BOTH ends (top +32 / bottom −32) so the stack height is fully determined; a rigid child aspect ratio inside a `.fill` stack of intrinsic-height siblings is over-constrained. UIKit force-breaks the aspect constraint and the plain UIView collapses to 0 again — `previewContainer.bounds={{0,0},{0,0}}` at viewDidLayoutSubviews. Round 2 swapped 'zero because no constraint' for 'zero because the constraint gets force-broken' — net effect identical. The aspect-constraint approach is itself the round-3 root cause."
  timestamp: 2026-05-17T18:00:00Z


- hypothesis: "DEBUG build injects a mock/stub camera instead of the real AVFoundationCameraSession."
  evidence: "KYCCoordinator.pushFaceCapture() constructs AVFoundationCameraSession() directly (line 252). No #if DEBUG branch, no mock. The real session is wired."
  timestamp: 2026-05-17T00:00:00Z
- hypothesis: "VC failed to load / lay out."
  evidence: "Tester reports all chrome (title, back, sign-out, instruction, cue) renders correctly — VC loaded and laid out fine."
  timestamp: 2026-05-17T00:00:00Z
- hypothesis: "[DISPROVEN ROOT CAUSE — round 1] AVFoundationCameraSession starts the AVCaptureSession before camera authorization is resolved (start-before-grant race)."
  evidence: "Commit b2983a9 added startAuthorizedSession() — awaits requestPermission() and only then configures + starts the session — plus an off-main serial queue. The tester re-tested on the physical iPhone and the preview is STILL solid black. The tester's device already had camera permission granted, so startAuthorizedSession's requestPermission() returns .authorized immediately and proceeds to configure + start — and it is still black. The defect is therefore NOT permission timing. b2983a9 is kept (permission-gating + off-main session work are still correct) but it is NOT the root cause."
  timestamp: 2026-05-17T12:00:00Z
- hypothesis: "[round 1 — INCORRECTLY eliminated] Preview layer has a zero frame / wrong frame."
  evidence: "Round 1 eliminated this by noting viewDidLayoutSubviews sets previewLayer.frame = previewContainer.bounds. That elimination was WRONG — it is circular. Copying previewContainer.bounds into previewLayer.frame does nothing if previewContainer.bounds is itself zero-height. Round 2 found previewContainer IS zero-height (plain UIView, no height constraint, no intrinsic size, in a .fill vertical stack) — this IS the root cause. Re-opened and confirmed."
  timestamp: 2026-05-17T12:00:00Z
- hypothesis: "Preview layer's session ≠ the session that gets startRunning()."
  evidence: "CameraSession.swift:112 — a single `private let session = AVCaptureSession()`. `_previewLayer` (line 114-118, lazy) is built as `AVCaptureVideoPreviewLayer(session: session)` against that same instance. start()/stop() (lines 173-185) call startRunning()/stopRunning() on the same `session`. VM.previewLayer and VC.previewLayer both resolve to the one `_previewLayer`. Session identity is correct — not the bug."
  timestamp: 2026-05-17T12:00:00Z
- hypothesis: "Front-camera device discovery returns nil / NSCameraUsageDescription missing."
  evidence: "captureDevice(for: .front) uses AVCaptureDevice.DiscoverySession([.builtInWideAngleCamera], .video, .front) — valid on every iPhone. Info.plist (validationLedger/App/Info.plist:81) contains NSCameraUsageDescription with a non-empty string (CameraUsageDescriptionTests asserts this). If discovery failed it would throw .cameraUnavailable and the VM would show the 'camera isn't available' failure copy — the tester sees the normal chrome, not that error. Not the bug."
  timestamp: 2026-05-17T12:00:00Z

## Evidence

- timestamp: 2026-05-17T18:00:00Z
  checked: "Round-3 on-device run of commit 873becd — the `kyc_camera` log block + the Auto Layout console output, captured verbatim by the tester."
  found: "(1) The camera SESSION is healthy: `permission_resolved status=rawValue 3` (.authorized), `device_discovered name=Front Camera`, `input_added inputs=1`, `configure_inputs.committed inputs=1 outputs=1`, `session_start.after_startRunning isRunning=true`. (2) The console prints `Unable to simultaneously satisfy constraints` and `Will attempt to recover by breaking constraint <NSLayoutConstraint kyc-face-preview.height == 1.33333*kyc-face-preview.width>` — the round-2 aspect constraint. (3) `face_vc.viewDidLayoutSubviews previewContainer.bounds={{0,0},{0,0}} previewLayer.frame={{0,0},{0,0}}` — the host view is 0×0 at the final layout callback."
  implication: "The black preview is a PURE Auto Layout failure, not a camera-session failure. The round-2 fix (873becd) is the proximate cause: its rigid aspect constraint is unsatisfiable inside a both-ends-pinned `.fill` stack, UIKit force-breaks it, and the plain UIView collapses to 0 height again. `FigCaptureSourceRemote err=-17281` is secondary noise (session still reaches isRunning=true)."
- timestamp: 2026-05-17T18:00:00Z
  checked: "The fix — removed the aspect constraint, set the preview's vertical content-hugging + compression-resistance to priority 1 while the labels hug at `.required`; added a priority-250 `>= 240` min-height floor. Built for the iPhone 16e simulator and ran the new regression suite."
  found: "TEST BUILD SUCCEEDED. `KYCCapturePreviewLayoutTests` — all 4 tests pass: the face + vehicle preview hosts resolve to a non-zero height (> 240pt at full device size) at BOTH 320×480 (the encapsulated-layout size from the device log) and 393×852. The simulator console is clean of `Unable to simultaneously satisfy constraints` for the capture screens. Full `validationLedgerTests` run: the 40 pre-existing networking/upload-pipeline failures (`KYCUploader*`, `APIClient`, `KYCStatusViewModel`, `KYCReviewViewModel` — all HTTP-stub 404s) are unchanged from the round-2 baseline; the 4 new layout tests all pass and no capture-screen test regressed."
  implication: "The layout fix is simulator-verified. On-device re-verification of actual camera frames is still required (Pitfall 1) — the tester must confirm the `kyc_camera viewDidLayoutSubviews previewContainer.bounds` now shows a non-zero height and the preview shows the live feed."

- timestamp: 2026-05-17T21:00:00Z
  checked: "Round-4. The on-device console for commit 93386af (post round-3 fix), and a WebSearch on the `FigCaptureSourceRemote` / `FigXPCUtilities err=-17281` console lines."
  found: "(1) The round-3 layout fix WORKED at the constraint level: the `Unable to simultaneously satisfy constraints` line is GONE from the 93386af console — the constraint conflict is resolved. The chrome all renders. The preview is STILL black. (2) `face_vc.viewDidLayoutSubviews` is logged exactly ONCE, at `previewContainer.bounds={0,0}`, fired BEFORE `start_authorized_session.begin` — i.e. the FIRST layout pass, which naturally precedes the async session start. Whether a LATER pass fired at a real size is UNKNOWN from this capture. (3) WebSearch (Apple Developer Forums threads 808736 / 810894 / 808479): `err=-17281` is BENIGN internal AVFoundation/CoreMedia log noise on iOS 17+/26 — frames are still delivered, the camera still works, it is not caused by app code, and needs NO configuration change. It is not the black-preview cause."
  implication: "Two hypotheses remained and could not be guessed between: H-layout (the VC view / preview host still never reaches a real size on-device — the simulator regression tests FORCE a known host size so they cannot catch a presentation failure) and H-camera (layout fine, no frames). The round-4 work: (a) a definitive `final_state` post-settle snapshot log to disambiguate, and (b) a structural `layerClass` preview-view fix that removes the manual-frame-sync 0×0 race entirely regardless of which hypothesis is true."
- timestamp: 2026-05-17T21:00:00Z
  checked: "Round-4 structural fix — replaced the manual add-`AVCaptureVideoPreviewLayer`-as-sublayer + sync-`.frame`-in-`viewDidLayoutSubviews` pattern with a `CameraPreviewView: UIView` whose `layerClass` override makes its BACKING layer the `AVCaptureVideoPreviewLayer`. Wired the session into it in both Face + Vehicle capture VCs; the oval guide moved to a sibling sublayer. Added the `final_state` snapshot log. Built + tested for iPhone 16e."
  found: "BUILD SUCCEEDED for iPhone 16e. `KYCCapturePreviewLayoutTests` — all 7 tests pass: the 4 round-3 non-zero-height tests still pass with the new `CameraPreviewView` host, plus 3 new round-4 tests — the backing layer IS an `AVCaptureVideoPreviewLayer`, and the preview layer bounds exactly track the host bounds with zero manual sync at full device size. The coordinator path was confirmed sound: `SceneDelegate.presentRoot(.kyc)` installs the `KYCCoordinator`'s `UINavigationController` directly as `window.rootViewController`; `pushFaceCapture()` pushes a full-screen VC — it DOES get real-size layout passes. No sizing/containment bug found there."
  implication: "The `layerClass` structural fix permanently eliminates the manual-frame-sync 0×0 race: a view's backing layer is always exactly `bounds`-sized by UIKit, so the preview layer can never be a different size than the view. On-device re-verification by the tester is still required (Pitfall 1) — the new `kyc_camera event=final_state` line is the disambiguator: if it shows a real non-zero `view.bounds`/`previewView.bounds` and the preview renders, fixed; if `previewView.bounds` height ≈ 0, H-layout (the VC view itself never sizes); if everything sized + `connectionActive`/`sessionRunning` true but still black, H-camera (frame delivery)."

- timestamp: 2026-05-17T00:00:00Z
  checked: "FaceCaptureViewModel.start() and the CameraSession protocol surface."
  found: "start() calls configure(.front) + start() with no requestPermission() call; the protocol HAS requestPermission() but it has zero callers in the capture path."
  implication: "[round 1] Thought to be the root cause — disproven in round 2: b2983a9 fixed this and the preview is still black."
- timestamp: 2026-05-17T12:00:00Z
  checked: "Round-2 re-test result reported by the tester after b2983a9 shipped."
  found: "Preview STILL solid black on the physical iPhone. The device already had camera permission granted, so startAuthorizedSession's requestPermission() returns .authorized immediately and still proceeds to a black preview."
  implication: "Permission timing is DISPROVEN as the (sole) root cause. The defect is downstream of permission — in the preview-rendering wiring or layout."
- timestamp: 2026-05-17T12:00:00Z
  checked: "Session instance identity — does the preview layer's session == the session that gets startRunning()."
  found: "CameraSession.swift:112 — one `private let session`. `_previewLayer` (lines 114-118) = `AVCaptureVideoPreviewLayer(session: session)` — same instance. start()/stop() act on the same `session`. VM.previewLayer + VC.previewLayer both resolve to the single `_previewLayer`."
  implication: "Session identity is correct. Ruled out."
- timestamp: 2026-05-17T12:00:00Z
  checked: "FaceCaptureViewController layout — how `previewContainer` is sized."
  found: "previewContainer is a plain UIView (line 36-42, background .black, no intrinsic content size). It is the middle arranged subview of a vertical UIStackView (line 107). `stack.distribution` is NEVER set → defaults to `.fill`. The stack is pinned to ALL FOUR safe-area edges (lines 114-131, top constant +xl, bottom constant -xl). There is NO height constraint, NO aspect constraint anywhere on previewContainer."
  implication: "ROOT CAUSE. In a `.fill` vertical stack the container must take some axis height; with no intrinsic size and no constraint, and the labels having real intrinsic heights, Auto Layout has nothing forcing the container tall — it collapses to height 0. previewLayer.frame := previewContainer.bounds is then a zero-height rect → no preview renders → solid black."
- timestamp: 2026-05-17T12:00:00Z
  checked: "VehicleCaptureViewController layout (DL-back/truck/trailer/plate)."
  found: "Identical defect — previewContainer plain UIView, vertical `.fill` stack pinned top+bottom, only shutterButton gets a height constraint (>= 44), previewContainer gets none (lines 96-124)."
  implication: "Same root cause across all 5 KYC capture screens; the same fix applies to both VCs."

## Resolution

### ROUND 5 (current) — two new bugs from on-device verification

bug_A_root_cause: "Bug A — `FaceCaptureViewController.view.bounds` is `{320,480}` on a ~393×852 device. ROOT CAUSE: the app declares NO launch screen. `validationLedger/App/Info.plist` (authoritative — `project.pbxproj` sets `GENERATE_INFOPLIST_FILE = NO` + `INFOPLIST_FILE = validationLedger/App/Info.plist`) contains a `UIApplicationSceneManifest` but NO `UILaunchScreen` key, and `grep`/`find` confirm there is no `LaunchScreen.storyboard` anywhere in the project. Without a launch screen iOS runs the app in a LEGACY compatibility canvas of 320×480 — the `UIWindowScene` never receives the modern full-device geometry. SceneDelegate's window code is CORRECT (`UIWindow(windowScene:)`, `window.rootViewController = …`), so the window faithfully inherits the 320×480 legacy canvas and every VC pushed into the KYC nav (incl. `FaceCaptureViewController`) is 320×480. The on-device `final_state` line from 13a1b7c proves it: `view.bounds={{0,0},{320,480}}` with `view.window=true navController=true`."
bug_A_fix: "Added `<key>UILaunchScreen</key><dict/>` to `validationLedger/App/Info.plist` (file:23). An empty `UILaunchScreen` dict opts the app into the modern launch path; iOS then sizes the `UIWindowScene` to the real device geometry and `UIWindow(windowScene:)` fills the screen on all iPhone + iPad sizes. ONE Info.plist key — no Swift change — so the auth/OTP screens (same `presentRoot` path) are unaffected EXCEPT they too become correctly full-screen (a latent bug they were silently carrying). Locked by NEW `AppLaunchConfigurationTests.infoPlistHasLaunchScreen` (mirrors `CameraUsageDescriptionTests` plist-assertion pattern) so the key cannot be silently dropped."
bug_B_root_cause: "Bug B — the D-04 face-capture auto-fire never fires; the cue is stuck on 'Center your face'. ROOT CAUSE: the video-frame pipeline that feeds the Vision `FaceQualityGate` was never implemented. `AVFoundationCameraSession.configureSessionInputs` added ONLY an `AVCapturePhotoOutput` (on-device log: `outputs=1`). There was ZERO `AVCaptureVideoDataOutput` and ZERO `setSampleBufferDelegate` anywhere in `validationLedger/`. `FaceQualityGate.swift` only had COMMENTS describing the intended delegate path. So `VisionFaceQualityGate.process(...)` had zero callers → its `signals()` `AsyncStream` continuation never `.yield`ed → `FaceCaptureViewModel.observeGateSignals()` awaited a stream that emitted nothing → `SteadyHoldTracker` never saw a `.pass` → `fireCapture()` never ran. The auto-fire chain downstream of the stream (`.pass → dwell → fireCapture → performCapture → .captured → presentPreview`) was sound — it just had no input."
bug_B_fix: "Wired the missing video-frame pipeline. (1) `CameraSession.swift`: added an `AVCaptureVideoDataOutput` attached in `configureSessionInputs` alongside the photo output, with a dedicated SERIAL delegate queue `videoFrameQueue` (separate from `sessionQueue` so frame delivery never contends with `startRunning()`). `AVFoundationCameraSession` now conforms to `AVCaptureVideoDataOutputSampleBufferDelegate`; its `nonisolated captureOutput(_:didOutput:from:)` forwards each `CMSampleBuffer` to a new `videoFrameHandler: (@Sendable (CMSampleBuffer) -> Void)?` protocol property (NSLock-guarded for the main-actor-set / frame-queue-read cross-thread access). `alwaysDiscardsLateVideoFrames = true`. (2) `FaceQualityGate.swift`: added `process(sampleBuffer:cameraPosition:)` to the `FaceQualityGate` protocol — extracts the `CVPixelBuffer` and resolves the Vision orientation via a NEW pure `VisionImageOrientation.portrait(for:)` helper (`.front → .leftMirrored`, `.back → .right` — front-camera portrait MUST be `.leftMirrored` or Vision silently finds no faces). (3) `FaceCaptureViewModel.observeGateSignals()`: sets `cameraSession.videoFrameHandler` to call `gate.process(sampleBuffer:cameraPosition:.front)` — AFTER `signals()` installs the continuation so the first frame always has a live stream; `stop()` clears the handler. Diagnostics: `kyc_camera event=video_data_output_wired` (session config) + one-shot `kyc_camera event=face_gate.first_frame pixelBuffer=WxH visionOrientation=N` (first frame reaching the gate). Simulator path unchanged — the video-data-output is added inside `configureSessionInputs`, only reached past the `isCameraAvailable` guard."
bug_B_verification: "BUILD SUCCEEDED for iPhone 16e. All capture/KYC unit suites green: `FaceQualityGateTests` (incl. 3 NEW Vision-orientation tests + the existing gate/steady-hold tests), `CameraSessionAvailabilityTests` (incl. NEW `videoFrameHandler` seam test), all 7 `KYCCapturePreviewLayoutTests`, `AppLaunchConfigurationTests` (NEW), `CameraUsageDescriptionTests`. The `validationLedgerTests` run reports 40-42 issues run-to-run — ALL are the documented pre-existing `MockURLProtocol` parallel-run networking/upload flakes (05-04-SUMMARY Deviation 2: `APIClient`/`KYCUploader`/`KYCReviewViewModel`/`KYCStatusViewModel`/endpoint decoders) — verified by listing every failing test: ZERO touch camera/face/capture/launch. My changes added zero new failures. Live camera frames + the auto-fire end-to-end cannot run on the simulator (RESEARCH Pitfall 1) — on-device re-verification by the tester is required."

### ROUND 3-4 — black-preview bug (RESOLVED, commit 13a1b7c — camera renders on-device)

root_cause: "ROUND 3 (decisive on-device evidence). The KYC capture preview is solid black because `previewContainer` (the host view for the AVCaptureVideoPreviewLayer) collapses to ZERO height — confirmed: the device log shows `previewContainer.bounds={{0,0},{0,0}}` at viewDidLayoutSubviews. The proximate cause is the ROUND-2 fix itself (commit 873becd): the rigid 3:4 aspect-ratio constraint (`previewContainer.heightAnchor == widthAnchor * 4/3`, layout name `kyc-face-preview`) is UNSATISFIABLE. The host `UIStackView` is pinned to the safe area on BOTH the top (+32) and the bottom (−32), so the stack's height is fully determined; its other arranged subviews (instruction + cue labels, and the vehicle shutter button) have intrinsic heights, leaving a fixed amount of vertical slack. The 4:3 aspect ratio demands more height than that slack, so the layout is over-constrained — UIKit resolves it by FORCE-BREAKING the aspect constraint (`Will attempt to recover by breaking constraint kyc-face-preview.height == 1.33333*kyc-face-preview.width`). With the aspect constraint broken, `previewContainer` — a plain UIView with no intrinsic content size and no other height constraint — is under-determined again and collapses to height 0. `previewLayer.frame = previewContainer.bounds` then copies a 0×0 rect → the AVCaptureVideoPreviewLayer renders nothing → only the container's `.black` background shows. Round 2 swapped 'zero because no constraint' for 'zero because the constraint gets force-broken' — identical net effect. The camera session is healthy and is NOT the cause: the device log shows authorization resolved (.authorized), front camera discovered, 1 input + 1 output committed, and `isRunning=true`. The permission/session wiring from b2983a9 is correct and is kept."
fix: "ROUND 4 (structural). Two layers of fix this round: (1) STRUCTURAL PREVIEW HOST — replaced the fragile manual pattern (add an `AVCaptureVideoPreviewLayer` as a sublayer of a plain `UIView`, then re-sync `previewLayer.frame = previewContainer.bounds` on every `viewDidLayoutSubviews`) with the idiomatic `layerClass` pattern. NEW file `CameraPreviewView.swift`: a `UIView` subclass with `override class var layerClass: AnyClass { AVCaptureVideoPreviewLayer.self }` — its BACKING layer IS the preview layer, which UIKit keeps EXACTLY `bounds`-sized at all times with zero manual work. There is no `.frame` to sync and no window where the layer is sized differently from the view — the 0×0 manual-sync race is permanently impossible. `FaceCaptureViewController` + `VehicleCaptureViewController` (and via subclass `DLBackCaptureViewController`) now host the preview in a `CameraPreviewView`; the session is wired into `previewView.session`; the oval guide is a sibling sublayer of the preview view's layer. The round-3 hugging-priority `.fill`-stack layout is RETAINED (it correctly resolved the constraint conflict). (2) DEFINITIVE DIAGNOSTIC — a one-shot `kyc_camera event=final_state` log, scheduled ~2s after `startAuthorizedSession` is kicked (on the main actor), emits ONE post-settle line capturing `view.bounds`, `view.window`, `navController`, `previewView.bounds`, `previewLayer.bounds`, `previewLayer.superlayer`, `sessionWired`, `sessionRunning`, and the connection's exists/active/enabled — removing the round-3 ambiguity of whether the tester's console capture caught the final layout pass. `FigCaptureSourceRemote err=-17281` needs NO fix — WebSearch (Apple Developer Forums 808736/810894/808479) confirms it is benign internal AVFoundation log noise; frames are still delivered. The coordinator path was audited and is sound (`.kyc` phase installs the KYC nav directly as `window.rootViewController`; the face VC is a full-screen pushed VC). b2983a9 (permission gating) + 93386af (the round-3 layout fix) are both kept untouched."
verification: "BUILD SUCCEEDED for the iPhone 16e simulator. Regression suite `KYCCapturePreviewLayoutTests` — all 7 tests pass: the 4 round-3 non-zero-height tests still pass with the new `CameraPreviewView` host (face + vehicle, at 320×480 and 393×852, > 240pt), plus 3 NEW round-4 structural tests — `CameraPreviewView.layerClass` is `AVCaptureVideoPreviewLayer`, the backing layer IS the preview layer, and `previewLayer.bounds` exactly tracks the host `bounds` with zero manual sync at full device size. Live camera frames cannot run on the simulator (RESEARCH Pitfall 1) — on-device re-verification by the tester is still required. The next device run is now CONCLUSIVE via the single `kyc_camera event=final_state` line: a real non-zero `previewView.bounds` + the preview rendering = fixed; `previewView.bounds` height ≈ 0 = H-layout (VC view itself never sizes); everything sized + `connectionActive`/`sessionRunning` true but still black = H-camera (frame delivery — next round looks at capture-device format negotiation, not layout)."
files_changed:
  - "validationLedger/Features/Onboarding/KYC/Capture/CameraPreviewView.swift — NEW. A `UIView` subclass whose `layerClass` override makes the backing layer an `AVCaptureVideoPreviewLayer`; exposes `session` / `videoGravity` / `previewLayer`. Eliminates the manual-frame-sync 0×0 race permanently."
  - "validationLedger/Features/Onboarding/KYC/Capture/FaceCaptureViewController.swift — host the preview in a `CameraPreviewView` (was: add layer as sublayer of a plain UIView + sync .frame in viewDidLayoutSubviews); oval guide moved to a sibling sublayer; added the one-shot `kyc_camera event=final_state` post-settle snapshot log. Round-3 hugging-priority layout retained."
  - "validationLedger/Features/Onboarding/KYC/Capture/VehicleCaptureViewController.swift — same `CameraPreviewView` host + `final_state` snapshot; applies to DLBackCaptureViewController via subclass."
  - "validationLedger/Features/Onboarding/KYC/Capture/FaceCaptureViewModel.swift — added `captureSession: AVCaptureSession?` accessor for the VC to wire into the `CameraPreviewView` backing layer."
  - "validationLedger/Features/Onboarding/KYC/Capture/VehicleCaptureViewModel.swift — same `captureSession` accessor."
  - "validationLedgerTests/KYC/KYCCapturePreviewLayoutTests.swift — updated to use `CameraPreviewView`; added 3 round-4 tests asserting the backing layer IS the preview layer and tracks bounds with zero manual sync (7 tests total, all pass)."
status_note: "Round-4: applied the structural `layerClass` preview-view fix (manual-frame-sync 0×0 race permanently removed) + a definitive `kyc_camera event=final_state` post-settle snapshot log to disambiguate H-layout vs H-camera. `err=-17281` confirmed benign. Simulator build + 7 regression tests green. On-device re-verification by the tester pending — the `final_state` line makes the next device run conclusive."

round_5_files_changed:
  - "validationLedger/App/Info.plist — Bug A. Added `<key>UILaunchScreen</key><dict/>` so iOS sizes the window to the real device geometry instead of the legacy 320×480 compatibility canvas."
  - "validationLedger/Core/Identity/Capture/CameraSession.swift — Bug B. Added an `AVCaptureVideoDataOutput` (attached in `configureSessionInputs`) + serial `videoFrameQueue`; `AVFoundationCameraSession` now conforms to `AVCaptureVideoDataOutputSampleBufferDelegate`; added the `videoFrameHandler` protocol property + `nonisolated captureOutput(_:didOutput:from:)` that forwards each `CMSampleBuffer`. Added `event=video_data_output_wired` log."
  - "validationLedger/Core/Identity/Capture/FaceQualityGate.swift — Bug B. Added `process(sampleBuffer:cameraPosition:)` to the `FaceQualityGate` protocol + `VisionFaceQualityGate` impl; added the pure testable `VisionImageOrientation.portrait(for:)` helper (front → `.leftMirrored`); added the one-shot `event=face_gate.first_frame` log."
  - "validationLedger/Features/Onboarding/KYC/Capture/FaceCaptureViewModel.swift — Bug B. `observeGateSignals()` now wires `cameraSession.videoFrameHandler` to feed `gate.process(sampleBuffer:)`; `stop()` clears the handler."
  - "validationLedgerTests/KYC/FaceQualityGateTests.swift — Bug B. Added 3 Vision-orientation tests + a `videoFrameHandler` protocol-seam regression test."
  - "validationLedgerTests/KYC/AppLaunchConfigurationTests.swift — NEW. Bug A. Asserts Info.plist declares `UILaunchScreen` so the legacy 320×480 canvas cannot regress."
