---
phase: 08-role-filtered-load-list
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - validationLedger/UI/Components/VerificationBadgeView.swift
  - validationLedger/UI/Components/LoadStatusBadgeView.swift
  - validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift
autonomous: true
requirements:
  - TRUST-02
  - LOAD-04
threat_refs:
  - T-08-05
  - T-08-06

must_haves:
  truths:
    - "A VerificationBadgeView renders 4 distinct visuals (verified/pending/unverified/flagged) per the UI-SPEC locked color ramp"
    - "VerificationBadgeView's nil-counterparty entry point (configure(stateOrNil: nil)) renders UNVERIFIED visuals AND the accessibilityLabel does NOT contain 'verified' (fail-closed D-03)"
    - "LoadStatusBadgeView renders all 13 LoadStatus cases with the UI-SPEC 3-tone informational ramp; never reuses the verification ramp"
    - "Both badges scale correctly with Dynamic Type — the pill corner radius recomputes in layoutSubviews"
    - "Both badges consume DS.* tokens only (no raw color/font/spacing literals except the documented exceptions: bounds.height/2 corner radius, .systemYellow on pending, .tertiarySystemFill/.systemGray5/.systemGray6 on neutrals)"
  artifacts:
    - path: "validationLedger/UI/Components/VerificationBadgeView.swift"
      provides: "TRUST-02 — single reusable 4-state verification pill"
      contains: "public final class VerificationBadgeView: UIView"
    - path: "validationLedger/UI/Components/LoadStatusBadgeView.swift"
      provides: "13-state status pill — informational ramp, NOT a security primitive"
      contains: "public final class LoadStatusBadgeView: UIView"
    - path: "validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift"
      provides: "4-state snapshot baselines + the fail-closed nil a11y-label assertion"
      contains: "VerificationBadgeViewSnapshotTests"
    - path: "validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift"
      provides: "13-case (or 3-tone representative) label + tone assertions"
      contains: "LoadStatusBadgeViewSnapshotTests"
  key_links:
    - from: "validationLedger/UI/Components/VerificationBadgeView.swift"
      to: "validationLedger/Core/Load/VerificationState.swift"
      via: "configure(state: VerificationState) entry point"
      pattern: "configure\\(state: VerificationState\\)"
    - from: "validationLedger/UI/Components/VerificationBadgeView.swift"
      to: "validationLedger/UI/DesignSystem/Colors.swift"
      via: "DS.Colors.primary / DS.Colors.destructive tokens"
      pattern: "DS\\.Colors\\.(primary|destructive)"
    - from: "validationLedger/UI/Components/LoadStatusBadgeView.swift"
      to: "validationLedger/Core/Load/LoadStatus.swift"
      via: "configure(status: LoadStatus) entry point covering 13 cases"
      pattern: "configure\\(status: LoadStatus\\)"
---

<objective>
Build the two reusable view-layer components in `validationLedger/UI/Components/` that Phase 8 cells consume and Phases 9–10 will reuse:

1. `VerificationBadgeView: UIView` — the TRUST-02 reusable 4-state pill (`configure(state:)` + a `configure(stateOrNil:)` overload for D-03 fail-closed nil).
2. `LoadStatusBadgeView: UIView` — the 13-state status pill (`configure(status:)`).
3. Snapshot tests for both, baseline-generated via the Wave 0 `UIKitSnapshot` helper (Plan 01) + `XCTAttachment`.

Purpose: Plan 04 (LoadRowCell) composes both badges; Plan 06's UI smoke test asserts their accessibility identifiers. Locking the badge contracts here unblocks both downstream paths.
Output: 2 new components under `validationLedger/UI/Components/` (NEW directory), 2 new snapshot test suites under `validationLedgerTests/Loads/Snapshot/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-role-filtered-load-list/08-CONTEXT.md
@.planning/phases/08-role-filtered-load-list/08-RESEARCH.md
@.planning/phases/08-role-filtered-load-list/08-PATTERNS.md
@.planning/phases/08-role-filtered-load-list/08-UI-SPEC.md
@.planning/phases/08-role-filtered-load-list/08-01-SUMMARY.md
@./CLAUDE.md

<interfaces>
From validationLedger/Core/Load/VerificationState.swift (Phase 7 D-09 — closed enum, fail-closed decoder):
```swift
public enum VerificationState: String, Decodable, Sendable {
    case verified, pending, unverified, flagged
}
```

From validationLedger/Core/Load/LoadStatus.swift (Phase 7 D-01 — 13-case full lifecycle):
```swift
public enum LoadStatus: String, Decodable, Sendable {
    case draft, posted, tendered, accepted, dispatched
    case inTransit  // wire form: "in_transit"
    case delivered
    case rejected, expired, cancelled
    case podCaptured  // wire form: "pod_captured"
    case invoiced, funded
}
```

From validationLedger/UI/DesignSystem/Colors.swift:
```swift
public enum DS {
    public enum Colors {
        public static let primary:    UIColor = .systemBlue
        public static let background: UIColor = .systemBackground
        public static let surface:    UIColor = .secondarySystemBackground
        public static let label:      UIColor = .label
        public static let labelSecondary: UIColor = .secondaryLabel
        public static let separator:  UIColor = .separator
        public static let destructive: UIColor = .systemRed
    }
}
```

From validationLedger/UI/DesignSystem/Spacing.swift:
```swift
public extension DS {
    enum Spacing {
        public static let xs:  CGFloat = 4
        public static let sm:  CGFloat = 8
        public static let md:  CGFloat = 16
        // (lg=24, xl=32, xxl=48)
    }
}
```

From validationLedger/UI/DesignSystem/Typography.swift:
```swift
public extension DS {
    enum Typography {
        public static var footnote: UIFont { .preferredFont(forTextStyle: .footnote) }
        // (headline, body, title1 also available)
    }
}
```

From validationLedger/UI/LimitedTrustBannerView.swift (closest DS-token-styled UIView analog — for the `setUp()` structure + NSLocalizedString + accessibilityLabel patterns):
```swift
// File-level pattern: programmatic UIView subclass with NSLocalizedString copy,
// DS-token-only styling, and explicit accessibilityLabel setting per state.
```

From validationLedgerTests/Support/UIKitSnapshot.swift (NEW from Plan 01):
```swift
enum UIKitSnapshot {
    static func image(of view: UIView, size: CGSize) -> UIImage
    static func attach(_ image: UIImage, name: String, to testCase: XCTestCase,
                       lifetime: XCTAttachment.Lifetime = .keepAlways)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VerificationBadgeView — 4-state pill + fail-closed nil overload</name>
  <files>validationLedger/UI/Components/VerificationBadgeView.swift</files>
  <behavior>
    - `configure(state: .verified)` -> backgroundColor = DS.Colors.primary; SF Symbol "checkmark.seal.fill"; label "VERIFIED" (uppercase); white foreground; accessibilityLabel localized "Counterparty verified".
    - `configure(state: .pending)` -> backgroundColor = .systemYellow (documented exception per UI-SPEC); SF Symbol "clock.fill"; label "PENDING"; .label foreground; accessibilityLabel localized "Counterparty verification pending".
    - `configure(state: .unverified)` -> backgroundColor = .tertiarySystemFill (neutral grey, documented exception); SF Symbol "questionmark.circle.fill"; label "UNVERIFIED"; .secondaryLabel foreground; accessibilityLabel localized "Counterparty not verified" — MUST NOT contain "verified" without the "not" prefix (fail-closed semantics).
    - `configure(state: .flagged)` -> backgroundColor = DS.Colors.destructive; SF Symbol "exclamationmark.triangle.fill"; label "FLAGGED"; white foreground; accessibilityLabel localized "Counterparty flagged".
    - `configure(stateOrNil: nil)` -> renders identical visuals to `.unverified` AND sets accessibilityLabel to the unverified copy (D-03 fail-closed). The view NEVER inherits a default accessibilityLabel that could leak "verified" semantics.
    - Pill geometry: `layer.cornerRadius = bounds.height / 2` recomputed in `layoutSubviews()` (Pitfall 8 — Dynamic Type changes height); label has `setContentCompressionResistancePriority(.required, for: .vertical)`.
    - Layout: horizontal `UIStackView` of [symbolView, label] with `spacing = DS.Spacing.xs`; outer padding `DS.Spacing.xs` on all four edges; symbolView width pinned to its height (square icon).
    - Typography: `label.font = DS.Typography.footnote`; `label.adjustsFontForContentSizeCategory = true`.
    - `isAccessibilityElement = true`; `accessibilityTraits = .staticText`; badge is non-interactive (UI-SPEC: "Phase 9 makes the graph-node container tappable, not the badge itself").
    - `isUserInteractionEnabled = false` on the internal stack view.
    - Every user-visible string uses `NSLocalizedString(_, value:, comment:)` with the key prefix `verification_badge.*` (e.g. `verification_badge.verified`, `verification_badge.verified.a11y`).
    - File-level doc comment names this as TRUST-02; calls out Phase 9 + 10 reuse; explicitly documents that the fail-closed nil overload is the locked entry point and that any future addition of a 5th case requires changing the `VerificationState` enum upstream (which would fail decode at the Phase 7 contract — UI-SPEC §Color verification table).
  </behavior>
  <read_first>
    - validationLedger/Core/Load/VerificationState.swift (the closed 4-case enum + fail-closed decoder)
    - validationLedger/UI/LimitedTrustBannerView.swift (closest DS-token-styled UIView analog — mirror its `setUp()` structure, NSLocalizedString-with-localized-comment pattern, and `accessibilityLabel` discipline)
    - validationLedger/UI/DesignSystem/Colors.swift (DS.Colors.primary, DS.Colors.destructive — the only DS color tokens this view uses; .systemYellow + .tertiarySystemFill are documented exceptions)
    - validationLedger/UI/DesignSystem/Spacing.swift (DS.Spacing.xs == 4pt — the only spacing token this view uses)
    - validationLedger/UI/DesignSystem/Typography.swift (DS.Typography.footnote — the only typography token)
    - .planning/phases/08-role-filtered-load-list/08-UI-SPEC.md (§ "Color > Verification badge color rules (TRUST-02 — locked)" table; § "Copywriting Contract" — uppercase labels VERIFIED/PENDING/UNVERIFIED/FLAGGED)
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "No-Analog Construction Guide > VerificationBadgeView" — pinned skeleton template; the planner template is NOT verbatim mandatory but the structural choices ARE)
    - .planning/phases/08-role-filtered-load-list/08-RESEARCH.md (§ "Code Examples > Example 2: VerificationBadgeView" — pinned implementation reference; § "Pitfall 8" — Dynamic Type breaks pill shape unless cornerRadius recomputes in layoutSubviews)
  </read_first>
  <action>
    Create `validationLedger/UI/Components/VerificationBadgeView.swift` (creating the `UI/Components/` directory). Implement `public final class VerificationBadgeView: UIView` with the exact public surface:
    - `public override init(frame: CGRect)` + `public required init?(coder: NSCoder) { fatalError("not used") }` — initializer-DI / programmatic UIKit (ARCH-04).
    - `public func configure(state: VerificationState)` — render a known state.
    - `public func configure(stateOrNil state: VerificationState?)` — render an OPTIONAL state; `nil` -> apply `.unverified` (D-03 fail-closed).
    - `public override func layoutSubviews()` — recompute `layer.cornerRadius = bounds.height / 2` (Pitfall 8).
    - Private `apply(state:)` switch covers all 4 cases per the `<behavior>` table; uses ONLY DS-token / system-color references named in `<behavior>`.
    - Private `setUp()` builds the [symbolView, label] horizontal UIStackView with the four-edge `DS.Spacing.xs` padding; sets `isAccessibilityElement = true`, `accessibilityTraits = .staticText`, `isUserInteractionEnabled = false` on the stack.
    Every user-visible string MUST go through `NSLocalizedString(_:value:comment:)` with the key prefix `verification_badge.*` per the file-level comment in `LimitedTrustBannerView.swift`.
    Per T-08-05 (PII): no logging in this view; the accessibilityLabel uses only the localized string (no party names, no IDs).
    Per T-08-06 (fail-closed): the `.unverified` accessibilityLabel string MUST be auditable — write it as `"Counterparty not verified"` (the literal "not" prefix prevents accidental "verified" substring match in tests).
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' build -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    `grep -c '^public final class VerificationBadgeView: UIView' validationLedger/UI/Components/VerificationBadgeView.swift == 1`; `grep -c 'public func configure(state: VerificationState)' validationLedger/UI/Components/VerificationBadgeView.swift >= 1`; `grep -c 'public func configure(stateOrNil' validationLedger/UI/Components/VerificationBadgeView.swift >= 1`; `grep -c 'layer.cornerRadius' validationLedger/UI/Components/VerificationBadgeView.swift >= 1` AND that grep matches a line inside a `layoutSubviews` body; `grep -c 'isAccessibilityElement = true' validationLedger/UI/Components/VerificationBadgeView.swift >= 1`; `grep -c 'NSLocalizedString' validationLedger/UI/Components/VerificationBadgeView.swift >= 8` (4 labels + 4 accessibilityLabels minimum); project builds clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LoadStatusBadgeView — 13-state informational pill (3 tone tiers)</name>
  <files>validationLedger/UI/Components/LoadStatusBadgeView.swift</files>
  <behavior>
    - `configure(status: .draft)` -> backgroundColor = .systemGray6 (pre-life tone); .secondaryLabel foreground; label "DRAFT".
    - `configure(status:)` for `.posted | .tendered | .accepted | .dispatched | .inTransit` -> backgroundColor = .tertiarySystemFill (in-progress / active tone); .label foreground; label "POSTED" / "TENDERED" / "ACCEPTED" / "DISPATCHED" / "IN TRANSIT".
    - `configure(status:)` for `.delivered | .podCaptured | .invoiced | .funded` -> backgroundColor = .systemGray5 (done tone); .secondaryLabel foreground; label "DELIVERED" / "POD CAPTURED" / "INVOICED" / "FUNDED".
    - `configure(status:)` for `.rejected | .expired | .cancelled` -> backgroundColor = .systemGray5 (terminal-error tone); .secondaryLabel foreground; label "REJECTED" / "EXPIRED" / "CANCELLED" with `NSAttributedString` strikethrough (UI-SPEC §Color status table — strikethrough on terminal-error tones).
    - All labels uppercase per UI-SPEC §Copywriting Contract cell-content-status-badge-label row.
    - Color ramp is **informational**, NOT the verification security primitive — MUST NEVER reuse DS.Colors.primary or DS.Colors.destructive for any case (RESEARCH §"Status badge color rules" — sharing color across two unrelated meanings is a design bug).
    - Layout, geometry, accessibility, and typography mirror `VerificationBadgeView` exactly: pill with `layer.cornerRadius = bounds.height / 2` in `layoutSubviews`; horizontal stack with `DS.Spacing.xs` padding; `DS.Typography.footnote`; `adjustsFontForContentSizeCategory = true`; `isAccessibilityElement = true`; `accessibilityTraits = .staticText`; non-interactive (`isUserInteractionEnabled = false` on stack).
    - The status badge does NOT need a symbol icon (UI-SPEC §Color status table mentions label-only); the layout is label-only (no `symbolView`) OR can mirror VerificationBadgeView's symbol slot with `UIImage?` (no symbol = symbolView hidden / removed from stack). The planner picks label-only for less code.
    - accessibilityLabel = the localized status label (e.g. "Status: In transit") — uses NSLocalizedString with key prefix `status_badge.*` (e.g. `status_badge.in_transit.a11y`).
  </behavior>
  <read_first>
    - validationLedger/Core/Load/LoadStatus.swift (the closed 13-case enum + every rawValue — confirm wire forms `in_transit` and `pod_captured`)
    - validationLedger/UI/Components/VerificationBadgeView.swift (built in Task 1 — mirror geometry, layoutSubviews cornerRadius recomputation, accessibility setup, DS-token discipline; the status badge is the sibling component)
    - validationLedger/UI/DesignSystem/Colors.swift (only system-managed neutrals are used: .tertiarySystemFill, .systemGray5, .systemGray6 — these are documented UI-SPEC exceptions, NOT DS tokens, because they are non-chromatic)
    - validationLedger/UI/DesignSystem/Typography.swift (DS.Typography.footnote — same as VerificationBadgeView)
    - .planning/phases/08-role-filtered-load-list/08-UI-SPEC.md (§ "Color > Status badge color rules (informational — NOT a security primitive)" table; § "Copywriting Contract" — uppercase labels including "IN TRANSIT" and "POD CAPTURED" — the bridge from camelCase Swift case to space-separated uppercase wire label)
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "No-Analog Construction Guide > LoadStatusBadgeView" reference + § "Pitfall 7" — `LoadStatus.cancelled` is double-L on the Swift side AND in the wire form; no spelling drift)
  </read_first>
  <action>
    Create `validationLedger/UI/Components/LoadStatusBadgeView.swift`. Implement `public final class LoadStatusBadgeView: UIView` with public surface:
    - `public override init(frame: CGRect)` + `public required init?(coder: NSCoder) { fatalError("not used") }`.
    - `public func configure(status: LoadStatus)` — render any of the 13 cases per the `<behavior>` mapping.
    - `public override func layoutSubviews()` — recompute `layer.cornerRadius = bounds.height / 2`.
    - Private `apply(status:)` switch with EXHAUSTIVE case coverage of all 13 LoadStatus cases (compiler enforces exhaustiveness on a closed enum — no `@unknown default` needed; relying on exhaustive switch is the locked discipline per Phase 7 D-01).
    - Private `setUp()` builds a single label (no symbolView) inside the badge with the same four-edge `DS.Spacing.xs` padding, `DS.Typography.footnote`, `adjustsFontForContentSizeCategory = true`, `setContentCompressionResistancePriority(.required, for: .vertical)`.
    Per the terminal-error tone (rejected/expired/cancelled), set the label text via an `NSAttributedString` with `.strikethroughStyle: NSUnderlineStyle.single.rawValue`. The other 10 cases use plain `label.text =`.
    Strings: every label + every accessibilityLabel through `NSLocalizedString(_:value:comment:)` with key prefix `status_badge.*` (e.g. `status_badge.in_transit.label` = "IN TRANSIT", `status_badge.in_transit.a11y` = "Status: In transit"). Locked labels per UI-SPEC §Copywriting — "DRAFT / POSTED / TENDERED / ACCEPTED / DISPATCHED / IN TRANSIT / DELIVERED / REJECTED / EXPIRED / CANCELLED / POD CAPTURED / INVOICED / FUNDED" (note: 13 cases, each maps to its own LoadStatus case).
    Per T-08-05 (PII): no logging; accessibilityLabel uses localized strings only. Per T-08-06: the status badge does NOT consume `VerificationState` — verification semantics are isolated to `VerificationBadgeView` (UI-SPEC: "The status badge does NOT reuse the verification ramp").
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' build -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    `grep -c '^public final class LoadStatusBadgeView: UIView' validationLedger/UI/Components/LoadStatusBadgeView.swift == 1`; `grep -c 'public func configure(status: LoadStatus)' validationLedger/UI/Components/LoadStatusBadgeView.swift >= 1`; `grep -c 'case \.draft' validationLedger/UI/Components/LoadStatusBadgeView.swift >= 1` AND `grep -c 'case \.podCaptured' validationLedger/UI/Components/LoadStatusBadgeView.swift >= 1` (exhaustive switch evidence); `grep -c 'DS.Colors.primary\|DS.Colors.destructive' validationLedger/UI/Components/LoadStatusBadgeView.swift == 0` (UI-SPEC: status badge does NOT reuse the verification ramp — these tokens MUST NOT appear in this file); `grep -c 'strikethrough' validationLedger/UI/Components/LoadStatusBadgeView.swift >= 1`; project builds clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Snapshot suites for both badges — 4-state + fail-closed nil a11y assertion + 13-state coverage</name>
  <files>
    validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift,
    validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift
  </files>
  <read_first>
    - validationLedgerTests/Support/UIKitSnapshot.swift (NEW from Plan 01 — the snapshot helper surface)
    - validationLedgerTests/KYC/KYCThumbnailTests.swift (lines 19–25 — class shape + Swift Testing import set)
    - validationLedger/UI/Components/VerificationBadgeView.swift (built in Task 1 — the public surface under test)
    - validationLedger/UI/Components/LoadStatusBadgeView.swift (built in Task 2 — the public surface under test)
    - validationLedger/Core/Load/VerificationState.swift (the 4 enum cases)
    - validationLedger/Core/Load/LoadStatus.swift (the 13 enum cases)
    - .planning/phases/08-role-filtered-load-list/08-RESEARCH.md (§ "Code Examples > Example 2" — VerificationBadgeView accessibility-label discipline; § "Security Domain > Known Threat Pattern: Information disclosure via VoiceOver label on UNVERIFIED row" — drives the assertion that the .unverified accessibilityLabel does NOT contain the substring "verified" with a leading space or non-"not" prefix)
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "10. Snapshot test files" — XCTest class shape, attachment-based baseline pattern; the .serialized attribute IS required when the test mutates global state — for pure render-only badge tests, .serialized is OPTIONAL but recommended for consistency)
  </read_first>
  <action>
    A. Create `validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift` (creating the `Snapshot/` directory). XCTest-based (subclass `XCTestCase`) because `UIKitSnapshot.attach(...)` needs the `XCTestCase.add(_ attachment:)` API surface — Swift Testing doesn't expose this cleanly. (NB: pattern-PATTERNS.md and RESEARCH both mention Swift Testing — but the XCTAttachment integration is XCTest-specific, so this snapshot suite is one of the XCTest-pattern test files. Document this choice in a file-level comment.)
    Tests:
    - `func test_verifiedBadgeRendersVerifiedVisuals()`: instantiate `VerificationBadgeView`; call `configure(state: .verified)`; render via `UIKitSnapshot.image(of: view, size: CGSize(width: 140, height: 28))`; assert `view.backgroundColor == DS.Colors.primary` AND `view.accessibilityLabel?.contains("verified") == true`; attach image via `UIKitSnapshot.attach(...)`.
    - `func test_pendingBadgeRendersPendingVisuals()`: same shape with `.pending`; assert `view.backgroundColor == .systemYellow` AND accessibilityLabel contains "pending".
    - `func test_unverifiedBadgeRendersUnverifiedVisuals()`: same shape with `.unverified`; assert `view.backgroundColor == .tertiarySystemFill`; accessibilityLabel contains "not verified" (literal substring — proves fail-closed); CRITICAL ASSERTION (T-08-06): `XCTAssertFalse(view.accessibilityLabel?.contains("verified") == true && view.accessibilityLabel?.contains("not verified") == false, "Fail-closed: UNVERIFIED badge a11y label must NEVER read as 'verified' without the 'not' prefix")`.
    - `func test_flaggedBadgeRendersFlaggedVisuals()`: same shape with `.flagged`; assert `view.backgroundColor == DS.Colors.destructive`; accessibilityLabel contains "flagged".
    - `func test_nilCounterpartyRendersUnverifiedFailClosed()` (the D-03 lock + T-08-06 mitigation): instantiate view; call `view.configure(stateOrNil: nil)`; assert visuals identical to `.unverified` case (background, foreground colors); assert accessibilityLabel substring discipline as in `test_unverifiedBadgeRendersUnverifiedVisuals` — `XCTAssertFalse(view.accessibilityLabel?.lowercased().contains(" verified") == true && view.accessibilityLabel?.lowercased().contains("not verified") == false)`. Document at the top of this test that the assertion locks Phase 7 D-03 + UI-SPEC fail-closed + T-08-06.
    - `func test_pillCornerRadiusRecomputesOnLayout()`: instantiate view; call `view.bounds = CGRect(x: 0, y: 0, width: 100, height: 40)`; call `view.layoutIfNeeded()`; assert `view.layer.cornerRadius == 20` (== bounds.height / 2). Then resize to `width: 100, height: 60`; call `layoutIfNeeded()`; assert `view.layer.cornerRadius == 30` (Pitfall 8 — Dynamic Type lock).

    B. Create `validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift`. XCTest-based for the same reason. Tests:
    - One test per representative tone-tier (4 tests: pre-life via `.draft`; in-progress via `.inTransit`; done via `.delivered`; terminal-error via `.cancelled`):
      - `func test_draftStatusRendersPreLifeTone()`: assert `view.backgroundColor == .systemGray6`; label text == "DRAFT"; attach image.
      - `func test_inTransitStatusRendersInProgressTone()`: assert `view.backgroundColor == .tertiarySystemFill`; label text == "IN TRANSIT"; attach image.
      - `func test_deliveredStatusRendersDoneTone()`: assert `view.backgroundColor == .systemGray5`; label text == "DELIVERED"; attach image.
      - `func test_cancelledStatusRendersTerminalErrorWithStrikethrough()`: assert `view.backgroundColor == .systemGray5`; assert label has `NSAttributedString` with `.strikethroughStyle` attribute set (extract via `(label.attributedText?.attribute(.strikethroughStyle, at: 0, effectiveRange: nil) as? Int) != nil`); attach image.
    - `func test_statusBadgeNeverReusesVerificationRampColors()` (lock — T-08-06 secondary): for every one of the 13 cases, instantiate + configure + assert `view.backgroundColor != DS.Colors.primary && view.backgroundColor != DS.Colors.destructive`. Loop through all 13 cases via `LoadStatus.allCases` (if `CaseIterable`) or an explicit literal array.

    Both files: `import XCTest` + `@testable import validationLedger`. Place under `validationLedgerTests/Loads/Snapshot/` (Plan 01 created the Wave 0 snapshot helper at `validationLedgerTests/Support/`).

    Per T-08-05 (PII): tests construct views with synthetic enum values only; no party names, no IDs reach the rendered image. Per T-08-06 (fail-closed): two of the five VerificationBadge tests lock the accessibilityLabel discipline; the LoadStatusBadge "never reuses verification ramp" loop locks the color separation.
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerTests/VerificationBadgeViewSnapshotTests -only-testing:validationLedgerTests/LoadStatusBadgeViewSnapshotTests test -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    Both files exist; `grep -c '^class VerificationBadgeViewSnapshotTests: XCTestCase' validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift == 1`; `grep -c '^class LoadStatusBadgeViewSnapshotTests: XCTestCase' validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift == 1`; `grep -c 'func test_' validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift >= 6` (4 states + nil + cornerRadius); `grep -c 'func test_' validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift >= 5`; `grep -c 'test_nilCounterpartyRendersUnverifiedFailClosed\|test_unverifiedBadgeRendersUnverifiedVisuals' validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift >= 2` (fail-closed assertions present); `grep -c 'test_statusBadgeNeverReusesVerificationRampColors' validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift >= 1`; the suite passes (exit 0) on the scoped invocation above.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `VerificationState` (server-supplied, fail-closed at Phase 7 D-09) -> `VerificationBadgeView` render path | The badge ASSUMES the enum is already fail-closed; the badge MUST NOT try to "soften" `.unverified` (e.g. by displaying "Awaiting verification" or any optimistic copy). The badge is the last visual gatekeeper before VoiceOver. |
| `nil` counterparty from `LoadListItem.displayedCounterparty` (D-03) -> badge nil overload | The nil-overload is a SECURITY boundary, not just a convenience. The fail-closed render of UNVERIFIED + the "Counterparty not verified" accessibilityLabel are locked by tests in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information disclosure (PII) | `VerificationBadgeView`, `LoadStatusBadgeView` | mitigate | NO logging in either view (pure render-from-input UIKit components); accessibilityLabel uses ONLY `NSLocalizedString` strings — never injects party `displayName` or `partyID`. The cell (Plan 04) is the layer that consumes party names; the badges are PII-free. |
| T-08-06 | Information disclosure / Spoofing | `VerificationBadgeView.accessibilityLabel` on `.unverified` / `nil` | mitigate | The nil-overload's accessibilityLabel MUST be "Counterparty not verified" (locked literal — the "not" prefix prevents accidental substring match of "verified" in a security-sensitive VoiceOver context). Unit-test `test_unverifiedBadgeRendersUnverifiedVisuals` + `test_nilCounterpartyRendersUnverifiedFailClosed` assert this. RESEARCH §Security Domain Known-Threat-1 ("Information disclosure via VoiceOver label on UNVERIFIED row") drives this control. |
</threat_model>

<verification>
1. Build green: project builds clean (no compile errors from the new files).
2. VerificationBadge snapshot suite passes: scoped `-only-testing:validationLedgerTests/VerificationBadgeViewSnapshotTests` exits 0; the fail-closed accessibilityLabel assertion holds for both `.unverified` and `nil` paths.
3. LoadStatusBadge snapshot suite passes: scoped `-only-testing:validationLedgerTests/LoadStatusBadgeViewSnapshotTests` exits 0; the "never reuses verification ramp" loop confirms color separation.
4. DS-token discipline: `grep -E '(DS\.Colors|DS\.Spacing|DS\.Typography|NSLocalizedString)' validationLedger/UI/Components/VerificationBadgeView.swift` returns high count; the only allowed exceptions (`.systemYellow`, `.tertiarySystemFill`, `bounds.height/2`) are documented in the file-level comment.
5. UI/Components directory exists: `ls validationLedger/UI/Components/` returns the two new files.
</verification>

<success_criteria>
- `VerificationBadgeView` renders all 4 `VerificationState` cases per the UI-SPEC locked color ramp.
- The `configure(stateOrNil: nil)` overload renders UNVERIFIED visuals and an accessibilityLabel that locks the fail-closed semantic (T-08-06).
- `LoadStatusBadgeView` renders all 13 `LoadStatus` cases per the UI-SPEC 3-tone informational ramp; never reuses `DS.Colors.primary` or `DS.Colors.destructive`.
- Both badges scale correctly with Dynamic Type (cornerRadius recomputes in layoutSubviews — Pitfall 8 locked by `test_pillCornerRadiusRecomputesOnLayout`).
- Both snapshot suites pass and produce XCTAttachment baseline images for visual review.
- Zero new SwiftPM dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/08-role-filtered-load-list/08-02-SUMMARY.md` when done. Record:
- VerificationBadgeView public surface (init, configure(state:), configure(stateOrNil:), layoutSubviews); the locked accessibility label strings for each of the 5 paths (4 states + nil); the documented color exceptions.
- LoadStatusBadgeView public surface; the 3-tone tier table (which LoadStatus cases map to which background color); the strikethrough discipline for terminal-error tones.
- The XCTest-vs-Swift-Testing choice for the snapshot suites (XCTAttachment requires XCTestCase) — surface as an explicit decision so Plans 04/05 can follow precedent.
- Any open question that surfaced (e.g. whether the status badge label should fold "POD CAPTURED" to "POD" for narrow widths — not currently in scope per UI-SPEC).
</output>
