---
phase: 08-role-filtered-load-list
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - validationLedger/App/AppContainer.swift
  - validationLedger/App/AppCoordinator.swift
  - validationLedger/Roles/Broker/BrokerTabBarController.swift
  - validationLedger/Roles/Shipper/ShipperTabBarController.swift
  - validationLedger/Roles/Carrier/CarrierTabBarController.swift
  - validationLedger/Roles/Dispatch/DispatchTabBarController.swift
  - validationLedger/Roles/Factoring/FactoringTabBarController.swift
  - validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift
autonomous: true
requirements:
  - LOAD-03
threat_refs:
  - T-08-11
  - T-08-12

must_haves:
  truths:
    - "AppContainer exposes a `makeLoadListScreen(role: Role) -> UIViewController` factory mirroring `makeKYCStatusScreen()`."
    - "AppCoordinator.roleCoordinator(for:container:) threads a `loadListScreenFactory: (Role) -> UIViewController` closure into each of the 5 `*TabBarController.init(...)` calls."
    - "Each of the 5 `*TabBarController` files (Broker, Shipper, Carrier, Dispatch, Factoring) accepts the new factory in its initializer and uses it to back the role-appropriate Loads tab — Factoring's tab title STAYS `\"Invoices\"` per PATTERNS.md Q1 + RoleShellSmokeTests.swift:151."
    - "Each role's Loads tab is backed by a `LoadListViewController(LoadListViewModel(role: ${role}, apiClient:_, logger:_))` — BrokerTabBarController routes to `.broker`, Shipper to `.shipper`, etc. (T-08-11)."
    - "The 5-role smoke XCUITest taps Loads (Invoices on Factoring) on each role shell post-OTP and asserts `loads-list` accessibility identifier resolves; populated-fixture roles also resolve at least one `loads-list.row.VL-*` identifier; the empty-fixture role resolves the `loads-list.empty-state` identifier."
    - "`RoleShellSmokeTests.swift:151`'s existing `app.tabBars.buttons[\"Invoices\"]` assertion continues to pass — the Factoring tab title remains exactly `\"Invoices\"` (T-08-12)."
  artifacts:
    - path: "validationLedger/App/AppContainer.swift"
      provides: "`makeLoadListScreen(role:) -> UIViewController` composition-root factory"
      contains: "func makeLoadListScreen(role: Role) -> UIViewController"
    - path: "validationLedger/App/AppCoordinator.swift"
      provides: "Per-role tab bar construction with the new factory closure forwarded"
      contains: "loadListScreenFactory"
    - path: "validationLedger/Roles/Broker/BrokerTabBarController.swift"
      provides: "Broker tab bar with the real Loads tab backed by LoadListViewController(.broker)"
      contains: "loadListScreenFactory"
    - path: "validationLedger/Roles/Shipper/ShipperTabBarController.swift"
      provides: "Shipper tab bar with the real Loads tab backed by LoadListViewController(.shipper)"
      contains: "loadListScreenFactory"
    - path: "validationLedger/Roles/Carrier/CarrierTabBarController.swift"
      provides: "Carrier tab bar with the real Loads tab backed by LoadListViewController(.carrier)"
      contains: "loadListScreenFactory"
    - path: "validationLedger/Roles/Dispatch/DispatchTabBarController.swift"
      provides: "Dispatch tab bar with the real Loads tab backed by LoadListViewController(.dispatch)"
      contains: "loadListScreenFactory"
    - path: "validationLedger/Roles/Factoring/FactoringTabBarController.swift"
      provides: "Factoring tab bar — `\"Invoices\"` tab title PRESERVED, backed by LoadListViewController(.factoring)"
      contains: "loadListScreenFactory"
    - path: "validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift"
      provides: "5-role smoke flow — tap Loads (Invoices on Factoring), assert loads-list identifier resolves, assert row or empty-state identifier resolves"
      contains: "RoleLoadsTabSmokeTests"
  key_links:
    - from: "validationLedger/App/AppContainer.swift"
      to: "validationLedger/Features/Loads/LoadListViewModel.swift"
      via: "makeLoadListScreen(role:) constructs LoadListViewModel(role:apiClient:logger:)"
      pattern: "LoadListViewModel\\(role:"
    - from: "validationLedger/App/AppCoordinator.swift"
      to: "validationLedger/App/AppContainer.swift"
      via: "loadListScreenFactory closure invokes container.makeLoadListScreen(role:)"
      pattern: "container\\.makeLoadListScreen\\(role:"
    - from: "validationLedger/Roles/Broker/BrokerTabBarController.swift"
      to: "validationLedger/Features/Loads/LoadListViewController.swift"
      via: "loadListScreenFactory(self.role) returns LoadListViewController instance for .broker"
      pattern: "loadListScreenFactory\\?\\(role\\)|loadListScreenFactory\\?\\(self\\.role\\)"
    - from: "validationLedger/Roles/Factoring/FactoringTabBarController.swift"
      to: "validationLedger/Features/Loads/LoadListViewController.swift"
      via: "Invoices tab is backed by LoadListViewController(.factoring) — title preserved as `\"Invoices\"`"
      pattern: "\"Invoices\""
---

<objective>
Wire the Phase 8 Loads feature (Plan 03) into the 5 role tab shells using the existing `kycStatusScreenFactory` precedent (PATTERNS.md §6 + §7 + §8). Specifically:

1. Add `AppContainer.makeLoadListScreen(role:) -> UIViewController` mirroring `makeKYCStatusScreen()` exactly.
2. Extend `AppCoordinator.roleCoordinator(for:container:)` to build a `loadListScreenFactory: (Role) -> UIViewController` closure (parallel to the existing `kycStatusFactory`) and forward it into each of the 5 `*TabBarController.init(...)` call sites.
3. Add a `loadListScreenFactory: ((Role) -> UIViewController)?` stored property + `init(...)` parameter to each of the 5 `*TabBarController` files. Replace the placeholder `Self.makeTab(title: "Loads", systemImage: "shippingbox")` (in Broker/Shipper/Carrier/Dispatch) with the real `loadListScreenFactory?(role) ?? Self.makeTab(...)` call. For Factoring, REPLACE the placeholder `"Invoices"` tab's content with `loadListScreenFactory?(.factoring) ?? Self.makeTab(title: "Invoices", systemImage: "doc.text.magnifyingglass")` but PRESERVE the tab title `"Invoices"` (PATTERNS.md Q1 + RoleShellSmokeTests:151 — T-08-12).
4. Add a new 5-role XCUITest `RoleLoadsTabSmokeTests` under `validationLedgerUITests/Loads/` that drives the existing `-MockOTPRoleForUITest` flow, then taps Loads (or Invoices on Factoring) and asserts `loads-list` resolves, plus role-specific row/empty-state identifier resolution.

Purpose: this is the LOAD-03 integration cap — the requirement that "each of the 5 roles sees a role-filtered list rendered in its tab shell" is only satisfied when this wiring is in place. Plan 03 builds the feature; Plan 04 makes it user-visible.
Output: 7 modified source files + 1 new XCUITest file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-role-filtered-load-list/08-CONTEXT.md
@.planning/phases/08-role-filtered-load-list/08-RESEARCH.md
@.planning/phases/08-role-filtered-load-list/08-PATTERNS.md
@.planning/phases/08-role-filtered-load-list/08-UI-SPEC.md
@.planning/phases/08-role-filtered-load-list/08-03-SUMMARY.md
@./CLAUDE.md

<interfaces>
From validationLedger/Features/Loads/LoadListViewModel.swift (Plan 03):
```swift
@MainActor
public final class LoadListViewModel {
    public init(role: Role, apiClient: APIClient, logger: any Logger)
    public func fetchLoads() async
    public enum State: Equatable, Sendable { case loading, empty, loaded(items: [LoadListItem], nextCursor: String?), error(message: String) }
    public var onStateChange: ((State) -> Void)?
}
```

From validationLedger/Features/Loads/LoadListViewController.swift (Plan 03):
```swift
final class LoadListViewController: UIViewController {
    init(viewModel: LoadListViewModel)
}
// Sets accessibilityIdentifier = "loads-list" on its collection view.
// Cells set accessibilityIdentifier = "loads-list.row.\(load.id)" on themselves.
// Empty state view sets accessibilityIdentifier = "loads-list.empty-state".
```

From validationLedger/App/AppContainer.swift lines 167–199 (the verbatim factory analog):
```swift
@MainActor
func makeKYCStatusScreen() -> UIViewController {
    let viewModel = KYCStatusViewModel(
        apiClient: apiClient,
        store: kycSessionStore,
        keychain: keychainStore,
        logger: logger
    )
    let viewController = KYCStatusViewController(viewModel: viewModel)
    viewModel.onVerified = { [weak viewController] in /* dismiss/pop */ }
    return viewController
}
```

From validationLedger/App/AppCoordinator.swift lines 191–227 (the verbatim threading analog):
```swift
@MainActor
static func roleCoordinator(for role: Role, container: AppContainer) -> UITabBarController {
    let kycStatusFactory: () -> UIViewController = { [weak container] in
        guard let container else { return UIViewController() }
        return container.makeKYCStatusScreen()
    }
    switch role {
    case .shipper:
        return ShipperTabBarController(logoutService: container.logoutService, kycStatusScreenFactory: kycStatusFactory)
    case .broker:   return BrokerTabBarController(  logoutService: container.logoutService, kycStatusScreenFactory: kycStatusFactory)
    case .carrier:  return CarrierTabBarController( logoutService: container.logoutService, kycStatusScreenFactory: kycStatusFactory)
    case .dispatch: return DispatchTabBarController(logoutService: container.logoutService, kycStatusScreenFactory: kycStatusFactory)
    case .factoring: return FactoringTabBarController(logoutService: container.logoutService, kycStatusScreenFactory: kycStatusFactory)
    }
}
```

From validationLedger/Roles/Shipper/ShipperTabBarController.swift (the structural analog — every sibling shares this shape):
```swift
final class ShipperTabBarController: UITabBarController, RoleCoordinator {
    let role: Role = .shipper
    let logoutService: any LogoutService
    let kycStatusScreenFactory: (() -> UIViewController)?
    init(logoutService: any LogoutService, kycStatusScreenFactory: (() -> UIViewController)? = nil) { ... }
    override func viewDidLoad() {
        viewControllers = [
            Self.makeTab(title: "Loads", systemImage: "shippingbox"),    // ← line 34 placeholder
            Self.makeTab(title: "Brokers",   systemImage: "person.2"),
            Self.makeTab(title: "BOL",       systemImage: "doc.text"),
            Self.makeTab(title: "Assistant", systemImage: "sparkles"),
        ]
        wrapTabsWithNavAndInstallAvatar { [weak self] in /* ProfileViewController */ }
    }
    static func makeTab(title: String, systemImage: String) -> UIViewController { ... }
}
```

From validationLedger/Roles/Factoring/FactoringTabBarController.swift (the KEEP-`"Invoices"` outlier — line 33):
```swift
viewControllers = [
    ShipperTabBarController.makeTab(title: "Invoices", systemImage: "doc.text.magnifyingglass"),  // ← title stays "Invoices"; backed by LoadListViewController(.factoring) per PATTERNS Q1
    ShipperTabBarController.makeTab(title: "Carriers", systemImage: "truck.box"),
    ShipperTabBarController.makeTab(title: "Chain",    systemImage: "link"),
    ShipperTabBarController.makeTab(title: "Assistant", systemImage: "sparkles"),
]
```

From validationLedgerUITests/RoleShellSmokeTests.swift lines 31–157 (verbatim XCUITest template):
```swift
override func setUp() { super.setUp(); executionTimeAllowance = 30; continueAfterFailure = false }

private func launch(role: String) -> XCUIApplication {
    let app = XCUIApplication()
    app.launchArguments = ["-MockOTPRoleForUITest", role]
    app.launch()
    return app
}

private func driveFullOTPFlow(_ app: XCUIApplication) {
    let phoneField = app.textFields["phone-entry-field"]
    XCTAssertTrue(phoneField.waitForExistence(timeout: 10), "...")
    phoneField.tap(); phoneField.typeText("5551234567")
    // (submit-enable wait + tap)
    let otpField = app.textFields["otp-field"]
    XCTAssertTrue(otpField.waitForExistence(timeout: 5), "...")
    otpField.tap(); otpField.typeText("123456")
    app.buttons["otp-verify"].tap()
}

// Line 151: XCTAssertTrue(app.tabBars.buttons["Invoices"].waitForExistence(timeout: 5))
// — MUST continue to pass (T-08-12).
```

From validationLedger/Core/Networking/Mock/MockOTPRoleFixtureRegistry.swift (Phase 3 D-32 + Plan 01 envelope-wrapped lists):
```swift
// SceneDelegate's -MockOTPRoleForUITest handler installs MockURLProtocol fixtures via MockOTPRoleFixtureRegistry.registerForRole(_:)
// which (per Plan 01) registers the envelope-wrapped loads-list-{role}.json fixtures.
// The UI test inherits the registered fixtures; no fixture re-registration needed here.
```

From validationLedger/Core/Logging/Logger.swift + AppContainer.swift line 244–247 (the logger shape):
```swift
// AppContainer.logger is an `OSLogLoggerImpl(subsystem: LoggingSubsystem.app, category: "bootstrap")` instance.
// The Loads feature creates its own feature-scoped logger inside makeLoadListScreen(role:):
//   OSLogLoggerImpl(subsystem: LoggingSubsystem.app, category: "feature.loads")
// per PATTERNS.md §6 lines 487–491.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppContainer.makeLoadListScreen(role:) + AppCoordinator.roleCoordinator threading</name>
  <files>
    validationLedger/App/AppContainer.swift,
    validationLedger/App/AppCoordinator.swift
  </files>
  <read_first>
    - validationLedger/App/AppContainer.swift (lines 159–199 — the `makeKYCStatusScreen()` factory analog this task mirrors verbatim; lines 100–158 — existing stored properties + the section the new factory lives in)
    - validationLedger/App/AppCoordinator.swift (lines 191–228 — the `roleCoordinator(for:container:)` static method; the existing `kycStatusFactory` closure + 5-case switch pattern this task extends)
    - validationLedger/Features/Loads/LoadListViewModel.swift (Plan 03 — the `init(role:apiClient:logger:)` signature this factory calls)
    - validationLedger/Features/Loads/LoadListViewController.swift (Plan 03 — the `init(viewModel:)` signature)
    - validationLedger/Roles/Role.swift (the `Role` enum the factory closure parameter carries)
    - validationLedger/Core/Logging/Logger.swift + grep `LoggingSubsystem` to confirm whether a `.loads` subsystem already exists OR whether `LoggingSubsystem.app` should be reused (PATTERNS.md §6 line 489 — "subsystem: LoggingSubsystem.app, // or a new .loads subsystem if/when added"; this task DOES NOT add a new subsystem — keep `LoggingSubsystem.app` for now to avoid scope creep into the LoggingSubsystem closed enum).
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "6. AppContainer.swift" lines 454–502 — the verbatim factory pattern; § "7. AppCoordinator.swift" lines 505–552 — the verbatim threading pattern, including the explicit role-currying note)
  </read_first>
  <action>
    A. MODIFY `validationLedger/App/AppContainer.swift`:
       - Insert a new method `makeLoadListScreen(role: Role) -> UIViewController` IMMEDIATELY AFTER the closing brace of `makeKYCStatusScreen()` (currently around line 199). Mirror that function's structure verbatim per PATTERNS.md §6: same `@MainActor` annotation; same `func` keyword visibility (internal — no explicit `public` modifier, matching `makeKYCStatusScreen()`'s scope); same body shape (locals declared in order, single `return` statement).
       - Body construction discipline (no inline implementation here — read PATTERNS.md §6 lines 487–501 and `makeKYCStatusScreen()` itself for the exact form): allocate a feature-scoped logger via `OSLogLoggerImpl(subsystem: LoggingSubsystem.app, category: "feature.loads")`. The `LoggingSubsystem.app` case is REUSED — DO NOT add a new `.loads` case to the `LoggingSubsystem` enum (out-of-scope for this phase; PATTERNS.md §6 line 489 explicitly defers it). Allocate the view model via `LoadListViewModel(role: role, apiClient: apiClient, logger: featureLogger)` (the parameter order is locked by Plan 03's VM `init`). Return `LoadListViewController(viewModel: viewModel)` (Plan 03's VC `init` takes the VM positionally).
       - Add a doc-comment block above the new function citing: "Phase 8 LOAD-03 — D-01..D-04. The per-role Loads-tab factory consumed by the 5 `*TabBarController`s through the `loadListScreenFactory: (Role) -> UIViewController` closure threaded by `AppCoordinator.roleCoordinator(for:container:)`. Closure-factory pattern + `Role` parameter mirrors the Phase 5 `kycStatusScreenFactory` precedent (`makeKYCStatusScreen()` immediately above). The Loads screen is constructed lazily — `makeLoadListScreen(role:)` is invoked only when the tab bar's closure fires; no eager VM allocation."
       - DO NOT add a stored property to `AppContainer` for this factory. The composition follows the `makeKYCStatusScreen()` precedent exactly: a method, not a property. Closure-factory wrapping happens in `AppCoordinator`.

    B. MODIFY `validationLedger/App/AppCoordinator.swift`:
       - In `static func roleCoordinator(for:container:)` (lines 191–228), DIRECTLY AFTER the existing `kycStatusFactory` closure declaration (lines 197–200), add a sibling closure named `loadListFactory` of type `(Role) -> UIViewController`. Capture discipline: `[weak container]` (mirrors the existing `kycStatusFactory` `[weak container]` capture — composition-root retention; a strong capture would cycle past a role-swap per ADR 0002). Body: guard-let the container (fallback returns an empty `UIViewController()`, mirroring `kycStatusFactory`'s fallback); on success, call `container.makeLoadListScreen(role: role)` and return its result.
       - Update each of the 5 `case` branches inside the role switch to pass `loadListScreenFactory: loadListFactory` as a new trailing argument to each `*TabBarController(...)` initializer. The locked argument order on the tab bar `init` is: `logoutService: ...`, `kycStatusScreenFactory: ...`, `loadListScreenFactory: ...` (new arg last — preserves source-compatibility for any existing test that constructs a tab bar with only the first two args, because the new param is defaulted-nil in Task 2). All 5 cases (`.shipper`, `.broker`, `.carrier`, `.dispatch`, `.factoring`) receive the SAME `loadListFactory` instance — the role-specific routing happens inside each tab bar's `viewDidLoad()` (Task 2) by invoking `loadListScreenFactory?(self.role)`.
       - Update the file-level / section comments inside `roleCoordinator(for:container:)` to cite Phase 8 LOAD-03 as the addition driver alongside the existing Phase 5 KYC citation.
       - Per T-08-11: the `roleCoordinator(for:)` switch passes the SAME `loadListFactory` to every tab bar. Role-specific dispatch occurs at the tab bar layer via the per-controller `let role: Role = .{role}` stored property (already correct in source as of Phase 3 — verified before edit via `grep -n 'let role: Role' validationLedger/Roles/*/*TabBarController.swift`).

    Per T-08-11: the unit-level guarantee that BrokerTabBarController routes to `.broker` (not `.shipper` etc.) is locked by the `*TabBarController.role` stored property in each file (already correct in source: `let role: Role = .broker` in BrokerTabBarController.swift line 7; same for each sibling).
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' build -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    `grep -c 'func makeLoadListScreen(role: Role) -> UIViewController' validationLedger/App/AppContainer.swift == 1`; `grep -c 'LoadListViewModel(' validationLedger/App/AppContainer.swift == 1`; `grep -c 'LoadListViewController(viewModel:' validationLedger/App/AppContainer.swift == 1`; `grep -c 'loadListFactory: (Role) -> UIViewController' validationLedger/App/AppCoordinator.swift == 1`; `grep -c 'container.makeLoadListScreen(role: role)' validationLedger/App/AppCoordinator.swift == 1`; `grep -c 'loadListScreenFactory:' validationLedger/App/AppCoordinator.swift == 5` (one per role case); the project builds clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add loadListScreenFactory to all 5 *TabBarController files (Broker/Shipper/Carrier/Dispatch use real Loads VC; Factoring keeps "Invoices" title backed by .factoring Loads VC)</name>
  <files>
    validationLedger/Roles/Broker/BrokerTabBarController.swift,
    validationLedger/Roles/Shipper/ShipperTabBarController.swift,
    validationLedger/Roles/Carrier/CarrierTabBarController.swift,
    validationLedger/Roles/Dispatch/DispatchTabBarController.swift,
    validationLedger/Roles/Factoring/FactoringTabBarController.swift
  </files>
  <read_first>
    - validationLedger/Roles/Shipper/ShipperTabBarController.swift (every line — the CONTEXT.md exemplar; lines 14–48 are the full shape; line 34 is the Loads-tab placeholder this task replaces)
    - validationLedger/Roles/Broker/BrokerTabBarController.swift (sibling — confirm `let role: Role = .broker` line 7; line 33 placeholder)
    - validationLedger/Roles/Carrier/CarrierTabBarController.swift (sibling — `let role: Role = .carrier`; tabs are `Loads, Drivers, Documents, Assistant` per RoleShellSmokeTests:129–132)
    - validationLedger/Roles/Dispatch/DispatchTabBarController.swift (sibling — `let role: Role = .dispatch`; tabs are `Loads, Fleet, Drivers, Assistant` per RoleShellSmokeTests:140–143)
    - validationLedger/Roles/Factoring/FactoringTabBarController.swift (every line — the KEEP-`"Invoices"` outlier; line 33 is the Invoices-tab placeholder; the TITLE stays `"Invoices"` but the backing VC becomes the real Loads VC per PATTERNS.md Q1)
    - validationLedgerUITests/RoleShellSmokeTests.swift (line 151 — the existing `app.tabBars.buttons["Invoices"].waitForExistence` assertion that MUST continue to pass — T-08-12 lock; lines 103–112 + 125–144 — the Broker/Shipper/Carrier/Dispatch tab-button assertions that MUST continue to pass)
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "8. The 5 `*TabBarController.swift` files" lines 555–635 — the verbatim diff template + the open question about Factoring; line 634 explicitly chose option (b): "the 'Invoices' tab is backed by `LoadListViewController(.factoring)` with the tab title kept as 'Invoices'")
    - validationLedger/Roles/RoleCoordinator.swift (the shared `wrapTabsWithNavAndInstallAvatar` helper this task does NOT touch — confirm it lives there and the call site `wrapTabsWithNavAndInstallAvatar { ... }` on each tab bar passes through unchanged)
  </read_first>
  <action>
    For EACH of the 5 `*TabBarController.swift` files apply the following changes (per PATTERNS.md §8 lines 594–631 — read it for the exact source shape):

    1. Add a new stored property `loadListScreenFactory: ((Role) -> UIViewController)?` IMMEDIATELY BELOW the existing `kycStatusScreenFactory: (() -> UIViewController)?` declaration. Access level matches its sibling (`let`, no `public` modifier — internal scope, matching the existing `kycStatusScreenFactory` discipline). Add a doc-comment block above it citing: "Phase 8 LOAD-03 (D-01..D-04). Composition-root factory for the role-scoped Loads screen; forwarded by `AppCoordinator.roleCoordinator(for:container:)`. The closure is parameterized by `Role` so this tab bar invokes `loadListScreenFactory?(self.role)` to back the role-appropriate Loads tab (Invoices on Factoring). `nil` fallback preserves the v1.1 placeholder for any test that constructs the tab bar without injecting the factory."

    2. Extend the existing `init(...)` by adding a third parameter `loadListScreenFactory: ((Role) -> UIViewController)? = nil` after the existing `kycStatusScreenFactory` parameter. Default-nil is REQUIRED to preserve source-compatibility with existing test/factory call sites that pass only the first two arguments (KYC tests, profile-flow tests). Inside the initializer body, store the new parameter via `self.loadListScreenFactory = loadListScreenFactory` BEFORE the existing `super.init(nibName: nil, bundle: nil)` call (same order discipline as the existing two assignments).

    3. In `viewDidLoad()`, replace the role-appropriate placeholder Loads/Invoices tab as follows:

       **For Broker / Shipper / Carrier / Dispatch (4 files):** locate the existing placeholder line — currently `Self.makeTab(title: "Loads", systemImage: "shippingbox"),` in ShipperTabBarController, and `ShipperTabBarController.makeTab(title: "Loads", systemImage: "shippingbox"),` in the other 3 files (because they delegate to `ShipperTabBarController.makeTab` for cross-file reuse — same convention as the existing tabs in those files). Replace that single line with a call to the new helper `Self.makeLoadsTab(loadListScreenFactory: loadListScreenFactory, role: role)` (ShipperTabBarController only) or `ShipperTabBarController.makeLoadsTab(loadListScreenFactory: loadListScreenFactory, role: role)` (the other 3 files — same delegation discipline as their existing `makeTab` calls). The replacement preserves the trailing comma (it's still one element of the `viewControllers` array literal).

    4. Add a NEW static helper `static func makeLoadsTab(loadListScreenFactory: ((Role) -> UIViewController)?, role: Role) -> UIViewController` LIVING IN `ShipperTabBarController.swift` ONLY — single source of truth, mirroring the existing `static func makeTab(title:systemImage:)` discipline. Doc-comment block: "Phase 8 LOAD-03: build the role-scoped Loads tab. If `loadListScreenFactory` is non-nil, returns the real `LoadListViewController` for the given role. Else falls back to the v1.1 placeholder `makeTab(title:systemImage:)` — preserves test compatibility. Tab item title is always `\"Loads\"` (UI-SPEC: tab title is \"Loads\" — never \"Broker Loads\" etc.). The Factoring caller does NOT route through this helper — it has its own inline `\"Invoices\"` branch in `FactoringTabBarController.viewDidLoad()` (PATTERNS Q1; T-08-12)."

       Helper body (described as prose, not implemented inline — read `makeTab(title:systemImage:)` in the same file for the exact UIKit boilerplate this helper mirrors):
       - Invoke the factory: `let vc = loadListScreenFactory?(role) ?? makeTab(title: "Loads", systemImage: "shippingbox")` (use the existing `makeTab` static as the nil-fallback — same string literals as today's placeholder, so any test currently relying on the placeholder still resolves the tab).
       - Set `vc.title = "Loads"` (locks the nav-bar title regardless of whether the path was factory or fallback).
       - Set `vc.tabBarItem = UITabBarItem(title: "Loads", image: UIImage(systemName: "shippingbox"), selectedImage: nil)` (locks the tab item title + SF Symbol; the LoadListViewController's own `title` property is set inside Plan 03's VC `viewDidLoad`, but the tab item is the ancestor's responsibility).
       - Return `vc`.

       **For Factoring (1 file — the outlier):** DO NOT route through `makeLoadsTab`. Locate the existing placeholder line `ShipperTabBarController.makeTab(title: "Invoices", systemImage: "doc.text.magnifyingglass"),` in the `viewControllers` array literal inside `viewDidLoad()`. Replace it with an inline construction that lifts the tab into a local variable named `invoicesTab` declared on the line ABOVE the `viewControllers = [...]` assignment, then references `invoicesTab` as the first element of the array. The inline construction does the following:
       - Call the factory: `let invoicesTab: UIViewController = loadListScreenFactory?(role) ?? ShipperTabBarController.makeTab(title: "Invoices", systemImage: "doc.text.magnifyingglass")` — the `?? ShipperTabBarController.makeTab(...)` fallback uses the SAME `"Invoices"` title literal currently in source (locks pre-Phase-8 placeholder behavior for any test that doesn't inject the factory).
       - Set `invoicesTab.title = "Invoices"` (T-08-12: locks the nav-bar title).
       - Set `invoicesTab.tabBarItem = UITabBarItem(title: "Invoices", image: UIImage(systemName: "doc.text.magnifyingglass"), selectedImage: nil)` (T-08-12: locks the tab item title to the EXACT literal that `RoleShellSmokeTests.swift:151`'s `app.tabBars.buttons["Invoices"]` assertion depends on). The string `"Invoices"` appears TWICE in the resulting source (once on `.title`, once on `.tabBarItem.title`) — both must be the same literal; the `done` clause greps for `>= 2` occurrences.
       - The other three Factoring tabs (`Carriers`, `Chain`, `Assistant`) remain unchanged in the `viewControllers` array — they continue calling `ShipperTabBarController.makeTab(title: ..., systemImage: ...)` exactly as today.
       - Add a comment block above the inline block citing: "PATTERNS.md Q1 + T-08-12 / RoleShellSmokeTests.swift:151. The factoring \"Invoices\" tab is backed by the real `LoadListViewController(.factoring)` — the loads ARE invoices for the factoring role per the existing fixture set (Plan 01 D-04). The TAB ITEM TITLE remains `\"Invoices\"` exactly (RoleShellSmokeTests:151 assertion)."

    5. `wrapTabsWithNavAndInstallAvatar { ... }` and the `ProfileViewController(logoutService:kycStatusScreenFactory:)` call inside it are NOT touched — the new Loads VC slots into the existing nav wrap automatically because the `viewControllers` array element type is unchanged (`UIViewController`). PATTERNS.md §8 line 632 confirms.

    Per T-08-11: each tab bar invokes `loadListScreenFactory?(self.role)` — that is, `loadListScreenFactory?(.broker)` from BrokerTabBarController (because `self.role == .broker` from the existing `let role: Role = .broker` line), `loadListScreenFactory?(.shipper)` from ShipperTabBarController, etc. The compile-time guarantee is that `self.role` is the tab bar's own stored `Role` property (a constant `let`, not mutable); the runtime guarantee is locked by the XCUITest in Task 3 which asserts the resolved row identifiers come from the role-appropriate fixture.

    Per T-08-12: the Factoring file's `viewDidLoad()` MUST set both `invoicesTab.title = "Invoices"` AND `invoicesTab.tabBarItem.title = "Invoices"` (literal string `"Invoices"` appears at least twice). A grep gate in the `done` clause locks the literal count.
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerUITests/RoleShellSmokeTests test -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    `grep -c 'loadListScreenFactory: ((Role) -> UIViewController)?' validationLedger/Roles/Broker/BrokerTabBarController.swift == 1` AND same for each of the 4 sibling files = 5 total occurrences across the 5 files; `grep -c 'static func makeLoadsTab' validationLedger/Roles/Shipper/ShipperTabBarController.swift == 1` (single source of truth); `grep -c 'makeLoadsTab(loadListScreenFactory:' validationLedger/Roles/Broker/BrokerTabBarController.swift >= 1` (and same for Shipper/Carrier/Dispatch — 4 total invocations of `makeLoadsTab`); `grep -c '"Invoices"' validationLedger/Roles/Factoring/FactoringTabBarController.swift >= 2` (title string preserved — T-08-12); `grep -c 'Self.makeTab(title: "Loads"' validationLedger/Roles/Broker/BrokerTabBarController.swift == 0` (the old placeholder is gone — the new path is via `makeLoadsTab` helper); the existing `RoleShellSmokeTests` suite passes — i.e. all 5 role-shell smoke tests including the Factoring `"Invoices"` assertion on line 151 still pass without modification. The pre-existing tab-bar-button existence assertions (`Loads`, `Brokers`, `BOL`, etc. for each role) MUST continue to pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: RoleLoadsTabSmokeTests — 5-role XCUITest asserting loads-list identifier resolves per role + row/empty-state resolution</name>
  <files>validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift</files>
  <read_first>
    - validationLedgerUITests/RoleShellSmokeTests.swift (every line — the verbatim XCUITest template; copy `setUp()` + `launch(role:)` + `driveFullOTPFlow(_:)` patterns; lines 1–157)
    - .planning/phases/08-role-filtered-load-list/08-UI-SPEC.md (lines 202–210 — the locked accessibility-identifier set this test probes: `loads-list`, `loads-list.row.{loadID}`, `loads-list.empty-state`, `loads-list.error-state`, `loads-list.error-state.retry`)
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "13. LoadsListSmokeTests.swift" lines 849–921 — the per-role test method pattern; the open question lines 920 about the Factoring tab name — RESOLVED here: tap `"Invoices"`, not `"Loads"`)
    - validationLedgerTests/Networking/Fixtures/loads-list-broker.json (the populated fixture — confirm at least one `VL-` prefixed load id exists in the row set)
    - validationLedgerTests/Networking/Fixtures/loads-list-empty.json (the empty fixture — drives the `loads-list.empty-state` assertion; confirm via Plan 01's lockstep edit that an empty fixture is what gets served for some role under `-MockOTPRoleForUITest` — if the existing MockOTPRoleFixtureRegistry serves the populated list for every role, the empty-state assertion lives in a separate test method that uses a different launch arg or skip the empty-state assertion in this plan and surface as an open question)
    - validationLedger/Core/Networking/Mock/MockOTPRoleFixtureRegistry.swift (confirm which fixtures are served per role under `-MockOTPRoleForUITest <role>`; if it serves the populated `loads-list-{role}.json` for every role, the empty-state probe must be a separate dedicated test path; if a role gets the empty fixture organically, that role's test asserts the empty-state identifier instead of the row identifier)
    - .planning/phases/08-role-filtered-load-list/08-CONTEXT.md (D-04 — the per-role fixture authoring; populated fixtures land for each role under `-MockOTPRoleForUITest`)
  </read_first>
  <action>
    Create `validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift`. The file lives under a new `Loads/` directory inside `validationLedgerUITests/` (mirrors the unit-test pattern `validationLedgerTests/Loads/`). Create the `Loads/` directory if it does not exist; the PBXFileSystemSynchronizedRootGroup auto-discovers files (Plan 01 Task 1 precedent — no `project.pbxproj` edit needed).

    File-level comment: cite Phase 8 LOAD-03 — the integration cap that asserts each of the 5 role shells renders the real Loads tab post-OTP. Reference `RoleShellSmokeTests.swift` as the template; explicitly call out that this file does NOT duplicate the tab-button existence assertions (those stay in `RoleShellSmokeTests`) — this file extends the smoke flow to tap into the Loads/Invoices tab and assert `loads-list` resolution.

    Imports: `import XCTest` only. Declare a single XCTestCase subclass: `final class RoleLoadsTabSmokeTests: XCTestCase` (XCTest, NOT Swift Testing — XCUITest is XCTest-only; `XCUIApplication` does not bridge to Swift Testing in iOS 17).

    Setup mirroring `RoleShellSmokeTests.swift` lines 31–34 exactly (read that file for the exact body): override `setUp()`; call `super.setUp()`; set `executionTimeAllowance = 30` (Warning 5 — 30s hard cap per test); set `continueAfterFailure = false`.

    Private helpers — COPY VERBATIM from `RoleShellSmokeTests.swift` lines 40–76 (read that file for the literal bodies; do not re-implement from scratch):
    - `private func launch(role: String) -> XCUIApplication` — instantiates `XCUIApplication`, sets `launchArguments = ["-MockOTPRoleForUITest", role]`, calls `app.launch()`, returns the app.
    - `private func driveFullOTPFlow(_ app: XCUIApplication)` — drives the phone-entry → submit → OTP-entry → verify flow using the locked accessibility identifiers `"phone-entry-field"`, `"phone-entry-submit"`, `"otp-field"`, `"otp-verify"`. Uses the same 10s cold-launch timeout for the phone field and 5s mock-backed transition timeout for the OTP field.

    Implement FIVE test methods (one per role). The shape is identical across all five — they vary ONLY in the launch-role string and the tab button text:

    | Test method                                          | Launch role arg | Tab button text tapped | Identifier asserted resolves | Row predicate |
    |------------------------------------------------------|-----------------|------------------------|-------------------------------|---------------|
    | `test_brokerLoadsTabRendersList()`                   | `"broker"`      | `"Loads"`              | `loads-list`                  | `identifier BEGINSWITH 'loads-list.row.VL-'` |
    | `test_shipperLoadsTabRendersList()`                  | `"shipper"`     | `"Loads"`              | `loads-list`                  | `identifier BEGINSWITH 'loads-list.row.VL-'` |
    | `test_carrierLoadsTabRendersList()`                  | `"carrier"`     | `"Loads"`              | `loads-list`                  | `identifier BEGINSWITH 'loads-list.row.VL-'` |
    | `test_dispatchLoadsTabRendersList()`                 | `"dispatch"`    | `"Loads"`              | `loads-list`                  | `identifier BEGINSWITH 'loads-list.row.VL-'` |
    | `test_factoringInvoicesTabRendersList()`             | `"factoring"`   | `"Invoices"`           | `loads-list`                  | `identifier BEGINSWITH 'loads-list.row.VL-'` |

    Per-test body sequence (every test follows this 5-step shape — read `RoleShellSmokeTests.swift` lines 95–155 for the literal XCUITest call patterns):

    1. `let app = launch(role: ${role})` — boot the app with the role launch arg.
    2. `driveFullOTPFlow(app)` — drive past the OTP gate to land on the role tab shell.
    3. Wait for the tab bar button to exist: `XCTAssertTrue(app.tabBars.buttons[${tab}].waitForExistence(timeout: 5), "${role} tab bar should render with a '${tab}' tab after OTP verify")`. Use the literal tab string from the table — `"Loads"` for the first 4 tests, `"Invoices"` for the factoring test (T-08-12: the factoring test EXPLICITLY taps `"Invoices"` — same literal as `RoleShellSmokeTests.swift:151`).
    4. Tap the tab: `app.tabBars.buttons[${tab}].tap()`.
    5. Assert the collection view resolves: `let list = app.collectionViews["loads-list"]; XCTAssertTrue(list.waitForExistence(timeout: 5), "loads-list collection view should appear after tapping the ${tab} tab")` — every test probes the SAME `"loads-list"` accessibility identifier (the VC's identifier does not change by role; the factoring tab also resolves to a `LoadListViewController` whose collection view has identifier `"loads-list"` — Plan 03 locked this).
    6. Assert at least one row identifier resolves: instantiate `let predicate = NSPredicate(format: "identifier BEGINSWITH 'loads-list.row.VL-'")`; query `let rows = app.cells.matching(predicate)`; assert `XCTAssertGreaterThan(rows.count, 0, "At least one loads-list.row.VL-* identifier should resolve from the populated ${role} fixture")`. The populated per-role fixture (`loads-list-{role}.json` from Plan 01) carries at least one `VL-`-prefixed `load.id`; each cell sets its own `accessibilityIdentifier = "loads-list.row.\(load.id)"` (Plan 03 Task 3).

    Per T-08-12 hardening: the factoring test's tab-button assertion uses the literal `"Invoices"` (string, capital I, no leading/trailing whitespace) — the SAME literal that `RoleShellSmokeTests.swift:151` asserts. Together the two test sites provide DOUBLE COVERAGE of the locked tab title; a regression to `"Loads"` for factoring would fail BOTH suites.

    **Empty-state probe — DEFERRED, NOT implemented in this test file.** The existing `MockOTPRoleFixtureRegistry.registerForRole(_:)` (Phase 3 D-32) serves the populated `loads-list-{role}.json` fixture for every role under `-MockOTPRoleForUITest <role>` — there is no organic role launch path that lands on the empty fixture. Do NOT add a sixth test method for the empty-state assertion in this plan. Instead, create or append to `validationLedger/Features/Loads/.spike-a1-notes.md` (Plan 03 Task 1 already established this notes file) a new section titled "Empty-state XCUITest probe (deferred from Plan 04)" recording: "`-MockOTPRoleForUITest <role>` always serves the populated fixture. A future test path that injects a launch arg like `-MockEmptyLoadsForUITest <role>` to swap in `loads-list-empty.json` would close the empty-state assertion gap. For Phase 8, the populated-fixture row-resolution assertion (this file's 5 tests) plus the empty-state UNIT test (`Test_loadingToEmptyOnEmptyFixture` in Plan 03 Task 2's `LoadListViewModelTests.swift`) together cover the LOAD-07 contract."

    Per T-08-11: each test method's role-launch-arg determines which `*TabBarController` is constructed at app startup, which determines which `role` is passed into `loadListScreenFactory?(self.role)` in `viewDidLoad()` (Task 2), which determines the role parameter on `LoadListViewModel(role:)` (Task 1 / Plan 03), which determines the `LoadListEndpoint(role:)` URL the VM hits. The populated fixture served per role under `-MockOTPRoleForUITest <role>` is Phase 7's role-filtered fixture wrapped in the Plan 01 envelope; the `loads-list.row.VL-*` identifiers resolved per role prove the end-to-end role routing is correct.

    No `assertProfileFlowAndLogout` call in any of these test methods — that helper lives in `RoleShellSmokeTests` and is scoped to the profile flow. This file's scope is strictly the Loads-tab probe.
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerUITests/RoleLoadsTabSmokeTests test -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    File `validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift` exists. `grep -c '^final class RoleLoadsTabSmokeTests: XCTestCase' validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift == 1`; `grep -c 'func test_' validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift == 5` (one per role); `grep -c 'app.tabBars.buttons\["Invoices"\]' validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift >= 1` (T-08-12 — factoring taps Invoices); `grep -c 'app.tabBars.buttons\["Loads"\]' validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift == 4` (Broker/Shipper/Carrier/Dispatch tap Loads); `grep -c 'app.collectionViews\["loads-list"\]' validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift == 5` (every test probes the same shared identifier); `grep -c "loads-list.row.VL-" validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift >= 5` (row-identifier predicate per role); the scoped suite passes — all 5 tests exit 0 under the simulator-lane scoped invocation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Role` (composition-root authority) → `loadListScreenFactory(role)` closure → `LoadListViewModel(role: role)` → `LoadListEndpoint(role: role)` URL | The role flows from the composition root (AppCoordinator's switch) through the closure to the endpoint URL. A bug that crosses the wires (e.g. `BrokerTabBarController` invoking `loadListScreenFactory(.shipper)`) would silently serve the wrong role's fixture / endpoint. This is a Privilege-Elevation / Spoofing surface: a broker user could in principle be served the shipper's view of the world. Mitigated by sourcing the role from each tab bar's `let role: Role = .broker` (etc.) stored property — a single source of truth per file — and locked by the XCUITest's per-role row-resolution assertions. |
| Tab title string `"Invoices"` (FactoringTabBarController) | The string is a product-surface contract: the existing `RoleShellSmokeTests.swift:151` test depends on it, and the factoring users see this exact word on their bottom bar. Regressing it to `"Loads"` would (a) break the existing UI test and (b) confuse the factoring user base. The string is locked at TWO test sites (the pre-existing RoleShellSmokeTests + the new RoleLoadsTabSmokeTests). |
| `wrapTabsWithNavAndInstallAvatar { ... }` (RoleCoordinator helper) | The shared helper is NOT touched in this plan. A bug here (e.g. a sibling future plan removing the helper call) would orphan the Loads VC from the nav wrapper, breaking the avatar affordance + the nav title. Mitigation: this plan adds no changes to `RoleCoordinator.swift`; verified by `git diff` containing no edits there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Spoofing / Privilege elevation | Each `*TabBarController.viewDidLoad()` invocation of `loadListScreenFactory?(self.role)` | mitigate | Each tab bar invokes the closure with its OWN stored `role` (`let role: Role = .broker` etc. — already correct in source as of Phase 3). The new helper `makeLoadsTab(loadListScreenFactory:role:)` requires the caller to pass `role` explicitly — the caller's role property is the source of truth. The 5-role XCUITest in Task 3 asserts each role launch resolves at least one `loads-list.row.VL-*` identifier from the role-appropriate populated fixture — proving the role flows correctly end-to-end. A future refactor that introduced a `role` parameter mismatch would fail this XCUITest. |
| T-08-12 | Tampering / regression | `FactoringTabBarController` tab item title | mitigate | The Factoring file's `viewDidLoad()` block EXPLICITLY sets `invoicesTab.tabBarItem.title = "Invoices"` (literal string preserved). Two test sites lock the string: (a) the pre-existing `RoleShellSmokeTests.swift:151` `app.tabBars.buttons["Invoices"].waitForExistence` — unchanged by this plan; (b) the new `RoleLoadsTabSmokeTests.test_factoringInvoicesTabRendersList` — adds redundant coverage. A regression to `"Loads"` for factoring would fail BOTH tests. PATTERNS.md Q1 lines 634 explicitly chose option (b) — keep title `"Invoices"`, back with `LoadListViewController(.factoring)`. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies introduced in this plan (CLAUDE.md SwiftPM-only + STACK-04). All changes are inside existing first-party files (AppContainer, AppCoordinator, 5 TabBarControllers) + one new XCUITest file using `XCTest` + `XCUIApplication` (iOS-bundled). Package legitimacy gate vacuously satisfied. |
</threat_model>

<verification>
1. Build green: `xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' build -parallel-testing-enabled NO` exits 0.
2. Existing role-shell smoke tests still pass: scoped `-only-testing:validationLedgerUITests/RoleShellSmokeTests` exits 0 — the Factoring `"Invoices"` assertion on line 151 holds (T-08-12); all 5 role-shell smoke tests pass without modification (T-08-11 is consistent with the existing role routing).
3. New role-loads smoke tests pass: scoped `-only-testing:validationLedgerUITests/RoleLoadsTabSmokeTests` exits 0 — 5 tests, one per role; each asserts `loads-list` resolves AND at least one `loads-list.row.VL-*` identifier resolves; factoring asserts `"Invoices"` not `"Loads"`.
4. No regressions in dependent UI test suites: scoped `-only-testing:validationLedgerUITests/KYCHardGateUITests` + `-only-testing:validationLedgerUITests/KYCProfileEntryUITests` + `-only-testing:validationLedgerUITests/LimitedTrustBannerTests` still pass — the `*TabBarController.init(...)` signature change (new third arg defaults to `nil`) is source-compatible with existing callers.
5. Source-level integrity: `grep -c 'static func makeLoadsTab' validationLedger/Roles/Shipper/ShipperTabBarController.swift == 1` (single source); `grep -c 'ShipperTabBarController.makeLoadsTab' validationLedger/Roles/{Broker,Carrier,Dispatch}/*TabBarController.swift | wc -l == 3` (the 3 non-Shipper Loads-tab-having roles delegate to Shipper's helper, mirroring the existing `makeTab` discipline); the Factoring file does NOT call `makeLoadsTab` (it has its own inline `"Invoices"` branch to preserve the title literal).
</verification>

<success_criteria>
- `AppContainer.makeLoadListScreen(role:)` exists and constructs the LoadListViewController per the kycStatusScreenFactory pattern.
- `AppCoordinator.roleCoordinator(for:container:)` threads `loadListScreenFactory: (Role) -> UIViewController` into all 5 `*TabBarController.init(...)` call sites.
- All 5 `*TabBarController` files accept the new factory arg, store it, and invoke it from `viewDidLoad()` to back the Loads / Invoices tab.
- Factoring's tab title remains `"Invoices"` literal (T-08-12 — locked by 2 test sites).
- The new 5-role `RoleLoadsTabSmokeTests` asserts each role resolves `loads-list` AND at least one `loads-list.row.VL-*` row identifier from the role-appropriate populated fixture (T-08-11).
- The pre-existing `RoleShellSmokeTests` (including line 151 `"Invoices"`) continues to pass unmodified.
- LOAD-03 — "each of the 5 roles sees a role-filtered list rendered in its tab shell" — is verifiably satisfied end-to-end.
- Zero new SwiftPM dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/08-role-filtered-load-list/08-04-SUMMARY.md` when done. Record:
- `AppContainer.makeLoadListScreen(role:)` signature + the chosen `LoggingSubsystem.app` reuse (no new `.loads` subsystem in this phase).
- The `loadListFactory: (Role) -> UIViewController` closure shape in AppCoordinator + the `[weak container]` capture discipline.
- The new `static func makeLoadsTab(loadListScreenFactory:role:)` helper signature in `ShipperTabBarController.swift` + the 4 sibling call sites that delegate to it (the same `ShipperTabBarController.makeTab` discipline).
- The explicit Factoring "Invoices" preservation block — the literal string is set TWICE (`vc.title` and `vc.tabBarItem`) so a future refactor that reads only one of them still preserves the string.
- The 5-test XCUITest matrix (one per role) + the role → tab name mapping (Loads × 4, Invoices × 1) + the row-identifier predicate.
- The deferred empty-state UI probe (`MockOTPRoleFixtureRegistry` serves populated fixtures only; the empty-state contract is unit-tested in Plan 03 Task 2's `Test_loadingToEmptyOnEmptyFixture`; a future plan can add the XCUITest empty-state probe via a `-MockEmptyLoadsForUITest <role>` launch arg if needed).
- Any open question that surfaced (e.g. whether the Factoring tab's SF Symbol `doc.text.magnifyingglass` should be reconciled with the broker-side `shippingbox` — UI-SPEC says no, the symbols are role-correct; defer until product feedback).
</output>
</content>