---
phase: 10-per-role-tender-accept-reject
plan: 06
type: execute
wave: 5
depends_on:
  - 10-04
  - 10-05
files_modified:
  - validationLedger/Features/Loads/Detail/TenderSheetViewController.swift
  - validationLedger/Features/Loads/Detail/TenderSheetCarrierRowView.swift
  - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
  - validationLedgerTests/Loads/TenderSheetViewControllerTests.swift
  - validationLedgerTests/Loads/TenderEligibilityGatingTests.swift
autonomous: true
requirements:
  - ACTION-01
  - ACTION-04
user_setup: []

must_haves:
  truths:
    - "Per D-06 + UI-SPEC line 438-456: `TenderSheetViewController` is presented via `UISheetPresentationController` `.medium` detent (`.large` available); `largestUndimmedDetentIdentifier = .medium`; `prefersGrabberVisible = true`; the SAME 7-line recipe Phase 9 uses verbatim per Pitfall 7 lock — DO NOT refactor into a helper."
    - "Per D-08 + ACTION-04 (CRITICAL — T-10-04): unverified / flagged / pending carriers in the picker are VISIBLE-BUT-DISABLED with an inline reason; the carrier's `VerificationBadgeView` stays at full alpha (the badge is the SIGNAL — must not dim); the contentView's alpha drops to 0.6; `userInteractionEnabled = false`. Per UI-SPEC line 446 + line 549-550."
    - "Per D-11 + UI-SPEC line 453: Send button is enabled only when (a) a carrier is selected AND (b) the carrier's verificationState is .verified AND (c) the resolved deadline is in the future. Helper line below the Send button explains which gate is closed (per UI-SPEC line 371-373 LOCKED copy)."
    - "Per D-10 + UI-SPEC line 447-449: deadline picker = 5 chips (1h / 4h / 1 day / 2 days / Custom…) with 1 day default; `UIDatePicker.compact` reveals on Custom… tap; the resolved-time line below the chips ('↳ Tomorrow, 5:00 PM') uses the same DateFormatter as the respond-by countdown (Plan 04 RespondByFormatter)."
    - "Per D-09 (load-level gate already enforced in Plan 04's action region): the sheet does NOT attempt to enforce ACTION-07 at the LOAD level — the tender BUTTON in the action region is hard-disabled before the sheet is ever presented when `Load.tenderEligibility.canTender == false`. The sheet ONLY enforces the CARRIER-LEVEL gate (D-08). Two independent gates per CONTEXT D-09."
    - "Per Pitfall 8 + UI-SPEC line 452: the Send-button helper line uses a FIXED-HEIGHT label with `alpha 0/1` toggling — NOT `isHidden` toggling — so the sheet detent does not reflow when the user picks a carrier."
    - "Per Open Question 4 (RESEARCH § Open Questions) + UI-SPEC line 454-455: the sheet exposes `onSend: (LoadActionEndpoint.RequestBody) async -> Void` and `onCancel: () -> Void` closures; on Send the sheet awaits the closure (the parent's `viewModel.submit(...)`); on 200 the parent VC imperatively dismisses the sheet from its `.loaded` render path; on failure the parent keeps the sheet visible and slides in the toast banner (Plan 07) over the parent."
    - "Per UI-SPEC line 444 (D-08 carrier row composition): each carrier row composes `RoleAvatarView(.tree)` (with the carrier's verificationState as the status dot) + `VerificationBadgeView` (Phase 8 — reused via DS.Colors.Verification.color(for:) per the 9.1 R12 lock) + carrier name + (if disabled) reason line."
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/TenderSheetViewController.swift"
      provides: "Modal sheet content VC: carrier list (UITableView.style = .insetGrouped), deadline section (5 chips + Custom… reveal), resolved-time line, Send + Cancel buttons; closure-callback shape `(onSend: async, onCancel)`; internal sheet VM for picker/deadline state"
      contains: "final class TenderSheetViewController"
    - path: "validationLedger/Features/Loads/Detail/TenderSheetCarrierRowView.swift"
      provides: "UITableViewCell composing RoleAvatarView + displayName + VerificationBadgeView + (if disabled) reason line; reuses DS.Colors.Verification.color(for:) per 9.1 R12 lock"
      contains: "TenderSheetCarrierRowView"
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      provides: "`presentTenderSheet()` stub (from Plan 04) now fully implemented: fetch directory via CarrierDirectoryEndpoint, construct TenderSheetViewController, present via `UISheetPresentationController` with the 7-line recipe (inlined — Pitfall 7); on send call `viewModel.submit(action: .tender, body:)`; on 200 dismiss the sheet"
      contains: "presentTenderSheet"
    - path: "validationLedgerTests/Loads/TenderSheetViewControllerTests.swift"
      provides: "Unit tests for picker rendering (mixed verification states), Send-button gate (3 conditions), helper-line copy resolver, deadline chip selection, resolved-time formatter integration"
      contains: "TenderSheetViewControllerTests"
    - path: "validationLedgerTests/Loads/TenderEligibilityGatingTests.swift"
      provides: "Per VALIDATION.md ACTION-04 row: explicit coverage of the two-gate model — Plan 04's load-level gate (Tender button isEnabled = false when canTender == false) + this plan's carrier-level gate (no .verified carriers => Send permanently disabled)"
      contains: "TenderEligibilityGatingTests"
  key_links:
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      to: "validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift"
      via: "presentTenderSheet fetches the directory via apiClient.request(CarrierDirectoryEndpoint())"
      pattern: "CarrierDirectoryEndpoint\\(\\)"
    - from: "validationLedger/Features/Loads/Detail/TenderSheetViewController.swift"
      to: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift"
      via: "onSend closure awaits viewModel.submit(action: .tender, body:)"
      pattern: "onSend.*RequestBody"
    - from: "validationLedger/Features/Loads/Detail/TenderSheetCarrierRowView.swift"
      to: "validationLedger/UI/Components/VerificationBadgeView.swift + RoleAvatarView (Phase 9.1)"
      via: "per-row composition reuses both Phase 8 + 9.1 components (TRUST-02 single-badge invariant)"
      pattern: "VerificationBadgeView"
---

<objective>
Build `TenderSheetViewController` and `TenderSheetCarrierRowView` per UI-SPEC § Component Geometry lines 433-456; wire the sheet's presentation from `LoadDetailViewController.presentTenderSheet()` (replacing the Plan 04 stub) using the verbatim 7-line `UISheetPresentationController` recipe Phase 9 established; enforce ACTION-04 at the carrier-level gate (D-08 — visible-but-disabled with inline reason); add `TenderEligibilityGatingTests` as the named VALIDATION.md ACTION-04 invariant covering BOTH gates (load-level from Plan 04 + carrier-level from this plan).

Purpose: ACTION-04 is the CRITICAL platform-thesis requirement (T-10-04). The carrier picker is where the broker FIRST meets "I cannot tender to this party." Per Specifics line 263, this is not "set isEnabled = false" treatment — it requires the same care Phase 9's chain-of-trust card received: the badge stays at full alpha (the SIGNAL must not dim), the reason line reads naturally, the row stays visible (hiding the unverified carriers would let the user assume "there were just no other carriers").

Output: 2 new VC/view files; 1 modified VC (filling the Plan 04 stub); 2 new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md
@.planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md
@.planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md
@.planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md
@.planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md
@.planning/phases/10-per-role-tender-accept-reject/10-04-SUMMARY.md
@.planning/phases/10-per-role-tender-accept-reject/10-05-SUMMARY.md

# Files this plan modifies
@validationLedger/Features/Loads/Detail/LoadDetailViewController.swift

# Analog files (read for shape)
@validationLedger/Features/Loads/Detail/VerificationBasisSheetViewController.swift
@validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift
@validationLedger/UI/Components/VerificationBadgeView.swift
@validationLedger/Features/Loads/Detail/LoadActionsView.swift
@validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift
@validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift
@validationLedger/Core/Load/ChainOfTrust.swift

<interfaces>
<!-- Sheet presentation contract — the 7-line recipe Phase 9 established, inlined per Pitfall 7 -->

Verbatim from `LoadDetailViewController.swift:1321-1333` (Phase 9 D-08 recipe):
```swift
sheetVC.modalPresentationStyle = .pageSheet
if let sheet = sheetVC.sheetPresentationController {
    sheet.detents = [.medium(), .large()]
    sheet.selectedDetentIdentifier = .medium
    sheet.prefersGrabberVisible = true
    sheet.largestUndimmedDetentIdentifier = .medium
    sheet.prefersScrollingExpandsWhenScrolledToEdge = false
    sheet.prefersEdgeAttachedInCompactHeight = true
    sheet.widthFollowsPreferredContentSizeWhenEdgeAttached = true
}
present(sheetVC, animated: true)
```

TenderSheetViewController init signature (Open Question 4 resolution):
```swift
public init(
    directory: [TrustNode],
    onSend: @escaping (LoadActionEndpoint.RequestBody) async -> Void,
    onCancel: @escaping () -> Void
)
```

Reused components:
- `VerificationBadgeView` (Phase 8; `UI/Components/VerificationBadgeView.swift`) — renders the 4-state badge; consumed in the carrier row
- `RoleAvatarView` (Phase 9.1; `UI/Components/RoleAvatarView.swift`) — the `.tree` 32pt variant; consumed in the carrier row
- `DS.Colors.Verification.color(for:)` (Phase 9.1) — the badge color; reused via VerificationBadgeView

UI-SPEC § Tender sheet copy (LOCKED — lines 356-373) — every string in the sheet uses these keys:
- Sheet nav title: `loads.detail.tender.sheet.title` → "Send tender"
- Carrier header: `loads.detail.tender.section.carrier` → "Carrier"
- Deadline header: `loads.detail.tender.section.deadline` → "Respond by"
- Chip 1h: `loads.detail.tender.deadline.chip.1h` → "1 hour"
- Chip 4h: `loads.detail.tender.deadline.chip.4h` → "4 hours"
- Chip 24h (default): `loads.detail.tender.deadline.chip.24h` → "1 day"
- Chip 48h: `loads.detail.tender.deadline.chip.48h` → "2 days"
- Custom row: `loads.detail.tender.deadline.chip.custom` → "Custom…"
- Resolved time: `loads.detail.tender.deadline.resolved.format` → "↳ %@"
- Send button: `loads.detail.tender.button.send` → "Send tender"
- Cancel button: `loads.detail.tender.button.cancel` → "Cancel"
- Helper (no carrier): `loads.detail.tender.helper.no_carrier` → "Pick a carrier to send."
- Helper (unverified carrier): `loads.detail.tender.helper.unverified_carrier` → "Pick a verified carrier."
- Helper (past deadline): `loads.detail.tender.helper.past_deadline` → "Pick a future deadline."
- Carrier disabled (unverified): `loads.detail.tender.carrier.disabled.unverified` → "Not verified — cannot tender"
- Carrier disabled (flagged): `loads.detail.tender.carrier.disabled.flagged` → "Flagged — cannot tender"
- Carrier disabled (pending): `loads.detail.tender.carrier.disabled.pending` → "Verification pending — cannot tender"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TenderSheetCarrierRowView + TenderSheetViewController with carrier-level ACTION-04 gate</name>
  <files>validationLedger/Features/Loads/Detail/TenderSheetCarrierRowView.swift, validationLedger/Features/Loads/Detail/TenderSheetViewController.swift, validationLedgerTests/Loads/TenderSheetViewControllerTests.swift</files>
  <read_first>
    - validationLedger/Features/Loads/Detail/VerificationBasisSheetViewController.swift (FULL — analog sheet VC; mirror its file-header structure, accessibility-identifier namespace, threat-model anchors)
    - validationLedger/UI/Components/VerificationBadgeView.swift (FULL — confirm how `.configure(state:)` is called; the carrier row composes it)
    - validationLedger/UI/Components/RoleAvatarView.swift (FULL — Phase 9.1; `.tree` variant 32pt; status-dot color via `DS.Colors.Verification.color(for:)`)
    - validationLedger/Features/Loads/Detail/LoadActionsView.swift (Plan 04 output — note the `RespondByFormatter` file-private helper; the sheet's resolved-time line reuses the SAME formatter)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §5 (TenderSheetViewController file-header pattern + onSend closure shape); §6 (sheet presentation 7-line recipe + the "do NOT refactor into a helper" Pitfall 7 lock)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Component Geometry lines 433-456 (LOCKED); § Tender sheet copy lines 356-386 (LOCKED keys)
    - .planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md § Pitfall 8 (Send-button helper line FIXED-HEIGHT + alpha 0/1 toggle, NOT isHidden — sheet detent stability)
  </read_first>
  <behavior>
    Tests in TenderSheetViewControllerTests (NEW — write first):

    Setup: each test constructs the sheet with a 6-carrier directory: 2 .verified (Acme + Northwind), 2 .pending (Cascadia + Summit), 1 .unverified (Anonymous), 1 .flagged (Chameleon). Mirror the Plan 05 fixture exactly so the tests reuse the same roster.

    - Test 1 (`test_sheet_rendersAllSixCarriersInPicker`): the table view has 6 rows; row count matches directory count (D-08 — unverified/flagged/pending carriers are VISIBLE, not hidden).
    - Test 2 (`test_verifiedCarrier_isSelectable_otherIsDisabled`): the Acme row's `userInteractionEnabled == true`; the Chameleon row's `userInteractionEnabled == false`; the Acme row's contentView alpha is 1.0; the Chameleon row's contentView alpha is 0.6; the Chameleon row's VerificationBadgeView alpha is 1.0 (per UI-SPEC line 446 — the badge stays at full alpha).
    - Test 3 (`test_disabledCarrier_inlineReason_matchesVerificationState`): the Chameleon row's reason label reads "Flagged — cannot tender" (`loads.detail.tender.carrier.disabled.flagged`); the Anonymous row reads "Not verified — cannot tender"; the Cascadia row reads "Verification pending — cannot tender"; the Acme row's reason label is NOT visible.
    - Test 4 (`test_sendButton_initiallyDisabled_noCarrierSelected`): on init (no carrier selected), Send button `isEnabled == false`; helper line below Send reads `loads.detail.tender.helper.no_carrier` ("Pick a carrier to send.").
    - Test 5 (`test_selectingVerifiedCarrier_enablesSend`): programmatically select the Acme row (tap or set internal state); after default 1-day deadline is in the future; assert Send `isEnabled == true`; helper line alpha is 0 (per Pitfall 8 — fixed height with alpha toggle, NOT isHidden).
    - Test 6 (`test_sendButton_helperCopy_pastDeadlineGate`): select a verified carrier, programmatically set the resolved deadline to a past date; assert Send `isEnabled == false`; helper line reads `loads.detail.tender.helper.past_deadline` ("Pick a future deadline.").
    - Test 7 (`test_deadlineChips_renderFive_oneDayDefault`): assert exactly 5 chips with titles "1 hour" / "4 hours" / "1 day" / "2 days" / "Custom…"; the "1 day" chip is initially selected (per UI-SPEC line 447).
    - Test 8 (`test_chipTap_updatesResolvedTimeLine`): tap the "4 hours" chip; assert the resolved-time label updates to a format containing the 4-hour-future formatted date.
    - Test 9 (`test_customChip_revealsDatePicker`): tap the "Custom…" chip; assert a `UIDatePicker` becomes visible below the chip row (per UI-SPEC line 449).
    - Test 10 (`test_onSendClosure_invokedWithCorrectRequestBody`): select Acme + a future deadline; capture the `onSend` closure invocation; assert the body's `actorRole`, `targetPartyID == Acme.partyID`, `respondByAt` matches the selected deadline, `note == nil`.
    - Test 11 (`test_onSend_keepsSheetVisibleDuringInFlight`): mock the onSend closure to suspend for 200ms; tap Send; during the suspension assert the sheet's `view.window != nil` (still presented) AND the Send button shows the in-flight spinner AND `isEnabled == false` (per UI-SPEC line 454).
    - Test 12 (`test_onCancelClosure_invokedOnCancelTap`): tap the nav-bar Cancel button; assert onCancel was called.
    - Test 13 (`test_sheetHelperLine_alphaToggleNotIsHidden`): on transition from no-carrier-selected (helper visible) to verified-carrier-selected (helper hidden), assert the helper label's `frame.height` does NOT change (Pitfall 8 — fixed-height + alpha guarantees layout stability).
    - Test 14 (`test_emptyDirectory_sendButton_permanentlyDisabled`): init with `directory: []`; assert Send `isEnabled == false`; helper line shows the no-carrier copy. Edge case for the carrier-directory empty case.
    - Test 15 (`test_directoryWithNoVerifiedCarriers_sendButton_permanentlyDisabled`): init with a directory containing ONLY .unverified / .flagged / .pending carriers; assert Send `isEnabled == false` no matter which row is tapped (since the only tappable rows are .verified). Per ACTION-04 carrier-level gate.

    File structure: place TenderSheetViewControllerTests at `validationLedgerTests/Loads/TenderSheetViewControllerTests.swift`; build a small in-test factory `makeDirectory(verifiedCount:pendingCount:unverifiedCount:flaggedCount:) -> [TrustNode]` (10-20 lines, file-private).
  </behavior>
  <action>
    Create `validationLedger/Features/Loads/Detail/TenderSheetCarrierRowView.swift`. UITableViewCell subclass per PATTERNS.md §5. Programmatic constructors. Composes (per UI-SPEC line 444):
    - `private let avatarView = RoleAvatarView(variant: .tree)` (32pt; the status-dot uses verificationState via `DS.Colors.Verification.color(for:)` — RoleAvatarView's 9.1 contract supports this; verify by reading RoleAvatarView.swift)
    - `private let nameLabel = UILabel()` (`DS.Typography.body`)
    - `private let badgeView = VerificationBadgeView()` (Phase 8 component)
    - `private let reasonLabel = UILabel()` (`DS.Typography.footnote`, `DS.Colors.labelSecondary`, indented to nameLabel's leading edge per UI-SPEC line 386)
    - Two stack views: outer vertical (name+badge row, reasonLabel); inner horizontal (avatar, name, badge, spacer)

    Method `func configure(carrier: TrustNode)`:
    - avatarView updated with carrier.verificationState
    - nameLabel.text = carrier.displayName
    - badgeView.configure(state: carrier.verificationState)
    - badgeView.alpha = 1.0 ALWAYS (UI-SPEC line 446)
    - If carrier.verificationState == .verified: contentView.alpha = 1.0, isUserInteractionEnabled = true, reasonLabel hidden
    - Else: contentView.alpha = 0.6, isUserInteractionEnabled = false, reasonLabel visible with text resolved per verificationState (NSLocalizedString keys per the interfaces block)

    Accessibility label per UI-SPEC line 456: `"[carrier name], [verification state], [disabled reason if any]"` — composed via `accessibilityLabel` set externally on the row.

    Create `validationLedger/Features/Loads/Detail/TenderSheetViewController.swift`. File header per PATTERNS.md §5: phase/plan citation, "Sheet presentation lives on the PRESENTING side" note, content-order block, accessibility-identifier namespace block (`load-detail.tender-sheet`, `…/carrier-row.<partyID>`, `…/deadline-chip.<token>`, `…/send-button`), threat-model anchors (T-10-04, T-09-04).

    **Accessibility-identifier wiring (per ISSUE-04 fix — REQUIRED, not optional):** The file-header namespace comment alone is NOT sufficient — every executor MUST set the following identifiers explicitly so plan 10-10 Test 1's XCUITest query `app.otherElements["load-detail.tender-sheet"]` (and the analogous per-row / per-chip / send-button queries) succeed without title-based fallback:
      - On the TenderSheetViewController root view: `view.accessibilityIdentifier = "load-detail.tender-sheet"` (set in `viewDidLoad()` after `view = UIView()` / programmatic view construction).
      - On each carrier row cell inside `tableView(_:cellForRowAt:)` (or in `TenderSheetCarrierRowView.configure(carrier:)`): `cell.accessibilityIdentifier = "load-detail.tender-sheet/carrier-row.\(carrier.partyID)"` — the partyID interpolation is the row's stable handle (matches Phase 9.1 convention).
      - On each respond-by deadline chip button (the 5 chips: 1h / 4h / 24h / 48h / Custom…): `chip.accessibilityIdentifier = "load-detail.tender-sheet/deadline-chip.\(token)"` where `token` is one of `"1h" | "4h" | "24h" | "48h" | "custom"`. Pick the token from the same enum that drives chip-title resolution.
      - On the Send button: `sendButton.accessibilityIdentifier = "load-detail.tender-sheet/send-button"`.
      These four identifier assignments are the wiring contract plan 10-10 depends on. Do not omit any; the header-comment namespace block is documentation, not implementation.

    Designated init signature per PATTERNS.md §5:
    ```swift
    public init(
        directory: [TrustNode],
        onSend: @escaping (LoadActionEndpoint.RequestBody) async -> Void,
        onCancel: @escaping () -> Void
    )
    ```
    `required init?(coder:)` trap.

    Internal structure:
    - `UINavigationItem` with title "Send tender" + leading "Cancel" UIBarButtonItem (system .cancel) wired to onCancel
    - `UIScrollView` containing a vertical `UIStackView`:
      - Carrier section header label ("Carrier")
      - Carrier list `UITableView.style = .insetGrouped`; estimatedRowHeight = 64; dataSource = self; delegate = self; registers `TenderSheetCarrierRowView`; rows return the registered cells per Task 1's row API
      - "Respond by" section header label
      - Deadline chip horizontal stack (5 buttons with the 5 LOCKED titles; UIButton.Configuration.bordered() for unselected, .filled() with `baseBackgroundColor = DS.Colors.primary` for selected; selection state via internal `selectedDeadlineChip` enum)
      - Resolved-time label ("↳ [formatted date]" — reuse RespondByFormatter from Plan 04 via @testable or by lifting RespondByFormatter to a shared file `Features/Loads/Detail/RespondByFormatter.swift` — PICK the cleanest: lift to a shared file with `internal` access)
      - (Custom… chip reveals an inline `UIDatePicker(date:, datePickerMode: .dateAndTime, preferredDatePickerStyle: .compact)` BELOW the chip row when selected)
    - Bottom: Send button (`UIButton.Configuration.filled()`, `baseBackgroundColor = DS.Colors.primary`, title "Send tender", min height 50pt, pinned to safeAreaLayoutGuide.bottomAnchor with `DS.Spacing.md` inset)
    - Bottom helper line label BELOW Send: `DS.Typography.footnote`, `DS.Colors.labelSecondary`, FIXED HEIGHT (e.g. 18pt — enough for 1 line at default Dynamic Type), alpha toggled 0/1 per Pitfall 8

    Internal state:
    - `private var selectedCarrierIndex: Int?` (the picked row)
    - `private var selectedDeadline: SelectedDeadline` (enum: case preset(TimeInterval) / case custom(Date))
    - Computed `var resolvedDeadlineDate: Date` (based on selectedDeadline)
    - Computed `var sendButtonState: SendButtonState` (enum: case enabled / case disabled(helperKey: String) — case disabled(.no_carrier) when no carrier; disabled(.unverified_carrier) when selectedCarrier is somehow not verified (defensive — shouldn't be reachable per D-08); disabled(.past_deadline) when resolvedDeadlineDate <= Date())
    - `updateSendButton()` reads the computed state, applies isEnabled + helper-label text + alpha

    Send button tap:
    - Disable send (in-flight visual); show spinner via `configuration.showsActivityIndicator = true`
    - Build `LoadActionEndpoint.RequestBody(actorRole: viewModel.role passed-in OR captured from the closure context — see step below, targetPartyID: selectedCarrier!.partyID, respondByAt: resolvedDeadlineDate, note: nil)`. **NOTE**: `actorRole` needs to come from somewhere — the sheet does NOT know the role intrinsically. Solution: change the init signature to accept `actorRole: Role` as an additional parameter (or include it inside the closure invocation by the parent — pick the cleanest). Per Open Question 4 the sheet awaits onSend; the cleanest shape is for the parent VC to inject the actorRole when constructing the request body inside its `viewModel.submit(...)` call, so the sheet's closure should produce a `(targetPartyID: String, respondByAt: Date) -> Void` shape and let the parent build the full RequestBody. **REVISED SIGNATURE:**
      ```swift
      onSend: @escaping (_ targetPartyID: String, _ respondByAt: Date) async -> Void
      ```
      The parent VC's closure body is then `{ [weak self] partyID, deadline in await self?.viewModel.submit(action: .tender, body: LoadActionEndpoint.RequestBody(actorRole: viewModel.role, targetPartyID: partyID, respondByAt: deadline, note: nil)) }`. This keeps the sheet completely role-agnostic (good — same sheet works for shipper and broker without modification).
    - Await the onSend closure
    - On return: re-enable send (the parent VC dismisses the sheet on 200 from its `.loaded` render path; on error the sheet stays visible and the parent slides in the toast)

    Cancel button tap: call onCancel (the parent dismisses the sheet).

    UITableViewDelegate.tableView(_:didSelectRowAt:):
    - Look up the carrier at the index
    - If `carrier.verificationState != .verified`: do not select; tableView.deselectRow at indexPath (defensive — userInteractionEnabled = false should prevent this but belt-and-suspenders)
    - Else: set `selectedCarrierIndex = indexPath.row`; mark the row with `.checkmark` accessory; call `updateSendButton()`

    NEVER log any PII or any server response text (T-09-04 view-layer lock — but the sheet doesn't currently log anything; if you add a logger call for analytics-style events, use `fields: [:]`).

    Create `validationLedgerTests/Loads/TenderSheetViewControllerTests.swift` with Tests 1-15. Use the in-test directory factory.
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/TenderSheetViewControllerTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -60</automated>
  </verify>
  <done>
    - All 15 TenderSheetViewControllerTests are green
    - Source assertion: `grep -c 'final class TenderSheetViewController' validationLedger/Features/Loads/Detail/TenderSheetViewController.swift` returns `1`
    - Source assertion: `grep -c 'final class TenderSheetCarrierRowView' validationLedger/Features/Loads/Detail/TenderSheetCarrierRowView.swift` returns `1`
    - Source assertion: `grep -nE 'isUserInteractionEnabled = false' validationLedger/Features/Loads/Detail/TenderSheetCarrierRowView.swift` returns at least one hit (D-08 carrier-level disable)
    - Source assertion: `grep -nE 'alpha = 1\.0' validationLedger/Features/Loads/Detail/TenderSheetCarrierRowView.swift` returns at least one hit (badge stays at full alpha — UI-SPEC line 446)
    - Source assertion (ISSUE-04 fix, root-view identifier): `grep -n 'accessibilityIdentifier = "load-detail.tender-sheet"' validationLedger/Features/Loads/Detail/TenderSheetViewController.swift` returns at least one line (plan 10-10 Test 1 query target)
    - Source assertion (ISSUE-04 fix, per-element identifiers): `grep -nE 'accessibilityIdentifier = "load-detail.tender-sheet/(carrier-row|deadline-chip|send-button)' validationLedger/Features/Loads/Detail/TenderSheetViewController.swift` returns at least 3 lines (carrier-row, deadline-chip, send-button — the three sub-identifier families plan 10-10 queries)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire LoadDetailViewController.presentTenderSheet() + add TenderEligibilityGatingTests for the ACTION-04 two-gate model</name>
  <files>validationLedger/Features/Loads/Detail/LoadDetailViewController.swift, validationLedgerTests/Loads/TenderEligibilityGatingTests.swift</files>
  <read_first>
    - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift (FULL — the Plan 04 `presentTenderSheet()` STUB you are filling in; verify the sheet-recipe-existing-sites at lines 1322-1333 (Phase 9 D-08 verification basis sheet) — copy those 7 lines VERBATIM per Pitfall 7)
    - validationLedger/Features/Loads/Detail/TenderSheetViewController.swift (Task 1 output — the sheet you're presenting)
    - validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift (Plan 05 — the directory fetch)
    - validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift (Plan 03 — note the `submit(action:body:) async` signature)
    - validationLedgerTests/Loads/LoadActionsViewTests.swift (Plan 04 Tests 7+8 — the LOAD-level gate; this plan adds the symmetric CARRIER-level gate test in TenderEligibilityGatingTests)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §6 (the verbatim 7-line recipe applied to the tender sheet, lines 263-284); Pitfall 7 lock — DO NOT refactor into a helper
    - .planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md row ACTION-04 (named test files: TenderEligibilityGatingTests.swift + TenderGateTests.swift — this task creates the first; the second is the XCUITest in Plan 10)
  </read_first>
  <behavior>
    Tests in TenderEligibilityGatingTests (NEW — write first):

    - Test 1 (`test_loadLevelGate_canTenderFalse_tenderButtonDisabled_inActionRegion`): construct VC with VM(role: .broker, loadID: "VL-1"), set state to `.loaded(<load with .posted + tenderEligibility(canTender: false, disabledReason: "Test reason")>, <chain>)`; force render; assert the Tender button in the action region is `isEnabled == false` (re-asserts Plan 04 Test 7 from a different test file as the named VALIDATION.md ACTION-04 row).
    - Test 2 (`test_loadLevelGate_canTenderFalse_tappingTender_doesNotPresentSheet`): in the same setup as Test 1, send a synthetic Tender-button tap (via the captured onTap closure if the button is somehow tapped despite being disabled); assert `presentedViewController == nil` (the sheet is not presented). Defensive guard test.
    - Test 3 (`test_loadLevelGate_canTenderTrue_tappingTender_presentsTenderSheet`): construct VC with `.loaded(<load with .posted + tenderEligibility(canTender: true)>, <chain>)`; tap the Tender button; assert `presentedViewController is TenderSheetViewController`.
    - Test 4 (`test_carrierLevelGate_directoryWithOnlyUnverifiedCarriers_sendPermanentlyDisabled`): present the sheet with a directory of ONLY .unverified + .flagged + .pending carriers (no .verified); assert the sheet's Send button is permanently disabled no matter which row is tapped (re-asserts Task 1 Test 15 from the integration angle).
    - Test 5 (`test_carrierLevelGate_directoryEmpty_sendPermanentlyDisabled`): present the sheet with an empty directory; assert Send is disabled (re-asserts Task 1 Test 14).
    - Test 6 (`test_endToEnd_brokerTapsTender_picksVerifiedCarrier_submitsRequest`): mock the directory fetch to return the standard Plan 05 fixture; tap Tender → assert sheet presented → simulate row tap on Acme + Send tap → assert `viewModel.submit(action: .tender, body:)` was called with `targetPartyID == Acme.partyID`, `actorRole == .broker`, `respondByAt != nil`. End-to-end gate-passes-then-submits coverage.
  </behavior>
  <action>
    Modify `validationLedger/Features/Loads/Detail/LoadDetailViewController.swift`:

    1. Replace the Plan 04 `presentTenderSheet()` STUB with the full implementation. Body:
       ```
       Task { [weak self] in
           guard let self else { return }
           // Fetch the directory (CarrierDirectoryEndpoint) via the same apiClient the VM uses.
           // Note: the VC needs access to apiClient — it should be injected at construction time
           // (modify the VC init signature to accept apiClient: APIClient if not already, OR
           // route through a new VM method `viewModel.fetchCarrierDirectory() async throws -> [TrustNode]`
           // — PICK the cleanest. Recommended: add to VM since it already owns apiClient.).
           do {
               let directory = try await self.viewModel.fetchCarrierDirectory()
               self.presentTenderSheet(directory: directory)
           } catch {
               // Log via viewModel.logger with fields: [:] (T-09-04 lock); show a minimal toast or no-op.
               // For v1.1 simplicity, slide in a generic toast via Plan 07's mechanism (stub for now
               // if Plan 07 hasn't merged yet; this code path tolerates the stub being a no-op).
           }
       }
       ```

    2. Add a NEW private method `presentTenderSheet(directory: [TrustNode])` that constructs the sheet + applies the 7-line recipe:
       ```
       private func presentTenderSheet(directory: [TrustNode]) {
           let sheetVC = TenderSheetViewController(
               directory: directory,
               onSend: { [weak self] targetPartyID, respondByAt in
                   guard let self else { return }
                   let body = LoadActionEndpoint.RequestBody(
                       actorRole: self.viewModel.role,
                       targetPartyID: targetPartyID,
                       respondByAt: respondByAt,
                       note: nil
                   )
                   await self.viewModel.submit(action: .tender, body: body)
                   // If the VM transitioned to .loaded (success), dismiss the sheet.
                   if case .loaded = self.viewModel.state {
                       self.dismiss(animated: true)
                   }
                   // On .actionFailed the sheet stays visible; Plan 07's toast slides in over the parent.
               },
               onCancel: { [weak self] in self?.dismiss(animated: true) }
           )
           sheetVC.modalPresentationStyle = .pageSheet
           if let sheet = sheetVC.sheetPresentationController {
               // VERBATIM 7-line recipe per PATTERNS.md §6 + Pitfall 7 (do NOT refactor into a helper)
               sheet.detents = [.medium(), .large()]
               sheet.selectedDetentIdentifier = .medium
               sheet.prefersGrabberVisible = true
               sheet.largestUndimmedDetentIdentifier = .medium
               sheet.prefersScrollingExpandsWhenScrolledToEdge = false
               sheet.prefersEdgeAttachedInCompactHeight = true
               sheet.widthFollowsPreferredContentSizeWhenEdgeAttached = true
           }
           present(sheetVC, animated: true)
       }
       ```

    3. Add `func fetchCarrierDirectory() async throws -> [TrustNode]` to `LoadDetailViewModel.swift` (modify Plan 03's file — additive). Body: `let response = try await apiClient.request(CarrierDirectoryEndpoint()); return response.carriers`. NOT a state machine transition — just a thin pass-through. Tests against the mock registry will cover end-to-end.

    Create `validationLedgerTests/Loads/TenderEligibilityGatingTests.swift` with Tests 1-6 above. Use `MockAPIClient` for the directory fetch; build a `MockLoadDetailViewModel` for sheet-presentation assertions; reuse the carrier-directory mock infrastructure from Plan 05.
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/TenderSheetViewControllerTests -only-testing:validationLedgerTests/Loads/TenderEligibilityGatingTests -only-testing:validationLedgerTests/Loads/LoadDetailViewControllerActionRenderTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -60</automated>
  </verify>
  <done>
    - All 15 Task 1 + 6 Task 2 tests green = 21 new
    - Pitfall 7 invariant: `grep -c 'selectedDetentIdentifier = .medium' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least `3` (Phase 9 verification basis sheet + Phase 9 handoff sheet + Phase 10 tender sheet — all inlined, none factored)
    - Source assertion: `grep -c 'TenderSheetViewController(' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `1` (the constructor call in presentTenderSheet)
    - Source assertion: `grep -c 'fetchCarrierDirectory' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `1` (the new VM method)
    - Whole-project build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → tender button | Load-level gate enforced in Plan 04's action region; if `canTender == false` the button is disabled before the sheet is presented. |
| user → carrier picker row | Carrier-level gate enforced in this plan's sheet; unverified/flagged/pending rows are visible-but-disabled with inline reason. |
| user → Send button | Three-condition gate (carrier selected + verified + future deadline) enforced via `sendButtonState` computed property. Helper line explains which gate is closed. |
| sheet → parent VC (onSend closure) | The sheet does not know about the load or the role — it only collects `(targetPartyID, respondByAt)` and awaits the parent's submission. Parent composes the full RequestBody with `actorRole: viewModel.role`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-10-04 (CRITICAL — platform thesis)** | Elevation of Privilege | Tender to unverified counterparty | mitigate | TWO independent gates: (a) Plan 04's load-level `Load.tenderEligibility.canTender` disables the Tender button (Tests 1-3 of TenderEligibilityGatingTests); (b) this plan's carrier-level picker filters by `VerificationState == .verified` for selectability + Send button enable (Tests 4-5 + Task 1 Tests 2/15). Server is canonical (Phase 7 D-18); client is defense-in-depth. Inline reason rendered for both gates ("identity that cannot be spoofed" — the broker SEES the fraud signal and learns it can't be bypassed). |
| T-10-PR-01 | Tampering | Pitfall 7 — Sheet 7-line recipe extracted into a helper; future site drifts silently | mitigate | `done` step asserts the grep count for `selectedDetentIdentifier = .medium` is at least 3 — Phase 9 (2) + Phase 10 (1). Drift to "factor into helper" reduces this to 1 and trips CI. |
| T-10-PR-02 | Tampering | Pitfall 8 — Send helper line `isHidden` toggle causes sheet detent reflow | mitigate | Helper label is fixed-height with alpha 0/1 toggling; Task 1 Test 13 (`test_sheetHelperLine_alphaToggleNotIsHidden`) is the regression guard. |
| T-09-04 (extended) | Information Disclosure | Server error text from the directory fetch leaking into the UI | mitigate | `presentTenderSheet()` catches the directory-fetch error and logs (via `fields: [:]`); does not render the error in the sheet. If the directory fetch fails, the sheet is not presented — a Plan 07 toast can fire if needed. |
| T-10-PR-SC | Tampering | Package legitimacy | accept | Phase 10 installs ZERO new packages. N/A. |
</threat_model>

<verification>
After both tasks:

`xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -80`

All Loads tests green.

T-10-04 mitigation surface area: 4 test files cover both gates (LoadActionsViewTests Tests 7+8 — load-level isEnabled; TenderEligibilityGatingTests Tests 1-3 — load-level integration; TenderSheetViewControllerTests Tests 14+15 — carrier-level; TenderEligibilityGatingTests Tests 4-5 — carrier-level integration).
</verification>

<success_criteria>
- `TenderSheetViewController` + `TenderSheetCarrierRowView` ship; UI-SPEC § Component Geometry lines 433-456 LOCKED geometry honored
- The 7-line `UISheetPresentationController` recipe is INLINED in `LoadDetailViewController.presentTenderSheet(directory:)`; Pitfall 7 invariant holds (count ≥ 3 across detail VC)
- Both ACTION-04 enforcement gates have explicit named tests; VALIDATION.md ACTION-04 row's named files (TenderEligibilityGatingTests) exist
- 21 new green tests
- `LoadDetailViewModel.fetchCarrierDirectory()` exists as a thin VM pass-through to CarrierDirectoryEndpoint
- The previously-stub `presentTenderSheet()` in LoadDetailViewController.swift is now real
</success_criteria>

<output>
Create `.planning/phases/10-per-role-tender-accept-reject/10-06-SUMMARY.md` with: the sheet's onSend closure shape, the carrier-row composition, the two-gate model summary (load-level + carrier-level both verified), the 7-line-recipe inlining count (proves Pitfall 7), and notes for Plan 07 (the toast slides in over the still-visible sheet on .actionFailed — sheet `largestUndimmedDetentIdentifier = .medium` keeps parent interactive).
</output>
