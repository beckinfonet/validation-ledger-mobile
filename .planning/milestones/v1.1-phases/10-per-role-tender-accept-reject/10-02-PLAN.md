---
phase: 10-per-role-tender-accept-reject
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - validationLedger/Core/Load/RoleLoadPolicy.swift
  - validationLedger/Core/Load/LoadActionTitleResolver.swift
  - validationLedgerTests/Loads/RoleLoadPolicyAvailableActionsTests.swift
  - validationLedgerTests/Loads/LoadActionTitleResolverTests.swift
autonomous: true
requirements:
  - ACTION-01
  - ACTION-05
  - ACTION-06
  - ACTION-09
user_setup: []

must_haves:
  truths:
    - "Per VALIDATION.md ACTION-01 row: an `availableActions(for:in:)` matrix test file exists in `validationLedgerTests/Loads/` so Wave-2/3 plans can attach the policy invariant test as `<automated>` without scaffolding a new harness mid-plan."
    - "Per ACTION-05 (button title varies by status — `.advanceStatus → Dispatch / Mark in transit / Mark delivered`): a pure `nextStatus(from:)` + `LoadActionTitleResolver` helper lives in Core/ so the action region (Plan 04) never inlines a `switch load.status` to compute a button title — the gate stays with RoleLoadPolicy, the title computation runs only after the gate opens (UI-SPEC line 282)."
    - "Per D-09 (ROADMAP §Phase 10 zero `switch load.status` lock): the title helper is in Core/Load/ (not in Features/Loads/), so the planned Plan 04 lint test that scans `Features/Loads/**/*ViewController.swift` and `**/*Cell.swift` for `switch.*\\.status` is not tripped by the title resolver."
    - "Per ACTION-09 (5 × 13 = 65 (Role × LoadStatus) policy table is the truth): the test file proves the action region's render contract — every cell of the cross product is asserted against `RoleLoadPolicy.actions(for:status:)` (`availableActions(for:in:)` is a thin wrapper for the VM call shape)."
  artifacts:
    - path: "validationLedger/Core/Load/RoleLoadPolicy.swift"
      provides: "Pre-existing policy gains an `availableActions(for role: Role, in load: Load) -> [LoadAction]` extension that delegates to the existing `actions(for:status:)` — the VM-facing call shape (Pitfall 4 anchor: VM uses `viewModel.role`, the load comes from the VM's `.loaded` state)"
      contains: "availableActions(for"
    - path: "validationLedger/Core/Load/LoadActionTitleResolver.swift"
      provides: "Pure namespace with `title(for: LoadAction, currentStatus: LoadStatus?) -> String` (NSLocalizedString resolution including the .advanceStatus → next-status name)"
      contains: "public enum LoadActionTitleResolver"
    - path: "validationLedgerTests/Loads/RoleLoadPolicyAvailableActionsTests.swift"
      provides: "Cross-product 5×13 coverage of `availableActions(for:in:)` mirroring the Phase 7 policy table; also asserts (Factoring, _) == []"
      contains: "RoleLoadPolicyAvailableActionsTests"
    - path: "validationLedgerTests/Loads/LoadActionTitleResolverTests.swift"
      provides: "Coverage for every (LoadAction, LoadStatus) the policy permits; .advanceStatus from .accepted/.dispatched/.inTransit produces the 3 distinct localized titles per UI-SPEC table line 277-280"
      contains: "LoadActionTitleResolverTests"
  key_links:
    - from: "validationLedger/Core/Load/RoleLoadPolicy.swift"
      to: "VM (Plan 03) and LoadActionsView (Plan 04)"
      via: "availableActions(for:in:) is the single call shape downstream consumers use"
      pattern: "availableActions\\(for:"
    - from: "validationLedger/Core/Load/LoadActionTitleResolver.swift"
      to: "LoadActionsView (Plan 04)"
      via: "consumed when rendering button titles — especially the .advanceStatus next-status name"
      pattern: "LoadActionTitleResolver\\.title"
---

<objective>
Add the two thin pure helpers the action region (Plan 04) will call once per render: `RoleLoadPolicy.availableActions(for: Role, in: Load) -> [LoadAction]` (a same-file extension that delegates to the frozen `actions(for:status:)` — the VM-facing call shape) and `LoadActionTitleResolver.title(for: LoadAction, currentStatus: LoadStatus?) -> String` (the pure title resolver that maps `.advanceStatus` against the current status to the three distinct localized button titles "Dispatch" / "Mark in transit" / "Mark delivered" per UI-SPEC line 280, plus the six other action titles).

Purpose: This is the second half of the Wave-1 foundation that locks the ROADMAP §Phase 10 invariant — the action region MUST NOT contain any `switch load.status` (the gate is `RoleLoadPolicy`, the post-gate title computation is `LoadActionTitleResolver`). Putting both in `Core/Load/` ensures the Plan 04 lint test (grep over `Features/Loads/**/*ViewController.swift` and `**/*Cell.swift` for `switch.*\.status`) is not tripped. VALIDATION.md's ACTION-01 row explicitly names `RoleLoadPolicyAvailableActionsTests` as the per-task automated command for the policy invariant — landing it in Wave 1 means every later wave can attach this test as its `<automated>` without re-scaffolding.

Output: One same-file extension on `RoleLoadPolicy.swift`; one new pure-Swift file (`LoadActionTitleResolver.swift`); two new XCTest files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md
@.planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md
@.planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md
@.planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md
@.planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md

# Frozen contract files this plan reads — DO NOT MUTATE existing definitions
@validationLedger/Core/Load/RoleLoadPolicy.swift
@validationLedger/Core/Load/LoadAction.swift
@validationLedger/Core/Load/LoadStatus.swift
@validationLedger/Core/Load/Load.swift
@validationLedger/Roles/Role.swift
@validationLedgerTests/Loads/LoadDetailViewModelTests.swift

<interfaces>
<!-- Phase 7 frozen contract — already in scope from Plan 01's interfaces; repeated only for the VM/title call sites -->

From `validationLedger/Core/Load/RoleLoadPolicy.swift` (FROZEN — Phase 7, existing):
```swift
public enum RoleLoadPolicy {
    public static func actions(for role: Role, status: LoadStatus) -> [LoadAction]
}
```

From `validationLedger/Roles/Role.swift` (FROZEN):
```swift
public enum Role: String, Codable, CaseIterable, Sendable {
    case shipper, broker, carrier, dispatch, factoring
    public var displayName: String  // Already exists; do not re-define.
}
```

UI-SPEC line 277-280 (advanceStatus title table — LOCKED):
| LoadStatus | Next status | Button title | Localization key |
| .accepted     | .dispatched | "Dispatch"        | loads.actions.button.advance.dispatched |
| .dispatched   | .inTransit  | "Mark in transit" | loads.actions.button.advance.in_transit |
| .inTransit    | .delivered  | "Mark delivered"  | loads.actions.button.advance.delivered |

UI-SPEC line 273-280 (non-advance action titles — LOCKED):
| .tender → "Tender" → loads.actions.button.tender |
| .accept → "Accept" → loads.actions.button.accept |
| .reject → "Reject" → loads.actions.button.reject |
| .cancel → "Cancel load" → loads.actions.button.cancel |
| .post   → "Post"   → loads.actions.button.post   |
(.advanceStatus uses the table above — currentStatus required)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RoleLoadPolicy.availableActions(for:in:) wrapper + exhaustive 5×13 cross-product test</name>
  <files>validationLedger/Core/Load/RoleLoadPolicy.swift, validationLedgerTests/Loads/RoleLoadPolicyAvailableActionsTests.swift</files>
  <read_first>
    - validationLedger/Core/Load/RoleLoadPolicy.swift (full file — note the exact `actions(for:status:)` signature you are wrapping; do NOT mutate the existing function body)
    - validationLedgerTests/Loads/RoleLoadPolicyTests.swift (existing Phase 7 test file — note the `policyIsTotal` cross-product sweep shape; mirror its style verbatim)
    - .planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md row ACTION-01 (this test file is the named automated command for the ACTION-01 invariant)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Per-Action State / Visibility Matrix lines 514-535 (the 5×13 table the test asserts against)
    - .planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md § Pitfall 4 (the VM stores `role`; `availableActions(for: viewModel.role, in: load)` is the call shape — Plan 04 will use it from the VC via the VM)
  </read_first>
  <behavior>
    Tests in RoleLoadPolicyAvailableActionsTests (NEW — write first):

    - Test 1 (`test_availableActions_isThinWrapper_overActionsForStatus`): for the full Role × LoadStatus cross product, build a `Load` test double with each `status`, call `RoleLoadPolicy.availableActions(for: role, in: load)`, assert the result equals `RoleLoadPolicy.actions(for: role, status: load.status)`. Proves the wrapper is delegating, not re-implementing.
    - Test 2 (`test_availableActions_factoring_returnsEmpty_forEveryStatus`): iterate `LoadStatus.allCases`, assert `availableActions(for: .factoring, in: <load with status>)` returns `[]`. Per ROADMAP §Phase 10 ("Factoring sees no actions") and UI-SPEC line 533.
    - Test 3 (`test_availableActions_broker_onPosted_isTenderAndCancel`): assert `availableActions(for: .broker, in: <load with .posted>) == [.tender, .cancel]`. Per UI-SPEC line 517.
    - Test 4 (`test_availableActions_broker_onRejected_isTenderAndCancel_retenderAlias`): same as Test 3 but `.rejected`. Per D-04 / UI-SPEC line 519.
    - Test 5 (`test_availableActions_broker_onExpired_isTenderAndCancel`): same as Test 3 but `.expired`. Per UI-SPEC line 520.
    - Test 6 (`test_availableActions_carrier_onTendered_isAcceptAndReject`): per UI-SPEC line 527 — true fork (justifies D-02 equal-weight).
    - Test 7 (`test_availableActions_carrier_advanceStatus_progression`): for `.accepted`, `.dispatched`, `.inTransit` — exactly `[.advanceStatus]` each. Per UI-SPEC lines 528-530 and ACTION-05.
    - Test 8 (`test_availableActions_broker_canCancelOnPosted`): explicit per VALIDATION.md ACTION-06 row.
    - Test 9 (`test_availableActions_broker_canPostOnDraft`): explicit per UI-SPEC line 516 (Shipper/Broker on `.draft` → `[.post, .cancel]`). Per ACTION-06.

    Test fixture helper: file-private `makeLoad(status: LoadStatus) -> Load` — same shape as the Plan 01 helper. Uses an in-test JSON literal decoded via `APIClient.defaultDecoder()`.
  </behavior>
  <action>
    Append to `validationLedger/Core/Load/RoleLoadPolicy.swift` (same file, after the existing enum closing brace): a `public extension RoleLoadPolicy { static func availableActions(for role: Role, in load: Load) -> [LoadAction] { actions(for: role, status: load.status) } }`. ONE-LINER delegation. Header comment block: "// Phase 10 — VM-facing call shape (per D-22 / Pitfall 4). The VM stores `role`; the load comes from the VM's `.loaded` state. Delegates to the frozen Phase 7 `actions(for:status:)` — this is naming sugar, not new policy. The 5×13 invariant continues to live in the Phase 7 truth-table tests; this extension's cross-product test asserts only the delegation."

    Create `validationLedgerTests/Loads/RoleLoadPolicyAvailableActionsTests.swift`. XCTest class. Implement Tests 1-9. The cross-product sweep uses nested `for role in Role.allCases` + `for status in LoadStatus.allCases` — same shape as the `RoleLoadPolicyTests.policyIsTotal` precedent.
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/RoleLoadPolicyAvailableActionsTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All 9 tests in `RoleLoadPolicyAvailableActionsTests` green
    - Source assertion: `grep -c 'static func availableActions(for' validationLedger/Core/Load/RoleLoadPolicy.swift` returns `1`
    - Source assertion: the existing `actions(for:status:)` function body in `RoleLoadPolicy.swift` is byte-identical to the pre-edit version (you only APPENDED an extension; you did not modify the original enum body)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement LoadActionTitleResolver pure namespace + per-action title coverage test</name>
  <files>validationLedger/Core/Load/LoadActionTitleResolver.swift, validationLedgerTests/Loads/LoadActionTitleResolverTests.swift</files>
  <read_first>
    - validationLedger/Core/Load/RoleLoadPolicy.swift (now with the Task-1 extension — the analog shape for a `public enum`-namespace pure helper)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Action button titles lines 270-282 (the LOCKED title table; the .advanceStatus row has the three next-status titles)
    - .planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md § Project Constraints (UI-SPEC-sanctioned exception: the `.advanceStatus` button-title resolver reads `LoadStatus` to produce a localized title; the GATE stays with `RoleLoadPolicy`, the title computation runs only after the gate opens; "MUST be in a pure helper, NOT inline in the action region")
    - validationLedger/Core/Load/LoadActionPredictor.swift (Plan 01) — the shape twin for `public enum`-namespace + single static entry
  </read_first>
  <behavior>
    Tests in LoadActionTitleResolverTests (NEW — write first):

    - Test 1 (`test_title_simpleActions`): for each of `.tender`, `.accept`, `.reject`, `.cancel`, `.post`, assert `LoadActionTitleResolver.title(for: action, currentStatus: nil)` returns the LOCKED English fallback per UI-SPEC: "Tender" / "Accept" / "Reject" / "Cancel load" / "Post". `currentStatus: nil` is valid for non-advance actions.
    - Test 2 (`test_title_advanceStatus_fromAccepted_isDispatch`): `title(for: .advanceStatus, currentStatus: .accepted)` returns "Dispatch".
    - Test 3 (`test_title_advanceStatus_fromDispatched_isMarkInTransit`): `title(for: .advanceStatus, currentStatus: .dispatched)` returns "Mark in transit".
    - Test 4 (`test_title_advanceStatus_fromInTransit_isMarkDelivered`): `title(for: .advanceStatus, currentStatus: .inTransit)` returns "Mark delivered".
    - Test 5 (`test_title_advanceStatus_fromOtherStatus_returnsGenericFallback`): `title(for: .advanceStatus, currentStatus: .posted)` returns a non-empty fallback (e.g. "Advance"). The policy gate prevents this combination from rendering in the UI, but the resolver MUST NOT crash.
    - Test 6 (`test_title_advanceStatus_currentStatusNil_returnsGenericFallback`): `title(for: .advanceStatus, currentStatus: nil)` returns the same generic "Advance" fallback (defensive — never trap on a nil status).
    - Test 7 (`test_nextStatus_fromAccepted_isDispatched`) + Test 8 (`fromDispatched_isInTransit`) + Test 9 (`fromInTransit_isDelivered`) + Test 10 (`fromOtherStatus_isNil`): the pure `nextStatus(from:)` helper.
  </behavior>
  <action>
    Create `validationLedger/Core/Load/LoadActionTitleResolver.swift`. File header block citing UI-SPEC § Action button titles (LOCKED) and ROADMAP §Phase 10 (zero `switch load.status` in views — this resolver is in Core/, NOT a view; same exception as the Phase 9 timeline display strings). Two public static functions in a single `public enum LoadActionTitleResolver` namespace:

    1. `public static func nextStatus(from current: LoadStatus) -> LoadStatus?` — pure helper. Switch over `current`: `.accepted -> .dispatched`, `.dispatched -> .inTransit`, `.inTransit -> .delivered`. Every other case (including `.draft`, `.posted`, terminal cases) -> `nil`. NO `default:` arm — explicit `case .draft, .posted, .tendered, .rejected, .expired, .delivered, .cancelled, .podCaptured, .invoiced, .funded: return nil`. (The Swift compiler enforces exhaustiveness when a future LoadStatus case lands.)

    2. `public static func title(for action: LoadAction, currentStatus: LoadStatus?) -> String` — switch over `action`. For `.tender`, `.accept`, `.reject`, `.cancel`, `.post`: return the LOCKED title via NSLocalizedString per UI-SPEC keys (`loads.actions.button.tender` → "Tender" English fallback; `…button.accept` → "Accept"; `…button.reject` → "Reject"; `…button.cancel` → "Cancel load"; `…button.post` → "Post"). For `.advanceStatus`: switch on `nextStatus(from: currentStatus ?? .draft)` — `.dispatched -> NSLocalizedString("loads.actions.button.advance.dispatched", value: "Dispatch", ...)`, `.inTransit -> NSLocalizedString("loads.actions.button.advance.in_transit", value: "Mark in transit", ...)`, `.delivered -> NSLocalizedString("loads.actions.button.advance.delivered", value: "Mark delivered", ...)`, and the generic fallback for any other / nil case is `NSLocalizedString("loads.actions.button.advance.generic", value: "Advance", ...)`.

    Create `validationLedgerTests/Loads/LoadActionTitleResolverTests.swift`. XCTest class. Implement Tests 1-10 above.

    DO NOT add this helper to `Features/Loads/Detail/` or to `LoadActionsView` — its location in `Core/Load/` is what makes the Plan 04 lint test pass (grep over `Features/Loads/**/*ViewController.swift` and `**/*Cell.swift` finds zero `switch.*\.status`).
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/LoadActionTitleResolverTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All 10 tests in `LoadActionTitleResolverTests` green
    - Source assertion: `grep -c '^public enum LoadActionTitleResolver' validationLedger/Core/Load/LoadActionTitleResolver.swift` returns `1`
    - Location assertion: `test -f validationLedger/Core/Load/LoadActionTitleResolver.swift && echo OK` outputs `OK` (file is in Core/Load/, not in Features/Loads/)
    - Source assertion: `grep -nE 'switch[[:space:]]+.*\.status' validationLedger/Features/Loads/ -r 2>/dev/null | (! grep .)` (no Features/Loads/ file uses `switch load.status` — Plan 04's lint test still passes pre-implementation)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view (action region, Plan 04) → policy / title resolver | The view trusts that `RoleLoadPolicy.availableActions(for:in:)` returns the canonical action set and that `LoadActionTitleResolver.title(for:currentStatus:)` returns the canonical title; both helpers are pure and exhaustively tested, so the view does not need to defend against drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | Action region showing wrong action set (privilege escalation via UI gate bypass) | mitigate | `availableActions(for:in:)` is the SINGLE call shape downstream consumers use; Test 1 asserts thin-wrapper delegation; the existing Phase 7 `RoleLoadPolicyTests.policyIsTotal` continues to assert the underlying truth table; Plan 04's `LoadActionsView` will call this helper only. |
| T-10-PR-01 | Tampering | `LoadActionTitleResolver` returns a wrong title that changes the user's mental model of what the button does | mitigate | Tests 1-6 lock the title strings against UI-SPEC's LOCKED table; English fallback strings are in source so localization drift does not break the unit assertion. |
| T-10-PR-02 | Tampering | A future `LoadStatus` case silently misses a `nextStatus(from:)` arm | mitigate | `nextStatus(from:)` switch has no `default:` arm — explicit case list covers all 13 current `LoadStatus` cases; Swift's exhaustiveness check flags a new case at compile time. |
| T-10-PR-SC | Tampering | Package legitimacy on new installs | accept | Phase 10 installs ZERO new packages. N/A. |
</threat_model>

<verification>
After both tasks:

`xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/RoleLoadPolicyAvailableActionsTests -only-testing:validationLedgerTests/Loads/LoadActionTitleResolverTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -40`

Both test files green.
</verification>

<success_criteria>
- `RoleLoadPolicy.swift` gains ONE extension; the original enum body is unchanged byte-for-byte
- `LoadActionTitleResolver.swift` exists in `Core/Load/` and compiles
- Both new test files green; together they cover all 9 `availableActions(for:in:)` cases the action region will hit + all 7 LoadAction title cases + 4 `nextStatus(from:)` cases (10 + 9 = 19 new green tests)
- VALIDATION.md ACTION-01 row's named test (`RoleLoadPolicyAvailableActionsTests`) is now real
- No new file under `Features/Loads/` — both helpers are in `Core/Load/` so the Plan 04 lint test (zero `switch load.status` in Features/Loads/**/*ViewController.swift / **/*Cell.swift) continues to be satisfiable
</success_criteria>

<output>
Create `.planning/phases/10-per-role-tender-accept-reject/10-02-SUMMARY.md` with: the new files' line counts, total green test count, and the per-action title table the resolver implements (for Plan 04 reference).
</output>
