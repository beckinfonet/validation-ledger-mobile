---
phase: 10-per-role-tender-accept-reject
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - validationLedger/Features/Loads/Detail/LoadActionsView.swift
  - validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift
  - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
  - validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift
  - validationLedgerTests/Loads/LoadActionsViewTests.swift
  - validationLedgerTests/Loads/Lint/LoadDetailNoStatusSwitchTests.swift
  - validationLedgerTests/Loads/RespondByLabelTests.swift
autonomous: true
requirements:
  - ACTION-01
  - ACTION-04
  - ACTION-05
  - ACTION-07
  - ACTION-09
user_setup: []

must_haves:
  truths:
    - "Per D-01 + UI-SPEC line 409: `LoadActionsView` mounts in `LoadDetailBodyView.contentStack` at index 2 (between `timelineContainer` and `freightDetailsContainer`); the body view's existing 5-child arrangement grows to 6."
    - "Per **D-02**: action buttons are arranged side-by-side equal width in a horizontal `UIStackView` with `.fillEqually` distribution — NOT primary-vs-secondary visual hierarchy. 44pt touch-target floor is preserved; Dynamic Type allows vertical re-stacking when content gets long."
    - "Per **D-03**: destructive actions (`.cancel`, `.reject`) receive a destructive tint via the UI-SPEC token (e.g., `DS.Colors.destructive` or equivalent design-system color), separate from the equal-weight layout decision."
    - "Per ACTION-09 (single policy source) + D-04 (caption never collapses): when `RoleLoadPolicy.availableActions(for:in:) == []`, the action region renders a centered localized caption per UI-SPEC line 297-313 — Factoring caption / terminal-state caption (`This load is %@. No actions available.` with `LoadStatus.localizedDisplayName` interpolation) / generic empty fallback — and the region keeps its vertical footprint (24pt top + bottom padding) so the body's scroll order does not visually snap closed."
    - "Per ACTION-07 + D-09 (load-level tender gate): when `Load.tenderEligibility?.canTender == false` AND the visible action set contains `.tender`, the Tender button renders `isEnabled = false` with an inline reason below — server-supplied `disabledReason` rendered VERBATIM (UI-SPEC line 290's documented exception to the no-server-text rule, because `disabledReason` is load-state metadata, NOT error-response payload); when `disabledReason == nil`, fallback `loads.actions.tender.disabled.generic` -> 'Tender unavailable'."
    - "Per ROADMAP §Phase 10 lock: a passing lint test asserts zero `switch.*\\.status` in `Features/Loads/**/*ViewController.swift` and `**/*Cell.swift` — the action gate flows from `RoleLoadPolicy`; the title gate (Plan 02 `LoadActionTitleResolver`) lives in `Core/Load/`. T-10-01 mitigation."
    - "Per D-12 + Pitfall 6: during `.actionInFlight`, `LoadActionsView` re-renders against the PRE-TAP action set (NOT the predicted post-tap action set) with the tapped button showing its spinner and other buttons disabled — the configure-method signature accepts an `inFlight: LoadAction?` parameter so the view knows which button is the tapped one. Only on `.loaded` does the action set snap forward."
    - "Per UI-SPEC § Respond-by countdown (LOCKED static rendering) + ACTION-07 sub-row: when `role ∈ {.carrier, .dispatch}` AND `load.status == .tendered` AND `load.respondByAt != nil`, an inline 'Respond by %@' label renders below the action buttons; past-due variant uses `DS.Colors.destructive`."
    - "Per Pitfall 1 (chain overlay single ref) + UI-SPEC § Chain overlay: the chain `updating…` overlay is owned by `LoadDetailViewController` via a single `private var chainOverlay: UIView?`; mounted on `.actionInFlight`, fades out on `.loaded` / `.actionFailed` (Plan 07 wires the fade-in shape; this plan stubs the mount/dismount API so Plan 07 can plug in without touching the VC render dispatcher)."
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/LoadActionsView.swift"
      provides: "Programmatic UIView; `configure(actions: [LoadAction], role: Role, currentStatus: LoadStatus, tenderEligibility: TenderEligibility?, respondByAt: Date?, inFlight: LoadAction?, onTap: @escaping (LoadAction) -> Void)`; empty-state caption variant; disabled-reason inline label; respond-by inline label"
      contains: "final class LoadActionsView"
    - path: "validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift"
      provides: "contentStack gains `actionsContainer` at index 2 (between timeline and freight)"
      contains: "actionsContainer"
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      provides: "`render(state:)` dispatcher gains `.actionInFlight` and `.actionFailed` arms (chain overlay mount/dismount stubs included; toast presentation in Plan 07); the VC owns a `LoadActionsView` instance, configures it on every render with `RoleLoadPolicy.availableActions(for: viewModel.role, in: load)`, and routes per-button taps to `viewModel.submit(action: <action>, body: <built body>)` — for `.tender` the tap presents the sheet (Plan 06 wires the sheet)"
      contains: "case .actionInFlight"
    - path: "validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift"
      provides: "Skeleton silhouette gains a 2-button row at the same scroll-order position the live action region occupies (UI-SPEC § Skeleton silhouette line 428-431)"
      contains: "actionSkeleton"
    - path: "validationLedgerTests/Loads/LoadActionsViewTests.swift"
      provides: "Unit tests for the configure-method contract: button count matches actions[]; empty-state caption renders for Factoring / terminal / generic; disabled-reason inline label appears when tenderEligibility.canTender == false; in-flight state disables non-tapped buttons; Dynamic Type axis flip"
      contains: "LoadActionsViewTests"
    - path: "validationLedgerTests/Loads/Lint/LoadDetailNoStatusSwitchTests.swift"
      provides: "Lint-as-test: scans the file contents under `Features/Loads/` for `switch.*\\.status` patterns and asserts the count is 0 (after a small allowlist for header-comment occurrences). T-10-01 mitigation."
      contains: "LoadDetailNoStatusSwitchTests"
    - path: "validationLedgerTests/Loads/RespondByLabelTests.swift"
      provides: "Unit coverage for the carrier/dispatch respond-by inline label per UI-SPEC line 322-335; future vs past dates; nil respondByAt = label absent"
      contains: "RespondByLabelTests"
  key_links:
    - from: "validationLedger/Features/Loads/Detail/LoadActionsView.swift"
      to: "validationLedger/Core/Load/RoleLoadPolicy.swift (availableActions wrapper from Plan 02)"
      via: "the VC computes actions via the wrapper and passes the result to configure(actions:); the view does NOT call the policy itself"
      pattern: "configure\\(actions:"
    - from: "validationLedger/Features/Loads/Detail/LoadActionsView.swift"
      to: "validationLedger/Core/Load/LoadActionTitleResolver.swift (Plan 02)"
      via: "per-button title rendering — `.advanceStatus` reads `currentStatus` for the next-state title"
      pattern: "LoadActionTitleResolver\\.title"
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      to: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift (Plan 03)"
      via: "render(state:) handles 2 new VM states; per-button tap calls viewModel.submit(action:body:)"
      pattern: "viewModel\\.submit\\(action:"
    - from: "validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift"
      to: "validationLedger/Features/Loads/Detail/LoadActionsView.swift"
      via: "structural — actionsContainer hosts the LoadActionsView instance the VC manages"
      pattern: "addArrangedSubview\\(actionsContainer"
---

<objective>
Build `LoadActionsView` per UI-SPEC § Component Geometry lines 407-422, mount it in `LoadDetailBodyView.contentStack` at index 2, wire the new VM states (`.actionInFlight`, `.actionFailed`) into `LoadDetailViewController.render(state:)` so the body re-renders against the predicted (or rollback) Load and the action region disables non-tapped buttons, add the respond-by inline label for the carrier/dispatch view of `.tendered` loads, and lock the ROADMAP §Phase 10 invariant via a lint-as-test that grep-scans `Features/Loads/` for forbidden `switch.*\.status`. Also stub the chain overlay's mount/dismount API on the VC so Plan 07 (which owns the actual overlay subview + animation) can plug in without re-entering the render dispatcher.

Purpose: This is the largest single plan in Phase 10 — it lands the actual visible action surface that meets the 5 success criteria from ROADMAP §Phase 10 (per-role visibility, state mutation, ACTION-07 disable, in-flight gating, pop-back refresh). It is the consumer for the Wave-1 (`LoadActionPredictor`, `LoadActionTitleResolver`, `availableActions(for:in:)`) and Wave-2 (VM state machine + role plumb) foundations. The tender SHEET (Plan 06) and CARRIER directory endpoint (Plan 05) ride on top of this in Wave 4; the toast banner + chain overlay animation (Plan 07) wire into the render dispatcher this plan stubs.

Output: 4 modified files (LoadActionsView NEW, LoadDetailBodyView modified, LoadDetailViewController modified, LoadDetailSkeletonView modified); 3 new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md
@.planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md
@.planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md
@.planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md
@.planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md
@.planning/phases/10-per-role-tender-accept-reject/10-01-SUMMARY.md
@.planning/phases/10-per-role-tender-accept-reject/10-02-SUMMARY.md
@.planning/phases/10-per-role-tender-accept-reject/10-03-SUMMARY.md

# Files this plan modifies (read whole files before editing)
@validationLedger/Features/Loads/Detail/LoadActionsView.swift
@validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift
@validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
@validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift

# Analog files (read for shape)
@validationLedger/Features/Loads/Detail/StatusTimelineView.swift
@validationLedger/UI/DesignSystem/Spacing.swift
@validationLedger/UI/DesignSystem/Typography.swift
@validationLedger/UI/DesignSystem/Colors.swift
@validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift

<interfaces>
<!-- The action region's call sites — what the VC passes in -->

`LoadActionsView.configure` contract:
```swift
public func configure(
    actions: [LoadAction],
    role: Role,
    currentStatus: LoadStatus,
    tenderEligibility: TenderEligibility?,
    respondByAt: Date?,
    inFlight: LoadAction?,             // nil when not in-flight; one of `actions` when in-flight
    onTap: @escaping (LoadAction) -> Void
)
```

The VC's render(state:) call sites (Plan 04 sketch):
```swift
case .loaded(let load, let chain):
    actionsView.configure(
        actions: RoleLoadPolicy.availableActions(for: viewModel.role, in: load),
        role: viewModel.role,
        currentStatus: load.status,
        tenderEligibility: load.tenderEligibility,
        respondByAt: load.respondByAt,
        inFlight: nil,
        onTap: { [weak self] action in self?.handleActionTap(action) }
    )
case .actionInFlight(let predicted, _, let action):
    actionsView.configure(
        actions: <pre-tap actions — see Pitfall 6: snapshot before submit, NOT recompute against predicted>,
        role: viewModel.role,
        currentStatus: predicted.status,       // for respond-by label, the predicted status drives visibility
        tenderEligibility: nil,                // hide disabled-reason during in-flight
        respondByAt: predicted.respondByAt,    // predicted respond-by drives the in-flight respond-by label
        inFlight: action,
        onTap: { _ in /* no-op: in-flight ignores taps */ }
    )
```

UI-SPEC § Skeleton silhouette line 428-431 (LOCKED):
- Skeleton button row: 2 side-by-side rounded rects, `cornerRadius = 8`, height `50pt`, full width split by `DS.Spacing.sm` (8pt) gap, shimmer-animated per Phase 8/9 pattern.
- Skeleton disabled-reason placeholder: NOT rendered.

UI-SPEC § Respond-by countdown rendering rules (LOCKED, lines 317-335):
- Visible only when `role ∈ {.carrier, .dispatch}` AND `Load.status == .tendered`
- Format `Respond by %@` (loads.actions.respond_by.format) normal case; `Past due — respond by %@` (loads.actions.respond_by.past_due.format) for past-due
- Style: `DS.Typography.footnote`, `DS.Colors.labelSecondary` (normal) / `DS.Colors.destructive` (past-due)
- Date format helper: `DateFormatter` with `.relative` + `.short` time fallback per UI-SPEC line 326-331
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LoadActionsView (programmatic UIView) + unit tests + lint-as-test</name>
  <files>validationLedger/Features/Loads/Detail/LoadActionsView.swift, validationLedgerTests/Loads/LoadActionsViewTests.swift, validationLedgerTests/Loads/Lint/LoadDetailNoStatusSwitchTests.swift</files>
  <read_first>
    - validationLedger/Features/Loads/Detail/StatusTimelineView.swift (FULL file — analog programmatic UIView in the same folder; mirror the `required init?(coder:)` trap, the `configure(...)` render contract, the explicit `accessibilityElements` shape)
    - validationLedger/UI/DesignSystem/Spacing.swift (verify `DS.Spacing.xs/sm/md/lg` values)
    - validationLedger/UI/DesignSystem/Typography.swift (verify `DS.Typography.headline/body/caption/footnote`)
    - validationLedger/UI/DesignSystem/Colors.swift (verify `DS.Colors.primary/destructive/surface/separator/label/labelSecondary`)
    - validationLedger/Core/Load/RoleLoadPolicy.swift (Plan 02 wrapper)
    - validationLedger/Core/Load/LoadActionTitleResolver.swift (Plan 02)
    - validationLedger/Core/Load/LoadStatus.swift + LoadAction.swift (frozen)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Component Geometry lines 405-422 (LoadActionsView LOCKED geometry); § Action button titles lines 270-282 (LOCKED titles); § Per-Action State / Visibility Matrix lines 514-535
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Empty-state captions lines 297-313 (LOCKED — Factoring + terminal + generic copy + `loads.actions.empty.terminal.format` interpolation with `LoadStatus.localizedDisplayName`)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § ACTION-07 disabled-reason lines 284-295 (LOCKED — server `disabledReason` rendered verbatim; documented exception to no-server-text rule)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Respond-by countdown lines 315-335 (static, NOT live counter)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §4 (configure-based render contract; equal-weight horizontal row; Dynamic Type axis flip; the "no `switch load.status`" lock — the `status` argument is consumed ONLY for empty-state-caption format-string interpolation)
    - .planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md § Pitfall 5 (caption uses `LoadStatus.localizedDisplayName` from Plan 01); § Pitfall 6 (in-flight renders PRE-TAP action set, not post-tap)
    - .planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md row ACTION-01 (`grep` lint command for the no-status-switch invariant)
  </read_first>
  <behavior>
    Tests in LoadActionsViewTests (NEW — write first):

    - Test 1 (`test_configure_brokerOnPosted_rendersTwoButtons`): configure with `actions: [.tender, .cancel]`, role: .broker, currentStatus: .posted; assert 2 visible UIButtons; first button's title is "Tender" (from `LoadActionTitleResolver`); second button's title is "Cancel load"; the empty-state caption label is hidden.
    - Test 2 (`test_configure_carrierOnTendered_acceptIsBlueRejectIsRed`): configure with `actions: [.accept, .reject]`, role: .carrier, currentStatus: .tendered, respondByAt: <future date>; assert Accept button's `configuration.baseBackgroundColor == DS.Colors.primary`; Reject button's `configuration.baseForegroundColor` or background reflects `DS.Colors.destructive` (per UI-SPEC line 70-71 + D-03); respond-by label is visible.
    - Test 3 (`test_configure_carrierOnAccepted_advanceStatusTitle_isDispatch`): configure with `actions: [.advanceStatus]`, currentStatus: .accepted; assert the advance button title is "Dispatch" (from `LoadActionTitleResolver.title(for: .advanceStatus, currentStatus: .accepted)`). Per UI-SPEC line 278.
    - Test 4 (`test_configure_emptyActions_factoring_rendersFactoringCaption`): configure with `actions: []`, role: .factoring, currentStatus: .posted; assert button stack is hidden; caption label is visible with text matching `loads.actions.empty.factoring` localization (English fallback "This is a view-only role. No actions available.").
    - Test 5 (`test_configure_emptyActions_terminal_rendersInterpolatedCaption`): configure with `actions: []`, role: .broker, currentStatus: .delivered; assert caption text matches `loads.actions.empty.terminal.format` with `LoadStatus.localizedDisplayName` interpolation (English: "This load is delivered. No actions available."). Per Pitfall 5.
    - Test 6 (`test_configure_emptyActions_terminal_inTransitDisplayName_hasNoUnderscore`): configure with `actions: []`, role: .broker, currentStatus: .inTransit; assert caption text contains "in transit" (with space), NOT "in_transit" (with underscore). Pitfall 5 explicit regression guard.
    - Test 7 (`test_configure_tenderEligibilityCanTenderFalse_tenderDisabled_withInlineReason`): configure with `actions: [.tender, .cancel]`, role: .broker, currentStatus: .posted, `tenderEligibility: TenderEligibility(canTender: false, disabledReason: "Counterparty USDOT authority suspended")`; assert Tender button `isEnabled == false`; assert the inline reason label is visible and reads "Counterparty USDOT authority suspended" verbatim (server-supplied per UI-SPEC line 290); assert Cancel button `isEnabled == true` (not gated by tenderEligibility per UI-SPEC line 535).
    - Test 8 (`test_configure_tenderEligibilityCanTenderFalse_nilReason_rendersGenericFallback`): same as Test 7 but `disabledReason: nil`; assert inline reason label reads `loads.actions.tender.disabled.generic` (English: "Tender unavailable").
    - Test 9 (`test_configure_inFlight_otherButtonsDisabled_tappedButtonShowsSpinner`): configure with `actions: [.tender, .cancel]`, `inFlight: .tender`; assert Tender button `isEnabled == false`, Cancel button `isEnabled == false`, the Tender button's `configuration` includes an `.medium` activity indicator OR an overlay subview is present indicating in-flight; the title's alpha is dimmed.
    - Test 10 (`test_configure_inFlight_actionsList_isPreTapList_pitfall6`): configure with `actions: [.tender, .cancel]` (pre-tap on .posted), `currentStatus: .tendered` (predicted post-tap), `inFlight: .tender` — assert the BUTTONS rendered are still Tender + Cancel (NOT the post-tap empty set the predicted status would produce). Pitfall 6 explicit regression guard — the view obeys the actions[] argument, never recomputes against currentStatus.
    - Test 11 (`test_configure_carrierOnTendered_respondByLabel_visibleWithFutureDate`): configure with `actions: [.accept, .reject]`, role: .carrier, currentStatus: .tendered, respondByAt: <future date>; assert the respond-by label is visible, text contains "Respond by", color is `DS.Colors.labelSecondary` (NOT destructive).
    - Test 12 (`test_configure_carrierOnTendered_respondByLabel_pastDateIsDestructive`): same as Test 11 but `respondByAt: <past date>`; assert the label color is `DS.Colors.destructive` and the text contains "Past due".
    - Test 13 (`test_configure_brokerOnTendered_respondByLabel_absent`): configure with role: .broker, currentStatus: .tendered, respondByAt: <future date>; assert the respond-by label is hidden (the label is ONLY for carrier/dispatch per UI-SPEC line 335).
    - Test 14 (`test_configure_emptyActions_respondByLabel_absent`): configure with `actions: []`; assert respond-by label is hidden even if respondByAt is set (caption takes over).
    - Test 15 (`test_configure_DynamicTypeAccessibilityCategory_buttonRowAxis_flipsToVertical`): set `traitCollection.preferredContentSizeCategory = .accessibilityLarge`; assert the button stack's `axis == .vertical` (per UI-SPEC line 419).
    - Test 16 (`test_configure_setsAccessibilityElements_inTapOrder`): configure with `actions: [.tender, .cancel]`; assert `accessibilityElements` includes the Tender button THEN the Cancel button THEN any visible inline label (respond-by or disabled-reason); each button's `accessibilityLabel` is the button title verbatim.
    - Test 17 (`test_configure_onTapClosure_calledWithCorrectAction`): configure with `actions: [.tender, .cancel]`, `onTap: { capturedAction = $0 }`; simulate a Tender button tap; assert `capturedAction == .tender`.
    - Test 18 (`test_configure_singleAction_rendersInTwoCellStackWithSpacer`): configure with `actions: [.advanceStatus]`, currentStatus: .accepted; assert the button stack still has 2 cells (one button + one invisible spacer UIView) per UI-SPEC line 417 (single full-width button reads as a checkout CTA — half-width keeps the visual posture).

    Tests in LoadDetailNoStatusSwitchTests (NEW — write first, will be GREEN immediately once Plan 02 + this plan land):

    - Test 1 (`test_noStatusSwitch_inFeaturesLoadsViewControllers`): use `FileManager.default.enumerator(atPath:)` over `validationLedger/Features/Loads/` to enumerate all `.swift` files under `*ViewController.swift` and `*Cell.swift` glob; for each file, read its bytes, strip lines starting with `//` (header comment allowlist), then assert the count of matches for the regex `switch[[:space:]]+[^{]*\.status` is 0. Per ROADMAP §Phase 10 lock + T-10-01 mitigation.

    Tests in RespondByLabelTests (NEW — write first):

    - Test 1 (`test_respondByLabel_format_today_returnsTimeWithToday`): given a Date today at 5:00 PM, assert the formatted string contains "5:00 PM" and "today" (per UI-SPEC line 328).
    - Test 2 (`test_respondByLabel_format_tomorrow_returnsTimeWithTomorrow`): given a Date tomorrow at 5:00 PM, assert the string contains "tomorrow" or "Tomorrow".
    - Test 3 (`test_respondByLabel_format_4daysOut_returnsTimeWithWeekday`): given a Date 4 days out, assert the string contains a weekday short form (Mon/Tue/Wed/Thu/Fri/Sat/Sun).
    - Test 4 (`test_respondByLabel_format_30daysOut_returnsTimeWithMonthDay`): given a Date 30 days out, assert the string contains a month name (Jan/Feb/Mar/...) and a numeric day.
  </behavior>
  <action>
    Create `validationLedger/Features/Loads/Detail/LoadActionsView.swift`. File header block citing Phase 10 D-01 (mount index 2), D-02 (equal-width row), D-03 (destructive tint), D-04 (empty caption never collapses), D-09 (load-level ACTION-07 gate), UI-SPEC § Component Geometry line numbers. Class signature: `final class LoadActionsView: UIView`. Programmatic `init(frame: CGRect = .zero)` + `required init?(coder:) { fatalError("LoadActionsView does not support NSCoder; programmatic only — Phase 10 D-01") }`.

    Internal subview hierarchy (per UI-SPEC line 411-422):
    - `private let backingView: UIView` with `backgroundColor = DS.Colors.surface`, pinned to self bounds (UI-SPEC line 421)
    - `private let buttonRow: UIStackView` with `.axis = .horizontal`, `.distribution = .fillEqually`, `.spacing = DS.Spacing.sm`, inset by `DS.Spacing.md` leading/trailing + `DS.Spacing.lg` top
    - Up to 3 lazily-allocated `UIButton` instances (created on first `configure` call when the actions list grows; reused on subsequent configures)
    - `private let spacerView: UIView` (invisible) for the single-button case (UI-SPEC line 417)
    - `private let emptyCaptionLabel: UILabel` with `DS.Typography.caption`, `DS.Colors.labelSecondary`, `numberOfLines = 0`, `textAlignment = .center`
    - `private let disabledReasonLabel: UILabel` with `DS.Typography.footnote`, `DS.Colors.labelSecondary`, `numberOfLines = 2`, `lineBreakMode = .byWordWrapping`, left-aligned
    - `private let respondByLabel: UILabel` with `DS.Typography.footnote` (color varies — see configure)

    `public func configure(actions:role:currentStatus:tenderEligibility:respondByAt:inFlight:onTap:)`:
    - If `actions.isEmpty`: buttonRow hidden, emptyCaptionLabel visible; resolve caption text per the 3-rule table (factoring / terminal / generic) using `LoadStatus.localizedDisplayName` for the terminal interpolation; disabledReasonLabel hidden; respondByLabel hidden.
    - Else: emptyCaptionLabel hidden, buttonRow visible. Reset the buttonRow's arranged subviews. For each action in `actions`, build a UIButton via `UIButton(configuration: .filled())`; set `configuration.baseBackgroundColor = DS.Colors.destructive` for `.reject` / `.cancel`, else `DS.Colors.primary`. Set the title via `LoadActionTitleResolver.title(for: action, currentStatus: currentStatus)`. Set `addAction(UIAction { [weak self] _ in onTap(action) })`. Apply `heightAnchor.constraint(greaterThanOrEqualToConstant: 50)`. For the single-action case, append an invisible `spacerView` so the row has 2 cells (UI-SPEC line 417). For the in-flight case: if `inFlight == action`, the button's `configuration.showsActivityIndicator = true` and the title alpha dims to 0; if `inFlight != nil && inFlight != action`, the button's `isEnabled = false`.
    - For ACTION-07: if `actions.contains(.tender)` AND `tenderEligibility?.canTender == false`: find the Tender button (the first .tender in actions), set its `isEnabled = false`, set the disabledReasonLabel's text to `tenderEligibility.disabledReason ?? NSLocalizedString("loads.actions.tender.disabled.generic", value: "Tender unavailable", comment: "Generic ACTION-07 fallback")`, set the disabledReasonLabel visible. Else: disabledReasonLabel hidden.
    - For the respond-by inline label: if `role ∈ {.carrier, .dispatch}` AND `currentStatus == .tendered` AND `respondByAt != nil` AND `!actions.isEmpty`: build the formatted date string per UI-SPEC line 326-331 (use a static `DateFormatter` helper file-private — see Task helper below). Visibility, color (`DS.Colors.labelSecondary` for future / `DS.Colors.destructive` for past), text. Else hidden.

    Add a file-private helper `private enum RespondByFormatter { static func format(_ date: Date) -> String }` that implements the 4-tier date formatting per UI-SPEC line 328-331 (today / tomorrow / 2-6 days / 7+ days) — exposed `internal` for `RespondByLabelTests` (or via `@testable import`; pick the cleanest seam). Returns the formatted string only — the surrounding "Respond by %@" / "Past due — respond by %@" wrapping happens in `configure(...)`.

    Add a `traitCollectionDidChange(_ previousTraitCollection: UITraitCollection?)` override that flips `buttonRow.axis` to `.vertical` when `traitCollection.preferredContentSizeCategory.isAccessibilityCategory == true` (UI-SPEC line 419).

    Set `accessibilityElements` explicitly after every configure call: visible buttons in tap order, then disabledReasonLabel (if visible), then respondByLabel (if visible), then emptyCaptionLabel (if visible). Each disabled button sets its `accessibilityHint` to the disabled reason verbatim (UI-SPEC line 575 — VoiceOver hears why the action is unavailable).

    NEVER call `RoleLoadPolicy.actions(...)` from inside `LoadActionsView`. NEVER `switch currentStatus`. The `currentStatus` argument is consumed ONLY by `LoadActionTitleResolver.title(for: .advanceStatus, currentStatus:)` and by the empty-state-caption format-string interpolation — neither is a `switch` on status (the resolver lives in Core/Load/).

    Create `validationLedgerTests/Loads/LoadActionsViewTests.swift` with Tests 1-18 above. XCTest class. Build the view in `setUp()`; reset between tests.

    Create `validationLedgerTests/Loads/Lint/LoadDetailNoStatusSwitchTests.swift` with Test 1 above. The test reads files from disk via `FileManager` — use `Bundle.module` only if Swift Package layout supports it; otherwise hardcode the path relative to `URL(fileURLWithPath: #file).deletingLastPathComponent()` with `../../../validationLedger/Features/Loads/` traversal. Document the regex in a comment: `switch[[:space:]]+[^{]*\.status` matches `switch x.status` AND `switch obj.status` AND `switch (a, b.status)` (the predictor's tuple form). To allow the predictor file's tuple, the test SCOPES to `Features/Loads/` (the predictor lives in `Core/Load/`); the predictor is exempt by location.

    Create `validationLedgerTests/Loads/RespondByLabelTests.swift` with Tests 1-4 above. The tests call the file-private `RespondByFormatter.format(_:)` via `@testable import validationLedger`.
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/LoadActionsViewTests -only-testing:validationLedgerTests/Loads/Lint/LoadDetailNoStatusSwitchTests -only-testing:validationLedgerTests/Loads/RespondByLabelTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -60</automated>
  </verify>
  <done>
    - All 18 LoadActionsViewTests + 1 lint test + 4 RespondByLabelTests are green
    - Source assertion: `grep -c 'final class LoadActionsView: UIView' validationLedger/Features/Loads/Detail/LoadActionsView.swift` returns `1`
    - Source assertion: `grep -nE 'required init\?\(coder:\)' validationLedger/Features/Loads/Detail/LoadActionsView.swift | grep -c 'fatalError'` returns at least `1`
    - Source assertion: `grep -nE 'switch[[:space:]]+[^{]*\.status' validationLedger/Features/Loads/Detail/LoadActionsView.swift | grep -v '^[^:]*://' | (! grep .)` (the view never switches on status — the lint contract holds in source)
    - Lint test green proves the WHOLE `Features/Loads/` tree is clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount LoadActionsView in LoadDetailBodyView, wire render(state:) for .actionInFlight + .actionFailed, stub chain overlay API, extend skeleton</name>
  <files>validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift, validationLedger/Features/Loads/Detail/LoadDetailViewController.swift, validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift</files>
  <read_first>
    - validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift (FULL file — note the existing 5-child contentStack arrangement at lines 200-298, the `inject(into:)` helper pattern at lines 38-48)
    - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift (FULL file — note the existing `render(state:)` at lines 1110-1126, the `applyLoadedRender(load:chainOfTrust:)` at line 1137+, the body container hierarchy, the size-class routing from Phase 9.1)
    - validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift (FULL file — the existing skeleton arrangement; you'll add a skeleton-button-row section)
    - validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift (Plan 03 — note the new State cases + `viewModel.role` + `viewModel.submit(action:body:)` signature)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §10 (the verbatim contentStack insertion: actionsContainer at index 2)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §11 (the verbatim render extension; the two new cases call `applyLoadedRender(load: predicted/rollbackTo, chainOfTrust: frozenChain)` then mount/dismount the overlay)
    - .planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md § Pattern 2 (NEVER swap view controllers; toggle `isHidden`); § Pitfall 1 (single chainOverlay ref); § Pitfall 6 (in-flight uses pre-tap actions snapshot — the VC stores the pre-tap actions[] when submitting)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Skeleton silhouette lines 428-431 (LOCKED 2-button row at the action region's scroll-order slot)
  </read_first>
  <behavior>
    Tests folded into Plan 04's existing test file scaffolds (extend Task 1's `LoadActionsViewTests` with a new VC integration test, OR add a small new file `LoadDetailViewControllerActionRenderTests.swift`):

    - Test 1 (`test_render_loadedState_configuresActionRegionWithCorrectPolicy`): construct a VC with VM(role: .broker, loadID: "VL-1"), set state to `.loaded(<load with .posted>, <chain>)`, force a render, assert the body view's actions subview's `configure` was called with `actions == [.tender, .cancel]` (use a spy on the LoadActionsView).
    - Test 2 (`test_render_actionInFlightState_actionsListIsPreTapSnapshot`): construct VC, set state to `.loaded(<load with .posted>, <chain>)`, capture pre-tap action set, transition to `.actionInFlight(predicted: <load with .tendered>, frozenChain: <chain>, action: .tender)`, force render, assert the actions list passed to configure() is STILL `[.tender, .cancel]` (pre-tap snapshot — Pitfall 6).
    - Test 3 (`test_render_actionFailedState_appliesRollbackLoad`): construct VC, set state to `.actionFailed(rollbackTo: <load with .posted>, frozenChain: <chain>, errorCopyKey: "loads.actions.error.tender_failed")`, force render, assert the body's status badge / status timeline rendered against the rollback load (assertable via the body view's `currentLoad` property or via mocking the LoadActionsView's last-configure call).
    - Test 4 (`test_render_actionInFlight_mountsChainOverlayStub`): force `.actionInFlight` transition, assert the VC's `chainOverlay` property is non-nil (the stub view this task provides — Plan 07 turns it into the alpha-blocking subview with the activity indicator).
    - Test 5 (`test_render_loadedFromActionInFlight_dismissesChainOverlayStub`): mount the overlay via Test 4, then transition to `.loaded(...)`, assert `chainOverlay` is nil (or marked for removal — accept either: the test asserts the dismount API was called).
    - Test 6 (`test_handleActionTap_tender_presentsTenderSheetStub`): with VM(role: .broker), tap the Tender button (via the captured onTap closure); assert the VC's `presentTenderSheet()` stub was called. (The actual sheet ships in Plan 06; this test just locks the routing.)
    - Test 7 (`test_handleActionTap_accept_callsSubmitDirectly`): tap the Accept button; assert `viewModel.submit(action: .accept, body: <body with viewModel.role>)` was called (capture via VM spy).

    Place these tests in a NEW file `validationLedgerTests/Loads/LoadDetailViewControllerActionRenderTests.swift` (separate file from Task 1's view-only tests — this file scopes to VC integration). Use a `MockLoadActionsView` (a small test double or subclass that captures the last configure-call arguments).
  </behavior>
  <action>
    Modify `validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift`:

    1. Add a `private(set) lazy var actionsContainer: UIView` to host the action region (mirrors the existing container-pattern at lines 38-48). Add `private(set) lazy var actionsView: LoadActionsView` directly accessible by the VC OR expose `actionsContainer` and have the VC inject the LoadActionsView via the existing `inject(into:)` pattern — pick the cleaner shape consistent with how the body view already handles its other children. PATTERNS.md §10 shows the `actionsContainer` host pattern (cleaner — keeps LoadActionsView VC-owned, body view stays structurally agnostic).

    2. In the `contentStack.addArrangedSubview` block around lines 214-218, insert `actionsContainer` at index 2 (between `timelineContainer` and `freightDetailsContainer`):
       ```
       contentStack.addArrangedSubview(pinnedSummaryHeader)       // index 0
       contentStack.addArrangedSubview(timelineContainer)         // index 1
       contentStack.addArrangedSubview(actionsContainer)          // index 2 NEW
       contentStack.addArrangedSubview(freightDetailsContainer)   // index 3 (was 2)
       contentStack.addArrangedSubview(partiesContainer)          // index 4 (was 3)
       contentStack.addArrangedSubview(verdictBlockContainer)     // index 5 (was 4)
       ```
       Update any explicit constraint references that might use array indices.

    3. Extend `accessibilityElements` to include the actions region in the right tap order (between the timeline and the freight rows).

    Modify `validationLedger/Features/Loads/Detail/LoadDetailViewController.swift`:

    1. Add a `private let actionsView = LoadActionsView()` stored property; in `viewDidLoad` (or wherever Phase 9's body-subview injection happens), call `bodyView.actionsContainer.addSubview(actionsView)` and pin actionsView to actionsContainer's edges.

    2. Add `private var lastConfiguredActions: [LoadAction] = []` — the pre-tap snapshot the in-flight render reads (Pitfall 6). On every `.loaded` render, this is overwritten with the newly-computed action set. On `.actionInFlight` render, it is the PRE-TAP set; on `.actionFailed` render, it is also the pre-tap set (because rollback restores pre-tap).

    3. Add `private var chainOverlay: UIView?` stored property (per Pitfall 1 — single ref).

    4. Add `private func mountChainOverlayIfNeeded()` and `private func dismissChainOverlay()` STUB methods. `mountChainOverlayIfNeeded`: if `chainOverlay == nil`, allocate a placeholder `UIView()` with `backgroundColor = DS.Colors.surface.withAlphaComponent(0.6)`, pin it over the chain region (use the existing `everyoneOnLoadStripView` / `chainOfVouchesView` / `trustGraphView` references — pin per UI-SPEC line 485-486 depending on size class), `view.addSubview(chainOverlay!)`. `dismissChainOverlay`: if `chainOverlay != nil`, `chainOverlay?.removeFromSuperview()`, set `chainOverlay = nil`. NOTE for Plan 07: replace these stubs with the alpha-fade + activity-indicator versions; the API surface (`mountChainOverlayIfNeeded` / `dismissChainOverlay`) stays the same.

    5. Add `private func presentTenderSheet()` STUB — empty body for now; Plan 06 fills it. The stub exists so Test 6 can assert routing. Add a doc-comment: "// STUB — Plan 06 fills in the actual UISheetPresentationController."

    6. Add `private func handleActionTap(_ action: LoadAction)`:
       - Guard: if `action == .tender`, call `presentTenderSheet()` and return (the sheet collects the body; on Send the sheet calls `viewModel.submit(action: .tender, body: <body>)`). For non-tender actions, build a `LoadActionEndpoint.RequestBody(actorRole: viewModel.role, targetPartyID: nil, respondByAt: nil, note: nil)` and call `Task { await viewModel.submit(action: action, body: body) }`.

    7. Extend `render(state:)` (lines 1110-1126) with two new arms per PATTERNS.md §11:
       ```
       case .actionInFlight(let predicted, let frozenChain, let action):
           applyLoadedRender(load: predicted, chainOfTrust: frozenChain)
           // Pitfall 6: use the pre-tap action set; reconfigure with inFlight: action
           actionsView.configure(
               actions: lastConfiguredActions,
               role: viewModel.role,
               currentStatus: predicted.status,
               tenderEligibility: nil,
               respondByAt: predicted.respondByAt,
               inFlight: action,
               onTap: { _ in /* in-flight ignores */ }
           )
           mountChainOverlayIfNeeded()
       case .actionFailed(let rollbackTo, let frozenChain, let errorCopyKey):
           applyLoadedRender(load: rollbackTo, chainOfTrust: frozenChain)
           actionsView.configure(
               actions: lastConfiguredActions,
               role: viewModel.role,
               currentStatus: rollbackTo.status,
               tenderEligibility: rollbackTo.tenderEligibility,
               respondByAt: rollbackTo.respondByAt,
               inFlight: nil,
               onTap: { [weak self] action in self?.handleActionTap(action) }
           )
           dismissChainOverlay()
           // Plan 07 inserts: presentToastBanner(copyKey: errorCopyKey)
           // STUB for this plan — log only; Plan 07 wires the actual banner
       ```

    8. In `applyLoadedRender(load:chainOfTrust:)`, after the existing body-rendering, compute and CACHE the pre-tap action set:
       ```
       let actions = RoleLoadPolicy.availableActions(for: viewModel.role, in: load)
       self.lastConfiguredActions = actions
       actionsView.configure(actions: actions, role: viewModel.role, currentStatus: load.status,
                             tenderEligibility: load.tenderEligibility, respondByAt: load.respondByAt,
                             inFlight: nil, onTap: { [weak self] action in self?.handleActionTap(action) })
       dismissChainOverlay()
       ```

    9. NEVER write `switch load.status` inside the VC. The action-set computation flows through `RoleLoadPolicy.availableActions(for:in:)`; button titles flow through `LoadActionTitleResolver.title(for:currentStatus:)` (already inside LoadActionsView). The VC's only `LoadStatus`-aware code is the `viewModel.role` lookup + the `load.status` read for the policy call.

    Modify `validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift`:

    1. Add a new skeleton section after the timeline-skeleton and before the freight-skeleton: a horizontal `UIStackView` `.fillEqually` with 2 rounded-rect placeholder views (`cornerRadius = 8`, height `50pt`, full width split by `DS.Spacing.sm` gap). Apply the existing shimmer mechanism from Phase 8/9. Per UI-SPEC line 428-431. DO NOT add a disabled-reason placeholder (line 431 — would mislead).

    2. Insert this skeleton section at the same scroll-order position the live action region occupies (index 2 in the body's contentStack — so the skeleton matches the body's geometry).

    Create `validationLedgerTests/Loads/LoadDetailViewControllerActionRenderTests.swift` with Tests 1-7 above. Use a `MockLoadActionsView` (subclass of `LoadActionsView` that overrides `configure(...)` to capture all arguments into a property). Use a `MockLoadDetailViewModel` (subclass exposing a state setter, capturing submit() calls).

    DO NOT implement the actual chain overlay alpha-fade + activity indicator in this plan — that's Plan 07. DO NOT implement the actual toast banner in this plan — Plan 07. DO NOT implement the actual tender sheet — Plan 06.
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/LoadActionsViewTests -only-testing:validationLedgerTests/Loads/LoadDetailViewControllerActionRenderTests -only-testing:validationLedgerTests/Loads/Lint/LoadDetailNoStatusSwitchTests -only-testing:validationLedgerTests/Loads/LoadDetailViewControllerCompositionTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -60</automated>
  </verify>
  <done>
    - All 18 LoadActionsViewTests + 7 LoadDetailViewControllerActionRenderTests + 1 lint test + 4 RespondByLabelTests green
    - Existing `LoadDetailViewControllerCompositionTests` (Phase 9) still green — proves the contentStack-index-2 insertion does not break the iPad split / size-class routing
    - Source assertion: `grep -c 'actionsContainer' validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift` returns at least `2` (declaration + addArrangedSubview)
    - Source assertion: `grep -c 'case .actionInFlight' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `1` (the render arm)
    - Source assertion: `grep -c 'case .actionFailed' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `1`
    - Source assertion: `grep -c 'private var chainOverlay: UIView?' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `1` (Pitfall 1 single ref)
    - Source assertion: `grep -c 'lastConfiguredActions' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least `3` (declaration + cache write in applyLoadedRender + read in actionInFlight render — Pitfall 6)
    - Lint test asserts no `switch.*\.status` anywhere in Features/Loads/
    - Whole-project build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view (LoadActionsView) → caller (VC) | The view trusts the actions[] list as the rendering contract; if the list is wrong, the buttons are wrong. The VC computes the list via `RoleLoadPolicy.availableActions(for:in:)`, which is the single source of truth. |
| VC → VM | The VC reads `viewModel.role` (D-22), calls `viewModel.submit(...)` for non-tender taps, and presents the sheet for tender taps. |
| user → action button | A tap on a disabled button must NOT silently submit; UI-SPEC requires the disabled treatment + the accessibilityHint announcing the reason. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | Wrong action shown for the role × status combination (privilege escalation via UI gate bypass) | mitigate | `LoadActionsView` consumes the actions[] argument verbatim; the VC computes via `RoleLoadPolicy.availableActions(for:in:)` (Plan 02 thin wrapper over the Phase 7 frozen policy); LoadDetailNoStatusSwitchTests asserts no view file branches on `load.status` directly. |
| T-10-04 (CRITICAL — platform thesis) | Elevation of Privilege | Tender to unverified counterparty | partial mitigate (this plan) | The load-level Tender button gate per `Load.tenderEligibility?.canTender == false` lands HERE (Test 7 + 8 of LoadActionsViewTests); the carrier-level picker gate lands in Plan 06 (the sheet); both gates ship in Wave 4 together. Server is canonical (Phase 7 D-18); client is defense-in-depth. |
| T-10-PR-01 | Tampering | Pitfall 6 — in-flight render against POST-tap action set causing the tapped button to vanish before its spinner animation completes | mitigate | The `lastConfiguredActions` cache + the `inFlight` parameter on `configure(...)`; Test 2 of the VC integration tests is the regression guard. |
| T-10-PR-02 | Tampering | Pitfall 5 — `loads.actions.empty.terminal.format` interpolated with raw `.inTransit` ("in_transit") instead of `.localizedDisplayName` ("in transit") | mitigate | Test 6 (`...inTransitDisplayName_hasNoUnderscore`) is the explicit regression guard; the helper is in `Core/Load/` per Plan 01. |
| T-10-PR-03 | Tampering | Pitfall 1 — chain overlay double-mount / outlives action | partial mitigate (stub) | The single `chainOverlay: UIView?` reference + `mountChainOverlayIfNeeded` idempotent semantics are stubbed here; Plan 07 finishes the alpha-fade animation cleanup. Tests 4 + 5 lock the mount/dismount API. |
| T-09-04 (extended view layer) | Information Disclosure | The view rendering a server error message instead of localized fixed copy | mitigate | This plan stubs the `.actionFailed` render to log only; Plan 07 wires the actual toast banner with a per-action localized copy resolver. The errorCopyKey is a LOCALIZATION KEY only; the actual localized string lookup in Plan 07 is the only point where a string reaches the screen. |
| T-10-PR-SC | Tampering | Package legitimacy | accept | Phase 10 installs ZERO new packages. N/A. |
</threat_model>

<verification>
After both tasks:

`xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -80`

All Loads tests green (Phase 7-9 + the new Wave-1/2/3 tests).

`grep -rnE 'switch[[:space:]]+[^{]*\.status' validationLedger/Features/Loads/ 2>/dev/null | grep -v '^[^:]*://' | (! grep .)` — zero hits in any `Features/Loads/` file. ROADMAP §Phase 10 lock satisfied.

`xcodebuild build -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20` BUILD SUCCEEDED.
</verification>

<success_criteria>
- `LoadActionsView` ships with the configure-method contract documented in the interfaces block
- `LoadDetailBodyView.contentStack` has 6 children with `actionsContainer` at index 2
- `LoadDetailViewController.render(state:)` handles all 5 State cases; the new arms re-render against predicted/rollback load; the action region uses the pre-tap action snapshot during in-flight (Pitfall 6)
- `LoadDetailSkeletonView` gains the 2-button skeleton row at the action region's scroll position
- `LoadDetailNoStatusSwitchTests` asserts zero `switch.*\.status` in `Features/Loads/` — T-10-01 mitigation
- `RespondByLabelTests` covers the 4-tier date format (today / tomorrow / weekday / month-day)
- 30 new green tests across the 3 new test files
- Existing Phase 9 composition + size-class routing tests still green
</success_criteria>

<output>
Create `.planning/phases/10-per-role-tender-accept-reject/10-04-SUMMARY.md` with: the LoadActionsView API surface, the 6-child contentStack arrangement, the chain-overlay stub API for Plan 07 to extend, the handleActionTap routing (`.tender` → sheet stub, others → submit), and the lint test's grep regex.
</output>
</output>
