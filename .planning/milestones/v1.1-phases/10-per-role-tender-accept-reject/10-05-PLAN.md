---
phase: 10-per-role-tender-accept-reject
plan: 05
type: execute
wave: 4
depends_on:
  - 10-04
files_modified:
  - validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift
  - validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift
  - validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json
  - validationLedgerTests/Loads/CarrierDirectoryDecodeTests.swift
  - validationLedgerTests/Networking/Mock/CarrierDirectoryMockTests.swift
autonomous: true
requirements:
  - ACTION-01
  - ACTION-04
user_setup: []

must_haves:
  truths:
    - "Per D-07 (tender picker source) + RESEARCH §Architectural Responsibility Map row 6: the carrier directory cannot be sourced from `ChainOfTrust.nodes` (a `.posted` load has no carrier yet — the canonical happy path). A new mock endpoint `GET /carriers/directory` exists, returning an envelope `{ carriers: [TrustNode] }` decoded via the standard `APIClient.defaultDecoder()`."
    - "Per Specifics line 263-265 + D-13 (Phase 7 fraud archetypes): the carrier-directory fixture is a PRODUCT SURFACE — ~6-8 carriers spanning all 4 `VerificationState` cases (.verified / .pending / .unverified / .flagged), with at least one carrier cross-referencing the 'Chameleon Cargo' fraud archetype as the .flagged row so the picker visibly demonstrates the platform thesis."
    - "Per the MockLoadFixtureRegistry handler convention (lines 154-167) and inline-payload discipline (lines 199-294): the carrier-directory handler is registered as a sixth MockURLProtocol handler using `make200(body: tenderCarrierDirectoryPayload, url: request.url)` where `tenderCarrierDirectoryPayload` is an inline `Data(#\"\"\"…\"\"\"#.utf8)` literal in the registry — keeping the DEBUG App bundle independent of the test bundle's .json files (per existing registry invariant)."
    - "Per CLAUDE.md zero-PII discipline + T-08-04: the directory's carrier names are SYNTHETIC (no real-world carrier names); the fixture decodes to TrustNode (Phase 7 frozen type — no new domain type)."
  artifacts:
    - path: "validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift"
      provides: "Typed APIEndpoint: GET /carriers/directory; Response = { carriers: [TrustNode] }"
      contains: "public struct CarrierDirectoryEndpoint: APIEndpoint"
    - path: "validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json"
      provides: "~6-8 synthetic carriers spanning .verified / .pending / .unverified / .flagged (D-07); cross-references the .flagged fraud archetype (Specifics line 264)"
      contains: "carriers"
    - path: "validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift"
      provides: "New `tenderCarrierDirectoryPayload` static Data + new `MockURLProtocol.register` handler for GET /carriers/directory (additive; existing handlers unchanged)"
      contains: "tenderCarrierDirectoryPayload"
    - path: "validationLedgerTests/Loads/CarrierDirectoryDecodeTests.swift"
      provides: "Decode coverage: the fixture decodes via APIClient.defaultDecoder(); all 4 VerificationState cases present; carriers count matches; the flagged archetype is present"
      contains: "CarrierDirectoryDecodeTests"
    - path: "validationLedgerTests/Networking/Mock/CarrierDirectoryMockTests.swift"
      provides: "End-to-end: `apiClient.request(CarrierDirectoryEndpoint())` returns the decoded envelope when MockURLProtocol is active; tests the registration-order invariant (the directory handler responds to GET /carriers/directory and does NOT shadow the GET /loads/{id} or POST /loads/{id}/{action} handlers)"
      contains: "CarrierDirectoryMockTests"
  key_links:
    - from: "validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift"
      to: "validationLedger/Core/Load/ChainOfTrust.swift (TrustNode)"
      via: "Response carries [TrustNode] — reuses the existing Phase 7 frozen type"
      pattern: "carriers: \\[TrustNode\\]"
    - from: "validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift"
      to: "validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json"
      via: "the fixture's JSON content is inlined into the registry as a Data literal — the test bundle's .json file is the source-of-truth for the inline content"
      pattern: "tenderCarrierDirectoryPayload"
---

<objective>
Add the `GET /carriers/directory` typed endpoint, the static demo carrier-directory fixture spanning all 4 verification states, and the mock-registry handler that returns the directory under MockURLProtocol — so Plan 06's `TenderSheetViewController` has a real data source for its picker. The directory must include at least one `.flagged` carrier cross-referencing the Phase 7 fraud archetype (Specifics line 263-265) so the broker visibly meets the platform thesis the first time the sheet opens.

Purpose: Plan 06 (tender sheet) is the highest-stakes single UI surface in Phase 10 — its picker IS the ACTION-04 enforcement gate. Without a real directory endpoint + fixture, the sheet has nothing to render. Splitting the directory data layer into a dedicated plan keeps Plan 06's scope focused on the sheet UX + the visible-but-disabled per-carrier gate.

Output: 1 new endpoint file; 1 new fixture JSON; 1 modified mock registry; 2 new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md
@.planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md
@.planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md
@.planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md
@.planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md
@.planning/phases/10-per-role-tender-accept-reject/10-04-SUMMARY.md

# Files this plan modifies
@validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift

# Analog files (read for shape)
@validationLedger/Core/Networking/Endpoints/LoadActionEndpoint.swift
@validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift
@validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift
@validationLedger/Core/Load/ChainOfTrust.swift

<interfaces>
<!-- Phase 7 frozen TrustNode — what the directory decodes to -->

From `validationLedger/Core/Load/ChainOfTrust.swift` (FROZEN):
```swift
public struct TrustNode: Decodable, Sendable, Identifiable {
    public let partyID: String
    public let role: Role
    public let displayName: String
    public let verificationState: VerificationState
    public let kycCompletedAt: Date?
    public let deviceBindingStatus: DeviceBindingStatus?
    public let usdotNumber: String?
    public let usdotAuthorityStatus: USDOTAuthorityStatus?
    public let priorRelationships: [PriorRelationship]
    public var id: String { partyID }
}
```

From `validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift` (envelope precedent — FROZEN):
```swift
public struct Response: Decodable, Sendable {
    public let loads: [LoadListItem]    // ← envelope shape; carriers: [TrustNode] mirrors this
}
```

UI-SPEC line 444 (tender picker carrier row composition — what the directory drives):
- `RoleAvatarView(.tree)` + `displayName` + `VerificationBadgeView` + (if disabled) verification reason line
- Carrier names display verbatim from `TrustNode.displayName`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CarrierDirectoryEndpoint + fixture JSON + decode tests</name>
  <files>validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift, validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json, validationLedgerTests/Loads/CarrierDirectoryDecodeTests.swift</files>
  <read_first>
    - validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift (FULL — closest envelope-style GET endpoint analog; mirror its `Response` struct shape, `path` constant, `method = .get`, and `body = nil` discipline)
    - validationLedger/Core/Networking/Endpoints/LoadActionEndpoint.swift (FULL — note the `nonisolated public struct … : APIEndpoint` requirement under MainActor default isolation)
    - validationLedger/Core/Load/ChainOfTrust.swift (FULL — TrustNode shape; verify ALL stored properties are accounted for in the fixture)
    - validationLedger/Core/Load/VerificationState.swift (FULL — confirm the 4 case raw values: "verified", "pending", "unverified", "flagged")
    - validationLedger/Core/Load/PriorRelationship.swift (Phase 9 — note the new `priorRelationships` field on TrustNode; the carrier-directory fixture's carriers should have plausible `priorRelationships` arrays — empty is acceptable for synthetic carriers)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-001.json (or similar existing fixture — verify the exact JSON shape and snake_case conventions for TrustNode the standard decoder consumes)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §2 (verbatim endpoint shape lines 119-127); §4 RESEARCH § Code Examples lines 605-642 (the inline payload pattern with sample carriers including "Chameleon Cargo LLC")
    - .planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md § Specifics line 263-265 (the directory as a product surface — Chameleon Cargo as the .flagged archetype)
  </read_first>
  <behavior>
    Tests in CarrierDirectoryDecodeTests (NEW — write first):

    - Test 1 (`test_fixture_decodesViaDefaultDecoder`): load `tender-carrier-directory.json` from the test bundle, decode via `APIClient.defaultDecoder()` (or the equivalent decoder factory the Phase 7 tests use), assert the result is a `CarrierDirectoryEndpoint.Response` with `carriers.count >= 6` and `carriers.count <= 8`.
    - Test 2 (`test_fixture_includesAllFourVerificationStates`): assert the `Set(carriers.map(\.verificationState))` equals `Set([.verified, .pending, .unverified, .flagged])` (all 4 states present per D-07).
    - Test 3 (`test_fixture_includesChameleonCargoAsFlagged`): assert there is at least one carrier whose `displayName` matches a "Chameleon" pattern (case-insensitive `contains("chameleon")`) AND `verificationState == .flagged` — the fraud-archetype anchor per Specifics line 264.
    - Test 4 (`test_fixture_allCarriersHaveCarrierRole`): assert every carrier's `role == .carrier`.
    - Test 5 (`test_fixture_carriersAreSyntheticNotRealNames`): assert no carrier `displayName` contains a string matching the real carrier names from the v1.0 zero-PII allowlist (e.g. "Schneider", "Knight", "Werner", "JB Hunt", "Swift", "FedEx", "UPS" — the planner picks a small allowlist OR documents the synthetic-name convention in a fixture-header comment). T-08-04 mitigation.
    - Test 6 (`test_fixture_hasAtLeastOneVerifiedCarrier`): assert there exists at least one carrier with `verificationState == .verified` (otherwise the Send button would be permanently disabled, blocking the happy path).
    - Test 7 (`test_endpoint_path`): construct `CarrierDirectoryEndpoint()`; assert `.path == "/carriers/directory"` and `.method == .get`.
  </behavior>
  <action>
    Create `validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift`. File header block citing Phase 10 D-07 (static demo directory), RESEARCH § Architectural Responsibility Map row 6 (cannot be sourced from ChainOfTrust.nodes), and the LoadListEndpoint envelope precedent. Implementation per PATTERNS.md §2:

    ```
    nonisolated public struct CarrierDirectoryEndpoint: APIEndpoint {
        public struct Response: Decodable, Sendable {
            public let carriers: [TrustNode]
        }
        public let path = "/carriers/directory"
        public let method: HTTPMethod = .get
        public let body: EmptyBody? = nil      // verify the project's GET-no-body type; if it's `Never` or a different idiom, match the LoadListEndpoint precedent verbatim
        public init() {}
    }
    ```

    Verify the `body` type by reading `LoadListEndpoint.swift` — match its exact GET-no-body shape (it may be `let body: Never? = nil`, or `let body: EmptyBody? = nil`, or a typealias). Mirror EXACTLY.

    Create `validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json`. Envelope: `{ "carriers": [ ... ] }`. Populate with 7 carriers (a nice middle of the D-07 range) covering:
    1. `.verified`, "Acme Logistics Inc." — strong KYC + active USDOT, prior relationships array empty
    2. `.verified`, "Northwind Freight Co." — second .verified so the picker shows multiple selectable rows
    3. `.pending`, "Cascadia Cartage" — KYC in progress (kycCompletedAt: null), pending USDOT
    4. `.pending`, "Summit Routing LLC" — second .pending so the disabled state has multiple examples
    5. `.unverified`, "Anonymous Hauling" — no KYC, no USDOT
    6. `.flagged`, "Chameleon Cargo LLC" — the fraud archetype per Specifics line 264; deviceBindingStatus: "mismatched", usdotAuthorityStatus: "revoked"
    7. `.flagged`, "Phantom Express" — second .flagged so the disabled-with-reason rendering has multiple examples; deviceBindingStatus: "unknown", usdotAuthorityStatus: "suspended"

    Use snake_case JSON keys per the project's standard decoder convention (`.convertFromSnakeCase`). Every TrustNode field present (partyID, role, displayName, verificationState, kycCompletedAt, deviceBindingStatus, usdotNumber, usdotAuthorityStatus, priorRelationships — even if empty array). Verify field names against the existing `load-detail-VL-*.json` fixtures.

    Header comment in the fixture (top of file, as a comment ABOVE the JSON OR inside the JSON as a `_comment` field that gets ignored by the decoder per project convention — pick whichever the existing fixtures use): "Phase 10 D-07. Static demo carrier directory for tender sheet picker. ~7 synthetic carriers spanning all 4 VerificationState cases; Chameleon Cargo + Phantom Express are fraud-archetype anchors (Specifics line 263-265). NO real-world carrier names — T-08-04 zero-PII discipline."

    Create `validationLedgerTests/Loads/CarrierDirectoryDecodeTests.swift` with Tests 1-7 above. Use the existing fixture-loading helper (read another Phase 7/8/9 decode test to find the loader pattern — e.g. `LoadListEnvelopeDecodeTests` likely shows the path).
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/CarrierDirectoryDecodeTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All 7 CarrierDirectoryDecodeTests are green
    - Source assertion: `grep -c '^nonisolated public struct CarrierDirectoryEndpoint' validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift` returns `1`
    - Source assertion: `grep -c 'public let path = "/carriers/directory"' validationLedger/Core/Networking/Endpoints/CarrierDirectoryEndpoint.swift` returns `1`
    - Source assertion: `test -f validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json && echo OK` outputs `OK`
    - Whole-project build succeeds
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the carrier-directory MockURLProtocol handler + integration mock tests</name>
  <files>validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift, validationLedgerTests/Networking/Mock/CarrierDirectoryMockTests.swift</files>
  <read_first>
    - validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift (FULL — focus on the existing 3 handlers at lines 128-167; the `make200` helper at lines 171-179; the `actionPathSegments` static set at lines 187-189; the inline payload literals at lines 199-294; the file-invariant comment about the DEBUG App bundle independence)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §3 (the verbatim handler-registration shape; lines 175-183 are the carrier-directory sketch)
    - .planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md § Code Examples lines 603-642 (the same sketch with the inline payload pattern)
    - validationLedgerTests/Networking/Mock/ (browse existing mock-integration tests for the test pattern)
    - validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json (Task 1 output — copy its bytes into the registry's inline payload literal)
  </read_first>
  <behavior>
    Tests in CarrierDirectoryMockTests (NEW — write first):

    - Test 1 (`test_apiClient_request_returnsDecodedDirectory`): with `MockURLProtocol` active (registered via `MockLoadFixtureRegistry.registerAppDefaults()`), call `apiClient.request(CarrierDirectoryEndpoint())`; assert the returned `Response.carriers.count >= 6` and includes the chameleon carrier.
    - Test 2 (`test_directoryHandler_doesNotShadowLoadDetail`): request `LoadDetailEndpoint(loadID: "VL-001")`; assert it still returns the existing load-detail fixture (proves the new directory handler's GET / path-prefix guard does not over-match).
    - Test 3 (`test_directoryHandler_doesNotShadowLoadList`): request `LoadListEndpoint(role: .broker)`; assert it still returns the broker-role list fixture (same shadow-check from the other angle).
    - Test 4 (`test_directoryHandler_doesNotShadowLoadAction`): POST `LoadActionEndpoint(loadID: "VL-001", action: .accept, body: <body>)`; assert it still returns `load-action-success` JSON (proves the directory handler — which guards on `.httpMethod == "GET"` — does not intercept POSTs).
    - Test 5 (`test_directoryHandler_returnsSameContentAsFixture`): load the fixture JSON via the existing fixture-loader helper; request the endpoint; assert the response's carriers array (e.g. count + IDs) matches the fixture's carriers exactly. Proves the inline-in-registry payload is byte-faithful to the fixture file (manual sync check — a future fixture edit MUST update both).
  </behavior>
  <action>
    Modify `validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift`:

    1. Add a new private static `tenderCarrierDirectoryPayload: Data` constant near the existing payload constants (lines 199-294). Use the `Data(#"""…"""#.utf8)` raw-string literal pattern. INLINE the carrier-directory JSON byte-for-byte from `validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json` (Task 1 output). Add a leading comment: "// Phase 10 D-07. Inlined from validationLedgerTests/Networking/Fixtures/tender-carrier-directory.json — keep these two in sync; a future fixture edit MUST update both (Test 5 of CarrierDirectoryMockTests is the regression guard)."

    2. Add a new handler registration in `registerAppDefaults()` AFTER the existing 3 handlers (the directory handler is independent of the action / load-detail / load-list handlers and order does not matter for shadowing — the path-guards are disjoint). Per PATTERNS.md §3 lines 175-183:

       ```
       // (4) NEW — Phase 10 D-07: GET /carriers/directory → tender-carrier-directory payload
       MockURLProtocol.register { request in
           guard request.httpMethod == "GET" else { return nil }
           guard request.url?.path == "/carriers/directory" else { return nil }
           return make200(body: tenderCarrierDirectoryPayload, url: request.url)
       }
       ```

    3. DO NOT change the existing 3 handlers (action-success / load-detail / load-list). DO NOT change `make200`, `actionPathSegments`, or the inline payload literals for those. Phase 7 contract preserved.

    Create `validationLedgerTests/Networking/Mock/CarrierDirectoryMockTests.swift` with Tests 1-5 above. The test file inherits the `MockURLProtocol`-registered `APIClient` setUp pattern from sibling `Mock/` tests (read one to confirm the recipe — typically there's a `setUp()` that calls `MockLoadFixtureRegistry.registerAppDefaults()` and builds an APIClient with the mock URLSession).
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Networking/Mock/CarrierDirectoryMockTests -only-testing:validationLedgerTests/Loads/CarrierDirectoryDecodeTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All 5 CarrierDirectoryMockTests + 7 CarrierDirectoryDecodeTests green
    - Source assertion: `grep -c 'tenderCarrierDirectoryPayload' validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift` returns at least `2` (declaration + handler usage)
    - Source assertion: `grep -c '/carriers/directory' validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift` returns at least `1`
    - The existing Phase 7 mock-fixture tests (`MockLoadFixtureRegistryTests` or similar) remain green — additive change to the registry has not broken the action / detail / list handlers
    - Whole-project build succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → mock network | The mock handler returns server-shape data; the client decoder fails closed on contract drift (Phase 7 D-09 + Decodable synthesized init throwing). |
| fixture → snapshot/CI | Carrier names must be synthetic (no real-world brand names); CarrierDirectoryDecodeTests Test 5 enforces this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 (partial — directory data layer) | Information Disclosure / Reputation | A real carrier name appearing in CI snapshots or in customer-facing demo builds | mitigate | Test 5 (`test_fixture_carriersAreSyntheticNotRealNames`) asserts the synthetic-name discipline; fixture-header comment documents the convention. |
| T-10-PR-01 | Tampering | A future fixture edit drifts from the inline registry payload, causing decode-tests vs mock-tests to disagree | mitigate | Test 5 of CarrierDirectoryMockTests compares the response's carriers to the fixture's carriers byte-for-byte (well, list-for-list); regression guard for the inline-keep-in-sync convention. |
| T-10-PR-02 | Tampering | The directory handler shadows GET /loads/{id} or POST /loads/{id}/{action} | mitigate | Tests 2-4 explicitly assert the four other Phase 7 endpoints continue to receive their proper fixtures after the directory handler is registered. The directory handler's `path == "/carriers/directory"` exact-match guard is the structural defense. |
| T-10-PR-SC | Tampering | Package legitimacy | accept | Phase 10 installs ZERO new packages. N/A. |
</threat_model>

<verification>
After both tasks:

`xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/CarrierDirectoryDecodeTests -only-testing:validationLedgerTests/Networking/Mock -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -40`

All directory tests + the existing Networking/Mock/* tests green.
</verification>

<success_criteria>
- `CarrierDirectoryEndpoint.swift` exists, conforms to `APIEndpoint`, GET /carriers/directory
- `tender-carrier-directory.json` fixture present with 7 synthetic carriers spanning all 4 VerificationState cases including a Chameleon Cargo flagged anchor
- MockLoadFixtureRegistry has the inline payload + handler registration; existing 3 handlers unchanged
- 12 new green tests (7 decode + 5 mock)
- No Phase 7 mock-registry test regresses
</success_criteria>

<output>
Create `.planning/phases/10-per-role-tender-accept-reject/10-05-SUMMARY.md` with: the endpoint surface, the 7-carrier fixture roster (name + verification state for each), the inline-in-registry payload convention (so a future fixture edit knows to update both files), and notes for Plan 06 on how `TenderSheetViewController` should request the directory.
</output>
