---
phase: 10-per-role-tender-accept-reject
plan: 09
type: execute
wave: 6
depends_on:
  - 10-04
  - 10-06
  - 10-07
  - 10-08
files_modified:
  - validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests.swift
  - validationLedgerTests/Loads/Snapshot/TenderSheetViewControllerSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/LoadActionToastBannerViewSnapshotTests.swift
  - validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/
autonomous: true
requirements:
  - ACTION-01
  - ACTION-04
  - ACTION-05
  - ACTION-07
  - ACTION-09
user_setup: []

must_haves:
  truths:
    - "Per ACTION-09 + UI-SPEC line 537 + VALIDATION.md § 65-cell snapshot matrix: the regression gate is a 5×13 = 65-cell snapshot matrix proving `RoleLoadPolicy.availableActions(for:in:)` ↔ `LoadActionsView` never silently drift; baselines RECORDED in this plan, VERIFY pass also runs in this plan (single RECORD-then-VERIFY cycle keeps the gate atomic)."
    - "Per UI-SPEC line 537 + Pitfall 7 (snapshot state leak): a FRESH `LoadActionsView()` is allocated per cell — no instance reuse across the 65 iterations. Helper: `UIKitSnapshot.image(of:size:)` and `UIKitSnapshot.attach(_:name:to:)` (Phase 8 in-house) at fixed 393×120pt canvas."
    - "Per VALIDATION.md ACTION-04 row + ACTION-07: an ADDITIONAL 5-test set covers the load-level `tenderEligibility.canTender == false` variant (one per role) — these are NOT part of the 65-cell matrix; they are a separate test set within the same file."
    - "Per UI-SPEC line 297-313 + Pitfall 5: an ADDITIONAL 2-test set covers the empty-state caption variants (Factoring caption + terminal-state non-Factoring caption like `.delivered` to lock the LoadStatus.localizedDisplayName interpolation)."
    - "Per UI-SPEC § Tender sheet § Component Geometry lines 433-456: tender sheet snapshot suite covers 4 scenarios — default state (no carrier selected), all-verified directory (Send enabled after picking), mixed-verification directory (rows show the visible-but-disabled treatment), and Send in-flight state (spinner overlay)."
    - "Per UI-SPEC § LoadActionToastBannerView lines 458-475: toast banner snapshot suite covers 6 scenarios — one per LOCKED per-action error key (UI-SPEC line 343-348). Each cell renders the banner pre-animation (at rest state), against a black backing canvas to make the rounded-rect chrome legible."
    - "Per Phase 9.1 D-05 + RESEARCH § Pattern 6 line 460: snapshot baselines are RECORDED in this plan run (first time) via the project's standard record-snapshot workflow; subsequent runs VERIFY against the baselines. A diff > pixel-difference threshold fails the test."
    - "Per CLAUDE.md zero-PII + T-08-04: every snapshot renders synthetic identifiers — no real party/carrier names; the matrix uses a small file-private `Load.fixture(for:)` factory so snapshot artifacts are PII-safe for CI."
  artifacts:
    - path: "validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests.swift"
      provides: "65-cell (Role × LoadStatus) matrix test + 5-cell tenderEligibility-disabled variant tests + 2-cell empty-state caption tests = 72 snapshot assertions total"
      contains: "test_actionRegion_matrix_5roles_x_13statuses"
    - path: "validationLedgerTests/Loads/Snapshot/TenderSheetViewControllerSnapshotTests.swift"
      provides: "4-scenario tender sheet snapshot suite covering default / all-verified / mixed-verification / Send-in-flight states"
      contains: "TenderSheetViewControllerSnapshotTests"
    - path: "validationLedgerTests/Loads/Snapshot/LoadActionToastBannerViewSnapshotTests.swift"
      provides: "6-scenario toast banner snapshot suite — one per LOCKED per-action error key"
      contains: "LoadActionToastBannerViewSnapshotTests"
    - path: "validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/"
      provides: "Baseline PNG directory; 72 baselines for the action-bar matrix + variants"
      contains: "actions-broker-posted"
  key_links:
    - from: "validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests.swift"
      to: "validationLedger/Core/Load/RoleLoadPolicy.swift"
      via: "every cell renders the policy's output for (role, status)"
      pattern: "RoleLoadPolicy\\.(actions|availableActions)"
    - from: "validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests.swift"
      to: "validationLedger/Features/Loads/Detail/LoadActionsView.swift"
      via: "the view under test"
      pattern: "LoadActionsView\\(\\)"
    - from: "validationLedgerTests/Loads/Snapshot/TenderSheetViewControllerSnapshotTests.swift"
      to: "validationLedger/Features/Loads/Detail/TenderSheetViewController.swift"
      via: "Plan 06 sheet under test"
      pattern: "TenderSheetViewController\\("
---

<objective>
Land the 65-cell (Role × LoadStatus) action-region snapshot matrix per UI-SPEC line 537 + VALIDATION.md § 65-Cell Snapshot Matrix — the regression gate that proves `RoleLoadPolicy.availableActions(for:in:)` and `LoadActionsView`'s rendering never silently drift; plus the 5-cell tenderEligibility-disabled variant (ACTION-04 / ACTION-07 visual lock), the 2-cell empty-state caption variant (Pitfall 5 visual lock), the 4-scenario tender sheet snapshot suite, and the 6-scenario toast banner snapshot suite. Total: 72 + 4 + 6 = 82 snapshot baselines.

Purpose: ACTION-09 (5×13 policy table is the truth) needs a VISIBLE regression gate — if a future plan accidentally changes a button title, a button order, a destructive tint, the empty-state caption format, or the tenderEligibility-disabled visual, the snapshot diff catches it. UI-SPEC line 537 EXPLICITLY assigns this gate to Phase 10's planner. Per Phase 9.1 D-05 baseline-record precedent, the RECORD pass runs as part of this plan and the VERIFY pass runs against those baselines so the gate is atomic — no "record now, verify in some later plan" temporal split.

Output: 3 new snapshot test files; 1 baselines directory populated with 82 PNGs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md
@.planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md
@.planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md
@.planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md
@.planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md
@.planning/phases/10-per-role-tender-accept-reject/10-04-SUMMARY.md
@.planning/phases/10-per-role-tender-accept-reject/10-06-SUMMARY.md
@.planning/phases/10-per-role-tender-accept-reject/10-07-SUMMARY.md

# Files under test
@validationLedger/Features/Loads/Detail/LoadActionsView.swift
@validationLedger/Features/Loads/Detail/TenderSheetViewController.swift
@validationLedger/Features/Loads/Detail/LoadActionToastBannerView.swift
@validationLedger/Core/Load/RoleLoadPolicy.swift

# Snapshot infrastructure + analogs
@validationLedgerTests/Support/UIKitSnapshot.swift
@validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift
@validationLedgerTests/Loads/Snapshot/VerificationBasisSheetViewControllerSnapshotTests.swift
@validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift

<interfaces>
<!-- The snapshot helper API (Phase 8 in-house) -->

From `validationLedgerTests/Support/UIKitSnapshot.swift` (VERIFIED):
```swift
public enum UIKitSnapshot {
    public static func image(of view: UIView, size: CGSize) -> UIImage
    public static func attach(_ image: UIImage, name: String, to testCase: XCTestCase)
    // Baseline comparison: read the project's existing snapshot tests to find the assertion shape —
    // typically `attach` writes to __Snapshots__/ and a pixel-diff helper compares against a baseline.
    // Phase 9.1 D-05 RE-RECORD precedent shows the workflow.
}
```

UI-SPEC + ACTION-09 requirement: the 65-cell matrix uses a single canvas size for all cells (visual consistency):
- Canvas: `CGSize(width: 393, height: 120)` per PATTERNS.md §13 line 571

The 5×13 = 65 (Role × LoadStatus) cross-product is:
- Roles: `.shipper, .broker, .carrier, .dispatch, .factoring` (5)
- LoadStatus.allCases: 13 cases per the frozen enum

Pitfall 7 (snapshot state leak): "Allocate a fresh `LoadActionsView()` for each cell. Cost is trivial (65 small UIView allocations per test run)." — DO NOT reuse a single LoadActionsView instance.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 65-cell action-bar snapshot matrix + tenderEligibility-disabled variant + empty-state variant — RECORD and VERIFY in one pass</name>
  <files>validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests.swift, validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/ (directory, populated with PNG baselines)</files>
  <read_first>
    - validationLedgerTests/Support/UIKitSnapshot.swift (FULL — confirm the helper's exact API + the baseline-write-and-compare workflow Phase 8 / 9 / 9.1 use)
    - validationLedgerTests/Loads/Snapshot/LoadStatusBadgeViewSnapshotTests.swift (FULL — Phase 8 analog of a cross-product snapshot; mirror its `for status in LoadStatus.allCases` shape per PATTERNS.md §13 line 549-558)
    - validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift (FULL — for the toast banner snapshot pattern in Task 3)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §13 (verbatim 65-cell matrix shape lines 561-578); § Pitfall 7 (fresh allocation per cell)
    - .planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md § 65-Cell Snapshot Matrix (the record + verify command shapes; the baseline directory `validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/`)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Per-Action State / Visibility Matrix lines 510-538 (the table the snapshot matrix asserts against)
  </read_first>
  <behavior>
    Tests in LoadActionBarSnapshotMatrixTests (NEW — write first; XCTest class — Swift Testing does NOT support `XCTestCase.add(_:)` which UIKitSnapshot.attach needs per PATTERNS.md §13 line 581):

    - Test 1 (`test_actionRegion_matrix_5roles_x_13statuses`): iterate `for role in Role.allCases` + `for status in LoadStatus.allCases`. For each cell: allocate a FRESH `LoadActionsView()`; build a synthetic `Load` with that status; call `view.configure(actions: RoleLoadPolicy.availableActions(for: role, in: load), role: role, currentStatus: status, tenderEligibility: nil, respondByAt: nil, inFlight: nil, onTap: { _ in })`; render via `UIKitSnapshot.image(of: view, size: CGSize(width: 393, height: 120))`; attach via `UIKitSnapshot.attach(image, name: "actions-\(role.rawValue)-\(status.rawValue)", to: self)`; compare against the baseline using the project's assertion helper (read the analog test to find the exact assertion call — typically `XCTAssertEqual(image.pngData(), baselineImage.pngData())` with a pixel-diff threshold, OR `assertSnapshot(view, named: ...)` if the helper exists).
    - Test 2 (`test_actionRegion_tenderEligibilityDisabled_5variants`): for each of the 5 roles in `Role.allCases`, configure with `actions: [.tender, .cancel]`, `currentStatus: .posted`, `tenderEligibility: TenderEligibility(canTender: false, disabledReason: "Counterparty USDOT authority suspended")`; render + attach with name `"actions-disabled-\(role.rawValue)"`. The 5 baselines capture the Tender button's disabled visual + the inline reason label across roles. (Some roles do not have .tender in their action set — Factoring/Carrier/Dispatch — for those, the gate test is informational only; the EXPECTED behavior is that the disabled-reason label is hidden because Tender is not in the actions list. Snapshot the actual rendering — it should match the Test 1 cell for the same role+status.)
    - Test 3 (`test_actionRegion_emptyStateCaption_factoring`): configure with `actions: []`, `role: .factoring`, `currentStatus: .posted`; render + attach as `"actions-empty-factoring"`. Baseline captures the empty-caption visual + the "view-only role" text per UI-SPEC line 303.
    - Test 4 (`test_actionRegion_emptyStateCaption_terminalDelivered`): configure with `actions: []`, `role: .broker`, `currentStatus: .delivered`; render + attach as `"actions-empty-terminal-delivered"`. Baseline captures the terminal-state caption with LoadStatus.localizedDisplayName interpolation ("This load is delivered. No actions available.") — Pitfall 5 visual lock; if a future regression makes the caption read "This load is in_transit." (with underscore for inTransit) it shows as a pixel-diff.
    - Test 5 (`test_actionRegion_emptyStateCaption_terminalInTransit_noUnderscore`): configure with `actions: []`, `role: .broker`, `currentStatus: .inTransit`; render + attach as `"actions-empty-terminal-inTransit"`. The baseline visually proves the "in transit" (with space) localization — backup to the unit test in Plan 04 Task 1 Test 6.
    - Test 6 (`test_actionRegion_inFlight_tenderTapped`): configure with `actions: [.tender, .cancel]`, `role: .broker`, `currentStatus: .posted`, `inFlight: .tender`; render + attach as `"actions-inflight-broker-tender"`. Baseline captures the in-flight visual (spinner over Tender title, both buttons disabled).
    - Test 7 (`test_actionRegion_respondByLabel_carrierTendered`): configure with `actions: [.accept, .reject]`, `role: .carrier`, `currentStatus: .tendered`, `respondByAt: Date().addingTimeInterval(24*60*60)`; render + attach as `"actions-respondby-future-carrier-tendered"`. Baseline captures the respond-by inline label.
    - Test 8 (`test_actionRegion_respondByLabel_pastDue_destructive`): same as Test 7 but `respondByAt: Date().addingTimeInterval(-3600)`; render + attach as `"actions-respondby-pastdue-carrier-tendered"`. Baseline captures the destructive-color past-due variant.

    Test infrastructure: a file-private helper `func makeLoad(status: LoadStatus, respondByAt: Date? = nil, tenderEligibility: TenderEligibility? = nil) -> Load` that builds a minimal Load fixture from in-test JSON literals — same shape as Plan 01's helper. Synthetic identifiers only (no real-world party names).

    **RECORD pass:** The first run of this test file produces baselines (if the helper writes to `__Snapshots__/` on first miss) OR fails with "baseline missing" diagnostics. Read `LoadStatusBadgeViewSnapshotTests.swift` to find the RECORD-mode convention — common patterns:
    - Environment variable `RECORD_SNAPSHOTS=YES` → write baselines + skip assertion
    - `XCTContext.runActivity` produces `.png` attachments that the developer commits as baselines after first review
    - `assertSnapshot(matching:as:record:)` with `record: true` for first run

    Document the RECORD workflow in a file-header comment block: "// PHASE 10 SNAPSHOT BASELINE RECORD INSTRUCTIONS: this file's first execution under `RECORD_SNAPSHOTS=YES` writes 80+ PNG baselines to validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/. After the first run, eyeball-review every PNG, commit the directory, then re-run WITHOUT the env var to confirm VERIFY pass. Per Phase 9.1 D-05 precedent."
  </behavior>
  <action>
    Create `validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests.swift`. XCTest class. Implement Tests 1-8 above. The cross-product sweep is the bulk — total cells: 65 (Test 1) + 5 (Test 2) + 1 (Test 3) + 1 (Test 4) + 1 (Test 5) + 1 (Test 6) + 1 (Test 7) + 1 (Test 8) = 76 snapshots. Round to 80 with any small variations the test author finds useful (e.g. Test 2 may need both with-and-without disabledReason permutations).

    Helper functions to reuse from sibling tests:
    - `func makeLoad(...)` — synthetic Load builder
    - `func renderedSnapshot(of view: UIView) -> UIImage` — wraps `UIKitSnapshot.image(of:size:)` with the standard 393×120 canvas
    - `func baselineComparison(image: UIImage, name: String)` — the assertion call matching the analog files' shape

    Mkdir + populate `validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/` via the first RECORD run. If your project's snapshot helper does not auto-create the directory, manually create it AND a `README.md` inside it documenting "Phase 10 baselines for LoadActionsView matrix — recorded YYYY-MM-DD by Plan 10-09. RE-RECORD: set RECORD_SNAPSHOTS=YES env var and re-run; eyeball-review every PNG before committing."

    The PASS criteria for this plan is BOTH: (a) the RECORD pass produces 80+ baselines, (b) the immediately-following VERIFY pass (re-run without RECORD env var) is green against those baselines. The orchestrator's automated verify command is the VERIFY pass.

    NEVER include real party names. Use synthetic strings ("Test Shipper Co.", "Test Broker LLC", "Test Carrier Inc.") in the makeLoad fixture.

    Do not modify any production-source files in this task.
  </action>
  <verify>
    <automated>RECORD_SNAPSHOTS=YES xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -30 && ls validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/*.png 2>/dev/null | wc -l | awk '$1 >= 70 { print "OK: " $1 " baselines"; exit 0 } { print "FAIL: only " $1 " baselines"; exit 1 }' && xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -30</automated>
  </verify>
  <done>
    - First xcodebuild (RECORD) passes; writes >= 70 PNG baselines to `validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/`
    - Second xcodebuild (VERIFY) is green — proves the baselines are stable
    - `ls validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/*.png | wc -l` returns >= 70
    - Source assertion: `grep -c 'for role in Role.allCases' validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests.swift` returns at least `1` (the Test 1 cross-product sweep)
    - Source assertion: `grep -c 'LoadActionsView()' validationLedgerTests/Loads/Snapshot/LoadActionBarSnapshotMatrixTests.swift` returns at least `8` (fresh allocation per test method — Pitfall 7 anchor)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TenderSheetViewController snapshot suite (4 scenarios) + LoadActionToastBannerView snapshot suite (6 scenarios)</name>
  <files>validationLedgerTests/Loads/Snapshot/TenderSheetViewControllerSnapshotTests.swift, validationLedgerTests/Loads/Snapshot/LoadActionToastBannerViewSnapshotTests.swift</files>
  <read_first>
    - validationLedgerTests/Loads/Snapshot/VerificationBasisSheetViewControllerSnapshotTests.swift (FULL — Phase 9 sheet snapshot analog; lines 40-51 = the renderedView helper + the canvas-size convention CGSize(width: 393, height: 500))
    - validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift (FULL — Phase 9 banner snapshot analog)
    - validationLedger/Features/Loads/Detail/TenderSheetViewController.swift (Plan 06 output)
    - validationLedger/Features/Loads/Detail/LoadActionToastBannerView.swift (Plan 07 output)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §14 (verbatim shape for the tender sheet snapshot — `sheetCanvasSize = CGSize(width: 393, height: 500)` + `renderedView` helper lines 596-606)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md § Tender sheet copy lines 356-386 (LOCKED strings the snapshots visually lock); § LoadActionToastBannerView lines 458-475 (LOCKED toast geometry)
  </read_first>
  <behavior>
    Tests in TenderSheetViewControllerSnapshotTests (NEW — write first):

    - Test 1 (`test_sheet_defaultState_noCarrierSelected`): construct with the full Plan 05 directory (7 carriers); render at default state (no row selected, "1 day" chip selected by default, Send disabled); attach as `"tender-sheet-default"`.
    - Test 2 (`test_sheet_verifiedCarrierSelected_sendEnabled`): construct; programmatically select the Acme row (the first verified carrier) via internal `selectedCarrierIndex` set; render; attach as `"tender-sheet-verified-selected"`. Captures the checkmark accessory + Send enabled visual + the resolved-time line.
    - Test 3 (`test_sheet_mixedDirectory_visibleButDisabledRows`): construct with a directory of 1 verified + 1 unverified + 1 pending + 1 flagged; render at default state; attach as `"tender-sheet-mixed-verification"`. Captures the visible-but-disabled row treatment (alpha 0.6 on contentView, badge at full alpha, inline reason line) — the ACTION-04 platform-thesis surface.
    - Test 4 (`test_sheet_sendInFlight_spinnerOverlay`): construct with a verified row selected; transition the sheet's internal state to in-flight (programmatically set the spinner-overlay state); attach as `"tender-sheet-send-inflight"`. Captures the spinner-over-Send-title state per UI-SPEC line 454.

    Tests in LoadActionToastBannerViewSnapshotTests (NEW — write first):

    For each of the 6 LOCKED keys (UI-SPEC line 343-348), render the banner at its REST position (post-slide-in, pre-auto-dismiss) and attach a baseline:

    - Test 1 (`test_toast_tender_failed`): configure(text: NSLocalizedString("loads.actions.error.tender_failed", value: "Couldn't send tender. Try again.", ...)); render; attach as `"toast-tender-failed"`.
    - Test 2 (`test_toast_accept_failed`): same shape for `.accept`; attach `"toast-accept-failed"`.
    - Test 3 (`test_toast_reject_failed`): same for `.reject`; attach `"toast-reject-failed"`.
    - Test 4 (`test_toast_cancel_failed`): same for `.cancel`; attach `"toast-cancel-failed"`.
    - Test 5 (`test_toast_post_failed`): same for `.post`; attach `"toast-post-failed"`.
    - Test 6 (`test_toast_advance_failed`): same for `.advanceStatus`; attach `"toast-advance-failed"`.

    Toast canvas: `CGSize(width: 393, height: 80)` (enough for the 56pt min height + the 16pt safe-area offset + a small margin). Rendered against a system-background backing UIView so the rounded-rect chrome is visible.

    Test infrastructure: each snapshot file's `renderedView(_ vc:)` helper mirrors PATTERNS.md §14 verbatim:
    ```swift
    private static let sheetCanvasSize = CGSize(width: 393, height: 500)
    private func renderedView(_ vc: TenderSheetViewController) -> UIView {
        vc.loadViewIfNeeded()
        vc.view.bounds = CGRect(origin: .zero, size: Self.sheetCanvasSize)
        vc.view.layoutIfNeeded()
        return vc.view
    }
    ```
  </behavior>
  <action>
    Create `validationLedgerTests/Loads/Snapshot/TenderSheetViewControllerSnapshotTests.swift` with Tests 1-4. XCTest class. Use `@testable import validationLedger` to access the sheet's internal state setters for Tests 2-4.

    Create `validationLedgerTests/Loads/Snapshot/LoadActionToastBannerViewSnapshotTests.swift` with Tests 1-6. XCTest class. Each test allocates a fresh `LoadActionToastBannerView()`, calls configure(text:), wraps it in a container UIView (for the backdrop), and renders + attaches.

    Run RECORD pass first to populate baselines for both files; then VERIFY pass. Total new baselines: 4 (tender sheet) + 6 (toast) = 10. Plus Task 1's 76+ = grand total ≥ 86 baselines for Phase 10.

    NEVER use real party / carrier names. Tender sheet snapshots use the Plan 05 fixture which has synthetic names per Plan 05's done step.
  </action>
  <verify>
    <automated>RECORD_SNAPSHOTS=YES xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/Snapshot/TenderSheetViewControllerSnapshotTests -only-testing:validationLedgerTests/Loads/Snapshot/LoadActionToastBannerViewSnapshotTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -30 && xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/Snapshot/TenderSheetViewControllerSnapshotTests -only-testing:validationLedgerTests/Loads/Snapshot/LoadActionToastBannerViewSnapshotTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Both test files green on VERIFY pass after RECORD pass
    - 10 new baselines (4 tender sheet + 6 toast)
    - Source assertion: `grep -c 'sheetCanvasSize = CGSize(width: 393, height: 500)' validationLedgerTests/Loads/Snapshot/TenderSheetViewControllerSnapshotTests.swift` returns `1` (PATTERNS.md §14 canvas-size convention preserved)
    - Source assertion: `grep -c 'toast-' validationLedgerTests/Loads/Snapshot/LoadActionToastBannerViewSnapshotTests.swift` returns at least `6` (one attachment name per LOCKED error key)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline PNG → CI snapshot artifact | Baselines must contain no PII (no real party / carrier names); CarrierDirectoryDecodeTests + the makeLoad helpers ensure synthetic names. |
| RECORD pass → VERIFY pass | Both passes must run in the same plan to keep the gate atomic — a future plan that records new baselines without verifying could ship a regression. Done step asserts both passes are green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 (visual regression) | Elevation of Privilege | A future plan accidentally adds a button to the action region without updating the policy table, or changes a button title to a non-localized string | mitigate | 65-cell matrix asserts the rendered cell matches the baseline; a missing button or extra button shows as a pixel-diff failure; the developer MUST consciously re-record with eyeball review per Phase 9.1 D-05 precedent. |
| T-10-04 (visual lock) | Information Disclosure | The visible-but-disabled carrier-row treatment regresses (e.g. badge accidentally dims with the row) | mitigate | Test 3 of TenderSheetViewControllerSnapshotTests is the visual lock — a future "let's just gray everything out" refactor shows as a pixel-diff. |
| T-09-04 (extended visual lock) | Information Disclosure | A future regression makes a toast banner render the LOCALIZATION KEY ("loads.actions.error.tender_failed") instead of the resolved string | mitigate | The 6 toast snapshots assert the rendered text visually; a key-vs-string regression shows as a diff (the baselines are the English fallback strings per UI-SPEC line 343-348). |
| T-10-PR-01 | Tampering | Pitfall 7 — snapshot state leak across iterations | mitigate | Source assertion: at least 8 `LoadActionsView()` allocations across Task 1's test methods (one per test method, plus Test 1's 65 in the inner loop). Each test method's `LoadActionsView()` is fresh; the 65-cell loop also allocates fresh per cell (visual-diff fails if state leaks because adjacent cells would have leftover styling). |
| T-10-PR-02 | Information Disclosure | Synthetic carrier names in baselines accidentally replaced with real names | mitigate | Plan 05's CarrierDirectoryDecodeTests Test 5 already asserts synthetic discipline at the fixture level; Plan 09's snapshots inherit that discipline. |
| T-10-PR-SC | Tampering | Package legitimacy | accept | Phase 10 installs ZERO new packages (UIKitSnapshot is in-house). N/A. |
</threat_model>

<verification>
After both tasks:

`xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/Snapshot -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -60`

All Phase 10 snapshot suites green AND all Phase 7-9 snapshot suites still green.

`ls validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/*.png | wc -l` returns >= 70 — baseline count.

VALIDATION.md § 65-Cell Snapshot Matrix sign-off: this plan completes the "Wave 4 — after action region + tender sheet have settled and DEBUG toggles are wired" gate.
</verification>

<success_criteria>
- 65-cell (Role × LoadStatus) baselines exist and the VERIFY pass is green
- 5-cell tenderEligibility-disabled variant baselines exist
- 2 empty-state caption baselines exist (including Pitfall 5 in-transit no-underscore visual lock)
- 4 tender sheet baselines exist (default / verified-selected / mixed / send-in-flight)
- 6 toast banner baselines exist (one per LOCKED per-action error key)
- VALIDATION.md ACTION-09 row's named test (`LoadActionBarSnapshotMatrixTests/test_actionRegion_matrix_5roles_x_13statuses`) exists and is green
- ZERO PII in any baseline image; ZERO real-world party/carrier names
- Baselines directory committed to git per the project's snapshot convention
</success_criteria>

<output>
Create `.planning/phases/10-per-role-tender-accept-reject/10-09-SUMMARY.md` with: total baseline count, the RECORD-then-VERIFY workflow used, the per-test-file baseline counts (matrix 70+ / sheet 4 / toast 6), the baseline directory path, and notes for future plans on how to RE-RECORD when a deliberate UI change lands (per Phase 9.1 D-05 precedent).
</output>
