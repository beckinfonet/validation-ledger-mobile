---
phase: 09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone
plan: 02
type: execute
wave: 2
depends_on: [09.1-01]
files_modified:
  - validationLedger/UI/Components/RoleAvatarView.swift
  - validationLedgerTests/Components/RoleAvatarViewTests.swift
  - validationLedgerTests/Components/RoleAvatarViewSnapshotTests.swift
autonomous: true
requirements: [R1, R12]

must_haves:
  truths:
    - "RoleAvatarView renders a per-role colored circle + 3-letter abbreviation + bottom-right status dot for all 5 roles × 4 verification states (20 cases)."
    - "RoleAvatarView's status-dot color is sourced VERBATIM from DS.Colors.Verification.color(for: state) — same source-of-truth function VerificationBadgeView consumes (R12)."
    - "RoleAvatarView's avatar circle fill is sourced VERBATIM from DS.Colors.Roles.color(for: role)."
    - "The 3-letter role abbreviation is a literal switch (SHP/BRK/CAR/DSP/FCT) — not derived from Role.rawValue or Role.displayName — and a unit test asserts each literal."
    - "RoleAvatarView is itself NOT an accessibility element (decorative); composed labels are owned by the strip-chip / tree-row parent per R6 leaf-element model."
    - "The avatar abbreviation font is a deliberate documented exception: literal point size, NOT Dynamic Type (UI-SPEC §Accessibility Contract)."
    - "After this plan completes, the R12 expected-consumer set is exactly two files: VerificationBadgeView.swift + RoleAvatarView.swift."
  artifacts:
    - path: "validationLedger/UI/Components/RoleAvatarView.swift"
      provides: "Programmatic UIView primitive — per-role colored circle + 3-letter abbreviation + verification-state status dot at bottom-right; 2 size variants (.strip 40pt, .tree 32pt)"
      contains: "DS.Colors.Verification.color(for:"
    - path: "validationLedgerTests/Components/RoleAvatarViewTests.swift"
      provides: "20 case assertions (5 roles × 4 states) + cornerRadius recompute + abbreviation-literal locks"
      contains: "DS.Colors.Verification.color(for:"
    - path: "validationLedgerTests/Components/RoleAvatarViewSnapshotTests.swift"
      provides: "40 attachment snapshots (5 roles × 4 states × 2 variants)"
      contains: "UIKitSnapshot.attach"
  key_links:
    - from: "validationLedger/UI/Components/RoleAvatarView.swift"
      to: "validationLedger/UI/DesignSystem/Colors.swift"
      via: "DS.Colors.Verification.color(for:) and DS.Colors.Roles.color(for:) calls"
      pattern: "DS\\.Colors\\.(Verification|Roles)\\.color\\(for:"
    - from: "validationLedgerTests/Components/RoleAvatarViewTests.swift"
      to: "validationLedger/UI/Components/RoleAvatarView.swift"
      via: "view instantiation + state/role-mapped assertion"
      pattern: "RoleAvatarView\\("
---

<objective>
Build the new `RoleAvatarView` primitive (R1) that all subsequent 9.1 plans depend on. Programmatic UIView with two size variants (strip 40pt / tree 32pt), per-role brand-color circle fill, centered 3-letter literal abbreviation, and a bottom-right status dot whose color reaches through `DS.Colors.Verification.color(for:)` — the same source-of-truth function `VerificationBadgeView` consumes (R12). After this plan, the R12 expected-consumer set is exactly two files: badge + avatar.

Purpose: Establish the shared verification primitive that the EveryoneOnLoadStripView (Plan 03) and ChainOfVouchesView (Plan 04) compose. The avatar primitive lives in `UI/Components/` alongside `VerificationBadgeView` — its closest analog and structural twin.

Output: One new source file (`RoleAvatarView.swift`); two new test files (unit + snapshot) under the `validationLedgerTests/Components/` directory that Plan 01 created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-CONTEXT.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-01-SUMMARY.md
@validationLedger/UI/Components/VerificationBadgeView.swift
@validationLedger/UI/DesignSystem/Colors.swift
@validationLedger/UI/DesignSystem/Spacing.swift
@validationLedger/UI/DesignSystem/Typography.swift
@validationLedger/Roles/Role.swift
@validationLedger/Core/Load/VerificationState.swift
@validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift
@validationLedgerTests/Support/UIKitSnapshot.swift

<interfaces>
<!-- Contracts the new RoleAvatarView consumes. -->

From validationLedger/UI/DesignSystem/Colors.swift (post-Plan-01):
- `DS.Colors.Verification.color(for state: VerificationState) -> UIColor` — the ONLY caller for the status-dot fill (R12 + D-03).
- `DS.Colors.Roles.color(for role: Role) -> UIColor` — the ONLY caller for the avatar circle fill.
- `DS.Colors.background` — used for the status-dot stroke ("punched-out" effect — UI-SPEC § Component Geometry).

From validationLedger/UI/Components/VerificationBadgeView.swift (the structural twin):
- Programmatic init pattern: `public override init(frame: CGRect) { super.init(frame:); setUp() }` + `required init?(coder:)` traps with `fatalError("... constructed programmatically only")`.
- `layoutSubviews()` override recomputes `cornerRadius = bounds.height / 2` (Pitfall 8) — RoleAvatarView mirrors with `cornerRadius = bounds.width / 2` (circle, not pill) AND publishes a `widthAnchor == heightAnchor` constraint so the avatar stays square.
- `accessibilityLabel` composition is applied externally by the parent in Phase 9.1 (R6 leaf-element model); the avatar itself is `isAccessibilityElement = false` per UI-SPEC § Component Geometry.

From validationLedger/UI/DesignSystem/Spacing.swift:
- `DS.Spacing.xs` = 4pt (status-dot stroke width).

From validationLedger/UI/DesignSystem/Typography.swift:
- `DS.Typography.footnote` — base for the 3-letter abbreviation; cast to `.semibold` via `UIFont.systemFont(ofSize: footnote.pointSize, weight: .semibold)` for the literal 11pt label per UI-SPEC § Accessibility Contract.

From validationLedger/Roles/Role.swift:
- 5 cases; `displayName` extension exists.

From validationLedger/Core/Load/VerificationState.swift:
- 4 cases (verified/pending/unverified/flagged).

From validationLedgerTests/Support/UIKitSnapshot.swift:
- `UIKitSnapshot.image(of: UIView, size: CGSize) -> UIImage` and `UIKitSnapshot.attach(_ image: UIImage, name: String, to: XCTestCase)` — attachment-only, no on-disk PNG baseline.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RoleAvatarView.swift programmatic primitive (2 size variants, status-dot subview)</name>
  <read_first>
    - validationLedger/UI/Components/VerificationBadgeView.swift (FULL FILE — programmatic init pattern at lines 79-128; cornerRadius Pitfall-8 mitigation at lines 92-97; isAccessibilityElement + .staticText pattern at line 84-88; the file header doc-comment cadence is the model for RoleAvatarView's contract block)
    - validationLedger/UI/DesignSystem/Colors.swift (post-Plan-01 — confirms the two helpers exist)
    - validationLedger/UI/DesignSystem/Spacing.swift, validationLedger/UI/DesignSystem/Typography.swift (tokens to reference)
    - validationLedger/Roles/Role.swift (5 cases, displayName extension)
    - validationLedger/Features/Loads/Detail/TrustNodeView.swift (secondary analog — lines 75-83 roleLabel: caption font, centered, Dynamic-Type-aware — though RoleAvatarView's abbreviation is DELIBERATELY a literal point size per UI-SPEC §Accessibility Contract)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (§ Group 2 → RoleAvatarView NEW lines 118-179 — exact construction template)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (§ Component Geometry → RoleAvatarView table — locks size, fill, abbreviation, status dot geometry)
  </read_first>
  <behavior>
    - `RoleAvatarView(role: .shipper, state: .verified, variant: .strip)` constructs a 40×40pt circle with `SHP` centered in white semibold; bottom-right 12×12pt status dot filled with `DS.Colors.Verification.color(for: .verified)` (.systemBlue) with a 2pt `DS.Colors.background` stroke.
    - `RoleAvatarView(role: .broker, state: .flagged, variant: .tree)` constructs a 32×32pt circle with `BRK` centered in white semibold; bottom-right 10×10pt status dot filled with `DS.Colors.Verification.color(for: .flagged)` (.systemRed).
    - `layoutSubviews()` recomputes `layer.cornerRadius = bounds.width / 2` on every pass (Pitfall 8 — circle, not pill).
    - `isAccessibilityElement = false` — the avatar is decorative per UI-SPEC § Component Geometry; parent owns the composed label per R6 leaf-element model.
    - `isUserInteractionEnabled = false` — chip wrapper owns the tap recognizer (Plan 03), not the avatar.
    - Per-role abbreviation literals are locked: `.shipper → "SHP"`, `.broker → "BRK"`, `.carrier → "CAR"`, `.dispatch → "DSP"`, `.factoring → "FCT"`. NOT derived from `Role.rawValue` or `Role.displayName`.
    - The abbreviation label font is a deliberate documented exception: `UIFont.systemFont(ofSize: 11, weight: .semibold)` — literal point size, NOT Dynamic Type (UI-SPEC § Accessibility Contract; reasoning: scaling the abbreviation either overflows the circle or shrinks the circle to a dot at large AX sizes; deferred per CONTEXT.md `<deferred>`).
    - `required init?(coder:)` traps with `fatalError("RoleAvatarView is constructed programmatically only")`.
  </behavior>
  <action>
    Create `validationLedger/UI/Components/RoleAvatarView.swift` as a `public final class RoleAvatarView: UIView`. File-header doc-comment block in the cadence of `VerificationBadgeView.swift`'s header (Contract / Color ramp / Accessibility / PII discipline sections). Public nested `enum Variant { case strip; case tree }` with computed `diameter: CGFloat` (40 vs 32), `dotDiameter: CGFloat` (12 vs 10), and `dotInset: CGPoint` (+2,+2 vs +1,+1) properties matching UI-SPEC § Component Geometry. Init: `public init(role: Role, state: VerificationState, variant: Variant = .strip)` calling `super.init(frame: .zero)` then `setUp()` then `configure(role: role, state: state)`; trap `required init?(coder:)` with `fatalError`. Subviews: (1) `private let abbreviationLabel: UILabel` centered, font `UIFont.systemFont(ofSize: 11, weight: .semibold)` (literal — NOT Dynamic Type; document the deliberate exception with a comment pointing at CONTEXT.md `<deferred>`), textColor `.white`, textAlignment `.center`; (2) `private let statusDot: UIView` with `layer.borderWidth = 2`, `layer.borderColor = DS.Colors.background.cgColor`, positioned bottom-right via constraints with the variant's `dotInset`. `setUp()` installs `widthAnchor.constraint(equalTo: heightAnchor)` (square), `widthAnchor.constraint(equalToConstant: variant.diameter)`, status-dot `widthAnchor/heightAnchor.constraint(equalToConstant: variant.dotDiameter)`, status-dot `trailingAnchor.constraint(equalTo: trailingAnchor, constant: variant.dotInset.x)` + `bottomAnchor.constraint(equalTo: bottomAnchor, constant: variant.dotInset.y)`, abbreviation `centerXAnchor/centerYAnchor.constraint(equalTo: ...)`. Set `isAccessibilityElement = false` (decorative — R6 leaf-element model owned by parent) and `isUserInteractionEnabled = false`. `layoutSubviews()` recomputes `layer.cornerRadius = bounds.width / 2` (Pitfall-8 mitigation for the circle shape) AND `statusDot.layer.cornerRadius = statusDot.bounds.width / 2`. `public func configure(role: Role, state: VerificationState)` sets `backgroundColor = DS.Colors.Roles.color(for: role)`, `statusDot.backgroundColor = DS.Colors.Verification.color(for: state)` (D-03), and `abbreviationLabel.text` via a private static `abbreviation(for role: Role) -> String` switch that returns the LOCKED literals `"SHP"/"BRK"/"CAR"/"DSP"/"FCT"` (R1 + UI-SPEC § Per-Role Avatar Palette). NO Logger / os_log / print calls anywhere in the file (T-09-04 view-layer logging lock; party data is PII-class). Add the new Swift file to the `validationLedger` target in `validationLedger.xcodeproj/project.pbxproj`.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild build -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20 &amp;&amp; echo '---R12 GATE---' &amp;&amp; expected=$(printf 'validationLedger/UI/Components/RoleAvatarView.swift\nvalidationLedger/UI/Components/VerificationBadgeView.swift' | sort); actual=$(git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift' | sort); diff &lt;(echo "$actual") &lt;(echo "$expected") &amp;&amp; echo 'R12 OK (2 expected consumers)' || { echo 'R12 DRIFT'; exit 1; } &amp;&amp; echo '---PII LOGGING GATE---' &amp;&amp; if git grep -nE '(os_log|Logger\.|print\()' validationLedger/UI/Components/RoleAvatarView.swift; then echo 'T-09.1-02 VIOLATION: log call in RoleAvatarView'; exit 1; else echo 'View-layer logging lock OK'; fi</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedger/UI/Components/RoleAvatarView.swift` exists.
    - `grep -cE 'public final class RoleAvatarView: UIView' validationLedger/UI/Components/RoleAvatarView.swift` returns 1.
    - `grep -cE 'public enum Variant' validationLedger/UI/Components/RoleAvatarView.swift` returns 1.
    - `grep -cE 'DS\.Colors\.Verification\.color\(for: state\)' validationLedger/UI/Components/RoleAvatarView.swift` returns at least 1 (the status-dot fill via the shared helper).
    - `grep -cE 'DS\.Colors\.Roles\.color\(for: role\)' validationLedger/UI/Components/RoleAvatarView.swift` returns at least 1 (the avatar circle fill).
    - `grep -cE 'isAccessibilityElement = false' validationLedger/UI/Components/RoleAvatarView.swift` returns at least 1 (decorative per UI-SPEC).
    - `grep -cE 'layer\.cornerRadius = bounds\.width / 2' validationLedger/UI/Components/RoleAvatarView.swift` returns at least 1 (Pitfall-8 circle mitigation).
    - `grep -cE 'fatalError\(.*constructed programmatically only.*\)' validationLedger/UI/Components/RoleAvatarView.swift` returns 1 (ARCH-04 init-coder trap).
    - R12 expected-consumer set now equals `[VerificationBadgeView.swift, RoleAvatarView.swift]` (verify block diff exits 0).
    - PII view-layer logging gate: no `os_log` / `Logger.` / `print(` in `RoleAvatarView.swift` (verify block grep returns empty).
    - Build succeeds.
  </acceptance_criteria>
  <done>RoleAvatarView ships as a programmatic primitive consuming the two new namespaces from Plan 01; R12 expected-consumer set is the locked 2-file pair; view-layer logging lock holds; project builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RoleAvatarViewTests.swift with 20 case assertions + abbreviation literal locks + cornerRadius recompute test</name>
  <read_first>
    - validationLedger/UI/Components/RoleAvatarView.swift (post-Task-1 — the view under test)
    - validationLedger/UI/DesignSystem/Colors.swift (the helpers being asserted as the source-of-truth)
    - validationLedger/Roles/Role.swift (5 cases — iterated in tests)
    - validationLedger/Core/Load/VerificationState.swift (4 cases — iterated in tests)
    - validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift (analog assertion shape at lines 48-63 for the per-state assertions; cornerRadius recompute test at lines 157-171)
    - validationLedgerTests/Components/VerificationBadgeViewTests.swift (Plan 01's new file — directory + style precedent for this new test file living in the same directory)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (§ Group 5 → RoleAvatarViewTests NEW lines 459-505 — locked test shape)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (§ Wave 0 Requirements → RoleAvatarViewTests line)
  </read_first>
  <behavior>
    - For every (role, state) pair (5 × 4 = 20), `RoleAvatarView(role: role, state: state, variant: .strip).statusDot.backgroundColor == DS.Colors.Verification.color(for: state)` and `view.backgroundColor == DS.Colors.Roles.color(for: role)`.
    - The 5 abbreviation literals (SHP/BRK/CAR/DSP/FCT) are tested as exact string matches against the rendered `abbreviationLabel.text` — proving the locked literal switch, not a `String(prefix(3))` derivation.
    - `RoleAvatarView` is NOT an accessibility element (`view.isAccessibilityElement == false`) — leaf-element model is owned by the parent (R6).
    - `RoleAvatarView` is non-interactive (`view.isUserInteractionEnabled == false`).
    - Pitfall-8 cornerRadius recompute test: after setting `view.bounds = CGRect(0, 0, 40, 40)` and calling `layoutIfNeeded()`, `view.layer.cornerRadius == 20`; after setting bounds to (0,0,32,32), cornerRadius == 16. Status dot cornerRadius == `dotBounds.width / 2` for both variants.
  </behavior>
  <action>
    Create `validationLedgerTests/Components/RoleAvatarViewTests.swift` as `final class RoleAvatarViewTests: XCTestCase`. Add `import XCTest`, `@testable import validationLedger`. Implement: (1) `test_allRoleStateCombinations_dotColorMatchesVerificationHelper` — nested loops over `Role.allCases` × `VerificationState.allCases` (20 assertions), each calling `let view = RoleAvatarView(role: role, state: state, variant: .strip); view.layoutIfNeeded(); XCTAssertEqual(view.statusDot.backgroundColor, DS.Colors.Verification.color(for: state), "R12: dot for (\(role), \(state)) must equal DS.Colors.Verification.color(for: \(state))")`. NOTE: if `statusDot` is `private`, expose it via `@testable` import OR add an `internal` accessor — choose the access level that matches `VerificationBadgeView`'s subview-access precedent. (2) `test_allRoleStateCombinations_avatarFillMatchesRolesHelper` — same nested-loop shape asserting `view.backgroundColor == DS.Colors.Roles.color(for: role)`. (3) `test_abbreviationLiterals_match_lockedSwitch` — 5 assertions: `let v = RoleAvatarView(role: .shipper, state: .verified, variant: .strip); v.layoutIfNeeded(); XCTAssertEqual(v.abbreviationLabel.text, "SHP")` and identical for `.broker → "BRK"`, `.carrier → "CAR"`, `.dispatch → "DSP"`, `.factoring → "FCT"`. (4) `test_isAccessibilityElement_false_decorative` — single assertion that `RoleAvatarView(...).isAccessibilityElement == false`. (5) `test_isUserInteractionEnabled_false` — single assertion. (6) `test_cornerRadius_recomputesForStripVariant` — set bounds to (0,0,40,40), `layoutIfNeeded()`, assert `layer.cornerRadius == 20` (Pitfall 8). (7) `test_cornerRadius_recomputesForTreeVariant` — same shape at (0,0,32,32), assert `layer.cornerRadius == 16`. Add file to `validationLedgerTests` target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/RoleAvatarViewTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedgerTests/Components/RoleAvatarViewTests.swift` exists.
    - `grep -cE 'func test_' validationLedgerTests/Components/RoleAvatarViewTests.swift` returns at least 7.
    - `grep -cE '"SHP"|"BRK"|"CAR"|"DSP"|"FCT"' validationLedgerTests/Components/RoleAvatarViewTests.swift` returns at least 5 (one per abbreviation literal lock).
    - Test invocation output contains "Test Suite 'RoleAvatarViewTests' passed".
    - All 5 roles × 4 states (20) status-dot color assertions pass; all 5 roles avatar-fill assertions pass; all 5 abbreviation literal assertions pass; isAccessibilityElement + isUserInteractionEnabled assertions pass; both cornerRadius recompute assertions pass.
  </acceptance_criteria>
  <done>RoleAvatarViewTests covers 20 (role, state) combinations + 5 abbreviation literal locks + a11y/interaction posture + Pitfall-8 cornerRadius recompute for both variants; suite is green on the iPhone 15 simulator lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create RoleAvatarViewSnapshotTests.swift with 40 attachments (5 roles × 4 states × 2 variants)</name>
  <read_first>
    - validationLedger/UI/Components/RoleAvatarView.swift (post-Task-1 — the view being snapshotted)
    - validationLedgerTests/Support/UIKitSnapshot.swift (the attachment-only snapshot helper — no on-disk PNG baselines; see VALIDATION.md A1)
    - validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift (analog snapshot helper pattern at lines 48-63 — `let image = UIKitSnapshot.image(of: view, size: ...); UIKitSnapshot.attach(image, name: "...", to: self)`)
    - validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift (matrix-helper precedent at lines 25-78 — per-fixture × per-state matrix render)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (§ Group 6 → RoleAvatarViewSnapshotTests NEW lines 640-665 — locked matrix sizes)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (§ Snapshot Test Matrix → Avatar row — 5 roles × 4 states × 2 variants = 40 baselines on a single 80×80pt canvas)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (§ Wave 0 Requirements → RoleAvatarViewSnapshotTests line)
  </read_first>
  <behavior>
    - The suite renders 40 distinct snapshot attachments under stable names like `RoleAvatar-shipper-verified-strip`, `RoleAvatar-broker-flagged-tree`, etc.
    - Each attachment is generated by `UIKitSnapshot.attach(UIKitSnapshot.image(of: view, size: Self.canvasSize), name: ..., to: self)` — attachment-only, no on-disk reference image (VALIDATION.md A1).
    - The canvas size is 80×80pt (per UI-SPEC § Snapshot Test Matrix — large enough to render the avatar + the bottom-right dot's protruding stroke ring without clipping).
    - No Dynamic Type variation matrix on this suite — the avatar abbreviation is a literal point size (deliberate exception), so Dynamic Type cannot change its rendering; verifying a single content size category per cell is sufficient.
  </behavior>
  <action>
    Create `validationLedgerTests/Components/RoleAvatarViewSnapshotTests.swift` as `final class RoleAvatarViewSnapshotTests: XCTestCase`. Add `import XCTest`, `@testable import validationLedger`. Declare `private static let canvasSize = CGSize(width: 80, height: 80)`. Implement a private helper `private func render(role: Role, state: VerificationState, variant: RoleAvatarView.Variant, name: String)` that: instantiates `RoleAvatarView(role: role, state: state, variant: variant)`, sets `view.bounds = CGRect(origin: .zero, size: Self.canvasSize)`, calls `view.layoutIfNeeded()`, then `UIKitSnapshot.attach(UIKitSnapshot.image(of: view, size: Self.canvasSize), name: name, to: self)`. Then generate 40 individual `test_<role>_<state>_<variant>_rendersExpected` test methods OR a parameterized loop helper invoked from a single `test_renderAllRoleStateVariantMatrix()` method — planner discretion (VerificationBadgeViewSnapshotTests precedent uses individual methods, which gives discrete CI test names). Cover all 5 × 4 × 2 = 40 cells. Snapshot names follow the template `"RoleAvatar-<roleRawValue>-<stateRawValue>-<variantTokenStrip|tree>"`. Add file to `validationLedgerTests` target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/RoleAvatarViewSnapshotTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedgerTests/Components/RoleAvatarViewSnapshotTests.swift` exists.
    - Test invocation output contains "Test Suite 'RoleAvatarViewSnapshotTests' passed".
    - The suite attaches at least 40 snapshots (5 roles × 4 states × 2 variants). Count check: `xcodebuild` output references at least 40 `UIKitSnapshot.attach` invocations OR the suite reports 40 test methods passing if the individual-method shape was chosen.
    - The matrix covers all 40 cells: no missing combination of `(role, state, variant)`.
  </acceptance_criteria>
  <done>40 RoleAvatar snapshot attachments populate the CI artifact bundle on the iPhone 15 lane; the human reviewer can inspect them via Xcode test report for visual correctness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| View layer → log surfaces | Untrusted assumption: a developer may add `Logger.debug("rendered \(role) \(state)")` while debugging RoleAvatarView geometry — leaks PII-class role/state combinations into logs reachable by analytics/crash vendors. |
| Design-token consumers → color helper | A future view may bypass `DS.Colors.Verification.color(for:)` and inline a `switch state` — re-opens the TRUST-02 lock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | Tampering (visual fraud signal) | `RoleAvatarView` (color-ramp drift between badge and avatar) | mitigate | R12 negative grep gate (Task 1 verify block) — after this plan, exactly 2 files call `DS.Colors.Verification.color(...)`: `VerificationBadgeView.swift` + `RoleAvatarView.swift`. CI runs the gate per VALIDATION.md. |
| T-09.1-02 | Information Disclosure (PII via log line) | `RoleAvatarView.swift` (party data is PII-class; the view ingests Role + VerificationState; a future `os_log("rendered \(role)")` leaks fraud-context to crash/analytics surfaces) | mitigate | View-layer logging lock — Task 1 verify block runs `git grep -nE '(os_log\|Logger\.\|print\()' validationLedger/UI/Components/RoleAvatarView.swift` and FAILS if anything matches. Wired into CI per VALIDATION.md. |
| T-09.1-04 | Tampering (client-side trust derivation) | `RoleAvatarView` (no `derive*` / `compute*` methods; consumes `verificationState` VERBATIM from caller) | accept | Component is a pure (role, state) → (chrome) mapping; no verdict computation. Accepted: T-09.1-04 does not surface here. |
</threat_model>

<verification>
- Build: `xcodebuild build ...` exits 0 (Task 1 verify block).
- R12 expected-consumer set after this plan: exactly `validationLedger/UI/Components/RoleAvatarView.swift` + `validationLedger/UI/Components/VerificationBadgeView.swift` (Task 1 verify block diff exits 0).
- View-layer logging lock: zero `os_log` / `Logger.` / `print(` in `RoleAvatarView.swift` (Task 1 verify block).
- Unit tests: `xcodebuild test ... -only-testing:validationLedgerTests/RoleAvatarViewTests` passes 20 (role, state) combinations + 5 abbreviation literal locks + isAccessibilityElement + isUserInteractionEnabled + 2 cornerRadius recompute tests (Task 2 verify block).
- Snapshot suite: `xcodebuild test ... -only-testing:validationLedgerTests/RoleAvatarViewSnapshotTests` passes; 40 attachments populate the CI artifact bundle (Task 3 verify block).
</verification>

<success_criteria>
- [ ] `validationLedger/UI/Components/RoleAvatarView.swift` exists and ships the `Variant` enum (.strip 40pt / .tree 32pt), the locked 3-letter abbreviation switch, the Pitfall-8 cornerRadius recompute, the `isAccessibilityElement = false` decorative posture, and the `isUserInteractionEnabled = false` non-interactive posture.
- [ ] RoleAvatarView reads its status-dot color through `DS.Colors.Verification.color(for: state)` (D-03) and its avatar fill through `DS.Colors.Roles.color(for: role)` (UI-SPEC).
- [ ] After this plan, the R12 expected-consumer set is exactly `[VerificationBadgeView.swift, RoleAvatarView.swift]`.
- [ ] `RoleAvatarViewTests.swift` covers 20 (role, state) combinations × 2 helper assertions (dot color + avatar fill), 5 abbreviation literal locks, and 2 cornerRadius recompute tests.
- [ ] `RoleAvatarViewSnapshotTests.swift` attaches 40 snapshots (5 × 4 × 2 matrix) to the CI artifact bundle on the iPhone 15 lane.
- [ ] View-layer logging lock holds (zero PII log call sites in RoleAvatarView.swift).
- [ ] The full Plan 02 test set passes on the scoped serial simulator lane.
</success_criteria>

<output>
Create `.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-02-SUMMARY.md` when done.
</output>
