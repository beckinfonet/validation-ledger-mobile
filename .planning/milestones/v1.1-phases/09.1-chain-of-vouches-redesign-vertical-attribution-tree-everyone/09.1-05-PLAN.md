---
phase: 09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone
plan: 05
type: execute
wave: 4
depends_on: [09.1-02, 09.1-03, 09.1-04]
files_modified:
  - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
  - validationLedgerTests/Loads/LoadDetailViewControllerSizeClassRoutingTests.swift
  - validationLedgerTests/Loads/LoadDetailViewControllerCompositionTests.swift
  - validationLedgerTests/Loads/LoadDetailViewControllerDebug2DOverrideTests.swift
  - validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift
autonomous: true
requirements: [R5, R6, R8, R10, R11]

must_haves:
  truths:
    - "iPhone compact horizontalSizeClass mounts EveryoneOnLoadStripView + ChainOfVouchesView; iPad regular horizontalSizeClass mounts TrustGraphView (unchanged from Phase 9) — R5 + R10."
    - "The iPhone Load Detail scroll order is [pinned summary] -> [strip] -> [chain-of-vouches card] -> [status timeline] -> [body] — verified by LoadDetailViewControllerCompositionTests/test_iPhone_scrollOrder (R8)."
    - "On iPhone, the standalone ChainIntegrityBannerView is REMOVED from the scroll order — the verdict pill renders ONLY inside the ChainOfVouchesView footer (R4). On iPad, the banner remains mounted at its existing Phase 9 position."
    - "The chip-tap -> tree-scroll wiring is in place: tapping a strip chip calls chainOfVouchesView.applySelectedRole(role) AND everyoneOnLoadStripView.applySelectedRole(role) AND computes the UNION of ALL matching tree row frames (D-10: 'role is suspect, not just one party' — multi-node roles MUST scroll so every implicated row is on-screen, not just the first) via convert(_:to:), passes the union rect (insetBy a top inset of DS.Spacing.md) to outerScrollView.scrollRectToVisible, and triggers a UISelectionFeedbackGenerator haptic (D-08, D-09, D-10)."
    - "Chip-tap on a multi-node role (VL-1009 BRK) scrolls so the union of ALL matching row frames is visible (both broker rows on-screen together) — Task 2 asserts the union rect's height exceeds a single row's height for VL-1009 BRK (W1 fix)."
    - "All 32 Phase 9 trust-graph tests pass on iPad simulator (R10) — the size-class routing change does NOT regress the iPad path."
    - "The -Mock2DTrustGraphOnIPhone DEBUG-only launch flag (D-12) mounts TrustGraphView on iPhone in DEBUG builds only; Release builds cannot reach the iPhone-2D code path (D-13)."
    - "The Phase 9 snapshot baselines for TrustGraphView / TrustNodeView / ChainIntegrityBannerView are constrained to iPad sizes only — iPhone-sized snapshot methods are removed or renamed (D-05); WR-04 sanity-check collapses into this re-record pass (D-06)."
    - "git diff shows ZERO changes to any file under validationLedger/Core/Load/ for trust types between branch-point and 9.1 completion (R11)."
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      provides: "Size-class routing that mounts new iPhone components (Plans 03+04) on compact; preserves TrustGraphView on regular; DEBUG-only -Mock2DTrustGraphOnIPhone fallback branch; chip-tap -> tree-scroll wiring with row-union scroll target (D-10)"
      contains: "Mock2DTrustGraphOnIPhone"
    - path: "validationLedgerTests/Loads/LoadDetailViewControllerSizeClassRoutingTests.swift"
      provides: "Size-class flip assertions: compact -> new components mounted, TrustGraphView absent; regular -> TrustGraphView mounted, new components absent; rotation iPhone<->iPad rebuilds without crash"
      contains: "horizontalSizeClass"
    - path: "validationLedgerTests/Loads/LoadDetailViewControllerCompositionTests.swift"
      provides: "Scroll-order assertion (R8); no-top-banner-on-iPhone assertion (R4); VL-1009 BRK chip-tap union-rect spans BOTH broker rows assertion (W1); a11y leaf-element posture cross-check"
      contains: "test_iPhone_scrollOrder"
    - path: "validationLedgerTests/Loads/LoadDetailViewControllerDebug2DOverrideTests.swift"
      provides: "DEBUG-only launch-flag literal lock + default-off assertion (D-14); #if DEBUG-gated"
      contains: "Mock2DTrustGraphOnIPhone"
  key_links:
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      to: "validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift"
      via: "everyoneOnLoadStripView property + onRoleTap closure wiring"
      pattern: "EveryoneOnLoadStripView"
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      to: "validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift"
      via: "chainOfVouchesView property + applySelectedRole(_:) call + rowViews(for:) consumer (union-rect scroll target)"
      pattern: "ChainOfVouchesView"
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      to: "validationLedger/Features/Loads/Detail/TrustGraphView.swift"
      via: "iPad-only mount (regular horizontalSizeClass) + DEBUG iPhone fallback"
      pattern: "TrustGraphView"
---

<objective>
Land the size-class routing changes in LoadDetailViewController (R5): the iPhone code path mounts the new EveryoneOnLoadStripView + ChainOfVouchesView (Plans 03 + 04); the iPad code path keeps TrustGraphView + the standalone ChainIntegrityBannerView untouched (R10); the standalone banner is REMOVED from the iPhone scroll order (R4). Wire the chip-tap -> tree-scroll behavior (D-08/D-09/D-10) using convert(_:to:) + scrollRectToVisible on the UNION of ALL matching row frames (W1 fix — multi-node roles must surface every implicated row, not just the first). Add the DEBUG-only -Mock2DTrustGraphOnIPhone launch flag (D-12, D-13, D-14) #if DEBUG-gated. Re-record the Phase 9 snapshot baselines to iPad-only sizes (D-05 + D-06 collapse). Land 3 new test suites validating the routing + composition + DEBUG-flag literal.

Purpose: Close the iPhone redesign loop — without this plan, the new components from Plans 03+04 are unmounted and the iPhone Load Detail still shows TrustGraphView. R11 grep gate (the phase-final assertion) runs in this plan's terminal verify step.

Output: Modified LoadDetailViewController.swift (size-class branch + #if DEBUG branch + chip-tap union-rect wiring); 3 new test files (size-class routing + composition + DEBUG override); 3 modified Phase 9 snapshot test files (iPad-only posture per D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-CONTEXT.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-02-SUMMARY.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-03-SUMMARY.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-04-SUMMARY.md
@validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
@validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift
@validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift
@validationLedger/Features/Loads/Detail/TrustGraphView.swift
@validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift
@validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift
@validationLedger/Features/Loads/Detail/StatusTimelineView.swift
@validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift
@validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift
@validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift
@validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift
@validationLedgerTests/Loads/TrustGraphViewLayoutTests.swift
@validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift
@validationLedgerTests/Loads/TrustGraphViewGestureTests.swift
@validationLedgerTests/Loads/TrustNodeViewGestureTests.swift

<interfaces>
<!-- Contracts wired together by the VC routing. -->

From validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift (Plan 03):
- public init()
- public var onRoleTap: ((Role) -> Void)?
- public func configure(chainOfTrust chain: ChainOfTrust)
- public func applySelectedRole(_ role: Role?)

From validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift (Plan 04):
- public init()
- public func configure(chainOfTrust chain: ChainOfTrust)
- public func applySelectedRole(_ role: Role?)
- public func rowViews(for role: Role) -> [ChainOfVouchesRowView] (for scroll-anchor frame computation — Plan 05 takes the UNION of these frames per D-10)

From validationLedger/Features/Loads/Detail/TrustGraphView.swift (FROZEN — R10):
- public func configure(chainOfTrust:) + the entire Phase 9 mount surface — no API changes.

From validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift (FROZEN component; mount narrows on iPhone per R4):
- public func apply(integrity: ChainIntegrity) — banner is mounted on iPad only after this plan.

From validationLedger/Features/Loads/Detail/LoadDetailViewController.swift (existing — read for in-place edit scope):
- private var trustGraphView: TrustGraphView (existing — preserved; iPad-only mount after this plan)
- private var chainIntegrityBanner: ChainIntegrityBannerView? (existing — preserved; iPad-only mount on iPhone path after this plan)
- private var compositionLayout: CompositionLayout (existing enum, .iPhoneCompact / .iPadRegularSplit)
- buildIPhoneLayout() (lines 489-566, existing) — body becomes a dispatcher between #if DEBUG legacy branch and new vertical-tree branch
- buildIPadSplitLayout() (existing) — UNCHANGED for 9.1 (R10)
- traitCollectionDidChange(_:) override at lines 349-388 — REPLACED with iOS 17 registerForTraitChanges per RESEARCH Example 3
- CR-02 reparenting fix at lines 504-507 + 585-588 — preserved; new iPhone-only components do NOT need reparenting (they only mount on iPhone — Pitfall 4)
- cachedLoad / cachedChainOfTrust (existing) — used by applyLoadedRender after a size-class flip rebuild

From validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift (DEBUG launch-flag precedent, lines 60-76):
- #if DEBUG; public static let verifiedKYCStatusLaunchFlag = "-MockKYCStatusVerified"; static var verifiedKYCStatusOverrideActive: Bool { ProcessInfo.processInfo.arguments.contains(verifiedKYCStatusLaunchFlag) }; #endif — direct mirror for D-12's -Mock2DTrustGraphOnIPhone flag.

iPhone scroll order LOCKED (UI-SPEC §iPhone Scroll Order):
1. System nav bar.
2. Pinned summary header (Phase 9 — REF-XXXX, origin->destination, LoadStatusBadgeView). UNCHANGED.
3. EveryoneOnLoadStripView (NEW) — gap to next DS.Spacing.md.
4. ChainOfVouchesView card (NEW) — gap to next DS.Spacing.xl (32pt).
5. StatusTimelineView (Phase 9). UNCHANGED. Gap to next DS.Spacing.lg.
6. LoadDetailBodyView freight rows (cargo / rate / BOL / ETA). UNCHANGED.

The standalone ChainIntegrityBannerView is REMOVED from this iPhone scroll order (R4); footer pill inside ChainOfVouchesView is the iPhone fraud signal.

Phase 9 snapshot test files (D-05 re-record posture):
- TrustGraphViewSnapshotTests.swift: iPhonePortraitSize at line 25 + iPadSplitSize at line 26; per-method matrix at lines 82+ covers iPhone + iPad. iPhone-sized methods are REMOVED (D-05); iPad methods remain. Per RESEARCH §4 + Assumption A1, snapshots are attachment-only — no .png baselines on disk to delete; the work is purely code edits.
- TrustNodeViewSnapshotTests.swift: chromeSize at line 31 — narrow to iPad-only-intent constant rename.
- ChainIntegrityBannerViewSnapshotTests.swift: bannerSize at line 37 (393pt — iPhone 15 width); widen to iPad mini portrait width (744pt) per UI-SPEC §Snapshot Test Matrix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modify LoadDetailViewController.swift — size-class routing, chip-tap UNION-RECT wiring (D-10), #if DEBUG flag, standalone banner removal on iPhone</name>
  <read_first>
    - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift (FULL FILE — in-place modification target; buildIPhoneLayout lines 489-566, traitCollectionDidChange lines 349-388, CR-02 reparenting fix lines 504-507 + 585-588, cached payload pattern)
    - validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift (Plan 03 — closure surface + applySelectedRole API)
    - validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift (Plan 04 — configure + applySelectedRole + rowViews(for:) API)
    - validationLedger/Features/Loads/Detail/TrustGraphView.swift (FROZEN — read only for the existing mount surface)
    - validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift (FROZEN component; iPhone mount removal scope)
    - validationLedger/Features/Loads/Detail/StatusTimelineView.swift, LoadDetailBodyView.swift (read for the existing iPhone subview positions in the scroll order)
    - validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift (FULL FILE — DEBUG launch-flag precedent at lines 60-76; D-12 mirrors exactly)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (Pattern 3 strip->tree scroll anchoring lines 388-426; Example 3 iOS 17 registerForTraitChanges lines 740-778; Example 4 DEBUG flag lines 780-845; Pitfall 2 convert(_:to:) window guard lines 535-543; Pitfall 4 CR-02 reparenting note lines 555-563)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (Group 4 LoadDetailViewController MOD lines 360-453)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (iPhone Scroll Order)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-CONTEXT.md (D-08..D-14; D-10 multi-node "all matching rows" highlight semantics — line 212 "BOTH broker rows in the tree light up")
  </read_first>
  <behavior>
    - On iPhone compact horizontalSizeClass, the VC mounts: pinned summary -> EveryoneOnLoadStripView -> ChainOfVouchesView -> StatusTimelineView -> LoadDetailBodyView. The standalone ChainIntegrityBannerView is NOT mounted on iPhone (R4).
    - On iPad regular horizontalSizeClass, the VC mounts the existing Phase 9 composition unchanged: ChainIntegrityBannerView at top + TrustGraphView + (per existing buildIPadSplitLayout) — no behavior change (R10).
    - Size-class flip (iPhone <-> iPad via rotation or split-view) deactivates existing composition constraints and rebuilds the layout for the new posture via iOS 17 registerForTraitChanges (RESEARCH Example 3) — replaces the existing traitCollectionDidChange override. CR-02 reparenting fix is preserved inside buildIPadSplitLayout for the bodyView.
    - On chip tap (EveryoneOnLoadStripView.onRoleTap), the VC: (1) calls chainOfVouchesView.applySelectedRole(role); (2) calls everyoneOnLoadStripView.applySelectedRole(role); (3) **W1 fix — locates ALL matching tree rows** via `let rows = chainOfVouchesView.rowViews(for: role)` (NOT just `.first`); (4) for each matching row, converts the row's bounds to the outer scroll view's coordinate space via convert(_:to:) — GUARDED with `firstRow.window != nil && outerScrollView.window != nil` per Pitfall 2 (defer with DispatchQueue.main.async if not yet in window); (5) folds the converted rects into a UNION via `rows.reduce(CGRect.null) { acc, row in acc.union(row.convert(row.bounds, to: outerScrollView)) }` — for single-node roles the union equals a single-row frame; for multi-node roles (VL-1009 BRK) the union spans BOTH broker rows so both are on-screen after the scroll (D-10 "role is suspect, not just one party"); (6) applies a top inset of `DS.Spacing.md` via `unionRect.insetBy(dx: 0, dy: -DS.Spacing.md)`; (7) calls outerScrollView.scrollRectToVisible(insetUnionRect, animated: true); (8) fires UISelectionFeedbackGenerator().selectionChanged() haptic.
    - applyLoadedRender(load:chainOfTrust:) configures BOTH new components on iPhone path (everyoneOnLoadStripView.configure + chainOfVouchesView.configure); on iPad path, only the Phase 9 trustGraphView + banner are configured (unchanged).
    - When -Mock2DTrustGraphOnIPhone launch flag is present AND build is DEBUG, the iPhone code path mounts TrustGraphView + ChainIntegrityBannerView (the pre-9.1 composition preserved verbatim in a new buildIPhoneLayoutLegacyTrustGraph() private method). When the flag is absent or build is Release, the iPhone path mounts the new components (default). D-13: the entire flag-check AND the legacy branch are wrapped in `#if DEBUG` blocks.
    - A new file-top public enum Debug2DGraphOverride { public static let launchFlag = "-Mock2DTrustGraphOnIPhone"; public static var isActive: Bool { ProcessInfo.processInfo.arguments.contains(launchFlag) } } lives inside a `#if DEBUG` block — extracted so unit tests can lock the literal and the default-off contract (D-14).
    - Zero new os_log / Logger. / print( calls added to the VC by this plan (T-09-04 view-layer logging lock — preserves the existing Phase 9 lock per LoadDetailViewController.swift:66-70).
  </behavior>
  <action>
    Edit validationLedger/Features/Loads/Detail/LoadDetailViewController.swift in place. (A) Add new instance properties next to the existing trustGraphView property: `private let everyoneOnLoadStripView = EveryoneOnLoadStripView()` and `private let chainOfVouchesView = ChainOfVouchesView()` — these get installed in bodyContainer by the existing installBodyViewIntoContainer() helper (or a new sibling helper) so their identity is stable across composition rebuilds. (B) At the TOP of the file (inside the existing imports + file header), add inside a `#if DEBUG ... #endif` block: `public enum Debug2DGraphOverride { public static let launchFlag = "-Mock2DTrustGraphOnIPhone"; public static var isActive: Bool { ProcessInfo.processInfo.arguments.contains(launchFlag) } }`. Add a comment block above documenting the marketing-screenshots use case + the -MockKYCStatusVerified precedent reference (mirror MockDefaultFixtures.swift:60-76 cadence). (C) REPLACE the existing traitCollectionDidChange(_:) override at lines 349-388 with: keep the override body as a private method `handleSizeClassChange()` (preserves the existing rebuild logic — constraint deactivation, compositionLayout flip, buildLayoutForCurrentComposition() call, cached payload re-apply); in `viewDidLoad()` after the existing initial composition setup, add `registerForTraitChanges([UITraitHorizontalSizeClass.self]) { [weak self] (vc: LoadDetailViewController, previous: UITraitCollection) in self?.handleSizeClassChange() }`; remove the standalone traitCollectionDidChange override (RESEARCH Example 3). (D) REFACTOR buildIPhoneLayout() into a dispatcher: at the top, add `#if DEBUG; if Debug2DGraphOverride.isActive { buildIPhoneLayoutLegacyTrustGraph(); return }; #endif` followed by a call to `buildIPhoneLayoutVerticalTree()`. (E) Extract the EXISTING pre-9.1 buildIPhoneLayout body (lines 489-566 — pinned summary + trustGraphView + body + standalone banner) into a NEW private `#if DEBUG ... #endif`-gated method `private func buildIPhoneLayoutLegacyTrustGraph()` preserving the existing composition verbatim. (F) Create NEW `private func buildIPhoneLayoutVerticalTree()` implementing the LOCKED iPhone scroll order: ensure bodyView.hidesPinnedSummaryHeader = true; ensure pinned summary header exists (call ensurePinnedHeaderTopLevelView()); DO NOT mount chainIntegrityBanner on iPhone (R4 — banner is iPad-only after 9.1); mount everyoneOnLoadStripView immediately below the pinned header; mount chainOfVouchesView below the strip; mount StatusTimelineView (existing) below the card; mount bodyView (LoadDetailBodyView) below the timeline. Inter-element vertical gaps per UI-SPEC §iPhone Scroll Order: strip->card DS.Spacing.md, card->timeline DS.Spacing.xl, timeline->body DS.Spacing.lg. Pinned-header->strip gap DS.Spacing.md. CR-02 reparenting note (Pitfall 4): the new components only mount on iPhone — they never live in rightPaneContainer, so no reparenting dance is needed. (G) In viewDidLoad after composition setup, wire the chip-tap closure: `everyoneOnLoadStripView.onRoleTap = { [weak self] role in self?.handleStripChipTap(role: role) }`. Implement private `func handleStripChipTap(role: Role)`: guard `everyoneOnLoadStripView.window != nil && chainOfVouchesView.window != nil` else `DispatchQueue.main.async { [weak self] in self?.handleStripChipTap(role: role) }; return` (Pitfall 2); call `chainOfVouchesView.applySelectedRole(role)` + `everyoneOnLoadStripView.applySelectedRole(role)`. **W1 fix — UNION of all matching row frames per D-10:** locate `let rows = chainOfVouchesView.rowViews(for: role)`; if `rows.isEmpty { return }`; assert `rows.allSatisfy { $0.window != nil }` (Pitfall 2); compute `let unionRect = rows.reduce(CGRect.null) { acc, row in acc.union(row.convert(row.bounds, to: outerScrollView)) }`; if `unionRect.isNull { return }`; apply a top inset by `let target = unionRect.insetBy(dx: 0, dy: -DS.Spacing.md)`; call `outerScrollView.scrollRectToVisible(target, animated: true)` (use whatever the VC's outer scroll-view property is named — read the file to confirm; likely a property like `scrollView` or similar); fire `UISelectionFeedbackGenerator().selectionChanged()`. (H) In applyLoadedRender(load:chainOfTrust:) (or wherever the VC currently configures trustGraphView), ADD calls to everyoneOnLoadStripView.configure(chainOfTrust: chain) AND chainOfVouchesView.configure(chainOfTrust: chain) when compositionLayout == .iPhoneCompact AND Debug2DGraphOverride.isActive is false (i.e., default iPhone path). On iPad path (compositionLayout == .iPadRegularSplit) AND on the DEBUG iPhone-legacy path, keep the existing trustGraphView.configure + chainIntegrityBanner.apply call sites untouched. (I) Do NOT modify buildIPadSplitLayout (R10). Do NOT add Logger / os_log / print. Do NOT modify any file under validationLedger/Core/Load/ (R11).
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild build -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20 &amp;&amp; grep -cE 'Debug2DGraphOverride' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift &amp;&amp; grep -cE 'EveryoneOnLoadStripView|ChainOfVouchesView' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift &amp;&amp; grep -cE 'registerForTraitChanges' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift &amp;&amp; grep -cE 'reduce\([ ]*CGRect\.null|reduce\([ ]*\.null' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift &amp;&amp; NEW_LOGGING=$(git diff HEAD -- validationLedger/Features/Loads/Detail/LoadDetailViewController.swift | grep -E '^\+' | grep -vE '^\+\+\+' | grep -E '(os_log|Logger\.|print\()'); if [ -n "$NEW_LOGGING" ]; then echo 'T-09.1-02 VIOLATION: new log call added to VC'; echo "$NEW_LOGGING"; exit 1; else echo 'View-layer logging lock OK (no new log calls in VC diff)'; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'Debug2DGraphOverride' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 2 (definition + isActive call site).
    - `grep -cE '#if DEBUG' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 2 (the enum + the buildIPhoneLayout DEBUG branch).
    - `grep -cE 'EveryoneOnLoadStripView' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 2 (property + configure call) AND `grep -cE 'ChainOfVouchesView' ...` returns at least 2.
    - `grep -cE 'registerForTraitChanges' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 1 (iOS 17 trait registration replaces the override).
    - `grep -cE 'buildIPhoneLayoutVerticalTree|buildIPhoneLayoutLegacyTrustGraph' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 2 (both dispatcher methods exist).
    - `grep -cE 'onRoleTap' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 1 (chip-tap closure wired).
    - `grep -cE 'scrollRectToVisible' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 1 (chip-tap -> outer-scroll behavior).
    - `grep -cE 'rowViews\(for:' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 1 (W1 — chip-tap handler iterates ALL matching rows, not just `.first`).
    - `grep -cE 'reduce\([ ]*CGRect\.null|reduce\([ ]*\.null' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 1 (W1 — union-rect fold over the matching rows).
    - `grep -cE 'UISelectionFeedbackGenerator' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns at least 1 (chip-tap haptic).
    - View-layer logging gate (verify block) exits with 'View-layer logging lock OK (no new log calls in VC diff)' — no new os_log / Logger. / print( added by this plan.
    - Build succeeds.
  </acceptance_criteria>
  <done>LoadDetailViewController routes by size class (R5) with the new iPhone components mounted in the locked scroll order (R8), preserves the iPad TrustGraphView path (R10), removes the standalone banner from the iPhone scroll order (R4), wires the chip-tap -> tree-scroll behavior with the Pitfall-2 window guard AND the W1 fix (UNION of ALL matching row frames per D-10), and gates the DEBUG -Mock2DTrustGraphOnIPhone fallback under #if DEBUG (D-12/D-13).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 3 LoadDetailViewController test suites (SizeClassRouting + Composition + Debug2DOverride) — Composition asserts D-10 union-rect spans BOTH broker rows for VL-1009</name>
  <read_first>
    - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift (post-Task-1 — VC under test)
    - validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift, ChainOfVouchesView.swift, TrustGraphView.swift (the views the VC swaps between)
    - validationLedgerTests/Loads/TrustGraphViewLayoutTests.swift (analog — canvas-bounds setup + view-hierarchy walk lines 58-81)
    - validationLedgerTests/Loads/LoadDetailViewModelTests.swift (existing — read for the VC + VM construction shape; need a mock LoadDetailViewModel for VC instantiation in tests)
    - validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift (lines 60-76 — analog launch-flag unit-test pattern; the verifiedKYCStatusOverrideActive default-off contract test is the model)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (Group 5 LoadDetailViewController* tests lines 584-635)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (Example 4 lines 820-845 DEBUG-flag unit-test shape)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (Wave 0 Requirements LoadDetailViewController* lines)
  </read_first>
  <behavior>
    - SizeClassRoutingTests: With traitOverrides.horizontalSizeClass = .compact (iPhone), the VC's view hierarchy CONTAINS EveryoneOnLoadStripView AND ChainOfVouchesView; trustGraphView.superview == nil OR trustGraphView is NOT in the visible composition tree. With traitOverrides.horizontalSizeClass = .regular (iPad), the inverse: trustGraphView is in the visible composition; EveryoneOnLoadStripView + ChainOfVouchesView are NOT (or have superview == nil). Toggling the trait override rebuilds the composition without crash.
    - CompositionTests: On iPhone path, the iPhone scroll order is correct (R8): index of everyoneOnLoadStripView < index of chainOfVouchesView < index of statusTimelineView < index of bodyView in the relevant container's arrangedSubviews/subviews. On iPhone path, NO subview in the VC view hierarchy has type ChainIntegrityBannerView mounted at top-of-screen (R4 — verify by walking the VC's subview tree and asserting count of `view as? ChainIntegrityBannerView` subviews mounted above the pinned summary header == 0). **W1 assertion — VL-1009 BRK chip-tap union-rect spans BOTH broker rows:** with VL-1009 loaded, invoke the VC's chip-tap handler with `Role.broker`, capture the unionRect passed to `outerScrollView.scrollRectToVisible(_:animated:)` (either via a test-seam override of the scrollView OR by instrumenting `handleStripChipTap(role:)` to expose its computed unionRect for tests via an `internal var lastUnionRectForTesting: CGRect?` written in test builds), then assert `unionRect.height > singleRowHeight` where singleRowHeight is computed from one broker row's `bounds.height`. The assertion proves the union spans more than one row (i.e., the chip-tap surfaces both VL-1009 brokers, not just the first).
    - Debug2DOverrideTests (`#if DEBUG`-gated): the launch-flag literal equals exactly "-Mock2DTrustGraphOnIPhone" (D-12); the flag is OFF by default in the unit-test process (D-14).
  </behavior>
  <action>
    Create THREE new test files under validationLedgerTests/Loads/. (A) validationLedgerTests/Loads/LoadDetailViewControllerSizeClassRoutingTests.swift as `final class LoadDetailViewControllerSizeClassRoutingTests: XCTestCase`. Implement `private func makeVC(sizeClass: UIUserInterfaceSizeClass) -> LoadDetailViewController` that constructs the VC with a mock LoadDetailViewModel (use the existing test helpers in LoadDetailViewModelTests.swift if available; otherwise instantiate via the AppContainer factory or a stub VM), sets `vc.view.traitOverrides.horizontalSizeClass = sizeClass`, embeds in a UIWindow (so window-guard checks work), calls `vc.loadViewIfNeeded()`, configures with `ChainOfTrustFactory.vl1001_fiveRolesClean()` via the VM mock, layoutIfNeeded(). Tests: (1) test_iPhoneCompact_mounts_newComponents_and_omits_trustGraphView — assert vc.view contains an EveryoneOnLoadStripView descendant AND a ChainOfVouchesView descendant AND does NOT contain a visible TrustGraphView descendant (mounted in superview). (2) test_iPadRegular_mounts_trustGraphView_and_omits_newComponents — assert vc.view contains a TrustGraphView descendant AND does NOT contain visible EveryoneOnLoadStripView / ChainOfVouchesView descendants. (3) test_sizeClassFlip_iPhoneToIPad_rebuildsWithoutCrash — start at .compact, flip to .regular, layoutIfNeeded(), assert no crash AND TrustGraphView is now in the hierarchy. (4) test_sizeClassFlip_iPadToIPhone_rebuildsWithoutCrash — inverse. (B) validationLedgerTests/Loads/LoadDetailViewControllerCompositionTests.swift as `final class LoadDetailViewControllerCompositionTests: XCTestCase`. Tests: (a) test_iPhone_scrollOrder_strip_card_timeline_body — with .compact size class, walk the relevant container's arrangedSubviews / subviews, find the indices of the EveryoneOnLoadStripView, ChainOfVouchesView, StatusTimelineView, LoadDetailBodyView descendants and assert strict ascending order matching R8. (b) test_iPhone_noTopBanner_for_5_fixtures — for each of VL-1001/1003/1005/1007/1009 fixtures, set up the VC with .compact size class, configure, layoutIfNeeded, walk the entire vc.view subview tree, assert there are ZERO ChainIntegrityBannerView instances mounted ABOVE the pinned summary header (R4). One way: filter `vc.view.subviewsRecursive().compactMap { $0 as? ChainIntegrityBannerView }` and assert the count is 0 on iPhone path; if helper doesn't exist, write a small recursive subview walker inline in the test file. (c) test_iPad_topBanner_remains_for_compromised_fixture — with .regular size class + a compromised-verdict fixture (VL-1010), assert ChainIntegrityBannerView IS in the hierarchy at the iPad top-of-screen position (R10 preservation regression). (d) **W1 — test_VL1009_brokerChipTap_unionRect_spansBothBrokerRows** — with .compact size class, configure with VL-1009 (two brokers), layoutIfNeeded, invoke `vc.handleStripChipTap(role: .broker)` (or equivalent test-seam exposed for this purpose), capture `vc.lastUnionRectForTesting` (an `#if DEBUG` instance var the Task 1 implementation writes after every union computation), locate one of the broker rows via `chainOfVouchesView.rowViews(for: .broker).first?.bounds.height` to get singleRowHeight; assert `unionRect.height > singleRowHeight * 1.5` (some slack for stack-spacing; proves the union spans more than one row). Also assert `chainOfVouchesView.rowViews(for: .broker).count == 2` precondition. (C) validationLedgerTests/Loads/LoadDetailViewControllerDebug2DOverrideTests.swift as `final class LoadDetailViewControllerDebug2DOverrideTests: XCTestCase` wrapped in `#if DEBUG ... #endif`. Tests: (i) test_debug2DGraphOverride_launchFlagLiteral — `XCTAssertEqual(Debug2DGraphOverride.launchFlag, "-Mock2DTrustGraphOnIPhone", "DEBUG 2D-graph override flag literal is locked per D-12.")`. (ii) test_debug2DGraphOverride_offByDefault_inUnitTestProcess — `XCTAssertFalse(Debug2DGraphOverride.isActive, "Flag must default to OFF in the unit test process — D-14 default-off contract.")`. Mirrors MockDefaultFixtures.verifiedKYCStatusOverrideActive unit-test shape. Note: the W1 union-rect test in suite (B) requires Task 1 to expose `internal var lastUnionRectForTesting: CGRect?` (or equivalent) under `#if DEBUG` for test introspection — the planner has confirmed this seam is acceptable per the test-seam precedent in MockDefaultFixtures.swift. Add all 3 files to validationLedgerTests target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/LoadDetailViewControllerSizeClassRoutingTests -only-testing:validationLedgerTests/LoadDetailViewControllerCompositionTests -only-testing:validationLedgerTests/LoadDetailViewControllerDebug2DOverrideTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files exist under validationLedgerTests/Loads/.
    - `grep -cE 'func test_' validationLedgerTests/Loads/LoadDetailViewControllerSizeClassRoutingTests.swift` returns at least 4.
    - `grep -cE 'func test_' validationLedgerTests/Loads/LoadDetailViewControllerCompositionTests.swift` returns at least 4 (now includes W1's VL-1009 union-rect test).
    - `grep -cE 'func test_' validationLedgerTests/Loads/LoadDetailViewControllerDebug2DOverrideTests.swift` returns at least 2.
    - `grep -cE '#if DEBUG' validationLedgerTests/Loads/LoadDetailViewControllerDebug2DOverrideTests.swift` returns at least 1.
    - `grep -cE 'VL1009.*broker.*[Uu]nion|union.*broker|unionRect' validationLedgerTests/Loads/LoadDetailViewControllerCompositionTests.swift` returns at least 1 (W1 union-rect assertion present).
    - Test invocation output contains "Test Suite 'LoadDetailViewControllerSizeClassRoutingTests' passed" AND "Test Suite 'LoadDetailViewControllerCompositionTests' passed" AND "Test Suite 'LoadDetailViewControllerDebug2DOverrideTests' passed".
  </acceptance_criteria>
  <done>The three new VC suites assert R5 (size-class routing), R8 (iPhone scroll order), R4 (no top banner on iPhone), R10 (iPad TrustGraphView preserved), W1 fix (VL-1009 BRK chip-tap union-rect spans BOTH broker rows per D-10), D-12 (flag literal locked), and D-14 (default-off in unit-test process).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-record Phase 9 snapshot test files to iPad-only sizes (D-05 + D-06 collapse) + run full phase verification (R10 + R11 + R12 final gates) — with Phase-9-suite pre-check (W3) and R12 strict gate UIColor inclusion (W5)</name>
  <read_first>
    - validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift (FULL FILE — in-place edit target; iPhonePortraitSize/iPadSplitSize constants at lines 25-26; per-method matrix from line 82)
    - validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift (FULL FILE — in-place edit target; chromeSize at line 31)
    - validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift (FULL FILE — in-place edit target; bannerSize at line 37 — currently 393pt iPhone width, widens to 744pt iPad mini portrait width)
    - validationLedgerTests/Loads/TrustGraphViewLayoutTests.swift, TrustGraphViewAccessibilityTests.swift, TrustGraphViewGestureTests.swift, TrustNodeViewGestureTests.swift (the 32 Phase 9 unit/gesture/accessibility tests — UNCHANGED per D-07; just confirm they still pass on iPad simulator after the routing change)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (§4 + Assumption A1 lines 962-963 — snapshots are attachment-only, no PNG baselines to delete; §D-05 Snapshot Re-Record Strategy lines 1136-1156)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (Group 6 MOD lines 718-749)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (Snapshot Test Matrix Phase 9 trust-graph re-record row — iPad mini portrait 744pt + iPad mini landscape 1133pt)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (D-05 Snapshot Re-Record Strategy + R11 + R12 grep gates)
  </read_first>
  <behavior>
    - TrustGraphViewSnapshotTests: iPhone-sized test methods REMOVED (D-05 — iPhone baselines deleted). iPad-sized methods (if any) RETAINED; if the suite only had iPad split sizes already, ADD iPad mini portrait + iPad mini landscape variants where verdict × Dynamic Type coverage benefits.
    - TrustNodeViewSnapshotTests: the existing chromeSize constant is RENAMED to convey iPad-only intent (e.g. `iPadChromeSize`) per RESEARCH §D-05; the underlying canvas is device-agnostic in concept but the test posture narrows to iPad.
    - ChainIntegrityBannerViewSnapshotTests: bannerSize widened from 393pt (iPhone 15) to 744pt (iPad mini portrait) per UI-SPEC Snapshot Test Matrix; height (36pt) unchanged.
    - All Phase 9 trust-graph snapshot suites continue to PASS on the iPad mini simulator (R10 + D-07 — view-layer tests untouched still green).
    - All 32 Phase 9 trust-graph unit/gesture/accessibility tests (TrustGraphView{Layout,Accessibility,Gesture}Tests + TrustNodeViewGestureTests) continue to PASS on iPad mini simulator (R10).
    - **W3 — Phase-9-suite pre-check:** before invoking `-only-testing:` for the 7 Phase 9 suites, the verify block runs `xcodebuild -list-tests` and asserts that ALL 7 expected suite names exist (defensive against a renamed/deleted Phase 9 suite silently making the -only-testing flag a no-op).
    - WR-04 baseline sanity-check collapses into this re-record pass (D-06) — the human reviewer inspects the new iPad attachments via Xcode test report (manual; no automated gate beyond suite passing).
    - Phase-final R11 gate: `git diff --name-only $(git merge-base main HEAD)..HEAD | grep -E '^validationLedger/Core/Load/(ChainOfTrust|TrustNode|TrustEdge|ChainIntegrity|PriorRelationship|VerificationState)\.swift$'` returns EMPTY (R11 LOCKED).
    - Phase-final R12 primary gate: `git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift'` returns EXACTLY the 2-element expected set: VerificationBadgeView.swift + RoleAvatarView.swift.
    - **W5 — Phase-final R12 strict gate INCLUDES BOTH `UIColor` AND `\.tertiarySystemFill`** in the alternation: no view file other than Colors.swift maps VerificationState cases to UIColor / .systemX / .tertiarySystemFill / DS.Colors.{primary|destructive|caution} literals within 3 lines of a `case .verified|.pending|.unverified|.flagged:` (matches VALIDATION.md §R12 Grep Gates strict variant).
    - Phase-final view-layer logging lock: zero os_log / Logger. / print( in EveryoneOnLoadStripView.swift, ChainOfVouchesView.swift, RoleAvatarView.swift.
  </behavior>
  <action>
    Step A: Edit validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift. (1) Inspect existing test methods (likely shaped `test_<verdict>_iPhonePortrait_dynamicType<size>_rendersExpectedFrame` per PATTERNS.md line 729-734). REMOVE every test method whose name contains "iPhonePortrait" OR whose body renders at Self.iPhonePortraitSize (D-05 deletes iPhone-sized tests). RETAIN every iPad-sized method. (2) Remove the `private static let iPhonePortraitSize = CGSize(width: 393, height: 600)` constant declaration (no longer referenced). (3) Add `private static let iPadMiniPortraitSize = CGSize(width: 744, height: 1024)` and `private static let iPadMiniLandscapeSize = CGSize(width: 1133, height: 744)` constants per UI-SPEC. (4) Add new test methods covering the 3 verdicts (.clean / .caution / .compromised) × 2 iPad sizes × 1 Dynamic Type baseline (.large) → ≥6 new iPad-sized test methods (replacing the removed iPhone matrix coverage). Snapshot names follow the template "TrustGraph-<verdict>-iPadMiniPortrait-DTLarge". Step B: Edit validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift. RENAME the `chromeSize` constant to `iPadChromeSize` (preserve its CGSize value). Add a file-header comment block documenting D-05: "Phase 9.1 D-05: this suite is now iPad-only; iPhone snapshots removed because TrustNodeView is mounted only on the iPad path after 9.1." Step C: Edit validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift. Change `bannerSize` from `CGSize(width: 393, height: 36)` to `CGSize(width: 744, height: 36)` (iPad mini portrait width, height unchanged). Update the file-header comment to document the D-05 iPad-only posture. Optionally rename the constant to `iPadMiniPortraitBannerSize` for clarity. Step D (terminal verification — runs all R10 + R11 + R12 + logging gates): no source edits in this step; only the verify block below executes the full phase gate. The verify block now (W3) pre-checks that all 7 Phase 9 suite names exist before invoking `-only-testing:`, and (W5) includes both `UIColor` and `\.tertiarySystemFill` in the R12 strict-gate alternation.
  </action>
  <verify>
    <automated>echo '---W3: PHASE 9 SUITE PRE-CHECK---' &amp;&amp; SUITES_FOUND=$(NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPad mini (6th generation),OS=latest' -list-tests 2>/dev/null | grep -E '(TrustGraphViewSnapshotTests|TrustNodeViewSnapshotTests|ChainIntegrityBannerViewSnapshotTests|TrustGraphViewLayoutTests|TrustGraphViewAccessibilityTests|TrustGraphViewGestureTests|TrustNodeViewGestureTests)' | sort -u | wc -l | tr -d ' '); if [ "$SUITES_FOUND" != "7" ]; then echo "W3 PRE-CHECK FAIL: expected 7 Phase 9 suite names, found $SUITES_FOUND. Re-run -list-tests manually and update the suite names in this verify block."; exit 1; else echo 'W3 OK: all 7 Phase 9 suite names found'; fi &amp;&amp; NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPad mini (6th generation),OS=latest' -only-testing:validationLedgerTests/TrustGraphViewSnapshotTests -only-testing:validationLedgerTests/TrustNodeViewSnapshotTests -only-testing:validationLedgerTests/ChainIntegrityBannerViewSnapshotTests -only-testing:validationLedgerTests/TrustGraphViewLayoutTests -only-testing:validationLedgerTests/TrustGraphViewAccessibilityTests -only-testing:validationLedgerTests/TrustGraphViewGestureTests -only-testing:validationLedgerTests/TrustNodeViewGestureTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -40</automated>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20</automated>
    <automated>echo '---R11 PHASE-FINAL GATE---' &amp;&amp; MERGE_BASE=$(git merge-base main HEAD); R11_VIOLATIONS=$(git diff --name-only "$MERGE_BASE"..HEAD | grep -E '^validationLedger/Core/Load/(ChainOfTrust|TrustNode|TrustEdge|ChainIntegrity|PriorRelationship|VerificationState)\.swift$'); if [ -n "$R11_VIOLATIONS" ]; then echo "R11 VIOLATION: Core/Load trust types changed:"; echo "$R11_VIOLATIONS"; exit 1; else echo 'R11 OK: zero changes to Core/Load trust types'; fi</automated>
    <automated>echo '---R12 PHASE-FINAL PRIMARY GATE---' &amp;&amp; expected=$(printf 'validationLedger/UI/Components/RoleAvatarView.swift\nvalidationLedger/UI/Components/VerificationBadgeView.swift' | sort); actual=$(git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift' | sort); diff <(echo "$actual") <(echo "$expected") &amp;&amp; echo 'R12 OK: exactly the expected consumers call the helper' || { echo 'R12 DRIFT: unexpected consumers or missing consumers'; exit 1; }</automated>
    <automated>echo '---R12 PHASE-FINAL STRICT GATE (W5 — UIColor + tertiarySystemFill included)---' &amp;&amp; for file in $(git grep -lE 'case \.(verified|pending|unverified|flagged):' -- 'validationLedger/**/*.swift' | grep -v 'Colors.swift'); do grep -A 3 -nE 'case \.(verified|pending|unverified|flagged):' "$file" | grep -E '(UIColor|\.systemRed|\.systemBlue|\.systemYellow|\.systemGreen|\.systemOrange|\.systemPurple|\.systemTeal|\.tertiarySystemFill|DS\.Colors\.(primary|destructive|caution))' &amp;&amp; { echo "R12 STRICT VIOLATION in $file"; exit 1; }; done; echo 'R12 strict gate OK'</automated>
    <automated>echo '---VIEW-LAYER LOGGING LOCK (PHASE-FINAL)---' &amp;&amp; if git grep -nE '(os_log|Logger\.|print\()' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift validationLedger/UI/Components/RoleAvatarView.swift; then echo 'T-09.1-02 VIOLATION: log call in 9.1 view file'; exit 1; else echo 'View-layer logging lock OK across all 3 new 9.1 view files'; fi</automated>
  </verify>
  <acceptance_criteria>
    - W3 pre-check exits with 'W3 OK: all 7 Phase 9 suite names found' (defensive: catches renamed/deleted Phase 9 suite making `-only-testing` a silent no-op).
    - The 3 modified Phase 9 snapshot files compile and pass on the iPad mini simulator.
    - All 32 Phase 9 trust-graph unit/gesture/accessibility tests pass on iPad mini simulator (R10 + D-07 preserved).
    - iPhone 15 simulator full-suite run is green — every 9.1 test (Plans 01-05) passes alongside the existing Phase 8/9 iPhone-applicable suites.
    - R11 phase-final gate: zero changes to validationLedger/Core/Load/{ChainOfTrust|TrustNode|TrustEdge|ChainIntegrity|PriorRelationship|VerificationState}.swift between merge-base(main) and HEAD.
    - R12 phase-final primary gate: `git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift'` returns EXACTLY 2 files: VerificationBadgeView.swift + RoleAvatarView.swift.
    - R12 phase-final strict gate (W5): no view file (other than Colors.swift) has a VerificationState case immediately followed (within 3 lines) by a UIColor / .systemX / **.tertiarySystemFill** / DS.Colors.{primary|destructive|caution} assignment.
    - View-layer logging lock phase-final gate: zero os_log / Logger. / print( in EveryoneOnLoadStripView.swift, ChainOfVouchesView.swift, RoleAvatarView.swift.
    - `grep -cE 'iPhonePortrait' validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift` returns 0 (iPhone-sized methods removed per D-05).
    - `grep -cE 'iPadMini(Portrait|Landscape)' validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift` returns at least 2 (new iPad-sized methods present).
    - ChainIntegrityBannerViewSnapshotTests.swift bannerSize width is 744 (or the suite uses an `iPadMiniPortraitBannerSize` constant set to 744 width).
  </acceptance_criteria>
  <done>Phase 9 snapshot baselines are constrained to iPad sizes (D-05 + D-06 collapse); the iPad full trust-graph regression set (snapshot + unit + gesture + accessibility) is green; the iPhone 15 full-suite run is green; all 3 phase-final R-gates pass (R10 / R11 / R12 — with W5's strict-gate UIColor + tertiarySystemFill inclusion); the W3 Phase-9-suite-name pre-check guards against silent `-only-testing` no-ops; the view-layer logging lock holds across all 3 new 9.1 view files. WR-04 baseline human sanity-check is queued for inspection via the new iPad attachments in the Xcode test report (manual verification listed in VALIDATION.md §Manual-Only Verifications).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Size-class trait input -> composition mount | A bypass of the size-class gate (e.g., a future "feature flag" mounting TrustGraphView on iPhone outside DEBUG) would re-introduce the Phase 9 chip-overcrowding + slot-collision defects. |
| DEBUG launch arg -> Release binary | If the -Mock2DTrustGraphOnIPhone flag's parse code or its legacy mount branch leak into Release builds, the iPhone-2D code path becomes reachable in production — Configuration / Privilege Escalation. |
| View-layer log surfaces | Composed a11y labels include party displayName; any new os_log / Logger. / print( in the VC (or in the new views) leaks PII. |
| Trust derivation (chain integrity) | The VC must NOT compute integrity from edges / implicated sets; it consumes chain.integrity.verdict + chain.integrity.reason VERBATIM via ChainOfVouchesView's footer pill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-03 | Configuration / Privilege Escalation (DEBUG flag reaching Release) | LoadDetailViewController.swift — Debug2DGraphOverride enum + the iPhone-legacy mount branch | mitigate | `#if DEBUG` gating on BOTH the Debug2DGraphOverride enum declaration AND the legacy-mount branch (D-13). Validation: `grep -cE '#if DEBUG' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns ≥2 (Task 1 acceptance). Build-config inspection: `xcodebuild -showBuildSettings -configuration Release` does not include DEBUG in SWIFT_ACTIVE_COMPILATION_CONDITIONS. The DEBUG-only Debug2DOverrideTests suite asserts the launch-flag literal AND default-off contract (Task 2 + D-14). |
| T-09.1-05 | Tampering (size-class bypass) | LoadDetailViewController.swift — buildIPhoneLayout dispatcher | mitigate | The non-DEBUG iPhone path is the ONLY production iPhone code path (the legacy branch is `#if DEBUG`-gated). LoadDetailViewControllerSizeClassRoutingTests asserts iPhone compact -> new components AND iPad regular -> TrustGraphView (Task 2). Any future "feature flag" mounting TrustGraphView on iPhone in production would require a new gate that's outside this plan's scope; T-09.1-05 is closed by the size-class branch + the DEBUG gate. |
| T-09.1-02 | Information Disclosure (PII via log line) | LoadDetailViewController.swift edits | mitigate | View-layer logging lock — Task 1 verify block fails if the diff introduces any new os_log / Logger. / print( call in the VC; Task 3 verify block also runs the lock across the 3 new 9.1 view files (EveryoneOnLoadStripView, ChainOfVouchesView, RoleAvatarView). |
| T-09.1-04 | Tampering (client-side trust derivation) | VC chip-tap wiring + composition rebuild | accept | The VC handler reads `chain.integrity.verdict` only via ChainOfVouchesView.configure (which passes it to footerPill.apply VERBATIM). No `derive*` / `compute*` methods are added by this plan. T-09.1-04 is not surfaced anew. |
| T-09.1-01 | Tampering (color-ramp drift) | VC chip-tap haptic + selection-outline path | accept | The VC does not assign any UIColor to a VerificationState; it delegates highlight to chainOfVouchesView/everyoneOnLoadStripView.applySelectedRole which use `DS.Colors.primary` (UI-SPEC §Color reserved-for list). R12 final gate (Task 3) confirms the expected-consumer set is still exactly [VerificationBadgeView.swift, RoleAvatarView.swift] and the W5-revised strict gate covers both `UIColor` and `.tertiarySystemFill` literals. |
</threat_model>

<verification>
- Build: xcodebuild build exits 0 (Task 1 verify block).
- New 9.1 VC test suites (Size-Class Routing + Composition + Debug2DOverride): all pass on iPhone 15 simulator (Task 2 verify block).
- Phase 9 iPad regression: all 32 Phase 9 trust-graph tests (4 snapshot suites + 4 unit/gesture/accessibility suites) pass on iPad mini simulator (Task 3 verify block — R10 lock).
- W3 Phase-9-suite pre-check: all 7 expected suite names exist before `-only-testing:` invocation (Task 3 verify block).
- iPhone full-suite green: every 9.1 + Phase 8/9-iPhone-applicable test passes on iPhone 15 simulator (Task 3 verify block).
- R11 phase-final gate: zero changes under `validationLedger/Core/Load/` for trust types between merge-base(main) and HEAD (Task 3 verify block).
- R12 phase-final primary gate: expected-consumer set equals exactly `[VerificationBadgeView.swift, RoleAvatarView.swift]` (Task 3 verify block).
- R12 phase-final strict gate (W5): no view file maps VerificationState cases to color literals — INCLUDING `UIColor` and `.tertiarySystemFill` — outside Colors.swift (Task 3 verify block).
- View-layer logging lock phase-final: zero os_log / Logger. / print( in the 3 new 9.1 view files (Task 3 verify block).
- DEBUG flag literal locked + default-off in unit-test process (Task 2 Debug2DOverrideTests).
- W1 union-rect assertion: VL-1009 BRK chip-tap produces a unionRect whose height exceeds singleRowHeight (Task 2 Composition tests).
</verification>

<success_criteria>
- [ ] LoadDetailViewController.swift: size-class routing mounts EveryoneOnLoadStripView + ChainOfVouchesView on iPhone compact; preserves TrustGraphView + ChainIntegrityBannerView on iPad regular; removes standalone banner from iPhone scroll order (R4/R5/R8/R10).
- [ ] Chip-tap -> tree-scroll wiring is in place with the Pitfall-2 window guard + UISelectionFeedbackGenerator haptic + **W1 union-rect target spanning ALL matching rows** (D-8/D-9/D-10).
- [ ] iOS 17 registerForTraitChanges replaces the legacy traitCollectionDidChange override (RESEARCH Example 3).
- [ ] -Mock2DTrustGraphOnIPhone DEBUG launch flag mounts TrustGraphView on iPhone in DEBUG builds only; entire flag-check + legacy mount branch are `#if DEBUG`-gated (D-12/D-13).
- [ ] LoadDetailViewControllerSizeClassRoutingTests, LoadDetailViewControllerCompositionTests (including the W1 VL-1009 union-rect assertion), LoadDetailViewControllerDebug2DOverrideTests all pass on iPhone 15 simulator.
- [ ] All 32 Phase 9 trust-graph tests stay green on iPad mini simulator (R10).
- [ ] Phase 9 snapshot baselines re-recorded to iPad-only sizes (D-05 — iPhone-sized methods removed from TrustGraphViewSnapshotTests; ChainIntegrityBannerViewSnapshotTests widened to iPad 744pt; TrustNodeViewSnapshotTests chromeSize renamed for iPad-only intent).
- [ ] W3 Phase-9-suite pre-check passes before the `-only-testing:` invocation (all 7 expected suite names exist in `-list-tests` output).
- [ ] R11 phase-final gate: zero changes to validationLedger/Core/Load/ trust types between merge-base(main) and HEAD.
- [ ] R12 phase-final primary gate: expected-consumer set = [VerificationBadgeView.swift, RoleAvatarView.swift].
- [ ] R12 phase-final strict gate (W5): no view file other than Colors.swift maps VerificationState cases to color literals — INCLUDING both `UIColor` AND `.tertiarySystemFill`.
- [ ] View-layer logging lock phase-final: zero os_log / Logger. / print( in the 3 new 9.1 view files (EveryoneOnLoadStripView, ChainOfVouchesView, RoleAvatarView).
- [ ] iPhone 15 full-suite + iPad mini full-suite both green on the scoped serial simulator lane.
</success_criteria>

<output>
Create .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-05-SUMMARY.md when done. WR-04 baseline sanity-check (D-06 collapse) is queued for human inspection via the new iPad attachments in the Xcode test report — listed in VALIDATION.md §Manual-Only Verifications.
</output>
</content>
</invoke>