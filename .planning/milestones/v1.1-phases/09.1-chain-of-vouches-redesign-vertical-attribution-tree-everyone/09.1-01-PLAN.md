---
phase: 09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - validationLedger/UI/DesignSystem/Colors.swift
  - validationLedger/UI/Components/VerificationBadgeView.swift
  - validationLedgerTests/Components/VerificationBadgeViewTests.swift
autonomous: true
requirements: [R12]

must_haves:
  truths:
    - "DS.Colors.Verification.color(for:) is the ONLY VerificationState→UIColor mapping in the codebase (R12 strict gate)."
    - "VerificationBadgeView.apply(state:) calls DS.Colors.Verification.color(for: state); no inline `backgroundColor = .systemBlue/.systemYellow/.tertiarySystemFill/.systemRed` switch branch remains in the badge."
    - "The TRUST-02 lock comment is preserved (relocated or duplicated) at the new DS.Colors.Verification namespace declaration in Colors.swift."
    - "VerificationBadgeViewTests proves the badge's backgroundColor equals DS.Colors.Verification.color(for: state) for all 4 VerificationState cases — the same source-of-truth the future RoleAvatarView will consume."
  artifacts:
    - path: "validationLedger/UI/DesignSystem/Colors.swift"
      provides: "DS.Colors.Verification namespace with `static func color(for: VerificationState) -> UIColor` AND DS.Colors.Roles namespace with `static func color(for: Role) -> UIColor`"
      contains: "enum Verification"
    - path: "validationLedger/UI/Components/VerificationBadgeView.swift"
      provides: "Refactored apply(state:) consuming the shared helper"
      contains: "DS.Colors.Verification.color(for: state)"
    - path: "validationLedgerTests/Components/VerificationBadgeViewTests.swift"
      provides: "R12 invariant test — badge backgroundColor == helper output for all 4 states"
      contains: "DS.Colors.Verification.color(for:"
  key_links:
    - from: "validationLedger/UI/Components/VerificationBadgeView.swift"
      to: "validationLedger/UI/DesignSystem/Colors.swift"
      via: "DS.Colors.Verification.color(for:) call"
      pattern: "DS\\.Colors\\.Verification\\.color\\(for:"
    - from: "validationLedgerTests/Components/VerificationBadgeViewTests.swift"
      to: "validationLedger/UI/DesignSystem/Colors.swift"
      via: "XCTAssertEqual(view.backgroundColor, DS.Colors.Verification.color(for: state))"
      pattern: "DS\\.Colors\\.Verification\\.color\\(for:"
---

<objective>
Foundation plan for Phase 09.1. Extracts the TRUST-02 verification color ramp out of `VerificationBadgeView`'s inline `apply(state:)` switch into a new `DS.Colors.Verification` nested namespace in `Colors.swift` (D-01), refactors the badge to consume the helper (D-02), and lands the `DS.Colors.Roles` per-role brand palette (UI-SPEC §Per-Role Avatar Palette) so Plan 02's `RoleAvatarView` can reach for both helpers on first instantiation. Wires the R12 invariant test that proves badge + future avatar share one source-of-truth.

Purpose: Strengthen TRUST-02 from a comment-and-discipline lock to a tooling-enforced lock (R12 negative grep gate). All downstream plans depend on these two helpers existing.

Output: `Colors.swift` gains two nested namespaces (`Verification`, `Roles`); `VerificationBadgeView.apply(state:)` switch loses its 4 inline `backgroundColor` literals; one new test file in a new `validationLedgerTests/Components/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-CONTEXT.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md
@validationLedger/UI/DesignSystem/Colors.swift
@validationLedger/UI/Components/VerificationBadgeView.swift
@validationLedger/Core/Load/VerificationState.swift
@validationLedger/Roles/Role.swift
@validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift

<interfaces>
<!-- Key types and contracts. Extracted from codebase. Use directly — no exploration needed. -->

From validationLedger/Core/Load/VerificationState.swift:
- `public enum VerificationState: String, Decodable, Sendable, CaseIterable { case verified, pending, unverified, flagged }` — 4 cases; FROZEN for 9.1 (R11).

From validationLedger/Roles/Role.swift:
- `public enum Role: String, Decodable, Sendable, CaseIterable { case shipper, broker, carrier, dispatch, factoring }` — 5 cases; `displayName` extension exists ("Shipper", "Broker", "Carrier", "Dispatch", "Factoring"). FROZEN for 9.1.

From validationLedger/UI/DesignSystem/Colors.swift (existing shape):
- `public enum DS { public enum Colors { ... } }` — flat tokens today; `primary = .systemBlue`, `background = .systemBackground`, `surface = .secondarySystemBackground`, `label = .label`, `labelSecondary = .secondaryLabel`, `separator = .separator`, `destructive = .systemRed`, `caution = .systemYellow`.

From validationLedger/UI/Components/VerificationBadgeView.swift (existing apply switch — the inline color literals to remove):
- `apply(state: VerificationState)` switch sets `backgroundColor = DS.Colors.primary` (.verified), `= .systemYellow` (.pending), `= .tertiarySystemFill` (.unverified), `= DS.Colors.destructive` (.flagged). These FOUR assignments are the only behavior moving to the helper; the per-case SF Symbol image, label text, label textColor, and accessibilityLabel assignments STAY inline in the switch (D-02).
- TRUST-02 lock comment lives at the top of the file (Phase 8). It is RELOCATED or DUPLICATED at the new namespace declaration in Colors.swift per D-02.
- `layoutSubviews` cornerRadius recompute at lines 92-97 (Pitfall 8) is UNCHANGED.
- T-08-06 fail-closed "not verified" literal accessibilityLabel on `.unverified` is UNCHANGED.

From validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift (analog for the new test file):
- `test_verifiedBadgeRendersVerifiedVisuals` (lines 48-63) shape: `let view = VerificationBadgeView(); view.configure(state: .verified); XCTAssertEqual(view.backgroundColor, DS.Colors.primary, "...")` — the R12 invariant test uses this same shape, with the RHS swapped from the literal `DS.Colors.primary` to `DS.Colors.Verification.color(for: .verified)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DS.Colors.Verification + DS.Colors.Roles nested namespaces to Colors.swift</name>
  <read_first>
    - validationLedger/UI/DesignSystem/Colors.swift (in-place modification target — read the entire 29-line file to absorb existing rationale-comment cadence)
    - validationLedger/Core/Load/VerificationState.swift (the 4 cases the new helper switches on)
    - validationLedger/Roles/Role.swift (the 5 cases the Roles helper switches on)
    - validationLedger/UI/Components/VerificationBadgeView.swift (file header — the TRUST-02 lock comment block at lines 1-55 is the prose being relocated/duplicated)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (§ Code Examples → Example 1 lines 638-694 — the exact namespace shape to mirror)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (§ New token additions; § Per-Role Avatar Palette — locks the color ramp values)
  </read_first>
  <behavior>
    - Calling `DS.Colors.Verification.color(for: .verified)` returns `DS.Colors.primary` (.systemBlue).
    - Calling `DS.Colors.Verification.color(for: .pending)` returns `UIColor.systemYellow`.
    - Calling `DS.Colors.Verification.color(for: .unverified)` returns `UIColor.tertiarySystemFill`.
    - Calling `DS.Colors.Verification.color(for: .flagged)` returns `DS.Colors.destructive` (.systemRed).
    - Calling `DS.Colors.Roles.color(for: .shipper)` returns `UIColor.systemBlue`; `.broker` → `.systemOrange`; `.carrier` → `.systemGreen`; `.dispatch` → `.systemPurple`; `.factoring` → `.systemTeal`.
    - The TRUST-02 lock comment block lives above the `Verification` namespace declaration, documenting the single-source-of-truth invariant (D-04 negative grep gate).
    - Compiles clean against iOS 17+ SDK; no new imports beyond the existing `import UIKit`.
  </behavior>
  <action>
    Edit `validationLedger/UI/DesignSystem/Colors.swift` per D-01: inside `public enum DS.Colors` add two new nested `public enum` namespaces preserving existing flat tokens (primary/background/surface/label/labelSecondary/separator/destructive/caution) UNCHANGED. (1) `public enum Verification` with a single `public static func color(for state: VerificationState) -> UIColor` that switches over VerificationState's 4 cases and returns `.verified → DS.Colors.primary`, `.pending → .systemYellow`, `.unverified → .tertiarySystemFill`, `.flagged → DS.Colors.destructive` (UI-SPEC § New token additions). (2) `public enum Roles` with `public static func color(for role: Role) -> UIColor` returning the locked per-role brand colors (UI-SPEC § Per-Role Avatar Palette): `.shipper → .systemBlue`, `.broker → .systemOrange`, `.carrier → .systemGreen`, `.dispatch → .systemPurple`, `.factoring → .systemTeal`. Add a multi-line `// TRUST-02 INVARIANT ...` doc-comment block IMMEDIATELY ABOVE the `enum Verification` declaration explaining the single-source-of-truth rule, the two expected consumers (`VerificationBadgeView` + `RoleAvatarView`), and the D-04 negative grep gate. Mirror the rationale-comment cadence of the existing `caution` token block (Phase 9). The file already imports `UIKit` — no new imports needed; `VerificationState` and `Role` are reachable through the default module access.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild build -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'public enum Verification' validationLedger/UI/DesignSystem/Colors.swift` returns at least one match.
    - `grep -nE 'public enum Roles' validationLedger/UI/DesignSystem/Colors.swift` returns at least one match.
    - `grep -nE 'static func color\(for( state)?: VerificationState\)' validationLedger/UI/DesignSystem/Colors.swift` returns exactly 1 match.
    - `grep -nE 'static func color\(for( role)?: Role\)' validationLedger/UI/DesignSystem/Colors.swift` returns exactly 1 match.
    - `grep -nE 'TRUST-02' validationLedger/UI/DesignSystem/Colors.swift` returns at least one match (the relocated/duplicated lock comment).
    - Build succeeds: above xcodebuild build command exits 0.
  </acceptance_criteria>
  <done>The Colors.swift file now exposes `DS.Colors.Verification.color(for:)` and `DS.Colors.Roles.color(for:)` returning the locked values per UI-SPEC, with a TRUST-02 lock comment block documenting the single-source-of-truth invariant. The build is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor VerificationBadgeView.apply(state:) to consume DS.Colors.Verification.color(for:)</name>
  <read_first>
    - validationLedger/UI/Components/VerificationBadgeView.swift (in-place refactor target — full file; the 4 case branches at lines 153-222 are the modification target)
    - validationLedger/UI/DesignSystem/Colors.swift (post-Task-1 state — confirms the helper exists)
    - validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift (regression surface — the existing 4 per-state snapshot tests must stay green after the refactor; they assert `view.backgroundColor == DS.Colors.primary` etc. which AFTER Task 1 + 2 still hold because the helper returns the same colors)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (§ Code Examples → Example 2 lines 696-738 — the exact refactor shape)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (§ Group 2 → VerificationBadgeView MOD lines 83-114 — the locked refactor scope)
  </read_first>
  <behavior>
    - `VerificationBadgeView().configure(state: .verified)` → `backgroundColor == DS.Colors.Verification.color(for: .verified)` (i.e. `DS.Colors.primary`).
    - Same equivalence for `.pending`, `.unverified`, `.flagged`.
    - SF Symbol image, label text, label textColor, accessibilityLabel for each case are UNCHANGED (per-state branches still set them inline).
    - The T-08-06 fail-closed `"not verified"` accessibilityLabel literal on `.unverified` is UNCHANGED (still locked by `VerificationBadgeViewSnapshotTests.test_unverifiedBadgeRendersUnverifiedVisuals`).
    - The Pitfall-8 cornerRadius recompute in `layoutSubviews` is UNCHANGED.
    - All existing `VerificationBadgeViewSnapshotTests` cases (verified/pending/unverified/flagged + cornerRadius recompute) stay green.
  </behavior>
  <action>
    Edit `validationLedger/UI/Components/VerificationBadgeView.swift` per D-02: in `private func apply(state: VerificationState)`, HOIST a single `backgroundColor = DS.Colors.Verification.color(for: state)` assignment OUTSIDE the switch (place it as the first line of the function body, before the switch). Then REMOVE the 4 inline `backgroundColor = <literal>` assignments from each of the 4 case branches (.verified/.pending/.unverified/.flagged) — leave every other line in each case branch UNCHANGED (SF Symbol image assignment, symbolView.tintColor, label.text NSLocalizedString call, label.textColor, accessibilityLabel NSLocalizedString call). Update the file-header TRUST-02 doc comment to point at the new home: append a 2-3 line note that the color ramp moved to `DS.Colors.Verification.color(for:)` in Colors.swift (the relocated/duplicated lock comment lives there now per D-02). Do NOT modify `init`, `setUp`, `configure(state:)`, `layoutSubviews`, or any other method. Do NOT add or remove any imports.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/VerificationBadgeViewSnapshotTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'DS\.Colors\.Verification\.color\(for: state\)' validationLedger/UI/Components/VerificationBadgeView.swift` returns exactly 1 (the hoisted call).
    - `grep -cE 'backgroundColor = (\.systemYellow|\.tertiarySystemFill|DS\.Colors\.primary|DS\.Colors\.destructive)' validationLedger/UI/Components/VerificationBadgeView.swift` returns 0 (all 4 inline literals removed).
    - Existing `VerificationBadgeViewSnapshotTests` suite passes: the xcodebuild test invocation above exits 0 with "Test Suite 'VerificationBadgeViewSnapshotTests' passed" in the tail output.
    - `grep -nE 'case \.unverified:' validationLedger/UI/Components/VerificationBadgeView.swift` still finds the .unverified branch and the immediately-following lines still set `symbolView.image`, `label.text`, `accessibilityLabel` (refactor preserved per-case branches).
  </acceptance_criteria>
  <done>VerificationBadgeView.apply(state:) consumes the shared helper for backgroundColor; per-state branches still set SF Symbol + label text + a11y label inline; all Phase 8 regression snapshots stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create VerificationBadgeViewTests.swift with R12 invariant assertions + wire R12 negative-grep gate</name>
  <read_first>
    - validationLedger/UI/Components/VerificationBadgeView.swift (post-Task-2 state — the refactored badge under test)
    - validationLedger/UI/DesignSystem/Colors.swift (post-Task-1 state — the helper being asserted as the source-of-truth)
    - validationLedger/Core/Load/VerificationState.swift (the 4 cases iterated in the test)
    - validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift (analog: per-state assertion shape at lines 48-63; CREATE the new file in the `validationLedgerTests/Components/` directory mirroring this style — class name `VerificationBadgeViewTests`, XCTestCase base)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (§ Group 5 → VerificationBadgeViewTests NEW lines 459-505 — locked test shape)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (§ R12 Grep Gates — locked CI gate language)
  </read_first>
  <behavior>
    - `VerificationBadgeViewTests` contains 4 test methods (one per VerificationState case), each constructing a `VerificationBadgeView`, calling `configure(state: <case>)`, then asserting `XCTAssertEqual(view.backgroundColor, DS.Colors.Verification.color(for: <case>))` — proving the badge and the helper are wired to the SAME source-of-truth color (R12 invariant).
    - All 4 tests pass on the scoped serial simulator lane.
    - The R12 primary grep gate (call-site counting) returns "R12 OK" when invoked against the current tree (only `VerificationBadgeView.swift` calls `DS.Colors.Verification.color`; `RoleAvatarView.swift` will be added in Plan 02).
  </behavior>
  <action>
    Create the new directory `validationLedgerTests/Components/` (first 9.1 test file lives here). Create `validationLedgerTests/Components/VerificationBadgeViewTests.swift` with a `final class VerificationBadgeViewTests: XCTestCase` (Swift Testing NOT used per VALIDATION.md — `UIKitSnapshot.attach` requires XCTestCase). Add 4 test methods `test_apply_verified_backgroundMatchesHelper`, `test_apply_pending_backgroundMatchesHelper`, `test_apply_unverified_backgroundMatchesHelper`, `test_apply_flagged_backgroundMatchesHelper`. Each method: instantiate `VerificationBadgeView()`, call `configure(state: <case>)`, assert `XCTAssertEqual(view.backgroundColor, DS.Colors.Verification.color(for: <case>), "TRUST-02 invariant: badge backgroundColor must equal DS.Colors.Verification.color(for: <case>) — R12 single-source-of-truth.")`. Add the new Swift file to the `validationLedgerTests` target in `validationLedger.xcodeproj/project.pbxproj` so the suite is discoverable. Add `import XCTest` + `@testable import validationLedger`. NO snapshot calls — these are pure assertions; snapshot coverage stays in the existing `VerificationBadgeViewSnapshotTests.swift`.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/VerificationBadgeViewTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20 &amp;&amp; echo '---R12 GATE---' &amp;&amp; expected_consumers=('validationLedger/UI/Components/VerificationBadgeView.swift'); actual=$(git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift' | sort); expected=$(printf '%s\n' "${expected_consumers[@]}" | sort); diff &lt;(echo "$actual") &lt;(echo "$expected") &amp;&amp; echo 'R12 OK (Plan 01 expected consumers: VerificationBadgeView only)' || { echo 'R12 DRIFT'; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedgerTests/Components/VerificationBadgeViewTests.swift` exists.
    - `grep -cE 'func test_apply_(verified|pending|unverified|flagged)_backgroundMatchesHelper' validationLedgerTests/Components/VerificationBadgeViewTests.swift` returns 4.
    - All 4 test methods pass: tail output of the xcodebuild test invocation contains "Test Suite 'VerificationBadgeViewTests' passed".
    - R12 primary gate (call-site counting): `git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift'` returns EXACTLY one file at this plan's completion: `validationLedger/UI/Components/VerificationBadgeView.swift`. (Plan 02 will add `RoleAvatarView.swift` as the second expected consumer.)
    - R12 strict gate: `for file in $(git grep -lE 'case \.(verified|pending|unverified|flagged):' -- 'validationLedger/**/*.swift' | grep -v 'Colors.swift'); do grep -A 3 -nE 'case \.(verified|pending|unverified|flagged):' "$file" | grep -E '(\.systemRed|\.systemBlue|\.systemYellow|\.systemGreen|\.systemOrange|\.systemPurple|\.systemTeal|\.tertiarySystemFill|DS\.Colors\.(primary|destructive|caution))' &amp;&amp; { echo "TRUST-02 VIOLATION in $file"; exit 1; }; done; echo 'R12 strict gate OK'` exits with "R12 strict gate OK".
  </acceptance_criteria>
  <done>VerificationBadgeViewTests proves the badge consumes the shared helper for all 4 states; R12 primary grep gate passes with only `VerificationBadgeView.swift` as the consumer (the second expected consumer `RoleAvatarView.swift` arrives in Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Design-token layer → view-layer consumers | Untrusted assumption: a future developer may add a parallel `VerificationState → UIColor` switch in a new view (e.g. an avatar dot, a notification chip) without going through the helper — TRUST-02 lock erosion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-01 | Tampering (visual fraud signal) | `DS.Colors.Verification` (the shared helper) — color-ramp drift between badge and any future verification surface | mitigate | R12 negative grep gate (Task 3 verify block) — `git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift'` must return ONLY the expected consumer set; the R12 strict gate (case-followed-by-color literal) also enforces single-source-of-truth at the file level. CI wires both gates per VALIDATION.md. |
| T-09.1-04 | Tampering (server-trust derivation bypass) | `Colors.swift` (no client-side trust derivation — color is a presentation mapping, not a verdict computation) | accept | No threat surface: the helper is a pure value→value map over the enum cases; it doesn't compute trust from edges or party data. |
</threat_model>

<verification>
- Phase 8 regression: `xcodebuild test ... -only-testing:validationLedgerTests/VerificationBadgeViewSnapshotTests` exits 0 (the refactor preserves the badge contract).
- Plan 01 invariant: `xcodebuild test ... -only-testing:validationLedgerTests/VerificationBadgeViewTests` exits 0 (4 R12 invariant assertions pass).
- R12 primary gate: `git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift'` returns EXACTLY `validationLedger/UI/Components/VerificationBadgeView.swift` at this plan's completion. After Plan 02, the expected set expands to include `validationLedger/UI/Components/RoleAvatarView.swift`.
- R12 strict gate (case-followed-by-color literal): EXITS with "R12 strict gate OK" — no view file other than `Colors.swift` maps `VerificationState` cases to UIColor literals.
- Build is green: `xcodebuild build ... -scheme validationLedger ...` exits 0.
</verification>

<success_criteria>
- [ ] `DS.Colors.Verification.color(for:)` exists in `validationLedger/UI/DesignSystem/Colors.swift` returning the locked color ramp for all 4 VerificationState cases.
- [ ] `DS.Colors.Roles.color(for:)` exists in the same file returning the locked per-role brand palette for all 5 Role cases.
- [ ] The TRUST-02 lock comment is present at the new namespace declaration.
- [ ] `VerificationBadgeView.apply(state:)` hoists a single `backgroundColor = DS.Colors.Verification.color(for: state)` call and removes the 4 inline `backgroundColor = <literal>` per-case assignments; SF Symbol + label text + a11y label per-case branches are UNCHANGED.
- [ ] `validationLedgerTests/Components/VerificationBadgeViewTests.swift` exists with 4 R12-invariant test methods that pass.
- [ ] R12 primary grep gate passes at this plan's completion with `VerificationBadgeView.swift` as the only consumer.
- [ ] R12 strict grep gate passes (no view file maps VerificationState cases to color literals outside `Colors.swift`).
- [ ] Phase 8 `VerificationBadgeViewSnapshotTests` suite stays green (regression).
- [ ] The project builds clean (`xcodebuild build`).
</success_criteria>

<output>
Create `.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-01-SUMMARY.md` when done.
</output>
