---
status: partial
phase: 09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone
source: [09.1-VERIFICATION.md, 09.1-REVIEW.md]
started: 2026-05-21T04:25:00Z
updated: 2026-05-21T04:25:00Z
total_scenarios: 4
pending_scenarios: 4
---

## Current Test

[awaiting human testing]

## Tests

### 1. Re-recorded iPad Phase 9 snapshot baselines — sanity-check
expected: TrustGraphView / TrustNodeView / ChainIntegrityBannerView attachments at iPad mini portrait (744pt) + landscape (1133pt) render correctly without composition regressions vs. the Phase 9 iPhone baselines that were deleted in D-05.
why_human: Project's snapshot infra is attachment-only (no automated pixel-diff). A human must walk the latest Xcode test report attachments for `TrustGraphViewSnapshotTests`, `TrustNodeViewSnapshotTests`, and `ChainIntegrityBannerViewSnapshotTests`.
how_to_run: Open the latest test report bundle in Xcode → filter to those 3 suites → expand attachments → eyeball each iPad-sized PNG.
result: [pending]

### 2. iPhone device UAT — 5 representative fixtures
expected: For each of VL-1001 / VL-1005 / VL-1007 / VL-1009 / VL-1010 on iPhone hardware:
  (a) strip chip count matches expected per the fixture role count;
  (b) tapping a chip scrolls to that role row in the tree and the highlight fades;
  (c) VoiceOver swipes through chips → rows reading composed labels correctly;
  (d) Dynamic Type at xxxLarge produces no clipping/overlap in strip or rows;
  (e) VL-1009 BRK chip-tap scrolls to BOTH broker rows on-screen (W1 union-rect fix).
why_human: Snapshot tests cover composition but not VoiceOver swipe behavior, Dynamic Type at the user's preferred size, perceived tap-to-scroll latency, or haptic feel on real hardware.
how_to_run: TestFlight beta build on a physical iPhone running iOS 17+. Toggle VoiceOver / Dynamic Type via Settings → Accessibility.
result: [pending]

### 3. iPad device UAT regression — preserved TrustGraphView (R10)
expected: For each fixture on iPad hardware in regular size class: TrustGraphView renders as in Phase 9 (2D positioned nodes + `CAShapeLayer` edges, dim-others on tap, dashed flagged edges, pulse-on-compromised). No visual or gesture regression vs. Phase 9 device-UAT baseline.
why_human: The R10 acceptance ("all 32 Phase 9 trust-graph tests stay green on iPad simulator") is necessary but not sufficient — device interaction (pan/zoom gestures, dim-others on tap, pulse animation) needs eyes-on. Note: 1 pre-existing test failure (`TrustGraphViewLayoutTests.test_nodeFrames_areDistinctAndPositionedFromRoleSlots`) is verified pre-existing, not a 9.1 regression — see VERIFICATION.md deferred items.
how_to_run: TestFlight beta on a physical iPad. Compare against Phase 9 device-UAT recordings.
result: [pending]

### 4. Dark-mode caution-pill contrast sanity-check (advisory WR-03)
expected: On a real iPhone in dark mode at the caution verdict tier (yellow footer pill background): pill text label is legible. The code review flagged this as a potential WCAG contrast failure (`.label` on `.systemYellow` in dark mode renders white-on-yellow) but it is advisory-only and not blocking.
why_human: Snapshot tests all force `overrideUserInterfaceStyle = .light`; dark-mode contrast is invisible in CI.
how_to_run: TestFlight on a physical iPhone. Settings → Display & Brightness → Dark. Open VL-1005 or VL-1007 (caution-tier fixtures). Inspect the footer pill text legibility against the yellow background.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
