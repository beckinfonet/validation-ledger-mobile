---
phase: 07-load-domain-model-mock-contract
plan: 06
type: execute
wave: 5
depends_on: [07-03, 07-04, 07-05]
files_modified:
  - validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift
  - validationLedger/App/AppContainer.swift
  - validationLedgerTests/App/AppContainerNetworkConfigTests.swift
autonomous: true
requirements: [LOAD-01]
must_haves:
  truths:
    - "MockLoadFixtureRegistry registers per-role list handlers (GET /loads/{role}) + per-VL detail handlers (GET /loads/{id}) + action success handler (POST /loads/{id}/{action}) on MockURLProtocol"
    - "MockLoadFixtureRegistry is #if DEBUG-gated — the symbol is not present in Release builds"
    - "AppContainer.init's DEBUG block calls MockLoadFixtureRegistry.registerAppDefaults() adjacent to MockDefaultFixtures.registerAppDefaults()"
    - "AppContainerNetworkConfigTests extension exercises the .live config + each of the 3 new endpoints to prove the swap compiles (Roadmap SC #5)"
    - "MockDefaultFixtures.swift is byte-identical — load cases are NOT added to its dispatchHandler (D-17 + Pitfall 4)"
  artifacts:
    - path: validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift
      provides: "DEBUG-only fixture registry mirroring MockOTPRoleFixtureRegistry (D-17)"
      contains: "#if DEBUG"
    - path: validationLedger/App/AppContainer.swift
      provides: "DEBUG block calls MockLoadFixtureRegistry.registerAppDefaults() adjacent to MockDefaultFixtures.registerAppDefaults()"
      contains: "MockLoadFixtureRegistry.registerAppDefaults()"
    - path: validationLedgerTests/App/AppContainerNetworkConfigTests.swift
      provides: "extension test loadEndpointsConfigSwap exercising mock/live swap with new endpoints (SC #5)"
  key_links:
    - from: validationLedger/App/AppContainer.swift
      to: MockLoadFixtureRegistry.registerAppDefaults
      via: "DEBUG block adjacent to MockDefaultFixtures.registerAppDefaults() at line 454"
      pattern: "MockLoadFixtureRegistry.registerAppDefaults"
    - from: validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift
      to: MockURLProtocol.register
      via: "appended handlers (NOT MockURLProtocol.reset() — would clobber MockDefaultFixtures)"
      pattern: "MockURLProtocol.register"
---

<objective>
Tie everything from Plans 01-05 together: build the DEBUG-only `MockLoadFixtureRegistry` that registers the per-role list / per-VL detail / action handlers on `MockURLProtocol`, wire its `registerAppDefaults()` call into `AppContainer.init`'s DEBUG block adjacent to `MockDefaultFixtures.registerAppDefaults()`, and extend `AppContainerNetworkConfigTests` to exercise the one-line mock/live network swap with the 3 new endpoints (Roadmap SC #5).

Purpose: This plan is the contract's "go-live" moment for the organic DEBUG tap-through that Phases 8-10 will rely on. When a developer runs the app in DEBUG with `.mock` config, every Load-domain request now routes through the named-load library Plan 05 authored. Roadmap SC #5 — the one-line swap to `.live` config still compiles + passes with the new endpoints registered.

Output: 1 new mock-registry source file + 1 additive line in AppContainer.swift + 1 extension test on the existing AppContainerNetworkConfigTests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md
@.planning/phases/07-load-domain-model-mock-contract/07-PATTERNS.md
@.planning/phases/07-load-domain-model-mock-contract/07-RESEARCH.md
@.planning/phases/07-load-domain-model-mock-contract/07-VALIDATION.md
@.planning/phases/07-load-domain-model-mock-contract/07-03-PLAN.md
@.planning/phases/07-load-domain-model-mock-contract/07-04-PLAN.md
@.planning/phases/07-load-domain-model-mock-contract/07-05-PLAN.md

<interfaces>
<!-- All the prior-plan outputs that Plan 06 composes. -->

From validationLedger/Core/Networking/Mock/MockOTPRoleFixtureRegistry.swift (the exact analog Plan 06 mirrors — D-17):
- `#if DEBUG` ... `#endif` file-level gating
- `import CoreLocation; import Foundation`
- `enum MockOTPRoleFixtureRegistry { static func registerForRole(_ role: Role, trustTier: TrustTier = .hardwareAttested) { MockURLProtocol.reset(); MockURLProtocol.register { req in guard req.url?.path == "/auth/otp/request" else { return nil }; … return (resp, body) }; … } }`
- Uses the LOW-LEVEL `MockURLProtocol.register(_:)` API (not `MockFixture.registerFixture<E>`) so a single closure can vary response by URL-path-suffix — the pattern Plan 06 uses for `/loads/{role}` and `/loads/{id}` routing.
- KEY DIFFERENCE: MockOTPRoleFixtureRegistry CALLS `MockURLProtocol.reset()` because it owns the entire UI-test request set. MockLoadFixtureRegistry MUST NOT call reset (would clobber MockDefaultFixtures handlers registered immediately before it); it only APPENDS handlers via `register(_:)`.

From validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift (lines 46-201 — the existing organic-tap-through registry):
- DEBUG-gated; calls `MockURLProtocol.register(...)` with a dispatchHandler that returns `nil` for unknown paths.
- BYTE-IDENTICAL PRESERVATION REQUIRED — DO NOT add load cases to `MockDefaultFixtures.dispatchHandler` (Pitfall 4; PR diff on MockDefaultFixtures.swift must be zero lines).

From validationLedger/App/AppContainer.swift (lines 451-456 — the DEBUG mock-fixture registration site):
- The DEBUG block:
  ```
  #if DEBUG
  let isUITestRolePath = ProcessInfo.processInfo.arguments.contains("-MockOTPRoleForUITest")
  if case .mock = resolvedConfig, !isUITestRolePath {
      MockDefaultFixtures.registerAppDefaults()
  }
  #endif
  ```
- The new call sits adjacent to `MockDefaultFixtures.registerAppDefaults()` on the next line: `MockLoadFixtureRegistry.registerAppDefaults()`. Same triple-gate (DEBUG + .mock + !isUITestRolePath).

From validationLedger/Core/Networking/Mock/MockURLProtocol.swift (Plan 04 outputs):
- `public static func register(_ handler: @escaping Handler)` — append-only registration
- `public static func registerFixture<E: APIEndpoint>(...)` — MockFixture.swift convenience extension; can also be called from MockLoadFixtureRegistry IF the path is fixed (but per-role routing typically uses the lower-level `register(_:)`)

From validationLedgerTests/App/AppContainerNetworkConfigTests.swift (the test the plan extends):
- Existing tests cover the .mock and .live config swap with the v1.0 endpoints (KYCStatus, DeviceRegister, etc.)
- The Plan 06 extension adds a `loadEndpointsConfigSwap` test that instantiates `.live` and constructs the 3 new endpoints, asserting they compile against `apiClient.request<E>` without further plumbing. The request itself is allowed to fail (e.g. against `https://mock.local` baseURL) — SC #5 asserts the COMPILE + CALL-SITE shape, not a successful live HTTP round-trip.

From validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift, LoadDetailEndpoint.swift, LoadActionEndpoint.swift (Plan 03 outputs) — the 3 endpoints the registry handles and the SC #5 test instantiates.

From validationLedgerTests/Networking/FixtureLoader.swift — the registry uses FixtureLoader to load JSON from the test bundle... WAIT: MockLoadFixtureRegistry lives in the APP bundle (DEBUG-gated), not the test bundle. It must load fixtures from the APP bundle. The PATTERN from MockDefaultFixtures.swift inspection: handlers either inline the JSON literal (Data("…JSON…".utf8)) OR load from `Bundle.main` if the fixture file is part of the app bundle.

DECISION: For Plan 06, the MockLoadFixtureRegistry will INLINE the per-role list JSON as compiled-in Swift Data literals. This avoids needing to add fixtures to the APP-target Resources phase (they're already in the TEST-target Resources phase per Plan 05). The drawback: the JSON appears twice — once in the test bundle as a fixture file, once inlined in the registry. Mitigation: provide a `// MARK: -` divider in MockLoadFixtureRegistry.swift, document that the inlined JSON must stay in sync with the test fixtures, and add a Plan 7-or-later todo to consolidate via a shared "demo bundle" if drift becomes a maintenance pain.

ALTERNATIVELY: Add the JSON fixture files ALSO to the APP target's Resources phase (in addition to the test target), then load them via Bundle.main. This avoids duplication but requires two-target-membership for each JSON file. The PATTERNS.md "MockLoadFixtureRegistry" section recommends `FixtureLoader.loadFixture("loads-list-\(role.rawValue)")` — BUT FixtureLoader resolves the test bundle, not the app bundle.

FINAL DECISION (executor: follow this): The simpler MVP-for-Phase-7 path is to inline the JSON as Data literals in MockLoadFixtureRegistry.swift, ACCEPTING the duplication. Phase 8 may revisit if drift surfaces. The inlined JSON content for each per-role list IS what `validationLedgerTests/Networking/Fixtures/loads-list-{role}.json` contains — copy verbatim. Document the duplication explicitly in the registry's file header.

A leaner alternative the executor MAY prefer: define an internal helper `private static func loadJSONFromAppBundle(_ name: String) -> Data` that reads from `Bundle.main` (after adding the JSON files to the app target's Resources phase). If the executor chooses this, also update `AppContainerNetworkConfigTests` to confirm the fixtures are present in the app bundle. PICK ONE of inline-Data-literal vs. Bundle.main; document the choice in 07-06-SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MockLoadFixtureRegistry.swift — DEBUG-only fixture registry mirroring MockOTPRoleFixtureRegistry (D-17)</name>
  <files>validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift</files>
  <read_first>
    - validationLedger/Core/Networking/Mock/MockOTPRoleFixtureRegistry.swift (FULL file — the exact analog this mirrors per D-17)
    - validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift (lines 46-201 — confirms the byte-identical preservation rule and shows the existing organic-tap-through pattern)
    - validationLedger/Core/Networking/Mock/MockURLProtocol.swift (Plan 04 output — register(_:) is the append-only API the registry uses)
    - validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift, LoadDetailEndpoint.swift, LoadActionEndpoint.swift (Plan 03 outputs — the endpoint paths the registry handlers match)
    - All Plan 05 JSON fixtures (Task 1 + Task 2 outputs — the source of the inline JSON literals OR the bundle-load names depending on the executor's chosen approach)
    - validationLedger/Roles/Role.swift
    - .planning/phases/07-load-domain-model-mock-contract/07-PATTERNS.md (MockLoadFixtureRegistry pattern-assignment section + the "do NOT add load cases to MockDefaultFixtures" anti-pattern)
    - .planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md (D-15, D-17, D-18)
    - .planning/phases/07-load-domain-model-mock-contract/07-RESEARCH.md ("MockLoadFixtureRegistry skeleton (D-17)" Code Example)
  </read_first>
  <action>
    Create `validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift`. The file is wrapped in `#if DEBUG ... #endif` (mirroring MockOTPRoleFixtureRegistry's gating — D-17 + Threat T-07 model: Release binary must not contain the symbol).

    File header documents: Phase 7 LOAD-01, the D-17 mirror-of-MockOTPRoleFixtureRegistry rationale, the chosen JSON-loading approach (inline Data literal vs Bundle.main), and the "MockDefaultFixtures.dispatchHandler is NOT extended for load cases" anti-pattern (Pitfall 4).

    Declare `enum MockLoadFixtureRegistry { static func registerAppDefaults() { ... } }`. The function:

    1. **Does NOT call `MockURLProtocol.reset()`** — that would clobber the MockDefaultFixtures handlers registered immediately before it in AppContainer.init's DEBUG block. The registry APPENDS handlers only.

    2. **Registers a per-role list handler** for `GET /loads/{role}` — one closure registered via `MockURLProtocol.register { req in … }`. The closure:
       - Returns `nil` if `req.url?.path` doesn't start with `/loads/` OR if the method isn't GET.
       - Extracts the path suffix and matches it against `Role.allCases` rawValues. If the suffix is a known role rawValue ("shipper", "broker", "carrier", "dispatch", "factoring"), load the matching JSON (inline Data literal copied verbatim from the Plan 05 `loads-list-{role}.json` file, OR `Bundle.main.url(forResource: "loads-list-\(role)", withExtension: "json")` if the executor chose the bundle approach).
       - Returns `(HTTPURLResponse(url: req.url!, statusCode: 200, httpVersion: "HTTP/1.1", headerFields: ["Content-Type": "application/json"])!, body)`.
       - If the path is `/loads/{role}` but the suffix is NOT a known role (e.g. `/loads/VL-1042` which is the detail path — handled by the next handler), returns `nil` to fall through.

    3. **Registers a per-VL detail handler** for `GET /loads/{loadID}` — separate closure that matches when the path suffix starts with `VL-`. Maintains a static dictionary `private static let detailFixtures: [String: Data] = [...]` mapping VL-IDs to inlined JSON bodies (one entry per the 12 VL- fixtures from Plan 05). If `loadID` matches a known fixture, return 200 + the body. If not, return `nil` to fall through to MockDefaultFixtures.dispatchHandler / 404.

    4. **Registers an action handler** for `POST /loads/{loadID}/{action}` — closure that matches when the path matches `^/loads/VL-[0-9]+/(post|tender|accept|reject|cancel|status)$` AND method is POST. Returns the `load-action-success.json` body (from Plan 05) with status 200. The DEBUG organic tap-through always succeeds — the FAILURE outcomes (409 / 422 / 500 / timeout) are exercised by Plan 04's forced-failure path in PER-TEST setup, not in the default registry.

    5. **No MockFixture.registerFixture<E>** — use the lower-level `MockURLProtocol.register(_:)` directly so a single closure can vary response by URL-path-suffix.

    JSON-loading approach: PICK ONE and document in the file header. Two acceptable options:
    - **(A) inline Data literal**: paste the contents of each Plan 05 JSON fixture verbatim as a multi-line string literal in Swift (Swift 5 supports `#"""..."""#` raw multi-line strings — fine for JSON which contains `"`). Largest files (~9 loads in broker list with full ChainOfTrust) may be 100-200 lines each. Acceptable file-size cost — the registry is DEBUG-only.
    - **(B) Bundle.main load**: add each Plan 05 fixture JSON to the APP TARGET's Resources build phase (in addition to its existing test-target membership). Load via `guard let url = Bundle.main.url(forResource: name, withExtension: "json"), let data = try? Data(contentsOf: url) else { return nil }`. Requires pbxproj edits.

    Pick option (B) if the executor is comfortable with pbxproj edits. Pick (A) otherwise. Both satisfy the acceptance criteria.

    Constraints:
    - Do NOT modify `MockDefaultFixtures.swift` — D-17 + Pitfall 4 (zero-line diff on that file).
    - Do NOT modify `MockURLProtocol.swift` — Plan 04 already did the additive work; Plan 06 only consumes the existing `register(_:)` API.
    - Do NOT introduce any new dependency.
  </action>
  <verify>
    <automated>xcodebuild build -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' 2>&1 | tail -5; git diff --stat validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift | grep -c "0 insertions\|^$"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift`
    - Source: file is wrapped in `#if DEBUG` ... `#endif` (verify: first non-comment line is `#if DEBUG` and last non-comment line is `#endif`)
    - Source: declares `enum MockLoadFixtureRegistry` with a `static func registerAppDefaults()` function
    - Source: contains at least 3 `MockURLProtocol.register { ` calls (one per handler category: list, detail, action) — verify with `grep -c "MockURLProtocol.register " validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift` returns at least 3
    - Source: does NOT call `MockURLProtocol.reset()` (verify: `! grep -F "MockURLProtocol.reset()" validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift`)
    - Source: handles all 5 Role rawValues in the per-role list handler (`grep -E "(shipper|broker|carrier|dispatch|factoring)" validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift` returns at least 5 matches)
    - Source: handles all 12 VL- IDs in the per-VL detail handler (the inline Data literal contains "VL-1001" through "VL-1012", verify with grep)
    - Source: handles the 6 LoadAction pathSegments (post, tender, accept, reject, cancel, status) in the action handler
    - Byte-identical preservation: `git diff validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift` shows zero changes (D-17 + Pitfall 4)
    - Module compiles: `xcodebuild build` exits 0
  </acceptance_criteria>
  <done>MockLoadFixtureRegistry.swift exists, DEBUG-gated, mirrors MockOTPRoleFixtureRegistry pattern, registers 3 handlers (list / detail / action) on top of the existing MockDefaultFixtures registration; MockDefaultFixtures.swift is byte-identical; module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MockLoadFixtureRegistry.registerAppDefaults() into AppContainer.init's DEBUG block (D-17)</name>
  <files>validationLedger/App/AppContainer.swift</files>
  <read_first>
    - validationLedger/App/AppContainer.swift (lines 440-485 — the DEBUG mock-fixture registration block; the executor reads the full block to confirm the triple-gate at lines 451-456)
    - validationLedger/Core/Networking/Mock/MockDefaultFixtures.swift (lines 1-50 — confirms `registerAppDefaults()` is the matching call site name pattern)
    - validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift (Task 1 output — the function being called)
    - .planning/phases/07-load-domain-model-mock-contract/07-PATTERNS.md ("App/AppContainer.swift" pattern-assignment section)
    - .planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md (D-17)
  </read_first>
  <action>
    Modify `validationLedger/App/AppContainer.swift`. Find the existing DEBUG block at lines 451-456 (or wherever the current file places it — verify via `grep -n "MockDefaultFixtures.registerAppDefaults"` in your read; the line number may have drifted):

    ```
    #if DEBUG
    let isUITestRolePath = ProcessInfo.processInfo.arguments.contains("-MockOTPRoleForUITest")
    if case .mock = resolvedConfig, !isUITestRolePath {
        MockDefaultFixtures.registerAppDefaults()
    }
    #endif
    ```

    Add EXACTLY ONE line — `MockLoadFixtureRegistry.registerAppDefaults()` — immediately after `MockDefaultFixtures.registerAppDefaults()`, inside the same `if case .mock = resolvedConfig, !isUITestRolePath {` block:

    ```
    #if DEBUG
    let isUITestRolePath = ProcessInfo.processInfo.arguments.contains("-MockOTPRoleForUITest")
    if case .mock = resolvedConfig, !isUITestRolePath {
        MockDefaultFixtures.registerAppDefaults()
        MockLoadFixtureRegistry.registerAppDefaults()   // Phase 7 LOAD-01 — D-17
    }
    #endif
    ```

    Order matters: `MockDefaultFixtures` registers a catch-all dispatchHandler that returns `nil` for unknown paths (line 102-103 of MockDefaultFixtures.swift); calling MockLoadFixtureRegistry AFTER means load-domain paths are checked first (first-match-wins per `MockURLProtocolRegistryTests.firstMatchWins`). Per the patterns-doc this is the documented order.

    The diff to AppContainer.swift is EXACTLY this one inserted line (plus optionally a trailing-comment annotation). No other change. No reformatting. No reordering. No removing of any existing line.

    If the patterns differ from this exact form (e.g. the DEBUG block at lines 451-456 has additional logic now), insert the new call at the equivalent place — adjacent to `MockDefaultFixtures.registerAppDefaults()` — and document the deviation in 07-06-SUMMARY.
  </action>
  <verify>
    <automated>git diff --stat validationLedger/App/AppContainer.swift; grep -n "MockLoadFixtureRegistry.registerAppDefaults" validationLedger/App/AppContainer.swift</automated>
  </verify>
  <acceptance_criteria>
    - Source modified: `validationLedger/App/AppContainer.swift` now contains exactly one line matching `MockLoadFixtureRegistry.registerAppDefaults()` (verify: `grep -c "MockLoadFixtureRegistry.registerAppDefaults" validationLedger/App/AppContainer.swift` returns at least 1)
    - The new line sits inside a `#if DEBUG` block AND inside an `if case .mock = resolvedConfig, !isUITestRolePath {` conditional (verify by reading the surrounding context — the executor pastes the 5-line block including the new call into the SUMMARY for human review)
    - The new line is AFTER `MockDefaultFixtures.registerAppDefaults()` (verify: line number of new call > line number of old call, via two `grep -n` calls)
    - The diff is minimal: `git diff --numstat validationLedger/App/AppContainer.swift` shows insertions ≤ 2 (the new line + optional trailing comment) and deletions = 0
    - Module compiles: `xcodebuild build` exits 0
  </acceptance_criteria>
  <done>AppContainer.swift has exactly one added line wiring MockLoadFixtureRegistry.registerAppDefaults() into the DEBUG block; the diff is minimal; module compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Extend AppContainerNetworkConfigTests with the SC #5 mock/live swap smoke test for the 3 new endpoints</name>
  <files>validationLedgerTests/App/AppContainerNetworkConfigTests.swift</files>
  <read_first>
    - validationLedgerTests/App/AppContainerNetworkConfigTests.swift (FULL existing file — the executor extends it; the executor must understand the existing setup, the .mock and .live config patterns, and how endpoints are tested)
    - validationLedger/App/AppContainer.swift (Task 2 output — confirm DEBUG-block wiring landed)
    - validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift, LoadDetailEndpoint.swift, LoadActionEndpoint.swift (Plan 03 outputs — the endpoints the test instantiates)
    - validationLedger/Roles/Role.swift (LoadListEndpoint init)
    - validationLedger/Core/Load/LoadAction.swift (Plan 01 — LoadActionEndpoint init)
    - .planning/phases/07-load-domain-model-mock-contract/07-RESEARCH.md ("Validation Architecture → Phase Requirements → Test Map" — SC #5 row maps to `loadEndpointsConfigSwap`)
    - .planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md (D-18, SC #5)
  </read_first>
  <action>
    Modify `validationLedgerTests/App/AppContainerNetworkConfigTests.swift` ADDITIVELY. The existing tests stay byte-identical. Add new tests that exercise the mock/live config swap with the 3 new Phase 7 endpoints.

    Add the following tests (signature exact within the executor's preferred Swift Testing organizational pattern — append to the existing `@Suite` if one wraps the file, else create a new sibling `@Suite("AppContainer config swap — Phase 7 load endpoints (SC #5)")`):

    Test 1 — `@Test("loadEndpointsConfigSwap: with .mock config, AppContainer constructs an APIClient that accepts all 3 load endpoints at the request<E> call site (compile-time + call-site shape)")`:
    - Instantiate an AppContainer configured for `.mock`. (Use whatever helper the existing tests use to make a config-overridden AppContainer.)
    - Construct `let listEndpoint = LoadListEndpoint(role: .broker)`, `let detailEndpoint = LoadDetailEndpoint(loadID: "VL-1001")`, `let actionEndpoint = LoadActionEndpoint(loadID: "VL-1004", action: .accept, body: LoadActionEndpoint.RequestBody(actorRole: .carrier, targetPartyID: nil, respondByAt: nil, note: nil))`.
    - For each: call `_ = try await container.apiClient.request(endpoint)` inside a `do { … } catch { … }` block. In the .mock path, expect SUCCESS for the list endpoint (the registry registered the broker list fixture in Task 1-2). For the detail endpoint, expect SUCCESS (the registry registered VL-1001 in the detail fixtures). For the action endpoint, expect SUCCESS (the registry registered the action success handler).
    - This proves the .mock side wires correctly through the AppContainer.

    Test 2 — `@Test("loadEndpointsConfigSwap: with .live config, AppContainer's APIClient compiles + accepts the 3 endpoints at the request<E> call site (SC #5 — request may fail, but the swap is compile-clean)")`:
    - Instantiate an AppContainer configured for `.live` (or the live-equivalent — whatever the existing AppContainerNetworkConfigTests uses).
    - Build the 3 endpoints exactly as in Test 1.
    - For each, call `_ = try await container.apiClient.request(endpoint)` inside a `do { … } catch { … }`. ASSERT that whatever the result is — success OR error — the call compiled. The request will likely throw a transport/HTTP error because the .live baseURL points at a non-existent server (`https://mock.local` or similar in the test environment). The acceptance criterion is: the code compiles, the request<E> call site accepts the new endpoint types, and the request returns either a Response value or a typed `NetworkError` — NOT a Swift compile error.
    - To make this test deterministic, you can register a forced-failure on each endpoint's path via Plan 04's `registerForcedFailure(... .urlError(.notConnectedToInternet))` (since the .live config still routes through MockURLProtocol when MockURLProtocol is set as the URLSession protocolClass — verify this assumption by reading the existing AppContainerNetworkConfigTests). Then assert the request throws.
    - The point of the test: `apiClient.request(LoadListEndpoint(...))` is a valid Swift expression in the .live path. Nothing in Phase 7 has touched APIClient or APIEndpoint, so this is a compile-clean smoke test.

    Test 3 — `@Test("loadEndpointsConfigSwap: the .mock config response decodes into the correct Response type for each of the 3 endpoints")`:
    - With the .mock container from Test 1, call each endpoint's `request<E>` and ASSERT the resulting type:
    - `let listResponse = try await container.apiClient.request(LoadListEndpoint(role: .broker))`; `#expect(listResponse.loads.count == 9)` (per Plan 05's broker list = 9 loads).
    - `let detailResponse = try await container.apiClient.request(LoadDetailEndpoint(loadID: "VL-1009"))`; `#expect(detailResponse.chainOfTrust.integrity.verdict == .compromised)` (VL-1009 = double-broker, compromised — verifies the end-to-end pipeline from registry → MockURLProtocol → APIClient.request<E> → Decodable Response → fail-closed verdict decoder).
    - `let actionResponse = try await container.apiClient.request(LoadActionEndpoint(loadID: "VL-1004", action: .accept, body: …))`; `#expect(actionResponse.load.id.hasPrefix("VL-"))`.

    This Test 3 is the END-TO-END proof of the Phase 7 contract: every layer (registry, MockURLProtocol, APIClient, endpoint, Response, Load decoder, ChainOfTrust decoder, ChainIntegrity decoder, VerificationState fail-closed decoder) composes correctly.

    Notes:
    - If the existing AppContainerNetworkConfigTests has `.serialized` discipline, the new tests inherit it (any test that touches MockURLProtocol must be `.serialized`).
    - Tests run in DEBUG mode (the test scheme is DEBUG); MockLoadFixtureRegistry IS available.
    - The .live config path likely needs the existing test's URL-pinning/baseURL approach; copy verbatim.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerTests/App/AppContainerNetworkConfigTests 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File modified: `validationLedgerTests/App/AppContainerNetworkConfigTests.swift` contains 3 new `@Test` methods covering: .mock-config swap (Test 1), .live-config swap compile-clean (Test 2), and end-to-end Response decoding (Test 3)
    - Existing tests are byte-identical (no removed or renamed @Test methods) — verify by `git diff` listing only additions
    - Behavior: with .mock config, `container.apiClient.request(LoadListEndpoint(role: .broker))` returns a Response with `loads.count == 9` (proves the broker list fixture from Plan 05 is wired through the registry from Task 1)
    - Behavior: with .mock config, `container.apiClient.request(LoadDetailEndpoint(loadID: "VL-1009"))` returns a Response with `chainOfTrust.integrity.verdict == .compromised` (proves the entire decode pipeline — registry → MockURLProtocol → APIClient → fail-closed decoder)
    - Behavior: with .live config, the 3 endpoint constructors + `apiClient.request<E>` calls compile (test passes regardless of network outcome)
    - Test command exits 0: `xcodebuild test … -only-testing:validationLedgerTests/App/AppContainerNetworkConfigTests` passes all existing tests + the 3 new tests
  </acceptance_criteria>
  <done>AppContainerNetworkConfigTests is additively extended with 3 SC #5 tests; the end-to-end pipeline is verified on the simulator lane; all existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MockLoadFixtureRegistry → MockURLProtocol global handler array | The DEBUG-only registry contributes handlers to the same global array as MockDefaultFixtures; ordering and first-match-wins discipline determines what response a request gets |
| MockLoadFixtureRegistry → Bundle.main (option B) OR inline Data literal (option A) | The fixture JSON content is the "demo data" the developer sees — drift between this content and the test-target fixtures becomes a demo-vs-test inconsistency |
| AppContainer.init DEBUG block → MockLoadFixtureRegistry symbol | The symbol must not be present in Release; `#if DEBUG` gating at both the file (registry) and call-site (AppContainer.init) is the enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-27 | Information Disclosure | MockLoadFixtureRegistry ships in Release | mitigate | Triple-gate: (1) `#if DEBUG` at the registry file (Task 1 acceptance), (2) `#if DEBUG` at the AppContainer call site (Task 2 acceptance criterion verifies the new line is inside #if DEBUG), (3) `if case .mock = resolvedConfig, !isUITestRolePath` runtime guard (Task 2 acceptance verifies the call is inside that conditional). Verifiable by: a Release build cannot link a reference to `MockLoadFixtureRegistry.registerAppDefaults` (would fail to compile, which is the intended fail-loud behavior). |
| T-07-28 | Tampering | MockDefaultFixtures grows to include load cases (Pitfall 4) | mitigate | Task 1 acceptance criterion: `git diff MockDefaultFixtures.swift` shows zero changes; Task 1 verify command grep checks. |
| T-07-29 | Spoofing | end-to-end pipeline produces wrong verdict on flagged load | mitigate | Task 3 Test 3 asserts VL-1009 produces `.compromised` end-to-end. If the fail-closed decoder (Plan 01) or the registry handler (Task 1) or the AppContainer wiring (Task 2) regresses, this test fails. |
| T-07-30 | Repudiation | mock-vs-live swap drift (test passes on .mock, code wouldn't compile on .live) | mitigate | Task 3 Test 2 instantiates the .live AppContainer and constructs the 3 endpoints — the Swift compiler enforces that the endpoints conform to APIEndpoint. SC #5 is the success criterion. |
| T-07-31 | Tampering | inline JSON in registry drifts from test-target fixtures (option A) | accept | Documented in Task 1 file header; the inlined JSON is "demo data only" and the test-target fixtures are the authoritative decode contract. A Phase 8 todo: consolidate via a shared "demo bundle" if drift becomes a maintenance pain. |
| T-07-32 | Tampering | byte-identical preservation of MockURLProtocol register/reset (Plan 04 → Plan 06 invariant) | mitigate | Plan 04's Task 1 acceptance criteria already verified this; Plan 06 only CONSUMES `register(_:)` via the registry — no further MockURLProtocol changes. Verified by `git diff MockURLProtocol.swift` since Plan 04's commit = zero changes in Plan 06's commit range. |
</threat_model>

<verification>
- MockLoadFixtureRegistry.swift exists, DEBUG-gated, registers 3 handlers (list, detail, action) without calling `MockURLProtocol.reset()`.
- AppContainer.swift's DEBUG block now has exactly one added line wiring the registry.
- MockDefaultFixtures.swift is byte-identical (zero-line diff).
- AppContainerNetworkConfigTests is additively extended with 3 SC #5 tests; all tests (existing + new) pass.
- The end-to-end pipeline test (Test 3) decodes VL-1009 with `integrity.verdict == .compromised` — exercises every layer.
- The .live-config compile-clean test (Test 2) proves the one-line swap still compiles.
- No new file under `Features/`, `Roles/`, or any UI tier.
</verification>

<success_criteria>
- Roadmap SC #5 is satisfied: mock/live swap compiles + passes with the new endpoints registered, with no change to APIClient or MockURLProtocol's existing API surface.
- LOAD-01 is fully satisfied: mock endpoints + fixtures registered, exercisable through the AppContainer's `apiClient.request<E>` for the 3 new endpoints.
- D-15, D-17, D-18, D-19 are reflected in the wiring.
- Phase 7 is complete after this plan: the contract-first foundation is ready for Phase 8 (list UI) and Phase 10 (action UI) to consume.
</success_criteria>

<output>
Create `.planning/phases/07-load-domain-model-mock-contract/07-06-SUMMARY.md` recording: the JSON-loading approach chosen (A inline vs B Bundle.main), the exact AppContainer.swift diff (paste the 5-line block around the new call), the test results for AppContainerNetworkConfigTests, any cross-plan drift caught (e.g. fixture content vs decode-test expectations), and the Phase 7 close-out state (all 5 success criteria satisfied).
</output>
