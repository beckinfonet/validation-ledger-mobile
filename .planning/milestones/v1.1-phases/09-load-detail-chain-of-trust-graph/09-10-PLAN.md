---
phase: 09-load-detail-chain-of-trust-graph
plan: 10
type: execute
wave: 7
depends_on:
  - 09-07
  - 09-08
  - 09-09
files_modified:
  - validationLedgerUITests/Loads/LoadDetailFlowTests.swift
  - .planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md
  - .planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md
  - .planning/STATE.md
autonomous: true
requirements:
  - LOAD-05
  - LOAD-06
  - TRUST-01
  - TRUST-03
  - TRUST-04
  - TRUST-05
threat_refs:
  - T-09-03
  - T-09-04
  - T-09-12

must_haves:
  truths:
    - "All 5 LoadDetailFlowTests XCUITest methods are populated (no XCTSkip remaining): test_rowTap_pushesDetail (Plan 03), test_nodeTap_opensVerificationBasisSheet (Plan 07), test_edgeTap_opensHandoffSheet (Plan 08), test_compromisedVerdict_bannerAccessibilityLabelContainsReason (this plan), test_singleFingerScroll_propagatesPastGraph_toBodyScrollView (this plan)"
    - "09-MANUAL-TESTS.md is authored from VALIDATION.md § Manual-Only Verifications — a device-test checklist that ships alongside the phase summary for human triage of pinch-zoom feel, halo-pulse continuity across rotation, VoiceOver traversal, iPad split aesthetic, skeleton shimmer continuity, dim-others treatment"
    - "VALIDATION.md § Per-Task Verification Map is updated with concrete Task IDs (currently TBD); every -only-testing: path the planner used in Plans 03-09 is reflected in the map"
    - "STATE.md Phase 9 status is updated to 'Executed — pending verify' (the verify step is a separate /gsd:verify-work invocation)"
    - "The 6 Phase 9 requirement IDs (LOAD-05, LOAD-06, TRUST-01, TRUST-03, TRUST-04, TRUST-05) each have at least one passing automated test from the phase's test corpus"
    - "Zero PII in the manual-test checklist (synthetic fixture names + no real customer data)"
    - "The compromised-verdict banner accessibility label is locked at the XCUITest level — proves a screen-reader user perceives the chain compromise reason"
    - "The outer-page-scroll propagation behavior is locked at the XCUITest level (RESEARCH §1 line 252 acceptance criterion + § Common Pitfalls Pitfall 3) — proves the graph doesn't swallow body scroll"
  artifacts:
    - path: "validationLedgerUITests/Loads/LoadDetailFlowTests.swift"
      provides: "All 5 test methods populated; the 5-role smoke flow + 4 specific gate-tests covering LOAD-05 / TRUST-03 / TRUST-04 / TRUST-05 / outer-scroll propagation"
      contains: "test_compromisedVerdict_bannerAccessibilityLabelContainsReason"
    - path: ".planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md"
      provides: "Device-test checklist authored from VALIDATION.md § Manual-Only Verifications"
      contains: "Pinch-zoom gesture feel"
    - path: ".planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md"
      provides: "Per-Task Verification Map updated with concrete Task IDs from Plans 03-09"
      contains: "09-03-T1"
  key_links:
    - from: ".planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md"
      to: ".planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md"
      via: "VALIDATION § Manual-Only Verifications table is the source for the checklist"
      pattern: "Manual"
---

<objective>
Cross-cutting close-out for Phase 9: fill in the remaining two XCUITest methods (banner-a11y + outer-scroll propagation), author the device-test checklist (`09-MANUAL-TESTS.md`) from VALIDATION.md § Manual-Only Verifications, update the VALIDATION.md Per-Task Verification Map with concrete Task IDs from Plans 03-09, and mark STATE.md Phase 9 status as "Executed — pending verify".

Purpose: Phase 9 is the highest-design-investment phase in v1.1; the marquee surface has six manual-only checks (gesture feel, halo-pulse continuity, VoiceOver traversal, iPad aesthetic, shimmer continuity, dim-others) that cannot be fully captured by snapshot tests. This plan packages them into a reviewable checklist that the verify-work step + the human reviewer can work through on device. It also closes the per-task verification map so VALIDATION.md becomes auditable (every Task ID is tied to a concrete -only-testing: command).

Output: 4 modified files. No new production source. Phase 9 is feature-complete after Plan 09; this plan is documentation + cross-cutting test coverage + state update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-01-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-02-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-03-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-04-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-05-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-06-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-07-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-08-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-09-PLAN.md

@validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift
@validationLedgerUITests/Loads/LoadDetailFlowTests.swift

<interfaces>
<!-- The two remaining XCUITest method stubs Plan 02 created and Plans 03/07/08 partially populated. -->
<!-- Plan 02 placed 5 method shells in LoadDetailFlowTests.swift. Plans 03/07/08 populated 3 of them. This plan populates the remaining 2. -->

Method shells to populate:
1. `test_compromisedVerdict_bannerAccessibilityLabelContainsReason()` — load VL-1009 (compromised archetype); assert `app.otherElements["chain-integrity-banner"].label` contains the fixture's reason copy fragment (e.g. "compromised" — Phase 7 D-13(a) banner copy).
2. `test_singleFingerScroll_propagatesPastGraph_toBodyScrollView()` — RESEARCH §1 line 252 acceptance criterion. The outer body scroll should be drivable by a single-finger drag starting INSIDE the graph region.

XCUIElement APIs to use:
- `app.otherElements["chain-integrity-banner"].label` — XCUIElement's `label` returns the accessibilityLabel string (UI-SPEC line 684 — "Chain integrity: Compromised. {reason}").
- For outer-scroll test: pick a body-element identifier (e.g. `app.staticTexts["load-detail.body.parties-section"]` or the freight-row container's identifier from Plan 04/09) and assert it becomes hittable AFTER a swipe-up gesture STARTED inside the graph region — meaning the body scrolled despite the gesture starting in graph area.

XCUIElement swipe API:
- `element.swipeUp()` — performs a swipe-up gesture starting at the element's hitPoint.
- Alternative: `XCUICoordinate` for precise start-point control: `let startCoord = app.otherElements["load-detail.trust-graph"].coordinate(withNormalizedOffset: CGVector(dx: 0.5, dy: 0.5)); let endCoord = startCoord.withOffset(CGVector(dx: 0, dy: -200)); startCoord.press(forDuration: 0.1, thenDragTo: endCoord)` — drag from graph center upward.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate the remaining 2 XCUITest methods in LoadDetailFlowTests + author 09-MANUAL-TESTS.md from VALIDATION.md § Manual-Only Verifications + update VALIDATION.md Per-Task Verification Map with concrete Task IDs + update STATE.md Phase 9 status</name>
  <files>
    validationLedgerUITests/Loads/LoadDetailFlowTests.swift,
    .planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md,
    .planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md,
    .planning/STATE.md
  </files>
  <behavior>
    - All 5 LoadDetailFlowTests methods are real assertions (no XCTSkip remains anywhere in the file).
    - 09-MANUAL-TESTS.md is a fresh markdown file packaging VALIDATION.md § Manual-Only Verifications table into a runnable device-test checklist (one entry per row, with explicit click-by-click instructions, prerequisites, expected outcomes, and pass/fail checkboxes).
    - VALIDATION.md § Per-Task Verification Map has every "TBD" Task ID replaced with the concrete Plan-XX Task-Y identifier (e.g. "09-03-T1" = Plan 03 Task 1; the locked nomenclature is `{phase}-{plan}-T{taskNumber}`).
    - STATE.md Phase 9 entry is updated: status moves from "Ready to plan" (current) to "Executed — pending verify"; `stopped_at` reflects the close of execution; `last_activity` is bumped to today.
    - Both new XCUITest methods exercise the marquee compromised-archetype load (VL-1009 per Phase 7 D-13(a)) so the banner + reason + graph render at full fidelity.
  </behavior>
  <read_first>
    - validationLedgerUITests/Loads/LoadDetailFlowTests.swift (state after Plans 03/07/08 — 3 methods populated, 2 still XCTSkip)
    - validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift (driveFullOTPFlow + role-launch helpers used by Plan 03 — same helpers needed here)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md (§ Manual-Only Verifications table lines 87-99 — the 6 rows the checklist packages; § Per-Task Verification Map lines 42-58 — the TBD rows this plan fills in)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§1 line 252 — outer-page-scroll acceptance criterion; § Common Pitfalls Pitfall 3 — outer scroll propagation behavior)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (§ Accessibility identifiers locked list — `chain-integrity-banner`, `load-detail.body.*`; § Pre-fraud-tier color invariants — banner copy templates)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-13 fraud archetypes — VL-1009 / VL-1010 / VL-1011 mapping)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1009.json (post-Plan-01 content — the canonical compromised-archetype reason text; planner reads to confirm the banner reason fragment for the XCUITest assertion)
    - .planning/STATE.md (current state — Phase 9 "Ready to plan"; the fields to update)
    - .planning/phases/08-role-filtered-load-list/08-04-SUMMARY.md (the Phase 8 manual-test posture if any — reference for tone/format of the manual-test markdown)
  </read_first>
  <action>
    A. POPULATE the 2 remaining XCUITest methods in `validationLedgerUITests/Loads/LoadDetailFlowTests.swift`. Replace XCTSkip stubs.

    1. `test_compromisedVerdict_bannerAccessibilityLabelContainsReason()`:
       ```
       let app = launch(role: "broker")
       driveFullOTPFlow(app)
       navigateToLoadsTab(app)  // reuse Phase 8 pattern if it exists; else inline
       
       // Tap the compromised-archetype fixture row (VL-1009).
       let row = app.cells["loads-list.row.VL-1009"]
       XCTAssertTrue(row.waitForExistence(timeout: 10), "VL-1009 row should appear in broker fixture")
       row.tap()
       
       // Wait for the detail screen.
       XCTAssertTrue(app.otherElements["load-detail"].waitForExistence(timeout: 5))
       
       // Assert the chain-integrity banner exists and its accessibilityLabel contains the locked reason fragment.
       // Banner accessibilityLabel format (UI-SPEC line 684): "Chain integrity: Compromised. {reason}"
       let banner = app.otherElements["chain-integrity-banner"]
       XCTAssertTrue(banner.exists, "Compromised-archetype load must render the chain-integrity banner")
       XCTAssertTrue(banner.label.contains("Compromised"), "Banner label must contain verdict 'Compromised'. Got: \(banner.label)")
       // The reason fragment is from the fixture's chain_integrity.reason — the planner reads VL-1009 to pick a stable fragment (e.g. "re-tender" or "broker") that's resilient to copy edits.
       // For v1.1 the fixture's reason is server-supplied and known; assert a substring match.
       ```
       The planner reads VL-1009's `chain_of_trust.integrity.reason` to pick a stable fragment for the assertion (after Plan 01 re-authoring; verify the fixture's text at execute time). Acceptable substring: "broker" / "compromised" — pick one that's likely permanent in fixture copy.

    2. `test_singleFingerScroll_propagatesPastGraph_toBodyScrollView()`:
       Per RESEARCH §1 line 252 — "An XCUITest scrolls the outer page body with a SINGLE-FINGER drag starting inside the graph region and asserts the body moved."
       ```
       let app = launch(role: "broker")
       driveFullOTPFlow(app)
       navigateToLoadsTab(app)
       
       let row = app.cells.matching(identifier: "loads-list.row.VL-1009").firstMatch  // or any fixture
       XCTAssertTrue(row.waitForExistence(timeout: 10))
       row.tap()
       XCTAssertTrue(app.otherElements["load-detail"].waitForExistence(timeout: 5))
       
       // Find a body section that's NOT visible on initial paint (below the fold).
       // The freight-section / parties-section / verdict-block (when present) live below the graph.
       let bodySection = app.otherElements["load-detail.body.parties-section"]
       // It may or may not be in the accessibility tree before scrolling. Confirm it's not hittable.
       let initialHittable = bodySection.isHittable
       
       // Drag from inside the graph region upward — single-finger drag.
       let graph = app.otherElements["load-detail.trust-graph"]
       let start = graph.coordinate(withNormalizedOffset: CGVector(dx: 0.5, dy: 0.5))
       let end = start.withOffset(CGVector(dx: 0, dy: -300))  // 300pt upward swipe
       start.press(forDuration: 0.1, thenDragTo: end)
       
       // The body section should now be hittable OR more elements should be present below the graph.
       // The cleanest assertion: after the swipe, the body section becomes hittable.
       XCTAssertTrue(bodySection.waitForExistence(timeout: 3), "Single-finger drag inside graph must propagate to body scroll — RESEARCH §1 / Pitfall 3")
       XCTAssertTrue(bodySection.isHittable, "Body section should be hittable after outer-page scroll")
       ```
       Per RESEARCH §1 Acceptance line 252 — the test proves the `panGestureRecognizer.minimumNumberOfTouches = 2` lock is doing its job at runtime.

    Optionally factor the navigateToLoadsTab(_:) helper into a shared `UITestHelpers.swift` or copy verbatim from RoleLoadsTabSmokeTests.

    B. AUTHOR `.planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md`. Use the VALIDATION.md § Manual-Only Verifications table (lines 87-99) as the source.

    Format:
    ```markdown
    # Phase 9 — Manual Device-Test Checklist

    Run on physical devices: iPhone 17 (compact width) AND iPad Air (regular width).
    Prerequisite: Phase 9 build is installed AND -MockOTPRoleForUITest is enabled for the broker role.
    
    Each entry below has explicit click-by-click instructions, expected outcomes, and a pass/fail checkbox.
    Failures are filed under VERIFICATION.md gaps for /gsd:verify-work consumption.

    ## 1. Pinch-zoom gesture feel + outer-scroll preservation (TRUST-01 / D-04)

    **Why manual:** Multi-touch + gesture-recognizer interaction quality is observation-only; snapshots can't assert "feel".

    **Prerequisites:** iPhone 17 + iPad Air physical devices.

    **Steps:**
    1. Launch the app; log in as broker.
    2. Tap any load in the list (suggest VL-1001 — clean archetype).
    3. The detail screen appears with the trust graph in the upper region.
    4. SINGLE-FINGER drag DOWN starting inside the graph region.
       - **Expected:** the outer page body scrolls UP (revealing timeline + freight rows below the graph). The graph does NOT pan.
    5. Two-finger PINCH inside the graph region.
       - **Expected:** the graph zooms in/out smoothly.
    6. Two-finger DRAG inside the graph (while zoomed > 1.0).
       - **Expected:** the graph pans inside its bounds.
    7. DOUBLE-TAP on the broker node.
       - **Expected:** the graph animates to recenter+zoom (~250ms ease-in-out) at ~1.8x scale on that node.
    8. DOUBLE-TAP on empty canvas inside the graph (away from any node).
       - **Expected:** the graph resets to fit-all-nodes-tight zoom + center.

    Pass: [ ] iPhone 17  [ ] iPad Air

    ## 2. Halo pulse animation on compromised node (TRUST-05 / D-15)

    ... (continue for all 6 rows in VALIDATION.md § Manual-Only Verifications)
    ```

    Author all 6 entries (pinch-zoom, halo pulse, VoiceOver traversal, iPad split, skeleton shimmer continuity, dim-others) per the VALIDATION.md table. Each entry includes the requirement ID, the "why manual" rationale, prerequisites, and explicit steps + expected outcomes.

    C. UPDATE `.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md` § Per-Task Verification Map (lines 42-58 currently "TBD"):

    Replace every `TBD | TBD | -` with the concrete Plan-XX Task-Y identifier from Plans 03-09:

    Example mapping (the planner finalizes by reading each plan's task names):
    | Task ID | Plan | Wave | Requirement |
    |---------|------|------|-------------|
    | 09-01-T1 | 01 | 1 | (foundation — PriorRelationship value type) |
    | 09-01-T2 | 01 | 1 | (foundation — TrustNode contract evolution) |
    | 09-01-T3 | 01 | 1 | (D-14 — fixture re-authoring) |
    | 09-02-T1 | 02 | 1 | (Wave 0 — test shells) |
    | 09-02-T2 | 02 | 1 | (D-14 contract gate — LoadDetailFixtureContractTests) |
    | 09-03-T1 | 03 | 2 | LOAD-05 — LoadDetailViewModel state machine |
    | 09-03-T2 | 03 | 2 | LOAD-05 — VC shell + factory wiring + row-tap XCUITest |
    | 09-04-T1 | 04 | 3 | (skeleton + shimmer lifecycle) |
    | 09-04-T2 | 04 | 3 | (body container + error state) |
    | 09-05-T1 | 05 | 3 | LOAD-06 — StatusTimelineView |
    | 09-06-T1 | 06 | 4 | TRUST-01 — TrustNodeView + gesture tests |
    | 09-06-T2 | 06 | 4 | TRUST-01 / TRUST-05 — TrustGraphView + a11y + snapshot |
    | 09-07-T1 | 07 | 5 | TRUST-03 — VerificationBasisSheetViewController |
    | 09-08-T1 | 08 | 5 | TRUST-04 — HandoffDetailSheetViewController |
    | 09-09-T1 | 09 | 6 | TRUST-05 — ChainIntegrityBannerView + verdict block |
    | 09-09-T2 | 09 | 6 | TRUST-05 / D-21 — iPhone vs iPad composition + accessibility order |
    | 09-10-T1 | 10 | 7 | (cross-cutting verification) |

    Update VALIDATION.md frontmatter: set `wave_0_complete: true` (after Plan 02 shipped) and `nyquist_compliant: true` (every task has an automated verify or a Wave 0 dependency satisfied by Plan 02). Set `status: complete`.

    D. UPDATE `.planning/STATE.md`:

    - `stopped_at: "Phase 9 — Load Detail + Trust Graph: Executed — pending verify"`
    - `last_updated: "<today's date in ISO 8601>"`
    - `last_activity: <today>`
    - `progress.completed_plans` += 10 (the 10 plans of Phase 9)
    - Current Position: `Phase: 9; Plan: 09-10 complete; Status: Executed — pending verify; Last activity: <today>`
    - Optionally add a note under "Accumulated Context > Decisions" recording the Phase 9 contract evolution (TrustNode.priorRelationshipCount → priorRelationships array; the new PriorRelationship value type; UISheetPresentationController first-usage; CAShapeLayer-as-edge-renderer pattern).

    Per T-09-04 (PII): the manual-test checklist contains zero customer data — only synthetic VL-#### IDs and party names. Per T-09-12 (manual-verification scope): the checklist explicitly NAMES the device hardware needed (iPhone 17 + iPad Air); any test that requires a self-hosted runner is flagged as such per the v1.0 device-CI-biometric-infra todo.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerUITests/LoadDetailFlowTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'XCTSkip' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `0` (all 5 methods are now real assertions)
    - Source: `grep -c 'test_compromisedVerdict_bannerAccessibilityLabelContainsReason\|test_singleFingerScroll_propagatesPastGraph' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `>= 2`
    - Source: `grep -c '"chain-integrity-banner"' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `>= 1` (the banner XCUITest)
    - Source: `grep -c 'press(forDuration.*thenDragTo' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `>= 1` (the outer-scroll-propagation test uses XCUICoordinate.press(forDuration:thenDragTo:) for single-finger drag inside graph)
    - File exists: `.planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md` (`ls` confirms; file size > 0)
    - Source: `grep -c '^## [0-9]\.\|^### [0-9]\.' .planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md` returns `>= 6` (one section per VALIDATION.md manual-verification row)
    - Source: `grep -c 'iPhone 17\|iPad Air' .planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md` returns `>= 6` (each entry names hardware)
    - Source: in `.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md`: `grep -c 'TBD' returns 0` (Per-Task Verification Map is fully filled in)
    - Source: `grep -c 'wave_0_complete: true\|nyquist_compliant: true\|status: complete' .planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md` returns `>= 3` (frontmatter updated)
    - Source: in `.planning/STATE.md`: `grep -c 'Executed — pending verify\|Phase: 9.*Plan: 09-10\|stopped_at:.*Phase 9' returns >= 1` (state update landed)
    - Behavior: `LoadDetailFlowTests` exits 0 with 5 passing methods
    - Behavior: VALIDATION.md per-task map has every requirement ID mapped to ≥1 Task ID (the 6 requirements: LOAD-05, LOAD-06, TRUST-01, TRUST-03, TRUST-04, TRUST-05)
  </acceptance_criteria>
  <done>
    All 5 XCUITest methods are real assertions; 09-MANUAL-TESTS.md ships with 6 device-test entries packaged from VALIDATION.md; VALIDATION.md Per-Task Verification Map is fully filled in with concrete Task IDs; VALIDATION.md frontmatter has `wave_0_complete: true` + `nyquist_compliant: true` + `status: complete`; STATE.md Phase 9 status is "Executed — pending verify".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XCUITest accessibility-label assertion against server-supplied banner reason | The banner-a11y test reads `app.otherElements["chain-integrity-banner"].label` — server-supplied reason text crosses through the UIKit accessibility API into the XCUITest assertion. Phase 7 D-18 LOCK enforced — the test asserts the SERVER-supplied content is reflected accurately in the VoiceOver-readable form. |
| Manual-test checklist → physical device run | The checklist directs a human reviewer through device-only checks; no automated runner. The risk is that a reviewer skips entries; mitigation is the pass/fail checkbox per device. |
| STATE.md update | The status transition "Executed — pending verify" signals downstream tooling (gsd:verify-work) that Phase 9 is ready for cross-AI review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | Client-side trust derivation in tests | mitigate | The banner-a11y XCUITest reads server-supplied `chain-integrity-banner.label` — never derives. The outer-scroll test exercises pure gesture behavior — never inspects trust state. |
| T-09-04 | Information Disclosure (PII) | XCUITest interactions + manual-test checklist | mitigate | XCUITests use synthetic fixture data only (VL-1009 from Phase 7 D-13 archetype). The manual-test checklist names synthetic party names + synthetic VL-#### IDs only. NO logger calls; NO test bundles capture customer data. |
| T-09-12 | Repudiation | Manual-test pass/fail without documented evidence | mitigate | The checklist provides explicit pass/fail checkboxes per entry per device; failures are filed under VERIFICATION.md by /gsd:verify-work (the next workflow step). The format is auditable. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies. |
</threat_model>

<verification>
1. Build green: `xcodebuild build` exits 0 (no new production source — verifies nothing regressed).
2. LoadDetailFlowTests green: `-only-testing:validationLedgerUITests/LoadDetailFlowTests` passes with 5 methods.
3. Manual-test file: `ls .planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md` returns 1 file; size > 0.
4. VALIDATION.md hygiene: `grep -c 'TBD' .planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md` returns 0; the frontmatter `nyquist_compliant: true` is set.
5. STATE.md hygiene: the Phase 9 entry shows "Executed — pending verify".
6. Phase 9 regression smoke (full Phase 9 test surface): `-only-testing:validationLedgerTests/Loads -only-testing:validationLedgerUITests/LoadDetailFlowTests` exits 0 across all 13 Wave 0 test files + LoadDetailFlowTests + LoadDetailFixtureContractTests.
</verification>

<success_criteria>
- All 5 LoadDetailFlowTests methods are real assertions (no XCTSkip).
- The 2 marquee verifications shipped: compromised-banner-a11y (TRUST-05 lock) + outer-page-scroll-propagation (RESEARCH §1 line 252 lock).
- 09-MANUAL-TESTS.md ships with 6 device-test entries (one per VALIDATION.md manual-verification row).
- VALIDATION.md Per-Task Verification Map is fully filled in (no TBD); frontmatter has wave_0_complete + nyquist_compliant + status complete.
- STATE.md Phase 9 status moves to "Executed — pending verify"; progress counters updated.
- Phase 9 is now ready for /gsd:verify-work — the next workflow step.
- The 6 Phase 9 requirement IDs (LOAD-05, LOAD-06, TRUST-01, TRUST-03, TRUST-04, TRUST-05) are each mapped to ≥1 passing automated test.
- Zero new SwiftPM dependencies.
- Zero new production source (this plan is documentation + cross-cutting tests + state update only).
</success_criteria>

<output>
Create `.planning/phases/09-load-detail-chain-of-trust-graph/09-10-SUMMARY.md` when done. Record:
- The 2 XCUITest methods populated (signatures + assertion fragments).
- The 09-MANUAL-TESTS.md table of contents (6 entries; per-entry hardware list).
- The VALIDATION.md Per-Task Verification Map snapshot (the full mapping from Task ID to plan + wave + requirement).
- The STATE.md transitions made.
- The Phase 9 cumulative artifact list: 8 new production files (PriorRelationship + 7 view layer files for Detail/) + 6 modified production files (ChainOfTrust, LoadListViewController, AppContainer, LoadDetailBodyView modifications across plans, LoadDetailViewController modifications across plans) + 14 new test files (Wave 0 shells from Plan 02 + LoadDetailFixtureContractTests fully implemented).
- Any open question — e.g. which specific reason-fragment from VL-1009 was chosen for the banner-a11y assertion; whether the navigateToLoadsTab helper was factored into a shared UITestHelpers.swift or kept inline.
- A reminder that `/gsd:verify-work 9` is the NEXT step (this plan does NOT run verify — that's a separate workflow).
</output>
