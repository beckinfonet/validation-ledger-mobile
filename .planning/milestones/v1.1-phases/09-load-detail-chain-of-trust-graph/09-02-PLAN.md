---
phase: 09-load-detail-chain-of-trust-graph
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/StatusTimelineViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/VerificationBasisSheetViewControllerSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift
  - validationLedgerTests/Loads/TrustNodeViewGestureTests.swift
  - validationLedgerTests/Loads/TrustGraphViewGestureTests.swift
  - validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift
  - validationLedgerTests/Loads/LoadDetailViewModelTests.swift
  - validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift
  - validationLedgerUITests/Loads/LoadDetailFlowTests.swift
autonomous: true
requirements:
  - LOAD-05
  - LOAD-06
  - TRUST-01
  - TRUST-03
  - TRUST-04
  - TRUST-05
threat_refs:
  - T-09-04
  - T-09-05

must_haves:
  truths:
    - "Every feature plan (03-09) has an existing test file to extend rather than create — Wave 0 ships the empty XCTestCase shells so per-task verification commands resolve from day one"
    - "LoadDetailFixtureContractTests is fully implemented in Wave 0 — it is the contract gate enforcing D-14 (every load-detail-VL-*.json has prior_relationships, no prior_relationship_count); Plan 01's fixture re-authoring MUST pass this gate"
    - "Snapshot test files import `UIKitSnapshot` (NOT a third-party snapshot library) per Phase 8 D-09 lock — no new SwiftPM dependencies"
    - "Snapshot artefacts use synthetic fixture data only — zero PII (UIKitSnapshot.swift lines 22-27)"
    - "Gesture-test files prepare the `singleTap.require(toFail: doubleTap)` + `minimumNumberOfTouches == 2` assertion harnesses (novel pattern per PATTERNS.md — first in-repo gesture test introspection)"
    - "Accessibility-test files prepare the `accessibilityElements` container-model harness (novel pattern per PATTERNS.md)"
    - "XCUITest file `LoadDetailFlowTests.swift` ships with the 5-role smoke flow scaffold + helper methods (driveFullOTPFlow reused from Phase 8); test methods are stub-shaped but assertions are filled in by Plan 10"
    - "All Phase 9 Wave 0 test files exist; subsequent feature plans add `@Test` / `func test_*` methods to these files without creating new files"
  artifacts:
    - path: "validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift"
      provides: "XCTest shell for the 12-fingerprint matrix (3 verdicts × 2 devices × 2 Dynamic Type sizes)"
      contains: "class TrustGraphViewSnapshotTests: XCTestCase"
    - path: "validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift"
      provides: "XCTest shell — per-verification-state node snapshots"
      contains: "class TrustNodeViewSnapshotTests: XCTestCase"
    - path: "validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift"
      provides: "XCTest shell — caution + compromised banner snapshots"
      contains: "class ChainIntegrityBannerViewSnapshotTests: XCTestCase"
    - path: "validationLedgerTests/Loads/Snapshot/StatusTimelineViewSnapshotTests.swift"
      provides: "XCTest shell — 6-state stepper snapshots"
      contains: "class StatusTimelineViewSnapshotTests: XCTestCase"
    - path: "validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.swift"
      provides: "XCTest shell — iPhone + iPad skeleton silhouettes + shimmer re-attach assertion"
      contains: "class LoadDetailSkeletonViewSnapshotTests: XCTestCase"
    - path: "validationLedgerTests/Loads/Snapshot/VerificationBasisSheetViewControllerSnapshotTests.swift"
      provides: "XCTest shell — 4-scenario sheet snapshots"
      contains: "class VerificationBasisSheetViewControllerSnapshotTests: XCTestCase"
    - path: "validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift"
      provides: "XCTest shell — 3-scenario sheet snapshots"
      contains: "class HandoffDetailSheetViewControllerSnapshotTests: XCTestCase"
    - path: "validationLedgerTests/Loads/TrustNodeViewGestureTests.swift"
      provides: "XCTest shell — singleTap.require(toFail: doubleTap) + recognizer introspection harness"
      contains: "class TrustNodeViewGestureTests: XCTestCase"
    - path: "validationLedgerTests/Loads/TrustGraphViewGestureTests.swift"
      provides: "XCTest shell — UIScrollView panGestureRecognizer.minimumNumberOfTouches + zoom-recenter math"
      contains: "class TrustGraphViewGestureTests: XCTestCase"
    - path: "validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift"
      provides: "XCTest shell — isAccessibilityElement == false + accessibilityElements container model"
      contains: "class TrustGraphViewAccessibilityTests: XCTestCase"
    - path: "validationLedgerTests/Loads/LoadDetailViewModelTests.swift"
      provides: "Swift Testing suite shell — .loading → .loaded → .error state machine"
      contains: "@Suite"
    - path: "validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift"
      provides: "FULL contract gate (NOT a stub) — D-14 enforcement: every load-detail-VL-*.json has prior_relationships; no prior_relationship_count remains"
      contains: "@Suite"
    - path: "validationLedgerUITests/Loads/LoadDetailFlowTests.swift"
      provides: "XCUITest shell — 5-role smoke flow + sheet-opens scaffold (reuses Phase 8 driveFullOTPFlow)"
      contains: "final class LoadDetailFlowTests: XCTestCase"
  key_links:
    - from: "validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift"
      to: "validationLedgerTests/Support/UIKitSnapshot.swift"
      via: "UIKitSnapshot.image(of:size:) + UIKitSnapshot.attach(_:name:to:)"
      pattern: "UIKitSnapshot\\.(image|attach)"
    - from: "validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift"
      to: "validationLedger/Core/Networking/APIClient.swift"
      via: "APIClient.defaultDecoder() consumed by every fixture decode"
      pattern: "APIClient\\.defaultDecoder"
---

<objective>
Ship the Wave 0 sampling baseline: every test file every subsequent Phase 9 plan needs MUST exist by the end of this plan. Empty-shell XCTestCases for the 7 snapshot suites + 3 gesture/accessibility suites + the LoadDetailViewModel Swift Testing suite + the XCUITest 5-role flow scaffold + the FULL `LoadDetailFixtureContractTests` (the only file that ships with real assertions in Wave 0 — it gates Plan 01's fixture re-authoring).

Per VALIDATION.md Sampling Rate: every task in plans 03–10 cites a scoped `-only-testing:` command targeting these files; without them, the per-task verification commands resolve to "no such test target" and the planner's Nyquist gate (no 3 consecutive tasks without an automated verify) fails. This plan satisfies the entire Wave 0 list in VALIDATION.md lines 67-81.

Purpose: separate test-infrastructure work from feature work. Plans 03–09 add `@Test` / `func test_*` methods to existing files; they never create new test files. This keeps each feature plan inside the ~50% context budget and makes file ownership unambiguous across waves.

Output: 13 new test files. One is fully implemented (the fixture contract gate); twelve are XCTestCase / @Suite shells with smoke-render or empty methods that compile and execute (exit 0 with zero tests run is acceptable for the shells; the contract test runs full assertions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md

@validationLedgerTests/Support/UIKitSnapshot.swift
@validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift
@validationLedgerTests/Loads/Snapshot/SkeletonLoadRowCellSnapshotTests.swift
@validationLedgerTests/Loads/LoadListViewModelTests.swift
@validationLedgerTests/Load/ChainOfTrustDecodeTests.swift
@validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift

<interfaces>
<!-- Wave 0 shells reference these types BEFORE the production code exists (Plans 03-09 create them).
     The shells import @testable validationLedger but the constructors they call are forward-declared
     references — the executor MUST add `#if false ... #endif` guards or `// FIXME(Plan-NN):` comments
     where the type doesn't yet exist, OR ship a minimal stub constructor in the production module to
     keep the shell compiling. The simpler path: ship MINIMAL stub types in Plan 03 (LoadDetailViewModel
     shell) and Plan 06 (TrustGraphView shell) and accept that the shells in this plan will be empty
     XCTestCases that contain ONLY a smoke `func test_smoke()` returning early. -->

Snapshot test template (from validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift):
```swift
import XCTest
@testable import validationLedger

class XSnapshotTests: XCTestCase {
    func test_smoke() throws {
        // Wave 0 shell. Real assertions added by Plan NN.
        throw XCTSkip("Wave 0 shell — populated by Plan NN")
    }
}
```

Swift Testing suite template (from validationLedgerTests/Loads/LoadListViewModelTests.swift):
```swift
import Testing
import Foundation
@testable import validationLedger

@Suite("LoadDetailViewModel — Phase 9 3-state machine", .serialized)
struct LoadDetailViewModelTests {
    @Test("smoke — Wave 0 shell")
    func smoke() { /* populated by Plan 03 */ }
}
```

XCUITest template (from validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift):
```swift
import XCTest

final class LoadDetailFlowTests: XCTestCase {
    override func setUp() {
        super.setUp()
        executionTimeAllowance = 30
        continueAfterFailure = false
    }
    func test_smoke_phase9_wave0() throws {
        throw XCTSkip("Wave 0 shell — populated by Plan 03 / Plan 10")
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 7 snapshot test shells + 3 gesture/accessibility test shells + LoadDetailViewModelTests shell + LoadDetailFlowTests XCUITest shell (12 files, all `XCTSkip`-stubbed)</name>
  <files>
    validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift,
    validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift,
    validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift,
    validationLedgerTests/Loads/Snapshot/StatusTimelineViewSnapshotTests.swift,
    validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.swift,
    validationLedgerTests/Loads/Snapshot/VerificationBasisSheetViewControllerSnapshotTests.swift,
    validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift,
    validationLedgerTests/Loads/TrustNodeViewGestureTests.swift,
    validationLedgerTests/Loads/TrustGraphViewGestureTests.swift,
    validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift,
    validationLedgerTests/Loads/LoadDetailViewModelTests.swift,
    validationLedgerUITests/Loads/LoadDetailFlowTests.swift
  </files>
  <read_first>
    - validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift (lines 37-122 — PATTERNS E8 snapshot test template; the per-state matrix shape Phase 9 shells mirror)
    - validationLedgerTests/Loads/Snapshot/SkeletonLoadRowCellSnapshotTests.swift (lines 24-112 — PATTERNS table row for `LoadDetailSkeletonViewSnapshotTests.swift`; shimmer re-attach assertion template at lines 83-102)
    - validationLedgerTests/Support/UIKitSnapshot.swift (lines 22-27 — the PII-zero discipline; lines 53-86 — the canonical `image(of:size:) + attach(_:name:to:)` recipe Phase 9 every snapshot shell will eventually call)
    - validationLedgerTests/Loads/LoadListViewModelTests.swift (PATTERNS E2 + table row for `LoadDetailViewModelTests` — the Swift Testing `@Suite(.serialized)` template + `StateRecorder` / `RecordingLogger` helper-class pattern)
    - validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift (PATTERNS E11 lines 415-441 — XCUITest 5-role flow template; `executionTimeAllowance = 30`, `continueAfterFailure = false`, `driveFullOTPFlow(_:)`)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md (lines 67-81 — the locked Wave 0 Requirements list; lines 42-58 — the Per-Task Verification Map's `-only-testing:` paths that MUST resolve)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (test-file table rows lines 33-47 — every shell's `Closest Analog` + `Key Pattern`; E8 / E9 / E10 / E11 / E2 excerpts)
  </read_first>
  <action>
    Create 12 test files. Every file MUST compile against the CURRENT module surface (Plan 03/06's production types do NOT yet exist) — therefore every test method body is `throw XCTSkip("Wave 0 shell — populated by Plan NN where N = the plan number that adds the production type")`. The XCTSkip path is the cleanest no-op that still terminates with exit code 0 and lets the test command resolve.

    File-by-file shell content:

    1. `validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift`:
       - `import XCTest` + `@testable import validationLedger`
       - `class TrustGraphViewSnapshotTests: XCTestCase`
       - One placeholder method per fingerprint in the 12-matrix per VALIDATION § Wave 0 (the eventual Plan 06 fills these in): `test_cleanVerdict_iPhonePortrait_dynamicTypeLarge_rendersExpectedFrame()`, `test_cleanVerdict_iPhonePortrait_dynamicTypeXXXLarge_rendersExpectedFrame()`, etc. — 12 method signatures, each body `throw XCTSkip("Wave 0 shell — populated by Plan 06")`.
       - File-header comment block citing "Phase 9 Wave 0 — VALIDATION.md line 68 + PATTERNS table row line 34"; reference UIKitSnapshot helper.

    2. `validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift`:
       - Same skeleton. Methods per UI-SPEC § snapshot test posture (4-6 representative states × roles): `test_verifiedCarrierNode_*`, `test_flaggedCarrierNode_compromised_*`, `test_pendingBrokerNode_*`, `test_unverifiedShipperNode_*` — 4 method shells, all XCTSkip. Populated by Plan 06.

    3. `validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift`:
       - Two method shells: `test_cautionVerdict_rendersYellowBanner()`, `test_compromisedVerdict_rendersRedBanner()`. Plus a `test_cleanVerdict_bannerIsHidden()` unit-test method shell (no snapshot, but lives in the file). All XCTSkip. Populated by Plan 09.

    4. `validationLedgerTests/Loads/Snapshot/StatusTimelineViewSnapshotTests.swift`:
       - Six method shells: `test_postedStepper_*`, `test_tenderedStepper_*`, `test_acceptedStepper_*`, `test_dispatchedStepper_*`, `test_inTransitStepper_*`, `test_deliveredStepper_*`. Plus `test_cancelledTerminalCard_*`. All XCTSkip. Populated by Plan 05.

    5. `validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.swift`:
       - Method shells: `test_iPhoneSilhouette_rendersExpectedFrame()`, `test_iPadSplitSilhouette_rendersExpectedFrame()`, AND `test_shimmerAnimationRestartsOnLayoutSubviews()` (Pitfall 1 lifecycle assertion — PATTERNS E9). All XCTSkip. Populated by Plan 04.

    6. `validationLedgerTests/Loads/Snapshot/VerificationBasisSheetViewControllerSnapshotTests.swift`:
       - Four method shells per RESEARCH §3 line 401 + PATTERNS table line 39: `test_cleanCarrier_*`, `test_cleanShipper_*`, `test_cautionImplicatedBroker_*`, `test_compromisedImplicatedCarrier_*`. All XCTSkip. Populated by Plan 07.

    7. `validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift`:
       - Three method shells: `test_cleanEdge_*`, `test_cautionImplicatedEdge_*`, `test_compromisedImplicatedEdge_*`. All XCTSkip. Populated by Plan 08.

    8. `validationLedgerTests/Loads/TrustNodeViewGestureTests.swift`:
       - Method shells: `test_singleTap_requiresDoubleTapFail()`, `test_doubleTap_doesNotOpenSheet()`, `test_singleTap_opensVerificationBasisSheetAfterDoubleTapFailWindow()`. All XCTSkip. Populated by Plan 06.

    9. `validationLedgerTests/Loads/TrustGraphViewGestureTests.swift`:
       - Method shells: `test_innerScroll_minimumNumberOfTouchesEqualsTwo()`, `test_doubleTapOnNode_animatesZoomToward1_8()`, `test_doubleTapOnEmptyCanvas_resetsZoomToOne()`, `test_voiceOverActive_disablesPinchZoom()`. All XCTSkip. Populated by Plan 06.

    10. `validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift`:
        - Method shells: `test_isAccessibilityElement_false()`, `test_accessibilityElements_orderedRoleThenEdges()`, `test_nodeAccessibilityLabel_followsComposedTemplate()`, `test_implicatedNode_appendsCompromiseSuffix()`, `test_voiceOver_disablesZoom()`, `test_reduceMotion_suspendsPulse()`. All XCTSkip. Populated by Plan 06.

    11. `validationLedgerTests/Loads/LoadDetailViewModelTests.swift`:
        - Swift Testing per PATTERNS table line 44.
        - `import Testing` + `import Foundation` + `@testable import validationLedger`.
        - `@Suite("LoadDetailViewModel — Phase 9 3-state machine + cancel-and-replace + zero-PII log", .serialized) struct LoadDetailViewModelTests { ... }`.
        - `@Test` method shells: `loadingToLoadedOnFixture()`, `loadingToErrorOnForcedFailure()`, `cancelAndReplaceOnRapidRefetch()`, `fetchLoadDetailLogsZeroPIIOnSuccessAndFailure()`. Each body just `// Wave 0 shell — populated by Plan 03`.

    12. `validationLedgerUITests/Loads/LoadDetailFlowTests.swift`:
        - `import XCTest` (NO `@testable` — UI tests target the product, not internal symbols).
        - `final class LoadDetailFlowTests: XCTestCase`. `override func setUp()` with `executionTimeAllowance = 30; continueAfterFailure = false`.
        - Method shells: `test_rowTap_pushesDetail()`, `test_nodeTap_opensVerificationBasisSheet()`, `test_edgeTap_opensHandoffSheet()`, `test_compromisedVerdict_bannerAccessibilityLabelContainsReason()`, `test_singleFingerScroll_propagatesPastGraph_toBodyScrollView()`. All `throw XCTSkip("Wave 0 shell — populated by Plan 10")`.

    Per T-09-04 (PII): every test file uses synthetic data only — Wave 0 shells contain no data at all. Per T-09-05 (no client-trust derivation): test files import `@testable validationLedger` but the methods are XCTSkip stubs; no derivation logic exists yet.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/Loads -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: every file in `<files>` above exists at the absolute path stated (verified by `ls`)
    - Source: `grep -l 'XCTSkip\|// Wave 0 shell' validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift validationLedgerTests/Loads/Snapshot/StatusTimelineViewSnapshotTests.swift validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.swift validationLedgerTests/Loads/Snapshot/VerificationBasisSheetViewControllerSnapshotTests.swift validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift validationLedgerTests/Loads/TrustNodeViewGestureTests.swift validationLedgerTests/Loads/TrustGraphViewGestureTests.swift validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift validationLedgerTests/Loads/LoadDetailViewModelTests.swift validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns 12 lines
    - Source: each snapshot shell `grep -c '@testable import validationLedger' <file>` returns `1` (every snapshot/gesture/a11y shell has @testable; XCUITest file does NOT)
    - Source: `grep -c 'import SnapshotTesting\|import snapshot_testing' validationLedgerTests/Loads/Snapshot/*.swift` returns `0` (negative gate — Phase 8 D-09 lock)
    - Source: `grep -c '@testable import validationLedger' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `0` (XCUITest does not use @testable)
    - Behavior: `xcodebuild test -only-testing:validationLedgerTests/Loads` exits 0 (every test either skips or passes — none fails)
    - Behavior: every file path in VALIDATION.md `Per-Task Verification Map` (lines 42-58) `-only-testing:` invocations resolves to an existing test target (no "no such test target" error from the test runner)
  </acceptance_criteria>
  <done>
    12 test-file shells exist and compile; running the scoped `Loads` test suite exits 0 with the shells reporting as `skipped` (XCTest's standard outcome for `throw XCTSkip`); every `-only-testing:` path in VALIDATION.md per-task map now resolves.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author LoadDetailFixtureContractTests.swift — the FULL D-14 contract gate (not a stub)</name>
  <files>validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift</files>
  <behavior>
    - The file is FULLY implemented in Wave 0 (not stubbed). It is the contract gate Plan 01's fixture re-authoring runs against in CI.
    - Test enumerates every `load-detail-VL-####.json` fixture in `validationLedgerTests/Networking/Fixtures/` (12 fixtures per Phase 7 D-13).
    - For each fixture:
      * Decode through `APIClient.defaultDecoder()` as `LoadDetailEndpoint.Response.self` — succeeds (Phase 7 contract gate still holds after Plan 01's mutation).
      * Assert raw JSON has ZERO occurrences of `prior_relationship_count` (the legacy key is fully removed).
      * Assert raw JSON has `prior_relationships` present on every node entry (matched by counting `"role":` occurrences + `"prior_relationships":` occurrences — equal counts).
      * Assert decoded `chain_of_trust.nodes` has at least one TrustNode whose `priorRelationships` is non-empty OR is the documented archetype exception (VL-1010 chameleon → flagged carrier carries `[]`).
    - Per D-14 archetype rules: VL-1010's flagged carrier has `priorRelationships.isEmpty == true`; VL-1009's flagged carrier has `priorRelationships.count <= 2`; VL-1011's factoring node has a curated pattern (count > 0).
    - Test uses Swift Testing (`@Suite(.serialized)` + `@Test`) per PATTERNS table row line 46.
  </behavior>
  <read_first>
    - validationLedgerTests/Load/ChainOfTrustDecodeTests.swift (PATTERNS E10 — per-fixture `@Test` template; fixture enumeration via `FixtureLoader.loadFixture(_:)`)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json (post-Plan-01 shape — `prior_relationships` arrays in place)
    - validationLedger/Core/Networking/APIClient.swift (`defaultDecoder()`)
    - validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift (the `Response` type the test decodes)
    - validationLedger/Core/Load/ChainOfTrust.swift (post-Plan-01 shape — `priorRelationships: [PriorRelationship]`)
    - validationLedger/Core/Load/PriorRelationship.swift (Plan 01 output)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-14 — fixture authoring contract)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md (line 56 — the contract assertion VL-#### filter)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (E10 template + table line 46)
  </read_first>
  <action>
    Create `validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift`. Full implementation, not a stub.

    Header:
    ```
    import Testing
    import Foundation
    @testable import validationLedger
    ```

    Suite declaration:
    ```
    @Suite("LoadDetailFixtureContractTests — Phase 9 D-14 contract gate", .serialized)
    struct LoadDetailFixtureContractTests { ... }
    ```

    Private helper struct or static let listing all 12 fixture IDs: `["VL-1001", ..., "VL-1012"]`.

    Tests (one per concern):

    - `@Test("Every load-detail-VL-####.json decodes through APIClient.defaultDecoder() with the new prior_relationships array shape")`:
      For each of the 12 fixture IDs, load via `FixtureLoader.loadFixture("load-detail-\(id)")`, decode `LoadDetailEndpoint.Response.self`, `#expect` decode succeeds (no throw).

    - `@Test("Legacy prior_relationship_count is absent from every fixture's raw JSON")`:
      For each fixture, load raw `Data`, render to `String(data:encoding: .utf8)`, `#expect(!rawJSON.contains("prior_relationship_count"))`. The negative-gate enforces D-14 / RESEARCH §7 line 865's global grep promise.

    - `@Test("Every TrustNode in every fixture has a prior_relationships array")`:
      For each fixture, decode, iterate `response.chainOfTrust.nodes`, `#expect` (`response.chainOfTrust.nodes.allSatisfy { _ = $0.priorRelationships; return true }`) — the type-system guarantees non-Optional `[PriorRelationship]`, but the test exists as a Nyquist gate proving the decoder ran on every node.

    - `@Test("Chameleon archetype: VL-1010's flagged carrier carries an empty priorRelationships array")`:
      Load VL-1010, decode, locate the carrier node where `verificationState == .flagged`, `#expect($0.priorRelationships.isEmpty)`. Per D-14 chameleon rule + RESEARCH §7 line 846.

    - `@Test("Double-broker archetype: VL-1009's flagged carrier carries 2 or fewer priorRelationships")`:
      Load VL-1009, decode, locate any flagged carrier node, `#expect($0.priorRelationships.count <= 2)`. Per D-14 double-broker rule.

    - `@Test("Factoring-fraud archetype: VL-1011's factoring node carries a curated non-empty pattern")`:
      Load VL-1011, decode, locate the factoring role node, `#expect($0.priorRelationships.count > 0)`. Per D-14 factoring-fraud rule (curated history).

    - `@Test("Clean baseline: VL-1001's carrier carries five or more priorRelationships")`:
      Load VL-1001, decode, locate carrier, `#expect($0.priorRelationships.count >= 5)`. Per D-14 clean-archetype rule.

    Per T-09-04 (PII): fixtures contain synthetic names only — Plan 01's task 3 acceptance criterion already gates this; this test is the runtime equivalent. Per T-09-05 (no client trust): the test only DECODES + READS server data; it never derives.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/LoadDetailFixtureContractTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '@Test' validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift` returns `>= 7`
    - Source: `grep -c '@Suite.*\.serialized' validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift` returns `1`
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift` returns `0` (negative gate — this file is NOT a stub)
    - Source: `grep -c 'prior_relationship_count' validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift` returns `1` (one occurrence — the assertion that gates against the legacy key)
    - Source: `grep -c 'priorRelationships' validationLedgerTests/Networking/Fixtures/LoadDetailFixtureContractTests.swift` returns `>= 4` (chameleon, double-broker, factoring, clean assertions)
    - Behavior: `LoadDetailFixtureContractTests` passes against the Plan 01 re-authored fixtures (exit 0)
    - Behavior: if Plan 01 ships fixture data not matching D-14 (e.g. a chameleon carrier with non-empty array), this test FAILS — closing the loop
  </acceptance_criteria>
  <done>
    LoadDetailFixtureContractTests.swift exists with full implementation (≥7 `@Test` methods); suite passes against the Plan 01 fixtures; gates the D-14 archetype rules; ZERO XCTSkip in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files (Wave 0 shells) → production module surface | Shells reference types that don't yet exist (TrustGraphView, LoadDetailViewModel, etc.). Mitigation: every method body is `XCTSkip` until Plan 03+ adds the production type; no symbol is named in an executable code path until production code lands. |
| Fixture contract test → fixture wire-format | `LoadDetailFixtureContractTests` is the gate that closes the Plan 01 fixture-authoring loop. Tampering with the test would let bad fixtures pass; the test is in source control and reviewed. |
| Snapshot tests → CI artefact bundle | Snapshot tests (when populated in Plans 04-09) attach `XCTAttachment` images to test bundles. Mitigation: PII-zero discipline (`UIKitSnapshot.swift` lines 22-27) — synthetic fixture data only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure (PII) | All Wave 0 test files + future snapshot artefacts | mitigate | Wave 0 shells contain no data; future feature plans MUST source views/fixtures from the synthetic Phase 7 corpus only (locked by `UIKitSnapshot.swift` line 22-27 file-header). Grep gate on test files for real-customer-name patterns returns zero. |
| T-09-05 | Tampering | Client-side trust derivation in test code | mitigate | Phase 7 D-18 LOCK extends to tests — tests assert DECODED server-supplied state; no test invokes a hypothetical `derive()` method. `LoadDetailFixtureContractTests` reads `node.priorRelationships` and `node.verificationState` verbatim; never derives. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies introduced; `import SnapshotTesting` gate in acceptance criteria (returns 0). |
</threat_model>

<verification>
1. Build green: `xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' build -parallel-testing-enabled NO` exits 0.
2. Wave 0 test compile + skip: `xcodebuild test -only-testing:validationLedgerTests/Loads -destination 'platform=iOS Simulator,name=iPhone 17'` exits 0 (every shell test reports `skipped`, the contract test reports `passed`).
3. Contract gate green: scoped `-only-testing:validationLedgerTests/LoadDetailFixtureContractTests` passes (exit 0) — proves Plan 01's fixture data is D-14-compliant.
4. Negative gate: `grep -c 'import SnapshotTesting' validationLedgerTests/Loads/Snapshot/*.swift` returns 0.
5. VALIDATION.md per-task verification map: every `-only-testing:` path in lines 42-58 now resolves to an existing test target.
</verification>

<success_criteria>
- 12 test-file shells exist; each compiles; each method is `XCTSkip`-stubbed with a "populated by Plan NN" reference.
- `LoadDetailFixtureContractTests.swift` is fully implemented (NOT a stub); ≥7 `@Test` methods; gates D-14.
- All scoped `Loads` and `LoadDetailFixtureContractTests` runs exit 0.
- Zero new SwiftPM dependencies (`Package.swift` byte-identical).
- VALIDATION.md per-task verification map resolves.
- Plan 03's first task can `xcodebuild test -only-testing:validationLedgerTests/LoadDetailViewModelTests` and get a real test run (the @Suite exists; the @Test methods are placeholders Plan 03 fills in).
</success_criteria>

<output>
Create `.planning/phases/09-load-detail-chain-of-trust-graph/09-02-SUMMARY.md` when done. Record:
- The 12 shell file paths + which downstream plan populates each (the `populated by Plan NN` map).
- LoadDetailFixtureContractTests `@Test` method list + which archetype each gates.
- The `XCTSkip` count by file (every shell file ≥ 1; contract test = 0).
- Any open question — e.g. if the project.pbxproj needs an explicit "Add Files" for the new test sources (PBXFileSystemSynchronizedRootGroup typically auto-discovers, but the new `Loads/Snapshot/` subfolder may need verification on the simulator-lane CI).
</output>
