---
phase: 09-load-detail-chain-of-trust-graph
plan: 08
type: execute
wave: 5
depends_on:
  - 09-01
  - 09-06
files_modified:
  - validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift
  - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
  - validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift
  - validationLedgerUITests/Loads/LoadDetailFlowTests.swift
autonomous: true
requirements:
  - TRUST-04
threat_refs:
  - T-09-03
  - T-09-04
  - T-09-10

must_haves:
  truths:
    - "TRUST-04: Tapping a graph edge shows that handoff/tender detail"
    - "D-07: TRUST-04 ships at FULL quality in Phase 9 — the ROADMAP-cited scope-trim option is EXPLICITLY REJECTED per CONTEXT D-07"
    - "D-08: Same UISheetPresentationController infrastructure as TRUST-03 sheet (Plan 07) — `[.medium(), .large()]` detents, `selectedDetentIdentifier = .medium`, `largestUndimmedDetentIdentifier = .medium`, `prefersGrabberVisible = true`"
    - "Sheet content (UI-SPEC § HandoffDetailSheetViewController lines 459-471): header (Handoff + fromRole → toRole + display names) + relationship-state row (reuses VerificationBadgeView for the edge.relationshipState) + tender-reference row (present OR `No formal tender on file.` informational line) + 'Why this handoff is flagged' conditional block (rendered ONLY when edgeID ∈ implicatedEdgeIDs)"
    - "Smaller surface than TRUST-03 sheet (UI-SPEC line 470 — handoff sheet is naturally less dense; `.medium` is the natural resting detent)"
    - "Server-supplied edge.relationshipState, edge.tenderRef, chainIntegrity.reason all rendered verbatim; client never derives any of these (Phase 7 D-18 LOCK; locked by source-assertion grep gate)"
    - "Plan 06's `presentHandoffDetailSheet(for:)` fatalError stub is replaced with full presentation logic"
    - "Zero PII — no logger calls; tender reference + party names rendered verbatim from server data"
    - "All user-facing strings via NSLocalizedString (UI-SPEC § Copywriting Handoff detail sheet lines 723-733)"
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift"
      provides: "TRUST-04 sheet content VC — header + relationship-state row + tender-reference row + implicated block"
      contains: "public final class HandoffDetailSheetViewController: UIViewController"
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      provides: "presentHandoffDetailSheet(for:) is fully implemented — same sheet detent recipe as Plan 07 (largestUndimmedDetentIdentifier = .medium)"
      contains: "HandoffDetailSheetViewController("
  key_links:
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      to: "validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift"
      via: "present(_:animated:) with sheetPresentationController detents [.medium(), .large()]"
      pattern: "HandoffDetailSheetViewController\\("
    - from: "validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift"
      to: "validationLedger/UI/Components/VerificationBadgeView.swift"
      via: "relationship-state row reuses the 4-state pill"
      pattern: "VerificationBadgeView"
---

<objective>
Ship TRUST-04: the handoff-detail modal sheet that opens when the user taps a graph edge (the invisible companion view per Plan 06's edge tap-target). Full quality per D-07 (the ROADMAP scope-trim option was EXPLICITLY REJECTED in CONTEXT D-07). Same UISheetPresentationController infrastructure as Plan 07 — shared presentation recipe, different content.

Purpose: TRUST-04 acceptance criterion. The graph in Plan 06 fires `trustGraphView.edgeTapped = { [weak self] edgeID in self?.presentHandoffDetailSheet(for: edgeID) }`; this plan implements the sheet content + presentation infrastructure.

Output: 1 new sheet content VC + LoadDetailViewController.presentHandoffDetailSheet wired (replaces Plan 06's fatalError stub) + 3 snapshot test methods (clean / caution-implicated / compromised-implicated) + the `LoadDetailFlowTests.test_edgeTap_opensHandoffSheet` XCUITest method body filled in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-06-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-07-PLAN.md

@validationLedger/Core/Load/ChainOfTrust.swift
@validationLedger/Core/Load/ChainIntegrity.swift
@validationLedger/UI/Components/VerificationBadgeView.swift
@validationLedger/UI/DesignSystem/Spacing.swift
@validationLedger/UI/DesignSystem/Colors.swift
@validationLedger/UI/DesignSystem/Typography.swift
@validationLedger/Features/Loads/Detail/VerificationBasisSheetViewController.swift

<interfaces>
<!-- The Plan 06 callback this plan implements. -->
From Plan 06 LoadDetailViewController:
- `trustGraphView.edgeTapped = { [weak self] edgeID in self?.presentHandoffDetailSheet(for: edgeID) }`
- `private func presentHandoffDetailSheet(for edgeID: String) { fatalError("Plan 08 wires the sheet presentation") }` — this plan replaces the body.

<!-- The shared sheet presentation recipe (same as Plan 07; reused infrastructure per UI-SPEC § Sheet presentation invariant lines 472-477). -->

<!-- TrustEdge surface (Phase 7, post-Plan-01 unchanged): -->
- `edgeID: String`, `fromPartyID: String`, `toPartyID: String`, `relationshipState: VerificationState`, `tenderRef: String?`

<!-- Sheet content (UI-SPEC § Handoff detail sheet content lines 463-468): -->
1. Header — "Handoff" title + "{fromRole.displayName} → {toRole.displayName}" subtitle (e.g. "Broker → Carrier"). Each role's display name beneath as `.body` line ("Acme Brokerage → Reliable Carriers Inc.").
2. Relationship-state row — VerificationBadgeView(state: edge.relationshipState) + "Relationship: {STATE}" label (uppercase: VERIFIED / PENDING / UNVERIFIED / FLAGGED).
3. Tender-reference row:
   - `edge.tenderRef != nil` → "Tender reference: **{tenderRef}**" (.body + .headline weight on the ref).
   - `edge.tenderRef == nil` → "No formal tender on file." (.body, informational — not alarming per UI-SPEC line 731).
4. "Why this handoff is flagged" implicated block — rendered ONLY when `edge.edgeID ∈ chainIntegrity.implicatedEdgeIDs`; same shape as TRUST-03's implicated block (D-11): tinted card + verdict icon + chainIntegrity.reason verbatim.

<!-- Locked accessibility identifiers (UI-SPEC lines 782-784): -->
- `"load-detail.handoff-detail-sheet"` (root)
- `"load-detail.handoff-detail-sheet.edge.{edgeID}"` (additional partition identifier)
- `"load-detail.handoff-detail-sheet.implicated-block"` (when rendered)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create HandoffDetailSheetViewController + populate snapshot tests (3 scenarios) + the XCUITest method body for test_edgeTap_opensHandoffSheet</name>
  <files>
    validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift,
    validationLedger/Features/Loads/Detail/LoadDetailViewController.swift,
    validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift,
    validationLedgerUITests/Loads/LoadDetailFlowTests.swift
  </files>
  <behavior>
    - `HandoffDetailSheetViewController: UIViewController`:
      * `public init(edge: TrustEdge, fromNode: TrustNode, toNode: TrustNode, integrity: ChainIntegrity)`.
      * View hierarchy: `UIScrollView` + vertical `UIStackView` (mirrors Plan 07's posture but smaller — UI-SPEC line 470 "less dense").
      * `accessibilityIdentifier = "load-detail.handoff-detail-sheet"`.
      * Edge-partition identifier additionally set for XCUITest assertion: `accessibilityIdentifier = "load-detail.handoff-detail-sheet.edge.\(edge.edgeID)"` on a wrapper element (planner picks how: either the root view OR a sibling label that XCUITest queries — UI-SPEC line 783 says "set additionally so XCUITest can assert which edge's sheet opened"; the cleanest path is to set it on the header section's container view).
      * Content (top to bottom):
        1. **Header**: vertical stack — title label "Handoff" (DS.Typography.headline) + subtitle label "{fromRole.displayName} → {toRole.displayName}" (DS.Typography.body) + a `.body` line beneath listing both display names "{fromNode.displayName} → {toNode.displayName}".
        2. **Relationship-state row**: horizontal stack — `VerificationBadgeView` configured with `edge.relationshipState` + label "Relationship: {state.uppercaseLabel}" (matches Phase 8 badge labels).
        3. **Tender-reference row**: horizontal stack — label rendering "Tender reference: {tenderRef}" with the tenderRef portion in `.headline` weight (via attributed string); OR (when tenderRef is nil) a single label rendering "No formal tender on file." per UI-SPEC line 731 (informational, not alarming — no special icon).
        4. **Implicated block**: same shape as Plan 07's implicated block — rounded card with 0.15 tint (caution yellow / compromised red), title "Why this handoff is flagged" (NSLocalizedString), body `integrity.reason` verbatim, icon (triangle/octagon). Renders ONLY when `integrity.implicatedEdgeIDs.contains(edge.edgeID)`.
      * Each row composed as a horizontal stack with `DS.Spacing.sm` interior spacing + `DS.Spacing.md` leading/trailing inset.
      * Section spacing: `DS.Spacing.lg`.
    - `LoadDetailViewController` modification — implement `presentHandoffDetailSheet(for edgeID: String)`:
      * Look up the edge by `edgeID` from `cachedChainOfTrust?.edges`. If not found, no-op.
      * Look up `fromNode` and `toNode` from `cachedChainOfTrust?.nodes` by `edge.fromPartyID` / `edge.toPartyID`. If either missing, no-op (defensive).
      * Build `HandoffDetailSheetViewController(edge:, fromNode:, toNode:, integrity:)`.
      * Configure `sheetPresentationController` IDENTICALLY to Plan 07's recipe (the sheet infrastructure is shared per D-08 / UI-SPEC line 472):
        - `detents = [.medium(), .large()]`
        - `selectedDetentIdentifier = .medium`
        - `prefersGrabberVisible = true`
        - `largestUndimmedDetentIdentifier = .medium`
        - `prefersScrollingExpandsWhenScrolledToEdge = false`
        - `prefersEdgeAttachedInCompactHeight = true`
        - `widthFollowsPreferredContentSizeWhenEdgeAttached = true`
      * Call `present(sheetVC, animated: true)`.
    - Snapshot tests (3 scenarios per RESEARCH §3 line 402):
      * `test_cleanEdge_rendersBasicRows()`: synthetic edge between two clean carriers; `relationshipState = .verified`; no implicated block; assert content stack has 3 sections (header + relationship-state + tender-ref).
      * `test_cautionImplicatedEdge_rendersImplicatedBlock()`: synthetic edge in a caution-archetype chain; `edgeID ∈ implicatedEdgeIDs`; verdict `.caution`; assert implicated block exists with yellow 0.15 tint.
      * `test_compromisedImplicatedEdge_rendersRedTintImplicatedBlock()`: synthetic edge in VL-1009 double-broker archetype; `verdict = .compromised`, `edgeID ∈ implicatedEdgeIDs`; assert implicated block exists with red 0.15 tint AND server-supplied reason text is rendered verbatim.
      * Optionally `test_nullTenderRef_rendersInformationalLine()` if needed for coverage (UI-SPEC line 731): synthetic edge with `tenderRef = nil`; assert the "No formal tender on file." line is rendered as `.body` text (not alarming icon).
    - `LoadDetailFlowTests.test_edgeTap_opensHandoffSheet()`:
      * Launch as broker; drive OTP; tap VL-1009 load row (compromised archetype — guaranteed flagged edge exists); wait for `load-detail`.
      * Tap an edge invisible-companion view via its accessibility identifier (e.g. `app.otherElements["load-detail.trust-graph.edge.\(edgeID)"]` — the planner picks a deterministic edgeID from VL-1009's fixture).
      * Assert `app.otherElements["load-detail.handoff-detail-sheet"].waitForExistence(timeout: 3) == true`.
      * For the compromised-archetype: assert `app.otherElements["load-detail.handoff-detail-sheet.implicated-block"].exists == true`.
  </behavior>
  <read_first>
    - validationLedger/Features/Loads/Detail/VerificationBasisSheetViewController.swift (Plan 07 output — the shared sheet-content posture and accessibility-identifier discipline; SAME presentation infrastructure)
    - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift (Plan 06 + 07 — the `cachedChainOfTrust` storage; the `presentHandoffDetailSheet(for:)` fatalError stub site)
    - validationLedger/Core/Load/ChainOfTrust.swift (TrustEdge + TrustNode surface — verify field names: `edgeID`, `fromPartyID`, `toPartyID`, `relationshipState`, `tenderRef`)
    - validationLedger/Core/Load/VerificationState.swift (the relationshipState enum + display labels)
    - validationLedger/UI/Components/VerificationBadgeView.swift (the reused 4-state badge)
    - validationLedger/UI/DesignSystem/Colors.swift / Spacing.swift / Typography.swift
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-07 ship full quality — scope-trim explicitly rejected; D-08 shared sheet infra)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (§ HandoffDetailSheetViewController lines 459-471; § Sheet presentation invariant lines 472-477; § Copywriting Handoff detail sheet lines 723-733; § Accessibility identifiers lines 782-784)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§5 — shared sheet recipe; §3 line 402 — 3-snapshot count for handoff sheet)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (table row 24 HandoffDetailSheetViewController — "shared with TRUST-03 sheet")
  </read_first>
  <action>
    A. CREATE `validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift`. Programmatic UIKit. Mirror Plan 07's VerificationBasisSheetViewController shape with smaller content.

    Header: cite "Phase 9 TRUST-04 / D-07 / D-08; mirrors VerificationBasisSheetViewController file shape with smaller content (UI-SPEC line 470 — naturally less dense)."

    Structure:
    - `import UIKit`.
    - `public final class HandoffDetailSheetViewController: UIViewController`.
    - Stored:
      * `private let edge: TrustEdge`.
      * `private let fromNode: TrustNode`.
      * `private let toNode: TrustNode`.
      * `private let integrity: ChainIntegrity`.
      * `private let scrollView: UIScrollView`.
      * `private let contentStack: UIStackView` — vertical, `DS.Spacing.lg` spacing.
    - `public init(edge: TrustEdge, fromNode: TrustNode, toNode: TrustNode, integrity: ChainIntegrity)`.
    - `required init?(coder:) { fatalError(...) }`.
    - `public override func viewDidLoad()`: super; backgroundColor; accessibilityIdentifier; `composeContent()`.
    - `private func composeContent()`:
      * Add the 3-4 sections in order. The header section's container view receives the edge-partition accessibilityIdentifier `"load-detail.handoff-detail-sheet.edge.\(edge.edgeID)"` (or set it on the root view alongside the main identifier — XCUITest queries either).
      1. `headerView = makeHeader(edge:, fromNode:, toNode:)`
      2. `relationshipStateRow = makeRelationshipStateRow(state: edge.relationshipState)`
      3. `tenderReferenceRow = makeTenderReferenceRow(tenderRef: edge.tenderRef)`
      4. If `integrity.implicatedEdgeIDs.contains(edge.edgeID)`: `implicatedBlock = makeImplicatedBlock(integrity:)` per UI-SPEC line 468.
    - `private func makeHeader(edge:, fromNode:, toNode:) -> UIView`:
      * Vertical stack:
        - Title "Handoff" (`DS.Typography.headline`, NSLocalizedString).
        - Subtitle: "{fromRole.displayName} → {toRole.displayName}" (e.g. "Broker → Carrier"), `DS.Typography.body`.
        - Display names line: "{fromNode.displayName} → {toNode.displayName}", `DS.Typography.body`, `DS.Colors.labelSecondary`.
    - `private func makeRelationshipStateRow(state: VerificationState) -> UIView`:
      * Horizontal stack:
        - `VerificationBadgeView` configured with `state`.
        - Label "Relationship: {STATE}" — STATE = state's uppercase label (matches Phase 8 4-state pill labels: VERIFIED / PENDING / UNVERIFIED / FLAGGED). NSLocalizedString-wrapped per state.
    - `private func makeTenderReferenceRow(tenderRef: String?) -> UIView`:
      * If non-nil: build an `NSAttributedString` "Tender reference: " + `{ref}` (the ref in `.headline` weight). UI-SPEC line 730 spec.
      * If nil: render `NSLocalizedString("loads.detail.handoff.no_tender", value: "No formal tender on file.", comment: ...)` as a plain `.body` line (no special icon — UI-SPEC line 731 "informational rather than alarming").
    - `private func makeImplicatedBlock(integrity:) -> UIView`:
      * Same shape as Plan 07's implicated block — rounded card, soft 0.15 tint (caution → DS.Colors.caution.withAlphaComponent(0.15); compromised → DS.Colors.destructive.withAlphaComponent(0.15)), title "Why this handoff is flagged" (NSLocalizedString), body `integrity.reason ?? ""` verbatim, icon triangle/octagon.
      * `accessibilityIdentifier = "load-detail.handoff-detail-sheet.implicated-block"`.

    No logger; per T-09-04: tenderRef + party names are server-supplied; rendered verbatim; not logged.

    B. MODIFY `validationLedger/Features/Loads/Detail/LoadDetailViewController.swift`:

    Replace the `fatalError(...)` body of `presentHandoffDetailSheet(for edgeID: String)` with:
    ```
    guard let chainOfTrust = cachedChainOfTrust,
          let edge = chainOfTrust.edges.first(where: { $0.edgeID == edgeID }),
          let fromNode = chainOfTrust.nodes.first(where: { $0.partyID == edge.fromPartyID }),
          let toNode = chainOfTrust.nodes.first(where: { $0.partyID == edge.toPartyID }) else {
        return  // defensive — edge or endpoints disappeared between configure and tap
    }
    let sheetVC = HandoffDetailSheetViewController(edge: edge, fromNode: fromNode, toNode: toNode, integrity: chainOfTrust.integrity)
    sheetVC.modalPresentationStyle = .pageSheet
    if let sheet = sheetVC.sheetPresentationController {
        sheet.detents = [.medium(), .large()]
        sheet.selectedDetentIdentifier = .medium
        sheet.prefersGrabberVisible = true
        sheet.largestUndimmedDetentIdentifier = .medium  // D-08 + RESEARCH §5 line 519 — same lock as Plan 07
        sheet.prefersScrollingExpandsWhenScrolledToEdge = false
        sheet.prefersEdgeAttachedInCompactHeight = true
        sheet.widthFollowsPreferredContentSizeWhenEdgeAttached = true
    }
    present(sheetVC, animated: true)
    ```

    Per T-09-03: pure equality lookups on server-supplied IDs; no derivation.

    C. POPULATE `validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift` (Plan 02 shells). Replace the 3 XCTSkip stubs:

    For each scenario, synthesize a `TrustEdge + TrustNode pair + ChainIntegrity`. Configure the VC. Render at `CGSize(width: 393, height: 350)` (handoff sheet is smaller per UI-SPEC line 470). UIKitSnapshot.

    - `test_cleanEdge_rendersBasicRows()`: synthetic clean edge (`verdict = .clean`, `edgeID ∉ implicated`); assert no subview has `accessibilityIdentifier == "load-detail.handoff-detail-sheet.implicated-block"`.
    - `test_cautionImplicatedEdge_rendersImplicatedBlock()`: caution verdict + implicated; assert implicated block exists with caution-tint background.
    - `test_compromisedImplicatedEdge_rendersRedTintImplicatedBlock()`: compromised verdict + implicated; assert implicated block exists with compromised-tint background AND its body label text matches `integrity.reason` verbatim.

    D. POPULATE `validationLedgerUITests/Loads/LoadDetailFlowTests.swift` — implement `test_edgeTap_opensHandoffSheet()`:

    - Launch as broker; driveFullOTPFlow; navigate to Loads; tap VL-1009 (compromised archetype).
    - Wait for `load-detail`.
    - Find an edge by identifier — pick a deterministic edgeID from the VL-1009 fixture's `chain_of_trust.edges` (the planner reads the fixture to identify e.g. `"edge-broker-carrier-001"`).
    - `app.otherElements["load-detail.trust-graph.edge.\(edgeID)"].tap()`.
    - Assert `app.otherElements["load-detail.handoff-detail-sheet"].waitForExistence(timeout: 3)`.
    - For compromised: assert `app.otherElements["load-detail.handoff-detail-sheet.implicated-block"].exists == true`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/HandoffDetailSheetViewControllerSnapshotTests -only-testing:validationLedgerUITests/LoadDetailFlowTests/test_edgeTap_opensHandoffSheet -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public final class HandoffDetailSheetViewController: UIViewController' validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift` returns `1`
    - Source: `grep -c 'public init(edge: TrustEdge' validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift` returns `1`
    - Source: `grep -c 'accessibilityIdentifier = "load-detail.handoff-detail-sheet"' validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift` returns `>= 1`
    - Source: `grep -c 'VerificationBadgeView' validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift` returns `>= 1` (relationship-state row reuses the badge)
    - Source: `grep -c 'NSLocalizedString' validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift` returns `>= 5` (header title, "No formal tender on file.", implicated title, relationship-state labels, tender-reference label)
    - Source: `grep -v '^//' validationLedger/Features/Loads/Detail/HandoffDetailSheetViewController.swift | grep -cE 'Logger|os_log'` returns `0`
    - Source: in LoadDetailViewController.swift: `grep -c 'HandoffDetailSheetViewController(edge:' returns 1` AND `grep -c 'fatalError("Plan 08' returns 0` (Plan 06's stub is replaced)
    - Source: `grep -c 'sheet.largestUndimmedDetentIdentifier = .medium' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `2` (Plan 07 added 1; Plan 08 adds the second — both sheet sites have the Pitfall 7 lock)
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/Snapshot/HandoffDetailSheetViewControllerSnapshotTests.swift` returns `0`
    - Source: `grep -c 'test_edgeTap_opensHandoffSheet' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `>= 1`
    - Source: `grep -c 'XCTSkip' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `2` (after this plan: 2 remain skipped — banner-a11y and outer-scroll, both Plan 10)
    - Behavior: HandoffDetailSheetViewControllerSnapshotTests passes (3 scenarios); test_edgeTap_opensHandoffSheet passes
  </acceptance_criteria>
  <done>
    HandoffDetailSheetViewController exists with the 4-section content per UI-SPEC; LoadDetailViewController.presentHandoffDetailSheet is fully wired (sheet detents per D-08; largestUndimmedDetentIdentifier locked); 3 snapshot tests pass; XCUITest validates the edge-tap → handoff sheet flow on the compromised archetype.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `TrustEdge` (decoded) → handoff sheet rendering | `relationshipState`, `tenderRef`, `fromPartyID`, `toPartyID` cross from the typed Decodable surface into the view. Phase 7 D-18 LOCK: rendered verbatim. |
| `ChainIntegrity.reason` + `implicatedEdgeIDs` → implicated block | Server-supplied data drives the conditional block + its content. Same verbatim-rendering invariant as Plan 07 sheet. |
| Sheet presentation infrastructure | Same UISheetPresentationController recipe as Plan 07 — `largestUndimmedDetentIdentifier = .medium` is the shared Pitfall 7 lock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | Client-side trust derivation in handoff sheet | mitigate | Every visual element (relationshipState badge color, tender-ref text, implicated-or-not, implicated reason text) reads directly from server-supplied TrustEdge / ChainIntegrity fields. Lookup `chainOfTrust.edges.first(where: edgeID)` is pure equality. No derive*() methods. Source-assertion grep gate. |
| T-09-04 | Information Disclosure (PII) | Sheet content + tender-ref render | mitigate | tenderRef + party display names are server-supplied (synthetic in Phase 7 fixture corpus per T-09-02). NO logger calls (T-08-08 mirror). Snapshot tests use the same fixture corpus. |
| T-09-10 | Tampering | Sheet detent configuration | mitigate | The same Pitfall 7 lock applies to this sheet — `largestUndimmedDetentIdentifier = .medium` is set at the source level. Both Plan 07 and Plan 08 sheets are presented from LoadDetailViewController with identical sheet configuration; the grep gate verifies BOTH presentations have the property set. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies. |
</threat_model>

<verification>
1. Build green: `xcodebuild build` exits 0.
2. Handoff sheet snapshot suite green: `-only-testing:validationLedgerTests/HandoffDetailSheetViewControllerSnapshotTests` passes (3 scenarios).
3. TRUST-04 XCUITest green: `-only-testing:validationLedgerUITests/LoadDetailFlowTests/test_edgeTap_opensHandoffSheet` passes; the sheet identifier resolves; on compromised archetype the implicated block exists.
4. Pitfall 7 source-level gate: `grep -c 'sheet.largestUndimmedDetentIdentifier = .medium' LoadDetailViewController.swift == 2` (both Plan 07 + Plan 08 sites have the lock).
5. Phase 9 regression smoke: Plan 07's test_nodeTap_opensVerificationBasisSheet still passes; Plan 03's test_rowTap_pushesDetail still passes.
6. Zero-PII gate: `grep -v '^//' HandoffDetailSheetViewController.swift | grep -cE 'Logger|os_log'` returns 0.
</verification>

<success_criteria>
- HandoffDetailSheetViewController exists with 4-section content (header + relationship-state + tender-ref + implicated-conditional).
- LoadDetailViewController.presentHandoffDetailSheet is fully implemented; sheet detents per D-08; Pitfall 7 locked at source level.
- 3 snapshot tests cover the locked scenarios (clean / caution-implicated / compromised-implicated).
- XCUITest test_edgeTap_opensHandoffSheet passes.
- "No formal tender on file." informational line renders per UI-SPEC line 731 (not alarming, no icon).
- Implicated block ONLY when edgeID ∈ implicatedEdgeIDs; reads chainIntegrity.reason verbatim.
- Zero new SwiftPM dependencies.
- Zero logger calls in the sheet VC.
- Both sheet sites (Plan 07 + Plan 08) have the largestUndimmedDetentIdentifier lock at the source level.
</success_criteria>

<output>
Create `.planning/phases/09-load-detail-chain-of-trust-graph/09-08-SUMMARY.md` when done. Record:
- HandoffDetailSheetViewController surface (init signature + the 4 section helpers).
- Plan 06's fatalError-stub replacement.
- The 3 snapshot test scenarios + the locked accessibility identifiers each gates.
- The XCUITest assertion fragment (fixture + edgeID tapped).
- Confirmation that BOTH sheet sites now have `largestUndimmedDetentIdentifier = .medium` locked (`grep -c` returns 2).
- Any open question — e.g. whether the edge-partition accessibility identifier (`load-detail.handoff-detail-sheet.edge.{edgeID}`) lives on the root view or on a sibling header element (UI-SPEC line 783 leaves the placement open); whether NSAttributedString for the tender-reference row is the best Dynamic Type strategy or whether two labels in an HStack is simpler.
</output>
