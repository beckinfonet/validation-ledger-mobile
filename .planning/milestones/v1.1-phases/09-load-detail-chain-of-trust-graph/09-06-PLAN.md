---
phase: 09-load-detail-chain-of-trust-graph
plan: 06
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-03
files_modified:
  - validationLedger/Features/Loads/Detail/TrustNodeView.swift
  - validationLedger/Features/Loads/Detail/TrustGraphView.swift
  - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
  - validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift
  - validationLedgerTests/Loads/TrustNodeViewGestureTests.swift
  - validationLedgerTests/Loads/TrustGraphViewGestureTests.swift
  - validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift
autonomous: true
requirements:
  - TRUST-01
  - TRUST-05
threat_refs:
  - T-09-03
  - T-09-04
  - T-09-08
  - T-09-09

must_haves:
  truths:
    - "TRUST-01: Load detail renders an interactive chain-of-trust graph — the load's parties as a directed, pannable/zoomable node-graph, each node carrying a verification badge"
    - "TRUST-05: Flagged nodes/edges and the chain-level integrity verdict are rendered distinctly from fixture-supplied data — the client never computes trust or integrity"
    - "D-04 gesture choreography: graphScrollView.panGestureRecognizer.minimumNumberOfTouches = 2 (outer page scroll preserved); singleTap.require(toFail: doubleTap) on every TrustNodeView; double-tap-on-node zooms to that node at 1.8x ~250ms ease-in-out; double-tap-on-empty-canvas resets to fit-all-nodes-tight"
    - "D-05: Fit-all-nodes-tight default zoom; computed in TrustGraphView.layoutSubviews(); scrollView.zoomScale = 1.0"
    - "D-06: Fixed role slots in normalized (0..1, 0..1) coordinates per UI-SPEC § Default zoom + role slots table; iPhone-tall coordinates AND iPad-wide coordinates; missing roles simply absent; edges drawn only between present nodes"
    - "D-15 tiered visual response: .clean → no halo + solid neutral edges; .caution → yellow halo (static, no pulse) + yellow dashed edges + dim-others 0.5 opacity; .compromised → red halo + animated pulse (opacity 0.6→1.0, 1.2s, easeInEaseOut, infinite repeat) + red dashed edges + dim-others 0.5 opacity"
    - "Pulse-only-on-compromised invariant: the pulse animation is added ONLY in the .compromised codepath; caution halos do NOT pulse"
    - "D-22 accessibility container model: TrustGraphView.isAccessibilityElement = false; accessibilityElements = orderedNodes + orderedEdges (nodes in fixed role order shipper/broker/carrier/dispatch/factoring; edges in (fromPartyID, toPartyID) lex order); each child .accessibilityTraits = .button; per-node composed accessibilityLabel ending with implicated-suffix when partyID ∈ implicatedNodeIDs"
    - "VoiceOver-active branch: pinch-zoom suspended (minimumZoomScale = maximumZoomScale = 1.0); double-tap recognizers disabled; observer on UIAccessibility.didChangeNotification re-enables when VoiceOver turns off"
    - "Reduce-motion branch: compromised halo renders solid (no pulse animation); color (red) still signals 'compromised', motion does not (UI-SPEC line 866)"
    - "Halo + edge CAShapeLayer + connector layer re-attach in layoutSubviews AND traitCollectionDidChange (Pitfall 1 / PATTERNS E3)"
    - "Edge tap-target: each edge has an invisible 28pt-band UIView companion overlaid on the path (44pt diagonal at every realistic edge angle); the companion is the recognizer's host, the CAShapeLayer is layer-only chrome (TRUST-04 wiring is in Plan 08; this plan ships the companion view shape)"
    - "Client never derives trust state — every halo color, every edge dash, every dim opacity comes from chainIntegrity.verdict / implicatedNodeIDs / implicatedEdgeIDs verbatim (Phase 7 D-18 LOCK; locked by source-assertion grep gate)"
    - "Zero PII — no logger calls in any new view file; snapshot artefacts use Phase 7 fixture corpus only"
    - "No new SwiftPM dependencies; halos + edges + pulse via CAShapeLayer + CABasicAnimation (in-tree)"
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/TrustNodeView.swift"
      provides: "Single TrustNodeView per node; chrome + VerificationBadgeView + role label + display-name label; tap recognizers (single + double, require(toFail:))"
      contains: "public final class TrustNodeView: UIView"
    - path: "validationLedger/Features/Loads/Detail/TrustGraphView.swift"
      provides: "UIScrollView-hosted canvas; 5 fixed role-slot positions; CAShapeLayer edges; halo layers; pulse animation; accessibility container; size-class branch for iPhone-tall vs iPad-wide role-slot coordinates"
      contains: "public final class TrustGraphView: UIView"
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      provides: ".loaded branch passes chainOfTrust to bodyView's graphContainer (or VC-managed graph container in iPhone composition); node-tap callback opens VerificationBasisSheetViewController (sheet wiring lands in Plan 07; this plan ships the callback closure surface)"
      contains: "trustGraphView"
  key_links:
    - from: "validationLedger/Features/Loads/Detail/TrustNodeView.swift"
      to: "validationLedger/UI/Components/VerificationBadgeView.swift"
      via: "addSubview composition; configure(state: node.verificationState) on the embedded badge"
      pattern: "VerificationBadgeView"
    - from: "validationLedger/Features/Loads/Detail/TrustGraphView.swift"
      to: "validationLedger/Core/Load/ChainOfTrust.swift"
      via: "configure(chainOfTrust:) ingests TrustNode[] + TrustEdge[] + ChainIntegrity verbatim"
      pattern: "ChainOfTrust"
    - from: "validationLedger/Features/Loads/Detail/TrustGraphView.swift"
      to: "validationLedger/Features/Loads/Cells/SkeletonLoadRowCell.swift"
      via: "halo pulse lifecycle mirrors startShimmer() lines 167-198"
      pattern: "animation\\(forKey: \"pulse\"\\) == nil"
---

<objective>
Ship the marquee surface of Phase 9 — `TrustGraphView` + `TrustNodeView` + the gesture model + the accessibility container + the fraud visual language (halos + pulse + dashed edges + dim-others). This is the highest-stakes plan in the phase; every CONTEXT decision (D-04 through D-16, D-22) lands here, except for the modal sheets (Plans 07/08) and the chain-integrity banner (Plan 09).

Purpose: TRUST-01 + TRUST-05 acceptance criteria. The graph is the screen's marquee region per D-01. This plan introduces THREE novel patterns (PATTERNS § Novel Patterns lines 57-68): pinch+pan+double-tap choreography on a hosted UIScrollView, CAShapeLayer-as-edge-renderer with invisible companion views, and `accessibilityElements` on a custom-rendered parent. Each is the first of its kind in the repo; this plan locks the recipes.

Output: 2 new production files (`TrustNodeView`, `TrustGraphView`) + LoadDetailViewController integration (the iPhone composition's graph region above the body scroll view, OR a graph subview attached inside the body — UI-SPEC § iPhone composition line 119 puts the graph between the pinned header and the body's scroll boundary; planner verifies the integration shape against UI-SPEC at execute time) + 3 fully-populated test files (gesture, gesture, accessibility) + 2 snapshot test files populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-01-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-03-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-04-PLAN.md

@validationLedger/Core/Load/ChainOfTrust.swift
@validationLedger/Core/Load/ChainIntegrity.swift
@validationLedger/Core/Load/VerificationState.swift
@validationLedger/UI/Components/VerificationBadgeView.swift
@validationLedger/UI/DesignSystem/Spacing.swift
@validationLedger/UI/DesignSystem/Colors.swift
@validationLedger/UI/DesignSystem/Typography.swift
@validationLedger/Features/Loads/Cells/SkeletonLoadRowCell.swift

<interfaces>
<!-- The Phase 8 VerificationBadgeView composition pattern Phase 9 TrustNodeView wraps. -->
<!-- PATTERNS E5 (lines 81-128). The fail-closed accessibility-label discipline. -->

From validationLedger/UI/Components/VerificationBadgeView.swift (PATTERNS E5):
- `public func configure(state: VerificationState)` — sets pill color + label
- Always `isAccessibilityElement = true` + `.staticText` traits
- Reused inside TrustNodeView via `addSubview`

From validationLedger/Core/Load/ChainOfTrust.swift (post-Plan-01):
- `public let nodes: [TrustNode]`
- `public let edges: [TrustEdge]`
- `public let integrity: ChainIntegrity`

TrustNode key fields:
- `partyID: String`, `role: Role`, `displayName: String`, `verificationState: VerificationState`
- `kycCompletedAt: Date?`, `deviceBindingStatus: DeviceBindingStatus`
- `usdotNumber: String?`, `usdotAuthorityStatus: USDOTAuthorityStatus`
- `priorRelationships: [PriorRelationship]` (Plan 01)

TrustEdge key fields:
- `edgeID: String`, `fromPartyID: String`, `toPartyID: String`, `relationshipState: VerificationState`, `tenderRef: String?`

ChainIntegrity:
- `verdict: ChainIntegrity.Verdict` — `.clean | .caution | .compromised` (fail-closed-to-compromised on unknown)
- `reason: String?` — server-supplied
- `implicatedNodeIDs: Set<String>`
- `implicatedEdgeIDs: Set<String>`

Role slot coordinates (UI-SPEC § Default zoom + role slots line 257; D-06):
- iPhone (compact, taller-than-wide): shipper (0.18, 0.18); broker (0.50, 0.30); carrier (0.50, 0.55); dispatch (0.82, 0.55); factoring (0.50, 0.85).
- iPad split (regular, near-square): shipper (0.18, 0.22); broker (0.40, 0.30); carrier (0.50, 0.55); dispatch (0.78, 0.45); factoring (0.82, 0.78).

Pulse animation (RESEARCH §2 lines 272-294; UI-SPEC line 276):
- `CABasicAnimation(keyPath: "opacity")`, `fromValue = 0.6`, `toValue = 1.0`, `duration = 1.2`, `timingFunction = .easeInEaseOut`, `autoreverses = true`, `repeatCount = .infinity`, `isRemovedOnCompletion = false`.

Halo geometry (UI-SPEC line 192-193):
- Path: `UIBezierPath(roundedRect: nodeChromeFrame.insetBy(dx: -6, dy: -6), cornerRadius: 12 + 6).cgPath` — inset −6pt; corner radius 12 (chrome) + 6 (halo).
- Fill: `DS.Colors.caution.cgColor` for caution; `DS.Colors.destructive.cgColor` for compromised.

Edge stroke (UI-SPEC § Edge visuals lines 197-210):
- Clean: solid `DS.Colors.separator`, `2pt`.
- Caution implicated: yellow dashed `[6, 4]`, 2pt.
- Compromised implicated: red dashed `[6, 4]`, 2pt.
- Non-implicated when chain !=clean: dimmed 0.5 opacity.
- Arrowhead: `7×7pt` filled triangle in stroke color at toPartyID end.

Edge invisible companion view (UI-SPEC line 208 + RESEARCH §1 line 241):
- 28pt-band `UIView` overlaid on the edge path; `isAccessibilityElement = true`; `.button` traits; carries the single-tap recognizer that opens TRUST-04 sheet (sheet wiring in Plan 08).
- `accessibilityIdentifier = "load-detail.trust-graph.edge.{edgeID}"`.

Gesture recipe (RESEARCH §1 lines 203-230):
```
graphScrollView.panGestureRecognizer.minimumNumberOfTouches = 2
graphScrollView.panGestureRecognizer.maximumNumberOfTouches = 2  // belt-and-braces
let singleTap = UITapGestureRecognizer(...); singleTap.numberOfTapsRequired = 1
let doubleTap = UITapGestureRecognizer(...); doubleTap.numberOfTapsRequired = 2
singleTap.require(toFail: doubleTap)
```

Zoom-recenter math (RESEARCH §6 lines 626-666):
```
let targetScale: CGFloat = 1.8
let nodeCenter = nodeView.center
let visibleSize = CGSize(width: graphScrollView.bounds.width / targetScale, height: graphScrollView.bounds.height / targetScale)
let targetRect = CGRect(x: nodeCenter.x - visibleSize.width/2, y: nodeCenter.y - visibleSize.height/2, width: visibleSize.width, height: visibleSize.height)
UIView.animate(withDuration: 0.25, delay: 0, options: .curveEaseInOut, animations: { graphScrollView.zoom(to: targetRect, animated: false) })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TrustNodeView — chrome + composed VerificationBadgeView + role/displayName labels + tap recognizers (singleTap.require(toFail: doubleTap)) + populate TrustNodeViewSnapshotTests + TrustNodeViewGestureTests</name>
  <files>
    validationLedger/Features/Loads/Detail/TrustNodeView.swift,
    validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift,
    validationLedgerTests/Loads/TrustNodeViewGestureTests.swift
  </files>
  <behavior>
    - `TrustNodeView: UIView` composes `VerificationBadgeView` + role caption label + displayName label per UI-SPEC § Node visuals lines 167-184.
    - Chrome: rounded square, cornerRadius 12pt; background `DS.Colors.surface` (.clean) or alpha 0.5 when non-clean AND this node is NOT implicated (D-15 dim-others); 0.5pt border `DS.Colors.separator`.
    - Internal vertical UIStackView: roleLabel (top) + verificationBadge (middle) + displayNameLabel (bottom); `.center` alignment; `DS.Spacing.xs` (4pt) spacing.
    - roleLabel: `.caption1` (`DS.Typography.caption`), uppercase, `DS.Colors.labelSecondary`. e.g. "BROKER".
    - displayNameLabel: `.footnote` (`DS.Typography.footnote`), `.label`, max 2 lines, centered, truncating-tail.
    - Touch target: `heightAnchor.constraint(greaterThanOrEqualToConstant: 44)` defensive (UI-SPEC line 182).
    - `accessibilityIdentifier = "load-detail.trust-graph.node.{partyID}"`.
    - `accessibilityTraits = .button` (D-22). `isAccessibilityElement = true`.
    - The accessibilityLabel is COMPOSED by the parent TrustGraphView via `applyAccessibilityElements()` after configuration (not internally — the composed label needs the chain integrity context the parent owns). The view exposes a method `internal func applyComposedAccessibilityLabel(_ label: String)` for the parent to call.
    - Tap recognizers:
      * `private let singleTap: UITapGestureRecognizer(target: self, action: #selector(handleSingleTap))` — `numberOfTapsRequired = 1`.
      * `private let doubleTap: UITapGestureRecognizer(target: self, action: #selector(handleDoubleTap))` — `numberOfTapsRequired = 2`.
      * `singleTap.require(toFail: doubleTap)` — the BL-04 / Pitfall 2 mitigation (RESEARCH §1 line 226).
      * Both attached to `self` in `setUp()`.
    - Callbacks (closures, set by TrustGraphView during configure):
      * `var onSingleTap: ((TrustNode) -> Void)?` — fires the TRUST-03 sheet open (wired in Plan 07).
      * `var onDoubleTap: ((TrustNodeView) -> Void)?` — fires the recenter+zoom (TrustGraphView handles).
    - `public func configure(node: TrustNode, isImplicated: Bool, isDimmed: Bool)`:
      * `verificationBadge.configure(state: node.verificationState)`.
      * `roleLabel.text = node.role.displayName.uppercased()`.
      * `displayNameLabel.text = node.displayName`.
      * `alpha = isDimmed ? 0.5 : 1.0`.
      * Cache `partyID` for the single-tap callback dispatch.
    - `TrustNodeViewSnapshotTests` (4 representative method shells populated):
      * `test_verifiedCarrierNode_rendersBlueRoundedSquare`, `test_flaggedCarrierNode_compromisedDimmed_rendersRedBadgeAt05Alpha` (the dim-others rule at the node level), `test_pendingBrokerNode_rendersYellowBadge`, `test_unverifiedShipperNode_rendersGreyBadge`.
    - `TrustNodeViewGestureTests`:
      * `test_singleTap_requiresDoubleTapFail()`: introspect `node.gestureRecognizers`; find the singleTap (numberOfTapsRequired == 1); call `XCTAssertTrue(singleTap.value(forKey: "_requireFailureForGestureRecognizers") as? Set<UITapGestureRecognizer> contains doubleTap)` — OR use KVC reflection to assert the failure-requirement chain. The simpler path: name the recognizers with `accessibilityIdentifier` ("trust-node.singleTap" / "trust-node.doubleTap") via setting `gestureRecognizer.name = "..."` (iOS 17 UIGestureRecognizer.name) and assert the relationship. Per RESEARCH §1 line 246-247 + PATTERNS table row line 41 (gesture-test harness is novel; reflection or KVC bridge is acceptable here).
      * `test_doubleTap_doesNotInvokeSingleTapCallback()`: build node; assign `onSingleTap` capturing `var singleTapFired = false`; programmatically fire the double-tap recognizer (`doubleTap.state = .recognized` — or via `UIGestureRecognizer.invokeTarget(action:on:)` reflection; the test asserts the singleTap closure did NOT execute).
      * `test_singleTap_opensVerificationBasisSheetAfterDoubleTapFailWindow()`: build node; assign `onSingleTap`; fire singleTap programmatically; assert closure fired.
  </behavior>
  <read_first>
    - validationLedger/UI/Components/VerificationBadgeView.swift (lines 81-128, 137-149, 153-222 — PATTERNS E5 the composable subview template Phase 9 wraps; the fail-closed accessibility-label discipline)
    - validationLedger/UI/Components/LoadStatusBadgeView.swift (the parallel composable component for comparison)
    - validationLedger/UI/DesignSystem/Colors.swift / Spacing.swift / Typography.swift
    - validationLedger/Core/Load/ChainOfTrust.swift (TrustNode + Role enum surface)
    - validationLedger/Core/Load/VerificationState.swift (the 4-case enum the badge consumes)
    - validationLedgerTests/Loads/Snapshot/VerificationBadgeViewSnapshotTests.swift (PATTERNS E8 — per-state snapshot test template)
    - validationLedgerTests/Loads/Snapshot/SkeletonLoadRowCellSnapshotTests.swift (Phase 8 snapshot tests reference)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (§ Node visuals lines 167-184; § Halo layer lines 185-195; § Accessibility identifiers — load-detail.trust-graph.node.*; § Gesture model line 235-237 for the single-tap / double-tap recognizer recipe)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§1 lines 203-230 — the canonical gesture recipe + lines 245-253 acceptance criteria; § Common Pitfalls Pitfall 2 lines 970-978; § Code Examples lines 1045-1090 gesture wiring)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (E5 + table row 18 TrustNodeView; § Novel Patterns lines 57-68; table line 41 — gesture-test harness is novel)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-04 gesture choreography; D-22 accessibility container model)
  </read_first>
  <action>
    A. CREATE `validationLedger/Features/Loads/Detail/TrustNodeView.swift`. Programmatic UIKit. Compose VerificationBadgeView per PATTERNS E5.

    Structure:
    - `import UIKit`.
    - `public final class TrustNodeView: UIView`.
    - Stored:
      * `private(set) public var partyID: String = ""` (cached for callback dispatch).
      * `private let verificationBadge = VerificationBadgeView()`.
      * `private let roleLabel = UILabel()`.
      * `private let displayNameLabel = UILabel()`.
      * `private let internalStack: UIStackView`.
      * `private let singleTap: UITapGestureRecognizer`.
      * `private let doubleTap: UITapGestureRecognizer`.
      * `internal var onSingleTap: ((String) -> Void)?` — closure receives partyID.
      * `internal var onDoubleTap: ((TrustNodeView) -> Void)?` — closure receives self (TrustGraphView resolves to nodeCenter).
    - `public override init(frame: CGRect) { super.init(frame: frame); setUp() }`.
    - `required init?(coder:) { fatalError("TrustNodeView is constructed programmatically only") }`.
    - `private func setUp()`:
      * `backgroundColor = DS.Colors.surface`.
      * `layer.cornerRadius = 12`.
      * `layer.borderWidth = 0.5`.
      * `layer.borderColor = DS.Colors.separator.cgColor`.
      * Configure roleLabel font `DS.Typography.caption`, color `DS.Colors.labelSecondary`, `adjustsFontForContentSizeCategory = true`.
      * Configure displayNameLabel font `DS.Typography.footnote`, color `.label`, numberOfLines = 2, textAlignment = .center, lineBreakMode = .byTruncatingTail, `adjustsFontForContentSizeCategory = true`.
      * Build internal vertical stack `[roleLabel, verificationBadge, displayNameLabel]`, axis vertical, alignment .center, spacing `DS.Spacing.xs`.
      * Add to self; pin with `DS.Spacing.sm` inset on all edges.
      * `heightAnchor.constraint(greaterThanOrEqualToConstant: 44)` defensive touch-target floor.
      * `isAccessibilityElement = true`.
      * `accessibilityTraits = .button` (D-22).
      * Set recognizers:
        - `singleTap.numberOfTapsRequired = 1; singleTap.name = "trust-node.singleTap"` (iOS 17 named recognizer — test introspection).
        - `doubleTap.numberOfTapsRequired = 2; doubleTap.name = "trust-node.doubleTap"`.
        - `singleTap.require(toFail: doubleTap)` — RESEARCH §1 line 226 / Pitfall 2.
        - `addGestureRecognizer(singleTap); addGestureRecognizer(doubleTap)`.
    - `public func configure(node: TrustNode, isImplicated: Bool, isDimmed: Bool)`:
      * `partyID = node.partyID`.
      * `accessibilityIdentifier = "load-detail.trust-graph.node.\(node.partyID)"`.
      * `verificationBadge.configure(state: node.verificationState)`.
      * `verificationBadge.accessibilityIdentifier = "load-detail.trust-graph.node.\(node.partyID).verification-badge"`.
      * `roleLabel.text = node.role.displayName.uppercased()`.
      * `displayNameLabel.text = node.displayName`.
      * `alpha = isDimmed ? 0.5 : 1.0`.
    - `internal func applyComposedAccessibilityLabel(_ label: String)`: `accessibilityLabel = label`.
    - `@objc private func handleSingleTap()`: `onSingleTap?(partyID)`.
    - `@objc private func handleDoubleTap()`: `onDoubleTap?(self)`.

    No logger. Per T-09-04: zero PII; node fields are server-supplied data rendered verbatim.

    B. POPULATE `validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift` (Plan 02 shells).

    For each of 4 representative states:
    - `test_verifiedCarrierNode_rendersBlueRoundedSquare()`: synthesize a TrustNode with `verificationState = .verified, role = .carrier`. Configure with `isImplicated = false, isDimmed = false`. Snapshot at `CGSize(width: 100, height: 100)`. Assert `view.layer.cornerRadius == 12` AND `verificationBadge.backgroundColor` matches `DS.Colors.primary` (per the Phase 8 badge color ramp).
    - `test_flaggedCarrierNode_compromisedDimmed_rendersRedBadgeAt05Alpha()`: configure with `isImplicated = false, isDimmed = true` (dim-others rule applied because chain is compromised but THIS node isn't implicated). Assert `view.alpha == 0.5`.
    - `test_pendingBrokerNode_rendersYellowBadge()`: `verificationState = .pending`. Assert badge background == `DS.Colors.caution`.
    - `test_unverifiedShipperNode_rendersGreyBadge()`: `verificationState = .unverified`. Assert badge background == neutral (verify against Phase 8 VerificationBadgeView's mapping).
    - Every test attaches an image via UIKitSnapshot.

    C. POPULATE `validationLedgerTests/Loads/TrustNodeViewGestureTests.swift` (Plan 02 shell). RESEARCH §1 acceptance criteria + PATTERNS § Novel Patterns table line 41:

    - `test_singleTap_requiresDoubleTapFail()`: build a `TrustNodeView`. Find the recognizers by name (`singleTap = view.gestureRecognizers.first { $0.name == "trust-node.singleTap" }`; same for doubleTap). Assert `XCTAssertNotNil(singleTap)` AND `XCTAssertNotNil(doubleTap)`. To assert the failure-requirement chain, use KVC introspection: `let failureRequirements = singleTap.value(forKey: "_requireFailureForGestureRecognizers") as? [UIGestureRecognizer]` — assert it contains the doubleTap (alternatively: assert the runtime behavior via the next two tests; the KVC path may be brittle across Swift versions, so the planner may opt to assert behavior only and treat the source-grep `grep -c 'singleTap.require(toFail:' TrustNodeView.swift returns 1` as the source-level gate).
    - `test_doubleTap_doesNotInvokeSingleTapCallback()`: build view; set `view.onSingleTap = { _ in singleTapFired = true }`; programmatically simulate a double-tap via two `UITapGestureRecognizer` state-machine invocations. The simplest approach: simulate the recognizer states directly: call `view.handleDoubleTap()` via reflection or expose an internal-testing surface `internal func _simulateDoubleTap() { handleDoubleTap() }`; assert `singleTapFired == false`. The behavioral gate proves the closure plumbing.
    - `test_singleTap_invokesOnSingleTapWithPartyID()`: build view; configure with a synthetic node `partyID = "party-broker-001"`; assign `onSingleTap = { id in capturedID = id }`; invoke single-tap (via `_simulateSingleTap()` or sending `.recognized` state); assert `capturedID == "party-broker-001"`.

    Plan a KVC-introspection helper that fails gracefully if private API access doesn't resolve — falling back to the source-grep assertion via the acceptance criteria.

    Per T-09-08 (novel pattern — gesture-test introspection): RESEARCH §1 line 245-253 calls out this exact need; PATTERNS § Novel Patterns line 68 documents no in-repo prior art. Acceptable to use source-grep + behavioral assertion combo.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/TrustNodeViewSnapshotTests -only-testing:validationLedgerTests/TrustNodeViewGestureTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public final class TrustNodeView: UIView' validationLedger/Features/Loads/Detail/TrustNodeView.swift` returns `1`
    - Source: `grep -c 'singleTap.require(toFail: doubleTap)' validationLedger/Features/Loads/Detail/TrustNodeView.swift` returns `1` (the Pitfall 2 mitigation — locked at source level)
    - Source: `grep -c 'accessibilityTraits = .button' validationLedger/Features/Loads/Detail/TrustNodeView.swift` returns `1` (D-22)
    - Source: `grep -c 'VerificationBadgeView' validationLedger/Features/Loads/Detail/TrustNodeView.swift` returns `>= 1` (the composed badge)
    - Source: `grep -c 'isAccessibilityElement = true' validationLedger/Features/Loads/Detail/TrustNodeView.swift` returns `>= 1`
    - Source: `grep -c 'singleTap.name = "trust-node.singleTap"\|doubleTap.name = "trust-node.doubleTap"' validationLedger/Features/Loads/Detail/TrustNodeView.swift` returns `>= 2`
    - Source: `grep -v '^//' validationLedger/Features/Loads/Detail/TrustNodeView.swift | grep -cE 'Logger|os_log'` returns `0`
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/Snapshot/TrustNodeViewSnapshotTests.swift` returns `0`
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/TrustNodeViewGestureTests.swift` returns `0`
    - Behavior: TrustNodeViewSnapshotTests passes (4 methods); TrustNodeViewGestureTests passes (3 methods including the singleTap-after-doubleTap-fail behavioral gate)
  </acceptance_criteria>
  <done>
    TrustNodeView ships with composed VerificationBadgeView + role + displayName labels + tap recognizers with locked Pitfall 2 mitigation + accessibility traits per D-22; 4 snapshot tests + 3 gesture tests pass; the single-tap-require-double-tap-fail relationship is locked at the source level (grep returns 1) and behaviorally locked by the gesture-test suite.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TrustGraphView (the marquee canvas) — UIScrollView host + fixed role slots + CAShapeLayer edges + halo layers + pulse animation + accessibility container (D-22) + gesture model (D-04) + integrate into LoadDetailViewController</name>
  <files>
    validationLedger/Features/Loads/Detail/TrustGraphView.swift,
    validationLedger/Features/Loads/Detail/LoadDetailViewController.swift,
    validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift,
    validationLedgerTests/Loads/TrustGraphViewGestureTests.swift,
    validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift
  </files>
  <behavior>
    - `TrustGraphView: UIView` is the marquee canvas. Hosts:
      * Internal `UIScrollView` (graphScrollView): `minimumZoomScale = 1.0`, `maximumZoomScale = 2.5`, `panGestureRecognizer.minimumNumberOfTouches = 2` AND `maximumNumberOfTouches = 2` (RESEARCH §1 lines 214-215), conforms to `UIScrollViewDelegate.viewForZooming(in:)` returning `self` (RESEARCH §6 line 582).
      * Inside scroll view: a content container `UIView` (the actual zoomable content; nodes are subviews here; edges are CAShapeLayers on the container's layer; halos are CAShapeLayers on the container's layer drawn BEFORE the node subviews so chrome paints over them).
      * `accessibilityIdentifier = "load-detail.trust-graph"`.
    - 5 fixed role slots from UI-SPEC § Default zoom + role slots line 257; iPhone-tall coordinates AND iPad-wide coordinates picked via `traitCollection.horizontalSizeClass`.
    - `public func configure(chainOfTrust: ChainOfTrust)`:
      * Clear existing node views + edge layers + halo layers.
      * For each `TrustNode` in `chainOfTrust.nodes`:
        - Build `TrustNodeView`; configure with `node`, `isImplicated = chainOfTrust.integrity.implicatedNodeIDs.contains(node.partyID)`, `isDimmed = (chainOfTrust.integrity.verdict != .clean) && !isImplicated`.
        - Position via fixed role-slot lookup + canvas dimensions (computed in `layoutSubviews`).
        - Set `node.onSingleTap = { [weak self] partyID in self?.nodeTapped?(partyID) }` — the callback that opens TRUST-03 sheet (wired by LoadDetailViewController in this task; the sheet VC ships in Plan 07).
        - Set `node.onDoubleTap = { [weak self] nodeView in self?.recenterAndZoom(to: nodeView) }`.
        - Add as subview to the content container.
      * For each `TrustEdge`:
        - Build a `CAShapeLayer` for the edge stroke; path = `UIBezierPath` from node[fromPartyID].center to node[toPartyID].center; strokeColor per verdict + implicated mapping (clean → separator, caution implicated → caution dashed, compromised implicated → destructive dashed, non-implicated when chain!=clean → separator @ 0.5 opacity); `lineWidth = 2`; arrowhead at `toPartyID` end via additional `CAShapeLayer` triangle.
        - Build a 28pt-band invisible companion `UIView` for the tap target (UI-SPEC line 208); position + rotation matches the edge angle; `accessibilityIdentifier = "load-detail.trust-graph.edge.\(edge.edgeID)"`; `accessibilityTraits = .button`; `isAccessibilityElement = true`; single-tap recognizer that calls `self.edgeTapped?(edge.edgeID)`.
      * Halo layers: for each `TrustNode` where `partyID ∈ implicatedNodeIDs` AND `verdict != .clean`:
        - Build `CAShapeLayer` halo; path inset −6pt from node chrome; fillColor `DS.Colors.caution.cgColor` (caution) or `DS.Colors.destructive.cgColor` (compromised).
        - For `compromised` verdict: attach pulse `CABasicAnimation(keyPath: "opacity", fromValue: 0.6, toValue: 1.0, duration: 1.2, timingFunction: easeInEaseOut, autoreverses: true, repeatCount: .infinity, isRemovedOnCompletion: false)` via `startPulseIfNeeded()` (`guard halo.animation(forKey: "pulse") == nil else { return }` — PATTERNS E3 mirror).
        - For `caution` verdict: NO animation (UI-SPEC line 192 — caution halo is static).
        - For `compromised` + Reduce-Motion ON: NO animation (UI-SPEC line 866); the halo renders solid at opacity 1.0.
      * Set accessibility container (D-22):
        - `isAccessibilityElement = false`.
        - Build orderedNodes by iterating `[Role.shipper, .broker, .carrier, .dispatch, .factoring]` and filtering to present nodes.
        - Build orderedEdges by sorting edges by `(fromPartyID, toPartyID)` lex order; for each, use the invisible companion view.
        - `accessibilityElements = orderedNodes + orderedEdges`.
        - For each node: call `nodeView.applyComposedAccessibilityLabel(composedNodeLabel(for: node, chainOfTrust:))` — see helper below.
        - For each edge companion: set `accessibilityLabel = composedEdgeLabel(for: edge, chainOfTrust:)`.
    - Composed node label (UI-SPEC line 841-845):
      * Base: `"{Role display name}, {display name}, verification state: {state}, KYC {basis summary}, device {binding summary}, {USDOT summary if applicable}"`.
      * If implicated: append `" Implicated in chain compromise."` (UI-SPEC line 845).
      * KYC basis summary: "verified {relative time}" if `kycCompletedAt != nil` else "not completed".
      * Device-binding summary: switch on `deviceBindingStatus`.
      * USDOT summary: switch on `usdotAuthorityStatus`; SKIP this segment for Shipper / Factoring (UI-SPEC line 419 — USDOT row absent for those roles).
    - Composed edge label (UI-SPEC line 849):
      * `"Handoff from {fromRole.displayName} to {toRole.displayName}, relationship state: {relationshipState}, {implicated suffix if applicable}"`.
    - `layoutSubviews()`:
      * `super.layoutSubviews()`.
      * Resize content container; recompute node positions from role-slot coordinates × canvas dimensions; recompute edge paths; recompute halo paths; recompute edge companion-view positions/rotations.
      * Re-attach pulse animations via `startPulseIfNeeded()` (Pitfall 1 / RESEARCH §2 line 967).
      * Recompute edge stroke widths: `edgeLayer.lineWidth = 2.0 / scrollView.zoomScale` (UI-SPEC line 266 — constant-in-screen-space; RESEARCH §2 line 266 + line 318).
    - `traitCollectionDidChange(_:)`:
      * Override; if `previousTraitCollection?.horizontalSizeClass != traitCollection.horizontalSizeClass`: switch role-slot coordinate set (iPhone vs iPad).
      * Call `startPulseIfNeeded()` (Pitfall 1).
    - `scrollViewDidZoom(_:)`:
      * Recompute edge `lineWidth = 2.0 / zoomScale` for every edge layer.
      * Call `startPulseIfNeeded()`.
    - Double-tap-on-empty-canvas reset (D-04 + RESEARCH §6 line 671):
      * Attached to the content container's background OR the scrollView's content area (planner finalizes). `UITapGestureRecognizer(numberOfTapsRequired: 2)` with action calling `UIView.animate(withDuration: 0.25, options: .curveEaseInOut) { scrollView.setZoomScale(1.0, animated: false); scrollView.setContentOffset(.zero, animated: false) }`.
    - VoiceOver branch (D-22, RESEARCH §4 line 485):
      * Observer on `UIAccessibility.didChangeNotification`; on `isVoiceOverRunning == true`: `scrollView.minimumZoomScale = scrollView.maximumZoomScale = 1.0`; disable all double-tap recognizers (`isEnabled = false`).
      * On VoiceOver off: restore `minimumZoomScale = 1.0, maximumZoomScale = 2.5`; re-enable.
      * `internal var voiceOverActiveOverride: Bool? = nil` for testing.
    - Reduce-motion branch:
      * Observer on `UIAccessibility.reduceMotionStatusDidChangeNotification`; suppress pulse on activation; resume on deactivation.
      * `internal var reduceMotionOverride: Bool?`.
    - Closures the parent VC consumes:
      * `internal var nodeTapped: ((String) -> Void)?` — fires with `partyID`; LoadDetailViewController owns the closure that opens the TRUST-03 sheet (sheet wiring in Plan 07).
      * `internal var edgeTapped: ((String) -> Void)?` — fires with `edgeID`; LoadDetailViewController wires this in Plan 08.
    - LoadDetailViewController integration:
      * Add a stored `private let trustGraphView = TrustGraphView()`.
      * In `layoutContent()`: per UI-SPEC § iPhone composition lines 117-121, the graph region lives BETWEEN the pinned header (from bodyView) and the rest of the body's scroll content — but for the iPhone path, UI-SPEC line 117 says the pinned header lives ABOVE the graph. The cleanest approach for this plan: attach the graph as a sibling of the bodyView inside the VC's view, positioned between the safeAreaLayoutGuide top and the bodyView's top, height `view.safeAreaLayoutGuide.layoutFrame.height * 0.62` (UI-SPEC line 119). The bodyView's pinned header moves to the iPad-only branch (Plan 09 handles the split-pane refactor). The pragmatic interim shape: iPhone keeps a simpler vertical stack [pinned-header-overlay, trustGraphView, bodyView]; Plan 09 refactors when adding the iPad split branch + banner.
      * `.loaded(load, chainOfTrust)`: call `trustGraphView.configure(chainOfTrust: chainOfTrust)`.
      * Set `trustGraphView.nodeTapped = { [weak self] partyID in self?.presentVerificationBasisSheet(for: partyID) }` — the `presentVerificationBasisSheet(for:)` method is a stub in this plan (`fatalError("Plan 07 wires the sheet")`) — Plan 07 fills it in.
      * Set `trustGraphView.edgeTapped = { [weak self] edgeID in self?.presentHandoffDetailSheet(for: edgeID) }` — same stub posture; Plan 08 wires.
    - Snapshot + gesture + a11y tests:
      * `TrustGraphViewSnapshotTests` populated with the 12-fingerprint matrix (3 verdicts × 2 devices × 2 Dynamic Type sizes) per RESEARCH §3 line 390. Each test sets `halo.opacity = 1.0` BEFORE rendering (RESEARCH §3 line 408 — capture the resting frame, not mid-pulse).
      * `TrustGraphViewGestureTests` populated:
        - `test_innerScroll_minimumNumberOfTouchesEqualsTwo()`: build view; layout; assert `scrollView.panGestureRecognizer.minimumNumberOfTouches == 2`. Per RESEARCH §1 line 247.
        - `test_innerScroll_maximumNumberOfTouchesEqualsTwo()`: assert `scrollView.panGestureRecognizer.maximumNumberOfTouches == 2`.
        - `test_doubleTapOnNode_animatesZoomToward1_8()`: build view; configure with fixture; programmatically invoke node.onDoubleTap closure with the carrier node; assert `scrollView.zoomScale > 1.0` after a render-loop hop.
        - `test_doubleTapOnEmptyCanvas_resetsZoomToOne()`: build view; programmatically set `scrollView.zoomScale = 1.5`; invoke the empty-canvas double-tap handler; assert `scrollView.zoomScale == 1.0` after the animation completes (or use animated=false in the reset for test determinism).
        - `test_voiceOverActive_disablesPinchZoom()`: build view; `view.voiceOverActiveOverride = true`; trigger `accessibilityChanged()` (the observer method); assert `scrollView.minimumZoomScale == scrollView.maximumZoomScale == 1.0`. Per RESEARCH §4 line 499.
      * `TrustGraphViewAccessibilityTests` populated (RESEARCH §4 lines 490-503):
        - `test_isAccessibilityElement_false()`: `XCTAssertFalse(view.isAccessibilityElement)`.
        - `test_accessibilityElements_orderedRoleThenEdges()`: build view; configure with 5-node fixture; assert `view.accessibilityElements!.count == 5 + edgeCount`; the first 5 elements are TrustNodeView in role order `[shipper, broker, carrier, dispatch, factoring]`.
        - `test_nodeAccessibilityLabel_followsComposedTemplate()`: assert each node's `accessibilityLabel` starts with role-display-name followed by comma + displayName + comma + "verification state: ".
        - `test_implicatedNode_appendsCompromiseSuffix()`: configure with compromised verdict + an implicated node; assert that node's accessibilityLabel contains "Implicated in chain compromise".
        - `test_voiceOver_disablesPinchZoom()`: as in the gesture test.
        - `test_reduceMotion_suppressesPulse()`: configure with compromised verdict + implicated node; set `reduceMotionOverride = true`; assert no `animation(forKey: "pulse")` is attached.
  </behavior>
  <read_first>
    - validationLedger/Features/Loads/Cells/SkeletonLoadRowCell.swift (lines 60-100, 167-198 — PATTERNS E3 the shimmer lifecycle; the EXACT lifecycle Phase 9's halo pulse mirrors)
    - validationLedger/Core/Load/ChainOfTrust.swift (post-Plan-01 — TrustNode + TrustEdge + the nested array of PriorRelationship + the Role enum)
    - validationLedger/Core/Load/ChainIntegrity.swift (verdict + reason + implicatedNodeIDs + implicatedEdgeIDs)
    - validationLedger/UI/DesignSystem/Colors.swift (DS.Colors.caution, .destructive, .separator)
    - validationLedger/UI/Components/VerificationBadgeView.swift (the embedded badge surface used in node composition)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-04 gesture model; D-05 fit-all-nodes-tight; D-06 fixed role slots; D-15 fraud visual language; D-22 accessibility container)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§1 lines 187-253 gesture arbitration FULL recipe + acceptance criteria; §2 lines 256-333 CAShapeLayer + pulse lifecycle; §3 lines 337-410 snapshot posture; §4 lines 413-503 accessibility container; §6 lines 575-705 zoom math; § Common Pitfalls lines 958-1043)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (§ Node visuals lines 167-184; § Halo layer lines 185-195; § Edge visuals lines 197-210; § Gesture model lines 227-242; § Default zoom + role slots lines 244-267; § Snapshot test posture lines 280-284; § Pre-fraud-tier color invariants lines 622-634; § Accessibility identifiers lines 757-788; § Graph accessibility container lines 832-862; § Pinch-zoom disabled while VoiceOver runs lines 856-862)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (E3 — shimmer lifecycle the pulse mirrors; E5 — TrustNodeView composition; § Novel Patterns lines 57-68; table rows 17/18 — graph + node files)
  </read_first>
  <action>
    A. CREATE `validationLedger/Features/Loads/Detail/TrustGraphView.swift`. This is the largest single file in Phase 9 (~400-600 lines once complete). Structure tightly per the behavior block above.

    Key implementation notes (planner pre-resolved):

    - `viewForZooming(in:)` returns `self.contentContainer` (the actual zoomable subview — NOT `self`; the scrollView's content is a subview of the scroll view per UIScrollView contract).
    - Role-slot coordinates table (D-06 / UI-SPEC line 257):
      ```
      private static let iPhoneSlots: [Role: CGPoint] = [
          .shipper: CGPoint(x: 0.18, y: 0.18),
          .broker: CGPoint(x: 0.50, y: 0.30),
          .carrier: CGPoint(x: 0.50, y: 0.55),
          .dispatch: CGPoint(x: 0.82, y: 0.55),
          .factoring: CGPoint(x: 0.50, y: 0.85),
      ]
      private static let iPadSlots: [Role: CGPoint] = [
          .shipper: CGPoint(x: 0.18, y: 0.22),
          .broker: CGPoint(x: 0.40, y: 0.30),
          .carrier: CGPoint(x: 0.50, y: 0.55),
          .dispatch: CGPoint(x: 0.78, y: 0.45),
          .factoring: CGPoint(x: 0.82, y: 0.78),
      ]
      ```
    - Pulse helper (PATTERNS E3 mirror, RESEARCH §2 + § Code Examples lines 1092-1141):
      ```
      private func startPulseIfNeeded() {
          for halo in haloLayers where halo.fillColor == DS.Colors.destructive.cgColor {
              if isReduceMotionOn { halo.removeAnimation(forKey: "pulse"); halo.opacity = 1.0; continue }
              guard halo.animation(forKey: "pulse") == nil else { continue }
              let pulse = CABasicAnimation(keyPath: "opacity")
              pulse.fromValue = 0.6; pulse.toValue = 1.0
              pulse.duration = 1.2
              pulse.timingFunction = CAMediaTimingFunction(name: .easeInEaseOut)
              pulse.autoreverses = true
              pulse.repeatCount = .infinity
              pulse.isRemovedOnCompletion = false
              halo.add(pulse, forKey: "pulse")
          }
      }
      ```
    - Composed-label helpers `composedNodeLabel(for:chainOfTrust:)` and `composedEdgeLabel(for:chainOfTrust:)` build the strings per UI-SPEC § Graph accessibility container.
    - Edge invisible companion is built as an internal helper class `EdgeCompanionView: UIView` carrying the edge + its single-tap recognizer; lives at the bottom of the file as a private nested type or in a separate `EdgeCompanionView.swift` file (planner discretion).

    Per T-09-03 / Phase 7 D-18 LOCK (no client trust): every visual treatment is driven by `chainIntegrity.verdict` + `implicatedNodeIDs.contains(...)` + `implicatedEdgeIDs.contains(...)` — never by client-side rules. Source-assertion grep gate enforces: no `derive*()`-style methods exist in this file; the implicated checks are membership lookups against the server-supplied sets.

    Per T-09-04 (PII): zero logger calls; node accessibility labels include `displayName` (server-supplied, PII-zero in the fixture corpus per Phase 7 D-13).

    Per T-09-08 (novel gesture pattern): `singleTap.require(toFail: doubleTap)` is the only `UIGestureRecognizerDelegate.shouldRecognizeSimultaneouslyWith` interaction Phase 9 uses (RESEARCH §1 line 242 — no shouldRecognizeSimultaneouslyWith override).

    Per T-09-09 (novel accessibility-container pattern): `isAccessibilityElement = false` + explicit ordered `accessibilityElements` array is the supported iOS 17 path (RESEARCH §4 lines 429-435).

    B. MODIFY `validationLedger/Features/Loads/Detail/LoadDetailViewController.swift`:

    1. Add `private let trustGraphView = TrustGraphView()`.
    2. In `layoutContent()`: Add the graph region BETWEEN the pinned header overlay (which Plan 04 placed inside bodyView's contentStack as the first arranged subview — for iPhone, lifting the pinned header above the graph is Plan 09's iPhone composition refactor; the pragmatic interim is to keep the pinned header inside bodyView for now AND add the trustGraphView ABOVE bodyView in the VC's view; the graph height is `view.safeAreaLayoutGuide.layoutFrame.height * 0.62` per UI-SPEC line 119; bodyView occupies the remaining height).
    3. In `render(state:)`, `.loaded(load, chainOfTrust)` branch: `trustGraphView.configure(chainOfTrust: chainOfTrust)`.
    4. Wire callbacks:
       ```
       trustGraphView.nodeTapped = { [weak self] partyID in
           self?.presentVerificationBasisSheet(for: partyID)
       }
       trustGraphView.edgeTapped = { [weak self] edgeID in
           self?.presentHandoffDetailSheet(for: edgeID)
       }
       ```
    5. Add stub method bodies for `presentVerificationBasisSheet(for:)` and `presentHandoffDetailSheet(for:)` — each `fatalError("Plan 07/08 wires the sheet presentation; this stub catches any unwired tap before downstream plans land.")` so Plans 07/08 have a clear method-replacement site. Alternative: log a warning and no-op; planner discretion — `fatalError` is the louder signal for catching missing wiring during development.

    C. POPULATE the 3 test files per `<behavior>` above. Each test file gains ≥4 `func test_*` methods. All XCTSkip stubs from Plan 02 are replaced.

    Specific assertion patterns to encode:
    - Source assertions for the 2 critical lock points (these run as test methods that read source text — the planner can opt to use grep in CI instead and keep tests behavioral; a robust path is the gesture-test that introspects gestureRecognizers and asserts `minimumNumberOfTouches == 2` at runtime).
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/TrustGraphViewSnapshotTests -only-testing:validationLedgerTests/TrustGraphViewGestureTests -only-testing:validationLedgerTests/TrustGraphViewAccessibilityTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public final class TrustGraphView: UIView' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `1`
    - Source: `grep -c 'panGestureRecognizer.minimumNumberOfTouches = 2' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `1` (D-04 / RESEARCH §1 line 214)
    - Source: `grep -c 'panGestureRecognizer.maximumNumberOfTouches = 2' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `1` (RESEARCH §1 line 215)
    - Source: `grep -c 'isAccessibilityElement = false' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `>= 1` (D-22; the container's accessibilityElements is the children)
    - Source: `grep -c 'accessibilityElements' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `>= 1`
    - Source: `grep -c 'animation(forKey: "pulse") == nil' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `>= 1` (Pitfall 1 + PATTERNS E3 mirror)
    - Source: `grep -c 'CABasicAnimation(keyPath: "opacity")' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `>= 1` (the pulse keyPath)
    - Source: `grep -c 'CAMediaTimingFunction(name: .easeInEaseOut)\|.easeInEaseOut' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `>= 1`
    - Source: `grep -c 'repeatCount = .infinity' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `>= 1`
    - Source: `grep -c 'implicatedNodeIDs\|implicatedEdgeIDs' validationLedger/Features/Loads/Detail/TrustGraphView.swift` returns `>= 2` (server-supplied implicated sets drive halo + dim + dashed-edge behavior; Phase 7 D-18 LOCK)
    - Source: `grep -v '^//' validationLedger/Features/Loads/Detail/TrustGraphView.swift | grep -cE 'Logger|os_log'` returns `0`
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift` returns `0`
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/TrustGraphViewGestureTests.swift` returns `0`
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift` returns `0`
    - Source: `grep -c '@testable import validationLedger' validationLedgerTests/Loads/TrustGraphViewGestureTests.swift` returns `1`
    - Behavior: TrustGraphViewSnapshotTests passes with ≥12 method bodies that attach images (the matrix from RESEARCH §3 line 390); compromised-verdict snapshot tests lock `halo.opacity = 1.0` BEFORE rendering (Pitfall 8 / RESEARCH §3 line 408)
    - Behavior: TrustGraphViewGestureTests passes — including the `minimumNumberOfTouches == 2` assertion proving D-04 / outer-page-scroll-preservation
    - Behavior: TrustGraphViewAccessibilityTests passes — including the `isAccessibilityElement == false` + ordered-elements assertions proving D-22
  </acceptance_criteria>
  <done>
    TrustGraphView ships with full D-04 / D-05 / D-06 / D-15 / D-22 implementation; the pulse-only-on-compromised invariant is locked; the role-slot coordinate system branches on size class; LoadDetailViewController wires the graph above bodyView with callback stubs for Plan 07/08; ≥12 snapshot fingerprints + gesture introspection + a11y container assertions all pass green; source-level grep gates lock the two most critical invariants (`minimumNumberOfTouches == 2`, `isAccessibilityElement = false`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ChainOfTrust` (decoded) → graph render | `verificationState`, `chainIntegrity.verdict`, `implicatedNodeIDs/EdgeIDs` cross from the typed Decodable surface into the visual treatment. Phase 7 D-18 LOCK: rendered verbatim; no client derivation. |
| Outer page scroll vs inner graph scroll | Gesture arbitration via `minimumNumberOfTouches = 2` on the graph's pan recognizer (RESEARCH §1 line 214). Without this lock, the body scroll is swallowed inside the graph region (Pitfall 3). |
| Single-tap vs double-tap on nodes | `require(toFail:)` is the only mitigation (Pitfall 2). Without it, every double-tap fires both the sheet open AND the recenter animation — UX wedged. |
| VoiceOver double-tap conflict with our double-tap | D-22 mitigation: VoiceOver-active branch disables our double-tap recognizers + clamps zoom (RESEARCH §4 line 485 + § Common Pitfalls Pitfall 6). |
| `CABasicAnimation` halo pulse lifecycle | Pitfall 1: stripped on layout / size-class change. Mitigation: `startPulseIfNeeded()` re-attach in layoutSubviews + traitCollectionDidChange (PATTERNS E3 mirror). |
| Snapshot artefact pixel determinism | Pitfall 8: animated halo mid-frame produces non-deterministic snapshots. Mitigation: every compromised-verdict snapshot test sets `halo.opacity = 1.0` BEFORE rendering (RESEARCH §3 line 408). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | Client-side trust derivation in TrustGraphView / TrustNodeView | mitigate | EVERY visual treatment (halo color, edge dash, dim opacity, implicated-suffix in accessibilityLabel) is driven by `chainIntegrity.verdict` enum case + `implicated*IDs.contains(...)` membership lookups against server-supplied Sets. Source-assertion grep returns ≥2 matches for `implicatedNodeIDs\|implicatedEdgeIDs` (proves the implicated sets ARE read); no `derive*()` methods exist (Phase 7 D-18 LOCK). |
| T-09-04 | Information Disclosure (PII) | Snapshot artefacts + accessibility labels | mitigate | Snapshot tests use synthetic Phase 7 fixture corpus only (PATTERNS § Zero-PII; UIKitSnapshot.swift lines 22-27 lock). Composed accessibility labels render `displayName` (server-supplied, synthetic in fixtures). NO logger calls in any new view file (T-08-08 mirror). |
| T-09-08 | Tampering | Novel gesture-recognizer wiring (singleTap require toFail doubleTap, minimumNumberOfTouches lock) | mitigate | RESEARCH §1 acceptance criteria lock the two source-level invariants; gesture tests assert them at runtime. The `require(toFail:)` mitigation is locked at the source level (grep returns 1 in TrustNodeView.swift). Without these locks: Pitfall 2 (sheet flashes on double-tap) and Pitfall 3 (outer page scroll swallowed) both materialize. |
| T-09-09 | Tampering | Novel accessibility-container pattern (isAccessibilityElement=false + explicit accessibilityElements array) | mitigate | RESEARCH §4 lines 437-468 + PATTERNS § Novel Patterns line 65 lock the recipe. Pitfall 4 (`isAccessibilityElement = true` on the container) is the #1 mistake; the source-level grep gate (`isAccessibilityElement = false` returns ≥1) catches it. TrustGraphViewAccessibilityTests.test_isAccessibilityElement_false asserts the runtime invariant. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies; halo + edge + pulse all use Apple-first-party CAShapeLayer / CABasicAnimation. |
</threat_model>

<verification>
1. Build green: `xcodebuild build` exits 0.
2. Trust-graph snapshot suite green: `-only-testing:validationLedgerTests/TrustGraphViewSnapshotTests` passes; ≥12 image fingerprints attached to XCTAttachment.
3. Gesture invariants green: `-only-testing:validationLedgerTests/TrustGraphViewGestureTests` passes including `test_innerScroll_minimumNumberOfTouchesEqualsTwo` (Pitfall 3 lock) and `test_voiceOverActive_disablesPinchZoom` (Pitfall 6 lock).
4. Accessibility container green: `-only-testing:validationLedgerTests/TrustGraphViewAccessibilityTests` passes including `test_isAccessibilityElement_false` (Pitfall 4 lock).
5. Phase 9 regression smoke: `LoadDetailFlowTests/test_rowTap_pushesDetail` still passes (the .loaded path now configures graph + body + timeline; navigation push survives the new view hierarchy).
6. Zero-PII gate: `grep -v '^//' validationLedger/Features/Loads/Detail/TrustGraphView.swift validationLedger/Features/Loads/Detail/TrustNodeView.swift | grep -cE 'Logger|os_log|OSLog'` returns 0.
7. Source-level lock gates: `grep -c 'singleTap.require(toFail: doubleTap)' TrustNodeView.swift == 1`; `grep -c 'minimumNumberOfTouches = 2' TrustGraphView.swift == 1`; `grep -c 'isAccessibilityElement = false' TrustGraphView.swift >= 1`.
</verification>

<success_criteria>
- TrustNodeView and TrustGraphView exist with full implementations of D-04 / D-05 / D-06 / D-15 / D-22.
- Server-supplied chain-integrity data drives every visual treatment (Phase 7 D-18 LOCK enforced by source-assertion grep).
- The 3 novel patterns (gesture choreography, CAShapeLayer-as-edge with companion views, accessibility container) are locked at both source and behavioral levels.
- ≥12 snapshot fingerprints + ≥5 gesture assertions + ≥6 accessibility assertions all pass green.
- LoadDetailViewController integrates the graph above bodyView (iPhone interim composition) with callback stubs for Plans 07/08.
- VoiceOver-active branch suspends pinch-zoom + double-tap recognizers; reduce-motion branch suppresses pulse.
- Zero new SwiftPM dependencies.
- Zero logger calls in any new view file.
- iPhone-tall vs iPad-wide role-slot coordinates are both defined; the size-class branch in `traitCollectionDidChange` switches them.
</success_criteria>

<output>
Create `.planning/phases/09-load-detail-chain-of-trust-graph/09-06-SUMMARY.md` when done. Record:
- TrustNodeView surface (configure / onSingleTap / onDoubleTap closure shapes; applyComposedAccessibilityLabel signature).
- TrustGraphView surface (configure(chainOfTrust:) entry point; nodeTapped / edgeTapped closures; the role-slot coordinate tables iPhone vs iPad).
- The 3 novel patterns and how each is locked (source grep + behavioral test).
- The 12-fingerprint snapshot matrix (the 12 method names + verdict × device × Dynamic-Type triple).
- LoadDetailViewController interim iPhone composition (graph above bodyView) and the Plan 09 refactor handoff (the pinned header lifts above the graph + iPad split branch).
- presentVerificationBasisSheet(for:) and presentHandoffDetailSheet(for:) stubs (the fatalError site Plan 07/08 replaces).
- Any open question — e.g. whether `viewForZooming(in:)` returns `self` or the contentContainer; whether the empty-canvas double-tap recognizer attaches to the scrollView or contentContainer; whether to factor EdgeCompanionView into its own file.
</output>
