---
phase: 04-app-attest-physical-device-ci-hardening
plan: 10
type: execute
wave: 5
depends_on: [05, 09]
files_modified:
  - .github/workflows/ci-device.yml
  - .github/workflows/ci-simulator.yml
  - scripts/report-flaky-passes.sh
  - docs/ci.md
autonomous: false
requirements: [CI-03, DEV-04]
requirements_addressed: [CI-03, DEV-04]
threat_model:
  threats:
    - id: T-CI-03-01
      stride: Elevation_of_Privilege
      component: GitHub admin branch-protection bypass
      disposition: accept
      mitigation: "Residual risk documented per 04-CONTEXT.md D-16: admins CAN override the required-status-check via GitHub UI. Accepted — solo-dev org; no adversarial admin. Documented in docs/ci.md."
    - id: T-CI-03-02
      stride: Spoofing
      component: Flaky-retry masks a real regression as a pass
      disposition: mitigate
      mitigation: "D-15: pass-on-retry logs 'flaky-passed' to Slack (scripts/report-flaky-passes.sh). Quarantine requires explicit .flaky annotation + tracking issue. Engineer-visible signal on every flaky pass prevents silent masking."
    - id: T-APP-ATTEST-03
      stride: Elevation_of_Privilege
      component: Simulator-bypass symbol in Release archive
      disposition: mitigate
      mitigation: "CI-simulator workflow runs scripts/verify-release-no-sim-bypass.sh (Plan 05 Task 3) — blocks merge if Release build contains 'sim-bypass-' strings."
must_haves:
  truths:
    - ".github/workflows/ci-device.yml job is named device-security-surface (required for branch-protection UI check-name lookup per Pitfall 4)"
    - "ci-device.yml runs xcodebuild test with -only-testing:validationLedgerDeviceTests -retry-tests-on-failure -test-iterations 2 (D-13 + D-15)"
    - "ci-device.yml uploads DeviceTestResults.xcresult artifact with 14-day retention"
    - "ci-device.yml runs scripts/report-flaky-passes.sh to notify Slack on flaky pass (D-15)"
    - "ci-simulator.yml runs scripts/verify-release-no-sim-bypass.sh to block sim-bypass leaks into Release (D-10)"
    - "docs/ci.md documents the Phase 4 Device Pipeline + merge-gate policy (D-16) + first-run branch-protection dance (Pitfall 4) + HUMAN-UAT checklist for branch-protection config"
  artifacts:
    - path: ".github/workflows/ci-device.yml"
      provides: "Upgraded device CI workflow — full security surface + single-retry + flaky-notification"
    - path: ".github/workflows/ci-simulator.yml"
      provides: "Release-strings guard step added for D-10 compile-out verification"
    - path: "scripts/report-flaky-passes.sh"
      provides: "POSIX sh script parsing xcresult for retry annotations + POSTing to Slack webhook"
    - path: "docs/ci.md"
      provides: "Updated Device Pipeline section documenting D-13/D-15/D-16 + first-run required-status-check dance"
  key_links:
    - from: "scripts/verify-release-no-sim-bypass.sh (Plan 05)"
      to: "ci-simulator.yml step invocation"
      via: "shell step with CODE_SIGNING_ALLOWED=NO env"
    - from: "scripts/report-flaky-passes.sh (NEW)"
      to: "ci-device.yml post step"
      via: "passing xcresult bundle path"
---

<objective>
Upgrade the CI pipeline to deliver CI-03: device-pipeline-as-required-status-check, full security surface, single-retry with flaky notification, and protection against simulator-bypass leaks into Release. Land the runbook updates in docs/ci.md + the first-run branch-protection HUMAN-UAT item.

Purpose: CI-03 (SC-3) — "CI physical-device pipeline runs SE + Keychain biometric + App Attest tests on every merge to main, and blocks merges that break any of them." D-13 (coverage), D-15 (flakiness policy), D-16 (merge gate).
Output: 2 YAML files edited + 1 shell script new + 1 docs file updated + 1 HUMAN checkpoint for the first-run branch-protection config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-app-attest-physical-device-ci-hardening/04-CONTEXT.md
@.planning/phases/04-app-attest-physical-device-ci-hardening/04-PATTERNS.md
@.planning/phases/04-app-attest-physical-device-ci-hardening/04-RESEARCH.md

<interfaces>
<!-- Existing CI workflows + Plan 09 device tests + Plan 05 release-strings script. -->

From .github/workflows/ci-device.yml (existing 34 lines — 04-PATTERNS.md Pattern 14 lines 702-740):
- Current: on push+pr with paths filter; single job `smoke`; timeout 15min; runs SecureEnclaveSmokeTests only.

From .github/workflows/ci-simulator.yml (existing):
- Simulator CI workflow; exact shape to be read at execution time.

From Plan 09: validationLedgerDeviceTests/{AppAttestRoundTripTests, KeychainBiometricACLTests, LogoutClearsAuthorizationKeyTests}.swift — to be executed by the upgraded device CI.

From Plan 05 Task 3: scripts/verify-release-no-sim-bypass.sh — to be invoked by ci-simulator.yml.

From Plan 05 Task 1+2: validationLedgerTests/Attestation/* + validationLedgerTests/Networking/Device*Tests.swift — run by ci-simulator.yml alongside existing tests.

From docs/ci.md (existing Phase 1):
- Documents simulator + device pipelines at a high level. Phase 4 extends.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade .github/workflows/ci-device.yml for full security surface + single-retry + flaky Slack notification</name>
  <read_first>
    - .github/workflows/ci-device.yml (full file — all 34 lines) — current job name, paths filter, test target, timeout
    - .github/workflows/ci-simulator.yml (full file) — reference for artifact-upload + notification step idioms
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-PATTERNS.md lines 742-750 (Pattern 14 deltas)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-RESEARCH.md lines 464-520 (verbatim CI YAML upgrade body)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-RESEARCH.md lines 610-622 (Pitfall 4 first-run dance)
  </read_first>
  <action>
    Replace the body of `.github/workflows/ci-device.yml` with the Phase 4 upgraded version. Full replacement file:

    ```yaml
    name: CI (Device)

    on:
      # D-16 (Pitfall 4 first-run): push to main ensures the check is registered with the
      # branch-protection UI before the rule is flipped ON.
      push:
        branches: [main]
      # D-16: PR gate — blocks merge when the job fails if branch protection is configured.
      pull_request:
        branches: [main]
        paths:
          - 'validationLedger/Core/Auth/**'
          - 'validationLedger/Core/KeyStore/**'
          - 'validationLedger/Core/Identity/**'
          - 'validationLedger/Core/Networking/CertificatePinning/**'
          - 'validationLedger/Core/Attestation/**'       # Phase 4 addition
          - 'validationLedger/Core/Storage/Keychain/**'  # Phase 4 addition — KeychainKey + KeychainScope touch attestation invariants
          - 'validationLedgerDeviceTests/**'             # Phase 4 addition — device tests themselves
          - '.github/workflows/ci-device.yml'

    jobs:
      # Job name MUST be stable (Pitfall 4 — this name is referenced in branch protection).
      device-security-surface:
        runs-on: [self-hosted, macOS, device]
        timeout-minutes: 25    # Bumped from 15 — App Attest round-trip adds latency
        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Show Xcode version
            run: xcodebuild -version

          - name: Run full device security surface (D-13) with single-retry (D-15)
            # -retry-tests-on-failure + -test-iterations 2 = retry the FAILED test once.
            # If it passes on retry → workflow green; flaky-pass annotation in xcresult → Slack.
            # If it fails twice → workflow red → merge blocked per D-16.
            # Native xcodebuild flag runs only the failed test on retry (not the whole step).
            run: |
              set -o pipefail
              xcodebuild test \
                -project validationLedger.xcodeproj \
                -scheme validationLedger \
                -destination "platform=iOS,id=${{ secrets.DEVICE_UDID }}" \
                -only-testing:validationLedgerDeviceTests \
                -retry-tests-on-failure \
                -test-iterations 2 \
                -resultBundlePath $PWD/build/DeviceTestResults.xcresult

          - name: Upload device test result bundle
            if: always()
            uses: actions/upload-artifact@v4
            with:
              name: device-test-results
              path: build/DeviceTestResults.xcresult
              retention-days: 14

          - name: Notify Slack on flaky-pass (D-15 visibility)
            if: success()
            env:
              SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
            # Parse xcresult for "retry"-labeled tests; POST to Slack only if any retries happened.
            # Quiet when zero retries. Best-effort — missing webhook doesn't fail the workflow.
            run: bash scripts/report-flaky-passes.sh $PWD/build/DeviceTestResults.xcresult || true
    ```

    Important: the job name change from `smoke` → `device-security-surface` is deliberate per Pitfall 4. After this commit is merged to main, the new job name appears in the branch-protection UI (Task 4 human checkpoint).
  </action>
  <verify>
    <automated>grep -c "name: CI (Device)" .github/workflows/ci-device.yml; grep -c "device-security-surface:" .github/workflows/ci-device.yml; grep -c "timeout-minutes: 25" .github/workflows/ci-device.yml; grep -c "validationLedger/Core/Attestation/\*\*" .github/workflows/ci-device.yml; grep -c "validationLedgerDeviceTests/\*\*" .github/workflows/ci-device.yml; grep -c "\\-retry-tests-on-failure" .github/workflows/ci-device.yml; grep -c "\\-test-iterations 2" .github/workflows/ci-device.yml; grep -c "\\-resultBundlePath" .github/workflows/ci-device.yml; grep -c "scripts/report-flaky-passes.sh" .github/workflows/ci-device.yml; grep -c "retention-days: 14" .github/workflows/ci-device.yml; yamllint .github/workflows/ci-device.yml 2>/dev/null || echo "yamllint not installed — skipping (file will be validated by GitHub Actions on push)"</automated>
  </verify>
  <acceptance_criteria>
    - Grep `"device-security-surface:"` returns 1 match (new job name).
    - Grep `"smoke:"` in the YAML returns 0 matches (old job name removed).
    - Grep `"timeout-minutes: 25"` = 1 (bumped from 15).
    - Grep `"validationLedger/Core/Attestation/**"` = 1; `"validationLedgerDeviceTests/**"` = 1 (paths filter extended).
    - Grep `"-retry-tests-on-failure"` = 1 AND `"-test-iterations 2"` = 1 (D-15 single-retry).
    - Grep `"-resultBundlePath"` = 1 AND `"retention-days: 14"` = 1 (artifact upload).
    - Grep `"scripts/report-flaky-passes.sh"` = 1 (flaky Slack step wired).
    - Grep `"validationLedgerDeviceTests/SecureEnclaveSmokeTests"` = 0 (single-test restriction removed; full target runs).
    - File parses as valid YAML (yamllint if available; otherwise rely on GitHub Actions validation on push).
  </acceptance_criteria>
  <done>ci-device.yml runs full validationLedgerDeviceTests target with retry, uploads xcresult, notifies Slack on flaky, job name stable for branch-protection.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ci-simulator.yml with scripts/verify-release-no-sim-bypass.sh invocation + create scripts/report-flaky-passes.sh</name>
  <read_first>
    - .github/workflows/ci-simulator.yml (FULL FILE) — existing structure; find the natural insertion point for a Release-build check
    - scripts/verify-release-no-sim-bypass.sh (Plan 05 Task 3)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-PATTERNS.md lines 753-763 (Pattern 15 — report-flaky-passes.sh deltas)
  </read_first>
  <action>
    **Step A: Create `scripts/report-flaky-passes.sh`** (POSIX sh, no external deps beyond xcrun):

    ```sh
    #!/bin/sh
    # scripts/report-flaky-passes.sh
    # D-15: Parse xcresult for tests that retried + passed on retry; POST to Slack webhook.
    # Input: $1 = path to .xcresult bundle.
    # Silent exit 0 if (a) no retries happened, (b) SLACK_WEBHOOK_URL unset.
    # Best-effort — ci-device.yml uses `|| true` so this never fails the workflow.

    set -eu

    XCRESULT_PATH="${1:-}"
    if [ -z "$XCRESULT_PATH" ] || [ ! -d "$XCRESULT_PATH" ]; then
        echo "No xcresult provided or bundle missing; skipping flaky-pass report."
        exit 0
    fi

    # Use xcrun xcresulttool (native Xcode) to extract test summary JSON.
    JSON_OUT="/tmp/xcresult-$$.json"
    xcrun xcresulttool get --path "$XCRESULT_PATH" --format json > "$JSON_OUT" 2>/dev/null || {
        echo "xcresulttool failed; skipping flaky-pass report."
        exit 0
    }

    # Look for any ActionTestMetadata with retry attempts > 1. The exact JSON path depends
    # on Xcode version; a simple grep works for "retries" or "attempts>1" markers.
    RETRY_COUNT=$(grep -c '"isRetry":true\|"retry_count":\|"numberOfTestRetries":' "$JSON_OUT" 2>/dev/null || echo 0)
    # Fallback grep on broader term if above nets nothing (xcresult JSON format varies):
    if [ "$RETRY_COUNT" -eq 0 ]; then
        RETRY_COUNT=$(grep -c 'retried' "$JSON_OUT" 2>/dev/null || echo 0)
    fi

    rm -f "$JSON_OUT"

    if [ "$RETRY_COUNT" -eq 0 ]; then
        echo "No test retries detected; quiet exit."
        exit 0
    fi

    if [ -z "${SLACK_WEBHOOK_URL:-}" ]; then
        echo "Detected $RETRY_COUNT retry markers in xcresult, but SLACK_WEBHOOK_URL unset; skipping post."
        exit 0
    fi

    MESSAGE="validationLedger device CI — flaky-pass detected ($RETRY_COUNT retry markers). Commit: ${GITHUB_SHA:-unknown}. Run: ${GITHUB_RUN_ID:-unknown}. See artifact 'device-test-results'. Add .flaky annotation + tracking issue if this recurs (D-15)."

    curl -s -X POST -H "Content-Type: application/json" \
        -d "{\"text\": \"$MESSAGE\"}" \
        "$SLACK_WEBHOOK_URL" \
        || echo "Slack POST failed; continuing anyway."
    ```

    Make executable: `chmod +x scripts/report-flaky-passes.sh`.

    **Step B: Extend `.github/workflows/ci-simulator.yml`** to run `verify-release-no-sim-bypass.sh` after the simulator tests pass.

    Read the existing ci-simulator.yml, then append (or insert before any artifact-upload) a new step:

    ```yaml
          - name: Verify Release binary has no sim-bypass symbols (D-10 guard)
            # Phase 4: ensure SimulatorBypassAttestationService does NOT leak into Release.
            # Script archives Release config, greps binary for "sim-bypass-" strings, fails if any.
            run: bash scripts/verify-release-no-sim-bypass.sh $PWD/build/release-check
    ```

    This step runs on every simulator CI invocation (PR + push to main). It's a ~30-60s overhead (archive + strings grep). If this becomes too slow in practice, add a `paths:` filter on just this step or move it to a separate workflow triggered only on touches to `validationLedger/Core/Attestation/**`.
  </action>
  <verify>
    <automated>test -f scripts/report-flaky-passes.sh; test -x scripts/report-flaky-passes.sh; sh -n scripts/report-flaky-passes.sh; grep -c "xcrun xcresulttool" scripts/report-flaky-passes.sh; grep -c "SLACK_WEBHOOK_URL" scripts/report-flaky-passes.sh; grep -c "verify-release-no-sim-bypass.sh" .github/workflows/ci-simulator.yml; grep -c "Verify Release binary has no sim-bypass" .github/workflows/ci-simulator.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/report-flaky-passes.sh` exists and is executable.
    - `sh -n` syntax check passes.
    - Grep `"xcrun xcresulttool"` = 1 match (native tool usage, no external deps).
    - Grep `"SLACK_WEBHOOK_URL"` returns >= 2 matches (env var read + check for unset).
    - Grep `"verify-release-no-sim-bypass.sh"` in ci-simulator.yml returns 1 match (step wired).
    - Grep `"Verify Release binary has no sim-bypass"` in ci-simulator.yml returns 1 match (step name).
  </acceptance_criteria>
  <done>Flaky-pass reporter + Release-strings guard step are wired into the CI pipelines. Both use safe-fallback patterns (|| true / early-exit on missing inputs).</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/ci.md with Phase 4 Device Pipeline + first-run branch-protection runbook</name>
  <read_first>
    - docs/ci.md (FULL FILE) — existing structure (Phase 1 documentation)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-RESEARCH.md lines 610-622 (Pitfall 4 first-run procedure verbatim)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-CONTEXT.md lines 76-87 (D-13..D-16)
  </read_first>
  <action>
    Append (or insert into the appropriate section of) `docs/ci.md` a new section titled "Phase 4 Device Pipeline" with the following content. Structure mirrors the cert-rotation runbook's procedural style:

    ```markdown
    ## Phase 4 Device Pipeline

    **Requirement:** CI-03 — physical-device tests on every merge to main, with merge-gate.

    **Coverage (D-13):** `.github/workflows/ci-device.yml` runs `validationLedgerDeviceTests` target on self-hosted macOS runner with attached iPhone. Suites:
    1. `SecureEnclaveKeyStoreTests` — Phase 2 DEV-01/DEV-02 SE round-trip
    2. `SecureEnclaveSmokeTests` — Phase 1 smoke
    3. `KeychainBiometricACLTests` — Phase 4 D-14 seeded-LAContext Keychain ACL path
    4. `AppAttestRoundTripTests` — Phase 4 D-13-3 real-hardware App Attest round-trip
    5. `LogoutClearsAuthorizationKeyTests` — Phase 4 D-13-4 SE ACL clearing + D-03 attestedKeyId preservation

    **Flakiness policy (D-15):** `-retry-tests-on-failure -test-iterations 2` retries a failed test ONCE. On pass-after-retry, `scripts/report-flaky-passes.sh` POSTs to the Slack webhook (`SLACK_WEBHOOK_URL` secret) so engineers investigate. On fail-twice, the workflow is red. Quarantine is explicit: engineer adds a `.flaky` Swift annotation to the test + opens a tracking issue — implicit quarantine is not acceptable.

    **Merge gate (D-16):** The job `device-security-surface` is a required GitHub branch-protection status check on `main`. Red pipeline blocks merge. Admins can override via GitHub UI for break-glass situations (documented residual risk).

    ### First-run branch-protection dance (Pitfall 4)

    GitHub branch-protection only lists check names that have actually run on the protected branch. The first time Phase 4's upgraded `ci-device.yml` lands, follow this sequence:

    1. Merge the Phase 4 PR containing `ci-device.yml` changes — this triggers the workflow on `main` via the `push: branches: [main]` trigger.
    2. Wait for the workflow to complete (pass or fail — only completion matters).
    3. Navigate to **Settings → Branches → Edit `main` rule**.
    4. Under **Require status checks to pass**, tick the checkbox.
    5. Select `device-security-surface` from the available-checks dropdown. (If not present, the workflow hasn't run yet — go back to step 2.)
    6. Also keep `CI (Simulator) / test` (or whatever the simulator job is named) checked.
    7. Save.

    After this one-time setup, every future PR to `main` is gated by both simulator CI + device CI.

    **Break-glass:** If `main` ends up blocked with no valid path forward (e.g., a device-runner outage), admins can temporarily un-tick the required-status-check in Settings, merge, then re-tick. Record these events in the PR description + an incident note in `docs/ops-incidents.md` (create file if needed).

    ### Simulator-bypass leak guard (D-10)

    Every simulator CI run also invokes `scripts/verify-release-no-sim-bypass.sh`, which archives the app in Release config and greps the binary for `sim-bypass-` strings. Any match fails the workflow — catches a regression where `SimulatorBypassAttestationService` loses its `#if DEBUG && targetEnvironment(simulator)` gate.

    ### Re-attestation CI coverage (D-04)

    `validationLedgerTests/Attestation/BackendDrivenReattestationTest` exercises the `clearPersistedKeyId() → regenerate` flow against a mocked `attestationInvalid` response. For full integration coverage (server-error-interceptor wired to attestation service), see Plan 09 + the attestation-rotation runbook at `docs/attestation-rotation.md`.
    ```

    Preserve existing docs/ci.md content — only append the new Phase 4 section. If docs/ci.md already has a "Device Pipeline" section from Phase 1, update that section in place rather than duplicating.
  </action>
  <verify>
    <automated>grep -c "Phase 4 Device Pipeline\|Phase 4 device pipeline" docs/ci.md; grep -c "device-security-surface" docs/ci.md; grep -c "First-run branch-protection dance\|Pitfall 4" docs/ci.md; grep -c "D-13\|D-14\|D-15\|D-16" docs/ci.md; grep -c "verify-release-no-sim-bypass.sh" docs/ci.md; grep -c "report-flaky-passes.sh" docs/ci.md; grep -c "validationLedgerDeviceTests" docs/ci.md</antomated></automated>
  </verify>
  <acceptance_criteria>
    - Grep `"Phase 4 Device Pipeline"` (or equivalent section header) in docs/ci.md = 1 match.
    - Grep `"device-security-surface"` = >= 1 match (job name referenced).
    - Grep `"First-run branch-protection dance"` or `"Pitfall 4"` = >= 1 match.
    - Grep decision IDs `"D-13"`, `"D-14"`, `"D-15"`, `"D-16"` — each appears >= 1 time.
    - Grep `"verify-release-no-sim-bypass.sh"` = 1 match.
    - Grep `"report-flaky-passes.sh"` = 1 match.
    - Grep for test-suite names (AppAttestRoundTripTests, KeychainBiometricACLTests, LogoutClearsAuthorizationKeyTests) — each = 1 match.
  </acceptance_criteria>
  <done>docs/ci.md documents the Phase 4 device pipeline + merge policy + first-run dance + two guard scripts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: HUMAN CHECKPOINT — First-run branch-protection configuration in GitHub UI (Pitfall 4)</name>
  <what-built>
    Automated tasks landed:
    - ci-device.yml with new job name `device-security-surface` + full test target + retry + flaky notification
    - ci-simulator.yml with Release-strings guard step
    - scripts/report-flaky-passes.sh + (referenced) scripts/verify-release-no-sim-bypass.sh
    - docs/ci.md documenting the pipeline

    Remaining CI-03 gap: the GitHub branch-protection rule for `main` has NOT been configured to require `device-security-surface`. This is a GitHub UI action — GH API could do it via `gh api` but the Pitfall 4 chicken-and-egg requires the workflow to have run once on main first.
  </what-built>
  <how-to-verify>
    **After** this Phase 4 PR merges into `main` and `device-security-surface` runs at least once on main:

    1. Navigate to **Repository Settings → Branches**.
    2. Edit (or create) the branch protection rule for `main`.
    3. Check **"Require status checks to pass before merging"**.
    4. In the dropdown search, type `device-security-surface` — MUST appear in the list (if not, workflow hasn't completed on main yet — wait).
    5. Select it. Also keep the simulator CI check (`CI (Simulator) / test` or similar) selected.
    6. Save.

    **Verification that the gate is active:**

    7. Open a test PR that deliberately fails `validationLedgerDeviceTests` (e.g., add a `#expect(false)` to AppAttestRoundTripTests, push as a branch, open PR).
    8. Wait for `device-security-surface` to complete and go red.
    9. Confirm the PR **"Merge" button is disabled** with "Required check failing."
    10. Remove the deliberate failure, push again, confirm merge re-enables.
    11. Close the test PR without merging. Delete the test branch.

    **Record in the PR description or Phase 4 SUMMARY:**
    - Screenshot (or text confirmation) of branch-protection rule showing `device-security-surface` as required.
    - Screenshot (or text) of a blocked-merge in the test PR.
    - Confirmation of re-enabled merge after fix.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `approved` — branch-protection configured + gate verified by test PR + `device-security-surface` is a required check on main.
    - `deferred: <reason>` — configuration couldn't be completed (e.g., org-level admin permissions needed). Document who owns the follow-up and when it completes. Phase 4 closure is blocked until `approved`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repo ↔ Self-hosted macOS runner | Self-hosted runner exposes real iPhone + Keychain; secrets.DEVICE_UDID + secrets.SLACK_WEBHOOK_URL cross this boundary. |
| Device CI ↔ Apple attestation servers | Real network call during AppAttestRoundTripTests; cert pinning does NOT apply to Apple's attestation framework traffic (Apple's framework handles its own trust). |
| Branch protection ↔ Repo write permission | Merge gate enforced by GitHub; admin override is documented residual risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-CI-03-01 | Elevation_of_Privilege | Admin branch-protection bypass | accept | D-16 explicit — admins can override; solo-dev org accepts this trade-off; documented in docs/ci.md. |
| T-CI-03-02 | Spoofing | Flaky-retry masks real break | mitigate | D-15 + scripts/report-flaky-passes.sh Slack notification; explicit .flaky annotation + tracking issue required for quarantine. |
| T-APP-ATTEST-03 | Elevation_of_Privilege | sim-bypass leak to Release | mitigate | scripts/verify-release-no-sim-bypass.sh on every ci-simulator run; blocks merge if symbols appear in Release binary. |
| T-CI-03-03 | Denial_of_Service | Self-hosted runner offline / device unplugged | accept | Runs go red; merges blocked; engineer fixes runner. Documented break-glass path in docs/ci.md. |
</threat_model>

<verification>
1. ci-device.yml job renamed to `device-security-surface` + runs full validationLedgerDeviceTests + single-retry + artifact + Slack step.
2. ci-simulator.yml has Release-strings guard step.
3. scripts/report-flaky-passes.sh exists + is executable + syntax-valid.
4. docs/ci.md documents the Phase 4 pipeline + first-run branch-protection dance + D-13..D-16 coverage.
5. Human checkpoint approves the branch-protection configuration in GitHub UI.
</verification>

<success_criteria>
- CI-03 delivered end-to-end: full security surface runs on merge to main, blocks merges that fail.
- D-13 / D-14 / D-15 / D-16 all wired at the CI layer.
- D-10 simulator-bypass leak guard runs on every simulator CI.
- Runbook in docs/ci.md covers first-run + break-glass + flaky policy.
</success_criteria>

<output>
Create `.planning/phases/04-app-attest-physical-device-ci-hardening/04-10-SUMMARY.md`.
</output>
