---
phase: 04-app-attest-physical-device-ci-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - validationLedger/Core/Attestation/AttestationStatus.swift
  - validationLedger/Core/Attestation/TrustTier.swift
  - validationLedger/Core/Attestation/AttestationError.swift
  - validationLedger/Core/Attestation/AttestationService.swift
  - validationLedger/Core/Storage/Keychain/KeychainKey.swift
  - validationLedger/Core/Storage/Keychain/KeychainScope.swift
  - validationLedger/validationLedger.entitlements
  - docs/adr/0005-three-key-device-register-payload.md
  - docs/attestation-rotation.md
autonomous: true
requirements: [DEV-04]
requirements_addressed: [DEV-04]
threat_model:
  threats:
    - id: T-APP-ATTEST-02
      stride: Information_Disclosure
      component: Keychain-stored attestedKeyId
      disposition: mitigate
      mitigation: "KeychainKey.attestedKeyId uses .afterFirstUnlockThisDeviceOnly accessibility per D-01; attestedKeyId alone is useless without the SE-held private key that DCAppAttestService refuses to expose."
    - id: T-APP-ATTEST-04
      stride: Information_Disclosure
      component: AttestationError enum + future logging sites
      disposition: mitigate
      mitigation: "AttestationError uses underlying(NSError) with DCError.errorDomain only; AttestationStatus is a String raw-value enum safe for logging. Enum design prevents attestationObject/attestedKeyId bytes from ever becoming a loggable associated value."
must_haves:
  truths:
    - "AttestationStatus enum has exactly 6 cases matching wire format: attested, unsupported, entitlementMissing, quotaExceeded, simulatorBypass, error"
    - "TrustTier enum has exactly 2 cases: hardwareAttested, softwareOnly (per D-12 initial M1 contract)"
    - "AttestationService protocol exposes generateKeyIfNeeded, attestKey, generateAssertion, clearPersistedKeyId members as async-throws"
    - "KeychainKey has attestedKeyId + lastHeartbeatAt constants; neither is included in KeychainScope.session (D-03 preserve-across-logout)"
    - "validationLedger.entitlements declares com.apple.developer.devicecheck.appattest-environment = development"
    - "ADR 0005 documents three-key /device/register payload; docs/attestation-rotation.md documents backend-driven re-attest flow"
  artifacts:
    - path: "validationLedger/Core/Attestation/AttestationStatus.swift"
      provides: "6-value wire-format enum (D-09)"
      contains: "case attested, case unsupported, case entitlementMissing, case quotaExceeded, case simulatorBypass, case error"
    - path: "validationLedger/Core/Attestation/TrustTier.swift"
      provides: "backend trust-tier wire enum (D-12)"
      contains: "case hardwareAttested, case softwareOnly"
    - path: "validationLedger/Core/Attestation/AttestationError.swift"
      provides: "Swift error taxonomy mapping DCError.Code to domain (D-09)"
    - path: "validationLedger/Core/Attestation/AttestationService.swift"
      provides: "protocol members + header comment citing D-01/D-03/D-04/D-05/D-06/D-07"
    - path: "validationLedger/Core/Storage/Keychain/KeychainKey.swift"
      provides: "two new constants: attestedKeyId, lastHeartbeatAt"
    - path: "validationLedger/Core/Storage/Keychain/KeychainScope.swift"
      provides: "doc-comment explicitly excluding attestedKeyId + lastHeartbeatAt from .session (D-03)"
    - path: "validationLedger/validationLedger.entitlements"
      provides: "App Attest entitlement (development env)"
    - path: "docs/adr/0005-three-key-device-register-payload.md"
      provides: "ADR documenting three-key payload (D-02)"
    - path: "docs/attestation-rotation.md"
      provides: "Re-attestation runbook (D-04)"
  key_links:
    - from: "AttestationStatus"
      to: "DeviceRegisterEndpoint.RequestBody.attestationStatus field (Plan 04)"
      via: "Codable String raw value"
    - from: "TrustTier"
      to: "DeviceRegisterEndpoint.Response.trustTier + DeviceHeartbeatEndpoint.Response.trustTier (Plan 04)"
      via: "Decodable String raw value"
    - from: "KeychainKey.attestedKeyId"
      to: "AttestedKeyStore persistence in DCAppAttestAttestationService (Plan 03)"
      via: "KeychainStore.get/set calls"
---

<objective>
Land the Wave-1 foundational types, protocols, Keychain key constants, entitlement file, ADR 0005, and the attestation-rotation runbook. These contracts are imported by every Wave-2+ plan. No production logic here — only types, enums, the protocol surface, the file header comments that cite decisions, and the docs.

Purpose: Establish the D-02 / D-09 / D-12 wire-format contract and the D-03 Keychain invariant as immovable interfaces before any implementation writes against them. Land ADR 0005 + the attestation runbook now so later plans can cite them.
Output: 9 files touched (6 new Swift files + 2 Keychain extensions + 1 entitlements + 1 ADR + 1 runbook, counted as 9 distinct paths per files_modified above).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-app-attest-physical-device-ci-hardening/04-CONTEXT.md
@.planning/phases/04-app-attest-physical-device-ci-hardening/04-PATTERNS.md

<interfaces>
<!-- Existing contracts Plan 01 extends or mirrors. -->

From validationLedger/Core/KeyStore/KeyStoreProtocol.swift (lines 1-76):
- Header style: "Protocol for device-bound signing keys. Implementations: ..."
- public enum KeyStoreError: Error, Sendable { case notImplemented; case signingFailed; case keyUnavailable; case keyGenerationFailed(CFError?); case keyDeletionFailed(OSStatus) }
- public enum Keyslot: Sendable { case device; case authorization }
- public protocol KeyStoreProtocol: AnyObject, Sendable { func sign(_ data: Data) throws -> Data; func publicKeyRepresentation() throws -> Data; func generateDeviceIdentityKeys() throws -> (devicePublicKey: Data, authorizationPublicKey: Data); func signWithAuthorization(_ data: Data, context: LAContext?) throws -> Data; func deleteKey(slot: Keyslot) throws }

From validationLedger/Core/Storage/Keychain/KeychainKey.swift (existing constants):
- public static let sessionToken = KeychainKey(rawValue: "session.token")
- public static let installUUID  = KeychainKey(rawValue: "install.uuid")
- public static let sessionRole          = KeychainKey(rawValue: "session.role")
- public static let sessionUserID        = KeychainKey(rawValue: "session.userID")
- public static let biometricDomainState = KeychainKey(rawValue: "biometric.domainState")

From validationLedger/Core/Storage/Keychain/KeychainScope.swift (existing .session case):
- case session { returns [sessionToken, sessionRole, sessionUserID, biometricDomainState].contains(key) }
- Existing doc-comment notes installUUID is INTENTIONALLY outside .session — Phase 4 follows the same pattern.

From docs/adr/0004-secure-enclave-two-key-pattern.md:
- 6-section structure: Context / Decision / Consequences (Positive / Negative / Acknowledged but NOT addressed) / Alternatives Considered / Operational Notes / References
- Frontmatter lines: Date, Status, Supersedes, Superseded by

From docs/cert-rotation.md:
- Runbook structure: "Why X Is Not Optional" / "Extraction" / "Procedure" / "Emergency Revoke Path" / "Rollback Procedure" / "CI Checks" / "Related"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three type files (AttestationStatus, TrustTier, AttestationError) + AttestationService protocol</name>
  <read_first>
    - validationLedger/Core/KeyStore/KeyStoreProtocol.swift (lines 1-76) — header style + error enum + protocol shape to mirror (04-PATTERNS.md Pattern 1, lines 52-119)
    - validationLedger/Core/Auth/LogoutService.swift (lines 21-25) — LogoutReason String-raw-value Sendable enum pattern (04-PATTERNS.md line 17)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-RESEARCH.md lines 283-395 — RESEARCH Pattern 1 has the verbatim `AttestationStatus` / `AttestationError` / `AttestationService` shape
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-CONTEXT.md lines 62-69 — D-09 six-value enum (note: spec lists 5 user-facing + simulatorBypass = 6 total)
  </read_first>
  <action>
    Create four new files in `validationLedger/Core/Attestation/`:

    **File 1: `AttestationStatus.swift`**
    - `public enum AttestationStatus: String, Sendable, Codable` with exactly 6 cases: `attested`, `unsupported`, `entitlementMissing`, `quotaExceeded`, `simulatorBypass`, `error`
    - Raw values are the lowercase enum names (Swift's default for String raw values).
    - Header doc-comment cites D-09 + lists the wire-format contract "attested | unsupported | entitlementMissing | quotaExceeded | simulatorBypass | error".
    - Add a Swift doc-comment on `simulatorBypass`: "`#if DEBUG && targetEnvironment(simulator)` path — emits fake attestationObject + attestedKeyId='sim-bypass-{installUUID}' per D-10. Production builds never ship this enum case in a real network call."

    **File 2: `TrustTier.swift`**
    - `public enum TrustTier: String, Sendable, Codable` with exactly 2 cases: `hardwareAttested`, `softwareOnly`.
    - Raw values lowercase-default.
    - Header doc-comment cites D-12: "Backend-driven trust tier returned by /device/register + /device/heartbeat. Client is a passive renderer — future tiers (attestedUnverified, revoked) can be added server-side without client changes, subject to `@unknown default:` update here."

    **File 3: `AttestationError.swift`**
    - `public enum AttestationError: Error, Sendable` with cases: `notSupported` (DCAppAttestService.isSupported == false), `featureUnsupported` (entitlement missing — maps to DCError.Code.featureUnsupported), `rateLimited` (serverUnavailable interpreted as quota per Pitfall 2), `serverUnavailable` (transient), `invalidKey` (key rotation required), `invalidInput`, `unknownSystemFailure`, and `underlying(NSError)` (catch-all for DCError.errorDomain edge cases).
    - No associated values on other cases — keep the surface clean.
    - Header doc-comment cites D-09: "Maps DCError.Code values to AttestationStatus via statusForDCError(_:) in DCAppAttestAttestationService (Plan 03). The .underlying case carries raw NSError for logging — NEVER expose its userInfo to Logger.LogField (FOUND-01 PII invariant)."

    **File 4: `AttestationService.swift`**
    - `public protocol AttestationService: AnyObject, Sendable` with four `async throws` methods mirroring RESEARCH Pattern 1 lines 305-322:
      - `func generateKeyIfNeeded() async throws -> (keyId: String, status: AttestationStatus)` — "D-01: once-per-install. Reads Keychain first; only calls DCAppAttestService.generateKey() if the attestedKeyId Keychain entry is absent. On failure routes the error through statusForDCError(_:)."
      - `func attestKey(keyId: String, challenge: Data) async throws -> Data` — "D-06: clientDataHash = SHA-256(challenge). Produces CBOR-encoded attestationObject for /device/register."
      - `func generateAssertion(keyId: String, challenge: Data) async throws -> Data` — "D-07: produces assertion for /device/heartbeat. Same clientDataHash rule as attestKey."
      - `func clearPersistedKeyId() throws` — "D-04: called ONLY on backend re-attestation trigger (attestationInvalid / nonceExpired / keyCompromised) OR by DEBUG-only DevMenu 'Re-attest now' row. NEVER called on logout (D-03: attestedKeyId preserved across logout)."
    - Header doc-comment mirrors KeyStoreProtocol.swift lines 1-10 style, naming the two impls Plan 03 will deliver: `DCAppAttestAttestationService` (device production) + `SimulatorBypassAttestationService` (#if DEBUG && targetEnvironment(simulator)).
    - File is NOT `@MainActor` at the protocol level — impls may opt in via `@MainActor` class annotations.
    - Import only `Foundation` (protocol surface — no DeviceCheck import at protocol file).
  </action>
  <verify>
    <automated>xcodebuild build -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15' -configuration Debug 2>&amp;1 | grep -E "(error|warning)" | head -50; grep -c "case attested\|case unsupported\|case entitlementMissing\|case quotaExceeded\|case simulatorBypass\|case error" validationLedger/Core/Attestation/AttestationStatus.swift; grep -c "case hardwareAttested\|case softwareOnly" validationLedger/Core/Attestation/TrustTier.swift; grep -cE "func generateKeyIfNeeded|func attestKey|func generateAssertion|func clearPersistedKeyId" validationLedger/Core/Attestation/AttestationService.swift</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedger/Core/Attestation/AttestationStatus.swift` exists.
    - Grep for `case attested`, `case unsupported`, `case entitlementMissing`, `case quotaExceeded`, `case simulatorBypass`, `case error` in AttestationStatus.swift returns all 6 matches.
    - Grep `"public enum AttestationStatus: String, Sendable, Codable"` in AttestationStatus.swift returns 1 match.
    - File `validationLedger/Core/Attestation/TrustTier.swift` exists; grep for `case hardwareAttested`, `case softwareOnly` returns both matches.
    - File `validationLedger/Core/Attestation/AttestationError.swift` exists; grep for `case underlying(NSError)` returns 1 match.
    - File `validationLedger/Core/Attestation/AttestationService.swift` exists.
    - Grep for `func generateKeyIfNeeded`, `func attestKey`, `func generateAssertion`, `func clearPersistedKeyId` in AttestationService.swift returns all 4 matches.
    - Grep `"protocol AttestationService: AnyObject, Sendable"` in AttestationService.swift returns 1 match.
    - Grep `"D-01"` and `"D-09"` and `"D-12"` each appear at least once across the four new files (decision-ID traceability in doc comments).
    - `xcodebuild build` on simulator scheme succeeds with zero new errors (may have pre-existing warnings from other files).
  </acceptance_criteria>
  <done>Types + protocol land. No call-site imports them yet (Plans 03 + 04 are the first consumers). AttestationStatus wire format matches D-09 exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Extend KeychainKey + KeychainScope for attestedKeyId/lastHeartbeatAt (D-03 invariant)</name>
  <read_first>
    - validationLedger/Core/Storage/Keychain/KeychainKey.swift — full file (current constants at lines 10-21 per 04-PATTERNS.md section 7)
    - validationLedger/Core/Storage/Keychain/KeychainScope.swift — full file (existing `.session` case at lines 16-35 per 04-PATTERNS.md section 8)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-CONTEXT.md line 41 (D-03: preserved across logout) + line 177-178 (Keychain key names verbatim)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-PATTERNS.md lines 359-411 — Pattern 7 + 8 for naming + doc-comment style
  </read_first>
  <action>
    **Edit `validationLedger/Core/Storage/Keychain/KeychainKey.swift`:**
    Append two new public static constants in the existing constant block (follow the existing dot-qualified naming pattern `device.*` / `session.*`):

    ```swift
    // Phase 4 DEV-04 (D-01, D-03): App Attest identifier + last-heartbeat-timestamp Keychain entries.
    // Both MUST NOT be members of KeychainScope.session — D-03 preserves attestedKeyId across logout
    // so the same install presents the same attestation after re-login. Raw values are referenced
    // by name in downstream plans (03, 06, 07, 09); do NOT rename.
    public static let attestedKeyId    = KeychainKey(rawValue: "device.attestedKeyId")
    public static let lastHeartbeatAt  = KeychainKey(rawValue: "device.lastHeartbeatAt")
    ```

    Raw values MUST be exactly `"device.attestedKeyId"` and `"device.lastHeartbeatAt"` (copy from CONTEXT.md lines 177-178 verbatim).

    **Edit `validationLedger/Core/Storage/Keychain/KeychainScope.swift`:**
    Do NOT modify the `contains(_:)` function body. Update the doc-comment on the `.session` case to explicitly enumerate the two new exclusions:

    ```swift
    /// All keys tied to an authenticated session (wiped on logout).
    /// Members: sessionToken, sessionRole, sessionUserID, biometricDomainState.
    /// Excluded from .session (intentional device-identity persistence):
    ///   - installUUID           (DEV-05 device identity — Phase 2)
    ///   - attestedKeyId         (DEV-04 D-03 — Phase 4, preserved across logout)
    ///   - lastHeartbeatAt       (DEV-04 D-07 — Phase 4, survives logout so next heartbeat is contiguous)
    case session
    ```

    The test `testAttestedKeyIdNotInSessionScope` that pins this invariant lands in Plan 02 (Wave 0 tests).
  </action>
  <verify>
    <automated>grep -c "attestedKeyId\s*=\s*KeychainKey(rawValue:\s*\"device.attestedKeyId\")" validationLedger/Core/Storage/Keychain/KeychainKey.swift; grep -c "lastHeartbeatAt\s*=\s*KeychainKey(rawValue:\s*\"device.lastHeartbeatAt\")" validationLedger/Core/Storage/Keychain/KeychainKey.swift; grep -c "D-03\|D-07\|DEV-04" validationLedger/Core/Storage/Keychain/KeychainScope.swift; xcodebuild build -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15' -configuration Debug 2>&amp;1 | grep -c "error:"</automated>
  </verify>
  <acceptance_criteria>
    - Grep `"device.attestedKeyId"` (exact string including dot) in KeychainKey.swift returns 1 match.
    - Grep `"device.lastHeartbeatAt"` in KeychainKey.swift returns 1 match.
    - Grep `"attestedKeyId"` in KeychainScope.swift returns >= 1 match (doc comment).
    - Grep `"lastHeartbeatAt"` in KeychainScope.swift returns >= 1 match.
    - The `.session` case body is unchanged (grep for the existing four-key list: `sessionToken, sessionRole, sessionUserID, biometricDomainState` still present in contains function).
    - `xcodebuild build` succeeds with 0 new errors.
  </acceptance_criteria>
  <done>KeychainKey has the two new constants with exact raw values from CONTEXT.md specifics; KeychainScope doc-comment explicitly excludes them from .session (code unchanged). The test pinning this invariant lands in Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Create validationLedger.entitlements + ADR 0005 + docs/attestation-rotation.md</name>
  <read_first>
    - docs/adr/0004-secure-enclave-two-key-pattern.md (full file) — section structure + frontmatter style (04-PATTERNS.md Pattern 17, lines 805-841)
    - docs/cert-rotation.md (full file) — runbook structure to mirror (04-PATTERNS.md Pattern 16, lines 765-801)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-CONTEXT.md lines 33-47 (D-01..D-04) + lines 72-73 (D-12)
    - .planning/phases/04-app-attest-physical-device-ci-hardening/04-RESEARCH.md lines 602-608 (Pitfall 3 — entitlement value in TF/App Store) + lines 67-81 (CLAUDE.md constraints distilled)
  </read_first>
  <action>
    **File 1: `validationLedger/validationLedger.entitlements`** (NEW, plist XML — see 04-PATTERNS.md Pattern 13 verbatim body):

    Write the exact plist content from 04-PATTERNS.md lines 681-689:
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <!-- Phase 4 DEV-04: App Attest environment entitlement.
             Value is 'development' for Xcode-direct dev + internal TestFlight.
             Apple IGNORES this value in TestFlight + App Store distributions and
             always routes to production — do NOT flip to 'production' for release.
             See docs/adr/0005-three-key-device-register-payload.md + 04-RESEARCH.md Pitfall 3. -->
        <key>com.apple.developer.devicecheck.appattest-environment</key>
        <string>development</string>
    </dict>
    </plist>
    ```

    Note: Wiring `CODE_SIGN_ENTITLEMENTS = validationLedger/validationLedger.entitlements` into `validationLedger.xcodeproj/project.pbxproj` is needed for the binary to carry the entitlement. Do this pbxproj edit in this task using `PBXBuildFile` / `buildSettings` entry for both Debug and Release configurations of the `validationLedger` target. Confirm via grep `CODE_SIGN_ENTITLEMENTS = validationLedger/validationLedger.entitlements;` appears in 2 configurations after edit.

    **File 2: `docs/adr/0005-three-key-device-register-payload.md`** (NEW):

    Mirror the 6-section structure from ADR 0004 exactly:
    - Frontmatter: `# ADR 0005: Three-key /device/register payload — deviceKey + authorizationKey + attestedKey`, `**Date:** 2026-04-22`, `**Status:** Accepted`, `**Supersedes:** — extends ADR 0004 (two-key pattern)`, `**Superseded by:** — (current)`
    - `## Context`: Quote CONTEXT.md D-02 verbatim. Explain Apple App Attest is the ONLY way to bind hardware attestation (Don't-Hand-Roll rationale); reusing deviceKey/authorizationKey for attestation is impossible — DCAppAttestService owns its own SE keypair opaquely. Three distinct keys serve three distinct roles.
    - `## Decision`: Bullet list:
      - `deviceKey` SPKI → device identity; ACL: `.devicePasscode`; DEV-01; passcode-only.
      - `authorizationKey` SPKI → sensitive-action signing; ACL: `.biometryCurrentSet`; DEV-02; invalidates on biometric re-enroll.
      - `attestedKeyId` + `attestationObject` → hardware attestation; managed by `DCAppAttestService`; DEV-04; ONCE per install (D-01); preserved across logout (D-03); regenerated ONLY on backend triggers attestationInvalid/nonceExpired/keyCompromised (D-04).
    - `## Consequences` / `### Positive`: Hardware-rooted proof; trust tier (D-12) decided server-side without client changes; rate-limit-friendly (D-01 once-per-install).
    - `## Consequences` / `### Negative`: Apple's undocumented quota (RESEARCH Pitfall 2); entitlement provisioning dependency (RESEARCH Pitfall 3); forever-dependent on Apple framework.
    - `## Consequences` / `### Acknowledged but NOT addressed`: Per-request assertions for M2+ sensitive actions (deferred to M2); attestation telemetry dashboard (deferred to observability phase).
    - `## Alternatives Considered`: (1) Reuse deviceKey as attestation key — REJECTED, framework owns its SE key. (2) Attestation assertion per request — REJECTED per D-07 heartbeat-only cadence. (3) Client-side challenge generation — REJECTED per D-05 backend-owned challenge.
    - `## Operational Notes`: `clearPersistedKeyId()` path is backend-driven; see docs/attestation-rotation.md.
    - `## References`: Link to CONTEXT D-01..D-12, REQUIREMENTS DEV-04, ADR 0004, RESEARCH lines 283-395, Apple DeviceCheck docs.

    **File 3: `docs/attestation-rotation.md`** (NEW):

    Mirror cert-rotation.md headers (04-PATTERNS.md section 16 lines 770-801):
    - `# Attestation Rotation Runbook`
    - `**Status:** ACTIVE — Phase 4 DEV-04 runbook. Review when backend emits attestationInvalid / nonceExpired / keyCompromised error codes.`
    - `**Canonical file to edit during rotation:** validationLedger/Core/Attestation/DCAppAttestAttestationService.swift` (clearPersistedKeyId → regenerate on next /device/register)
    - `## Why Client-Self-Rotation Is Forbidden` — D-04 rationale (quota pressure, Pitfall 2).
    - `## attestedKeyId Regeneration Procedure`:
      - Backend emits error code → Client receives error → clearPersistedKeyId() called → next /device/register triggers generateKey() fresh.
      - List the three canonical trigger codes: `attestationInvalid`, `nonceExpired`, `keyCompromised`.
      - Note: wire-format field name is notional per RESEARCH Open Question 1 — coordinate with backend team before Phase 4 execution; client emits generic `AttestationError.underlying(...)` case until confirmed.
    - `## Emergency Revoke Path` — If a fleet-wide compromise is suspected: backend returns `keyCompromised` on /device/heartbeat → client silently clearPersistedKeyId() + next cold-boot register regenerates. No user UI (RESEARCH Open Q4).
    - `## Manual Re-attestation (DEBUG-only)` — DevMenu "Re-attest now" row (Plan 07) triggers the same clearPersistedKeyId() + forced /device/register path, without waiting for a backend trigger. DEBUG builds only.
    - `## CI Checks` — AppAttestRoundTripTests (Plan 09) on device CI exercises the re-attest trigger via a MockURLProtocol-driven attestationInvalid response.
    - `## Related` — Link to RESEARCH Open Q1/Q4, CONTEXT D-04, ADR 0005, REQUIREMENTS DEV-04.
  </action>
  <verify>
    <automated>plutil -lint validationLedger/validationLedger.entitlements; grep -c "com.apple.developer.devicecheck.appattest-environment" validationLedger/validationLedger.entitlements; grep -c "development" validationLedger/validationLedger.entitlements; grep -c "CODE_SIGN_ENTITLEMENTS = validationLedger/validationLedger.entitlements" validationLedger.xcodeproj/project.pbxproj; test -f docs/adr/0005-three-key-device-register-payload.md &amp;&amp; grep -c "ADR 0005" docs/adr/0005-three-key-device-register-payload.md; test -f docs/attestation-rotation.md &amp;&amp; grep -c "attestationInvalid\|nonceExpired\|keyCompromised" docs/attestation-rotation.md</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint validationLedger/validationLedger.entitlements` prints "OK".
    - Grep `"com.apple.developer.devicecheck.appattest-environment"` in entitlements file returns 1 match.
    - Grep `"<string>development</string>"` in entitlements file returns 1 match (NOT "production").
    - Grep `"CODE_SIGN_ENTITLEMENTS = validationLedger/validationLedger.entitlements"` in project.pbxproj returns exactly 2 matches (Debug + Release configurations).
    - File `docs/adr/0005-three-key-device-register-payload.md` exists; grep `"ADR 0005"`, `"Status:** Accepted"`, `"deviceKey"`, `"authorizationKey"`, `"attestedKeyId"`, `"D-01"`, `"D-12"` each return >= 1 match.
    - File `docs/attestation-rotation.md` exists; grep `"attestationInvalid"`, `"nonceExpired"`, `"keyCompromised"`, `"clearPersistedKeyId"` each return >= 1 match.
    - Grep `"## Why Client-Self-Rotation Is Forbidden"` in docs/attestation-rotation.md returns 1 match.
  </acceptance_criteria>
  <done>Entitlement file wired into the Xcode project; ADR 0005 landed with 6-section structure citing D-01..D-12; attestation-rotation runbook landed with backend-trigger flow + manual DEBUG path + CI reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process ↔ Keychain | attestedKeyId + lastHeartbeatAt live in Keychain with `.afterFirstUnlockThisDeviceOnly` accessibility; crosses here on every read/write. |
| Binary ↔ Apple signing infrastructure | validationLedger.entitlements is embedded by codesign into the signed binary; crosses here at build time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-APP-ATTEST-02 | Information_Disclosure | Keychain-stored `attestedKeyId` on jailbroken device | mitigate | D-01 `.afterFirstUnlockThisDeviceOnly` accessibility + attestedKeyId alone is useless without the SE-held private key (Apple framework refuses to export). Phase 4 adds no new code that exports or displays the raw bytes. |
| T-APP-ATTEST-04 | Information_Disclosure | AttestationError enum + future logging call sites | mitigate | AttestationError's `underlying(NSError)` carries the raw error but AttestationService protocol never exposes attestationObject bytes through an error path; file doc-comments explicitly cite the "never log the blob" rule. Enforced downstream at Plan 03 (impl) + Plan 05 (tests). |
</threat_model>

<verification>
After all three tasks:
1. `xcodebuild build -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15' -configuration Debug` succeeds (0 new errors).
2. `plutil -lint validationLedger/validationLedger.entitlements` returns "OK".
3. The 6 AttestationStatus cases + 2 TrustTier cases + 4 AttestationService methods + 2 KeychainKey constants + 1 entitlement + 1 ADR + 1 runbook all exist and pass grep-verifiable acceptance criteria above.
4. No code in the file tree imports the new types yet — this is expected (Plans 03/04 are the first consumers).
</verification>

<success_criteria>
- All 9 files (4 new Attestation Swift + 2 Keychain extensions + 1 entitlement + 1 ADR + 1 runbook) exist.
- All D-01 / D-02 / D-03 / D-04 / D-09 / D-12 decisions have at least one artifact with a doc-comment citing the decision ID.
- AttestationStatus wire format matches D-09 exactly (6 cases).
- KeychainScope.session still excludes the 2 new keys (D-03 invariant — enforced by test in Plan 02, documented here).
- CODE_SIGN_ENTITLEMENTS set in project.pbxproj for both Debug and Release configurations.
- Project builds clean.
</success_criteria>

<output>
Create `.planning/phases/04-app-attest-physical-device-ci-hardening/04-01-SUMMARY.md`.
</output>
