---
phase: 10-per-role-tender-accept-reject
plan: 10
type: execute
wave: 7
depends_on:
  - 10-04
  - 10-06
  - 10-07
  - 10-08
  - 10-09
files_modified:
  - validationLedgerUITests/Loads/LoadActionFlowsTests.swift
  - .planning/phases/10-per-role-tender-accept-reject/10-MANUAL-TESTS.md
autonomous: true
requirements:
  - ACTION-01
  - ACTION-02
  - ACTION-03
  - ACTION-04
  - ACTION-05
  - ACTION-08
  - ACTION-09
user_setup: []

must_haves:
  truths:
    - "Per VALIDATION.md § XCUITest Smoke Flows lines 78-86: 6 XCUITest smoke flows cover the per-role action surface end-to-end through the simulator — broker happy-path tender, carrier accept (with pop-back list reflects new state), carrier reject, ACTION-04 unverified-counterparty hard-disable, rollback on server 500, double-submit prevented. The 6 flows are the minimum XCUITest coverage matrix."
    - "Per project memory `mock-kyc-status-verified-toggle` + the Phase 5 `-MockKYCStatusVerified` precedent: XCUITest sets `app.launchArguments.append(\"-MockKYCStatusVerified\")` to bypass the D-12 KYC gate and reach the role shell + load detail. Phase 10's 4 new toggles (Plan 08) are appended for the failure-injection flows."
    - "Per project memory `ios-test-suite-pitfalls`: every Phase 10 xcodebuild test command uses the scoped serial-simulator form (`-skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO`); bare `xcodebuild test` is FORBIDDEN."
    - "Per Phase 9 + 9.1 XCUITest precedent (`LoadDetailFlowTests.swift` + `RoleLoadsTabSmokeTests.swift`): the OTP-and-row-tap helper + the per-role launch-arg flow (`-MockOTPRoleForUITest`) are inlined — no shared helper file is invented (PATTERNS E11 lock per RESEARCH § Code Examples for Phase 9 precedent)."
    - "Per ACTION-09 + UI-SPEC line 537: after success the load list reflects the new state on pop-back; this is achieved by Phase 8's existing `viewWillAppear → fetchLoads()` (Plan 03 done step confirms `LoadListViewController.swift:381` is preserved). The XCUITest flow `test_carrierCanAcceptActiveTender` is the integration assertion."
    - "Per **D-18**: list-refresh-on-pop-back leverages Phase 8's existing `LoadListViewController.viewWillAppear → fetchLoads()` at `LoadListViewController.swift:381` — NO push-notification observer between detail and list, NO custom propagation channel. The XCUITest `test_carrierCanAcceptActiveTender` pop-back arm exercises this Phase-8-by-construction contract. Cost: one extra `GET /loads/{role}` per pop-back from a screen the user acted on — acceptable for v1.1 mock-only against ~12 loads."
    - "Per VALIDATION.md § Manual-Only Verifications + the 65-cell matrix legibility row: the per-task manual checklist file `10-MANUAL-TESTS.md` documents the 3 device-UAT signoffs (toast animation feel + tender sheet detent ergonomics + matrix legibility at Default + Large Dynamic Type) that the snapshot suite cannot assert."
  artifacts:
    - path: "validationLedgerUITests/Loads/LoadActionFlowsTests.swift"
      provides: "6 XCUITest smoke flows matching VALIDATION.md § XCUITest Smoke Flows table verbatim; each flow drives the simulator through OTP → role shell → loads tab → load detail row tap → action button tap (+ tender sheet flow where applicable); asserts the predicted/swapped state + the pop-back list state where applicable"
      contains: "LoadActionFlowsTests"
    - path: ".planning/phases/10-per-role-tender-accept-reject/10-MANUAL-TESTS.md"
      provides: "Phase 10 device-UAT checklist for the 3 Manual-Only verifications + an end-to-end happy-path smoke for human eye coverage"
      contains: "Manual Test Checklist"
  key_links:
    - from: "validationLedgerUITests/Loads/LoadActionFlowsTests.swift"
      to: "validationLedger/Core/Networking/Mock/MockActionFailureToggles.swift (Plan 08)"
      via: "the rollback flow + the double-submit flow append the 4 launch-arg flags from Plan 08 to drive failure-injection / latency"
      pattern: "-MockAction"
    - from: "validationLedgerUITests/Loads/LoadActionFlowsTests.swift"
      to: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      via: "XCUITest queries via accessibility identifiers — the per-button + per-sheet a11y identifiers Plan 04 + Plan 06 set are the test seams"
      pattern: "app\\.buttons\\["
---

<objective>
Land the 6 XCUITest smoke flows per VALIDATION.md § XCUITest Smoke Flows lines 78-86; each flow drives the simulator end-to-end through OTP → role shell → loads tab → load-detail → action button tap (+ tender sheet flow where applicable), using the 4 DEBUG launch-arg toggles from Plan 08 to drive failure injection where needed. Author `10-MANUAL-TESTS.md` with the 3 Manual-Only verifications from VALIDATION.md plus an end-to-end happy-path checklist for device UAT. This is the Phase 10 close-out plan — after this plan, every ACTION-XX requirement has at least one automated XCUITest verification covering the on-device flow.

Purpose: Snapshot tests (Plan 09) prove the visual contract; XCUITest proves the INTERACTION contract — every action button tap actually drives the predicted state forward, the rollback toast actually slides in, the list actually refreshes on pop-back. The 6 flows are the matrix VALIDATION.md owns; they map to the 9 ACTION-XX requirements via the per-flow coverage column in the validation map.

Output: 1 new XCUITest file; 1 new manual-test checklist document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md
@.planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md
@.planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md
@.planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md
@.planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md
@.planning/phases/10-per-role-tender-accept-reject/10-08-SUMMARY.md
@.planning/phases/10-per-role-tender-accept-reject/10-09-SUMMARY.md

# XCUITest analog files
@validationLedgerUITests/Loads/LoadDetailFlowTests.swift
@validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift

# Source files (for the a11y identifiers the tests query against)
@validationLedger/Features/Loads/Detail/LoadActionsView.swift
@validationLedger/Features/Loads/Detail/TenderSheetViewController.swift
@validationLedger/Features/Loads/Detail/LoadActionToastBannerView.swift

<interfaces>
<!-- VALIDATION.md § XCUITest Smoke Flows table (LOCKED — this plan implements verbatim) -->

| Flow | Coverage |
| test_brokerCanTenderToVerifiedCarrier | ACTION-04 success path + idempotency header in dispatched request |
| test_carrierCanAcceptActiveTender | ACTION-02 (forward path) + ACTION-09 (pop-back list reflects new state) |
| test_carrierCanRejectActiveTender | ACTION-03 (returns to .posted) + ACTION-09 |
| test_unverifiedCounterpartyHardDisable | ACTION-04 (gate, with inline reason) |
| test_rollbackOnServerError500 | ACTION-05 (DEBUG -MockActionServerError500 toggle drives the failure-injection path) |
| test_doubleSubmitPrevented | ACTION-05 in-flight gating |

<!-- XCUITest launch-arg conventions (project memory + Phase 5/9.1 precedents) -->

OTP / role bypass per Phase 9 analog (`LoadDetailFlowTests.swift:64-71`):
```swift
private func launch(role: String) -> XCUIApplication {
    let app = XCUIApplication()
    app.launchArguments = ["-MockOTPRoleForUITest", role]
    app.launchArguments.append("-MockKYCStatusVerified")  // bypass D-12 KYC gate
    app.launch()
    return app
}
```

Phase 10 failure-injection toggles (Plan 08):
- `-MockActionConflict409`
- `-MockActionValidation422`
- `-MockActionServerError500`
- `-MockActionLatencySlow`

Existing pattern for the rollback flow: `app.launchArguments.append(contentsOf: ["-MockActionServerError500", "-MockActionLatencySlow"])` — failure + latency together so the human eye sees the optimistic-predict THEN the rollback.

PATTERNS.md §15 (XCUITest):
- `executionTimeAllowance = 90` (matches Phase 9 WR-05 fix for 5-role iteration on physical-device CI)
- Three minimum flows per CONTEXT § Claude's Discretion line 147; this plan ships 6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the 6 XCUITest smoke flows in LoadActionFlowsTests.swift</name>
  <files>validationLedgerUITests/Loads/LoadActionFlowsTests.swift</files>
  <read_first>
    - validationLedgerUITests/Loads/LoadDetailFlowTests.swift (FULL — Phase 9 LOAD-05 analog; mirror the launch() + driveFullOTPFlow() helpers VERBATIM per PATTERNS E11 — no shared helper file)
    - validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift (FULL — Phase 8 5-role flow analog)
    - validationLedger/Features/Loads/Detail/LoadActionsView.swift (Plan 04 output — find every `accessibilityIdentifier` set on buttons; the XCUITest queries against these. If Plan 04 did not set explicit identifiers, you may need to FIRST add them — but Plan 04 was instructed to set per-button a11y labels per UI-SPEC line 574-577; XCUITest queries against `app.buttons["Tender"]` by label work even without an identifier)
    - validationLedger/Features/Loads/Detail/TenderSheetViewController.swift (Plan 06 output — the file header has an a11y-identifier namespace block: `load-detail.tender-sheet`, `…/carrier-row.<partyID>`, `…/deadline-chip.<token>`, `…/send-button` — query against these)
    - validationLedger/Features/Loads/Detail/LoadActionToastBannerView.swift (Plan 07 output — the banner's accessibilityLabel IS its localized text; query `app.staticTexts["Couldn't accept this tender. Try again."]` for assertion)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §15 (verbatim XCUITest analog shape lines 622-639)
    - .planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md § XCUITest Smoke Flows (the 6 flow descriptions)
  </read_first>
  <behavior>
    Tests in LoadActionFlowsTests (NEW — write first):

    Setup helpers (inlined per PATTERNS E11 — copy from `LoadDetailFlowTests.swift:64-100`, do NOT extract to a shared file):
    - `private func launch(role: String, additionalArgs: [String] = []) -> XCUIApplication` — launches with `-MockOTPRoleForUITest <role> -MockKYCStatusVerified` + appended args.
    - `private func driveFullOTPFlow(_ app: XCUIApplication)` — enters mock OTP, taps verify, lands on role shell (verbatim from Phase 9 analog).
    - `private func openLoadDetail(_ app: XCUIApplication, loadID: String)` — taps the loads tab, scrolls to the row with the given loadID, taps it; waits for the load detail to appear (`app.staticTexts[loadID]` or similar).

    Each test sets `executionTimeAllowance = 90` per PATTERNS §15.

    **Test 1 (`test_brokerCanTenderToVerifiedCarrier`)** — ACTION-04 success path + ACTION-08 idempotency through the wire:
    - Launch as "broker"; drive OTP; open the load-detail for a `.posted` load (planner picks a load ID from the Phase 7 fixture set that decodes to `.posted` — `VL-001` or similar)
    - Assert the action region's Tender button exists and is enabled (`app.buttons["Tender"].isEnabled == true`)
    - Tap Tender; wait for the tender sheet (`app.otherElements["load-detail.tender-sheet"].waitForExistence(timeout: 3)`)
    - Tap the first verified carrier row (`app.cells.containing(.staticText, identifier: "Acme Logistics Inc.").firstMatch.tap()`)
    - Assert the Send button is enabled
    - Tap Send; wait for the sheet to dismiss (`app.otherElements["load-detail.tender-sheet"].waitForNonExistence(timeout: 5)`)
    - Assert the load status badge in the pinned summary header now shows the .tendered state (`app.staticTexts["Tendered"].waitForExistence(timeout: 3)`)
    - NOTE: The idempotency-header assertion (Plan 08 Test 1 of MockLoadActionDispatchTests) covers the wire-level invariant; this XCUITest only proves the user-flow side.

    **Test 2 (`test_carrierCanAcceptActiveTender`)** — ACTION-02 + ACTION-09 pop-back list:
    - Launch as "carrier"; drive OTP; open a `.tendered` load (planner picks an appropriate fixture load ID, e.g. one preconfigured to `.tendered` in the carrier-role fixture)
    - Assert Accept and Reject buttons are visible (`app.buttons["Accept"].exists && app.buttons["Reject"].exists`)
    - Tap Accept; wait for the status badge to update to "Accepted" in the pinned header
    - Pop back to the list (`app.navigationBars.buttons.firstMatch.tap()` for the back button)
    - Wait for the list to be visible again
    - Scroll to the same load's row; assert its status badge shows "Accepted" (the pop-back list refresh per Phase 8's `viewWillAppear`)

    **Test 3 (`test_carrierCanRejectActiveTender`)** — ACTION-03 + ACTION-09:
    - Launch as "carrier"; OTP; open a `.tendered` load
    - Tap Reject; wait for status update (predicted .rejected, server may return .posted per Phase 7 D-04 — the test asserts the eventual server-returned state, not the prediction; if the server returns .posted, the badge reads "Posted"; if .rejected, the badge reads "Rejected" — read the action-success fixture for the reject case to determine the expected post-state, then assert that)
    - Pop back to list; assert the row reflects the post-action state

    **Test 4 (`test_unverifiedCounterpartyHardDisable`)** — ACTION-04 load-level gate:
    - Launch as "broker"; OTP; open a load where `tenderEligibility.canTender == false` (planner picks the fixture load ID with this property — likely a load whose chain has a flagged broker or unverified carrier; check the Phase 7 fixtures to identify one, OR construct a per-test mock that injects a Load fixture with `canTender: false`)
    - Assert Tender button exists but is DISABLED (`app.buttons["Tender"].isEnabled == false`)
    - Assert the inline disabled-reason label is visible (`app.staticTexts.containing(.staticText, identifier: "USDOT").firstMatch.exists` or assert the literal `tenderEligibility.disabledReason` string)
    - Tap on the disabled Tender button (defensive — should be no-op); assert the sheet is NOT presented (`app.otherElements["load-detail.tender-sheet"].waitForNonExistence(timeout: 2)`)

    **Test 5 (`test_rollbackOnServerError500`)** — ACTION-05 + Plan 08 toggle integration:
    - Launch as "carrier" with `additionalArgs: ["-MockActionServerError500", "-MockActionLatencySlow"]`
    - OTP; open a `.tendered` load
    - Tap Accept; immediately (during the ~1.5s latency delay) assert the action region is in-flight (`app.activityIndicators.firstMatch.exists`); the chain overlay is visible (`app.otherElements.matching(identifier: "chain-updating-overlay").firstMatch.exists` — Plan 07 should set this identifier OR query the activity indicator inside the overlay region)
    - Wait for the server response (the 500 fires after the latency); assert the chain overlay dismisses; assert the toast banner slides in (`app.staticTexts["Couldn't accept this tender. Try again."].waitForExistence(timeout: 5)`); assert the action region restored (Accept + Reject buttons re-enabled)
    - Wait for the 3.5s auto-dismiss; assert the toast is gone (`app.staticTexts["Couldn't accept this tender. Try again."].waitForNonExistence(timeout: 5)`)
    - Assert the load status badge is BACK to "Tendered" (rollback completed)

    **Test 6 (`test_doubleSubmitPrevented`)** — ACTION-05 in-flight gating:
    - Launch as "carrier" with `additionalArgs: ["-MockActionLatencySlow"]` (slow so we can attempt the second tap during in-flight)
    - OTP; open a `.tendered` load
    - Tap Accept; immediately (during the in-flight) attempt to tap Accept AGAIN (`app.buttons["Accept"].tap()` — the button should be disabled and the tap a no-op)
    - Also attempt to tap Reject during in-flight (`app.buttons["Reject"].tap()` — also disabled)
    - Wait for the server response (success after latency); assert the state transitioned exactly ONCE to .accepted (not twice; not into .rejected from the spurious Reject tap)
    - This is a SOFT assertion — XCUITest cannot easily count Mock calls; the test asserts the post-action state is the correct single transition. The wire-level "exactly one POST" assertion is implicit via the BL-01 cancel-and-replace in the VM + the IdempotencyInterceptor in the network stack (both already tested at the unit level in Plans 03 + 08).
  </behavior>
  <action>
    Create `validationLedgerUITests/Loads/LoadActionFlowsTests.swift`. XCUITest class. Implement the 6 tests above plus the 3 inlined helpers (launch, driveFullOTPFlow, openLoadDetail).

    The exact load IDs to use depend on the Phase 7 fixture set — read `validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift` (or the role-list fixtures it inlines) to find:
    - A `.posted` load for the broker (Test 1, Test 4)
    - A `.tendered` load for the carrier (Tests 2, 3, 5, 6)
    - A load with `tenderEligibility.canTender == false` (Test 4) — if no such load exists in the fixtures, ADD ONE in this task by adding a new fixture entry to `MockLoadFixtureRegistry` OR by setting the toggle in the test's launch-args context (DEBUG-only). If the fixtures don't have one, the cleanest option is: add a 5th DEBUG launch arg `-MockLoadDetailCanTenderFalse` that wraps a single load fixture with `canTender: false` — but that's beyond this plan's scope. PREFERRED: use a Phase 7 named-load fixture that already has the chameleon-carrier fraud archetype (likely the `load-detail-VL-chameleon-cargo.json` if Phase 7 created one — read the fixture directory to confirm). If neither exists, document in the SUMMARY a follow-up task: "Phase 10 follow-up: add a fixture load with `tenderEligibility.canTender == false` for Test 4 — Plan 10 Test 4 is currently a soft skip until this fixture lands."

    For Test 5's chain-overlay identifier: ensure Plan 07's `mountChainOverlayIfNeeded()` sets `overlay.accessibilityIdentifier = "chain-updating-overlay"` — if Plan 07 did not, ADD THIS LINE (one-line edit; do NOT cascade other changes). The grep-able identifier is the XCUITest seam.

    Document the fixture-ID assumptions in a test-file header comment: "// FIXTURE DEPENDENCIES: Tests 1+4 assume a .posted broker load (e.g. VL-001); Tests 2/3/5/6 assume a .tendered carrier load (e.g. VL-002); Test 4 assumes a load with tenderEligibility.canTender == false. If the fixture IDs change in a future phase, update the loadID constants below."

    DO NOT extract the helpers to a shared file (PATTERNS E11 lock per Phase 9 precedent — inline duplication is intentional so each XCUITest file is self-contained and Phase-version-pinned).
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerUITests/Loads/LoadActionFlowsTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -80</automated>
  </verify>
  <done>
    - All 6 XCUITest flows green
    - Source assertion: `grep -c 'func test_' validationLedgerUITests/Loads/LoadActionFlowsTests.swift` returns at least `6`
    - Source assertion: `grep -c '-MockActionServerError500' validationLedgerUITests/Loads/LoadActionFlowsTests.swift` returns at least `1` (Test 5)
    - Source assertion: `grep -c 'executionTimeAllowance = 90' validationLedgerUITests/Loads/LoadActionFlowsTests.swift` returns at least `6` (per test method — Phase 9 WR-05 precedent)
    - Source assertion: `grep -c 'driveFullOTPFlow' validationLedgerUITests/Loads/LoadActionFlowsTests.swift` returns at least `6` (each test uses the inlined helper — PATTERNS E11 inline-helper convention)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 10-MANUAL-TESTS.md device-UAT checklist</name>
  <files>.planning/phases/10-per-role-tender-accept-reject/10-MANUAL-TESTS.md</files>
  <read_first>
    - .planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md § Manual-Only Verifications lines 105-113 (the 3 manual items)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-MANUAL-TESTS.md (if it exists — Phase 9 close-out analog; mirror the file structure)
    - Phase memories: `phase-9-execution-closeout`, `phase-9.1-execution-closeout` (device-UAT pattern — what the human reviewer is asked to do on the physical device)
  </read_first>
  <behavior>
    Document structure:

    1. **Header**: `# Phase 10 — Manual Test Checklist` + phase / plan / date metadata.
    2. **Purpose**: short paragraph naming the 3 Manual-Only verifications + the end-to-end happy-path smoke that complements the XCUITest flows on a real device.
    3. **Setup**: install build to device via TestFlight or Xcode device deploy; sign in as the test user; navigate to a `.posted` load as broker (the test seed data).
    4. **Section 1 — Toast banner animation feel** (VALIDATION.md row 1):
       - Step: enable the DEBUG menu (or set launch args via Xcode scheme) for `-MockActionServerError500`
       - Step: tap Accept on a `.tendered` load (or Tender on a `.posted` load)
       - Acceptance: toast slides up over the chain overlay, dwells ~3s, fades smoothly; the haptic fires at slide-in; the swipe-up-to-dismiss works
       - Acceptance: light + dark mode both look correct
    5. **Section 2 — Tender sheet detent ergonomics** (VALIDATION.md row 2):
       - Step: tap Tender on a `.posted` load
       - Acceptance: sheet sits in lower 50% of screen (`.medium` detent); reachable with thumb on iPhone 17 / iPhone 16 / iPhone SE-class devices; the date picker is reachable with the thumb
       - Acceptance: dragging the grabber up to `.large` detent works; the picker remains usable
    6. **Section 3 — 65-cell matrix legibility at Default + Large Dynamic Type** (VALIDATION.md row 3):
       - Step: after Plan 09 baselines are committed, spot-check 5 rows (one per role) in `validationLedgerTests/__Snapshots__/LoadActionBarSnapshotMatrix/`
       - Acceptance: each cell reads cleanly at Default Dynamic Type
       - Step: change Dynamic Type to Large via iOS Settings → Accessibility → Display & Text Size → Larger Text; relaunch the app
       - Acceptance: action region's button row flips to vertical axis at accessibilityLarge+; titles don't truncate
    7. **Section 4 — End-to-end happy-path device flow** (covers ACTION-01..09 from a single user's perspective):
       - Sign in as broker; open a .posted load; tap Tender; pick Acme; pick 1-day deadline; tap Send; wait for sheet dismiss; assert pinned summary header shows Tendered.
       - Sign out; sign in as carrier; open the load list; assert the same load row shows Tendered; tap into detail.
       - Tap Accept; wait for status update; pop back to list; assert row shows Accepted.
       - Tap into detail again; tap "Dispatch"; wait for transition to .dispatched. Tap "Mark in transit". Tap "Mark delivered". End-of-flow assertion: detail shows Delivered, action region shows the terminal-state caption.
    8. **Sign-off**: a checkbox table for the human reviewer to tick off each section + a date + reviewer initials.
  </behavior>
  <action>
    Write the markdown file with the structure above. Match the style of `09-MANUAL-TESTS.md` if it exists; otherwise use the structure above. Keep it under 200 lines. The file is committed to the phase directory for the future `/gsd:verify-work` reviewer to use.

    Include a footer: "After completing this checklist, mark the phase as `human_verified` in STATE.md and proceed to `/gsd:close-phase`."
  </action>
  <verify>
    <automated>test -f .planning/phases/10-per-role-tender-accept-reject/10-MANUAL-TESTS.md && wc -l .planning/phases/10-per-role-tender-accept-reject/10-MANUAL-TESTS.md | awk '$1 >= 60 && $1 <= 250 { print "OK"; exit 0 } { print "FAIL: line count " $1 " out of range [60, 250]"; exit 1 }'</automated>
  </verify>
  <done>
    - `10-MANUAL-TESTS.md` exists at the correct path, 60-250 lines (covers the 3 manual items + end-to-end happy-path without being a novel)
    - File includes the 4 sections + setup + sign-off table
    - File references the Plan 09 baselines directory by path
    - File documents the launch-arg toggle workflow (`-MockActionServerError500` etc.) so the human reviewer can exercise the rollback path
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XCUITest simulator → app launch | DEBUG launch args (Phase 5 + Plan 08) drive the mock surface; production app sees none of them. |
| XCUITest queries → a11y identifiers | Tests query against a11y labels + identifiers set by Plan 04 + Plan 06 + Plan 07; if a future plan removes an identifier, the test fails loudly (good — that's the regression signal). |
| Manual test checklist → human reviewer | Device QA reviewer signs off on the 3 Manual-Only items; without sign-off the phase cannot close. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 (end-to-end coverage) | Tampering | Rollback path regressed in a future plan but not caught by unit tests alone | mitigate | Test 5 of LoadActionFlowsTests drives the full simulator stack: launch args set failure-injection + latency, action tap, observe spinner, observe toast appearance, observe auto-dismiss, observe restored state. Catches integration regressions unit tests miss. |
| T-10-08 (end-to-end coverage) | Tampering | Idempotency interceptor stops firing in production builds | partial mitigate | Test 1 of LoadActionFlowsTests drives the broker-tender-to-verified flow end-to-end; the IdempotencyInterceptorRegistrationTests + MockLoadActionDispatchTests (Plan 08) cover the wire-level invariant; XCUITest covers the user-visible side. Both layers together = full coverage. |
| T-10-01 (UI regression) | Elevation of Privilege | A future plan changes the per-role policy and the wrong button shows | mitigate | XCUITest flows query against button titles ("Tender", "Accept", "Reject", etc.); a wrong-role policy regression makes the test fail (e.g. Test 2 fails if Carrier no longer sees Accept on .tendered). |
| T-10-09 | Tampering | Stale list state on pop-back | mitigate | Test 2 of LoadActionFlowsTests explicitly pops back and asserts the row's status badge — the `viewWillAppear → fetchLoads` integration is the assertion. |
| T-10-PR-01 | Repudiation | Human reviewer skips manual checklist | accept (process control) | The phase close-out workflow (`/gsd:verify-work` + `/gsd:close-phase`) requires the human-verified flag; this is an organizational control, not a code defense. |
| T-10-PR-SC | Tampering | Package legitimacy | accept | Phase 10 installs ZERO new packages. N/A. |
</threat_model>

<verification>
After both tasks:

`xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerUITests/Loads/LoadActionFlowsTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -80`

All 6 XCUITest flows green.

`xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -40` — FULL SUITE: every Phase 7 / 8 / 9 / 9.1 / 10 test green. This is the phase-final verification.

Per VALIDATION.md § Validation Sign-Off: every ACTION-XX has at least one `<automated>` verify, no 3 consecutive tasks without automated verify, Wave 0 scaffolds landed alongside implementation in Wave 1, no watch-mode flags, snapshot matrix recorded + verified.
</verification>

<success_criteria>
- 6 XCUITest flows from VALIDATION.md § XCUITest Smoke Flows table all green on simulator
- Each flow uses `executionTimeAllowance = 90` per PATTERNS §15
- The rollback flow uses `-MockActionServerError500 -MockActionLatencySlow` per CONTEXT § Claude's Discretion line 147
- `10-MANUAL-TESTS.md` exists with the 3 Manual-Only items + end-to-end happy-path device flow
- Full project test suite green (Phase 7 + 8 + 9 + 9.1 + 10 all together)
- Every ACTION-XX requirement (01..09) has at least one automated test (unit + snapshot + XCUITest tiers covered)
</success_criteria>

<output>
Create `.planning/phases/10-per-role-tender-accept-reject/10-10-SUMMARY.md` with: the 6 XCUITest flow titles + the per-test ACTION-XX coverage mapping (mirrors VALIDATION.md § XCUITest Smoke Flows table), the manual-test checklist sign-off pattern, the full suite test count (Phase 10 tests added × Phase 7-9 still green), and the phase close-out checklist anchored to ROADMAP §Phase 10's 5 success criteria.
</output>
