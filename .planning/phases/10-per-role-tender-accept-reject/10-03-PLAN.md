---
phase: 10-per-role-tender-accept-reject
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift
  - validationLedger/App/AppContainer.swift
  - validationLedgerTests/Loads/LoadDetailViewModelActionTests.swift
  - validationLedgerTests/Loads/LoadDetailViewModelRollbackTests.swift
autonomous: true
requirements:
  - ACTION-01
  - ACTION-02
  - ACTION-03
  - ACTION-04
  - ACTION-05
  - ACTION-08
user_setup: []

must_haves:
  truths:
    - "Per D-16: a single `LoadDetailViewModel` owns the screen's state — the State enum gains `.actionInFlight(predicted, frozenChain, action)` and `.actionFailed(rollbackTo, frozenChain, errorCopyKey)`, and a new `submit(action:body:) async` method drives the predict → 200 → loaded / predict → error → rollback transition. NO separate `LoadActionViewModel`."
    - "Per D-13: the chain-of-trust is NEVER predicted — `.actionInFlight` carries the FROZEN pre-tap chain, and only `.loaded(response.load, response.chainOfTrust)` (success) or `.actionFailed(rollbackTo: preLoad, frozenChain: preChain, ...)` (error) transitions the chain."
    - "Per D-17 + ACTION-08: the cancel-and-replace task lifecycle (S-1 — `actionTask?.cancel()` before any state mutation; `Task.isCancelled` checkpoint after the network hop; same in the `catch` arm) extends to actions — Pitfall 9 (rapid back-tap mid-action) inherits the Phase 9 safety."
    - "Per D-22 + Pitfall 4: `AppContainer.makeLoadDetailScreen(loadID:)` becomes `makeLoadDetailScreen(loadID:role:)`; the `LoadListViewController` factory closure (lines 255-258) captures the outer `role` and passes it through; the VM stores `role` as a designated-initializer parameter and the call-site in Plan 04 reads `viewModel.role` for the policy lookup. NO window where `role` is plumbed but unconsumed (D-22 / specifics line 271-272)."
    - "Per **D-23**: the role passed into `makeLoadDetailScreen(loadID:role:)` is fixed for that detail screen's lifetime — NOT client-changeable on the detail screen. This mirrors the v1.0 SHELL-04 lock (role not client-changeable at the navigation level). A user 'switching roles' requires logout + re-login. The VM exposes role as a non-mutable property (let or read-only computed)."
    - "Per T-09-04 / T-08-08 view-layer lock (extended): every logger call in `submit(action:body:)` and `performAction(...)` uses `fields: [:]`; the rollback `errorCopyKey` is one of the 6 LOCKED keys per UI-SPEC line 343-348 (`loads.actions.error.<action>_failed`); server-supplied error text is LOGGED at most via classification (409/422/500/network) but never rendered or placed in `fields`."
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift"
      provides: "State enum grows from 3 to 5 cases; new `private let role: Role` stored property; new designated init `init(loadID:role:apiClient:logger:)`; new `submit(action:body:) async` method; new `private func performAction(...)` private body; new `private var actionTask: Task<Void, Never>?` (twin of `fetchTask`)"
      contains: "actionInFlight"
    - path: "validationLedger/App/AppContainer.swift"
      provides: "`makeLoadDetailScreen(loadID:)` signature evolves to `makeLoadDetailScreen(loadID:role:)`; the factory closure inside `makeLoadListScreen(role:)` (lines 255-258) becomes `{ [weak self] loadID in self?.makeLoadDetailScreen(loadID: loadID, role: role) }`"
      contains: "makeLoadDetailScreen(loadID: String, role: Role)"
    - path: "validationLedgerTests/Loads/LoadDetailViewModelActionTests.swift"
      provides: "Forward-path coverage: submit drives predict transition; 200 swaps to loaded; cancel-and-replace mid-flight"
      contains: "LoadDetailViewModelActionTests"
    - path: "validationLedgerTests/Loads/LoadDetailViewModelRollbackTests.swift"
      provides: "Error-path coverage: 4xx and 5xx and URLError each transition to `.actionFailed(rollbackTo:..., errorCopyKey:...)` with the correct per-action key; cancelled task does not overwrite a fresher predicted state; T-09-04 lock — `fields: [:]` discipline asserted via a logger spy that records the fields dict"
      contains: "LoadDetailViewModelRollbackTests"
  key_links:
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift"
      to: "validationLedger/Core/Load/LoadActionPredictor.swift"
      via: "submit() calls LoadActionPredictor.predict(...) to derive the predicted Load"
      pattern: "LoadActionPredictor\\.predict"
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift"
      to: "validationLedger/Core/Networking/Endpoints/LoadActionEndpoint.swift"
      via: "submit() dispatches via apiClient.request(LoadActionEndpoint(...)) — IdempotencyInterceptor auto-injects the header"
      pattern: "LoadActionEndpoint"
    - from: "validationLedger/App/AppContainer.swift"
      to: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift"
      via: "makeLoadDetailScreen passes role through to the new VM designated init"
      pattern: "LoadDetailViewModel\\(loadID:.*role:"
---

<objective>
Extend `LoadDetailViewModel` with the action-submission state machine per D-16 — two new State cases (`.actionInFlight`, `.actionFailed`), a new `submit(action:body:) async` method that follows the BL-01 cancel-and-replace pattern, a new `private func performAction(...)` body mirroring `performFetch()` line-for-line, the per-action error-key resolver, and the `role: Role` designated-init parameter (D-22 — the structural surprise of Phase 10). Close the role-propagation loop in `AppContainer.makeLoadDetailScreen` so the new VM signature lands in the SAME plan as a consumer-ready surface (per CONTEXT § Specifics: "separating it would create a window where the detail VC has `role:` plumbed but no consumer" — Plan 04 IS the consumer, this plan plumbs).

Purpose: ACTION-08 (optimistic UI rollback) and ACTION-01..05 (per-role action submission) require the VM to be the single state-machine owner. The Phase 9 D-20 state-machine + isHidden-toggled-subviews architecture extends cleanly — the VC's `render(state:)` in Plan 04 will add two new arms that re-render against the predicted Load (for `.actionInFlight`) or the rollback Load (for `.actionFailed`). The role plumb is the structural prerequisite for Plan 04's `LoadActionsView.configure(actions: RoleLoadPolicy.availableActions(for: viewModel.role, in: load), ...)` call shape.

Output: 2 modified files (LoadDetailViewModel, AppContainer); 2 new test files covering the forward and rollback paths exhaustively.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-per-role-tender-accept-reject/10-CONTEXT.md
@.planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md
@.planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md
@.planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md
@.planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md
@.planning/phases/10-per-role-tender-accept-reject/10-01-SUMMARY.md
@.planning/phases/10-per-role-tender-accept-reject/10-02-SUMMARY.md

# Files this plan modifies (read the WHOLE file before editing)
@validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift
@validationLedger/App/AppContainer.swift

# Frozen contract files this plan reads
@validationLedger/Core/Load/Load.swift
@validationLedger/Core/Load/ChainOfTrust.swift
@validationLedger/Core/Load/LoadAction.swift
@validationLedger/Core/Networking/Endpoints/LoadActionEndpoint.swift
@validationLedger/Core/Networking/Interceptors/IdempotencyInterceptor.swift
@validationLedger/Roles/Role.swift

# Test file analog
@validationLedgerTests/Loads/LoadDetailViewModelTests.swift

<interfaces>
<!-- Existing surface the VM extends and the call sites that consume it -->

From `validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` (CURRENT):
```swift
@MainActor public final class LoadDetailViewModel {
    public enum State: Equatable, Sendable {
        case loading
        case loaded(Load, ChainOfTrust)
        case error(message: String)
    }
    public init(loadID: String, apiClient: APIClient, logger: (any Logger)? = nil)
    public func fetchLoadDetail() async   // BL-01 cancel-and-replace; see lines 135-206
    public var state: State { get }
    public var onStateChange: ((State) -> Void)?
    // existing extensions: Load Equatable, ChainOfTrust Equatable (lines 253-279)
}
```

From `validationLedger/App/AppContainer.swift` lines 255-258 + 283-294 (CURRENT):
```swift
let detailFactory: (String) -> UIViewController = { [weak self] loadID in
    guard let self else { return UIViewController() }
    return self.makeLoadDetailScreen(loadID: loadID)
}
// ...
@MainActor
func makeLoadDetailScreen(loadID: String) -> UIViewController {
    let featureLogger = OSLogLoggerImpl(subsystem: LoggingSubsystem.app, category: "feature.loads")
    let viewModel = LoadDetailViewModel(loadID: loadID, apiClient: apiClient, logger: featureLogger)
    return LoadDetailViewController(viewModel: viewModel)
}
```

From `validationLedger/Core/Networking/Endpoints/LoadActionEndpoint.swift`:
```swift
public struct LoadActionEndpoint: APIEndpoint {
    public struct Response: Decodable, Sendable {
        public let load: Load
        public let chainOfTrust: ChainOfTrust
    }
    public struct RequestBody: Encodable, Sendable { let actorRole: Role; let targetPartyID: String?; let respondByAt: Date?; let note: String? }
    public init(loadID: String, action: LoadAction, body: RequestBody)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend LoadDetailViewModel state machine + add `submit(action:body:)` + plumb `role` through</name>
  <files>validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift, validationLedgerTests/Loads/LoadDetailViewModelActionTests.swift</files>
  <read_first>
    - validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift (FULL file — lines 30-300 — note the BL-01 cancel-and-replace pattern at 122-206, the State enum at 61-74, the Equatable extensions at 253-279, the existing `userFacingMessage(for:)` helper)
    - validationLedger/Core/Networking/Endpoints/LoadActionEndpoint.swift (FULL — request/response shape; note the symmetric `{ load, chainOfTrust }` response per Phase 7 contract)
    - validationLedger/Core/Load/LoadActionPredictor.swift (Plan 01 output — the pure predictor the new submit() calls)
    - validationLedgerTests/Loads/LoadDetailViewModelTests.swift (FULL — analog test class shape; note the existing mock-APIClient pattern + the logger-spy pattern at the bottom if present, otherwise build the simplest logger spy)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §8 (the verbatim submit() shape; lines 357-373 give the full method body)
    - .planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md § Pattern 1 (cancel-and-replace 3 invariants); § Pattern 2 (state-machine isHidden discipline — VM-side: just transitions, never view operations); § Pitfall 9 (task lifetime > VC lifetime — `[weak self]` is the inherited Phase 9 safety)
    - .planning/phases/10-per-role-tender-accept-reject/10-UI-SPEC.md lines 343-348 (the 6 LOCKED per-action error-key strings — the `errorCopyKey` enum maps an action to a key)
  </read_first>
  <behavior>
    Tests in LoadDetailViewModelActionTests (NEW — write first; mirror `LoadDetailViewModelTests.swift` style):

    - Test 1 (`test_init_storesRole`): `LoadDetailViewModel(loadID: "VL-1", role: .broker, apiClient: <mock>)` — assert `vm.role == .broker`. Per D-22 / Pitfall 4 (the role plumb must actually store role on the VM, not just pass through).
    - Test 2 (`test_submit_fromLoaded_transitionsToActionInFlight_withPredictedLoad`): start with `state = .loaded(load: postedLoad, chain: cleanChain)`; call `submit(action: .tender, body: <body with respondByAt: T+24h>)`; capture the IMMEDIATE state transition before awaiting (mock APIClient delays the response). Assert `state == .actionInFlight(predicted: <load with .tendered + respondByAt: T+24h>, frozenChain: cleanChain, action: .tender)`. The predicted load is `LoadActionPredictor.predict(load: postedLoad, action: .tender, body: <body>)`.
    - Test 3 (`test_submit_serverResponds200_transitionsToLoaded_withResponseLoadAndChain`): mock APIClient returns `LoadActionEndpoint.Response(load: <load with .tendered>, chainOfTrust: <updated chain with new edge>)`; assert final state is `.loaded(responseLoad, responseChain)` — both fields swap from the response per D-14.
    - Test 4 (`test_submit_fromNonLoadedState_isNoOp`): with `state = .loading`, call `submit(action: .tender, body: <any>)`; assert state stays `.loading`, no network call dispatched (guard at the top of submit per PATTERNS.md line 361 — `guard case .loaded(...) = state else { return }`).
    - Test 5 (`test_submit_cancelAndReplace_supersedesEarlierAction`): call `submit(action: .tender, body: <body1>)` (mock delays); BEFORE the first await completes, call `submit(action: .cancel, body: <body2>)`. Assert the earlier task is cancelled (`Task.isCancelled` true after the network hop returns) and only the second action's terminal state lands.
    - Test 6 (`test_submit_pluggedIntoIdempotencyInterceptor_chain_dispatches`): assert the mock APIClient received a `LoadActionEndpoint` request (test inspects the captured endpoint type) — proves the VM's submit() is dispatching through the typed endpoint, which is what carries the `IdempotencyInterceptor` injection (the assertion that the header is actually present is in Plan 08).
    - Test 7 (`test_submit_concurrentToFetch_doesNotInterfere`): start a `fetchLoadDetail()` call (mocked to delay), then call `submit(...)`. Assert both tasks complete cleanly; the final state is the submit's terminal state (per BL-01 the later operation wins). Documents that `fetchTask` and `actionTask` are independent lifecycles.

    Test infrastructure: build a `MockAPIClient` (or extend the existing one in `LoadDetailViewModelTests.swift`) with handlers keyed by endpoint type. Build a `RecordingLogger` (in-test stub) that captures every `error(event:fields:)` and `info(event:fields:)` call so Tests 8-10 in Task 2 can assert `fields: [:]` discipline.
  </behavior>
  <action>
    Modify `validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` (additive — do NOT mutate existing function bodies or signatures except as listed):

    1. Extend the `State` enum (line 61-74) with two new cases:
       - `case actionInFlight(predicted: Load, frozenChain: ChainOfTrust, action: LoadAction)`
       - `case actionFailed(rollbackTo: Load, frozenChain: ChainOfTrust, errorCopyKey: String)`
       Both reuse the EXISTING `Load` / `ChainOfTrust` Equatable extensions at lines 253-279 — no new conformance work. `LoadAction` is `Equatable` for free (the underlying enum has no associated values per Phase 7).

    2. Add a `private let role: Role` stored property.

    3. Modify the designated initializer (line 102 area) to add `role: Role` as a designated parameter. Old signature: `init(loadID: String, apiClient: APIClient, logger: (any Logger)? = nil)`. New signature: `init(loadID: String, role: Role, apiClient: APIClient, logger: (any Logger)? = nil)`. (D-22 — this is the structural change that cascades through AppContainer.) Update the body to store `self.role = role`.

    4. Expose a public read-only computed property `public var role: Role { _role }` if Swift's access-control on the stored property would otherwise leak it as internal — OR more simply, rename the stored property to `private let _role: Role` with the public getter, OR keep `role: Role` as a `public let` directly. Pick the simplest shape; the VC needs to read `viewModel.role` from outside the VM in Plan 04. **Recommended:** `public let role: Role` directly; matches the existing `public let loadID: String` precedent on line 78.

    5. Add a `private var actionTask: Task<Void, Never>?` stored property (twin of `fetchTask` at line 99) with the same `// BL-01 — cancel-and-replace` header comment block extended to mention action submission.

    6. Add the `submit(action:body:) async` method per PATTERNS.md §8 lines 357-373 (mirror verbatim). Header comment block mirrors `fetchLoadDetail()` lines 122-134 — include the 3 cancel-and-replace invariants and a citation to D-12 (predict forward), D-13 (chain NEVER predicted), D-16 (single VM ownership).

    7. Add a `private func performAction(loadID:action:body:rollbackTo:frozenChain:) async` body. Mirror `performFetch()` lines 167-206 LINE-FOR-LINE in structure: the `do { … } catch is CancellationError { return } catch { … }` skeleton, the `if Task.isCancelled { return }` checkpoint AFTER the network hop and BEFORE the terminal state assignment, the `logger?.error(event: LogEvent("load_action_failed"), fields: [:])` call (S-2 view-layer lock — `fields: [:]`). On success (`response: LoadActionEndpoint.Response`): `state = .loaded(response.load, response.chainOfTrust)` (single assignment per D-14). On error: `state = .actionFailed(rollbackTo: rollbackTo, frozenChain: frozenChain, errorCopyKey: errorCopyKey(for: action))`.

    8. Add a `private static func errorCopyKey(for action: LoadAction) -> String` helper that switches over `action` and returns the LOCKED per-action keys per UI-SPEC line 343-348: `.tender -> "loads.actions.error.tender_failed"`, `.accept -> "loads.actions.error.accept_failed"`, `.reject -> "loads.actions.error.reject_failed"`, `.cancel -> "loads.actions.error.cancel_failed"`, `.post -> "loads.actions.error.post_failed"`, `.advanceStatus -> "loads.actions.error.advance_failed"`. No `default:` arm — exhaustive switch over the 6 LoadAction cases.

    9. DO NOT add any `print` or `os_log` calls outside the existing `logger?.error(...)` and `logger?.info(...)` patterns. DO NOT include any server response body string in either the `state.actionFailed.errorCopyKey` (which is a LOCALIZATION KEY only) or in any `fields:` dictionary. T-09-04 view-layer lock.

    Create `validationLedgerTests/Loads/LoadDetailViewModelActionTests.swift` with Tests 1-7 above. Use the same `MockAPIClient` pattern as `LoadDetailViewModelTests.swift`. Build a `RecordingLogger` stub (5-10 lines) that captures `(event, fields)` tuples so this test file's Test 6 — and Task 2's logger-spy tests — can assert call counts and `fields.isEmpty` discipline.
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/LoadDetailViewModelActionTests -only-testing:validationLedgerTests/Loads/LoadDetailViewModelTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -60</automated>
  </verify>
  <done>
    - All 7 tests in `LoadDetailViewModelActionTests` green
    - Existing `LoadDetailViewModelTests` still green (proves the VM modifications are additive — no regression on the existing `fetchLoadDetail()` path)
    - Source assertion: `grep -c 'case actionInFlight' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `1`
    - Source assertion: `grep -c 'case actionFailed' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `1`
    - Source assertion: `grep -nE 'init\(loadID: String, role: Role' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns at least one hit (designated init signature change)
    - Source assertion: `grep -c 'public let role: Role' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `1` (the VC can read viewModel.role)
    - Source assertion: `grep -nE 'actionTask\?\.cancel\(\)' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns at least one hit (BL-01 cancel-and-replace extends to actions)
    - Source assertion: `grep -nE 'fields: \[:\]' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns at least 2 hits including the new submit/performAction paths (T-09-04 view-layer lock extended)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rollback-path tests + AppContainer.makeLoadDetailScreen(loadID:role:) signature evolution</name>
  <files>validationLedgerTests/Loads/LoadDetailViewModelRollbackTests.swift, validationLedger/App/AppContainer.swift</files>
  <read_first>
    - validationLedger/App/AppContainer.swift (READ lines 215-300 — the existing makeLoadListScreen + makeLoadDetailScreen pair; note line 572's IdempotencyInterceptor registration which proves T-10-08 mitigation is already in place; you do NOT modify line 572)
    - validationLedgerTests/Loads/LoadDetailViewModelActionTests.swift (just landed; the `MockAPIClient` + `RecordingLogger` test infrastructure you'll reuse)
    - .planning/phases/10-per-role-tender-accept-reject/10-PATTERNS.md §9 (the verbatim AppContainer modification — lines 432-444 show the new factory closure + the new makeLoadDetailScreen signature)
    - .planning/phases/10-per-role-tender-accept-reject/10-RESEARCH.md § Code Examples lines 713-727 (the same AppContainer surface, with rationale on `[weak self]` and the captured role)
    - .planning/phases/10-per-role-tender-accept-reject/10-VALIDATION.md row ACTION-05 (this test file is the named automated command for the rollback invariant)
  </read_first>
  <behavior>
    Tests in LoadDetailViewModelRollbackTests (NEW — write first):

    - Test 1 (`test_submit_serverReturns500_transitionsToActionFailed_withRollbackLoad`): start at `.loaded(preLoad, preChain)`; submit `.tender`; mock APIClient throws an error (or returns 500); assert final state is `.actionFailed(rollbackTo: preLoad, frozenChain: preChain, errorCopyKey: "loads.actions.error.tender_failed")`. Both load and chain restored to pre-tap values.
    - Test 2 (`test_submit_serverReturns422_transitionsToActionFailed_perActionKey`): same as Test 1 but with `.accept` action; assert `errorCopyKey == "loads.actions.error.accept_failed"`. Per UI-SPEC line 344.
    - Test 3 (`test_submit_serverReturns409_transitionsToActionFailed_perActionKey`): same with `.cancel`; assert `errorCopyKey == "loads.actions.error.cancel_failed"`. Per UI-SPEC line 346.
    - Test 4 (`test_submit_urlErrorNetworkOffline_transitionsToActionFailed`): mock APIClient throws `URLError(.notConnectedToInternet)`; assert state transitions to `.actionFailed(...)` with the action's key (NOT a network-specific key — UI-SPEC line 354 explicitly says classification is logged but never rendered).
    - Test 5 (`test_submit_failure_doesNotLeakServerTextIntoState`): mock APIClient returns a 500 with an embedded error message in the body (e.g. JSON with `error: "Internal server error: party_id not found"`); assert `state.actionFailed.errorCopyKey` is the LOCKED localization key per UI-SPEC, NOT any substring of the server response body. T-09-04 view-layer lock.
    - Test 6 (`test_submit_failure_loggerCalledWith_emptyFieldsDict`): use the RecordingLogger to capture the `logger?.error(event: LogEvent("load_action_failed"), fields: <X>)` call after a forced error; assert `<X> == [:]`. T-09-04 / T-08-08 view-layer lock.
    - Test 7 (`test_submit_cancelledMidFlight_doesNotOverwriteFresherState`): submit `.tender` (mock delays); before the first task completes, submit `.cancel` (which transitions to a new predicted state and starts a new task). When the first task's network hop returns (mock then errors), assert the state is NOT overwritten with the first action's `.actionFailed` — instead the state reflects the second action's outcome. (Pitfall 9 / S-1 invariant 3 — the `catch` arm respects `Task.isCancelled`.)
    - Test 8 (`test_submit_vcDeallocMidFlight_noOp_noCrash`): construct a VM, capture a weak reference, submit an action with a delayed mock, then nil the VM's strong reference. After the network hop completes (the Task has [weak self] capture), assert no crash and the state mutation is a no-op (weak self is nil). Per Pitfall 9.
  </behavior>
  <action>
    Modify `validationLedger/App/AppContainer.swift`:

    1. Locate line 255-258 (the `detailFactory` closure inside `makeLoadListScreen(role:)`). Change the closure body to `return self.makeLoadDetailScreen(loadID: loadID, role: role)` — the `role` parameter is in scope from the enclosing `makeLoadListScreen(role:)` function signature, so the capture is implicit and `[weak self]` already present.

    2. Locate `makeLoadDetailScreen(loadID:)` (around line 283-294). Change the signature to `func makeLoadDetailScreen(loadID: String, role: Role) -> UIViewController`. Update the body to pass `role: role` to the `LoadDetailViewModel(...)` initializer call. Keep `@MainActor`, keep the `OSLogLoggerImpl(subsystem: LoggingSubsystem.app, category: "feature.loads")` line UNCHANGED, keep `[weak self]` semantics. DO NOT touch line 572 (the `requestInterceptors: [IdempotencyInterceptor()]` line — that is the T-10-08 critical infrastructure proven in Plan 08 to satisfy the ACTION-08 idempotency invariant).

    3. Update any other call site of `makeLoadDetailScreen` if one exists outside `makeLoadListScreen`. Run `grep -rn "makeLoadDetailScreen" validationLedger/` first to confirm — RESEARCH confirms the only call site is inside `makeLoadListScreen`'s closure, but verify.

    4. If `validationLedgerTests/App/AppContainerTests.swift` exists (or similar) and references the old signature, update those test call sites to match the new signature.

    Create `validationLedgerTests/Loads/LoadDetailViewModelRollbackTests.swift` with Tests 1-8 above. Reuse the `MockAPIClient` and `RecordingLogger` patterns from Task 1's test file (you can extract them to a small shared in-test helper file like `validationLedgerTests/Loads/Support/MockAPIClientForVMActionTests.swift` if it cleans up duplication; otherwise duplicate the small spies and document the parallel).

    DO NOT modify `LoadDetailViewController.swift` in this plan — its `render(state:)` extension lands in Plan 04 along with the action region UI. The two new VM states will simply not have a render handler until Plan 04 lands, which is acceptable for unit-test-only verification at the VM layer.
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads/LoadDetailViewModelRollbackTests -only-testing:validationLedgerTests/Loads/LoadDetailViewModelTests -only-testing:validationLedgerTests/Loads/LoadDetailViewModelActionTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -60</automated>
  </verify>
  <done>
    - All 8 tests in `LoadDetailViewModelRollbackTests` green
    - Existing `LoadDetailViewModelTests` + Task 1's `LoadDetailViewModelActionTests` still green
    - Source assertion: `grep -c 'func makeLoadDetailScreen(loadID: String, role: Role)' validationLedger/App/AppContainer.swift` returns `1`
    - Source assertion: `grep -c 'self.makeLoadDetailScreen(loadID: loadID, role: role)' validationLedger/App/AppContainer.swift` returns `1` (the closure passes role through)
    - Source assertion: `grep -c 'requestInterceptors: \[IdempotencyInterceptor()\]' validationLedger/App/AppContainer.swift` returns `1` (UNCHANGED — T-10-08 infrastructure preserved)
    - Whole-project compile passes (assertion: `xcodebuild build -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20` shows BUILD SUCCEEDED). The VM signature change cascades to AppContainer ONLY — if any other call site exists, Swift compile failure will catch it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VC → VM | The VC (Plan 04) invokes `viewModel.submit(action:body:)` and reads `viewModel.role`; it trusts the VM's state machine for predict / 200 / error transitions. |
| VM → network | The VM dispatches via `apiClient.request(LoadActionEndpoint(...))`; trusts the IdempotencyInterceptor (registered at AppContainer.swift:572 — UNCHANGED) to auto-inject the `Idempotency-Key` header (T-10-08 infrastructure mitigation). |
| AppContainer.role → VM.role | The `role` captured in `makeLoadListScreen(role:)` is set at composition root from the OTP-verified session role. The VM stores it as a `public let`; the VC reads it. The role is NOT client-changeable mid-screen (D-23). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | State mutation without server confirmation | mitigate | `submit()` predicts only forward (D-12); on 200 the VM swaps to `response.load + response.chainOfTrust` (D-14); on error the VM rolls back to the captured `preLoad + preChain` (D-15). Tests 1-4 of Rollback suite assert the rollback boundary; Test 7 asserts cancel-and-replace safety. |
| T-10-05 | Tampering | State desync after rollback (a fresher action's predicted state silently overwritten by a stale failure) | mitigate | BL-01 cancel-and-replace S-1 invariant 3 — the `catch` arm in `performAction(...)` checks `Task.isCancelled` BEFORE writing `.actionFailed`. Test 7 (`test_submit_cancelledMidFlight_doesNotOverwriteFresherState`) is the regression guard. |
| T-09-04 (extended) | Information Disclosure | Server-supplied error text leaking into the UI or into logger fields | mitigate | The `errorCopyKey` resolver maps actions to LOCKED localization keys per UI-SPEC line 343-348; the server response body NEVER reaches `state.actionFailed`. Logger calls use `fields: [:]`. Tests 5 + 6 assert both halves of the view-layer lock. |
| T-10-PR-01 | Repudiation / Crash | VC dealloc mid-action causing a write into a dealloc'd closure | accept (Phase 9 inherited safety) | `[weak self]` in the Task closure; terminal-state writes are no-ops when `self` is nil. Test 8 documents the lifecycle. |
| T-10-08 | Tampering | Replayed POST mutates state twice (idempotency) | mitigate (infrastructure unchanged) | `apiClient.requestInterceptors: [IdempotencyInterceptor()]` registered at `AppContainer.swift:572` — UNCHANGED in this plan. Plan 08 owns the assertion test that the header propagates on the wire. Two-layer defense: in-flight button gating (Plan 04) prevents user-driven double-submit; interceptor catches the rare race. |
| T-10-PR-SC | Tampering | Package legitimacy | accept | Phase 10 installs ZERO new packages. N/A. |
</threat_model>

<verification>
After both tasks:

`xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/Loads -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&1 | tail -60`

All Loads tests green (Phase 7-9 tests + the new Wave-1 and Wave-2 tests).

`xcodebuild build -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20` shows BUILD SUCCEEDED — proves the `makeLoadDetailScreen` signature change has no orphan call sites.
</verification>

<success_criteria>
- `LoadDetailViewModel.State` enum has 5 cases including `.actionInFlight` and `.actionFailed`
- `submit(action:body:) async` method exists, follows BL-01 cancel-and-replace, calls `LoadActionPredictor.predict(...)` for the predicted Load
- `performAction(...)` private body mirrors `performFetch()` line-for-line: `Task.isCancelled` checkpoint after the network hop AND in the catch arm; `fields: [:]` discipline on every logger call
- `role: Role` is a `public let` on the VM, stored at designated-init time
- `AppContainer.makeLoadDetailScreen(loadID:role:)` accepts the new role parameter; the factory closure captures the outer role
- 15 new tests (7 in ActionTests + 8 in RollbackTests) all green
- Existing `LoadDetailViewModelTests` still green (no regression on fetch path)
- Whole-project build succeeds
</success_criteria>

<output>
Create `.planning/phases/10-per-role-tender-accept-reject/10-03-SUMMARY.md` with: the State enum's new shape, the submit() method signature, the per-action errorCopyKey table the resolver implements, the AppContainer signature change, total new green test count, and any notes for Plan 04 about the `viewModel.role` consumption pattern.
</output>
</output>
