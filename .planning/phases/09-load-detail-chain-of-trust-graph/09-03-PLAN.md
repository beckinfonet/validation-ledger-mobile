---
phase: 09-load-detail-chain-of-trust-graph
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift
  - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
  - validationLedger/Features/Loads/LoadListViewController.swift
  - validationLedger/App/AppContainer.swift
  - validationLedgerTests/Loads/LoadDetailViewModelTests.swift
  - validationLedgerUITests/Loads/LoadDetailFlowTests.swift
autonomous: true
requirements:
  - LOAD-05
threat_refs:
  - T-09-01
  - T-09-03
  - T-09-04
  - T-09-05

must_haves:
  truths:
    - "LOAD-05: Tapping a load-list row in any role's LoadListViewController pushes LoadDetailViewController onto the navigation stack (no animation overrides, default UIKit push)"
    - "LoadDetailViewModel state machine has exactly three cases — .loading, .loaded(load, chainOfTrust), .error(message) — matching D-20 (no .empty state on detail; 404 collapses to .error)"
    - "Cancel-and-replace fetch behavior: a fresh viewWillAppear cancels any in-flight task before starting a new one (mirrors LoadListViewModel BL-01 lines 173-195)"
    - "userFacingMessage(for:) collapses ALL errors (network, decode, server-error) to the single localized copy 'loads.detail.error.generic'; server-supplied error text never reaches the screen (Phase 8 zero-PII precedent)"
    - "Client never derives trust state — verificationState, chainIntegrity.verdict, flagged status are read verbatim from the decoded LoadDetailEndpoint.Response (Phase 7 D-18 LOCK; iOS is a passive renderer)"
    - "AppContainer.makeLoadDetailScreen(loadID:) factory closure mirrors makeLoadListScreen(role:) (PATTERNS E12); reuses LoggingSubsystem.app + category 'feature.loads' (no new subsystem case)"
    - "LoadListViewController gains a UICollectionViewDelegate conformance with collectionView(_:didSelectItemAt:) that invokes the threaded detailScreenFactory closure and pushes the resulting VC (LOAD-05 wiring)"
    - "No PII reaches any logger in the new view layer — view-layer files (LoadDetailViewController) contain ZERO Logger / os_log invocations (mirrors Phase 8 LoadListViewController file-header T-08-08 lock)"
    - "iOS 17 deployment minimum honored — no iOS 18+-only APIs used"
    - "All user-facing strings via NSLocalizedString(_:value:) with English fallback (Phase 8 precedent)"
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift"
      provides: "@MainActor 3-case state machine consumer of LoadDetailEndpoint"
      contains: "public enum State: Equatable, Sendable"
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      provides: "UIKit VC shell: fetch-on-appear + bindViewModel(WR-06 priming pump) + render(state:) dispatcher"
      contains: "public final class LoadDetailViewController: UIViewController"
    - path: "validationLedger/App/AppContainer.swift"
      provides: "@MainActor makeLoadDetailScreen(loadID:) factory + threading into makeLoadListScreen(role:)"
      contains: "makeLoadDetailScreen"
    - path: "validationLedger/Features/Loads/LoadListViewController.swift"
      provides: "UICollectionViewDelegate.collectionView(_:didSelectItemAt:) → push detail VC (LOAD-05)"
      contains: "didSelectItemAt"
  key_links:
    - from: "validationLedger/Features/Loads/LoadListViewController.swift"
      to: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      via: "navigationController?.pushViewController(detailVC, animated: true) inside collectionView(_:didSelectItemAt:)"
      pattern: "navigationController\\?\\.pushViewController"
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift"
      to: "validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift"
      via: "apiClient.request(LoadDetailEndpoint(loadID:))"
      pattern: "LoadDetailEndpoint\\(loadID:"
    - from: "validationLedger/App/AppContainer.swift"
      to: "validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift"
      via: "makeLoadDetailScreen(loadID:) constructs LoadDetailViewModel and the VC"
      pattern: "LoadDetailViewModel\\("
---

<objective>
Ship the state-machine spine for Phase 9: `LoadDetailViewModel` (3-case state machine + cancel-and-replace fetch + userFacingMessage collapsing), `LoadDetailViewController` (programmatic UIKit shell with state-driven render dispatcher), the `LoadCoordinator`-or-factory navigation wiring (LOAD-05), and the AppContainer factory closure. Plans 04-09 build subview composition on top of this shell; this plan owns the spine.

Purpose: every downstream feature plan (skeleton, error state, timeline, graph, sheets, banner) renders into the view controller's view hierarchy by toggling `isHidden` on pre-attached state-subviews — never by view-controller swap (Phase 8 precedent + PATTERNS E1). Without this spine, the downstream plans have nowhere to render.

Output: 2 new production files under `validationLedger/Features/Loads/Detail/` + 2 modified files (`AppContainer.swift`, `LoadListViewController.swift`) + the `LoadDetailViewModelTests` shell from Plan 02 fully populated with the 4 state-machine `@Test` methods + the XCUITest `test_rowTap_pushesDetail()` method body filled in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-01-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-02-PLAN.md

@validationLedger/Features/Loads/LoadListViewController.swift
@validationLedger/Features/Loads/LoadListViewModel.swift
@validationLedger/App/AppContainer.swift
@validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift
@validationLedger/Core/Load/Load.swift
@validationLedger/Core/Load/ChainOfTrust.swift
@validationLedger/Features/Onboarding/KYC/KYCStatusViewController.swift
@validationLedger/App/AppCoordinator.swift

<interfaces>
<!-- The Phase 8 state-machine VC template Phase 9 mirrors line-for-line. -->
<!-- PATTERNS E1 (LoadListViewController.swift:402-431) + E2 (LoadListViewModel.swift:73-195) + E12 (AppContainer.swift:217-247) + E13 (AppCoordinator.swift:26-44) all cite the EXACT in-tree code. -->

From validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift:
- `public struct LoadDetailEndpoint: APIEndpoint` consumed by `apiClient.request(_:)`
- `Response { let load: Load; let chainOfTrust: ChainOfTrust }` (Phase 7 D-08 — one round-trip)
- Initialized with `LoadDetailEndpoint(loadID: String)`

From validationLedger/Features/Loads/LoadListViewModel.swift (PATTERNS E2):
- `@MainActor public final class LoadListViewModel`
- `public enum State: Equatable, Sendable { case loading; case empty; case loaded(items:, nextCursor:); case error(message:) }`
- `public private(set) var state: State = .loading { didSet { onStateChange?(state) } }`
- `public var onStateChange: ((State) -> Void)?`
- `private var fetchTask: Task<Void, Never>?` + `fetchTask?.cancel()` + `Task.isCancelled` checkpoints
- `userFacingMessage(for: Error) -> String` collapses every error to one localized copy

From validationLedger/Features/Loads/LoadListViewController.swift (PATTERNS E1):
- `init(viewModel:)` + `viewDidLoad → layoutContent / wireActions / bindViewModel` + `viewWillAppear → Task { await viewModel.fetchLoads() }`
- `bindViewModel` uses `MainActor.assumeIsolated` + WR-06 priming pump `render(state: viewModel.state)`
- State subviews toggled via `isHidden`, never VC swap

From validationLedger/App/AppContainer.swift (PATTERNS E12):
- `@MainActor func makeLoadListScreen(role: Role) -> UIViewController`
- Constructs `OSLogLoggerImpl(subsystem: LoggingSubsystem.app, category: "feature.loads")` (REUSE subsystem; do NOT add a new `.loads` case)
- Returns the configured VC

From validationLedger/App/AppCoordinator.swift (PATTERNS E13 — coordinator retain pattern):
- Owners hold `private var <name>Coordinator: <Name>Coordinator?` so the coordinator survives past `present/push` (Phase 3 bug fix).
- Phase 9 discretion (CONTEXT line 111): direct factory-closure threading is acceptable in lieu of a new `LoadDetailCoordinator`. **Chosen here: factory closure** (simpler; no coordinator state to retain across single push; mirrors `kycStatusScreenFactory` precedent).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LoadDetailViewModel — @MainActor 3-case state machine + cancel-and-replace fetch + userFacingMessage error collapsing + populate LoadDetailViewModelTests with 4 @Test methods</name>
  <files>
    validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift,
    validationLedgerTests/Loads/LoadDetailViewModelTests.swift
  </files>
  <behavior>
    - `LoadDetailViewModel.fetchLoadDetail()` async function:
      * Sets `state = .loading`.
      * Cancels any in-flight `fetchTask` (cancel-and-replace, BL-01 — PATTERNS E2 lines 120-131).
      * Calls `apiClient.request(LoadDetailEndpoint(loadID: self.loadID))`.
      * On success: `state = .loaded(response.load, response.chainOfTrust)`.
      * On error: `state = .error(message: userFacingMessage(for: error))`.
      * After the network hop, checks `Task.isCancelled` BEFORE mutating state (cancelled tasks bail out silently).
    - `userFacingMessage(for: Error) -> String` returns ONLY the localized generic copy `NSLocalizedString("loads.detail.error.generic", value: "We couldn't load this load.", comment: ...)` (UI-SPEC § Copywriting line 740) — never the server-supplied error description. Zero PII (T-09-04).
    - Logger calls (if any) use `Logger.event(_:fields:)` with `fields: [:]` — never include party names, load reference numbers, or partyIDs (PATTERNS § Zero-PII discipline).
    - `LoadDetailViewModelTests` `@Test` methods (populate the Plan-02 shells):
      * `loadingToLoadedOnFixture()`: register `load-detail-VL-1001.json` via `MockURLProtocol`; construct VM; call `await fetchLoadDetail()`; `#expect` state transitions `[.loading, .loaded(load, chainOfTrust)]` via `StateRecorder` (PATTERNS § LoadListViewModelTests precedent).
      * `loadingToErrorOnForcedFailure()`: register forced 500 via `MockURLProtocol.registerFailure(for: LoadDetailEndpoint.self, statusCode: 500)`; `#expect` final state `.error(message: <localized generic>)`.
      * `cancelAndReplaceOnRapidRefetch()`: launch `await vm.fetchLoadDetail()`, then immediately re-call before first completes; `#expect` only the LAST task's state transition lands; `#expect` the first fetch's continuation hits `Task.isCancelled` and bails.
      * `userFacingMessageIsZeroPIIOnDecodeAndNetworkErrors()` (T-09-04 / T-08-08 mirror): synthesize a `DecodingError.keyNotFound(...)` and an `URLError(.notConnectedToInternet)`; `#expect(vm.userFacingMessage(for: decodeErr) == localizedGeneric)` AND `#expect(vm.userFacingMessage(for: networkErr) == localizedGeneric)`; `#expect(!localizedGeneric.contains(decodeErr.localizedDescription))` (never echoes server / system error text).
  </behavior>
  <read_first>
    - validationLedger/Features/Loads/LoadListViewModel.swift (lines 73-195 — PATTERNS E2; the line-for-line analog Phase 9 mirrors with one fewer state)
    - validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift (the typed endpoint signature: `LoadDetailEndpoint(loadID: String)`; `Response { load: Load; chainOfTrust: ChainOfTrust }`)
    - validationLedger/Core/Load/Load.swift (the `Load` type the state's associated value carries)
    - validationLedger/Core/Load/ChainOfTrust.swift (the post-Plan-01 mutated `ChainOfTrust` type the state's associated value carries)
    - validationLedger/Core/Logging/Logger.swift (the `Logger.event(_:fields:)` signature — Phase 9 VM either passes `fields: [:]` or omits the logger entirely)
    - validationLedger/Core/Networking/APIClient.swift (the `apiClient.request(_:)` signature the VM consumes)
    - validationLedger/Core/Networking/Mock/MockURLProtocol.swift (the `registerFixture(for:..)` + `registerFailure(for:...)` surfaces the tests consume)
    - validationLedgerTests/Loads/LoadListViewModelTests.swift (PATTERNS § LoadListViewModelTests — the StateRecorder + RecordingLogger helper classes verbatim; the `@Suite(.serialized)` discipline)
    - validationLedgerTests/Networking/FixtureLoader.swift (the test-bundle JSON loader)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-20 — 3-case state machine; D-19 — skeleton-with-shimmer is the .loading UI; D-18 LOCK no client trust derivation)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§ Architectural Responsibility Map line 124 — VM tier; § Security Domain Known Threat Patterns)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (line 740 — `loads.detail.error.generic` copy template)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (table row `LoadDetailViewModel.swift` line 16 + E2 excerpt + line 44 LoadDetailViewModelTests)
  </read_first>
  <action>
    A. CREATE `validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift`. Mirror `LoadListViewModel.swift` lines 73-195 (PATTERNS E2) line-for-line, with these specific deviations:

    1. `@MainActor public final class LoadDetailViewModel`.
    2. `public enum State: Equatable, Sendable { case loading; case loaded(Load, ChainOfTrust); case error(message: String) }` — exactly 3 cases per D-20 (NO `.empty` — detail is by-ID, 404 → `.error`).
    3. `public private(set) var state: State = .loading { didSet { onStateChange?(state) } }`.
    4. `public var onStateChange: ((State) -> Void)?`.
    5. `private let loadID: String` (initializer-injected).
    6. `private let apiClient: APIClient` (or whatever DI surface Phase 8's VM takes — verify against `LoadListViewModel.swift` lines 73-105).
    7. `private let logger: Logger?` (Optional; the VC may pass `nil` for view-only flows; PATTERNS § Zero-PII).
    8. `private var fetchTask: Task<Void, Never>?`.
    9. `public init(loadID: String, apiClient: APIClient, logger: Logger? = nil)`.
    10. `public func fetchLoadDetail() async`:
        - `fetchTask?.cancel()`
        - `state = .loading` (no-op when already `.loading`; equatable enum)
        - `let task = Task { [weak self] in guard let self else { return }; await self.performFetch() }`
        - `fetchTask = task; await task.value`
    11. `private func performFetch() async`:
        - Wrap in `do { let response = try await apiClient.request(LoadDetailEndpoint(loadID: loadID)) }`
        - `if Task.isCancelled { return }` (BL-01 checkpoint)
        - `state = .loaded(response.load, response.chainOfTrust)`
        - `catch let error`: `if Task.isCancelled { return }`; `state = .error(message: userFacingMessage(for: error))`; optional `logger?.event("load_detail.fetch_failed", fields: [:])` (PII-zero per T-09-04).
    12. `public func userFacingMessage(for error: Error) -> String`:
        - `_ = error` (unused — error is consumed for logging only; never rendered)
        - `return NSLocalizedString("loads.detail.error.generic", value: "We couldn't load this load. Check your connection and try again.", comment: "Generic load-detail fetch error; server-supplied error text never reaches the screen")`
    13. NO custom `init(from:)`, NO server-error reflection, NO try-decode dispatcher that branches on `error.localizedDescription`. All errors collapse to one string.

    File-header comment block: cite "Phase 9 D-20 — 3-case state machine; Phase 8 D-09 lock — no PII in logs; Phase 7 D-18 LOCK — iOS is a passive renderer." Reference PATTERNS E2 (LoadListViewModel.swift:73-195).

    B. POPULATE `validationLedgerTests/Loads/LoadDetailViewModelTests.swift` (Plan 02 created the shell). Replace the `@Test` shells with real implementations per the `<behavior>` block. Reuse the `StateRecorder` + `RecordingLogger` pattern from `LoadListViewModelTests.swift` (copy or factor into a `LoadDetailTestHelpers.swift` if shared — planner discretion; the simpler path is to copy verbatim into the file). All tests `await vm.fetchLoadDetail()` and assert against the recorded state transitions.

    Per T-09-03 (no client trust): VM reads `response.load` + `response.chainOfTrust` verbatim; never derives. Per T-09-04 (PII): tests use only the synthetic Phase 7 fixture corpus. Per T-09-01 (decode safety): `performFetch`'s catch arm DOES NOT inspect the error to construct a message — only logs the failure with `fields: [:]` and renders the locked generic copy.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/LoadDetailViewModelTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^@MainActor$\|@MainActor public final class LoadDetailViewModel' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `>= 1`
    - Source: `grep -c 'case loading$\|case loading,' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `1`
    - Source: `grep -c 'case loaded(Load, ChainOfTrust)' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `1`
    - Source: `grep -c 'case error(message: String)' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `1`
    - Source: `grep -c 'case empty' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `0` (negative gate — D-20 no .empty state)
    - Source: `grep -c 'fetchTask?.cancel()' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `>= 1`
    - Source: `grep -c 'Task.isCancelled' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift` returns `>= 1`
    - Source: `grep -v '^//' validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift | grep -E 'Logger|os_log' | grep -v 'fields: \[:\]' | grep -v 'private let logger' | grep -v 'Logger?' | wc -l` returns `0` (every logger invocation passes `fields: [:]` — no PII)
    - Source: `grep -c '@Test' validationLedgerTests/Loads/LoadDetailViewModelTests.swift` returns `>= 4`
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/LoadDetailViewModelTests.swift` returns `0` (Plan 02 stubs replaced)
    - Behavior: `LoadDetailViewModelTests` passes (exit 0); the 4 `@Test` methods report `passed`
  </acceptance_criteria>
  <done>
    LoadDetailViewModel.swift exists with the 3-case state machine; tests cover loading→loaded, loading→error, cancel-and-replace, userFacingMessage zero-PII; all assertions green; zero PII in logger invocations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LoadDetailViewController shell (fetch-on-appear, bindViewModel WR-06 priming pump, render(state:) dispatcher with isHidden state-subview toggles) + wire AppContainer.makeLoadDetailScreen(loadID:) factory + push from LoadListViewController.collectionView(_:didSelectItemAt:) (LOAD-05) + populate LoadDetailFlowTests.test_rowTap_pushesDetail</name>
  <files>
    validationLedger/Features/Loads/Detail/LoadDetailViewController.swift,
    validationLedger/App/AppContainer.swift,
    validationLedger/Features/Loads/LoadListViewController.swift,
    validationLedgerUITests/Loads/LoadDetailFlowTests.swift
  </files>
  <behavior>
    - `LoadDetailViewController`:
      * `public init(viewModel: LoadDetailViewModel)`.
      * `required init?(coder:)` traps with `fatalError("LoadDetailViewController is constructed programmatically only")`.
      * `viewDidLoad()` calls `layoutContent()` + `wireActions()` + `bindViewModel()` (PATTERNS E1).
      * `viewWillAppear(_:)` launches `Task { await viewModel.fetchLoadDetail() }`.
      * `bindViewModel()` sets `viewModel.onStateChange = { [weak self] state in MainActor.assumeIsolated { self?.render(state: state) } }` then primes via `render(state: viewModel.state)` (WR-06 priming pump — PATTERNS E1 lines 90-96).
      * `render(state:)` is the dispatcher. It toggles `isHidden` on state-subviews — NEVER swaps view controllers. State-subviews are placeholder attached subviews (`skeletonContainer`, `errorContainer`, `bodyContainer`) — Phase 9 plans 04/05/06/09 populate these containers' children; this plan only attaches empty containers and sets `accessibilityIdentifier` per UI-SPEC.
      * Layout uses `UIView` containers pinned to `view.safeAreaLayoutGuide` per UI-SPEC § iPhone composition (single-column iPhone; iPad split lives in Plan 09).
      * `accessibilityIdentifier = "load-detail"` on `view`; per UI-SPEC § Accessibility identifiers locked list.
      * NO Logger / os_log calls in this file (Phase 8 LoadListViewController file-header T-08-08 lock; PATTERNS § Zero-PII discipline).
    - `AppContainer.makeLoadDetailScreen(loadID:)`:
      * `@MainActor func makeLoadDetailScreen(loadID: String) -> UIViewController`.
      * Mirrors `makeLoadListScreen(role:)` line-for-line (PATTERNS E12):
        - Build `featureLogger = OSLogLoggerImpl(subsystem: LoggingSubsystem.app, category: "feature.loads")` (REUSE category — same category Phase 8 uses; do NOT introduce `feature.loadDetail`).
        - Construct `LoadDetailViewModel(loadID: loadID, apiClient: apiClient, logger: featureLogger)`.
        - Return `LoadDetailViewController(viewModel: vm)`.
      * Thread the factory into `makeLoadListScreen(role:)`: extend the call to `LoadListViewController.init(...)` to pass an additional `detailScreenFactory: (String) -> UIViewController` closure (this requires extending LoadListViewController's init — see below). Closure body captures `self` weakly and calls `self?.makeLoadDetailScreen(loadID:)`. Mirrors the `kycStatusScreenFactory` threading.
    - `LoadListViewController` modifications (Phase 8 file — additive only):
      * Add a stored property `private let detailScreenFactory: (String) -> UIViewController` (or `(String) -> UIViewController?` if the planner prefers Optional — chosen: non-Optional, container guarantees the closure is wired).
      * Extend `init(viewModel:navTitle:)` to `init(viewModel:navTitle:detailScreenFactory:)`. Stored property assignment in init.
      * Inside `viewDidLoad()` (or `layoutContent()` — verify by reading the file) add `collectionView.delegate = self` (PATTERNS table row 27 confirms the row-tap is currently inert).
      * Add an extension at the bottom of the file: `extension LoadListViewController: UICollectionViewDelegate { public func collectionView(_ cv: UICollectionView, didSelectItemAt indexPath: IndexPath) { guard let item = dataSource.itemIdentifier(for: indexPath) else { return }; let loadID = item.load.id; let detailVC = detailScreenFactory(loadID); navigationController?.pushViewController(detailVC, animated: true) } }`.
      * Verify against the actual diffable datasource shape (`item.load.id` vs `item.load.referenceNumber` — the planner reads `LoadListItem` to confirm which String identifier is the `loadID` LoadDetailEndpoint accepts).
    - `LoadDetailFlowTests.test_rowTap_pushesDetail()`:
      * For each of the 5 roles, launch via `app.launchArguments = ["-MockOTPRoleForUITest", role]`; call `driveFullOTPFlow(app)`; wait for `loads-list.row.VL-*` cell; tap first cell; `XCTAssertTrue(app.otherElements["load-detail"].waitForExistence(timeout: 5))`.
      * Other test methods in this file remain XCTSkip (Plan 07/08/10 populate them).
  </behavior>
  <read_first>
    - validationLedger/Features/Loads/LoadListViewController.swift (lines 147-174 init shape; lines 178-280 accessibility-identifier namespace; lines 312-359 layoutContent / wireActions / bindViewModel structure; lines 402-431 PATTERNS E1 priming pump; lines 441-568 render(state:) dispatcher pattern; the FULL file is needed to understand the additive modification surface)
    - validationLedger/Features/Loads/LoadListViewModel.swift (DI surface — confirm `apiClient`, `logger` parameter names)
    - validationLedger/Features/Loads/Detail/LoadDetailViewModel.swift (Task 1 output — the constructor signature `init(loadID: String, apiClient: APIClient, logger: Logger? = nil)`)
    - validationLedger/App/AppContainer.swift (lines 217-247 — PATTERNS E12 makeLoadListScreen template; the file shape Phase 9 mirrors)
    - validationLedger/App/AppCoordinator.swift (lines 26-44 — PATTERNS E13 coordinator retain pattern; documented even though Phase 9 chose the factory-closure path — informational reference for the planner's decision per CONTEXT line 111)
    - validationLedger/Features/Onboarding/KYC/KYCStatusViewController.swift (PATTERNS table row for LoadDetailBodyView — the fetch-on-appear UIKit precedent; this plan uses the bare-bones VC shell, body composition lands in Plan 04/05)
    - validationLedgerUITests/Loads/RoleLoadsTabSmokeTests.swift (PATTERNS E11 lines 38-80 — driveFullOTPFlow + `executionTimeAllowance = 30` setUp; the 5-role launchArgument pattern Phase 9 reuses)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-20 state machine; D-19 skeleton placeholder; § Claude's Discretion line 111 — coordinator vs factory closure; CONTEXT line 144 — LoadListViewController collectionView(_:didSelectItemAt:) currently inert)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§8 lines 911-924 — MVVM + state-machine VC; § Reference-Implementation Analogs lines 870-930)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (§ Accessibility identifiers lines 757-788 — the LOCKED identifier namespace for `load-detail.*`; § State Machine lines 791-803)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (E1 + E12 + table rows lines 15, 27, 28; § Shared Patterns — Programmatic UIKit + initializer DI; § Zero-PII discipline lines 504-506)
  </read_first>
  <action>
    A. CREATE `validationLedger/Features/Loads/Detail/LoadDetailViewController.swift`. Programmatic-only UIKit VC.

    Structure (mirror `LoadListViewController.swift` PATTERNS E1):
    - File-header comment: cite "Phase 9 LOAD-05 / D-01 / D-20; PATTERNS E1 (LoadListViewController:402-431); Zero-PII: no Logger / os_log in this file (T-08-08 mirror)."
    - `import UIKit`.
    - `public final class LoadDetailViewController: UIViewController`.
    - Stored properties:
      * `private let viewModel: LoadDetailViewModel`
      * `private let skeletonContainer = UIView()` — empty container placeholder; Plan 04 populates with `LoadDetailSkeletonView`.
      * `private let errorContainer = UIView()` — empty placeholder; Plan 04 populates with the `UIContentUnavailableView` posture.
      * `private let bodyContainer = UIView()` — empty placeholder; Plans 04 (header), 05 (timeline), 06 (graph), 09 (banner + iPad split) compose into this.
    - `public init(viewModel: LoadDetailViewModel) { self.viewModel = viewModel; super.init(nibName: nil, bundle: nil) }`.
    - `required init?(coder: NSCoder) { fatalError("LoadDetailViewController is constructed programmatically only") }`.
    - `public override func viewDidLoad()`: call `super`; `view.backgroundColor = DS.Colors.background` (UI-SPEC § Color line 600); `view.accessibilityIdentifier = "load-detail"`; call `layoutContent()` + `wireActions()` + `bindViewModel()`.
    - `private func layoutContent()`: add the 3 containers as subviews; pin each edge-to-edge in `view.safeAreaLayoutGuide`; set per-container `accessibilityIdentifier` (`load-detail.skeleton`, `load-detail.error-state`, `load-detail.body`) per UI-SPEC § Accessibility identifiers locked list. All three containers start with `isHidden = true`; `render(state:)` toggles.
    - `private func wireActions()`: empty for now (the "Try again" button + sheet presentations land in Plans 04 and 07/08).
    - `private func bindViewModel()`: per PATTERNS E1 (verbatim):
      ```
      viewModel.onStateChange = { [weak self] state in
          MainActor.assumeIsolated { self?.render(state: state) }
      }
      render(state: viewModel.state)  // WR-06 priming pump
      ```
    - `public override func viewWillAppear(_ animated: Bool)`: super; `Task { [viewModel] in await viewModel.fetchLoadDetail() }`.
    - `private func render(state: LoadDetailViewModel.State)`: switch on state; for each case set `skeletonContainer.isHidden`, `errorContainer.isHidden`, `bodyContainer.isHidden` per case (`.loading` → only skeleton visible; `.loaded` → only body; `.error` → only error). Plan 04 will populate the actual subview content; this plan only manages the toggle.

    NO logger property, NO log call sites. Per T-08-08: the view layer is logging-free. (The VM does any logging that happens; the VC doesn't.)

    B. MODIFY `validationLedger/App/AppContainer.swift`:

    Add `@MainActor func makeLoadDetailScreen(loadID: String) -> UIViewController` after the existing `makeLoadListScreen(role:)` (lines 217-247). Body per PATTERNS E12:
    - Build `featureLogger = OSLogLoggerImpl(subsystem: LoggingSubsystem.app, category: "feature.loads")` — reuse the same category as the list (no new case in `LoggingSubsystem` enum).
    - Construct `vm = LoadDetailViewModel(loadID: loadID, apiClient: apiClient, logger: featureLogger)`.
    - Return `LoadDetailViewController(viewModel: vm)`.

    Modify `makeLoadListScreen(role:)` to thread the factory: extend the call to `LoadListViewController.init(viewModel: vm, navTitle: title)` to add a closure `detailScreenFactory: { [weak self] loadID in self?.makeLoadDetailScreen(loadID: loadID) ?? UIViewController() }` — the fallback empty VC handles the (logically impossible) case where AppContainer is deallocated mid-tap. Better: assert `self` is alive (since AppContainer is a long-lived application-scoped object) — planner can choose `[unowned self]` if confident; the `weak` form is the safer default.

    C. MODIFY `validationLedger/Features/Loads/LoadListViewController.swift`:

    Three additive changes:
    1. Stored property: `private let detailScreenFactory: (String) -> UIViewController`.
    2. `init` extension: change `public init(viewModel: LoadListViewModel, navTitle: String)` to `public init(viewModel: LoadListViewModel, navTitle: String, detailScreenFactory: @escaping (String) -> UIViewController)`. Inside body: `self.detailScreenFactory = detailScreenFactory`. Update the implicit AppContainer call site.
    3. Add at end of file:
       ```
       extension LoadListViewController: UICollectionViewDelegate {
           public func collectionView(_ collectionView: UICollectionView, didSelectItemAt indexPath: IndexPath) {
               guard let item = dataSource.itemIdentifier(for: indexPath) else { return }
               let detailVC = detailScreenFactory(item.load.id)
               navigationController?.pushViewController(detailVC, animated: true)
           }
       }
       ```
       Inside `viewDidLoad` or `layoutContent` (verify against the actual file), add `collectionView.delegate = self`. The diffable datasource and `LoadListItem` shape are already in tree (Plan 08-01); verify `item.load.id` is the correct string accessor (`Load.id` or `Load.referenceNumber` — read `Load.swift` to confirm).

    D. POPULATE `validationLedgerUITests/Loads/LoadDetailFlowTests.swift`:

    Replace the `test_rowTap_pushesDetail()` XCTSkip stub. Implement:
    - For each of `["broker", "shipper", "carrier", "dispatch", "factoring"]` (the 5 roles per Phase 7 D-11): subtest via `XCTContext.runActivity(named: "Role: \(role)")`.
    - Inside each: `let app = launch(role: role)`; `driveFullOTPFlow(app)`; navigate to Loads tab (per Phase 8 RoleLoadsTabSmokeTests pattern); wait for `app.cells.matching(identifier: NSPredicate(format: "identifier BEGINSWITH 'loads-list.row.'")).element.firstMatch`; tap; `XCTAssertTrue(app.otherElements["load-detail"].waitForExistence(timeout: 5))` (the LOAD-05 acceptance — the detail screen appears on the navigation stack).
    - Reuse `driveFullOTPFlow` private helper from `RoleLoadsTabSmokeTests` — either copy verbatim into this file (PATTERNS E11) or factor into a `UITestHelpers.swift` shared file (planner discretion; the simpler path is copy verbatim).
    - Other test methods (`test_nodeTap_*`, `test_edgeTap_*`, etc.) remain `throw XCTSkip` — Plans 07/08/10 populate them.

    Per T-09-05 (no client trust derivation): the VC's `render(state:)` reads `state` verbatim — never derives badge state from raw `verificationState` strings.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerUITests/LoadDetailFlowTests/test_rowTap_pushesDetail -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public final class LoadDetailViewController: UIViewController' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `1`
    - Source: `grep -c 'fatalError.*programmatically only' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `1`
    - Source: `grep -c 'MainActor.assumeIsolated' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1`
    - Source: `grep -c 'accessibilityIdentifier = "load-detail"' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1`
    - Source: `grep -v '^//' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift | grep -cE 'Logger|os_log|OSLog'` returns `0` (negative gate — T-08-08 view-layer logging-free)
    - Source: `grep -c 'func makeLoadDetailScreen(loadID: String)' validationLedger/App/AppContainer.swift` returns `1`
    - Source: `grep -c 'category: "feature.loads"' validationLedger/App/AppContainer.swift` returns `>= 2` (one for list, one for detail — same category, no `feature.loadDetail`)
    - Source: `grep -c 'category: "feature.loadDetail"' validationLedger/App/AppContainer.swift` returns `0` (negative gate — same category as list)
    - Source: `grep -c 'detailScreenFactory:' validationLedger/Features/Loads/LoadListViewController.swift` returns `>= 2` (property + init)
    - Source: `grep -c 'didSelectItemAt indexPath' validationLedger/Features/Loads/LoadListViewController.swift` returns `1`
    - Source: `grep -c 'navigationController?.pushViewController' validationLedger/Features/Loads/LoadListViewController.swift` returns `1`
    - Source: `grep -c 'XCTSkip' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `4` (5 method total, 1 implemented → 4 remain XCTSkip)
    - Source: `grep -c 'test_rowTap_pushesDetail' validationLedgerUITests/Loads/LoadDetailFlowTests.swift` returns `1`
    - Behavior: `xcodebuild test -only-testing:validationLedgerUITests/LoadDetailFlowTests/test_rowTap_pushesDetail` exits 0 across all 5 role subtests
    - Behavior: Phase 8 RoleLoadsTabSmokeTests still passes (no regression on the list flow)
  </acceptance_criteria>
  <done>
    LoadDetailViewController shell created with state-driven render(state:) dispatcher; AppContainer wires the factory + threads it into LoadListViewController; row-tap pushes the detail VC; XCUITest exits green across 5 roles; zero logger calls in the new VC file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decoded `LoadDetailEndpoint.Response` → view-layer render | `verificationState`, `chainIntegrity.verdict`, `implicatedNodeIDs/EdgeIDs` cross from the typed Decodable surface into the view layer. Phase 7 D-18 LOCK: iOS renders verbatim; never derives. |
| Server / network error → user-facing copy | A `URLError` / `DecodingError` / 5xx response crosses into the VM. Mitigation: every error collapses to the locked localized generic copy (T-09-04 / Phase 8 T-08-08 mirror). |
| `LoadListViewController` row-tap → factory closure → push | The factory closure captures `AppContainer` weakly; if the container is deallocated (impossible in practice — it's app-scoped) the tap is a no-op. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `LoadDetailViewModel` Decodable surface (consumed via APIClient) | mitigate | NO custom `init(from:)` on the consumed types (Phase 7 contract + Plan 01 + `.convertFromSnakeCase`); the VM never inspects the error to construct a message (T-09-04). Locked by `LoadDetailViewModelTests` Test 4 (userFacingMessage zero-PII). |
| T-09-03 | Tampering | Client-side trust derivation in VM or VC | mitigate | VM's state's associated value carries the WHOLE Decodable `Load + ChainOfTrust` — no derived booleans. VC's render(state:) reads verbatim. Source-assertion grep gate: future plans MUST NOT call any `derive*()` method in the view layer. |
| T-09-04 | Information Disclosure (PII) | Logger calls in VM / VC + error rendering | mitigate | VC file has ZERO `Logger`/`os_log` calls (negative grep gate). VM logger calls use `fields: [:]` (no PII). `userFacingMessage` never reflects server error text — locked by Test 4. |
| T-09-05 | Information Disclosure | XCUITest `test_rowTap_pushesDetail` interaction with real launch arguments | mitigate | Mock-only `-MockOTPRoleForUITest` launch argument routes through `MockURLProtocol` — no live backend. Test exercises the navigation push only; never inspects render state. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies in this plan. `Package.swift` byte-identical. |
</threat_model>

<verification>
1. Build green: `xcodebuild build` exits 0.
2. VM state machine green: `-only-testing:validationLedgerTests/LoadDetailViewModelTests` passes with `>= 4` `@Test` methods.
3. LOAD-05 XCUITest green: `-only-testing:validationLedgerUITests/LoadDetailFlowTests/test_rowTap_pushesDetail` passes across all 5 roles.
4. Phase 8 regression smoke: `-only-testing:validationLedgerUITests/Loads/RoleLoadsTabSmokeTests` still passes (list flow unchanged from user perspective; the init signature change is internal).
5. Zero-PII gate: `grep -v '^//' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift | grep -cE 'Logger|os_log|OSLog'` returns 0.
6. Subsystem reuse: `grep -c 'category: "feature.loadDetail"' validationLedger/App/AppContainer.swift` returns 0 (no new subsystem case added).
</verification>

<success_criteria>
- LoadDetailViewModel exists with the locked 3-case state machine, cancel-and-replace fetch, and userFacingMessage error-collapsing.
- LoadDetailViewController exists with the state-driven render(state:) dispatcher and `isHidden` state-subview toggling.
- AppContainer.makeLoadDetailScreen(loadID:) factory + threading into LoadListViewController are wired.
- LoadListViewController.collectionView(_:didSelectItemAt:) pushes the detail VC (LOAD-05).
- LoadDetailViewModelTests (4+ methods) and LoadDetailFlowTests.test_rowTap_pushesDetail (5 roles) pass green.
- Zero new SwiftPM dependencies.
- Zero logger calls in the view layer.
- LoggingSubsystem unchanged (still uses `feature.loads`).
</success_criteria>

<output>
Create `.planning/phases/09-load-detail-chain-of-trust-graph/09-03-SUMMARY.md` when done. Record:
- LoadDetailViewModel surface (the 3-case state enum signature + init signature + fetchLoadDetail / userFacingMessage signatures).
- LoadDetailViewController structural map (containers / render(state:) toggle behavior).
- LoadListViewController init signature change (the `detailScreenFactory:` parameter addition) and any call-site updates beyond AppContainer.
- AppContainer.makeLoadDetailScreen factory signature.
- LoadDetailFlowTests methods populated (1) vs still XCTSkip (4) — with the plan number that populates each.
- Any open question — e.g. whether `Load.id` vs `Load.referenceNumber` is the correct identifier for `LoadDetailEndpoint(loadID:)`; whether a thin `LoadDetailCoordinator` should be introduced in a future plan if the navigation stack grows (Phase 10 tender flow may require it).
</output>
