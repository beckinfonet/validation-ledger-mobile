---
phase: 09-load-detail-chain-of-trust-graph
plan: 09
type: execute
wave: 6
depends_on:
  - 09-04
  - 09-05
  - 09-06
files_modified:
  - validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift
  - validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift
  - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
  - validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift
  - validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift
  - validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift
  - validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.swift
autonomous: true
requirements:
  - TRUST-05
threat_refs:
  - T-09-03
  - T-09-04
  - T-09-11

must_haves:
  truths:
    - "TRUST-05: Flagged nodes/edges and the chain-level integrity verdict are rendered distinctly from fixture-supplied data; the graph renders natively on iPad and is VoiceOver-traversable"
    - "D-16: ChainIntegrityBannerView is pinned and never scrolls away on iPhone (fraud signal is persistent); spans full width above the iPad split (UI-SPEC line 128); rendered ONLY when verdict != .clean (when clean: isHidden = true AND removed from any UIStackView arranged subview to avoid invisible-height hit-test surfaces — UI-SPEC line 118)"
    - "Banner copy template per UI-SPEC § Copywriting Chain-integrity banner lines 676-684: caution → 'Risk flagged: {reason}'; compromised → 'Chain compromised: {reason}' — server-supplied chainIntegrity.reason rendered verbatim"
    - "Banner mirrors LimitedTrustBannerView file shape (PATTERNS E4 lines 184-230) — programmatic UIView, DS-token styled, NSLocalizedString-backed accessibilityLabel, `isUserInteractionEnabled = false`"
    - "D-02 chain-integrity verdict block inside LoadDetailBodyView — duplicated render of the same data (D-16 — intentional duplication; banner at top is marquee surface, verdict block in body is read-in-context surface); ONLY rendered when verdict != .clean"
    - "D-01 / D-02 / D-03 iPhone vs iPad composition: traitCollectionDidChange(_:) rebuilds the layout — iPhone single-column vertical stack [pinnedHeader, banner, trustGraph (~62% height), bodyView]; iPad regular split [banner full-width, left ~60% trustGraph, right ~40% bodyView (pinnedHeader on top of right pane + body scroll)]; UIScrollView.zoomScale on the graph is preserved across the rebuild"
    - "D-19 skeleton iPad-split silhouette — Plan 04 shipped the iPhone silhouette only; this plan adds the iPad split silhouette (left pane: skeleton graph, right pane: skeleton header + 4 body rows)"
    - "D-21 VoiceOver traversal order: iPhone (pinned header → banner → graph container → timeline → freight rows → parties → verdict block); iPad (banner → right pane FIRST → left pane graph LAST) — set the VC view's accessibilityElements array explicitly per UI-SPEC line 827-830"
    - "Client never derives trust state — banner color, halo dim, verdict block tint all come from chainIntegrity.verdict + reason verbatim (Phase 7 D-18 LOCK; locked by source-assertion grep gate)"
    - "Zero PII — banner + verdict block render chainIntegrity.reason verbatim (server-supplied); no logger calls"
    - "Source has zero references to a fail-closed defensive default for `verdict` in the view layer — the contract layer (Phase 7 D-09 / ChainIntegrity.swift line 83) already fail-closed-decodes unknown values to .compromised; the view layer trusts that contract"
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift"
      provides: "D-15 / D-16 fixed-position fraud banner; verdict-driven color + icon + copy"
      contains: "public final class ChainIntegrityBannerView: UIView"
    - path: "validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift"
      provides: "D-02 in-body verdict block (rendered when verdict != .clean); soft 0.15 tint per verdict"
      contains: "public final class ChainIntegrityVerdictBlockView: UIView"
    - path: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      provides: "iPhone vs iPad composition branching in traitCollectionDidChange(_:); banner pinned above the layout root; pinned summary header refactored from bodyView into a top-level view (iPhone) OR into the right pane (iPad)"
      contains: "traitCollectionDidChange"
  key_links:
    - from: "validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift"
      to: "validationLedger/UI/LimitedTrustBannerView.swift"
      via: "file-shape twin per PATTERNS E4 — programmatic UIView with DS-token styled per-verdict color/icon/copy"
      pattern: "isAccessibilityElement = true.*accessibilityTraits = .staticText"
    - from: "validationLedger/Features/Loads/Detail/LoadDetailViewController.swift"
      to: "validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift"
      via: "verdictBlockContainer is populated via bodyView.installVerdictBlock(_:) when verdict != .clean"
      pattern: "installVerdictBlock"
---

<objective>
Ship the chain-integrity composition: the pinned `ChainIntegrityBannerView` (D-15 / D-16), the in-body `ChainIntegrityVerdictBlockView` (D-02), and the iPhone-vs-iPad composition branch (D-01/D-02/D-03/D-21). Refactor Plan 04's pinned summary header placement so it lives ABOVE the graph on iPhone (per UI-SPEC § iPhone composition line 90) and AT THE TOP OF THE RIGHT PANE on iPad. Ship the iPad-split skeleton silhouette (D-19) as the last skeleton-snapshot test from Plan 04. Set the VC view's `accessibilityElements` array explicitly per D-21.

Purpose: TRUST-05 acceptance criterion ("Flagged nodes/edges and the chain-level integrity verdict are rendered distinctly from fixture-supplied data; the graph renders natively on iPad and is VoiceOver-traversable"). Plan 06 already renders flagged nodes/edges; this plan ships the chain-level surfaces (banner + verdict block) AND the iPad native rendering AND the VoiceOver traversal order.

Output: 2 new production files (banner, verdict block) + 3 modified production files (VC composition refactor, body slot for verdict block, skeleton iPad silhouette) + the Plan 02 banner snapshot test populated + the Plan 04 iPad-split skeleton test populated (replaces the lone remaining XCTSkip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-04-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-05-PLAN.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-06-PLAN.md

@validationLedger/UI/LimitedTrustBannerView.swift
@validationLedger/Core/Load/ChainIntegrity.swift
@validationLedger/UI/Components/LoadStatusBadgeView.swift
@validationLedger/UI/DesignSystem/Spacing.swift
@validationLedger/UI/DesignSystem/Colors.swift
@validationLedger/UI/DesignSystem/Typography.swift
@validationLedger/Features/Loads/Detail/LoadDetailViewController.swift
@validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift
@validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift
@validationLedger/Features/Loads/Detail/TrustGraphView.swift

<interfaces>
<!-- PATTERNS E4 (LimitedTrustBannerView.swift:35-104) — the EXACT file-shape twin Phase 9 mirrors. -->
<!-- Phase 9 differs in TWO ways: per-verdict color tier (caution yellow / compromised red); the banner sits ABOVE content rather than at the top of the screen. -->

From validationLedger/UI/LimitedTrustBannerView.swift (PATTERNS E4):
- `public final class LimitedTrustBannerView: UIView`
- `private static let bannerHeight: CGFloat = 36.0`
- Programmatic UIView, `isUserInteractionEnabled = false`, `accessibilityIdentifier`, NSLocalizedString accessibilityLabel, `accessibilityTraits = .staticText`, `isAccessibilityElement = true`
- `intrinsicContentSize` override for fixed minimum height
- `setUp()` body builds label + horizontal layout with leading/trailing constants

From validationLedger/Core/Load/ChainIntegrity.swift:
- `public enum Verdict: String, Decodable, Sendable { case clean, caution, compromised }` — fail-closed-decodes unknown → .compromised
- `public let verdict: Verdict`
- `public let reason: String?`
- `public let implicatedNodeIDs: Set<String>`
- `public let implicatedEdgeIDs: Set<String>`

Banner copy templates (UI-SPEC § Copywriting Chain-integrity banner lines 676-684):
- `.clean` → not rendered
- `.caution` → "Risk flagged: {reason}"
- `.compromised` → "Chain compromised: {reason}"

Banner accessibilityLabel composition: "Chain integrity: {verdict-spoken}. {reason}" where verdict-spoken is "Caution" / "Compromised" (UI-SPEC line 684).

Verdict block content (UI-SPEC § Chain-integrity verdict block lines 389-402):
- Container: `UIView`, cornerRadius 8, soft tint (caution → 0.15 yellow / compromised → 0.15 red).
- Row 1: SF Symbol (triangle for caution / octagon for compromised) + verdict label (.headline, "Caution" / "Compromised")
- Row 2: `chainIntegrity.reason` rendered as .body, .label color, multi-line (server-supplied verbatim)
- Row 3 (optional): when `implicatedNodeIDs.count > 0`: a .footnote caption "{N} parties implicated" (singular: "1 party implicated")
- When `.clean`: ENTIRE section omitted (no "All clean" placeholder per UI-SPEC line 402)

Composition refactor (D-01 / D-02 / D-03):
- iPhone (`horizontalSizeClass == .compact`):
  ```
  view (VC.view)
  ├─ pinnedSummaryHeaderContainer (NEW top-level — lifted out of bodyView; pinned to safeAreaLayoutGuide.top)
  ├─ chainIntegrityBanner (NEW; pinned below header; hidden when verdict == .clean)
  ├─ trustGraphView (already in tree per Plan 06; pinned below banner; height ~62% of safeAreaLayoutGuide layoutFrame)
  └─ bodyView (LoadDetailBodyView; pinned below trustGraphView; pinnedSummaryHeader REMOVED from its content stack; body sections remain: timeline + freight + parties + verdict block)
  ```
- iPad (`horizontalSizeClass == .regular`):
  ```
  view
  ├─ chainIntegrityBanner (full-width above split; hidden when clean)
  ├─ horizontal split:
  │   ├─ trustGraphView (left 60%; widthAnchor.multiplier = 0.60)
  │   └─ rightPaneContainer (right 40%; widthAnchor.multiplier = 0.40)
  │       ├─ pinnedSummaryHeaderContainer (top)
  │       └─ bodyView (below pinnedSummaryHeaderContainer; same content as iPhone body sections minus the pinned header)
  ```

VoiceOver traversal order (UI-SPEC § Traversal order lines 823-830):
- iPhone: pinned header → banner (if rendered) → trust graph (container) → status timeline → freight rows → parties → verdict block
- iPad: banner → right pane FIRST (pinned header → timeline → freight → parties → verdict) → left pane (trust graph)

`accessibilityElements` set on `LoadDetailViewController.view` in the order above.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ChainIntegrityBannerView (D-15 / D-16) + populate Banner snapshot tests (caution + compromised; clean is isHidden + unit test) + create ChainIntegrityVerdictBlockView (D-02) + slot it into LoadDetailBodyView's verdictBlockContainer</name>
  <files>
    validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift,
    validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift,
    validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift,
    validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift
  </files>
  <behavior>
    - `ChainIntegrityBannerView: UIView`:
      * Programmatic UIView mirroring `LimitedTrustBannerView.swift` file-shape verbatim per PATTERNS E4.
      * `public init(verdict: ChainIntegrity.Verdict, reason: String?)`.
      * Background per verdict: `.caution` → `DS.Colors.caution`; `.compromised` → `DS.Colors.destructive`; `.clean` → does not render (init returns but `setUp()` calls `isHidden = true` AND `intrinsicContentSize = .zero`).
      * Foreground (icon + text): caution → `.label` (yellow needs dark text per UI-SPEC line 219); compromised → `.white`.
      * SF Symbol: caution → `exclamationmark.triangle.fill`; compromised → `exclamationmark.octagon.fill` (UI-SPEC line 220 — octagon is louder; visual escalation parallels color escalation).
      * Text: per UI-SPEC § Copywriting line 681-682: caution → `"Risk flagged: \(reason ?? "")"`; compromised → `"Chain compromised: \(reason ?? "")"` (NSLocalizedString-wrapped templates).
      * Layout: horizontal `UIStackView` `[symbol, label]`, `DS.Spacing.sm` (8pt) spacing, `DS.Spacing.md` (16pt) leading/trailing padding, `DS.Spacing.sm` top/bottom padding. Minimum intrinsic height 36pt (matches LimitedTrustBannerView).
      * `isUserInteractionEnabled = false` (informational; not tappable — UI-SPEC line 223).
      * `accessibilityIdentifier = "load-detail.banner"` (alternatively `"chain-integrity-banner"` per UI-SPEC line 763 + PATTERNS E4 mirror — `chain-integrity-banner` is the locked identifier).
      * `accessibilityIdentifier = "chain-integrity-banner"` (PATTERNS table row 19 — mirrors `limited-trust-banner` precedent; top-level, not under `load-detail.*`).
      * `isAccessibilityElement = true`; `accessibilityTraits = .staticText`.
      * `accessibilityLabel = "Chain integrity: \(verdictSpoken). \(reason ?? "")"` per UI-SPEC line 684.
      * `intrinsicContentSize` override returning `CGSize(width: noIntrinsicMetric, height: 36.0)` (mirrors LimitedTrustBannerView).
    - `ChainIntegrityVerdictBlockView: UIView`:
      * `public init(verdict: ChainIntegrity.Verdict, reason: String?, implicatedNodeCount: Int)`.
      * Rounded card, cornerRadius 8pt.
      * Background: `.caution` → `DS.Colors.caution.withAlphaComponent(0.15)`; `.compromised` → `DS.Colors.destructive.withAlphaComponent(0.15)`; `.clean` → init does not call setUp; `isHidden = true` (parent's `installVerdictBlock` is what conditionally adds this view at all — see below).
      * Vertical stack:
        - Row 1: horizontal stack — SF Symbol (triangle for caution; octagon for compromised) tinted DS.Colors.caution / DS.Colors.destructive + verdict label `DS.Typography.headline`, text "Caution" / "Compromised" (NSLocalizedString).
        - Row 2: `chainIntegrity.reason ?? ""` rendered verbatim as `DS.Typography.body`, `.label` color, multi-line, `adjustsFontForContentSizeCategory = true`.
        - Row 3: when `implicatedNodeCount > 0`: `.footnote` `DS.Colors.labelSecondary`, text "{N} parties implicated" (singular "1 party implicated") — NSLocalizedString-wrapped with a plural rule.
      * `accessibilityIdentifier = "load-detail.body.verdict-block"`.
      * Internal padding `DS.Spacing.md` on all sides.
    - `LoadDetailBodyView` modifications:
      * Add a public method `public func installVerdictBlock(verdict: ChainIntegrity.Verdict, reason: String?, implicatedNodeCount: Int)`:
        - If `verdict == .clean`: ensure `verdictBlockContainer.isHidden = true` and (defensively) clear its subviews.
        - Else: clear `verdictBlockContainer.subviews`; build a new `ChainIntegrityVerdictBlockView(...)`; add as subview; pin edges; `verdictBlockContainer.isHidden = false`.
      * Modify `configure(load:)` to NOT call this directly (chainIntegrity is needed); the VC calls `installVerdictBlock` AFTER `configure(load:)` (the VC owns the chainOfTrust storage from Plan 07).
    - `ChainIntegrityBannerViewSnapshotTests` populated (3 methods per RESEARCH §3 line 398):
      * `test_cautionVerdict_rendersYellowBanner()`: build banner with `.caution` + sample reason; configure; snapshot at `CGSize(width: 393, height: 36)`; assert `backgroundColor` matches `DS.Colors.caution` ramp; assert `accessibilityLabel.contains("Caution")`.
      * `test_compromisedVerdict_rendersRedBanner()`: build with `.compromised`; assert red background; assert accessibilityLabel contains "Compromised" + reason fragment.
      * `test_cleanVerdict_bannerIsHidden()`: NO snapshot; assert that `ChainIntegrityBannerView(verdict: .clean, reason: nil)` has `isHidden == true` AND `intrinsicContentSize.height == 0` (or .zero) — proves the clean state doesn't claim hit-test surface (UI-SPEC line 118).
  </behavior>
  <read_first>
    - validationLedger/UI/LimitedTrustBannerView.swift (lines 35-104 — PATTERNS E4 the EXACT file-shape twin Phase 9 mirrors verbatim)
    - validationLedger/Core/Load/ChainIntegrity.swift (the Verdict enum + reason + implicated sets surface)
    - validationLedger/UI/DesignSystem/Colors.swift (DS.Colors.caution, DS.Colors.destructive)
    - validationLedger/UI/DesignSystem/Spacing.swift / Typography.swift
    - validationLedgerUITests/LimitedTrustBannerTests.swift (the Phase 4 XCUITest precedent for banner accessibility identifier — Phase 9 mirrors `chain-integrity-banner` identifier)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-15 fraud visual language; D-16 banner fixed posture + intentional duplication; D-11 implicated reason inline)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (§ Banner lines 212-225; § Pre-fraud-tier color invariants table lines 626-630; § Chain-integrity verdict block lines 389-402; § Copywriting Chain-integrity banner lines 676-684; § Copywriting Bill-of-lading body lines 686-697; § Accessibility identifiers — `chain-integrity-banner` line 763 + `load-detail.body.verdict-block` line 775)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§2 lines 320-323 banner shimmer — none; just static color; § Security Domain — verdict drives visual; Phase 7 D-18 LOCK)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (E4 — banner file shape; table row 19 ChainIntegrityBannerView; § Shared Patterns — DS-token + Dynamic Type)
  </read_first>
  <action>
    A. CREATE `validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift`. Mirror `LimitedTrustBannerView.swift` lines 35-104 verbatim with the per-verdict deltas.

    Header: cite "Phase 9 TRUST-05 / D-15 / D-16; file-shape twin of `LimitedTrustBannerView` (PATTERNS E4); accessibilityIdentifier `chain-integrity-banner` mirrors `limited-trust-banner` precedent."

    Structure (PATTERNS E4 mirror):
    - `import UIKit`.
    - `public final class ChainIntegrityBannerView: UIView`.
    - `private static let bannerHeight: CGFloat = 36.0`.
    - Stored:
      * `private let verdict: ChainIntegrity.Verdict`.
      * `private let reason: String?`.
      * `private let label: UILabel`.
      * `private let symbolView: UIImageView`.
    - `public init(verdict: ChainIntegrity.Verdict, reason: String?)`:
      * Store; `super.init(frame: .zero)`; `setUp()`.
    - `required init?(coder:) { fatalError(...) }`.
    - `private func setUp()`:
      * If `verdict == .clean`: `isHidden = true`; return early (don't build content; PATTERNS table row 19 acceptance criterion).
      * Else: configure background, symbol, text per the locked mapping (see `<behavior>`).
      * `isUserInteractionEnabled = false` (banner is informational; UI-SPEC line 223).
      * `accessibilityIdentifier = "chain-integrity-banner"`.
      * `isAccessibilityElement = true; accessibilityTraits = .staticText`.
      * Build accessibilityLabel: `NSLocalizedString("loads.detail.banner.a11yLabel", value: "Chain integrity: \(verdictSpoken). \(reason ?? "")", comment: ...)` where `verdictSpoken` is "Caution" or "Compromised" (localized from rawValue).
      * Layout: horizontal stack [symbolView, label]; pin with `DS.Spacing.md` leading/trailing, `DS.Spacing.sm` top/bottom (UI-SPEC line 222).
    - `public override var intrinsicContentSize: CGSize { verdict == .clean ? .zero : CGSize(width: UIView.noIntrinsicMetric, height: Self.bannerHeight) }`.

    B. CREATE `validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift`. Programmatic UIKit.

    Structure (UI-SPEC § Chain-integrity verdict block lines 389-402):
    - `import UIKit`.
    - `public final class ChainIntegrityVerdictBlockView: UIView`.
    - `public init(verdict: ChainIntegrity.Verdict, reason: String?, implicatedNodeCount: Int)`:
      * Store; super.init; setUp.
    - `required init?(coder:) { fatalError(...) }`.
    - `private func setUp()`:
      * `accessibilityIdentifier = "load-detail.body.verdict-block"`.
      * `layer.cornerRadius = 8`.
      * `backgroundColor`: caution → `DS.Colors.caution.withAlphaComponent(0.15)`; compromised → `DS.Colors.destructive.withAlphaComponent(0.15)`; clean → `isHidden = true` and return.
      * Build vertical stack:
        - Row 1 horizontal: SF Symbol (icon) tinted to the full-saturation verdict color + verdict-label (`DS.Typography.headline`, text "Caution" / "Compromised" via NSLocalizedString).
        - Row 2: `reason ?? ""` rendered as `DS.Typography.body`, `.label` color, multi-line, `adjustsFontForContentSizeCategory = true`.
        - Row 3 (only when implicatedNodeCount > 0): `.footnote`, `DS.Colors.labelSecondary`, text via NSLocalizedString with plural rule (Localizable.stringsdict-style) — for v1.1's English-only ship, render the simple `String(format: NSLocalizedString("loads.detail.verdict.implicated_count", value: "%d parties implicated", comment: ...), implicatedNodeCount)`. Handle singular "1 party implicated" with a separate localized key OR a conditional Swift expression: `implicatedNodeCount == 1 ? NSLocalizedString("loads.detail.verdict.implicated_singular", value: "1 party implicated", ...) : String(format: ..., implicatedNodeCount)`.
      * Pin stack with `DS.Spacing.md` inset on all edges.

    No logger; no derivation. Per T-09-04: reason text is rendered verbatim from server data.

    C. MODIFY `validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift`:

    Add a public method:
    ```
    public func installVerdictBlock(verdict: ChainIntegrity.Verdict, reason: String?, implicatedNodeCount: Int) {
        // Clear any existing content
        verdictBlockContainer.subviews.forEach { $0.removeFromSuperview() }
        guard verdict != .clean else {
            verdictBlockContainer.isHidden = true
            return
        }
        let block = ChainIntegrityVerdictBlockView(verdict: verdict, reason: reason, implicatedNodeCount: implicatedNodeCount)
        verdictBlockContainer.addSubview(block)
        block.translatesAutoresizingMaskIntoConstraints = false
        NSLayoutConstraint.activate([
            block.topAnchor.constraint(equalTo: verdictBlockContainer.topAnchor),
            block.bottomAnchor.constraint(equalTo: verdictBlockContainer.bottomAnchor),
            block.leadingAnchor.constraint(equalTo: verdictBlockContainer.leadingAnchor),
            block.trailingAnchor.constraint(equalTo: verdictBlockContainer.trailingAnchor),
        ])
        verdictBlockContainer.isHidden = false
    }
    ```

    The VC (Task 2 below) calls this in `render(state:)` `.loaded(...)` branch.

    D. POPULATE `validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift` (Plan 02 shells):

    - `test_cautionVerdict_rendersYellowBanner()`: build banner with `(verdict: .caution, reason: "Broker re-tendered to chameleon carrier")`. Render at `CGSize(width: 393, height: 36)`. UIKitSnapshot.image + attach. Assert `view.backgroundColor == DS.Colors.caution` (or equivalent — confirm DS token value). Assert `view.accessibilityLabel?.contains("Caution") == true` AND `view.accessibilityLabel?.contains("Broker re-tendered to chameleon carrier") == true`.
    - `test_compromisedVerdict_rendersRedBanner()`: same shape with `.compromised`; assert red background; accessibilityLabel contains "Compromised" + reason fragment.
    - `test_cleanVerdict_bannerIsHidden()`: build with `.clean`; assert `view.isHidden == true`; assert `view.intrinsicContentSize == .zero` (or `.height == 0`). NO snapshot attached for this case — the banner doesn't render.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/ChainIntegrityBannerViewSnapshotTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public final class ChainIntegrityBannerView: UIView' validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift` returns `1`
    - Source: `grep -c 'public init(verdict: ChainIntegrity.Verdict, reason: String?)' validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift` returns `1`
    - Source: `grep -c 'accessibilityIdentifier = "chain-integrity-banner"' validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift` returns `1`
    - Source: `grep -c 'isUserInteractionEnabled = false' validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift` returns `1`
    - Source: `grep -c 'verdict == .clean' validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift` returns `>= 1` (the clean-case early return + intrinsicContentSize override)
    - Source: `grep -c 'public final class ChainIntegrityVerdictBlockView: UIView' validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift` returns `1`
    - Source: `grep -c 'withAlphaComponent(0.15)' validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift` returns `>= 2` (caution + compromised soft tints)
    - Source: `grep -c 'public func installVerdictBlock' validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift` returns `1`
    - Source: `grep -v '^//' validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift | grep -cE 'Logger|os_log'` returns `0`
    - Source: `grep -c 'NSLocalizedString' validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift` returns `>= 1`
    - Source: `grep -c 'NSLocalizedString' validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift` returns `>= 3` (verdict title, plural counts, etc.)
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift` returns `0`
    - Behavior: ChainIntegrityBannerViewSnapshotTests passes with 3 methods (2 attaching images + 1 isHidden assertion)
  </acceptance_criteria>
  <done>
    ChainIntegrityBannerView + ChainIntegrityVerdictBlockView ship per UI-SPEC; banner is data-driven by verdict + reason, never derives; LoadDetailBodyView.installVerdictBlock() populates the verdict block conditionally; banner snapshot tests cover all 3 verdicts (2 visible + 1 hidden).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor LoadDetailViewController composition — lift pinnedSummaryHeader out of bodyView, add chainIntegrityBanner, branch on horizontalSizeClass for iPhone single-column vs iPad split layout (D-01/D-02/D-03), preserve zoomScale across the rebuild, set view.accessibilityElements per D-21, ship the iPad-split skeleton silhouette (Plan 04 holdover) + populate that one snapshot test</name>
  <files>
    validationLedger/Features/Loads/Detail/LoadDetailViewController.swift,
    validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift,
    validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift,
    validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.swift
  </files>
  <behavior>
    - `LoadDetailViewController` composition refactor:
      * Lift the pinned summary header OUT of LoadDetailBodyView into a top-level subview (`private let pinnedHeader = PinnedSummaryHeaderView()` — extract from bodyView OR keep it inside bodyView and accept the UI-SPEC line 90 vs 146 difference by passing a flag).
      * The cleaner refactor: extract the pinned-header subview from LoadDetailBodyView into a NEW small view (`PinnedSummaryHeaderView` inside `LoadDetailBodyView.swift` as a private nested type → make it internal/public) OR just expose `bodyView.pinnedHeaderView` so the VC can reposition it. Planner picks the cleanest path — the goal is: iPhone places the pinned header at the TOP of the VC view (above the banner); iPad places the pinned header at the top of the right pane (inside bodyView still works).
      * Add stored `private var chainIntegrityBanner: ChainIntegrityBannerView?` — built fresh on every `render(state:)` `.loaded` transition based on `chainOfTrust.integrity.verdict`.
      * Add stored `private var compositionLayout: CompositionLayout = .iPhoneCompact` (enum with `.iPhoneCompact, .iPadRegularSplit` cases).
      * Override `traitCollectionDidChange(_ previousTraitCollection:)`:
        - If `previousTraitCollection?.horizontalSizeClass != traitCollection.horizontalSizeClass`: capture current `trustGraphView.scrollView.zoomScale` AND `contentOffset`; tear down current constraints; rebuild for the new layout; restore zoomScale + contentOffset.
      * `private func buildIPhoneLayout()`:
        ```
        view subviews (top → bottom):
          pinnedHeader (pinned to safeAreaLayoutGuide.top)
          chainIntegrityBanner (pinned below pinnedHeader; hidden when clean)
          trustGraphView (pinned below banner; height: view.safeAreaLayoutGuide.layoutFrame.height * 0.62 via heightAnchor.constraint with multiplier=0.62 against the layoutFrame's height)
          bodyView (pinned below trustGraphView; bottom to safeAreaLayoutGuide.bottom)
        ```
        bodyView is configured with pinnedHeader REMOVED from its content stack (or hidden inside).
      * `private func buildIPadSplitLayout()`:
        ```
        view subviews:
          chainIntegrityBanner (pinned to safeAreaLayoutGuide.top, full width)
          horizontalStack (UIStackView, horizontal, .fill, spacing 0):
            trustGraphView (widthAnchor.multiplier = 0.60 against the horizontalStack's width)
            rightPaneContainer (widthAnchor.multiplier = 0.40):
              pinnedHeader (top)
              bodyView (below pinnedHeader, fills remaining; pinnedHeader REMOVED from body's content stack)
        ```
      * `render(state:)` `.loaded(load, chainOfTrust)` branch additions:
        - Build a fresh `ChainIntegrityBannerView(verdict: chainOfTrust.integrity.verdict, reason: chainOfTrust.integrity.reason)`; replace `chainIntegrityBanner` reference; add to view; pin per current composition.
        - Banner.isHidden flows from the `.clean` early-return logic in the banner's setUp (Task 1).
        - Call `bodyView.installVerdictBlock(verdict: ..., reason: ..., implicatedNodeCount: chainOfTrust.integrity.implicatedNodeIDs.count)`.
      * Set `view.accessibilityElements` per D-21:
        - iPhone: `[pinnedHeader, chainIntegrityBanner-if-not-nil, trustGraphView, bodyView]`. The bodyView's internal sections are individually accessible via the existing accessibility identifiers (the timelineContainer's stepper is a single combined element; the freight rows are individual elements; etc.).
        - iPad: `[chainIntegrityBanner, rightPaneContainer (pinnedHeader → timeline → freight → parties → verdictBlock), trustGraphView]`.
      * Call `view.setNeedsLayout()` after composition rebuild.
    - `LoadDetailSkeletonView` modification: add the iPad-split silhouette (Plan 04 holdover).
      * Add `public func renderSplitSilhouette()` — sets up the iPad split silhouette: left pane (skeleton graph circles) + right pane (skeleton header rectangle + 4 grey body rows).
      * Branch in `setUp()` OR add a `public var renderMode: RenderMode = .iPhonePortrait { didSet { rebuild() } }` enum with `.iPhonePortrait, .iPadSplit`. The VC's skeleton container sets `renderMode = .iPadSplit` when the VC's compositionLayout is iPadRegularSplit.
    - `LoadDetailSkeletonViewSnapshotTests.test_iPadSplitSilhouette_rendersExpectedFrame()` (Plan 04's XCTSkip): replace with real assertion:
      - Build view; set `renderMode = .iPadSplit`; render at `CGSize(width: 1024, height: 768)` (iPad landscape); UIKitSnapshot; assert the split silhouette has left + right regions with the expected child count (e.g. ≥5 circle subviews in left pane + ≥4 row rectangles in right pane).
  </behavior>
  <read_first>
    - validationLedger/Features/Loads/Detail/LoadDetailViewController.swift (Plan 03/04/06/07/08 cumulative state — the layoutContent, render(state:), cachedChainOfTrust, presentVerificationBasisSheet, presentHandoffDetailSheet, the graph + body subviews; needs careful refactor to lift pinnedHeader)
    - validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift (Plan 04 — the existing pinnedSummaryHeader subview position; whether it's tightly coupled to the body's contentStack or extractable)
    - validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift (Plan 04 output — the existing iPhone silhouette implementation; add the iPad branch additively)
    - validationLedger/Features/Loads/Detail/TrustGraphView.swift (Plan 06 — verify `scrollView.zoomScale` is publicly readable; the role-slot iPad coordinates already shipped per Plan 06's traitCollectionDidChange branch)
    - validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift (Task 1 output)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-01 graph dominates; D-02 iPhone below-the-fold body; D-03 iPad split; D-19 skeleton mirrors split; D-21 traversal order)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (§ iPhone composition lines 86-121 ASCII diagram; § iPad regular width lines 122-147 ASCII diagram; § iPad Native-Rendering Contract lines 807-818; § VoiceOver Traversal order lines 823-830; § Skeleton iPad skeleton lines 503-505)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§ Common Pitfalls — `traitCollectionDidChange` size-class branch is a NOVEL pattern per PATTERNS § Novel Patterns line 66; no in-repo prior art; UI-SPEC + CONTEXT D-03 are the source of truth)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (§ Novel Patterns line 66 traitCollectionDidChange; table row 25 — the size-class branch is the novel pattern this plan ships)
  </read_first>
  <action>
    A. MODIFY `validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift` (extend Plan 04's implementation):

    1. Add `public enum RenderMode { case iPhonePortrait, iPadSplit }`.
    2. Add stored `public var renderMode: RenderMode = .iPhonePortrait { didSet { rebuildSilhouette() } }`.
    3. Refactor `setUp()` to call `rebuildSilhouette()`.
    4. Add `private func rebuildSilhouette()`:
       - Clear current silhouette subviews (NOT the shimmerLayer; preserve animation lifecycle).
       - Switch on renderMode:
         - `.iPhonePortrait`: build the existing iPhone silhouette (Plan 04 work).
         - `.iPadSplit`: build the iPad split silhouette:
           - Left pane (60% width): 5 grey circles at the iPad-coordinate role slots + connector lines (mirrors the iPhone graph silhouette geometry, scaled).
           - Right pane (40% width): pinned-header rectangle + 4 grey body rows.
           - Use a horizontal `UIStackView` to host the two panes.
    5. Re-attach `shimmerLayer.frame = bounds; startShimmer()` after rebuild (Pitfall 1).

    B. MODIFY `validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift` (Plan 04 + 05 + this plan):

    1. Expose the pinned summary header behavior through a parameter on `LoadDetailBodyView.configure(load:hidesPinnedHeader:)`. The body keeps `pinnedSummaryHeader` as an internal stored subview of its content stack; the configure-time flag controls whether the body populates and shows that embedded pinned header. (LOCKED path resolves I-05 — DO NOT expose `pinnedSummaryHeaderView` as a public `var`; DO NOT extract `PinnedSummaryHeaderView` into a new type in this plan.)
    2. Add `public var hidesPinnedSummaryHeader: Bool` as a stored property on `LoadDetailBodyView`. When `true`, the body's `configure(load:hidesPinnedHeader:)` path does NOT populate the embedded pinned header (iPhone composition: the VC owns a top-level pinned-header instance instead — currently a duplicate built by the VC's `pinnedHeaderTopLevelView`). When `false`, the body populates and shows its embedded pinned header (iPad composition: the body owns the pinned header at the top of the right pane).
    3. Add the new method signature `public func configure(load: Load, hidesPinnedHeader: Bool)` to `LoadDetailBodyView`. The implementation: stash `hidesPinnedHeader` (or read directly), set the embedded pinned-header subview's `isHidden` accordingly, and proceed with the rest of the body configuration (timeline + freight + parties + verdict-block slots). The VC calls this new signature exclusively (replaces any prior `configure(load:)` call from earlier plans within this composition refactor).

    C. MODIFY `validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` — the largest refactor in this plan:

    1. Add `private enum CompositionLayout { case iPhoneCompact, iPadRegularSplit }`.
    2. Add stored `private var compositionLayout: CompositionLayout = .iPhoneCompact` (initial value updated in viewDidLoad based on `traitCollection.horizontalSizeClass`).
    3. Add stored `private var chainIntegrityBanner: ChainIntegrityBannerView?`.
    4. Add stored `private var pinnedHeaderTopLevelView: UIView?` (the lifted-out pinned header for iPhone; nil for iPad where bodyView owns it).
    5. Add stored constraint properties for the composition (so they can be deactivated on rebuild): `private var compositionConstraints: [NSLayoutConstraint] = []`.
    6. Override `traitCollectionDidChange(_ previousTraitCollection:)`:
       - Detect size-class change: `let oldSC = previousTraitCollection?.horizontalSizeClass; let newSC = traitCollection.horizontalSizeClass; guard oldSC != newSC else { return }`.
       - Capture state: `let savedZoom = trustGraphView.scrollView.zoomScale; let savedOffset = trustGraphView.scrollView.contentOffset`.
       - Tear down: `NSLayoutConstraint.deactivate(compositionConstraints); compositionConstraints.removeAll()`.
       - Set `compositionLayout = (newSC == .regular) ? .iPadRegularSplit : .iPhoneCompact`.
       - Call `buildLayoutForCurrentComposition()`.
       - Restore state: `trustGraphView.scrollView.zoomScale = savedZoom; trustGraphView.scrollView.contentOffset = savedOffset`.
       - Update view.accessibilityElements per D-21.
       - Update `skeletonView.renderMode = (compositionLayout == .iPadRegularSplit) ? .iPadSplit : .iPhonePortrait`.
    7. `private func buildLayoutForCurrentComposition()`:
       - Per the `compositionLayout` value, call either `buildIPhoneLayout()` or `buildIPadSplitLayout()`.
    8. `private func buildIPhoneLayout()`:
       - Set up the iPhone vertical stack: pinned header (top) + banner-if-present + trust graph (62% height) + bodyView (remaining).
       - Activate constraints; append to `compositionConstraints` for future teardown.
       - Tell bodyView `hidesPinnedSummaryHeader = true`; ensure top-level pinned header exists.
    9. `private func buildIPadSplitLayout()`:
       - Set up the horizontal split: banner (top, full width) + horizontalSplit (trust graph 60% left + rightPaneContainer 40% right with bodyView whose `hidesPinnedSummaryHeader = false` so the body owns its pinned header).
       - Activate constraints; append to `compositionConstraints`.
    10. Modify `render(state:)`:
        - `.loaded(load, chainOfTrust)` branch:
          - Save chainOfTrust to cachedChainOfTrust (Plan 07 storage already exists).
          - Build/replace the ChainIntegrityBannerView with the new verdict + reason; add to view if non-clean (else hide).
          - Call `bodyView.installVerdictBlock(verdict:, reason:, implicatedNodeCount:)`.
          - Configure trustGraphView per Plan 06 (already wired).
          - Configure body (Plan 04 + 05 wiring).
          - Update view.accessibilityElements per D-21.
    11. `private func updateAccessibilityElements()`:
        - Per current compositionLayout, build the ordered array per UI-SPEC line 827-830.
        - Assign `view.accessibilityElements = [...]`.

    D. POPULATE `validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.test_iPadSplitSilhouette_rendersExpectedFrame()` (Plan 04's lone XCTSkip):

    - Build `LoadDetailSkeletonView()`; set `renderMode = .iPadSplit`; force layoutIfNeeded.
    - Render at `CGSize(width: 1024, height: 768)` (iPad landscape canvas).
    - UIKitSnapshot.image + attach.
    - Assertions: subviews include a horizontal split structure (find by traversal); ≥5 circle-shaped subviews in the left region; ≥4 rectangle row subviews in the right region.

    Per T-09-03 (no client trust): the banner's color + verdict block's tint come ENTIRELY from `chainIntegrity.verdict` + `reason` + `implicatedNodeIDs.count` — server-supplied. No derivation. Source-assertion grep gate.

    Per T-09-04 (PII): banner and verdict block render `reason` verbatim from server data; no logger touches the reason string.

    Per T-09-11 (composition rebuild on trait change is novel): RESEARCH §2 Pitfall 1 + PATTERNS § Novel Patterns line 66 — pulse animation must re-attach after the rebuild; Plan 06's `traitCollectionDidChange(_:)` in TrustGraphView calls `startPulseIfNeeded()`, and Plan 04's `LoadDetailSkeletonView.traitCollectionDidChange(_:)` re-attaches shimmer. This plan's VC-level rebuild must preserve those callsites — verified by leaving `trustGraphView` and `skeletonView` instances in place across the rebuild (only constraints are deactivated/reactivated; the views themselves are not torn down).
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/LoadDetailSkeletonViewSnapshotTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'traitCollectionDidChange' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1`
    - Source: `grep -c 'enum CompositionLayout' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1`
    - Source: `grep -c 'horizontalSizeClass' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1`
    - Source: `grep -c 'view.accessibilityElements' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1` (D-21 traversal order)
    - Source: `grep -c 'savedZoom\|zoomScale' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1` (preserve zoomScale across rebuild per D-03 / UI-SPEC line 813)
    - Source: `grep -c 'ChainIntegrityBannerView(' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1`
    - Source: `grep -c 'installVerdictBlock' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 1`
    - Source: `grep -c 'widthAnchor.constraint.*multiplier' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 2` (the 0.60 / 0.40 iPad split)
    - Source: `grep -c '0\.60\|0\.40' validationLedger/Features/Loads/Detail/LoadDetailViewController.swift` returns `>= 2`
    - Source: `grep -c 'enum RenderMode' validationLedger/Features/Loads/Detail/LoadDetailSkeletonView.swift` returns `>= 1`
    - Source: `grep -c 'XCTSkip' validationLedgerTests/Loads/Snapshot/LoadDetailSkeletonViewSnapshotTests.swift` returns `0` (Plan 04 had 1; this plan removes it)
    - Source: `grep -c 'hidesPinnedSummaryHeader' validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift` returns `>= 1` (LOCKED path per I-05 — the stored property exists on the body)
    - Source: `grep -c 'configure(load:hidesPinnedHeader:)' validationLedger/Features/Loads/Detail/LoadDetailBodyView.swift` returns `>= 1` (LOCKED path per I-05 — the new method signature exists on the body)
    - Source: `grep -v '^//' validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift validationLedger/Features/Loads/Detail/ChainIntegrityVerdictBlockView.swift validationLedger/Features/Loads/Detail/LoadDetailViewController.swift | grep -cE 'derive.*Verdict\|computeIntegrity\|isChainCompromised'` returns `0` (negative gate — no client-side derivation; Phase 7 D-18 LOCK)
    - Behavior: LoadDetailSkeletonViewSnapshotTests passes with all 5 methods (Plan 04's iPhone + shimmer-re-attach + traitCollection-re-attach + reduce-motion + this plan's iPad split)
    - Behavior: build green; the existing TrustGraphView + Plan 07/08 sheets still work post-refactor (LoadDetailFlowTests.test_rowTap_pushesDetail + test_nodeTap_opensVerificationBasisSheet + test_edgeTap_opensHandoffSheet all still pass)
    - Behavior: TrustGraphView's pulse animation continues running across a programmatic horizontalSizeClass change (the rebuild does not tear down the trustGraphView instance — only its constraints — so the pulse re-attaches via the existing traitCollectionDidChange handler in Plan 06)
  </acceptance_criteria>
  <done>
    iPhone vs iPad composition refactor lands; chain-integrity banner is pinned (D-16); chain-integrity verdict block renders conditionally in the body (D-02); pinned summary header lives at top of iPhone view OR top of iPad right pane; zoomScale + contentOffset preserved across rebuild (UI-SPEC line 813); view.accessibilityElements ordered per D-21; iPad-split skeleton silhouette ships + Plan 04's XCTSkip removed; ChainIntegrityBannerViewSnapshotTests + LoadDetailSkeletonViewSnapshotTests all pass; full Phase 9 regression smoke (rowTap + nodeTap + edgeTap XCUITests) still passes post-refactor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ChainIntegrity.verdict` + `reason` → banner + verdict block render | Server-supplied chain-level data drives BOTH the marquee banner AND the in-body verdict block (D-16 intentional duplication). Phase 7 D-18 LOCK enforced verbatim; the contract layer (ChainIntegrity.swift line 83) already fail-closed-decodes unknown verdicts to .compromised. |
| `traitCollectionDidChange(_:)` size-class rebuild | Novel pattern (PATTERNS § Novel Patterns line 66). The rebuild tears down and reactivates constraints but never destroys the trustGraphView / skeletonView instances — preserves zoomScale, contentOffset, and pulse animation continuity. |
| iPad split layout proportions | The 0.60 / 0.40 ratio is locked by UI-SPEC line 143 + 815 + tested at iPad mini portrait (744pt) → ~298pt right pane, clearing Apple HIG readable-width minimum. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | Client-side trust derivation in banner / verdict block / composition | mitigate | EVERY visual element (banner color/icon/copy, verdict block tint/title/body, banner isHidden) is driven by `chainIntegrity.verdict` enum case + `reason` literal + `implicatedNodeIDs.count`. No derive*() methods; no computed-property bools synthesizing trust state. Source-assertion grep gate returns 0 for derived-verdict patterns. |
| T-09-04 | Information Disclosure (PII) | Banner / verdict block / composition rebuild | mitigate | `reason` is server-supplied; rendered verbatim in BOTH banner accessibilityLabel AND verdict-block body text. NO logger calls (T-08-08 mirror). Snapshot tests use Phase 7 fixture corpus only. |
| T-09-11 | Denial of Service | Animation continuity across size-class rebuild | mitigate | Pitfall 1 across the VC-level rebuild: the rebuild deactivates constraints but does NOT remove the trustGraphView / skeletonView from the view hierarchy → existing `traitCollectionDidChange(_:)` handlers in those subviews call `startPulseIfNeeded()` / `startShimmer()` to re-attach animations. Manual verification per VALIDATION.md "Halo pulse animation on compromised node" + "Skeleton-with-shimmer continuity" rows. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies. |
</threat_model>

<verification>
1. Build green: `xcodebuild build` exits 0.
2. Banner snapshot suite green: `-only-testing:validationLedgerTests/ChainIntegrityBannerViewSnapshotTests` passes (3 methods — caution + compromised + clean-hidden).
3. iPad skeleton snapshot green: `-only-testing:validationLedgerTests/LoadDetailSkeletonViewSnapshotTests` passes with ZERO XCTSkip (Plan 04's iPad-split test now passes).
4. Phase 9 regression smoke: all XCUITests still pass post-composition-refactor — `test_rowTap_pushesDetail` (Plan 03), `test_nodeTap_opensVerificationBasisSheet` (Plan 07), `test_edgeTap_opensHandoffSheet` (Plan 08).
5. Zero-PII gate: `grep -v '^//' ChainIntegrityBannerView.swift ChainIntegrityVerdictBlockView.swift LoadDetailViewController.swift | grep -cE 'Logger|os_log'` returns 0.
6. No-client-derivation gate: `grep -v '^//' ChainIntegrityBannerView.swift ChainIntegrityVerdictBlockView.swift LoadDetailViewController.swift | grep -cE 'derive.*Verdict|computeIntegrity|isChainCompromised'` returns 0.
7. UI-SPEC composition lock: `grep -c '0\.60\|0\.40' LoadDetailViewController.swift >= 2` (iPad split).
</verification>

<success_criteria>
- ChainIntegrityBannerView (D-15/D-16) and ChainIntegrityVerdictBlockView (D-02) exist with verdict-driven rendering.
- LoadDetailViewController's composition branches on horizontalSizeClass per D-01/D-02/D-03; iPhone single-column vs iPad 60/40 split.
- ZoomScale + contentOffset preserved across rebuild (UI-SPEC line 813).
- view.accessibilityElements ordered per D-21 (iPhone: pinned-header → banner → graph → timeline → body; iPad: banner → right-pane-first → graph-last).
- Banner is pinned and never scrolls away (D-16); rendered ONLY when verdict != .clean.
- Verdict block rendered in body ONLY when verdict != .clean (D-02 intentional duplication of banner data).
- iPad-split skeleton silhouette ships (D-19); the Plan 04 XCTSkip is removed.
- 3 banner snapshot tests + 1 new iPad skeleton snapshot test all pass.
- All Phase 9 XCUITests (rowTap + nodeTap + edgeTap) still pass post-refactor.
- Zero new SwiftPM dependencies.
- Zero logger calls in any view file.
- Zero client-side derivation of trust state (Phase 7 D-18 LOCK enforced by grep gate).
</success_criteria>

<output>
Create `.planning/phases/09-load-detail-chain-of-trust-graph/09-09-SUMMARY.md` when done. Record:
- ChainIntegrityBannerView surface (init signature + accessibilityIdentifier + verdict→color/icon/copy mapping).
- ChainIntegrityVerdictBlockView surface (init signature + plural-rule for implicated count).
- LoadDetailViewController composition refactor (the lifted-out pinnedHeader path + the buildIPhoneLayout / buildIPadSplitLayout methods + the savedZoom restore + the accessibilityElements ordering).
- The LoadDetailBodyView changes (hidesPinnedSummaryHeader, installVerdictBlock).
- LoadDetailSkeletonView.RenderMode + the iPad-split silhouette geometry.
- The 4 snapshot tests landed (3 banner + 1 iPad skeleton).
- D-21 traversal-order assertion strategy (programmatic test OR manual verification — VALIDATION.md Manual Verifications already lists VoiceOver traversal as manual).
- Any open question — e.g. whether to factor PinnedSummaryHeaderView into its own file vs keeping it inside LoadDetailBodyView; whether the LoadDetailSkeletonView.RenderMode enum should also expose a `.iPadPortraitSingleColumn` for the Multitasking-narrow size class on iPad (planner discretion; UI-SPEC line 147 says rotation animates the split into existence — multitasking-narrow falls back to compact, so no new mode is needed).
</output>
