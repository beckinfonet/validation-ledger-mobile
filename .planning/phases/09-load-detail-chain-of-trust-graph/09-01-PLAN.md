---
phase: 09-load-detail-chain-of-trust-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - validationLedger/Core/Load/PriorRelationship.swift
  - validationLedger/Core/Load/ChainOfTrust.swift
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1002.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1003.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1004.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1005.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1006.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1007.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1008.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1009.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1010.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1011.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1012.json
  - validationLedgerTests/Load/ChainOfTrustDecodeTests.swift
  - validationLedgerTests/Load/PriorRelationshipDecodeTests.swift
autonomous: true
requirements:
  - TRUST-03
threat_refs:
  - T-09-01
  - T-09-02
  - T-09-03

must_haves:
  truths:
    - "D-12: TrustNode.priorRelationshipCount is REMOVED; TrustNode.priorRelationships: [PriorRelationship] is the only surface — no aliasing, no parallel fields"
    - "D-13: PriorRelationship is a pure Decodable & Sendable value type with exactly four fields (loadID, occurredAt, counterpartyRole, counterpartyDisplayName?); only loadID has an explicit CodingKey case (post-conversion form 'loadId')"
    - "D-14: Every load-detail-VL-*.json fixture (12 files) has prior_relationship_count REMOVED and prior_relationships: [...] ADDED on every TrustNode; clean carriers carry 5+ priors; chameleon-archetype flagged carriers carry 0 (the empty-array fraud signal); double-broker and factoring-fraud archetypes get curated patterns"
    - "Phase 8 D-18 (no-client-trust) inheritance: PriorRelationship is server-supplied data; iOS renders only"
    - "Snapshot artefacts and fixtures contain no PII — synthetic company names only (Acme Brokerage, FreightWise, etc.)"
    - "No new SwiftPM dependencies (Package.swift byte-identical)"
    - "iOS 17 deployment minimum honored; Decodable & Sendable types are iOS 17+ safe"
  artifacts:
    - path: "validationLedger/Core/Load/PriorRelationship.swift"
      provides: "D-13 value type; renders prior load history in TRUST-03 sheet"
      contains: "public struct PriorRelationship: Decodable, Sendable"
    - path: "validationLedger/Core/Load/ChainOfTrust.swift"
      provides: "TrustNode contract evolution — priorRelationships: [PriorRelationship] replaces the legacy Int"
      contains: "public let priorRelationships: [PriorRelationship]"
    - path: "validationLedgerTests/Load/PriorRelationshipDecodeTests.swift"
      provides: "Wire-format gate: load_id → loadID via .convertFromSnakeCase + explicit CodingKey raw value 'loadId'; D-13 acceptance"
      contains: "@Suite"
  key_links:
    - from: "validationLedger/Core/Load/ChainOfTrust.swift"
      to: "validationLedger/Core/Load/PriorRelationship.swift"
      via: "TrustNode.priorRelationships array element type"
      pattern: "priorRelationships:\\s*\\[PriorRelationship\\]"
    - from: "validationLedger/Core/Networking/APIClient.swift"
      to: "validationLedger/Core/Load/PriorRelationship.swift"
      via: ".convertFromSnakeCase decoding strategy recurses into nested arrays of Decodable structs"
      pattern: "loadID\\s*=\\s*\"loadId\""
---

<objective>
Evolve the Phase 7 TrustNode contract (D-12) so that every fixture and downstream consumer reads prior-relationship history as a fully-populated `[PriorRelationship]` array — never as an `Int` count. Author the new `PriorRelationship` value type (D-13). Re-author every Phase 7 `load-detail-VL-*.json` fixture so the prior-relationship history reinforces the platform thesis (D-14): clean carriers carry 5+ priors, chameleon-archetype flagged carriers carry 0 ("First time working together" — the empty-array IS the fraud signal). Update the Phase 7 ChainOfTrust decode tests for the new shape; ship a dedicated PriorRelationship decode test asserting the trailing-acronym wire bridge.

Purpose: Phase 9's verification-basis sheet (TRUST-03, Plan 07) renders a tappable LIST of prior loads — not a count badge. Without this contract evolution, Plan 07 has no data to render. This plan is the foundation that unlocks every downstream plan that touches the detail data.

Output: One new value type + one modified domain aggregate + 12 re-authored fixtures + an updated Phase 7 decode test + a new contract-level decode test. Zero new dependencies. Zero changes to APIClient, MockURLProtocol, or LoadDetailEndpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md
@.planning/phases/09-load-detail-chain-of-trust-graph/09-VALIDATION.md

@.planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md

@validationLedger/Core/Load/ChainOfTrust.swift
@validationLedger/Core/Load/Load.swift
@validationLedger/Core/Networking/APIClient.swift
@validationLedgerTests/Load/ChainOfTrustDecodeTests.swift
@validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json

<interfaces>
<!-- The Phase 7 trailing-acronym CodingKeys discipline Phase 9 follows verbatim.
     Source: validationLedger/Core/Load/ChainOfTrust.swift lines 32-49, 117-132. -->

From validationLedger/Core/Load/ChainOfTrust.swift (current state, lines 117-132):
- `public let priorRelationshipCount: Int` — REMOVED in this plan
- `private enum CodingKeys: String, CodingKey { ... case priorRelationshipCount }` — case REMOVED

After this plan:
- `public let priorRelationships: [PriorRelationship]` — ADDED
- `CodingKeys` enum has NO explicit case for `priorRelationships` (no trailing acronym; `.convertFromSnakeCase` handles `prior_relationships` → `priorRelationships`)

From validationLedger/Core/Networking/APIClient.swift `defaultDecoder()`:
- `keyDecodingStrategy = .convertFromSnakeCase` (recurses through nested Decodable arrays — verified per Sarunw docs)
- `dateDecodingStrategy = .iso8601` (used by `occurredAt`)

PriorRelationship CodingKeys discipline (PATTERNS E6 + RESEARCH §7):
- Only `loadID` is a trailing-acronym field → explicit case `loadID = "loadId"` (POST-CONVERSION form, NOT `"load_id"`)
- `occurredAt`, `counterpartyRole`, `counterpartyDisplayName` all rely on synthesized + `.convertFromSnakeCase` (NO explicit cases)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PriorRelationship.swift value type (D-13)</name>
  <files>validationLedger/Core/Load/PriorRelationship.swift</files>
  <read_first>
    - validationLedger/Core/Load/ChainOfTrust.swift (lines 32-49, 117-132 — the trailing-acronym CodingKeys discipline and the file-header doc-block template; PATTERNS E6 cites this verbatim)
    - validationLedger/Core/Networking/APIClient.swift (verify `defaultDecoder()` returns `JSONDecoder` with `.convertFromSnakeCase` + `.iso8601`)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-13 — indicative field set; D-12 — the breaking change rationale)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§7 "Codable Contract Refactor" lines 706-867 — full recipe including the §7 Gotcha that `loadID = "load_id"` would fail; §7 Acceptance criteria block)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (E6 — trailing-acronym CodingKeys discipline lines 278-305)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-UI-SPEC.md (§ Reusable Component Inventory → Contract evolution, line 916 — `counterpartyDisplayName` is OPTIONAL; planner discretion preserved as String?)
  </read_first>
  <action>
    Create `validationLedger/Core/Load/PriorRelationship.swift`. Per D-13 + RESEARCH §7, declare `public struct PriorRelationship: Decodable, Sendable` with exactly four properties in this order:

    - `public let loadID: String` — server-supplied stable VL-#### reference of the prior load; renders as "VL-1023" in the TRUST-03 sheet
    - `public let occurredAt: Date` — ISO-8601 wire value; renders via `RelativeDateTimeFormatter` ("3 months ago")
    - `public let counterpartyRole: Role` — server-supplied; renders as the "Broker → Carrier" framing in the sheet row
    - `public let counterpartyDisplayName: String?` — Optional; renders alongside the framing when present (UI-SPEC Open Question #1; Plan 07 finalizes the row rendering decision — keep this OPTIONAL on the value type)

    Add a `private enum CodingKeys: String, CodingKey` with ONLY one explicit case: `case loadID = "loadId"`. The other three fields rely on `.convertFromSnakeCase` synthesis (no explicit case — RESEARCH §7 line 805 confirms `occurredAt`, `counterpartyRole`, `counterpartyDisplayName` map cleanly under the post-conversion form). NEVER use raw value `"load_id"` — that fails to match because `.convertFromSnakeCase` converts BEFORE matching (RESEARCH Pitfall 5).

    Add a file-header doc-block mirroring `ChainOfTrust.swift` lines 32-49 — cite "Phase 9 D-12 / D-13", state the wire-format mapping recipe verbatim (per RESEARCH §7 lines 750-758), reference Phase 7 D-18 ("server-supplied; iOS renders only") and `APIClient.defaultDecoder()`'s `.convertFromSnakeCase` + `.iso8601` strategy. No `import` beyond `Foundation`.

    Do NOT add a custom `init(from:)` — rely on the synthesized initializer. Per T-09-04 (PII): no logging in this file. Per T-09-03 (no-client-trust): pure value type, no behavior.
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' build -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^public struct PriorRelationship: Decodable, Sendable' validationLedger/Core/Load/PriorRelationship.swift` returns `1`
    - Source: `grep -c 'public let loadID: String' validationLedger/Core/Load/PriorRelationship.swift` returns `1`
    - Source: `grep -c 'public let occurredAt: Date' validationLedger/Core/Load/PriorRelationship.swift` returns `1`
    - Source: `grep -c 'public let counterpartyRole: Role' validationLedger/Core/Load/PriorRelationship.swift` returns `1`
    - Source: `grep -c 'public let counterpartyDisplayName: String?' validationLedger/Core/Load/PriorRelationship.swift` returns `1`
    - Source: `grep -c 'case loadID = "loadId"' validationLedger/Core/Load/PriorRelationship.swift` returns `1` (post-conversion form, NOT `"load_id"`)
    - Source: `grep -c 'case loadID = "load_id"' validationLedger/Core/Load/PriorRelationship.swift` returns `0` (negative gate — Pitfall 5)
    - Source: `grep -c 'init(from' validationLedger/Core/Load/PriorRelationship.swift` returns `0` (no custom decoder)
    - Behavior: project compiles green
  </acceptance_criteria>
  <done>
    File exists at `validationLedger/Core/Load/PriorRelationship.swift`; struct compiles; no custom decoder; trailing-acronym CodingKey raw value is `"loadId"` (post-conversion form per Pitfall 5); project build green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mutate ChainOfTrust.swift — replace TrustNode.priorRelationshipCount with priorRelationships: [PriorRelationship] (D-12) + update Phase 7 ChainOfTrustDecodeTests for the new shape</name>
  <files>
    validationLedger/Core/Load/ChainOfTrust.swift,
    validationLedgerTests/Load/ChainOfTrustDecodeTests.swift
  </files>
  <read_first>
    - validationLedger/Core/Load/ChainOfTrust.swift (lines 41-44 header doc-block; line 117 `priorRelationshipCount`; lines 122-132 CodingKeys enum — the exact mutation sites)
    - validationLedger/Core/Load/PriorRelationship.swift (Task 1 output — the new element type)
    - validationLedgerTests/Load/ChainOfTrustDecodeTests.swift (every reference to `priorRelationshipCount` — assertions to rewrite as `priorRelationships.count`)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json (line 95-150 — the existing nodes array with legacy `prior_relationship_count`; Task 3 below re-authors)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§7 "Codable Contract Refactor" lines 706-867 — full recipe + acceptance criteria + Gotchas table line 853 "Breaking change to a shipped contract")
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (table row for `ChainOfTrust.swift (modified)` — pinned mutation sites at line 26)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-12 — breaking change; Phase 8 D-02 precedent — list envelope mid-milestone refactor)
  </read_first>
  <action>
    A. MODIFY `validationLedger/Core/Load/ChainOfTrust.swift`:

    1. At line 117 region: REMOVE `public let priorRelationshipCount: Int`. ADD `public let priorRelationships: [PriorRelationship]` in the same position (preserves field ordering in the public surface).

    2. In the `private enum CodingKeys: String, CodingKey` block (lines 122-132): REMOVE the `case priorRelationshipCount` line. Do NOT add an explicit case for `priorRelationships` — `.convertFromSnakeCase` handles `prior_relationships` → `priorRelationships` (no trailing acronym; RESEARCH §7 line 805 confirms).

    3. Update the file-header doc-block (lines 41-44 region) to reflect the contract evolution: cite "Phase 9 D-12 — replaces priorRelationshipCount: Int with priorRelationships: [PriorRelationship] in lockstep with the D-14 fixture re-authoring." Reference `validationLedger/Core/Load/PriorRelationship.swift` for the nested type.

    4. Update the trailing-acronym discipline doc-block (lines 32-49 region) ONLY if the existing comment list `priorRelationshipCount` by name — adjust to reflect the rename. Do not introduce other doc-block edits.

    Per RESEARCH §7 Gotcha line 853: this is a breaking change but verified safe — `grep -RnE 'priorRelationshipCount' validationLedger/` after the mutation MUST return zero matches (negative gate). Phase 8's `LoadListItem` does NOT reach into TrustNode's `priorRelationshipCount`; verified via grep before edit.

    B. MODIFY `validationLedgerTests/Load/ChainOfTrustDecodeTests.swift`:

    - For every test asserting `priorRelationshipCount == N` (the `@Test` annotations in the file already cover the per-fixture decode matrix): rewrite the assertion as `priorRelationships.count == N` (same numeric expectation — the new array's `.count` equals the old Int). Where the existing test had `priorRelationshipCount == 0` (chameleon-archetype carrier in VL-1010), assert `priorRelationships.isEmpty == true` for clarity.
    - Add NO new test methods here — Tests 3 in this plan creates the dedicated `PriorRelationshipDecodeTests.swift` suite for the new wire-bridge assertions; ChainOfTrustDecodeTests is a regression update only.
    - Do NOT touch other assertions in the file (verdict, flagged-node count, edge cases — all UNCHANGED).

    Per T-09-01 (server-supplied data) + T-09-03 (no client trust derivation): the mutation is a contract-level rename; iOS still reads server data verbatim. Per T-09-02 (PII): no logging.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/ChainOfTrustDecodeTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public let priorRelationships: \[PriorRelationship\]' validationLedger/Core/Load/ChainOfTrust.swift` returns `1`
    - Source: `grep -c 'public let priorRelationshipCount' validationLedger/Core/Load/ChainOfTrust.swift` returns `0` (negative gate — Phase 9 D-12 removes this)
    - Source: `grep -RnE 'priorRelationshipCount' validationLedger/ validationLedgerTests/` returns zero matches (the global grep gate — RESEARCH §7 line 865)
    - Source: `grep -c 'case priorRelationshipCount' validationLedger/Core/Load/ChainOfTrust.swift` returns `0`
    - Source: `grep -c 'case priorRelationships' validationLedger/Core/Load/ChainOfTrust.swift` returns `0` (no explicit case needed — `.convertFromSnakeCase` handles it)
    - Behavior: ChainOfTrustDecodeTests passes against the (about-to-be-re-authored) fixtures (this task's verify is gated on Task 3 completing; verification runs after Task 3 commit OR Task 2 + Task 3 are committed as a single atomic transaction)
  </acceptance_criteria>
  <done>
    TrustNode contract evolution committed; ChainOfTrustDecodeTests rewritten for the new shape; global grep for `priorRelationshipCount` returns zero matches; build green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-author all 12 load-detail-VL-*.json fixtures (D-14) — replace prior_relationship_count with fully-populated prior_relationships arrays per fraud-archetype + ship PriorRelationshipDecodeTests</name>
  <files>
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1002.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1003.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1004.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1005.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1006.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1007.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1008.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1009.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1010.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1011.json,
    validationLedgerTests/Networking/Fixtures/load-detail-VL-1012.json,
    validationLedgerTests/Load/PriorRelationshipDecodeTests.swift
  </files>
  <behavior>
    - Every `load-detail-VL-*.json` fixture has `"prior_relationship_count": N` (where it appears) REMOVED and `"prior_relationships": [...]` ADDED on every `TrustNode` inside `chain_of_trust.nodes[]`.
    - Per D-14 + RESEARCH §7 Fixture-as-product-surface §840-846:
      * Clean carriers (the "good guy" archetype in non-fraud fixtures): 5+ prior_relationships entries with the same broker — recognizable trust history.
      * Chameleon-carrier archetype (Phase 7 D-13(b) — VL-1010 per existing fixture-content convention; planner confirms the chameleon ID from the fixture's `chain_integrity.reason` text): the flagged carrier's `prior_relationships` is an EMPTY ARRAY `[]` — surfaces in the TRUST-03 sheet as "First time working together." (UI-SPEC § Prior relationships empty-state line 442); the empty array IS the marquee fraud signal.
      * Double-brokered archetype (Phase 7 D-13(a) — VL-1009 per existing fixture-content): the flagged intermediary broker's `prior_relationships` is short or empty (1-2 entries or `[]`); the flagged carrier likewise carries `[]` or 1-2 entries (signals "this counterparty has no real history with us").
      * Factoring-fraud archetype (Phase 7 D-13(c) — VL-1011 per existing fixture-content): the factoring node's `prior_relationships` carries a patterned history (e.g. same broker, same carrier — collusion signature; planner curates).
      * All other non-fraud clean fixtures (VL-1001 through VL-1008, VL-1012): every TrustNode carries 5+ entries with realistic counterparties.
    - Each `prior_relationships` entry is a JSON object with snake_case wire keys: `load_id`, `occurred_at`, `counterparty_role`, `counterparty_display_name`.
    - `load_id` values reference plausible VL-#### identifiers; reuse other fixture IDs where realistic (broker's prior load with carrier may reference an actual fixture VL-####, building cross-fixture coherence — fixture-as-product-surface).
    - `occurred_at` uses ISO-8601 timestamps that produce realistic relative-time renderings ("3 months ago", "Last week" — i.e., dates between 2026-01-01 and 2026-05-01).
    - `counterparty_role` is the OTHER role on the prior load relative to this node (e.g. on a Broker node, prior relationships' counterparty_role is typically "carrier" or "shipper"; the framing in the UI sheet is "Broker → Carrier" rendered from this field).
    - `counterparty_display_name` reuses the synthetic party names already in the fixture corpus (Acme Brokerage, FreightWise, Reliable Carriers Inc., etc. — never real customer names per T-09-02).
    - `PriorRelationshipDecodeTests.swift` exists and contains 5+ `@Test` methods covering: clean baseline decode (VL-1001 — 5+ priors on the carrier), chameleon empty-array decode (VL-1010 — carrier `.priorRelationships.isEmpty == true`), explicit wire-bridge proof (synthesized JSON with `load_id` → `loadID` matches the `loadID = "loadId"` CodingKey), Date round-trip via `.iso8601`, `counterpartyDisplayName` optional decodes as nil when omitted.
  </behavior>
  <read_first>
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json (lines 95-150 — existing TrustNode shape with legacy `prior_relationship_count`; structural reference)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1009.json (existing double-broker archetype `chain_integrity.reason` and `verdict`; identifies WHICH fixture is which fraud archetype)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1010.json (chameleon-carrier archetype)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1011.json (factoring-fraud archetype)
    - validationLedgerTests/Load/ChainOfTrustDecodeTests.swift (the existing per-fixture `@Test` shape — PATTERNS E10 lines 393-413; the test-coverage matrix Phase 9 PriorRelationshipDecodeTests mirrors)
    - validationLedger/Core/Load/PriorRelationship.swift (Task 1 output — the wire bridge the new tests assert)
    - validationLedger/Core/Networking/APIClient.swift (`defaultDecoder()` configuration — `.convertFromSnakeCase` + `.iso8601`; the new tests use this verbatim)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-RESEARCH.md (§7 lines 807-846 — wire fixture shape recipe + Fixture-as-product-surface)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-PATTERNS.md (E10 — `ChainOfTrustDecodeTests.swift` template lines 393-413; per-fixture `@Test` shape)
    - .planning/phases/09-load-detail-chain-of-trust-graph/09-CONTEXT.md (D-14 — fixture authoring + curated history; § Specifics line 208-209)
    - .planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md (D-11 shared-world; D-13 fraud archetypes — pins which fixture IDs are which archetype)
  </read_first>
  <action>
    A. For each of the 12 `load-detail-VL-*.json` fixture files in `validationLedgerTests/Networking/Fixtures/`:

    1. Open the file. Locate `chain_of_trust.nodes[]`. For each `TrustNode` entry:
       - REMOVE the line `"prior_relationship_count": N` (any value).
       - ADD a sibling `"prior_relationships": [...]` array immediately AFTER `"usdot_authority_status"` (preserves field ordering parallel to the Swift struct).

    2. Populate `prior_relationships` according to the archetype mapping per `<behavior>`:
       - Clean fixtures (VL-1001 through VL-1008, VL-1012): every node carries 5+ entries.
       - VL-1009 (double-broker): the flagged broker/carrier nodes carry 0-2 entries; non-implicated nodes carry 5+.
       - VL-1010 (chameleon carrier): the flagged carrier carries `[]` (empty array); other nodes carry 5+.
       - VL-1011 (factoring fraud): the factoring node carries a patterned history (e.g. 3-5 entries all naming the same broker + same carrier — collusion signature); other nodes carry 5+ as normal.

    3. Each `prior_relationships` element shape (snake_case wire keys):
       ```
       {
         "load_id": "VL-####",
         "occurred_at": "2026-##-##T##:##:##Z",
         "counterparty_role": "broker" | "carrier" | "shipper" | "dispatch" | "factoring",
         "counterparty_display_name": "<synthetic name from existing corpus>"
       }
       ```
       Values are CURATED, not random. Reuse existing display names (Acme Brokerage, FreightWise Brokerage Inc., Reliable Carriers Inc., etc. — from the existing fixture corpus). Per T-09-02 (PII): zero real customer data.

    4. Preserve every other field in every TrustNode unchanged (party_id, role, display_name, verification_state, kyc_completed_at, device_binding_status, usdot_number, usdot_authority_status, and any other field already present).

    Apply edits to all 12 files in one atomic commit. The contract test in `LoadDetailFixtureContractTests` (created in Plan 02) enforces this in CI; this task ships the fixture data the test will gate against.

    B. CREATE `validationLedgerTests/Load/PriorRelationshipDecodeTests.swift`:

    - Use Swift Testing per PATTERNS E10. Header: `import Testing` + `import Foundation` + `@testable import validationLedger`.
    - `@Suite("PriorRelationshipDecodeTests — Phase 9 D-12 / D-13", .serialized)` — `.serialized` matches the Phase 7 `ChainOfTrustDecodeTests` pattern (any test touching the `MockURLProtocol` global registry).
    - Test 1 — `cleanBaselineDecodesFivePlusPriorsOnCarrier`: load `load-detail-VL-1001.json` via `FixtureLoader.loadFixture("load-detail-VL-1001")`; decode through `APIClient.defaultDecoder()` as `LoadDetailEndpoint.Response.self`; locate the carrier node; assert `node.priorRelationships.count >= 5` (D-14 clean carrier rule).
    - Test 2 — `chameleonCarrierDecodesEmptyPriorRelationships`: load `load-detail-VL-1010.json`; locate the carrier node where `verificationState == .flagged`; assert `node.priorRelationships.isEmpty == true` (D-14 chameleon rule + RESEARCH §7 line 846).
    - Test 3 — `loadIDWireBridgeDecodesPostConversionForm`: synthesize a minimal JSON `{"load_id": "VL-1023", "occurred_at": "2026-02-01T09:00:00Z", "counterparty_role": "broker", "counterparty_display_name": "Acme Brokerage"}`. Decode through the same configured `JSONDecoder` (`.convertFromSnakeCase` + `.iso8601`). Assert `result.loadID == "VL-1023"` (proves the `loadID = "loadId"` CodingKey raw value works under `.convertFromSnakeCase` — RESEARCH Pitfall 5 lock).
    - Test 4 — `occurredAtRoundTripsAsISO8601`: same shape as Test 3; assert `result.occurredAt == ISO8601DateFormatter().date(from: "2026-02-01T09:00:00Z")!`.
    - Test 5 — `counterpartyDisplayNameOmittedDecodesAsNil`: synthesize JSON WITHOUT `counterparty_display_name`; assert `result.counterpartyDisplayName == nil` (Optional + synthesized initializer handles missing key cleanly).
    - Test 6 — `doubleBrokeredArchetypeCarrierHasZeroOrFewPriors`: load `load-detail-VL-1009.json`; locate the flagged carrier; assert `node.priorRelationships.count <= 2` (D-14 double-broker rule).

    Per T-09-02 (PII): tests use synthetic data only. Per T-09-01 (decode safety): Test 5 locks the optional decode behavior.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:validationLedgerTests/PriorRelationshipDecodeTests -only-testing:validationLedgerTests/ChainOfTrustDecodeTests -enableCodeCoverage NO</automated>
  </verify>
  <acceptance_criteria>
    - Source (every fixture): for each of `load-detail-VL-1001.json` through `load-detail-VL-1012.json`: `grep -c '"prior_relationship_count"' <file>` returns `0` AND `grep -c '"prior_relationships"' <file>` returns the TrustNode count for that fixture (at least 1)
    - Source (chameleon archetype): in `load-detail-VL-1010.json` the flagged carrier's `"prior_relationships": []` is present (empty array — verified by inspection or a JSON parse step in the contract test built in Plan 02)
    - Source: `grep -c '@Test' validationLedgerTests/Load/PriorRelationshipDecodeTests.swift` returns `>= 6`
    - Source: `grep -c '\.serialized' validationLedgerTests/Load/PriorRelationshipDecodeTests.swift` returns `1`
    - Source: `grep -RnE 'real customer name|john doe|jane smith' validationLedgerTests/Networking/Fixtures/load-detail-VL-*.json` returns `0` (PII gate — only synthetic names)
    - Behavior: `PriorRelationshipDecodeTests` suite passes against the re-authored fixtures (exit 0)
    - Behavior: `ChainOfTrustDecodeTests` (Task 2 updates) passes against the re-authored fixtures (exit 0 — proves no regression)
  </acceptance_criteria>
  <done>
    All 12 fixtures re-authored with curated prior-relationship history per fraud archetype; `PriorRelationshipDecodeTests` covers ≥6 assertions including the wire-bridge proof, the chameleon empty-array rule, and the Optional decode behavior; both suites pass green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture JSON wire format → iOS `Decodable` types | Malformed or hostile wire data crosses into `PriorRelationship` decode; the `.convertFromSnakeCase` strategy + Phase 7 fail-closed enum decoders (`VerificationState`, `Role`) are the input-validation layer (ASVS V5). |
| `priorRelationshipCount` legacy consumer | A stale consumer reading the removed Int would still type-check briefly during the refactor; the global grep gate forces the rename to be atomic. |
| Snapshot/fixture content → CI artefacts | Fixtures and any snapshot images surface in test bundles; the synthetic-name discipline (T-09-02) keeps real PII out. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering / Information Disclosure | `PriorRelationship` Decodable surface; `TrustNode.priorRelationships` array | mitigate | NO custom `init(from:)` (synthesized + `.convertFromSnakeCase` only). The trailing-acronym `loadID = "loadId"` raw value is the post-conversion form (Pitfall 5). Phase 7 `Role` enum already fail-closed-decodes unknown wire values. Locked by Task 3 PriorRelationshipDecodeTests Tests 3, 4, 5. |
| T-09-02 | Information Disclosure (PII) | All 12 fixture re-authorings + the new test file | mitigate | Synthetic party names only (Acme Brokerage, FreightWise, Reliable Carriers Inc. — already in the fixture corpus). CLAUDE.md "Zero PII in analytics or crash logs" — verified by grep gate on the fixture corpus (acceptance criterion). |
| T-09-03 | Tampering | Client-side trust derivation via stale `priorRelationshipCount` consumer | mitigate | Phase 7 D-18 LOCK (iOS never derives trust). The Int → Array refactor in this plan does NOT introduce new derivation — `priorRelationships` is server-supplied data; iOS renders only. Atomic Int → Array rename across types + tests + fixtures in one commit; the global grep gate on `priorRelationshipCount` returns 0 matches after the plan lands. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies introduced in this plan; `Package.swift` byte-identical (CLAUDE.md SwiftPM-only + RESEARCH § Package Legitimacy Audit). |
</threat_model>

<verification>
1. Build green: `xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 17' build -parallel-testing-enabled NO` exits 0.
2. PriorRelationshipDecodeTests green: scoped `-only-testing:validationLedgerTests/PriorRelationshipDecodeTests` passes (exit 0) with ≥6 `@Test` methods.
3. ChainOfTrustDecodeTests regression smoke: scoped `-only-testing:validationLedgerTests/ChainOfTrustDecodeTests` still passes against the re-authored fixtures (exit 0).
4. Global grep gate: `grep -RnE 'priorRelationshipCount' validationLedger/ validationLedgerTests/` returns zero matches.
5. Fixture contract: every `load-detail-VL-*.json` has `prior_relationships` (no `prior_relationship_count` remains). (The dedicated `LoadDetailFixtureContractTests` lives in Plan 02 and runs against this plan's output.)
</verification>

<success_criteria>
- `PriorRelationship.swift` exists with the locked field set + trailing-acronym `loadID = "loadId"` CodingKey raw value.
- `ChainOfTrust.swift` `TrustNode` carries `priorRelationships: [PriorRelationship]` (no `priorRelationshipCount` anywhere).
- All 12 detail fixtures re-authored per the fraud-archetype rules.
- `PriorRelationshipDecodeTests` ships ≥6 assertions covering the contract evolution.
- `ChainOfTrustDecodeTests` updated for the new array shape; still passes.
- Zero new SwiftPM dependencies.
- The global grep for `priorRelationshipCount` returns zero matches.
</success_criteria>

<output>
Create `.planning/phases/09-load-detail-chain-of-trust-graph/09-01-SUMMARY.md` when done. Record:
- `PriorRelationship` signature + the trailing-acronym CodingKeys raw value choice (`"loadId"` post-conversion).
- The TrustNode mutation diff (`priorRelationshipCount: Int` → `priorRelationships: [PriorRelationship]`).
- Per-fixture archetype mapping (which IDs are chameleon, which double-broker, which factoring-fraud; how many priors each fraud-archetype's implicated node carries).
- Test methods landed in `PriorRelationshipDecodeTests` + the `ChainOfTrustDecodeTests` regression-update count.
- Any open question (e.g. whether `counterpartyDisplayName` is consistently populated across all fixtures, or which fixtures share VL-#### IDs in prior-relationship arrays for cross-fixture coherence).
</output>
