---
context: phase
phase: 05-kyc-capture-upload-pipeline
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-17T17:54:08Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: gsd-sdk crashes without a Node polyfill — every `gsd-sdk` call fails with `structuredClone is not defined` (system Node is v16; gsd-sdk needs v17+). Structural mitigation: in EVERY Bash command that runs `gsd-sdk`, first `export NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js"`. If that file is missing (it lives in /tmp, lost on reboot), recreate it first with the polyfill (see Critical Anti-Patterns row + auto-memory `gsd-sdk-node16-workaround`).

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| gsd-sdk node16 crash | Every `gsd-sdk query …` aborts with `structuredClone is not defined` — system Node v16 lacks it. Broke the very first init call this session. | blocking | Prefix every gsd-sdk Bash call with `export NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js"`. If `/tmp/structured-clone-polyfill.js` is missing, recreate it: `if (typeof globalThis.structuredClone !== 'function') { const v8 = require('v8'); globalThis.structuredClone = (v) => v8.deserialize(v8.serialize(v)); }` |
| Simulator hides camera bugs | The iOS Simulator cannot run a camera at all, so the KYC capture flow could not be validated end-to-end pre-device. ~12 device-only bugs surfaced during this checkpoint. | advisory | Any camera/capture/DataScanner work in this phase MUST be verified on a physical iPhone — simulator green ≠ working. |

<current_state>
Mid `/gsd-execute-phase 5`. Phase 05 (KYC Capture & Upload Pipeline), 8 plans.
6 plans complete with SUMMARYs: 05-01, 05-02, 05-03, 05-04, 05-05, 05-07.
Plan **05-06 is parked at its Task 3 human-verify checkpoint** (Tasks 1-2 committed: e95b271, e627fb7 — no SUMMARY yet).
Plan **05-08 not started** (Wave 4).

During the 05-06 checkpoint, on-device testing surfaced ~12 device-only KYC capture bugs — all fixed and committed to main as `fix(05):` commits:
`41026cb` NSCameraUsageDescription · `b2983a9` permission-gated camera start · `873becd`/`93386af`/`13a1b7c` preview-layer hosting (→ layerClass `CameraPreviewView`) · `6db896f` UILaunchScreen (fixed app-wide 320×480) · `9e686eb` video-frame pipeline → face auto-fire · `d0ae874` show captured photo in preview · `aee1358` DL-front capture+persist (was dead-ending Submit) · `273a2f1` retake re-uploads only the retaken artifact · `33dbdb0` Review thumbnails render · `055469e` preview-collapse + DL-rescan guards.

On device, confirmed working: camera opens, face auto-fire, capture, Use/Retake preview shows the photo.
NOT yet confirmed: full end-to-end flow, and **artifact persistence** (user's open concern).
</current_state>

<completed_work>

Plan-level (this phase):
- 05-01..05-05, 05-07 — complete (SUMMARYs written, STATE/ROADMAP updated).
- 05-06 Task 1 — KYCReviewViewController + ViewModel — Done (e95b271).
- 05-06 Task 2 — KYCStatusViewController + ViewModel — Done (e627fb7).
- 05-06 Task 3 — human-verify checkpoint — In progress: device testing underway, 12 capture-flow fixes applied, partial verification done.
</completed_work>

<remaining_work>

- 05-06 Task 3: pass the full KYC flow device test (guide: `.planning/debug/kyc-flow-device-audit.md`), then spawn a fresh gsd-executor continuation agent to write `05-06-SUMMARY.md` + update STATE.md/ROADMAP.md and close the plan.
- Plan 05-08 (Wave 4): integration + role-shell entry point + 05-VALIDATION.md + the SC-2 device test + HUMAN-UAT checkpoint. `autonomous: false` — has its own checkpoint.
- Phase 5 verification (gsd-verifier) → phase completion → code-review gate.
</remaining_work>

<decisions_made>

- Phase 5 executed sequentially on `main`, no parallel worktrees — branching is "none", checkpoint-heavy phase, cross-wave deps. Keep this mode on resume.
- The 12 checkpoint fixes were committed as `fix(05):` on main with no SUMMARY/STATE edits — they are checkpoint gap fixes, tracked via debug session files.
- OPEN (undecided): CameraPermissionViewController — wire the blocking screen (per plan 05-05 Task 4) vs. keep current inline copy. Awaiting user.
</decisions_made>

<blockers>
- **Artifact persistence unconfirmed** — user reports capture works on device but "I dont see that it is getting preserved". Investigate whether each captured artifact actually writes to `KYCSessionStore` and survives. This is the #1 thing to verify/debug before approving the 05-06 checkpoint.
- 05-06 Task 3 human-verify checkpoint is open — needs a full end-to-end device pass.
- CameraPermissionViewController not wired — product decision pending.
- Live KYCUploader.onProgress → Review badge updates not wired — non-blocking polish gap.
</blockers>

## Required Reading (in order)
1. `.planning/debug/kyc-flow-device-audit.md` — the proactive batch audit; contains the authoritative one-pass end-to-end device-test guide and the "needs device verification" list.
2. `.planning/debug/front-camera-preview-black.md` — the 5-round black-preview debug saga (RESOLVED); explains the camera architecture (layerClass `CameraPreviewView`, permission-gated session, video-frame pipeline).
3. `.planning/phases/05-kyc-capture-upload-pipeline/05-06-PLAN.md` — plan 05-06 intent (Review + Status screens, Task 3 checkpoint).
4. `.planning/phases/05-kyc-capture-upload-pipeline/05-05-PLAN.md` — capture-flow intent (incl. the blocking permission-screen expectation).
5. `.planning/HANDOFF.json` — machine-readable state mirror of this file.

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: trusting simulator builds for camera work → always verify capture/DataScanner on a physical device.
- ANTI-PATTERN: guess-fixing without device evidence → rounds 1-2 of the black-preview debug guessed wrong; the fix only landed once on-device `kyc_camera` logs gave hard evidence. For any remaining capture bug, instrument + get device logs before fixing.

## Infrastructure State
- Branch: `main`. Working tree clean except `.planning/config.json` (pre-existing unrelated modification — leave untouched; never stage it).
- All capture-flow fixes committed. HEAD ≈ `57df163`.
- Debug sessions open: `front-camera-preview-black.md` (resolved — archive to `.planning/debug/resolved/` once device-confirmed) and `kyc-flow-device-audit.md`.
- Test note: ~40 pre-existing `MockURLProtocol` parallel-run networking failures are known/unrelated — run KYC suites with `-parallel-testing-enabled NO`. Simulator destination: `iPhone 16e` (`iPhone 16` not installed).

<context>
The user paused after a long device-debugging session to start fresh. They want an "implementation/test/debug session": re-test the KYC flow on device after clearing context, with focus on confirming artifact persistence. The camera flow itself is in good shape; the open questions are (1) does capture actually persist, (2) does the full flow run end-to-end (DL scan → extraction → DL back → vehicle ×3 → review → submit → status), (3) the permission-screen decision. Once the flow is verified, 05-06 closes and execution continues to 05-08.
</context>

<next_action>
Start with: re-test the full KYC flow on a physical iPhone following the device-test guide in `.planning/debug/kyc-flow-device-audit.md`. Filter the device console on `kyc_camera`. Pay specific attention to artifact persistence — confirm the `dlfront_photo_persisted` line and that the Review screen renders all 6 real thumbnails (proof artifacts persisted to KYCSessionStore). If persistence is broken, open a debug session on it. Once the flow passes end-to-end and the user approves, spawn a fresh gsd-executor continuation agent to write `05-06-SUMMARY.md` + update STATE.md/ROADMAP.md, then continue `/gsd-execute-phase 5` into Wave 4 (plan 05-08). Also resolve the open CameraPermissionViewController decision with the user.
</next_action>
