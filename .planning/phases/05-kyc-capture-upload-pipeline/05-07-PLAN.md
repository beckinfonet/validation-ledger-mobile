---
phase: 05-kyc-capture-upload-pipeline
plan: 07
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-04, 05-05]
files_modified:
  - validationLedger/App/AppDelegate.swift
  - validationLedger/App/SceneDelegate.swift
  - validationLedger/App/AppCoordinator.swift
  - validationLedger/App/AppContainer.swift
  - validationLedger/Core/Storage/Keychain/KeychainKey.swift
  - validationLedger/Core/Storage/Keychain/KeychainScope.swift
  - validationLedger/Core/Auth/SessionRestoreService.swift
  - validationLedger/Core/Auth/SessionRestoreProbe.swift
  - validationLedger/Features/Onboarding/Auth/OTPViewModel.swift
  - validationLedger/Core/Identity/KYC/KYCUploadScheduler.swift
  - validationLedgerTests/KYC/BackgroundUploadSchedulingTests.swift
  - validationLedgerTests/Auth/SessionRestoreServiceTests.swift
autonomous: true
requirements: [KYC-01, UPL-05]
must_haves:
  truths:
    - "D-12: after OTP-verify, a user whose KYC is not yet verified is routed into KYCCoordinator via a new .kyc AppPhase; the role shell is unreachable until KYC is submitted"
    - "D-13: cold boot reads the cached kycStatus from Keychain (via OTPVerifyEndpoint.Response.kycStatus) and routes to .kyc or .role optimistically, then GET /kyc/status refreshes once routed"
    - "KYCCoordinator is retained by AppCoordinator for its full lifetime — its callbacks never orphan"
    - "When the app backgrounds with uploads pending, a BGProcessingTaskRequest is submitted; its handler captures the scene AppContainer's kycUploader (never a freshly-built AppContainer) and resumes the chunk loop"
    - "D-14: the KYC sign-out affordance runs LogoutService and returns to phone-entry, leaving the on-disk KYC session intact"
  artifacts:
    - path: "validationLedger/Core/Identity/KYC/KYCUploadScheduler.swift"
      provides: "BGProcessingTaskRequest scheduling + handler logic — UPL-05"
      contains: "BGProcessingTaskRequest"
    - path: "validationLedger/App/AppCoordinator.swift"
      provides: ".kyc AppPhase routing + KYCCoordinator retention"
      contains: "kycCoordinator"
  key_links:
    - from: "validationLedger/App/SceneDelegate.swift"
      to: "AppPhase.kyc"
      via: "cold-boot probe routes to .kyc when kycStatus != verified"
      pattern: "case .kyc"
    - from: "validationLedger/App/AppDelegate.swift"
      to: "BGTaskScheduler.register"
      via: "upload-continuation task registered at launch; handler captures the scene AppContainer's kycUploader"
      pattern: "BGTaskScheduler"
---

<objective>
The app-wiring plan: the `.kyc` `AppPhase` hard gate (D-12), cold-boot routing on the cached `kycStatus` (D-13), `KYCCoordinator` retention, the `BGProcessingTaskRequest` upload-continuation wiring (UPL-05), and the KYC sign-out path (D-14).

Purpose: this connects the Phase 5 subsystem into the app. D-12 — KYC is a hard gate: after OTP-verify a non-verified user routes into `KYCCoordinator`, and the role shell is unreachable until KYC is submitted. D-13 — `kycStatus` is cached in Keychain (extending the Phase 3 `session.*` pattern) and read optimistically on cold boot. UPL-05 — when the app backgrounds mid-upload, a `BGProcessingTaskRequest` grants runtime to keep the foreground chunk loop running (the RATIFIED USER DECISION's model — foreground loop + BGTask, not a file-based background `URLSession`).

Output: extended `AppDelegate` / `SceneDelegate` / `AppCoordinator` / `AppContainer`, `KeychainKey.kycStatus`, an extended `SessionRestoreService`/`SessionRestoreProbe`, and a new `KYCUploadScheduler`. Mirrors the existing `.auth` phase wiring exactly (05-PATTERNS.md). Runs in Wave 3 parallel with plan 06 — plan 06 owns `Features/Onboarding/KYC/`, this plan owns `App/` + `Core/`; zero file overlap. It depends on plan 05 only for the `KYCCoordinator` type + its `onKYCSubmitted`/`onSignOut` callback surface — not on plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-CONTEXT.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-RESEARCH.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-PATTERNS.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-04-SUMMARY.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-05-SUMMARY.md

<interfaces>
<!-- Repo contracts to extend. -->

AppPhase (validationLedger/App/SceneDelegate.swift):
  public enum AppPhase { case launch; case auth; case role(Role); case anotherActiveSession }
  — add `case kyc`.

AppCoordinator (validationLedger/App/AppCoordinator.swift):
  init switch over AppPhase builds rootViewController; `.auth` constructs AuthCoordinator,
  stores it in `private var authCoordinator: AuthCoordinator?` (the retention pattern —
  lines 21-28 document why), surfaces coord.rootViewController, then wires
  auth.onAuthenticated → onRoleResolved. phaseDescription(_:) needs a `.kyc` case.

KYCCoordinator (validationLedger/Features/Onboarding/KYC/KYCCoordinator.swift — plan 05):
  @MainActor final class; init(container:); let rootViewController: UIViewController;
  var onKYCSubmitted: (() -> Void)?; var onSignOut: (() -> Void)?.

SceneDelegate cold-boot: SessionRestoreProbe.probe(env:) yields .restored(role) | .needsAuth;
the `scene(_:willConnectTo:)` switch routes accordingly. presentRoot(_:checkLockState:)
handles any phase. sceneDidEnterBackground is NOT yet implemented — add it for BGTask
scheduling. sceneDidDisconnect/deinit clean up observers. The SceneDelegate already holds
a reference to the scene's AppContainer (it builds AppCoordinator from it).

SessionRestoreService (validationLedger/Core/Auth/SessionRestoreService.swift):
  enum SessionRestoreResult { case restored(role: Role); case needsAuth } — extend.
  DefaultSessionRestoreService.probe() reads sessionToken + sessionRole from Keychain.
SessionRestoreProbe.probe(env:) — the lightweight cold-boot helper (constructs only
KeychainStore + DefaultSessionRestoreService + a NoOpProbeLogger).

KeychainKey (validationLedger/Core/Storage/Keychain/KeychainKey.swift):
  static keys sessionToken, sessionRole, sessionUserID, biometricDomainState, installUUID,
  attestedKeyId, lastHeartbeatAt. — add `kycStatus`.
KeychainScope.session (validationLedger/Core/Storage/Keychain/KeychainScope.swift):
  .contains(_:) lists the 4 session-wiped keys (sessionToken, sessionRole, sessionUserID,
  biometricDomainState).

OTPVerifyEndpoint.Response.kycStatus: String? (plan 01) — the post-OTP cached value source.
OTPViewModel (validationLedger/Features/Onboarding/Auth/OTPViewModel.swift) — D-27 step 2
persists sessionToken/role/userID to Keychain; add the kycStatus persist next to it.
KYCUploader.resumeAllPendingUploads() (plan 04) — the BGTask handler's entry point.
KYCSessionStore (plan 02) — loadSession() tells the scheduler whether uploads are pending.
LogoutService.logout(reason: .userInitiated) — the D-14 sign-out path.

BGTask identifier (Info.plist, plan 01): "com.maldin.validationLedger.kyc-upload".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KeychainKey.kycStatus + the .kyc AppPhase + cold-boot routing</name>
  <files>validationLedger/Core/Storage/Keychain/KeychainKey.swift, validationLedger/Core/Storage/Keychain/KeychainScope.swift, validationLedger/App/SceneDelegate.swift, validationLedger/App/AppCoordinator.swift, validationLedger/Core/Auth/SessionRestoreService.swift, validationLedger/Core/Auth/SessionRestoreProbe.swift, validationLedger/Features/Onboarding/Auth/OTPViewModel.swift, validationLedgerTests/Auth/SessionRestoreServiceTests.swift</files>
  <read_first>
    - validationLedger/App/SceneDelegate.swift (full — the AppPhase enum, the cold-boot `scene(_:willConnectTo:)` switch, `presentRoot(_:checkLockState:)`)
    - validationLedger/App/AppCoordinator.swift (full — the init switch, the `authCoordinator` retention pattern lines 21-28, `phaseDescription`)
    - validationLedger/Core/Auth/SessionRestoreService.swift + SessionRestoreProbe.swift (full — the probe to extend)
    - validationLedger/Core/Storage/Keychain/KeychainKey.swift + KeychainScope.swift (full — where `kycStatus` goes)
    - validationLedger/Features/Onboarding/Auth/OTPViewModel.swift (the D-27 step-2 Keychain-persist site — add the kycStatus persist there)
    - validationLedger/Features/Onboarding/KYC/KYCCoordinator.swift (plan 05 — the coordinator AppCoordinator constructs + retains)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-PATTERNS.md "App/AppDelegate + SceneDelegate + AppCoordinator" section (the exact wiring deltas + the KeychainKey-addition note)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-CONTEXT.md D-12, D-13
    - validationLedgerTests/Auth/ (find an existing SessionRestore test file, or create SessionRestoreServiceTests.swift following APIClientEndpointTests conventions)
  </read_first>
  <action>
    1. `KeychainKey` — D-13: add `public static let kycStatus = KeychainKey(rawValue: "session.kycStatus")` — this is the Keychain-cached KYC state, sourced from `OTPVerifyEndpoint.Response.kycStatus` and refreshed via `GET /kyc/status`. Add it to `KeychainScope.session.contains(_:)`'s member set — the cached *status string* is session metadata and IS wiped on logout (consistent with `sessionRole`). CRITICAL distinction (RESEARCH Runtime State Inventory / 05-PATTERNS.md): the Keychain `kycStatus` cache is a DIFFERENT store from the on-disk `KYCSessionStore` blob — only the cache is session-scoped; the on-disk session must NOT be wiped on logout (D-02, enforced by plan 02 — the store is never wired into `LogoutService`). Update the `KeychainScope.session` doc comment to list `kycStatus`.

    2. `AppPhase` — D-12: add `case kyc` to the enum in `SceneDelegate.swift` — the hard-gate phase.

    3. `SessionRestoreResult` — D-13: extend `Core/Auth/SessionRestoreService.swift`. The probe must distinguish "restored, KYC verified → role shell" from "restored, KYC not verified → KYC gate". Either add a `case needsKYC(role: Role)` to `SessionRestoreResult`, or add a `kycVerified: Bool` payload to `.restored`. `DefaultSessionRestoreService.probe()` reads the new `KeychainKey.kycStatus` value alongside `sessionToken`/`sessionRole` (optimistic, no round-trip — the Phase 3 D-04 philosophy: a fast offline-tolerant read; `GET /kyc/status` refreshes once routed). When `kycStatus != "verified"`, the result routes to the KYC gate.

    4. `SessionRestoreProbe.probe(env:)` — unchanged in shape (it already constructs `DefaultSessionRestoreService`); it transparently returns the extended result.

    5. `SceneDelegate.scene(_:willConnectTo:)` — D-12/D-13: extend the cold-boot switch: a `needsKYC(role:)` (or `.restored` with `kycVerified == false`) result calls `presentRoot(.kyc)`; `.restored` with verified calls `presentRoot(.role(role), checkLockState: true)` as today; `.needsAuth` unchanged. Add `case .kyc` to any phase switch (`presentRoot`'s phase-tracking is fine — `.kyc` does not need the biometric lock overlay).

    6. `AppCoordinator` — D-12: add a `case .kyc:` branch in the init switch: construct `KYCCoordinator(container:)`, store it in a NEW `private var kycCoordinator: KYCCoordinator?` strong property (mirror `authCoordinator` exactly — RESEARCH Pitfall 6: without retention the coordinator deallocates immediately after `makeRoot` and the callbacks orphan; this was a real Phase 3 bug). Surface `coord.rootViewController`. After init, wire `kyc.onKYCSubmitted` → `self.onRoleResolved?(role)` (the verified "Continue" CTA routes to the role shell — the role comes from the cached/restored session) and `kyc.onSignOut` → forward to the existing logout path (D-14). Add `case .kyc` to `phaseDescription(_:)`.

    7. `OTPViewModel` — D-13: at the D-27 step-2 Keychain-persist site (where `sessionToken`/`sessionRole`/`sessionUserID` are written), also persist the OTP-verify response's `kycStatus` to `KeychainKey.kycStatus` when present. D-12: after OTP-verify, the post-verify routing (the `onAuthenticated` callback / `AppCoordinator`) must route to `.kyc` when `kycStatus != "verified"` rather than straight to `.role` — wire the routing decision so a non-verified user lands in the KYC gate.

    8. Tests — extend (or create) `SessionRestoreServiceTests`: assert that with `kycStatus == "verified"` in Keychain the probe yields the role-shell result, and with `kycStatus` absent or != "verified" it yields the KYC-gate result.
  </action>
  <verify>
    <automated>cd /Users/ustatb01/development/mobileApps/validation-ledger-mobile && xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/SessionRestoreServiceTests -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `SessionRestoreServiceTests` passes (GREEN) — verified-status → role result, non-verified/absent → KYC-gate result.
    - `grep -c "kycStatus" validationLedger/Core/Storage/Keychain/KeychainKey.swift` returns 1; `grep -c "kycStatus" validationLedger/Core/Storage/Keychain/KeychainScope.swift` returns ≥ 1 (in `.contains` and the doc comment).
    - `grep -c "case kyc" validationLedger/App/SceneDelegate.swift` returns ≥ 1 — the AppPhase case.
    - `grep -c "kycCoordinator" validationLedger/App/AppCoordinator.swift` returns ≥ 2 — the strong property + the `.kyc` branch.
    - `grep -c "case .kyc" validationLedger/App/AppCoordinator.swift` returns ≥ 2 — the init switch + `phaseDescription`.
    - `grep -ci "kycStatus" validationLedger/Features/Onboarding/Auth/OTPViewModel.swift` returns ≥ 1 — the post-verify persist.
    - The simulator scheme builds clean.
  </acceptance_criteria>
  <done>The `.kyc` hard gate is wired: cold boot routes on the cached kycStatus, OTP-verify routes a non-verified user into KYCCoordinator, and AppCoordinator retains the coordinator.</done>
</task>

<task type="auto">
  <name>Task 2: KYCUploadScheduler + BGProcessingTaskRequest wiring</name>
  <files>validationLedger/Core/Identity/KYC/KYCUploadScheduler.swift, validationLedger/App/AppDelegate.swift, validationLedger/App/SceneDelegate.swift, validationLedger/App/AppContainer.swift, validationLedgerTests/KYC/BackgroundUploadSchedulingTests.swift</files>
  <read_first>
    - validationLedger/App/AppDelegate.swift (full — `didFinishLaunchingWithOptions`; the BGTaskScheduler.register goes alongside the existing `KeychainWiper.wipeOnFirstLaunch` call)
    - validationLedger/App/SceneDelegate.swift (the lifecycle methods — add `sceneDidEnterBackground`; note the scene already owns its AppContainer)
    - validationLedger/App/AppContainer.swift (the composition root — `kycUploader` + `kycSessionStore` get added as `let` properties; see how `apiClient` etc. are constructed in dependency order)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-RESEARCH.md "Pattern 6" (BGProcessingTaskRequest registration + scheduling) + "Pitfall 2" (the RATIFIED option-1 model — foreground loop + BGTask, NOT a file-based background URLSession)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-PATTERNS.md "AppDelegate" section
    - validationLedger/Core/Identity/KYCUploader.swift (plan 04 — `resumeAllPendingUploads()` is the handler's entry point)
    - validationLedger/Core/Storage/KYCSessionStore.swift (plan 02 — `loadSession()` tells the scheduler whether uploads are pending)
    - validationLedgerTests/KYC/BackgroundUploadSchedulingTests.swift (the RED scaffold from plan 01)
  </read_first>
  <action>
    Implement the UPL-05 background-continuation wiring per the RATIFIED USER DECISION: foreground `URLSession` chunk loop (already built in plan 04 — `KYCUploader` runs through `APIClient`) PLUS a `BGProcessingTaskRequest` that grants runtime when the app backgrounds mid-upload. Do NOT build a file-based background `URLSession` — that is the explicit M2 follow-up.

    1. `KYCUploadScheduler` — a new type in `Core/Identity/KYC/`. Holds the BGTask identifier constant `"com.maldin.validationLedger.kyc-upload"` (must match the Info.plist `BGTaskSchedulerPermittedIdentifiers` entry from plan 01). Expose:
       - `registerHandler(uploaderProvider:)` — calls `BGTaskScheduler.shared.register(forTaskWithIdentifier:using:nil)`; `uploaderProvider` is an injected `@Sendable () -> KYCUploader?` closure that returns the *already-constructed* scene `AppContainer.kycUploader` (see step 3 — the SceneDelegate stores the container's uploader into the scheduler during `sceneDidEnterBackground`). The handler closure resolves the uploader via that provider, sets `task.expirationHandler` (the `KYCUploader` checkpoints chunk progress to the store on expiry — progress is already persisted after every ack in plan 04, so expiry loses at most the in-flight chunk), runs `await uploader.resumeAllPendingUploads()`, then `task.setTaskCompleted(success:)`. If the provider returns nil (no scene container yet — extremely rare, app was never foregrounded), the handler completes with `success: false` and does NOT construct anything. The handler MUST NOT call `AppContainer()` — constructing a second container would create a second `KYCSessionStore` instance pointed at the same on-disk store, risking a torn read/write.
       - `scheduleUploadContinuation(hasPendingUploads: Bool)` — when `hasPendingUploads` is true, build a `BGProcessingTaskRequest(identifier:)` with `requiresNetworkConnectivity = true`, `requiresExternalPower = false`, and `BGTaskScheduler.shared.submit(request)`.
       - To make the scheduling LOGIC simulator-testable (`BGTaskScheduler` itself cannot run in CI), put the decision logic behind a small injectable seam — e.g. a `BGTaskScheduling` protocol with a `submit`/`register` surface that `BGTaskScheduler` satisfies in production and a fake satisfies in tests. `BackgroundUploadSchedulingTests` drives the fake and asserts: a request is submitted when uploads are pending, and NOT submitted when none are pending.
    2. `AppDelegate.didFinishLaunchingWithOptions` — call `KYCUploadScheduler.registerHandler(...)` (registration MUST happen before launch completes — RESEARCH Pattern 6, or `register` traps). Because `AppContainer` does not exist at the `KeychainWiper` line, pass the scheduler the `uploaderProvider` closure that reads from a shared/weak slot the SceneDelegate fills once its scene's `AppContainer` is built — register the *handler* at launch but defer *uploader resolution* to handler-fire time via the provider. The `AppDelegate` owns the single `KYCUploadScheduler` instance and exposes it so the SceneDelegate can hand it the scene container's `kycUploader`. Document in a comment: the handler captures a reference to the scene `AppContainer`'s `kycUploader`, never constructs a new `AppContainer`.
    3. `SceneDelegate` — (a) when the scene's `AppContainer` is built, store `container.kycUploader` into the `AppDelegate`'s `KYCUploadScheduler` (so the BGTask handler's `uploaderProvider` returns the live uploader); (b) add `sceneDidEnterBackground` — query the scene `AppContainer`'s `KYCSessionStore` for pending uploads (`loadSession()` with any non-committed artifact) and call `scheduler.scheduleUploadContinuation(hasPendingUploads:)`. The pending-upload check and the scheduling both use the scene's existing container — no new container is constructed anywhere.
    4. `AppContainer` — add `let kycSessionStore: KYCSessionStore`, `let kycUploader: KYCUploader`, `let geoContext: GeoContext`, and `let kycUploadScheduler: KYCUploadScheduler` (and the capture services if KYCCoordinator needs them via the container) as `let` properties, constructed in dependency order in `init` alongside the existing services. `KYCCoordinator(container:)` reads `container.kycUploader` etc.
  </action>
  <verify>
    <automated>cd /Users/ustatb01/development/mobileApps/validation-ledger-mobile && xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/BackgroundUploadSchedulingTests -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `BackgroundUploadSchedulingTests` passes (GREEN) — request submitted when uploads pending, not submitted when none pending.
    - `grep -c "BGProcessingTaskRequest" validationLedger/Core/Identity/KYC/KYCUploadScheduler.swift` returns ≥ 1.
    - `grep -c "com.maldin.validationLedger.kyc-upload" validationLedger/Core/Identity/KYC/KYCUploadScheduler.swift` returns ≥ 1 — matches the Info.plist identifier.
    - `grep -c "BGTaskScheduler" validationLedger/App/AppDelegate.swift` returns ≥ 1 — registration at launch.
    - `grep -c "sceneDidEnterBackground" validationLedger/App/SceneDelegate.swift` returns 1.
    - `grep -c "kycUploader\|kycSessionStore" validationLedger/App/AppContainer.swift` returns ≥ 2 — the new container properties.
    - `grep -c "background(withIdentifier" validationLedger/Core/Identity/KYC/KYCUploadScheduler.swift` returns 0 — no file-based background URLSession (RATIFIED decision).
    - `grep -c "AppContainer()" validationLedger/App/AppDelegate.swift` returns 0 — the BGTask handler captures the scene container's `kycUploader`, never constructs a new `AppContainer` (no second `KYCSessionStore` instance).
    - `grep -c "AppContainer()" validationLedger/Core/Identity/KYC/KYCUploadScheduler.swift` returns 0 — the scheduler resolves the uploader via the injected provider, never builds a container.
  </acceptance_criteria>
  <done>The BGProcessingTaskRequest is registered at launch and scheduled on background when uploads are pending; the handler resumes the scene AppContainer's foreground chunk loop via the injected uploader provider (never a new AppContainer); the scheduling logic is simulator-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cold-boot routing decision | Cached `kycStatus` controls whether the role shell is reachable |
| App ↔ iOS BackgroundTasks scheduler | OS grants background runtime for the upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Elevation of Privilege | KYC gate bypassed to reach the role shell without verified KYC | mitigate | The `.kyc` hard gate is enforced at `AppCoordinator.makeRoot` routing — a non-verified `kycStatus` routes to `.kyc`; the role shell is only constructed for `.role`; `GET /kyc/status` re-confirms after routing (D-12 / RESEARCH V4) |
| T-05-07-02 | Tampering | Tampered/absent cached `kycStatus` grants role-shell access | mitigate | The cached value is read OPTIMISTICALLY for routing speed only; `GET /kyc/status` (backend-authoritative) refreshes once routed; an absent/non-"verified" value fails CLOSED to the KYC gate, never open to the role shell |
| T-05-07-03 | Information Disclosure | `kycStatus` Keychain entry surviving logout | mitigate | `KeychainKey.kycStatus` is a member of `KeychainScope.session` — wiped by `deleteAll(under: .session)` on logout, exactly like `sessionRole`; the on-disk `KYCSessionStore` blob is a separate store that deliberately survives (D-02) |
| T-05-07-04 | Repudiation | User signs out and loses in-progress KYC work | accept | D-14: sign-out runs `LogoutService` (wipes the session-scope Keychain incl. `kycStatus`) but the on-disk `KYCSessionStore` is untouched — the partial KYC resumes on next login |
| T-05-07-05 | Spoofing | A foreign app submitting work under the BGTask identifier | accept | The BGTask identifier is bundle-scoped by iOS; no secret involved (carried over from T-05-01-02) |
| T-05-07-06 | Tampering | A second `KYCSessionStore` instance (from a handler-built `AppContainer`) racing the scene store on disk | mitigate | The BGTask handler captures the scene `AppContainer`'s `kycUploader` via an injected provider — it never constructs a new `AppContainer`; `grep` gate asserts zero `AppContainer()` in `AppDelegate.swift` and `KYCUploadScheduler.swift`, so exactly one `KYCSessionStore` instance touches the on-disk store |
| T-05-07-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 installs zero packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `SessionRestoreServiceTests` + `BackgroundUploadSchedulingTests` GREEN.
- `grep` gates: `.kyc` AppPhase case present, `kycCoordinator` retained, `BGProcessingTaskRequest` wired, `kycStatus` in `KeychainScope.session`, zero file-based background URLSession, zero `AppContainer()` in `AppDelegate.swift`/`KYCUploadScheduler.swift`.
- The simulator scheme builds clean with the wired-in KYC subsystem.
</verification>

<success_criteria>
- D-12: KYC is a hard gate — the role shell is unreachable until KYC is verified.
- D-13: cold boot routes on the cached `kycStatus`.
- UPL-05: a `BGProcessingTaskRequest` is scheduled on background mid-upload and its handler resumes the scene AppContainer's chunk loop — never a freshly-built container.
- D-14: KYC sign-out runs `LogoutService` and leaves the on-disk session intact.
- `KYCCoordinator` is retained — its callbacks do not orphan.
</success_criteria>

<output>
Create `.planning/phases/05-kyc-capture-upload-pipeline/05-07-SUMMARY.md` when done.
</output>
