---
phase: 05-kyc-capture-upload-pipeline
plan: 08
type: execute
wave: 4
depends_on: [05-04, 05-06, 05-07]
files_modified:
  - validationLedger/Features/Profile/ProfileViewController.swift
  - validationLedger/Resources/en.lproj/Localizable.strings
  - validationLedgerTests/KYC/KYCEndToEndIntegrationTests.swift
  - validationLedgerTests/KYC/LogoutPreservesKYCSessionTests.swift
  - validationLedgerDeviceTests/KYCForceQuitResumeDeviceTests.swift
  - .planning/phases/05-kyc-capture-upload-pipeline/05-VALIDATION.md
autonomous: false
requirements: [KYC-01, KYC-05, KYC-06, UPL-02, UPL-05]
user_setup: []
must_haves:
  truths:
    - "D-08: the KYC status screen is reachable from a Profile-tab row in the role shell — the same single status screen plan 06 builds, now reached from its second entry point"
    - "A full init→chunk→commit→submit→status flow passes end-to-end against mock fixtures"
    - "Logout leaves the on-disk KYC session intact — a logged-out user resuming on the same account picks up where they left off"
    - "A physical-device force-quit during a 6 MB upload resumes from the last committed chunk, not from zero"
    - "BGProcessingTaskRequest scheduling logic is simulator-tested (BackgroundUploadSchedulingTests, plan 07); end-to-end background-upload completion under real OS suspension is verified by the HUMAN-UAT checkpoint (Task 3)"
  artifacts:
    - path: "validationLedgerTests/KYC/KYCEndToEndIntegrationTests.swift"
      provides: "Full KYC pipeline integration test"
      contains: "@Suite"
    - path: "validationLedgerTests/KYC/LogoutPreservesKYCSessionTests.swift"
      provides: "D-02 logout-does-not-wipe assertion"
      contains: "@Suite"
    - path: "validationLedgerDeviceTests/KYCForceQuitResumeDeviceTests.swift"
      provides: "SC-2 physical-device force-quit-resume test"
      contains: "XCTestCase"
  key_links:
    - from: "validationLedger/Features/Profile/ProfileViewController.swift"
      to: "KYCStatusViewController"
      via: "a Profile row presents the KYC status screen"
      pattern: "KYCStatus"
---

<objective>
The integration + entry-point + verification plan: the role-shell entry to the KYC status screen (D-08), the end-to-end integration test, the D-02 logout-preservation assertion, and the physical-device force-quit-resume test (SC-2) plus the HUMAN-UAT checkpoint for the not-simulator-testable behaviors.

Purpose: Phase 5's hardest success criteria — SC-2 (force-quit mid-upload resumes from the last committed chunk) and SC-4 (background upload finishes) — cannot run in the simulator (RESEARCH Pitfall 1/2). This plan routes them to the Phase-4 physical-device CI lane and a HUMAN-UAT checkpoint, while landing the simulator-testable end-to-end integration. It also wires D-08: the status screen's second entry point — a Profile-tab row in the role shell (so a verified/under-review user can re-check status without re-running KYC). And it finalizes `05-VALIDATION.md` (the Nyquist validation contract — pre-filled before execution; this plan approves it).

Output: the Profile KYC-status row, 2 simulator integration tests, 1 device test, the approved `05-VALIDATION.md`, and a HUMAN-UAT checkpoint. `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-CONTEXT.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-RESEARCH.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-PATTERNS.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-UI-SPEC.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-VALIDATION.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-04-SUMMARY.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-06-SUMMARY.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-07-SUMMARY.md

<interfaces>
<!-- Repo + prior-plan contracts. -->

ProfileViewController (validationLedger/Features/Profile/ProfileViewController.swift) — the
modal Profile screen reached via the Phase 3 D-03 top-bar avatar. It already hosts a
"Log out" row; D-08 adds a KYC-status row consistent with that pattern.

KYCStatusViewController + KYCStatusViewModel (plan 06) — the status screen the Profile row
presents. It is fetch-on-appear so opening it from Profile re-fetches GET /kyc/status (D-09).

LogoutService.logout(reason:) — wipes KeychainScope.session (incl. kycStatus, plan 07);
NEVER touches Core/Storage files (the KYCSessionStore blob — D-02 / RESEARCH Assumption A4).

KYCSessionStore (plan 02) — persist/loadSession/clearSession. KYCUploader (plan 04) —
upload/resumeAllPendingUploads. The 4 KYC-status fixtures + kyc-submit fixtures (plan 01).

validationLedgerDeviceTests/ — the Phase-4 physical-device test target (XCTest, runs on
the self-hosted Mac runner + iPhone 16 via ci-device.yml on merge to main). Plan 01 already
landed `DLExtractionScannerDeviceTests.swift` in this target (KYC-03 smoke test);
this plan adds `KYCForceQuitResumeDeviceTests.swift`.
APIClientEndpointTests.swift — the @Suite(.serialized) + MockURLProtocol conventions.

05-VALIDATION.md — by the time this plan runs the file is ALREADY FILLED (plan 01 / the
planning step pre-populated the Per-Task Verification Map, Wave 0 Requirements, and the
Manual-Only Verifications table from the 8-plan structure). It carries `status: draft` and
`nyquist_compliant: false`. This plan's Task 2 reconciles the map against the actual
execution outcome and flips it to approved.

ARCH-05 — Features never import each other; Profile reaches KYC only through Core/ protocols
or the coordinator. The KYC status row presents KYCStatusViewController constructed from the
AppContainer's dependencies — it does not `import Features.Onboarding` cross-feature.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile KYC-status row + end-to-end integration test + logout-preservation test</name>
  <files>validationLedger/Features/Profile/ProfileViewController.swift, validationLedger/Resources/en.lproj/Localizable.strings, validationLedgerTests/KYC/KYCEndToEndIntegrationTests.swift, validationLedgerTests/KYC/LogoutPreservesKYCSessionTests.swift</files>
  <read_first>
    - validationLedger/Features/Profile/ProfileViewController.swift (full — the existing row layout + the "Log out" row to mirror)
    - validationLedger/Features/Onboarding/KYC/KYCStatusViewController.swift + KYCStatusViewModel.swift (plan 06 — what the Profile row presents)
    - validationLedger/Core/Auth/LogoutService.swift (full — confirm it only wipes KeychainScope.session + the SE auth key; it must NOT touch Core/Storage)
    - validationLedger/Core/Storage/KYCSessionStore.swift (plan 02 — the store the logout test asserts survives)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-CONTEXT.md D-08 (status screen second entry point), D-02 (logout does not wipe), D-14
    - .planning/phases/05-kyc-capture-upload-pipeline/05-UI-SPEC.md (the Profile-row + status-screen copy)
    - validationLedgerTests/Networking/APIClientEndpointTests.swift (the integration-test conventions; the kyc-* fixtures)
  </read_first>
  <action>
    1. `ProfileViewController` — D-08: add a "Verification status" row, styled like the existing "Log out" row, that presents `KYCStatusViewController` (constructed from the `AppContainer` dependencies — do NOT cross-import `Features/Onboarding`; build the VC from `Core/` deps per ARCH-05, or route through a container-provided factory). Tapping it opens the status screen, which fetch-on-appear re-fetches `GET /kyc/status` (D-09). Copy via `NSLocalizedString(_, value:)`. D-08: this is the role-shell re-entry path — the same single status screen plan 06 builds, now reached from its second entry point so a verified or under-review user re-checks status without re-running KYC.

    2. `KYCEndToEndIntegrationTests` — a new simulator integration suite (`@Suite(.serialized)`, `import Testing`, `@testable import validationLedger`). Drive the full pipeline against `MockURLProtocol`: seed a `KYCSession` with 6 small synthetic artifacts in a temp `KYCSessionStore`; register the init/chunk/commit success fixtures; run `KYCUploader.upload(...)` for all 6; register `kyc-submit-success.json` and fire `KYCSubmitEndpoint`; register `kyc-status-under-review.json` and run `KYCStatusViewModel.fetchStatus()`; assert the end-to-end result is the under-review state and that all 6 artifacts are committed. This is the SC-1/SC-3/SC-5 simulator-side end-to-end proof.

    3. `LogoutPreservesKYCSessionTests` — a new simulator suite asserting D-02 / RESEARCH Assumption A4: persist a `KYCSession` to a `KYCSessionStore`; seed `KeychainKey.kycStatus` and the other `session.*` Keychain keys; run `LogoutService.logout(reason: .userInitiated)`; assert (a) `KeychainScope.session` keys including `kycStatus` are wiped, and (b) `KYCSessionStore.loadSession()` STILL returns the persisted session — the on-disk KYC blob survived logout. This is the explicit acceptance criterion called out in the planner notes (A4).
  </action>
  <verify>
    <automated>cd /Users/ustatb01/development/mobileApps/validation-ledger-mobile && xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:validationLedgerTests/KYCEndToEndIntegrationTests -only-testing:validationLedgerTests/LogoutPreservesKYCSessionTests -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `KYCEndToEndIntegrationTests` + `LogoutPreservesKYCSessionTests` pass (GREEN).
    - `LogoutPreservesKYCSessionTests` explicitly asserts `KYCSessionStore.loadSession()` is non-nil after `LogoutService.logout` AND that the `session.*` Keychain keys (incl. `kycStatus`) are wiped — D-02 / A4.
    - `grep -ci "KYCStatus" validationLedger/Features/Profile/ProfileViewController.swift` returns ≥ 1 — the status-screen entry point.
    - `grep -c "import Features" validationLedger/Features/Profile/ProfileViewController.swift` returns 0 — no cross-feature import (ARCH-05).
    - The end-to-end test exercises init→chunk→commit→submit→status against MockURLProtocol fixtures.
  </acceptance_criteria>
  <done>The status screen is reachable from Profile; the end-to-end integration passes; logout provably preserves the on-disk KYC session (D-02).</done>
</task>

<task type="auto">
  <name>Task 2: Physical-device force-quit-resume test + finalize 05-VALIDATION.md</name>
  <files>validationLedgerDeviceTests/KYCForceQuitResumeDeviceTests.swift, .planning/phases/05-kyc-capture-upload-pipeline/05-VALIDATION.md</files>
  <read_first>
    - validationLedgerDeviceTests/ (find an existing device-test file — e.g. AppAttestRoundTripTests, or DLExtractionScannerDeviceTests landed by plan 01 — to copy the XCTest device-test conventions and the launch-arg seam)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-RESEARCH.md "Validation Architecture" (the per-SC test map + the device-lane note) + "Pitfall 4" (the SC-2 force-quit spec)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-VALIDATION.md (the ALREADY-FILLED validation contract — Dimension 8; this task reconciles + approves it, it does not author it from scratch)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-04-SUMMARY.md (the KYCUploader resume contract)
    - validationLedger/Core/Identity/KYCUploader.swift + KYCSessionStore.swift (the resume mechanism the device test exercises)
  </read_first>
  <action>
    1. `KYCForceQuitResumeDeviceTests` — an `XCTestCase` in `validationLedgerDeviceTests/` (the Phase-4 physical-device target, runs on the self-hosted runner via `ci-device.yml` on merge to `main`). SC-2: simulate a force-quit mid-upload by starting a 6 MB artifact upload, terminating the upload partway (e.g. the test injects a kill point after N chunks acked, or uses a launch-arg-driven seam to abort), reconstructing the `KYCUploader` + `KYCSessionStore` fresh, calling `upload(...)` again, and asserting it resumes from the persisted `chunksAcked` cursor — NOT from chunk 0 — and that `chunksAcked/totalChunks` restores correctly. This is the device-lane counterpart to plan 04's `KYCUploaderResumeTests` (which proves the logic; this proves it on real hardware with a real 6 MB payload). Keep all camera-dependent surfaces OUT of this test — it exercises only the upload pipeline + persistence (both simulator-and-device-runnable; the device run validates real-storage / real-process-lifecycle behavior).

    2. `05-VALIDATION.md` — the file is ALREADY FILLED (the planning step pre-populated every section from the 8-plan structure). This task RECONCILES it against the actual execution outcome and APPROVES it. Do NOT rewrite it from a skeleton.
       - Walk the Per-Task Verification Map row by row: confirm each row's automated command and file-exists status matches what the executed plans actually produced; correct any drift (a renamed test, a moved file).
       - Confirm the Wave 0 Requirements checklist items (the 14 KYC test files + the `DLExtractionScannerDeviceTests` device scaffold + the fixtures from plan 01) are all now checked complete.
       - Confirm the Manual-Only Verifications table still accurately routes the live-camera capture (KYC-02), the DataScanner DL-OCR (KYC-03), iPad camera rotation, real-background-suspension upload completion (SC-4), and the physical-device force-quit (SC-2 device portion) to their HUMAN-UAT / device-lane homes.
       - Flip the frontmatter: `nyquist_compliant: false → true`, `wave_0_complete: false → true`, `status: draft → approved`. Fill the Validation Sign-Off "Approval:" line with `approved {today}` and check every sign-off box.
  </action>
  <verify>
    <automated>cd /Users/ustatb01/development/mobileApps/validation-ledger-mobile && xcodebuild build-for-testing -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -6 && grep -c "nyquist_compliant: true" .planning/phases/05-kyc-capture-upload-pipeline/05-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `validationLedgerDeviceTests/KYCForceQuitResumeDeviceTests.swift` exists, is an `XCTestCase`, and compiles (`build-for-testing` succeeds).
    - The device test asserts resume from the persisted `chunksAcked` cursor — `grep -ci "chunksAcked\|resume" validationLedgerDeviceTests/KYCForceQuitResumeDeviceTests.swift` returns ≥ 1.
    - `05-VALIDATION.md` frontmatter has `nyquist_compliant: true`, `wave_0_complete: true`, and `status: approved`.
    - `05-VALIDATION.md` Per-Task Verification Map has a row for every Phase 5 plan-task with an automated command or a manual-only justification, and the row contents match the executed plans (no skeleton placeholders remain).
    - The Manual-Only Verifications table lists the live-camera (KYC-02), DataScanner DL-OCR (KYC-03), iPad-rotation, SC-4 background, and SC-2 device-force-quit items with routing.
  </acceptance_criteria>
  <done>The SC-2 device test compiles for the physical-device lane; `05-VALIDATION.md` is reconciled, approved, and Nyquist-compliant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Phase 5 success criteria that cannot run in the simulator: SC-2 (force-quit mid-upload resumes from the last committed chunk) and SC-4 (background upload finishes via `BGProcessingTaskRequest`), plus the role-shell Profile entry to the KYC status screen.</what-built>
  <how-to-verify>
    On a physical iPhone:
    1. SC-2 force-quit resume: start the KYC capture flow and let artifact uploads begin (pipelined, D-01). While a ~6 MB artifact is mid-upload (watch the determinate progress bar), force-quit the app (swipe up from the app switcher). Relaunch. Confirm the upload resumes from where it left off — the progress bar restores to the prior `chunksAcked/totalChunks`, NOT 0% — and the artifact eventually commits. The device-CI `KYCForceQuitResumeDeviceTests` automates the pipeline portion; this confirms the real end-to-end UX.
    2. SC-4 background upload: with an artifact mid-upload, background the app (Home / swipe up but do not kill). Wait. Confirm the upload continues and completes — re-open the app and confirm the artifact shows ✓ committed. (iOS grants `BGProcessingTaskRequest` runtime on its own schedule; this may take a short while.) This is the end-to-end check for the truth that `BackgroundUploadSchedulingTests` only proves at the scheduling-logic level.
    3. D-08 Profile entry: complete (or fixture-seed) KYC to a verified/under-review state, reach the role shell, open Profile (top-bar avatar), and confirm a "Verification status" row that opens the KYC status screen and re-fetches status.
    4. Confirm the hard gate (D-12): a non-verified account cannot reach the role shell — after OTP-verify it lands in the KYC flow.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (upload restarts from zero after force-quit, background upload stalls, Profile row missing, gate bypassable). Once approved, Phase 5 is complete — run `/gsd:verify-work 5`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logout teardown ↔ on-disk KYC store | Logout must wipe the session cache but NOT the in-progress KYC blob |
| App suspension ↔ in-flight upload | A force-quit / background must not lose chunk progress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Repudiation | A "resumable" upload that silently restarts from zero after a force-quit | mitigate | `chunksAcked` is persisted to the encrypted store after every server ack (plan 02/04); `KYCForceQuitResumeDeviceTests` proves resume-from-cursor on real hardware with a 6 MB payload (RESEARCH Pitfall 4 / SC-2) |
| T-05-08-02 | Information Disclosure | In-progress KYC artifacts readable after logout | mitigate | The on-disk `KYCSessionStore` is `NSFileProtectionComplete`-encrypted (plan 02) and survives logout by design (D-02); `LogoutPreservesKYCSessionTests` asserts logout wipes only the `session.*` Keychain cache, not the encrypted blob |
| T-05-08-03 | Elevation of Privilege | KYC status screen reachable by an unverified user via the Profile row to skip the gate | accept | The Profile row exists only inside the role shell, which is only reachable post-verification (D-12 gate, plan 07); an unverified user is inside `KYCCoordinator` and has no Profile tab — the row cannot be a bypass |
| T-05-08-04 | Tampering | Background upload bypassing certificate pinning | mitigate | The `BGProcessingTaskRequest` handler resumes the SAME foreground `KYCUploader` chunk loop through the cert-pinned `APIClient` (plan 04/07 — RATIFIED option 1); no file-based background session, so no pinning bypass (RESEARCH V9 caveat) |
| T-05-08-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 installs zero packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `KYCEndToEndIntegrationTests` + `LogoutPreservesKYCSessionTests` GREEN (Task 1).
- `KYCForceQuitResumeDeviceTests` compiles for the device lane; `05-VALIDATION.md` reconciled, approved + Nyquist-compliant (Task 2).
- HUMAN-UAT checkpoint (Task 3) — SC-2 force-quit resume, SC-4 background upload, D-08 Profile entry, D-12 gate.
- Full simulator suite green before `/gsd:verify-work`.
</verification>

<success_criteria>
- D-08: the KYC status screen has a role-shell Profile entry point.
- D-02: logout provably preserves the on-disk KYC session (the explicit A4 acceptance criterion).
- SC-2: force-quit-resume verified on the physical-device lane + HUMAN-UAT.
- SC-4: background-upload completion verified by HUMAN-UAT (scheduling logic already simulator-tested in plan 07).
- `05-VALIDATION.md` is reconciled, approved, and Nyquist-compliant.
</success_criteria>

<output>
Create `.planning/phases/05-kyc-capture-upload-pipeline/05-08-SUMMARY.md` when done.
Record any HUMAN-UAT items that could not be completed in this session in `.planning/phases/05-kyc-capture-upload-pipeline/05-HUMAN-UAT.md`.
</output>
