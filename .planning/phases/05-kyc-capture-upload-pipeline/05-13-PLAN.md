---
phase: 05-kyc-capture-upload-pipeline
plan: 13
type: execute
wave: 3
depends_on: ["05-12"]
files_modified:
  - .github/workflows/ci-device.yml
  - validationLedger.xcodeproj/xcshareddata/xcschemes/validationLedger.xcscheme
  - .planning/phases/05-kyc-capture-upload-pipeline/05-HUMAN-UAT.md
  - .planning/phases/05-kyc-capture-upload-pipeline/05-VERIFICATION.md
  - docs/ci.md
autonomous: false
requirements: [SC-2, D-08, D-12, Test-10-lifecycle]
must_haves:
  truths:
    - "The device CI lane runs the validationLedgerUITests XCUITest suite on the device destination"
    - "05-HUMAN-UAT.md records that only SC-4 and the Test-10 runtime-error injection remain human-UAT items"
    - "05-VERIFICATION.md human_verification array lists only SC-4 and the Test-10 runtime-error injection"
  artifacts:
    - path: ".github/workflows/ci-device.yml"
      provides: "device CI step that runs the XCUITest suite on the device destination"
      contains: "validationLedgerUITests"
    - path: ".planning/phases/05-kyc-capture-upload-pipeline/05-HUMAN-UAT.md"
      provides: "updated human-UAT checklist with 2 remaining items"
      contains: "SC-4"
  key_links:
    - from: ".github/workflows/ci-device.yml"
      to: "validationLedgerUITests"
      via: "-only-testing:validationLedgerUITests on the device test action"
      pattern: "only-testing:validationLedgerUITests"
    - from: ".github/workflows/ci-device.yml changes job PATTERN"
      to: "validationLedgerUITests/ path"
      via: "the security-surface path filter includes the UITest directory so the device lane runs when XCUITests change"
      pattern: "validationLedgerUITests"
---

<objective>
Wire the four new XCUITests from plan 05-12 into the self-hosted device CI lane, confirm the
`validationLedgerUITests` target is included in the shared scheme's test action, and update the
phase tracking docs so the manual-UAT burden is recorded as dropping from 5 items to 2.

Purpose: The XCUITests only become a regression gate once `ci-device.yml` actually runs them on
the device destination on every merge to `main`. After this plan, SC-2 / D-08 / D-12 / Test-10
background-foreground are automated and only SC-4 (BGProcessingTaskRequest completion) and the
Test-10 deliberate `AVCaptureSessionRuntimeError` injection remain as permanent human-UAT items.

Output: An updated `ci-device.yml` device test action, a verified scheme test action, an updated
`05-HUMAN-UAT.md` and `05-VERIFICATION.md`, and a `docs/ci.md` note describing the device
XCUITest lane.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-VERIFICATION.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-HUMAN-UAT.md
@.planning/phases/05-kyc-capture-upload-pipeline/05-12-PLAN.md

<interfaces>
<!-- Current ci-device.yml structure (extracted) the executor must work within. -->

ci-device.yml has two jobs:
  - `changes` (ubuntu): computes `device` output from a `PATTERN` regex over the PR diff;
    fail-safe defaults to `device=true`. The PATTERN currently includes
    `validationLedgerDeviceTests/` and `\.github/workflows/ci-device\.yml` but NOT
    `validationLedgerUITests/`.
  - `device-security-surface` (self-hosted macOS device): job name is STABLE — referenced in
    branch protection; do NOT rename it. Its core step runs:
      xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger
        -destination "platform=iOS,id=${{ secrets.DEVICE_UDID }}"
        -only-testing:validationLedgerDeviceTests
        -retry-tests-on-failure -test-iterations 2
        -resultBundlePath $PWD/build/DeviceTestResults.xcresult
    timeout-minutes: 25.

Shared scheme `validationLedger.xcscheme` TestAction already declares three TestableReference
blocks: validationLedgerTests, validationLedgerUITests, validationLedgerDeviceTests. So the
UITest target is already in the scheme — confirm it is not `skipped`.

Project memory — a "cancelled" device CI run is usually a 25-min timeout because the runner
iPhone is locked. The XCUITest suite adds runtime; account for this in the timeout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the XCUITest suite to the device CI lane</name>
  <files>.github/workflows/ci-device.yml, validationLedger.xcodeproj/xcshareddata/xcschemes/validationLedger.xcscheme, docs/ci.md</files>
  <read_first>
    - .github/workflows/ci-device.yml (the file being modified — read the `changes` job PATTERN regex and the `device-security-surface` job's `xcodebuild test` step in full)
    - validationLedger.xcodeproj/xcshareddata/xcschemes/validationLedger.xcscheme (confirm the `validationLedgerUITests` TestableReference is present and not `skipped="YES"`)
    - docs/ci.md (the existing CI documentation — match its tone and section structure)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-12-PLAN.md (the four XCUITest file names so the doc note is accurate)
  </read_first>
  <action>
    Make three coordinated edits:

    1. `validationLedger.xcscheme` — confirm the `validationLedgerUITests` `TestableReference`
       exists and does NOT carry `skipped = "YES"`. If it is marked skipped, remove that
       attribute so the UITest target runs under the `validationLedger` scheme's test action.
       If it is already un-skipped, make no change to the scheme and note that in the summary.

    2. `ci-device.yml` `changes` job — extend the `PATTERN` regex so the
       `validationLedgerUITests/` directory is part of the security-surface path set. Add only
       `validationLedgerUITests/` so a change to the XCUITests themselves triggers the device
       lane. Keep the existing fail-safe behavior intact.

    3. `ci-device.yml` `device-security-surface` job — extend the `xcodebuild test` step so the
       new XCUITest suite runs on the device destination. Add `-only-testing:validationLedgerUITests`
       as a SECOND `-only-testing` flag alongside the existing `-only-testing:validationLedgerDeviceTests`
       (xcodebuild accepts multiple `-only-testing` flags — both suites run). Keep
       `-retry-tests-on-failure`, `-test-iterations 2`, and the `-resultBundlePath`. Raise
       `timeout-minutes` from 25 to 35 to accommodate the added XCUITest runtime (the four
       XCUITests each cold-launch and drive live UI); add a comment that the bump covers the
       XCUITest suite and that a locked runner iPhone still manifests as a ~timeout
       cancellation (project memory note). Do NOT rename the `device-security-surface` job —
       branch protection references it.

    4. `docs/ci.md` — add a short subsection describing that the device lane now also runs the
       `validationLedgerUITests` XCUITest suite, which automates the four Phase 5 device-UAT
       items (SC-2, D-08, D-12, Test-10 background/foreground), and naming the four test files.
  </action>
  <verify>
    <automated>grep -c 'only-testing:validationLedgerUITests' .github/workflows/ci-device.yml &amp;&amp; grep -c 'validationLedgerUITests' .github/workflows/ci-device.yml</automated>
  </verify>
  <acceptance_criteria>
    - Source: `ci-device.yml` `device-security-surface` step contains `-only-testing:validationLedgerUITests` AND keeps `-only-testing:validationLedgerDeviceTests`.
    - Source: the `changes` job `PATTERN` regex includes `validationLedgerUITests/`.
    - Source: `timeout-minutes` is raised above 25 with a comment noting the XCUITest runtime and the locked-iPhone caveat.
    - Source: the `device-security-surface` job name is unchanged.
    - Source: `validationLedger.xcscheme` `validationLedgerUITests` TestableReference is present and not `skipped`.
    - Source: `docs/ci.md` describes the device XCUITest lane and names the four files.
    - CLI: `grep -c 'only-testing:validationLedgerUITests' .github/workflows/ci-device.yml` returns 1.
  </acceptance_criteria>
  <done>The device CI lane runs both `validationLedgerDeviceTests` and `validationLedgerUITests` on the device destination; the scheme and CI docs reflect this.</done>
</task>

<task type="auto">
  <name>Task 2: Update 05-HUMAN-UAT.md and 05-VERIFICATION.md to 2 remaining human items</name>
  <files>.planning/phases/05-kyc-capture-upload-pipeline/05-HUMAN-UAT.md, .planning/phases/05-kyc-capture-upload-pipeline/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-kyc-capture-upload-pipeline/05-HUMAN-UAT.md (the file being modified — the 5-item checklist and the resume signal)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-VERIFICATION.md (the file being modified — the `human_verification:` frontmatter array of 5 entries)
    - .planning/phases/05-kyc-capture-upload-pipeline/05-12-PLAN.md (which 4 items were automated and the names of the XCUITest files that now cover them)
  </read_first>
  <action>
    Update both tracking docs to reflect that 4 of the 5 device-UAT items are now automated as
    device XCUITests, leaving exactly 2 human items.

    `05-HUMAN-UAT.md`:
    - Mark items 1 (SC-2), 3 (D-08), 4 (D-12), and the background/foreground portion of item 5
      (Test-10) as AUTOMATED, naming the covering XCUITest file from plan 05-12 for each
      (`KYCForceQuitResumeUITests`, `KYCProfileEntryUITests`, `KYCHardGateUITests`,
      `KYCCaptureLifecycleUITests`). Keep the historical text but clearly annotate each as
      automated on the device CI lane.
    - Keep exactly two items as remaining human-UAT: SC-4 (background-upload completion via
      `BGProcessingTaskRequest`) and the Test-10 deliberate `AVCaptureSessionRuntimeError`
      injection. State explicitly why each cannot be automated (iOS discretionary BGTask
      scheduling; a real camera hardware fault cannot be forced on demand).
    - Add a dated note (2026-05-18) recording that the device-UAT automation landed via plans
      05-11/05-12/05-13 and that the manual burden dropped from 5 items to 2.

    `05-VERIFICATION.md`:
    - Replace the 5-entry `human_verification:` frontmatter array with a 2-entry array
      containing ONLY the SC-4 item and the Test-10 runtime-error-injection item. For each
      remaining entry keep the `test` / `expected` / `why_human` keys; for the Test-10 entry
      narrow the `test`/`expected` wording to the deliberate runtime-error injection only (the
      background/foreground portion is now automated).
    - In the report body, add a short subsection noting that SC-2, D-08, D-12, and the Test-10
      background/foreground portion are now covered by device XCUITests
      (`validationLedgerUITests`) on the `ci-device.yml` lane, and that 2 human items remain.
    Do NOT change `score` or `status` — that is the verifier's call on a re-verify run; this
    task only records the automation and reduces the human_verification array.
  </action>
  <verify>
    <automated>grep -c 'SC-4' .planning/phases/05-kyc-capture-upload-pipeline/05-VERIFICATION.md &amp;&amp; grep -c 'AVCaptureSessionRuntimeError' .planning/phases/05-kyc-capture-upload-pipeline/05-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `05-VERIFICATION.md` `human_verification:` frontmatter array has exactly 2 entries — SC-4 and the Test-10 deliberate runtime-error injection.
    - Source: `05-HUMAN-UAT.md` marks items SC-2, D-08, D-12, and the Test-10 background/foreground portion as automated, each naming its covering XCUITest file.
    - Source: `05-HUMAN-UAT.md` retains exactly two open human-UAT items (SC-4 and the Test-10 runtime-error injection) with stated rationale for why each stays human.
    - Source: `05-HUMAN-UAT.md` carries a dated 2026-05-18 note recording the automation via plans 05-11/05-12/05-13.
    - Source: `score` and `status` in `05-VERIFICATION.md` frontmatter are unchanged by this task.
  </acceptance_criteria>
  <done>Both phase tracking docs record the device-UAT automation; the human-UAT burden is explicitly documented as 5 items reduced to 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the four XCUITests pass on the device CI lane</name>
  <action>
    Trigger the "CI (Device)" workflow on this gap-closure branch and confirm the four new
    XCUITests run and pass on the self-hosted device runner. The XCUITests drive a live camera
    and a real process kill — they cannot run inside this autonomous workflow, so a human must
    confirm the device CI lane result. See the verification steps below.
  </action>
  <what-built>
    The four XCUITests from plan 05-12 are now wired into the `ci-device.yml` self-hosted
    device lane (`-only-testing:validationLedgerUITests` added alongside the existing device
    test suite, `changes`-job path filter extended, `timeout-minutes` raised). The phase
    tracking docs (`05-HUMAN-UAT.md`, `05-VERIFICATION.md`) are updated to record 4 of 5
    device-UAT items as automated.
  </what-built>
  <how-to-verify>
    The XCUITests drive live camera and a real process kill — they only execute meaningfully on
    the self-hosted device runner, which cannot run inside this autonomous workflow. Verify the
    device CI lane on a real merge / manual dispatch:
    1. Push this gap-closure branch (or trigger the "CI (Device)" workflow via
       Actions → Run workflow on the branch).
    2. Confirm the `device-security-surface` job runs and the `xcodebuild test` step now
       executes BOTH `validationLedgerDeviceTests` AND `validationLedgerUITests` on the device
       destination — check the step log for the four new test classes
       (`KYCForceQuitResumeUITests`, `KYCProfileEntryUITests`, `KYCHardGateUITests`,
       `KYCCaptureLifecycleUITests`).
    3. Confirm the four XCUITest classes report PASSED in the uploaded `DeviceTestResults.xcresult`.
    4. Ensure the runner iPhone is UNLOCKED before the run — a locked iPhone manifests as a
       ~25-35 min "cancelled" timeout (project memory note), not a genuine failure.
    If any of the four XCUITests fails on the device, describe the failure (which test, which
    assertion) so it can be diagnosed.
  </how-to-verify>
  <verify>
    <human-check>The four XCUITest classes (KYCForceQuitResumeUITests, KYCProfileEntryUITests, KYCHardGateUITests, KYCCaptureLifecycleUITests) report PASSED in the device CI lane's DeviceTestResults.xcresult.</human-check>
  </verify>
  <acceptance_criteria>
    - The "CI (Device)" workflow `device-security-surface` job runs `validationLedgerUITests` on the device destination.
    - All four new XCUITest classes report PASSED on the real device runner.
    - No required-check status is broken (the `device-security-surface` job name is unchanged).
  </acceptance_criteria>
  <done>A human has confirmed the four XCUITests pass on the self-hosted device CI lane.</done>
  <resume-signal>Type "approved" once the four XCUITests pass on the device CI lane, or describe the failing test and assertion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → device runner | The self-hosted runner executes `xcodebuild test` with repo secrets (DEVICE_UDID, KEYCHAIN_PASSWORD) |
| test results → CI artifacts | `DeviceTestResults.xcresult` is uploaded and retained 14 days |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13-01 | Tampering | A required-check job rename breaking branch protection | mitigate | The `device-security-surface` job name is explicitly left unchanged; only the test step args and timeout change. Branch protection continues to reference the same job. |
| T-05-13-02 | Information Disclosure | The XCUITest run producing a screenshot/log carrying PII into `DeviceTestResults.xcresult` | mitigate | The plan-05-12 XCUITests assert on accessibility identifiers and synthetic seeded state only — no real identity data is typed. No `XCUIScreenshot` of identity imagery is captured. The xcresult bundle therefore carries no PII; the existing 14-day retention is acceptable. |
| T-05-13-03 | Denial of Service | The added XCUITest runtime exceeding the job timeout and blocking merges | accept | `timeout-minutes` is raised to 35 to absorb the four XCUITests. A genuine timeout (vs a locked-iPhone cancellation) would surface as a red required check — visible, not silent. |
| T-05-13-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan. SwiftPM dependency graph unchanged. |
</threat_model>

<verification>
- `grep 'only-testing:validationLedgerUITests' .github/workflows/ci-device.yml` returns the new flag on the device test step.
- The `changes` job `PATTERN` includes `validationLedgerUITests/`.
- The `device-security-surface` job name is byte-identical to before.
- `05-VERIFICATION.md` `human_verification:` array has exactly 2 entries; `05-HUMAN-UAT.md` has exactly 2 open human items.
- The human-verify checkpoint confirms the four XCUITests pass on the real device runner.
</verification>

<success_criteria>
- The device CI lane runs `validationLedgerUITests` on the device destination on every merge to `main` and PR touching the security/UITest surface.
- The four XCUITests pass on a real device (confirmed at the human-verify checkpoint).
- `05-HUMAN-UAT.md` and `05-VERIFICATION.md` record the device-UAT burden as reduced from 5 items to 2 (only SC-4 and the Test-10 runtime-error injection remain human).
- The `device-security-surface` required check name is unchanged — branch protection is not broken.
</success_criteria>

<output>
Create `.planning/phases/05-kyc-capture-upload-pipeline/05-13-SUMMARY.md` when done
</output>
