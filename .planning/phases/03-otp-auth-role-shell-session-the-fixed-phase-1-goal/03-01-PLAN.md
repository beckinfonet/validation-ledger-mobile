---
phase: 03-otp-auth-role-shell-session-the-fixed-phase-1-goal
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json
  - validationLedgerTests/Auth/SessionRestoreServiceTests.swift
  - validationLedgerTests/Auth/BiometricServiceTests.swift
  - validationLedgerTests/Auth/LogoutServiceTests.swift
  - validationLedgerTests/Auth/SensitiveActionServiceTests.swift
  - validationLedgerTests/Networking/Auth401ResponseInterceptorTests.swift
  - validationLedgerTests/Networking/APIClientRateLimitTests.swift
  - validationLedgerTests/Identity/PlatformPayloadFieldTests.swift
  - validationLedgerTests/Identity/Geo/LocationProviderTests.swift
  - validationLedgerTests/Identity/Geo/CountryGateTests.swift
  - validationLedgerTests/Features/Onboarding/Auth/PhoneEntryViewModelTests.swift
  - validationLedgerTests/Features/Onboarding/Auth/OTPViewModelTests.swift
  - validationLedgerTests/App/AppCoordinatorPhase3RoutingTests.swift
  - validationLedgerTests/Networking/EndpointEncodingTests.swift
autonomous: true
requirements: []
tags:
  - ios
  - test-infrastructure
  - wave-0
  - fixtures

must_haves:
  truths:
    - "Every Wave 1+ task has a placeholder test file or fixture to red→green against"
    - "otp-verify-rate-limited.json fixture exists and is loadable by FixtureLoader"
    - "All new test files compile against the existing test target (no missing imports, no untyped stubs)"
    - "Test files use Swift Testing (`import Testing`) — NOT XCTest — per project convention (only XCUITests stay on XCTest)"
    - "Each stub test file declares one @Suite + at least one @Test marked .disabled('Wave N implements') so the file compiles + reports skipped"
  artifacts:
    - path: "validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json"
      provides: "MockURLProtocol fixture body for D-02 429 rate-limited path"
      contains: "rate_limited"
    - path: "validationLedgerTests/Auth/SessionRestoreServiceTests.swift"
      provides: "Wave 0 stub — Plan 06 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Auth/BiometricServiceTests.swift"
      provides: "Wave 0 stub — Plan 06 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Auth/LogoutServiceTests.swift"
      provides: "Wave 0 stub — Plan 06 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Auth/SensitiveActionServiceTests.swift"
      provides: "Wave 0 stub — Plan 06 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Networking/Auth401ResponseInterceptorTests.swift"
      provides: "Wave 0 stub — Plan 06 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Networking/APIClientRateLimitTests.swift"
      provides: "Wave 0 stub — Plan 05 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Identity/PlatformPayloadFieldTests.swift"
      provides: "Wave 0 stub — Plan 03 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Identity/Geo/LocationProviderTests.swift"
      provides: "Wave 0 stub — Plan 07 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Identity/Geo/CountryGateTests.swift"
      provides: "Wave 0 stub — Plan 07 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Features/Onboarding/Auth/PhoneEntryViewModelTests.swift"
      provides: "Wave 0 stub — Plan 08 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Features/Onboarding/Auth/OTPViewModelTests.swift"
      provides: "Wave 0 stub — Plan 08 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/App/AppCoordinatorPhase3RoutingTests.swift"
      provides: "Wave 0 stub — Plan 09 fills in"
      contains: "@Suite"
    - path: "validationLedgerTests/Networking/EndpointEncodingTests.swift"
      provides: "Wave 0 stub — Plan 02 fills in"
      contains: "@Suite"
  key_links:
    - from: "validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json"
      to: "MockURLProtocol response handler"
      via: "FixtureLoader.data(named:)"
      pattern: "rate.limited"
---

<objective>
Wave 0 gate. Land all test stub files + the one new JSON fixture so every Wave 1+ task can immediately produce a red→green cycle without first creating its own scaffolding. This is purely test-target plumbing — zero source-target changes, zero application behavior changes.

Purpose: Phase 3 has 14 distinct new test files across Auth, Networking, Identity, Geo, Features, and App; without Wave 0 scaffolding, each Wave 1+ plan duplicates the "create empty file + add to scheme" boilerplate AND defers test-driven development by one round-trip per file. The Phase 3 RESEARCH `## Validation Architecture` section enumerates these files explicitly as the Wave 0 prerequisite.

Output: 13 new Swift Testing stub files + 1 new JSON fixture. Each stub compiles, declares a `@Suite("...")` named after its target requirement(s), and declares one `@Test(.disabled("Wave N implements"))` placeholder so the suite file is non-empty (Swift Testing requires at least one @Test method to register the suite). Compile is GREEN; tests are SKIPPED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-CONTEXT.md
@.planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-RESEARCH.md
@.planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-PATTERNS.md
@.planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-VALIDATION.md
@validationLedgerTests/Auth/SessionLockServiceTests.swift
@validationLedgerTests/Networking/RetryInterceptorTests.swift
@validationLedgerTests/Networking/Fixtures/otp-verify-failure.json
@validationLedger/Core/Networking/Mock/MockFixture.swift
@CLAUDE.md

<interfaces>
<!-- Existing test conventions (extracted from validationLedgerTests/Auth/SessionLockServiceTests.swift): -->

```swift
import Testing
import Foundation
@testable import validationLedger

@Suite("SessionLockService — unified invariant (FOUND-07)")
struct SessionLockServiceTests {
    @Test("Cold boot — shouldRequireBiometric is true when lastSuccess is nil")
    func coldBoot() {
        let svc = DefaultSessionLockService()
        #expect(svc.shouldRequireBiometric(now: Date()) == true)
    }
}
```

<!-- Phase 3 conventions (per 03-PATTERNS.md):
     - Unit tests: Swift Testing (`import Testing`) ALWAYS — Phase 1 STACK-03 + 03-PATTERNS.md flagged convention
     - UI tests (XCUITest): XCTest stays — separate target
     - Disabled-test syntax: `@Test(.disabled("reason"))` registers the test but skips it
-->

<!-- Existing fixture pattern (validationLedgerTests/Networking/Fixtures/otp-verify-failure.json): -->

```json
{
  "error_code": "auth.invalid_code",
  "message": "Invalid OTP code. Please try again.",
  "remaining_attempts": 2
}
```

<!-- Important: Retry-After is delivered via HTTP HEADER, not body — see 03-RESEARCH §4.
     The fixture body is informational; the test that uses it must register a MockURLProtocol
     handler that sets Retry-After: 60 in the response headers. See 03-PATTERNS.md
     §validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json for the exact
     registerFixture call. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create otp-verify-rate-limited.json fixture (D-02)</name>
  <read_first>
    - validationLedgerTests/Networking/Fixtures/otp-verify-failure.json (analog — exact 5-line shape)
    - .planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-RESEARCH.md lines 805–828 (D-02 fixture content + headers)
    - .planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-PATTERNS.md lines 1213–1259 (fixture pattern + registerFixture with custom headers)
    - validationLedger/Core/Networking/Mock/MockFixture.swift (registerFixture signature with `headers:` argument)
  </read_first>
  <action>
    Create file `validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json` with exactly this content:

    ```json
    {
      "error_code": "auth.rate_limited",
      "message": "Too many attempts. Try again in 60 seconds.",
      "retry_after_seconds": 60
    }
    ```

    Note (per D-02): the body is informational — the actual `Retry-After: 60` header is set by the test-side MockURLProtocol handler when `registerFixture` is called with `headers: ["Content-Type": "application/json", "Retry-After": "60"]`. Plan 05 wires that handler when implementing APIClient 429 parsing tests.

    No code changes. Fixture-only file addition.

    **Xcode project membership:** Per Phase 1 `PBXFileSystemSynchronizedRootGroup` setup, JSON files added to `validationLedgerTests/Networking/Fixtures/` are auto-included as resources for the test target. No project.pbxproj edit required.
  </action>
  <verify>
    <automated>test -f validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json && grep -q "rate_limited" validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json && grep -q "retry_after_seconds" validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json` exists
    - Contains string `"error_code": "auth.rate_limited"`
    - Contains string `"retry_after_seconds": 60`
    - Valid JSON (parses cleanly via `python3 -c "import json,sys; json.load(open(sys.argv[1]))" validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json` exits 0)
  </acceptance_criteria>
  <done>
    - Fixture file present and valid JSON
    - Plan 05 can call `FixtureLoader.data(named: "otp-verify-rate-limited")` without error
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 13 stub test files across Auth/Networking/Identity/Geo/Features/App</name>
  <read_first>
    - validationLedgerTests/Auth/SessionLockServiceTests.swift (canonical Swift Testing shape; lines 1–39)
    - validationLedgerTests/Networking/RetryInterceptorTests.swift (parameterized + actor-counter pattern; lines 1–60)
    - .planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-VALIDATION.md lines 53–62 (Wave 0 file checklist)
    - .planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-RESEARCH.md lines 1900–1918 (Wave 0 Gaps enumeration)
  </read_first>
  <action>
    Create 13 new Swift Testing stub files. Each follows the EXACT same template (only `@Suite` name + `@Test` reason string + filename change). Use the Write tool — never heredoc.

    **Stub template (apply to each file with the appropriate Suite/Test name):**

    ```swift
    // validationLedgerTests/<path>/<TargetName>Tests.swift
    // Wave 0 stub — Plan <NN> fills in red→green cycles for <REQ-IDs>.
    // Per 03-PATTERNS.md flagged convention: unit tests use Swift Testing (`import Testing`),
    // NOT XCTest. XCUITests stay on XCTest in a separate target.

    import Testing
    import Foundation
    @testable import validationLedger

    @Suite("<TargetName> — <one-line behavior summary> (<REQ-IDs>)")
    struct <TargetName>Tests {
        @Test(.disabled("Wave <N> Plan <NN> implements"))
        func placeholder() {
            // Implementation lands in Plan <NN>.
        }
    }
    ```

    **Files to create (path + Suite name + Plan that will fill it in):**

    1. `validationLedgerTests/Auth/SessionRestoreServiceTests.swift`
       - Suite: `"SessionRestoreService — cold-boot probe (SESS-01, AUTH-03)"`
       - Filled by Plan 06

    2. `validationLedgerTests/Auth/BiometricServiceTests.swift`
       - Suite: `"BiometricService — LAContext wrapper + domainState capture (SESS-03, D-09, D-10)"`
       - Filled by Plan 06

    3. `validationLedgerTests/Auth/LogoutServiceTests.swift`
       - Suite: `"LogoutService — single-funnel teardown (SESS-04, AUTH-04, D-16)"`
       - Filled by Plan 06

    4. `validationLedgerTests/Auth/SensitiveActionServiceTests.swift`
       - Suite: `"SensitiveActionService — M1 constructibility surface (AUTH-06, D-11, D-12)"`
       - Filled by Plan 06

    5. `validationLedgerTests/Networking/Auth401ResponseInterceptorTests.swift`
       - Suite: `"Auth401ResponseInterceptor — non-OTP 401 → logout (AUTH-05, D-28)"`
       - Filled by Plan 06

    6. `validationLedgerTests/Networking/APIClientRateLimitTests.swift`
       - Suite: `"APIClient — 429 + Retry-After parsing (AUTH-02, D-02)"`
       - Filled by Plan 05

    7. `validationLedgerTests/Identity/PlatformPayloadFieldTests.swift`
       - Suite: `"PlatformPayloadField — phantom-typed enum disjoint from LogField (GEO-03, D-23)"`
       - Filled by Plan 03

    8. `validationLedgerTests/Identity/Geo/LocationProviderTests.swift`
       - Suite: `"LocationProvider — CLLocationManager async wrapper (GEO-01, D-20)"`
       - Filled by Plan 07
       - **Directory create note:** `validationLedgerTests/Identity/Geo/` does not yet exist; create the directory before writing the file. Per Phase 1 PBXFileSystemSynchronizedRootGroup, new subdirectories under `validationLedgerTests/Identity/` are auto-included.

    9. `validationLedgerTests/Identity/Geo/CountryGateTests.swift`
       - Suite: `"CountryGate — reverse-geocode US-only refusal (GEO-02, D-20, D-21)"`
       - Filled by Plan 07

    10. `validationLedgerTests/Features/Onboarding/Auth/PhoneEntryViewModelTests.swift`
        - Suite: `"PhoneEntryViewModel — E.164 + geo gate orchestration (AUTH-01, GEO-01, GEO-02, D-20, D-26)"`
        - Filled by Plan 08
        - **Directory create note:** `validationLedgerTests/Features/Onboarding/Auth/` does not yet exist; create the full path.

    11. `validationLedgerTests/Features/Onboarding/Auth/OTPViewModelTests.swift`
        - Suite: `"OTPViewModel — Retry-After countdown + D-27 7-step orchestration (AUTH-02, AUTH-03, D-02, D-27)"`
        - Filled by Plan 08

    12. `validationLedgerTests/App/AppCoordinatorPhase3RoutingTests.swift`
        - Suite: `"AppCoordinator — .auth + .anotherActiveSession routing (SHELL-01, DEV-06, D-01, D-18)"`
        - Filled by Plan 09

    13. `validationLedgerTests/Networking/EndpointEncodingTests.swift`
        - Suite: `"Endpoint encoding — acronym CodingKeys snake_case bridge (Pre-Phase-3 IN-01, IN-05)"`
        - Filled by Plan 02

    **All 13 files use the EXACT template above** with `<TargetName>` substituted appropriately. The placeholder `@Test` is named `func placeholder()` consistently.

    **Compile gate:** After all files created, `xcodebuild build-for-testing -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5'` MUST succeed. Each file must compile. The `@testable import validationLedger` line ensures the test target sees internal types — this matches Phase 1+2 convention.

    **Do NOT:** use XCTest, add @available, write any non-stub assertions, add any `#expect`, or create any source-target file.
  </action>
  <verify>
    <automated>test -f validationLedgerTests/Auth/SessionRestoreServiceTests.swift && test -f validationLedgerTests/Auth/BiometricServiceTests.swift && test -f validationLedgerTests/Auth/LogoutServiceTests.swift && test -f validationLedgerTests/Auth/SensitiveActionServiceTests.swift && test -f validationLedgerTests/Networking/Auth401ResponseInterceptorTests.swift && test -f validationLedgerTests/Networking/APIClientRateLimitTests.swift && test -f validationLedgerTests/Identity/PlatformPayloadFieldTests.swift && test -f validationLedgerTests/Identity/Geo/LocationProviderTests.swift && test -f validationLedgerTests/Identity/Geo/CountryGateTests.swift && test -f validationLedgerTests/Features/Onboarding/Auth/PhoneEntryViewModelTests.swift && test -f validationLedgerTests/Features/Onboarding/Auth/OTPViewModelTests.swift && test -f validationLedgerTests/App/AppCoordinatorPhase3RoutingTests.swift && test -f validationLedgerTests/Networking/EndpointEncodingTests.swift && xcodebuild build-for-testing -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -derivedDataPath build 2>&1 | tail -5 | grep -E "BUILD SUCCEEDED|TEST BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 stub files exist at exact paths above
    - Each contains `import Testing` (NOT `import XCTest`)
    - Each contains `@testable import validationLedger`
    - Each contains exactly one `@Suite("...")` declaration matching the names above
    - Each contains exactly one `@Test(.disabled("Wave ... implements"))` declaration
    - `grep -L "import Testing" validationLedgerTests/Auth/SessionRestoreServiceTests.swift validationLedgerTests/Auth/BiometricServiceTests.swift validationLedgerTests/Auth/LogoutServiceTests.swift validationLedgerTests/Auth/SensitiveActionServiceTests.swift validationLedgerTests/Networking/Auth401ResponseInterceptorTests.swift validationLedgerTests/Networking/APIClientRateLimitTests.swift validationLedgerTests/Identity/PlatformPayloadFieldTests.swift validationLedgerTests/Identity/Geo/LocationProviderTests.swift validationLedgerTests/Identity/Geo/CountryGateTests.swift validationLedgerTests/Features/Onboarding/Auth/PhoneEntryViewModelTests.swift validationLedgerTests/Features/Onboarding/Auth/OTPViewModelTests.swift validationLedgerTests/App/AppCoordinatorPhase3RoutingTests.swift validationLedgerTests/Networking/EndpointEncodingTests.swift` returns NO output (every file imports Testing)
    - `xcodebuild build-for-testing` exits 0
  </acceptance_criteria>
  <done>
    - All 13 stub files compile
    - Test discovery picks up 13 new @Suites; each reports "1 disabled" on test run
    - Wave 1+ plans can MODIFY these files (not create) for their red→green cycles
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test target ↔ source target | `@testable import validationLedger` exposes internal types — only test files (which never ship) cross this boundary. No release-time impact. |
| fixture file ↔ MockURLProtocol | Fixture JSON loaded only in test process; never loaded by application code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information disclosure | Stub test files | accept | Stubs contain zero PII / zero secrets; `@testable import` is test-only. |
| T-03-01-02 | Tampering | Fixture JSON | accept | Fixture has no auth value (informational body only); Retry-After header is set by test code, not by the fixture file content. |

(Wave 0 has no production-surface threats — all changes are test-target-only.)
</threat_model>

<verification>
- 1 fixture file present and valid JSON
- 13 stub Swift Testing files present, all with `import Testing` (not XCTest)
- `xcodebuild build-for-testing -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5'` exits 0
- Test run lists 13 new @Suites, each with 1 disabled @Test
</verification>

<success_criteria>
- [ ] `validationLedgerTests/Networking/Fixtures/otp-verify-rate-limited.json` exists, valid JSON, contains `rate_limited` + `retry_after_seconds: 60`
- [ ] All 13 stub test files exist at the exact paths in `files_modified`
- [ ] Each stub file: `import Testing`, `@testable import validationLedger`, one `@Suite`, one `@Test(.disabled(...))`
- [ ] `xcodebuild build-for-testing -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5'` exits 0
- [ ] Wave 1 plans (02, 03, 04, 05) can immediately MODIFY (not CREATE) their respective stubs to add real assertions
</success_criteria>

<output>
After completion, create `.planning/phases/03-otp-auth-role-shell-session-the-fixed-phase-1-goal/03-01-SUMMARY.md` with:
- Files created (14 — 1 fixture + 13 stubs)
- Stub-to-Plan mapping (which Wave 1+ plan fills each)
- `xcodebuild build-for-testing` output snippet showing TEST BUILD SUCCEEDED
- Confirmation that Wave 1 plans now have a red→green target
</output>
</content>
</invoke>