---
phase: 09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone
plan: 03
type: execute
wave: 3
depends_on: [09.1-02]
files_modified:
  - validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift
  - validationLedgerTests/Loads/EveryoneOnLoadStripViewTests.swift
  - validationLedgerTests/Loads/Snapshot/EveryoneOnLoadStripViewSnapshotTests.swift
autonomous: true
requirements: [R2, R6]

must_haves:
  truths:
    - "EveryoneOnLoadStripView renders a horizontally-scrollable strip of role-avatar chips dynamically — chip count equals the de-duplicated set of `chainOfTrust.nodes[].role` in canonical role order (R2)."
    - "VL-1009 (2 brokers + 4 other roles = 6 nodes / 5 distinct roles) collapses to 5 chips — duplicate broker role merges into ONE BRK chip in the strip (R2 + D-10)."
    - "Each chip is a 44pt-minimum hit-test interactive surface that fires an `onRoleTap: (Role) -> Void` closure with the tapped role (D-08, D-09)."
    - "Each chip is its OWN accessibility leaf with `[.button, .staticText]` traits and a 3-field composed label `\"<RoleDisplayName>, <DisplayName>, <VerificationStateCopy>\"` per SPEC R6 (binding requirement; e.g. \"Broker, FreightWise Brokerage, verified\"). For single-node roles `<DisplayName>` is the node's `displayName`; for the multi-node collapsed chip (VL-1009 BRK) `<DisplayName>` is the literal `\"multiple parties\"` (UI-SPEC does not lock a multi-node string; planner default — documented in Task 1 action)."
    - "For multi-node roles (VL-1009 brokers), the collapsed chip's verification state is the MOST-SUSPICIOUS state (`.flagged > .pending > .unverified > .verified`) per RESEARCH §8 Q1 recommendation."
    - "A right-edge trailing 'All watching live · tap for details' caption sits OUTSIDE the horizontal scroll view (never scrolls off-screen)."
    - "Strip chips compose `RoleAvatarView(variant: .strip)` from Plan 02; no parallel verification-color switch exists in this file."
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift"
      provides: "Programmatic horizontal strip; consumes ChainOfTrust; emits Role taps via closure; per-chip composed accessibility labels"
      contains: "onRoleTap"
    - path: "validationLedgerTests/Loads/EveryoneOnLoadStripViewTests.swift"
      provides: "Chip count + ordering + de-dup + onRoleTap closure invocation + per-chip composed a11y label assertions across 5 representative fixtures"
      contains: "VL-1009"
    - path: "validationLedgerTests/Loads/Snapshot/EveryoneOnLoadStripViewSnapshotTests.swift"
      provides: "10 attachments (5 fixtures × 2 widths) for visual fingerprint matrix"
      contains: "UIKitSnapshot.attach"
  key_links:
    - from: "validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift"
      to: "validationLedger/UI/Components/RoleAvatarView.swift"
      via: "RoleAvatarView(variant: .strip) composition"
      pattern: "RoleAvatarView\\(.*variant: .strip"
    - from: "validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift"
      to: "validationLedger/Core/Load/ChainOfTrust.swift"
      via: "ingest chainOfTrust.nodes[].role for de-dup + canonical-order chip render"
      pattern: "chainOfTrust\\.nodes"
---

<objective>
Build `EveryoneOnLoadStripView` (R2): the dynamic horizontal strip of role-avatar chips that anchors the top of the iPhone Load Detail chain block. Composes `RoleAvatarView(variant: .strip)` from Plan 02; consumes `ChainOfTrust` directly; emits role taps via an `onRoleTap` closure that the LoadDetailViewController (Plan 05) wires to ChainOfVouchesView's scroll-and-highlight. Implements the R6 leaf-element accessibility model and the D-10 multi-broker "role is suspect, not just one party" semantics via most-suspicious-wins collapsed chip state.

Purpose: Replace the iPhone TrustGraphView's top-of-detail surface with an at-a-glance "who is on this load" affordance — the user can read all 5 roles' verification states without reading the tree itself, and tap any chip to scroll the tree to that role.

Output: One new source file (`EveryoneOnLoadStripView.swift`); two new test files (unit + snapshot) under `validationLedgerTests/Loads/` matching Phase 9 conventions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-CONTEXT.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-02-SUMMARY.md
@validationLedger/UI/Components/RoleAvatarView.swift
@validationLedger/UI/Components/VerificationBadgeView.swift
@validationLedger/UI/DesignSystem/Colors.swift
@validationLedger/UI/DesignSystem/Spacing.swift
@validationLedger/UI/DesignSystem/Typography.swift
@validationLedger/Features/Loads/Detail/TrustNodeView.swift
@validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift
@validationLedger/Core/Load/ChainOfTrust.swift
@validationLedger/Roles/Role.swift
@validationLedger/Core/Load/VerificationState.swift
@validationLedgerTests/Loads/ChainOfTrustFactory.swift
@validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift
@validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift
@validationLedgerTests/Support/UIKitSnapshot.swift

<interfaces>
<!-- Contracts the strip consumes + emits. -->

From validationLedger/UI/Components/RoleAvatarView.swift (Plan 02):
- `init(role: Role, state: VerificationState, variant: Variant = .strip)` — used with `.strip` (40pt) in every chip.
- `isAccessibilityElement = false` on the avatar — the chip wrapper owns the composed a11y label (R6 leaf model).

From validationLedger/Core/Load/ChainOfTrust.swift (FROZEN — R11):
- `public struct ChainOfTrust: Decodable, Sendable { public let nodes: [TrustNode]; public let edges: [TrustEdge]; public let integrity: ChainIntegrity }` (per RESEARCH §Sources).
- `public struct TrustNode { public let partyID: String; public let role: Role; public let displayName: String; public let verificationState: VerificationState; ... }`.

From validationLedger/Roles/Role.swift:
- 5 cases with `displayName` extension ("Shipper", "Broker", "Carrier", "Dispatch", "Factoring"); canonical order: shipper → broker → carrier → dispatch → factoring.

From validationLedger/Core/Load/VerificationState.swift:
- 4 cases (verified/pending/unverified/flagged); spoken-state vocabulary locked by UI-SPEC §Accessibility Contract: "verified" / "verification pending" / "not verified" / "flagged" (T-08-06 "not" prefix preserved).

From validationLedger/Features/Loads/Detail/TrustNodeView.swift (gesture-recognizer analog):
- `private let singleTap = UITapGestureRecognizer()` + `singleTap.numberOfTapsRequired = 1; singleTap.name = "trust-node.singleTap"; addGestureRecognizer(singleTap)` (lines 175-191) — Plan 03 uses the same shape, single-tap only (no double-tap arbitration).
- `internal var onSingleTap: ((String) -> Void)?` closure-callback surface (lines 109-127) — Plan 03 mirrors with `internal var onRoleTap: ((Role) -> Void)?`.

From validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift (NSLocalizedString format-string analog):
- Lines 211-218: `String(format: NSLocalizedString("loads.detail.banner.caution.format", value: "Risk flagged: %@", comment: "..."), reasonText)` — Plan 03 mirrors for the "All watching live · tap for details" caption and per-chip composed a11y labels.

From validationLedgerTests/Loads/ChainOfTrustFactory.swift:
- Existing factory pre-builds named fixtures used across Phase 9 tests (e.g., `ChainOfTrustFactory.fiveNodeClean(...)`). Plan 03 EXTENDS this file with factory methods for the 5 representative fixtures (VL-1001/1003/1005/1007/1009) if not already present — VL-1009 specifically needs a two-broker chain with mixed verification states (one .flagged, one .verified) so the "most-suspicious wins" rule is exercised.

From validationLedgerTests/Support/UIKitSnapshot.swift:
- `UIKitSnapshot.image(of:size:)` + `UIKitSnapshot.attach(_:name:to:)` — attachment-only, no PNG baselines (VALIDATION.md A1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EveryoneOnLoadStripView.swift — dynamic chip strip + onRoleTap closure + leaf-element a11y</name>
  <read_first>
    - validationLedger/Features/Loads/Detail/TrustNodeView.swift (FULL FILE — closest "programmatic chip-like feature view with tap closure" analog; lines 109-127 for the closure-callback surface shape; lines 175-191 for the gesture-recognizer setUp; lines 170-172 for the 44pt touch-target floor)
    - validationLedger/UI/Components/RoleAvatarView.swift (Plan 02 output — the primitive each chip wraps; verify the `.strip` variant constants)
    - validationLedger/UI/DesignSystem/Spacing.swift, validationLedger/UI/DesignSystem/Typography.swift (DS tokens used for chip layout + caption styling)
    - validationLedger/Core/Load/ChainOfTrust.swift (FROZEN — the type the strip consumes; read for nodes[]/integrity field shapes; do NOT modify per R11)
    - validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift (NSLocalizedString format-string + comment-cadence analog at lines 211-218)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (§ Group 3 → EveryoneOnLoadStripView NEW lines 184-228 — locked composition shape)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (§ Component Geometry → EveryoneOnLoadStripView table — full property contract)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (§8 Example 5 → strip chip a11y composition lines 887-899; §Open Questions Q1 — most-suspicious chip-state rule)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-SPEC.md (R6 — the BINDING 3-field strip-chip a11y label specification on line 52, takes precedence over UI-SPEC line 298's 2-field shorthand)
  </read_first>
  <behavior>
    - `configure(chainOfTrust: chain)` causes the strip to render N chips where N = `Set(chain.nodes.map { $0.role }).count`, ordered canonically `shipper → broker → carrier → dispatch → factoring`.
    - For VL-1003 (2 distinct roles): 2 chips. VL-1005 (3): 3. VL-1007 (4): 4. VL-1001 (5): 5. VL-1009 (6 nodes, 5 distinct roles): 5 chips (the duplicate broker collapses).
    - For multi-node roles (VL-1009 brokers — one .flagged, one .verified), the collapsed chip's avatar status-dot color reflects the MOST-SUSPICIOUS state: `.flagged > .pending > .unverified > .verified`. The composed a11y label reflects the same state.
    - Each chip has a `.button + .staticText` a11y trait combo and a **3-field composed label** `"<Role.displayName>, <DisplayName>, <VerificationStateCopy>"` per SPEC R6 (binding). For single-node roles the `<DisplayName>` slot is the node's `displayName` (e.g. "Broker, FreightWise Brokerage, verified"). For the multi-node collapsed chip (VL-1009 BRK) the `<DisplayName>` slot is the literal `"multiple parties"` (e.g. "Broker, multiple parties, flagged") — UI-SPEC does NOT lock a multi-node string at any line; this is the planner-chosen default per the B1 revision directive.
    - Tapping a chip fires `onRoleTap?(role)` exactly once with the chip's `Role`; haptic `UISelectionFeedbackGenerator().selectionChanged()` fires on tap.
    - Each chip's hit-test surface is ≥44×44pt (CLAUDE.md HIG; UI-SPEC §Spacing Scale).
    - The trailing "All watching live · tap for details" caption sits OUTSIDE the horizontal `UIScrollView` (never scrolls off-screen — UI-SPEC §Component Geometry).
    - `applySelectedRole(_ role: Role?)` applies a 2pt `DS.Colors.primary` outline highlight to the matching chip (or clears it when nil); animation 120ms fade-in / 200ms fade-out per D-11.
    - Container is `isAccessibilityElement = false`; only chips are leaves.
    - Zero `os_log` / `Logger.` / `print(` in the file (T-09-04 view-layer logging lock).
  </behavior>
  <action>
    Create `validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` as `public final class EveryoneOnLoadStripView: UIView`. File-header doc-comment block in the cadence of `ChainIntegrityBannerView.swift` (Contract / Composition / Accessibility / PII discipline sections). Layout: a horizontal `UIScrollView` (`alwaysBounceHorizontal = true`, `showsHorizontalScrollIndicator = false`) holding a horizontal `UIStackView` (`spacing = DS.Spacing.sm`, `alignment = .center`); the trailing "All watching live · tap for details" `UILabel` lives in a sibling subview OUTSIDE the scroll view, right-anchored to the strip's trailing edge. Private nested `final class ChipView: UIView` wrapping a vertical `UIStackView` `[RoleAvatarView(.strip), UILabel(role.displayName, DS.Typography.footnote, .label, .center, adjustsFontForContentSizeCategory=true)]`; ChipView publishes 44pt min hit-test via `widthAnchor.constraint(greaterThanOrEqualToConstant: 44)` + `heightAnchor.constraint(greaterThanOrEqualToConstant: 44)`; owns a `private let highlightLayer: CAShapeLayer` with `strokeColor = DS.Colors.primary.cgColor`, `lineWidth = 2`, `fillColor = nil`, `opacity = 0` and a `setHighlighted(_: Bool, animated: Bool)` method that fades opacity 0↔1 with 120ms/200ms timings (per RESEARCH Pattern 4). ChipView owns a `UITapGestureRecognizer` named `"everyone-on-load-strip.chip"` (mirror TrustNodeView.swift:191 naming) and a `onTap: (() -> Void)?` callback; tap handler fires `UISelectionFeedbackGenerator().selectionChanged()` + the callback. ChipView's a11y: `isAccessibilityElement = true`, `accessibilityTraits = [.button, .staticText]`, label composed via `private func composeAccessibilityLabel(role: Role, displayName: String, state: VerificationState) -> String` using NSLocalizedString with key `loads.detail.chain.9_1.strip.chip.a11y_label.format` value `"%1$@, %2$@, %3$@"` (positional substitutions: role.displayName + displayName + verificationStateSpoken(state)) and a `private static func verificationStateSpoken(_:) -> String` returning the LOCKED vocabulary "verified" / "verification pending" / "not verified" / "flagged" (T-08-06 "not" prefix preserved). The 3-field label is BINDING per SPEC R6 (line 52); UI-SPEC line 298 shows a 2-field shorthand that the SPEC supersedes. EveryoneOnLoadStripView public surface: `public init()` programmatic, `required init?(coder:)` traps with `fatalError`; `public var onRoleTap: ((Role) -> Void)?` closure (D-09); `public func configure(chainOfTrust chain: ChainOfTrust)` that (a) derives `presentRoles = Array(Set(chain.nodes.map { $0.role })).sorted(by: canonicalOrder)`, (b) for each role, computes the most-suspicious state across all nodes of that role via `mostSuspiciousState(in chain: ChainOfTrust, role: Role) -> VerificationState` using the ordering `.flagged > .pending > .unverified > .verified` (RESEARCH §Open Q1), (c) computes the chip's display-name slot via `private func chipDisplayName(in chain: ChainOfTrust, role: Role) -> String` — for single-node roles returns the node's `displayName`; for multi-node roles (count > 1) returns the literal `NSLocalizedString("loads.detail.chain.9_1.strip.chip.multi_node_displayname", value: "multiple parties", comment: "Phase 9.1 EveryoneOnLoadStripView collapsed-chip displayName slot when a role has >1 node (VL-1009 BRK). UI-SPEC does not lock this string at any line; planner-chosen default per B1 revision; if user/UI-SPEC subsequently lock a different string, update the localized value here."), (d) tears down existing ChipView subviews + recreates one per present role, (e) for each chip passes `(role: role, displayName: chipDisplayName(...), state: mostSuspiciousState(...))` to ChipView; ChipView calls `composeAccessibilityLabel(role:displayName:state:)` to set `accessibilityLabel`, (f) wires each ChipView's `onTap` to fire `self.onRoleTap?(chip.role)`; `public func applySelectedRole(_ role: Role?)` iterates chips and calls `chip.setHighlighted(chip.role == role, animated: true)`. Caption: trailing `UILabel.text = NSLocalizedString("loads.detail.chain.9_1.strip.caption", value: "All watching live · tap for details", comment: "Phase 9.1 EveryoneOnLoadStripView trailing meta caption — static text, NOT realtime backed (M3)")`, font `DS.Typography.footnote` regular, textColor `.secondaryLabel`, `adjustsFontForContentSizeCategory = true`. Container: `isAccessibilityElement = false`. NO Logger / os_log / print anywhere (T-09-04). Use `\u{00A0}·\u{00A0}` for the middle-dot separator in the caption per UI-SPEC §Copywriting Contract. Add file to `validationLedger` target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild build -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20 &amp;&amp; echo '---PII LOGGING GATE---' &amp;&amp; if git grep -nE '(os_log|Logger\.|print\()' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift; then echo 'T-09.1-02 VIOLATION'; exit 1; else echo 'View-layer logging lock OK'; fi</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` exists.
    - `grep -cE 'public final class EveryoneOnLoadStripView: UIView' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns 1.
    - `grep -cE 'public var onRoleTap: \(\(Role\) -> Void\)\?' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns 1 (D-09 closure surface).
    - `grep -cE 'public func configure\(chainOfTrust' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns 1.
    - `grep -cE 'public func applySelectedRole' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns 1.
    - `grep -cE 'composeAccessibilityLabel\(role:[ ]*displayName:[ ]*state:' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 1 (3-field a11y composer signature per SPEC R6 binding).
    - `grep -cE 'RoleAvatarView\(.*variant: \.strip' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 1 (composes Plan 02's primitive).
    - `grep -cE 'greaterThanOrEqualToConstant: 44' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 2 (44pt touch-target floor on chip width + height).
    - `grep -cE 'isAccessibilityElement = false' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 1 (container is not a leaf — R6).
    - `grep -cE 'accessibilityTraits = \[\.button, \.staticText\]' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 1 (D-08 + R6 chip traits).
    - `grep -cE 'UISelectionFeedbackGenerator' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 1 (chip-tap haptic).
    - `grep -cE 'loads\.detail\.chain\.9_1\.strip\.caption' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 1 (caption NSLocalizedString key per UI-SPEC).
    - `grep -cE 'loads\.detail\.chain\.9_1\.strip\.chip\.multi_node_displayname' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 1 (multi-node displayName localized key for VL-1009 BRK chip per B1).
    - `grep -cE '%1\$@, %2\$@, %3\$@' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` returns at least 1 (3-field positional format string).
    - PII view-layer logging gate (verify block grep): empty output → "View-layer logging lock OK".
    - Build succeeds.
  </acceptance_criteria>
  <done>EveryoneOnLoadStripView ships with dynamic chip composition, 44pt-floor interactive chips, leaf-element a11y model with the **3-field** (role, displayName, state) composed label per SPEC R6, `onRoleTap` closure surface, multi-node collapsed-chip displayName slot ("multiple parties" default), and the trailing caption — all consuming RoleAvatarView from Plan 02 and the locked NSLocalizedString keys from UI-SPEC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create EveryoneOnLoadStripViewTests.swift — chip count / ordering / de-dup / closure / a11y across 5 fixtures</name>
  <read_first>
    - validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift (post-Task-1 — view under test)
    - validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift (analog — per-fixture `makeView()` helper at lines 32-47; a11y-element introspection patterns at lines 55-101)
    - validationLedgerTests/Loads/ChainOfTrustFactory.swift (existing fixture builder — EXTEND with factory methods for the 5 representative fixtures if missing: `vl1001_fiveRolesClean()`, `vl1003_twoRolesClean()`, `vl1005_threeRolesClean()`, `vl1007_fourRolesClean()`, `vl1009_sixNodesTwoBrokersCompromised()` — VL-1009 MUST construct two broker nodes with DIFFERENT verification states (one .flagged, one .verified) to exercise the most-suspicious-wins rule)
    - validationLedger/Roles/Role.swift, validationLedger/Core/Load/VerificationState.swift, validationLedger/Core/Load/ChainOfTrust.swift (FROZEN — read only)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (§ Group 5 → EveryoneOnLoadStripViewTests lines 508-539 — locked test shape)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (§ Wave 0 Requirements + § R6 acceptance + § R2 acceptance)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-SPEC.md (R6 — the binding 3-field strip-chip a11y label specification on line 52)
  </read_first>
  <behavior>
    - Across the 5 fixtures, chip count matches expected: VL-1003=2, VL-1005=3, VL-1007=4, VL-1001=5, VL-1009=5.
    - Chip order matches canonical SHP→BRK→CAR→DSP→FCT for every fixture.
    - Tapping a chip via its tap recognizer fires `onRoleTap` exactly once with the correct Role (use a captured-closure test seam).
    - For VL-1009 (two brokers, .flagged + .verified): the collapsed BRK chip's composed a11y label is a **3-field** string with exactly 2 comma separators (per SPEC R6 binding), CONTAINS the role display name ("Broker"), CONTAINS the multi-node displayName literal ("multiple parties"), and CONTAINS the word "flagged" (most-suspicious wins per RESEARCH Q1).
    - For single-node roles (e.g. VL-1001 Shipper), the chip's composed a11y label is a 3-field string with exactly 2 commas, contains the role display name AND the node's `displayName` AND the verification-state spoken copy.
    - Container is `isAccessibilityElement = false`; each chip is a leaf with `[.button, .staticText]` traits.
    - The trailing caption text equals exactly `"All watching live · tap for details"` (using \u{00A0} non-breaking-space middle-dot separator per UI-SPEC §Copywriting Contract).
  </behavior>
  <action>
    First EXTEND `validationLedgerTests/Loads/ChainOfTrustFactory.swift` with the 5 fixture factory methods listed in `<read_first>` if any are missing. Each factory builds a `ChainOfTrust` value with the exact node count and role distribution required (read RESEARCH §8 Example 5 lines 902-910 for the VL-1001/1005/1007/1009/1010 expected shapes; for VL-1009 ensure the two broker nodes use `verificationState = .flagged` and `.verified` respectively so most-suspicious-wins is exercised). DO NOT modify any file under `validationLedger/Core/Load/` — only the test-fixture factory. Then create `validationLedgerTests/Loads/EveryoneOnLoadStripViewTests.swift` as `final class EveryoneOnLoadStripViewTests: XCTestCase` with `import XCTest`, `@testable import validationLedger`. Implement `private func makeStrip(for chain: ChainOfTrust) -> EveryoneOnLoadStripView` helper that instantiates, sets `view.bounds = CGRect(x: 0, y: 0, width: 390, height: 80)`, calls `configure(chainOfTrust:)`, then `layoutIfNeeded()`. Add tests: (1) `test_chipCount_per_fixture` — 5 sub-tests asserting chip count equals expected (2/3/4/5/5) for the 5 fixtures. (2) `test_chipOrder_canonical_shipper_broker_carrier_dispatch_factoring` — assert chip-by-chip role order on VL-1001 (all 5 roles present). (3) `test_VL1009_duplicateBroker_collapses_to_one_chip` — chip count = 5 (not 6) for VL-1009. (4) `test_VL1009_collapsedBrokerChip_a11yLabel_isThreeFieldAndCarriesMostSuspiciousState` — locate the chip whose role is `.broker`; assert `chip.accessibilityLabel` is non-nil; assert the label, split by ", ", produces exactly 3 components (3-field per SPEC R6); assert component[0] == "Broker"; assert component[1] == "multiple parties" (planner-default multi-node displayName); assert component[2] == "flagged" (most-suspicious wins). (5) `test_singleNodeChip_a11yLabel_isThreeFieldAndUsesNodeDisplayName` — for VL-1001's Shipper chip (single-node role), assert label splits into exactly 3 components, component[0] == "Shipper", component[1] equals the VL-1001 shipper node's `displayName` (e.g. "Global Exports"), component[2] == "verified". (6) `test_onRoleTap_closure_fires_with_tappedRole` — set `onRoleTap = { role in tappedRoles.append(role) }`, locate a chip view, manually invoke its tap callback (the internal `onTap` closure OR send `.touchUpInside` if the chip uses a UIButton; planner discretion based on Task 1's actual implementation), assert exactly one role captured and it matches. (7) `test_container_isAccessibilityElement_false` — assert `EveryoneOnLoadStripView.isAccessibilityElement == false`. (8) `test_eachChip_isAccessibilityLeaf_withButtonAndStaticTextTraits` — iterate chips, assert each `isAccessibilityElement == true` and `accessibilityTraits.contains(.button)` and `accessibilityTraits.contains(.staticText)`. (9) `test_trailingCaption_matchesLockedLocalizedString` — assert the strip's trailing caption label text equals the NSLocalizedString resolved value `"All watching live\u{00A0}·\u{00A0}tap for details"`. Add file to `validationLedgerTests` target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/EveryoneOnLoadStripViewTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -25 &amp;&amp; echo '---R11 GATE (interim)---' &amp;&amp; git diff --name-only HEAD~1..HEAD -- 'validationLedger/Core/Load/' | tee /tmp/r11.txt &amp;&amp; if [ -s /tmp/r11.txt ]; then echo 'R11 VIOLATION: Core/Load changed in Plan 03'; exit 1; else echo 'R11 OK: zero Core/Load changes'; fi</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedgerTests/Loads/EveryoneOnLoadStripViewTests.swift` exists.
    - `grep -cE 'func test_' validationLedgerTests/Loads/EveryoneOnLoadStripViewTests.swift` returns at least 9 (the 9 test methods listed — added one test for the single-node 3-field assertion).
    - Test invocation output contains "Test Suite 'EveryoneOnLoadStripViewTests' passed".
    - VL-1009 3-field a11y label test (asserts 3 components AND "Broker" AND "multiple parties" AND "flagged"), single-node 3-field test, and onRoleTap closure invocation test all pass.
    - R11 interim gate: `git diff --name-only` shows ZERO files touched under `validationLedger/Core/Load/` for this plan (only ChainOfTrustFactory.swift under tests/ was modified, which is NOT a Core/Load file).
  </acceptance_criteria>
  <done>EveryoneOnLoadStripView's R2 (dynamic chip count + de-dup + canonical ordering), D-09/D-10 (closure surface + multi-broker collapse with most-suspicious state), and R6 (leaf-element a11y traits + **3-field composed labels**) contracts are all assertion-locked across the 5 representative fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create EveryoneOnLoadStripViewSnapshotTests.swift — 10 attachments (5 fixtures × 2 iPhone widths)</name>
  <read_first>
    - validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift (post-Task-1 — view being snapshotted)
    - validationLedgerTests/Loads/ChainOfTrustFactory.swift (post-Task-2 — the 5 fixture factories must be present)
    - validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift (matrix-helper precedent at lines 25-78 — `renderedView(...)` + `render(...)` shape; Pitfall-8 `traitOverrides.preferredContentSizeCategory` BEFORE configure at line 63)
    - validationLedgerTests/Support/UIKitSnapshot.swift (attach helper; attachment-only)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (§ Group 6 → EveryoneOnLoadStripViewSnapshotTests lines 667-714 — locked matrix shape)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (§ Snapshot Test Matrix → Strip row — 5 fixtures × 2 iPhone widths: iPhone 15 portrait 390pt + iPhone SE 3 portrait 375pt)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (§ Wave 0 Requirements → EveryoneOnLoadStripViewSnapshotTests line)
  </read_first>
  <behavior>
    - The suite attaches 10 distinct snapshots (5 fixtures × 2 widths) with names like `EveryoneOnLoadStrip-VL-1001-iPhone15Portrait` and `EveryoneOnLoadStrip-VL-1009-iPhoneSE3Portrait`.
    - At iPhone SE 3 portrait (375pt — the narrowest target), the 5-chip fixture (VL-1001 and VL-1009) MUST render without truncating the trailing caption (UI-SPEC §Component Geometry — SE width is the worst-case "must fit").
    - Trait overrides are applied BEFORE `configure(chainOfTrust:)` + `layoutIfNeeded()` (Pitfall 8 / WR-04 lock).
  </behavior>
  <action>
    Create `validationLedgerTests/Loads/Snapshot/EveryoneOnLoadStripViewSnapshotTests.swift` as `final class EveryoneOnLoadStripViewSnapshotTests: XCTestCase`. Declare `private static let iPhone15PortraitWidth: CGFloat = 390`, `private static let iPhoneSE3PortraitWidth: CGFloat = 375`, `private static let canvasHeight: CGFloat = 80` (chip + caption + insets). Implement `private func render(chain: ChainOfTrust, width: CGFloat, name: String)`: instantiate `EveryoneOnLoadStripView()`, set `view.bounds = CGRect(x: 0, y: 0, width: width, height: Self.canvasHeight)`, `view.overrideUserInterfaceStyle = .light`, set `view.traitOverrides.preferredContentSizeCategory = .large` BEFORE configure (Pitfall 8), call `view.configure(chainOfTrust: chain)`, `view.layoutIfNeeded()`, then `UIKitSnapshot.attach(UIKitSnapshot.image(of: view, size: view.bounds.size), name: name, to: self)`. Generate 10 test methods `test_VL_<id>_<iPhone15Portrait|iPhoneSE3Portrait>_rendersExpected` covering all 5 fixtures × 2 widths. Use the `ChainOfTrustFactory.vl1001_...` / `vl1003_...` / `vl1005_...` / `vl1007_...` / `vl1009_...` factories from Plan-03-Task-2. Snapshot names follow the template `"EveryoneOnLoadStrip-VL-1001-iPhone15Portrait"` etc. Add file to `validationLedgerTests` target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/EveryoneOnLoadStripViewSnapshotTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `validationLedgerTests/Loads/Snapshot/EveryoneOnLoadStripViewSnapshotTests.swift` exists.
    - Test invocation output contains "Test Suite 'EveryoneOnLoadStripViewSnapshotTests' passed".
    - The suite generates at least 10 test methods covering 5 fixtures × 2 widths (one method per cell).
    - `traitOverrides.preferredContentSizeCategory` is set BEFORE `configure(chainOfTrust:)` in the render helper (grep `validationLedgerTests/Loads/Snapshot/EveryoneOnLoadStripViewSnapshotTests.swift` — `traitOverrides` line must appear before `configure(` line in source order).
  </acceptance_criteria>
  <done>10 EveryoneOnLoadStripView snapshots populate the CI artifact bundle on the iPhone 15 lane; the iPhone SE 3 worst-case width is covered for all fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strip view → log surfaces | Party `displayName` is consumed by this view in the 3-field a11y label (single-node chips include the node's `displayName`; multi-node chips include the literal `"multiple parties"`); the composed label can become PII-class IF a future developer logs it for debugging. |
| ChainOfTrust ingest → fraud derivation | A naive developer could implement most-suspicious-wins by re-deriving "is this chain compromised" — but RESEARCH locks this as a pure presentation rule (state ordering), NOT a chain-integrity computation. The chip state remains a per-role aggregation of `node.verificationState` values that are themselves server-supplied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-02 | Information Disclosure (PII via log line) | `EveryoneOnLoadStripView.swift` (chip composed labels include Role + party `displayName` + VerificationState — single-node chips carry PII-class party names) | mitigate | View-layer logging lock — Task 1 verify block runs `git grep -nE '(os_log\|Logger\.\|print\()' validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` and FAILS if anything matches. Wired into CI per VALIDATION.md. The B1-revised 3-field label now carries party `displayName` for single-node chips, raising the importance of the logging lock relative to the prior 2-field design. |
| T-09.1-04 | Tampering (client-side trust derivation) | Strip chip state for multi-node roles (e.g. VL-1009 broker collapse) | mitigate | The "most-suspicious wins" rule is a pure presentation aggregation over `node.verificationState` values (server-supplied). NO `derive*` / `compute*` methods over `edges` or `chain.integrity`. The strip never reads `chain.integrity.verdict` or `chain.integrity.implicatedNodeIDs`. Test `test_VL1009_collapsedBrokerChip_a11yLabel_isThreeFieldAndCarriesMostSuspiciousState` proves the aggregation is presentation-only. |
| T-09.1-01 | Tampering (color-ramp drift) | Status-dot color via composed RoleAvatarView | mitigate | RoleAvatarView reaches through `DS.Colors.Verification.color(for:)` per Plan 02; this plan adds NO new switch over `VerificationState` for color assignment. R12 strict-grep gate (Plan 01) covers. |
</threat_model>

<verification>
- Build: `xcodebuild build ...` exits 0 (Task 1 verify block).
- Strip unit tests: `xcodebuild test ... -only-testing:validationLedgerTests/EveryoneOnLoadStripViewTests` passes 9 test methods including the 3-field a11y assertions (Task 2 verify block).
- Strip snapshot tests: `xcodebuild test ... -only-testing:validationLedgerTests/EveryoneOnLoadStripViewSnapshotTests` passes; 10 attachments populate the CI bundle (Task 3 verify block).
- View-layer logging lock: zero `os_log` / `Logger.` / `print(` in `EveryoneOnLoadStripView.swift` (Task 1 verify block).
- R11 interim gate (Task 2 verify block): no files under `validationLedger/Core/Load/` were modified by this plan.
</verification>

<success_criteria>
- [ ] `validationLedger/Features/Loads/Detail/EveryoneOnLoadStripView.swift` exists; composes `RoleAvatarView(.strip)`; exposes `onRoleTap: ((Role) -> Void)?`, `configure(chainOfTrust:)`, `applySelectedRole(_:)`.
- [ ] Strip chip count for the 5 fixtures equals 2/3/4/5/5; chips are ordered canonically SHP→BRK→CAR→DSP→FCT.
- [ ] VL-1009's two brokers collapse to ONE BRK chip whose **3-field** composed a11y label is `"Broker, multiple parties, flagged"` ("flagged" wins per most-suspicious rule).
- [ ] Single-node chips render a 3-field composed a11y label `"<RoleDisplayName>, <node.displayName>, <VerificationStateCopy>"` per SPEC R6.
- [ ] Each chip is a 44pt-floor interactive surface with `[.button, .staticText]` a11y traits; container is `isAccessibilityElement = false` (R6).
- [ ] Trailing "All watching live · tap for details" caption (non-breaking middle-dot) sits outside the horizontal scroll view; sourced via NSLocalizedString key `loads.detail.chain.9_1.strip.caption`.
- [ ] `EveryoneOnLoadStripViewTests.swift` (9 test methods including the two new 3-field a11y assertions) passes on iPhone 15 simulator lane.
- [ ] `EveryoneOnLoadStripViewSnapshotTests.swift` attaches 10 snapshots (5 fixtures × 2 widths).
- [ ] View-layer logging lock holds (no `os_log` / `Logger.` / `print(` in the new view file).
- [ ] R11 interim gate: zero `validationLedger/Core/Load/` files modified by this plan.
- [ ] Project builds clean.
</success_criteria>

<output>
Create `.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-03-SUMMARY.md` when done.
</output>
</content>
</invoke>