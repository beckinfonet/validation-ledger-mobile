---
phase: 09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone
plan: 04
type: execute
wave: 3
depends_on: [09.1-01, 09.1-02]
files_modified:
  - validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift
  - validationLedgerTests/Loads/ChainOfVouchesViewTests.swift
  - validationLedgerTests/Loads/ChainOfVouchesViewAccessibilityTests.swift
  - validationLedgerTests/Loads/Snapshot/ChainOfVouchesViewSnapshotTests.swift
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1003.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1005.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1007.json
autonomous: true
requirements: [R3, R4, R7, R9, R11]

must_haves:
  truths:
    - "ChainOfVouchesView renders the chain as a vertical UIStackView of ChainOfVouchesRowView instances in depth-first canonical role order (SHP, BRK, CAR, DSP, FCT), with siblings sorted by partyID alphabetical for determinism (R3)."
    - "Each row's attribution suffix comes from the LOCKED role-hierarchy parent table BRK->SHP, CAR->BRK, DSP->CAR, FCT->CAR — NEVER from edges[].fromPartyID (R9)."
    - "When an intermediate role is missing (sparse chain), the attribution walks UP the canonical hierarchy until a present role is found (Pitfall 7)."
    - "VL-1009 renders both broker rows as siblings under SHP with attribution 'VIA SHP' (R9), regardless of the edges' fromPartyID."
    - "The chain-integrity footer pill (R4) renders on EVERY verdict tier — including .clean (green tinted pill) — as a folded child of the card, NOT a standalone banner."
    - "On iPhone, the tree carries ZERO fraud-signaling visual treatment (R7): no dim-others, no dashed-flagged-edge stroke (connectors are always dashed-neutral), no pulse halo. The footer pill is the sole fraud signal."
    - "Each tree row is its own accessibility leaf with .staticText traits and a composed label per UI-SPEC §Accessibility Contract; root row uses 'root of the chain' instead of 'vouched by ...' (R6). Composition is computed by a `composeRowLabel(node:parentNode:stateCopy:)` helper and ASSIGNED on each row instance in `buildRows(from:)` BEFORE rows are added to the tree stack (B2 fix)."
    - "Zero changes to any file under validationLedger/Core/Load/ — ChainOfTrust, TrustNode, TrustEdge, ChainIntegrity, PriorRelationship, VerificationState, Role (R11)."
    - "Fixture text-only edit to chainIntegrity.reason for the 4 clean fixtures (VL-1001/1003/1005/1007) sets the affirmative-cadence text 'N vouches, no anonymous parties' (UI-SPEC Q3 — text-content only, NOT a data-model change)."
  artifacts:
    - path: "validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift"
      provides: "Vertical attribution tree card (eyebrow + title + subtitle + tree-of-rows + footer pill); per-row CAShapeLayer dashed connectors with Y-fork for siblings; applySelectedRole highlight API; row a11y composition + assignment inside buildRows(from:)"
      contains: "ChainOfVouchesRowView"
    - path: "validationLedgerTests/Loads/ChainOfVouchesViewTests.swift"
      provides: "Tree-builder assertions (attribution suffix per row x 5 fixtures; sparse-chain fallback; VL-1009 multi-broker forced-siblings; footer pill verdict rendering for clean/caution/compromised)"
      contains: "test_VL1009_multiBrokerSiblings"
    - path: "validationLedgerTests/Loads/ChainOfVouchesViewAccessibilityTests.swift"
      provides: "Leaf-element a11y assertions (container not a leaf; each row a leaf with .staticText + composed label for 5 fixtures; footer pill a11y label)"
      contains: "isAccessibilityElement"
    - path: "validationLedgerTests/Loads/Snapshot/ChainOfVouchesViewSnapshotTests.swift"
      provides: "Fingerprint matrix — 5 fixtures x 2 iPhone widths x at least 3 verdicts; compromised fixtures show full-alpha rows + footer pill as sole fraud signal (R7)"
      contains: "UIKitSnapshot.attach"
  key_links:
    - from: "validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift"
      to: "validationLedger/UI/Components/RoleAvatarView.swift"
      via: "RoleAvatarView(variant: .tree) per row"
      pattern: "RoleAvatarView\\(.*variant: \\.tree"
    - from: "validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift"
      to: "validationLedger/UI/DesignSystem/Colors.swift"
      via: "DS.Colors.caution + DS.Colors.destructive for footer pill tint"
      pattern: "DS\\.Colors\\."
---

<objective>
Build ChainOfVouchesView (R3, R4, R7, R9): the vertical attribution tree card that replaces the iPhone 2D TrustGraphView. The card chrome holds an eyebrow + title + subtitle paragraph + a vertical UIStackView of ChainOfVouchesRowView instances connected by dashed CAShapeLayer Y-fork connectors, with a folded chain-integrity footer pill that renders on every verdict (including the new green .clean tier). The role-hierarchy parent table (R9) drives the "VIA X" attribution suffix — siblings are forced under the canonical parent regardless of edges[].fromPartyID data. Phase 9 D-15 fraud-signaling visual treatments (dim-others / dashed-flagged-edges / pulse) are SUPERSEDED on iPhone (R7); the footer pill carries the entire fraud signal.

Purpose: Deliver the marquee surface of Phase 9.1 — the chain-of-vouches card the user sees when opening any iPhone Load Detail. This is what device UAT 2026-05-20 was redesigning toward.

Output: One new source file (ChainOfVouchesView.swift — single file per RESEARCH Recommended Project Structure recommendation; planner may split into a ChainOfVouches/ subdirectory if it crosses ~400 LOC); three new test files (unit + a11y + snapshot); 4 fixture text-only edits to chainIntegrity.reason (NOT a data-model change — UI-SPEC Q3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-CONTEXT.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md
@.planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-02-SUMMARY.md
@validationLedger/UI/Components/RoleAvatarView.swift
@validationLedger/UI/DesignSystem/Colors.swift
@validationLedger/UI/DesignSystem/Spacing.swift
@validationLedger/UI/DesignSystem/Typography.swift
@validationLedger/Features/Loads/Detail/TrustGraphView.swift
@validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift
@validationLedger/Core/Load/ChainOfTrust.swift
@validationLedger/Core/Load/ChainIntegrity.swift
@validationLedger/Roles/Role.swift
@validationLedger/Core/Load/VerificationState.swift
@validationLedgerTests/Loads/ChainOfTrustFactory.swift
@validationLedgerTests/Loads/TrustGraphViewLayoutTests.swift
@validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift
@validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift
@validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift
@validationLedgerTests/Support/UIKitSnapshot.swift

<interfaces>
<!-- Contracts the tree consumes + emits. -->

From validationLedger/UI/Components/RoleAvatarView.swift (Plan 02):
- init(role: Role, state: VerificationState, variant: Variant = .strip) — used with .tree (32pt) for each row.

From validationLedger/UI/DesignSystem/Colors.swift (Plan 01):
- DS.Colors.Verification.color(for:) — NOT directly consumed by ChainOfVouchesView (avatar dot color flows through RoleAvatarView).
- DS.Colors.caution, DS.Colors.destructive — footer pill tint for .caution / .compromised verdicts (mirrors ChainIntegrityBannerView.backgroundColorForVerdict shape at lines 170-202 of that file).
- DS.Colors.surface, DS.Colors.separator, DS.Colors.label, DS.Colors.labelSecondary — card chrome + connector stroke + label colors.

From validationLedger/Core/Load/ChainOfTrust.swift (FROZEN — R11):
- ChainOfTrust { nodes: [TrustNode], edges: [TrustEdge], integrity: ChainIntegrity }.
- TrustNode { partyID: String, role: Role, displayName: String, verificationState: VerificationState, ... }.

From validationLedger/Core/Load/ChainIntegrity.swift (FROZEN — R11):
- public enum Verdict: String, Sendable, CaseIterable { case clean; case caution; case compromised }
- public let verdict: Verdict
- public let reason: String

From validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift (footer-pill verdict-mapping analog):
- Lines 170-202: backgroundColorForVerdict() / foregroundColorForVerdict() / symbolNameForVerdict(). IMPORTANT: Phase 9 banner SHORT-CIRCUITS on .clean (hidden); the 9.1 footer pill MUST render on .clean with a GREEN tinted pill per UI-SPEC Footer pill verdict-tier colors.
- Lines 75-87 + 134: adjustsImageSizeForAccessibilityContentSizeCategory = true + UIImage.SymbolConfiguration(textStyle: .footnote) (Pitfall 6 / CR-03) — footer pill MUST replicate.
- Lines 211-218: String(format: NSLocalizedString(...), reasonText) cadence — footer pill copy uses the locked UI-SPEC Copywriting Contract keys.

From validationLedger/Features/Loads/Detail/TrustGraphView.swift (CAShapeLayer + traitChange + layoutSubviews analog):
- Lines 117-128: edgeLayers: [CAShapeLayer] + currentChain cache pattern.
- Lines 335-370: per-edge layer construction in configure.
- Lines 608-647: UIBezierPath path geometry recomputed in layoutSubviews() (Pitfall 1).
- Lines 649-658: traitCollectionDidChange + setNeedsLayout pattern.
- Lines 372-391: halo-layer lifecycle (Pattern 4 analog for the highlight outline).

Role-hierarchy parent table (UI-SPEC Role-Hierarchy Parent Table — LOCKED):
- .shipper -> nil (root -> "ROOT" suffix)
- .broker -> .shipper -> "VIA SHP"
- .carrier -> .broker -> "VIA BRK"
- .dispatch -> .carrier -> "VIA CAR"
- .factoring -> .carrier -> "VIA CAR"
- When intermediate role missing, walk UP canonical hierarchy until a present role is found (Pitfall 7 — e.g., shipper+carrier-only chain attributes carrier to "VIA SHP").

NSLocalizedString keys (UI-SPEC Copywriting Contract — LOCKED):
- loads.detail.chain.9_1.card.eyebrow = "Every party attested"
- loads.detail.chain.9_1.card.title = "Chain of vouches"
- loads.detail.chain.9_1.card.subtitle = "Every party on this load is here because someone above them attested to them. Tap a chip to see the vouch."
- loads.detail.chain.9_1.row.attribution.root = "ROOT"
- loads.detail.chain.9_1.row.attribution.format = "VIA %@" (parent role's 3-letter uppercase abbreviation: SHP/BRK/CAR/DSP/FCT)
- loads.detail.chain.9_1.footer.clean.format = "✓ Chain intact · %@"
- loads.detail.chain.9_1.footer.caution.format = "⚠ Risk flagged · %@"
- loads.detail.chain.9_1.footer.compromised.format = "⛔ Chain compromised · %@"
- Middle-dot separator is `\u{00A0}·\u{00A0}` (non-breaking spaces).

Footer pill verdict-tier visual tokens (UI-SPEC Color):
- .clean: background = .systemGreen.withAlphaComponent(0.18), foreground = .systemGreen, icon = checkmark.seal.fill.
- .caution: background = DS.Colors.caution (.systemYellow), foreground = .label, icon = exclamationmark.triangle.fill.
- .compromised: background = DS.Colors.destructive (.systemRed), foreground = .white, icon = exclamationmark.octagon.fill.

Row a11y composition template (UI-SPEC §Accessibility Contract — LOCKED; UI-SPEC.md is 407 lines so SPEC R6's "implicated-suffix per UI-SPEC line 845" is a stale reference. UI-SPEC does NOT define any implicated-row a11y suffix; R7 supersedes by mandating the footer pill as the sole fraud signal on iPhone. The composed row label therefore carries NO implicated suffix — consistent with R7):
- Root row: `"<RoleDisplayName> <node.displayName>, root of the chain, <VerificationStateCopy>"` (e.g. "Shipper Global Exports, root of the chain, verified").
- Non-root row: `"<RoleDisplayName> <node.displayName>, vouched by <ParentRoleDisplayName> <Parent.displayName>, <VerificationStateCopy>"` (e.g. "Broker FreightWise Brokerage, vouched by Shipper Global Exports, verified").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ChainOfVouchesView.swift — card chrome + tree-builder + CAShapeLayer Y-fork connectors + footer pill + per-row a11y composition wired in buildRows</name>
  <read_first>
    - validationLedger/Features/Loads/Detail/TrustGraphView.swift (FULL FILE — analog for CAShapeLayer ownership, layoutSubviews path recompute lines 608-647, traitCollectionDidChange + setNeedsLayout lines 649-658, edgeLayers + currentChain cache lines 117-128, per-edge layer construction lines 335-370, halo-layer lifecycle lines 372-391)
    - validationLedger/Features/Loads/Detail/ChainIntegrityBannerView.swift (FULL FILE — analog for the footer pill: backgroundColorForVerdict / foregroundColorForVerdict / symbolNameForVerdict at lines 170-202; AX symbol scaling at lines 75-87 + 134; NSLocalizedString format-string at lines 211-218; CRITICAL difference: Phase 9 banner short-circuits on .clean to isHidden = true — the 9.1 footer pill MUST render on .clean with the GREEN tinted pill per UI-SPEC)
    - validationLedger/UI/Components/RoleAvatarView.swift (Plan 02 — the .tree variant primitive each row composes)
    - validationLedger/UI/Components/VerificationBadgeView.swift (analog for Pitfall-8 cornerRadius recompute pattern at lines 92-97 — apply to the footer pill bounds.height/2)
    - validationLedger/UI/DesignSystem/Spacing.swift, validationLedger/UI/DesignSystem/Typography.swift, validationLedger/UI/DesignSystem/Colors.swift (Plan 01 state — DS tokens consumed)
    - validationLedger/Core/Load/ChainOfTrust.swift, validationLedger/Core/Load/ChainIntegrity.swift, validationLedger/Roles/Role.swift, validationLedger/Core/Load/VerificationState.swift (FROZEN — R11; read only for field shapes)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (Pattern 1 lines 264-334; Pattern 2 lines 336-385; Pattern 4 lines 429-468; Pitfall 1 lines 525-533; Pitfall 3 lines 545-553; Pitfall 5 lines 565-573; Pitfall 6 lines 575-583; Pitfall 7 lines 585-611 sparse-chain attribution-fallback algorithm; Example 5 lines 849-885 row a11y composition shape)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (Group 3 ChainOfVouchesView NEW lines 232-354)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (Component Geometry ChainOfVouchesView table; Role-Hierarchy Parent Table; Footer pill verdict-tier colors; §Accessibility Contract line 298-299 — locked composed-label template for tree rows)
  </read_first>
  <behavior>
    - configure(chainOfTrust:) rebuilds the card content: eyebrow -> title -> subtitle -> tree (depth-first canonical-role-order rows) -> footer pill.
    - Tree-builder visits roles in canonical order (shipper, broker, carrier, dispatch, factoring); within a role, sorts nodes by partyID alphabetical for determinism (RESEARCH Pattern 2 lines 354-360).
    - Each row composes RoleAvatarView(role:, state: node.verificationState, variant: .tree) + a vertical label stack: role name (headline semibold) + attributionRow (attribution suffix + displayName).
    - Attribution suffix is computed by walking UP the canonical parent table from the row's role until a parent role IS PRESENT in Set(chain.nodes.map { $0.role }). If the row's role is .shipper OR no canonical ancestor is present in the chain, the row gets the literal ROOT suffix (root of a sparse subtree).
    - VL-1009 (two brokers) produces two adjacent broker rows, BOTH with attribution "VIA SHP", both with their per-node verification states (one .flagged, one .verified).
    - Each row owns a CAShapeLayer connector (lineWidth = 1.5, lineDashPattern = [3, 3], strokeColor = DS.Colors.separator.cgColor, fillColor = nil). The connector path is recomputed in layoutSubviews (Pitfall 1) and inserted into the PARENT stack's layer (RESEARCH Pattern 1: geometry spans parent-row's avatar bottom-center to this row's avatar top-center; for sibling rows the path is an elbow — vertical-down, horizontal fork bar at midY, vertical-down to child avatar). Root row's connector has path = nil.
    - On iPhone, ALL rows render at alpha = 1.0 regardless of chain.integrity.verdict or implicatedNodeIDs (R7); connectors stay dashed-neutral; NO pulse halo on implicated rows.
    - The footer pill renders on EVERY verdict tier (including .clean with green tint); pill cornerRadius = bounds.height / 2 in layoutSubviews (Pitfall 8); icon uses UIImage.SymbolConfiguration(textStyle: .footnote) + adjustsImageSizeForAccessibilityContentSizeCategory = true (Pitfall 6); copy uses String(format: NSLocalizedString(<verdict format key>), chain.integrity.reason) from the LOCKED keys.
    - public func applySelectedRole(_ role: Role?) iterates rows and toggles a 2pt DS.Colors.primary outline (CAShapeLayer overlay per RESEARCH Pattern 4) on every row whose node.role == role; 120ms fade-in, 200ms fade-out; sticky.
    - public func rowViews(for role: Role) -> [ChainOfVouchesRowView] lets the VC compute scroll-anchor frames (used by Plan 05).
    - Container is isAccessibilityElement = false; **each row is an accessibility leaf with `.staticText` traits and a composed `accessibilityLabel` assigned BY THE CONTAINER inside `buildRows(from:)` (B2 fix — the prior plan implied a11y composition lived only in Task 2; this plan now explicitly wires the labels onto every row instance at construction time).**
    - Zero os_log / Logger. / print( calls anywhere (T-09-04 view-layer logging lock).
    - Cache private var currentChain: ChainOfTrust? (mirrors TrustGraphView.swift:126-128) so layoutSubviews can re-path connectors against the live canvas geometry.
  </behavior>
  <action>
    Create validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift as `public final class ChainOfVouchesView: UIView`. File-header doc-comment block (Contract / Tree-builder / Attribution table / Footer pill / Accessibility / PII discipline / D-15 supersedure note). Card chrome: backgroundColor = DS.Colors.surface, layer.cornerRadius = 16, layer.cornerCurve = .continuous. Subview hierarchy (vertical UIStackView mainStack, axis = .vertical, spacing = 0, with explicit per-pair custom spacing via setCustomSpacing(_:after:) to match UI-SPEC Component Geometry vertical gaps: eyebrow->title 4pt, title->subtitle 8pt, subtitle->tree 24pt, tree->footer 24pt; card outer insets lg=24pt top/bottom + md=16pt leading/trailing): (1) eyebrowLabel UILabel — DS.Typography.footnote regular, uppercase letter-spaced, .secondaryLabel, text from NSLocalizedString loads.detail.chain.9_1.card.eyebrow; (2) titleLabel DS.Typography.headline semibold, .label, NSLocalizedString loads.detail.chain.9_1.card.title; (3) subtitleLabel DS.Typography.body regular, .secondaryLabel, numberOfLines=2, .byTruncatingTail, NSLocalizedString loads.detail.chain.9_1.card.subtitle; (4) treeStack vertical UIStackView, axis = .vertical, spacing = DS.Spacing.md; (5) footerPill instance of the nested ChainOfVouchesFooterPillView. Define private struct AttributionParents with `static let canonical: [Role: Role] = [.broker: .shipper, .carrier: .broker, .dispatch: .carrier, .factoring: .carrier]` and static func attributionParent(for role: Role, presentRoles: Set<Role>) -> Role? that walks UP the canonical hierarchy: var current = role; while let parent = canonical[current] { if presentRoles.contains(parent) { return parent }; current = parent }; return nil. Define private func abbreviation(for role: Role) -> String returning LOCKED literals SHP/BRK/CAR/DSP/FCT. Define nested final class ChainOfVouchesRowView: UIView holding: avatar (RoleAvatarView .tree variant), roleNameLabel (DS.Typography.headline semibold, .label), attributionPrefixLabel (DS.Typography.footnote regular, .secondaryLabel, contentHugging .required horizontal), displayNameLabel (DS.Typography.body regular, .label, single line, .byTruncatingTail); horizontal stack [avatar | vstack[roleNameLabel | hstack[attributionPrefixLabel, displayNameLabel, spacer]]] with avatar->labelStack gap DS.Spacing.md; row owns connectorLayer: CAShapeLayer (lineWidth 1.5, dash [3,3], stroke DS.Colors.separator.cgColor) and highlightLayer: CAShapeLayer (lineWidth 2, stroke DS.Colors.primary.cgColor, opacity 0). Row's layoutSubviews recomputes connector path per RESEARCH Pattern 1: guard parentRow != nil AND parent.superview === self.superview; compute parentBottomCenter and myTopCenter; build a UIBezierPath with 3-segment elbow (move to parentBottomCenter; line to (parentBottomCenter.x, midY); line to (myTopCenter.x, midY); line to myTopCenter); assign connectorLayer.path = path.cgPath; insert into parent.superview.layer.insertSublayer(connectorLayer, at: 0) on first call. Row's layoutSubviews also recomputes highlight path as UIBezierPath(roundedRect: bounds.insetBy(dx: 4, dy: 4), cornerRadius: 12).cgPath. Row exposes weak var parentRow: ChainOfVouchesRowView?, var node: TrustNode?, func setHighlighted(_ on: Bool, animated: Bool) per RESEARCH Pattern 4 (CABasicAnimation on "opacity" with fillMode = .forwards, isRemovedOnCompletion = false, 120ms/200ms, easeOut/easeIn). Row is isUserInteractionEnabled = false (non-interactive per CONTEXT deferred). Define nested final class ChainOfVouchesFooterPillView: UIView holding: symbolView UIImageView (adjustsImageSizeForAccessibilityContentSizeCategory = true, contentMode = .scaleAspectFit), pillLabel UILabel (DS.Typography.footnote semibold, adjustsFontForContentSizeCategory = true); horizontal stack [symbol | label], padding DS.Spacing.md horizontal + DS.Spacing.sm vertical; layoutSubviews recomputes layer.cornerRadius = bounds.height / 2 (Pitfall 8). Footer pill exposes func apply(verdict: ChainIntegrity.Verdict, reason: String) that switches over verdict (mirror Phase 9 ChainIntegrityBannerView lines 170-202 BUT render .clean instead of hiding): .clean -> backgroundColor = .systemGreen.withAlphaComponent(0.18); pillLabel.textColor = .systemGreen; symbolView.tintColor = .systemGreen; symbolView.image = UIImage(systemName: "checkmark.seal.fill", withConfiguration: UIImage.SymbolConfiguration(textStyle: .footnote)); pillLabel.text = String(format: NSLocalizedString("loads.detail.chain.9_1.footer.clean.format", value: "✓ Chain intact\u{00A0}·\u{00A0}%@", comment: "..."), reason); .caution -> backgroundColor = DS.Colors.caution; pillLabel.textColor = .label; symbolView.tintColor = .label; SF Symbol "exclamationmark.triangle.fill"; key loads.detail.chain.9_1.footer.caution.format value "⚠ Risk flagged\u{00A0}·\u{00A0}%@"; .compromised -> backgroundColor = DS.Colors.destructive; pillLabel.textColor = .white; symbolView.tintColor = .white; SF Symbol "exclamationmark.octagon.fill"; key loads.detail.chain.9_1.footer.compromised.format value "⛔ Chain compromised\u{00A0}·\u{00A0}%@". Footer pill a11y: footerPill.isAccessibilityElement = true; footerPill.accessibilityTraits = .staticText; footerPill.accessibilityLabel = String(format: NSLocalizedString("loads.detail.chain.9_1.footer.a11y_label.format", value: "Chain integrity: %1$@. %2$@", comment: "Phase 9.1 footer pill VoiceOver label (UI-SPEC §Accessibility Contract line 298). %1$@ is the spoken verdict ('clean' / 'risk flagged' / 'compromised'); %2$@ is the chainIntegrity.reason text."), verdictSpoken(verdict), reason) — assigned inside apply(verdict:reason:) using a private static verdictSpoken(_:) helper. Public surface on ChainOfVouchesView: public init() programmatic; required init?(coder:) traps with fatalError; public func configure(chainOfTrust chain: ChainOfTrust) caches currentChain = chain, calls private buildRows(from chain: ChainOfTrust) -> [ChainOfVouchesRowView] per RESEARCH Pattern 2 lines 348-385 (tear down treeStack arrangedSubviews, walk roleOrder = [.shipper, .broker, .carrier, .dispatch, .factoring], filter+sort nodes by partyID, instantiate Row, compute attribution suffix via AttributionParents.attributionParent helper + NSLocalizedString loads.detail.chain.9_1.row.attribution.format value "VIA %@" formatted with abbreviation(for: parentRole) OR the root literal NSLocalizedString loads.detail.chain.9_1.row.attribution.root value "ROOT", set row.parentRow = rowsByRole[parentRole]?.first, append to treeStack). **(B2 fix — explicit a11y wiring inside buildRows):** After each `ChainOfVouchesRowView` is constructed and after `row.parentRow` is assigned, the container MUST set on that row instance: (i) `row.isAccessibilityElement = true`, (ii) `row.accessibilityTraits = .staticText`, (iii) `row.accessibilityLabel = composeRowLabel(node: row.node!, parentNode: row.parentRow?.node, stateCopy: ChainOfVouchesView.verificationStateSpoken(row.node!.verificationState))`. Define `private func composeRowLabel(node: TrustNode, parentNode: TrustNode?, stateCopy: String) -> String`: if `parentNode == nil` return `String(format: NSLocalizedString("loads.detail.chain.9_1.row.a11y_label.root.format", value: "%1$@ %2$@, root of the chain, %3$@", comment: "Phase 9.1 tree row VoiceOver label for the ROOT row — UI-SPEC §Accessibility Contract line 298. %1$@ = role displayName, %2$@ = party displayName, %3$@ = verification state spoken copy."), node.role.displayName, node.displayName, stateCopy)`; else return `String(format: NSLocalizedString("loads.detail.chain.9_1.row.a11y_label.nonRoot.format", value: "%1$@ %2$@, vouched by %3$@ %4$@, %5$@", comment: "Phase 9.1 tree row VoiceOver label for NON-ROOT rows — UI-SPEC §Accessibility Contract line 298. %1$@ = row role displayName, %2$@ = row party displayName, %3$@ = parent role displayName, %4$@ = parent party displayName, %5$@ = verification state spoken copy."), node.role.displayName, node.displayName, parentNode!.role.displayName, parentNode!.displayName, stateCopy)`. NO implicated-row suffix is appended: SPEC R6 references "UI-SPEC line 845" for the suffix, but UI-SPEC.md ends at line 407 and locks no implicated suffix; R7 forbids any row-level fraud signaling on iPhone (footer pill is the sole fraud signal), so omitting the suffix is the consistent interpretation across both spec layers. Define `internal static func verificationStateSpoken(_ state: VerificationState) -> String` returning the LOCKED UI-SPEC §Accessibility Contract vocabulary ("verified" / "verification pending" / "not verified" / "flagged"). After buildRows assigns all labels, the container's mainStack is finalized and `footerPill.apply(verdict: chain.integrity.verdict, reason: chain.integrity.reason)` is called. public func applySelectedRole(_ role: Role?) iterates treeStack.arrangedSubviews filtered to ChainOfVouchesRowView, calls setHighlighted(row.node?.role == role, animated: true). public func rowViews(for role: Role) -> [ChainOfVouchesRowView] returns matching rows. Override traitCollectionDidChange(_:) to call setNeedsLayout() on horizontalSizeClass change (Pitfall 1 re-attach trigger). Set isAccessibilityElement = false on the ChainOfVouchesView container. NO Logger / os_log / print. Add file to validationLedger target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild build -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20 &amp;&amp; echo '---PII LOGGING GATE---' &amp;&amp; if git grep -nE '(os_log|Logger\.|print\()' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift; then echo 'T-09.1-02 VIOLATION'; exit 1; else echo 'View-layer logging lock OK'; fi &amp;&amp; echo '---R12 INTERIM GATE (3 consumers expected after this plan)---' &amp;&amp; expected=$(printf 'validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift\nvalidationLedger/UI/Components/RoleAvatarView.swift\nvalidationLedger/UI/Components/VerificationBadgeView.swift' | sort); actual=$(git grep -l 'DS\.Colors\.Verification\.color' -- 'validationLedger/**/*.swift' | sort); diff <(echo "$actual") <(echo "$expected") &amp;&amp; echo 'R12 OK (expected 3 consumers — Plan 04 adds ChainOfVouchesView indirectly through RoleAvatarView; if the new file does not directly call DS.Colors.Verification.color this gate passes with only 2 entries, which is also acceptable — gate is satisfied if actual is a SUBSET of expected: re-check)' || { echo 'R12 OK alt-check: confirming actual is a subset of expected'; comm -23 <(echo "$actual") <(echo "$expected") | tee /tmp/r12_drift.txt; if [ -s /tmp/r12_drift.txt ]; then echo 'R12 DRIFT — unexpected consumer added'; exit 1; else echo 'R12 OK (no unexpected consumers; expected set may be looser than actual)'; fi; }</automated>
  </verify>
  <acceptance_criteria>
    - File validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift exists.
    - `grep -cE 'public final class ChainOfVouchesView: UIView' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns 1.
    - `grep -cE 'class ChainOfVouchesRowView' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (nested row class).
    - `grep -cE 'class ChainOfVouchesFooterPillView' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (nested footer pill class).
    - `grep -cE 'canonical: \[Role: Role\]' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (LOCKED attribution parent table).
    - `grep -cE 'RoleAvatarView\(.*variant: \.tree' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (composes Plan 02's primitive).
    - `grep -cE 'lineDashPattern' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (dashed connector — R3).
    - `grep -cE 'loads\.detail\.chain\.9_1\.(card|row|footer)\.' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 9 (the 7 Copywriting-Contract keys + 2 new a11y label format keys for root/nonRoot rows added by B2).
    - `grep -cE 'composeRowLabel\(node:[ ]*parentNode:[ ]*stateCopy:' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (B2 — composed-label helper signature).
    - `grep -cE 'row\.accessibilityLabel = composeRowLabel\(' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (B2 — wiring inside buildRows assigns the composed label onto each row instance).
    - `grep -cE 'row\.isAccessibilityElement = true' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (B2 — each row is an accessibility leaf, set by the container).
    - `grep -cE 'row\.accessibilityTraits = \.staticText' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (B2 — leaf trait per R6).
    - `grep -cE 'isAccessibilityElement = false' validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift` returns at least 1 (container not a leaf — R6).
    - PII view-layer logging gate exits with 'View-layer logging lock OK'.
    - R12 interim gate exits with either 'R12 OK' message (the new file should NOT introduce a new direct call site to DS.Colors.Verification.color; allowlist now includes ChainOfVouchesView.swift as the new third consumer, but if Task 1 keeps the indirection through RoleAvatarView, the actual set stays at 2 entries — both states are acceptable per the alt-check branch).
    - Build succeeds.
  </acceptance_criteria>
  <done>ChainOfVouchesView ships with card chrome, attribution-table-driven tree-builder, per-row dashed Y-fork connectors, the 3-tier footer pill (clean / caution / compromised), the leaf-element a11y posture **with composed `accessibilityLabel` assigned by the container onto every row instance inside `buildRows(from:)` (B2 wiring)**, and the LOCKED NSLocalizedString keys; view-layer logging lock holds; R12 expected-consumer set is within the allowlist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ChainOfVouchesViewTests.swift + ChainOfVouchesViewAccessibilityTests.swift — tree-builder + footer pill + a11y assertions</name>
  <read_first>
    - validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift (post-Task-1 — view under test)
    - validationLedgerTests/Loads/ChainOfTrustFactory.swift (post-Plan-03-Task-2 — factories must include VL-1001/1003/1005/1007/1009; EXTEND with vl1010_compromisedChain() and twoRoleShipperCarrierClean() if missing)
    - validationLedgerTests/Loads/TrustGraphViewLayoutTests.swift (analog — makeLaidOutView helper lines 58-81; per-fixture canvas setup)
    - validationLedgerTests/Loads/TrustGraphViewAccessibilityTests.swift (analog — isAccessibilityElement container assertion lines 55-59; per-element composed-label assertion lines 86-101)
    - validationLedger/Core/Load/ChainIntegrity.swift (verdict + reason field shape; FROZEN per R11)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (Group 5 ChainOfVouchesViewTests + ChainOfVouchesViewAccessibilityTests lines 543-580)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (Code Examples Example 5 lines 849-885 composed-label shapes for VL-1001/1005/1007/1009/1010)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (Wave 0 Requirements ChainOfVouchesViewTests + ChainOfVouchesViewAccessibilityTests lines)
  </read_first>
  <behavior>
    - Tree-builder (ChainOfVouchesViewTests): For each of the 5 representative fixtures (VL-1001/1005/1007/1009/1010), row count and ordering match the canonical depth-first walk; each row's attribution prefix label text matches the expected "VIA SHP|BRK|CAR" or "ROOT" literal from the role-hierarchy table.
    - VL-1009 multi-broker forced siblings (R9): Both broker rows in VL-1009 have attribution prefix label text "VIA SHP"; their adjacent indices in the tree stack confirm they're stack siblings (not horizontally arranged).
    - Sparse-chain fallback (Pitfall 7): A 2-role shipper+carrier chain (no broker) renders the carrier row with attribution "VIA SHP" (walks UP to find the present role).
    - Footer pill verdict rendering (R4): For .clean verdict footer pill backgroundColor alpha component is < 1.0 AND pillLabel.text contains "Chain intact"; .caution background == DS.Colors.caution, label contains "Risk flagged"; .compromised background == DS.Colors.destructive, label contains "Chain compromised". All 3 verdicts render the pill (the .clean short-circuit from Phase 9 is REVERSED).
    - D-15 supersedure on iPhone (R7): All rows render at alpha == 1.0 regardless of verdict; no pulse halo CALayer exists on any row.
    - Accessibility (ChainOfVouchesViewAccessibilityTests): Container isAccessibilityElement == false. Each row isAccessibilityElement == true with accessibilityTraits == .staticText (NOT .button) and composed label matching the locked template: root row = "<RoleDisplayName> <displayName>, root of the chain, <stateCopy>"; non-root = "<RoleDisplayName> <displayName>, vouched by <ParentRoleDisplayName> <ParentDisplayName>, <stateCopy>". Footer pill is a leaf with .staticText traits and label "Chain integrity: <verdictSpoken>. <reason>".
  </behavior>
  <action>
    First EXTEND validationLedgerTests/Loads/ChainOfTrustFactory.swift with the missing factories: vl1010_compromisedChain() (per RESEARCH Example 5 line 910 — fewer roles, compromised verdict + reason), twoRoleShipperCarrierClean() for the Pitfall-7 sparse-chain test. DO NOT modify any file under validationLedger/Core/Load/ (R11). Create validationLedgerTests/Loads/ChainOfVouchesViewTests.swift as `final class ChainOfVouchesViewTests: XCTestCase` with helper `private func makeTree(for chain: ChainOfTrust) -> ChainOfVouchesView` (instantiate, set bounds = CGRect(0, 0, 390, 600), configure, layoutIfNeeded). Tests: (1) test_VL1001_rowCount_andAttribution_perRole — 5 rows in order shipper/broker/carrier/dispatch/factoring; attribution labels ROOT / VIA SHP / VIA BRK / VIA CAR / VIA CAR. (2) test_VL1005_threeRoles_attributionChain — 3 rows ROOT / VIA SHP / VIA BRK. (3) test_VL1007_fourRoles_attributionChain — 4 rows ROOT / VIA SHP / VIA BRK / VIA CAR. (4) test_VL1009_multiBrokerSiblings_bothBrokersAttributeToShipper — 6 rows; the two broker rows are at adjacent indices in treeStack.arrangedSubviews; both broker rows' attribution prefix label text equals "VIA SHP". (5) test_sparseChain_shipperPlusCarrier_carrierAttributesToShipper_pitfall7 — pass twoRoleShipperCarrierClean(), locate the carrier row, assert attribution label is "VIA SHP" (NOT "VIA BRK"). (6) test_footerPill_renders_for_cleanVerdict_withGreenTint — pass a clean-verdict chain, assert footer pill is visible (NOT isHidden) AND its background color's alpha component is between 0.1 and 0.3 AND pillLabel text contains "Chain intact". (7) test_footerPill_renders_for_cautionVerdict — assert footer pill background == DS.Colors.caution; label contains "Risk flagged". (8) test_footerPill_renders_for_compromisedVerdict — assert footer pill background == DS.Colors.destructive; label contains "Chain compromised". (9) test_D15_supersedure_allRowsRenderAtFullAlpha_evenForCompromisedFixture — pass VL-1010 (compromised), iterate ChainOfVouchesRowView subviews, assert each row.alpha == 1.0. (10) test_D15_supersedure_noPulseHaloLayer_onAnyRow — iterate row.layer.sublayers, assert none has animation(forKey: "pulse") or layer.name == "pulse-halo". Create validationLedgerTests/Loads/ChainOfVouchesViewAccessibilityTests.swift as `final class ChainOfVouchesViewAccessibilityTests: XCTestCase`. Tests: (a) test_container_isAccessibilityElement_false for VL-1001. (b) test_eachRow_isAccessibilityLeaf_withStaticTextTrait — iterate rows, assert each isAccessibilityElement && accessibilityTraits == .staticText && !accessibilityTraits.contains(.button). (c) test_rootRow_a11yLabel_containsRootOfChain — for VL-1001's shipper row, assert label hasPrefix "Shipper " AND contains "root of the chain" AND ends with "verified". (d) test_nonRootRow_a11yLabel_containsVouchedBy — for VL-1001's broker row, assert label hasPrefix "Broker " AND contains "vouched by Shipper " AND ends with "verified". (e) test_footerPill_a11yLeaf — assert footer pill isAccessibilityElement == true && accessibilityTraits == .staticText && accessibilityLabel?.contains("Chain integrity") == true. (f) test_VL1009_bothBrokerRows_a11yLabels_carryPerNodeState — for the two VL-1009 broker rows, assert one ends with "flagged" and the other ends with "verified" (per-node a11y composition, NOT collapsed). Add both files to validationLedgerTests target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/ChainOfVouchesViewTests -only-testing:validationLedgerTests/ChainOfVouchesViewAccessibilityTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -30 &amp;&amp; echo '---R11 INTERIM GATE---' &amp;&amp; CHANGED=$(git diff --name-only HEAD~5..HEAD -- 'validationLedger/Core/Load/' 2>/dev/null); if [ -n "$CHANGED" ]; then echo "R11 VIOLATION: Core/Load changed:"; echo "$CHANGED"; exit 1; else echo 'R11 OK: zero Core/Load changes across recent commits'; fi</automated>
  </verify>
  <acceptance_criteria>
    - Files validationLedgerTests/Loads/ChainOfVouchesViewTests.swift and validationLedgerTests/Loads/ChainOfVouchesViewAccessibilityTests.swift exist.
    - `grep -cE 'func test_' validationLedgerTests/Loads/ChainOfVouchesViewTests.swift` returns at least 10 (the 10 tree-builder + footer pill + D-15 supersedure test methods).
    - `grep -cE 'func test_' validationLedgerTests/Loads/ChainOfVouchesViewAccessibilityTests.swift` returns at least 6 (the 6 a11y test methods).
    - Test invocation output contains "Test Suite 'ChainOfVouchesViewTests' passed" AND "Test Suite 'ChainOfVouchesViewAccessibilityTests' passed".
    - R11 interim gate exits with 'R11 OK: zero Core/Load changes across recent commits'.
  </acceptance_criteria>
  <done>Tree-builder R3/R9 + sparse-chain Pitfall-7 + footer pill R4 (all 3 verdicts) + D-15 supersedure R7 + leaf-element a11y R6 are all assertion-locked across the 5 representative fixtures + 1 sparse fixture; R11 data-model freeze holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ChainOfVouchesViewSnapshotTests.swift (10 attachments) + clean-verdict fixture reason re-author (UI-SPEC Q3)</name>
  <read_first>
    - validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift (post-Task-1 — view being snapshotted)
    - validationLedgerTests/Loads/ChainOfTrustFactory.swift (post-Task-2 — all 6 factories present)
    - validationLedgerTests/Loads/Snapshot/TrustGraphViewSnapshotTests.swift (matrix-helper precedent lines 25-78 — renderedView(...) + render(...) shape; Pitfall-8 traitOverrides.preferredContentSizeCategory BEFORE configure at line 63)
    - validationLedgerTests/Loads/Snapshot/ChainIntegrityBannerViewSnapshotTests.swift (per-verdict-tier snapshot precedent)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json, load-detail-VL-1003.json, load-detail-VL-1005.json, load-detail-VL-1007.json (clean-verdict fixtures whose chainIntegrity.reason is being text-only edited to the affirmative cadence "N vouches, no anonymous parties" per UI-SPEC Q3 — text-content change ONLY, no schema change, no key changes, NOT a R11 violation)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-PATTERNS.md (Group 6 ChainOfVouchesViewSnapshotTests lines 667-714)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-UI-SPEC.md (Snapshot Test Matrix Tree row — 5 fixtures × 2 iPhone widths × at least 3 verdicts; Copywriting Contract line 187 "fixture text-content change, NOT a data-model change")
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-RESEARCH.md (Open Question Q3 lines 998-1004 — fixture re-author scope is IN scope as small additive task)
    - .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-VALIDATION.md (Wave 0 Requirements ChainOfVouchesViewSnapshotTests line)
  </read_first>
  <behavior>
    - The snapshot suite attaches at least 10 distinct snapshots covering 5 fixtures × 2 iPhone widths (iPhone 15 portrait 390pt, iPhone SE 3 portrait 375pt) — additional verdict variants (caution + compromised at iPhone 15 portrait) bring the total to ≥12.
    - traitOverrides.preferredContentSizeCategory is applied BEFORE configure(chainOfTrust:) + layoutIfNeeded() (Pitfall 8 / WR-04 lock).
    - For the 4 clean fixtures (VL-1001/1003/1005/1007), chainIntegrity.reason text is the affirmative cadence "N vouches, no anonymous parties" so the rendered footer pill copy reads "✓ Chain intact · N vouches, no anonymous parties" exactly as the UI-SPEC reference photo cadence specifies.
    - The compromised-fixture snapshots (VL-1009, VL-1010) visually demonstrate R7 supersedure: all rows at full alpha, no dashed-flagged-edges, no pulse halo — the footer pill (red tinted) is the sole fraud signal.
    - The text-only fixture edits do NOT change any JSON keys, types, structure, or any other field — only the string value of chainIntegrity.reason on those 4 clean fixtures.
  </behavior>
  <action>
    Step A: Edit the 4 clean-verdict JSON fixtures under validationLedgerTests/Networking/Fixtures/ (load-detail-VL-1001.json, load-detail-VL-1003.json, load-detail-VL-1005.json, load-detail-VL-1007.json). For each file, locate the chainIntegrity object's `reason` field and CHANGE ITS VALUE to "N vouches, no anonymous parties" where N is the actual vouch count (number of edges) for that fixture: VL-1001 = "4 vouches, no anonymous parties" (5 nodes -> 4 edges); VL-1003 = "1 vouch, no anonymous parties" (2 nodes -> 1 edge); VL-1005 = "2 vouches, no anonymous parties"; VL-1007 = "3 vouches, no anonymous parties". DO NOT change any other field, key, or structural element in any fixture. DO NOT modify any file under validationLedger/Core/Load/ — this is a fixture text-content change only, which is explicitly NOT a data-model change per UI-SPEC line 187 and RESEARCH Q3. Step B: Create validationLedgerTests/Loads/Snapshot/ChainOfVouchesViewSnapshotTests.swift as `final class ChainOfVouchesViewSnapshotTests: XCTestCase`. Declare `private static let iPhone15PortraitWidth: CGFloat = 390`, `private static let iPhoneSE3PortraitWidth: CGFloat = 375`, `private static let canvasHeight: CGFloat = 600` (card + tree + footer pill). Implement `private func render(chain: ChainOfTrust, width: CGFloat, contentSize: UIContentSizeCategory = .large, name: String)`: instantiate ChainOfVouchesView(), set view.bounds = CGRect(x: 0, y: 0, width: width, height: Self.canvasHeight), view.overrideUserInterfaceStyle = .light, set view.traitOverrides.preferredContentSizeCategory = contentSize BEFORE configure (Pitfall 8), call view.configure(chainOfTrust: chain), view.layoutIfNeeded(), then UIKitSnapshot.attach(UIKitSnapshot.image(of: view, size: view.bounds.size), name: name, to: self). Generate test methods covering: 5 fixtures × 2 widths at .large content size (10 attachments, names "ChainOfVouches-VL-1001-iPhone15Portrait-DTLarge" etc.); additionally render VL-1009 (compromised) at iPhone 15 portrait AND VL-1010 (compromised, fewer roles) at iPhone 15 portrait AND one .caution-verdict fixture at iPhone 15 portrait (3 additional attachments, drives R4 + R7 verdict coverage). Use the ChainOfTrustFactory.vl1001_... / vl1003_... / vl1005_... / vl1007_... / vl1009_... / vl1010_... factories. Add file to validationLedgerTests target in pbxproj.
  </action>
  <verify>
    <automated>NODE_OPTIONS="--require /tmp/structured-clone-polyfill.js" xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' -only-testing:validationLedgerTests/ChainOfVouchesViewSnapshotTests -skip-testing:validationLedgerDeviceTests -parallel-testing-enabled NO 2>&amp;1 | tail -20 &amp;&amp; echo '---FIXTURE TEXT CHECK---' &amp;&amp; FIXTURE_COUNT=$(grep -E '"reason"[ ]*:[ ]*"[0-9]+ vouch(es)?, no anonymous parties"' validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json validationLedgerTests/Networking/Fixtures/load-detail-VL-1003.json validationLedgerTests/Networking/Fixtures/load-detail-VL-1005.json validationLedgerTests/Networking/Fixtures/load-detail-VL-1007.json | wc -l | tr -d ' '); if [ "$FIXTURE_COUNT" != "4" ]; then echo "FIXTURE REASON COUNT MISMATCH: expected 4, got $FIXTURE_COUNT"; exit 1; else echo 'Fixture reason text count OK (4)'; fi &amp;&amp; echo '---R11 INTERIM GATE---' &amp;&amp; CHANGED=$(git diff --name-only HEAD~5..HEAD -- 'validationLedger/Core/Load/' 2>/dev/null); if [ -n "$CHANGED" ]; then echo "R11 VIOLATION: Core/Load changed:"; echo "$CHANGED"; exit 1; else echo 'R11 OK: still zero Core/Load changes'; fi</automated>
  </verify>
  <acceptance_criteria>
    - File validationLedgerTests/Loads/Snapshot/ChainOfVouchesViewSnapshotTests.swift exists.
    - Test invocation output contains "Test Suite 'ChainOfVouchesViewSnapshotTests' passed".
    - At least 10 test methods covering 5 fixtures × 2 widths + 3 extra verdict-coverage snapshots.
    - The 4 clean fixtures (VL-1001/1003/1005/1007) have chainIntegrity.reason matching the affirmative cadence — the in-block fixture-text check confirms count == 4.
    - The render helper applies traitOverrides BEFORE configure (grep for `traitOverrides.preferredContentSizeCategory` line appearing before `configure(` line in the source file).
    - R11 interim gate exits with 'R11 OK: still zero Core/Load changes' — fixture JSON edits are NOT under Core/Load.
  </acceptance_criteria>
  <done>The Tree snapshot matrix populates the CI artifact bundle with ≥10 attachments across iPhone 15 + iPhone SE 3 widths and all 3 verdict tiers; the 4 clean fixtures' chainIntegrity.reason text now reads the affirmative-cadence "N vouches, no anonymous parties" per UI-SPEC Q3 (text-only fixture change, NOT a R11 violation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tree row composition -> log surfaces | Composed a11y labels include party displayName values (PII-class data); a future os_log or print of the label for debug leaks PII to crash/analytics surfaces. |
| ChainOfTrust ingest -> client-side trust derivation | A naive developer could attempt to recompute "is this chain compromised" by walking edges; SPEC R9 + R7 lock against this — only the server-supplied chain.integrity.verdict drives the footer pill, and the tree topology is purely organizational. |
| Fixture text changes -> R11 freeze | A misread of UI-SPEC could lead the executor to "improve" the JSON structure or rename fields; UI-SPEC line 187 + RESEARCH Q3 explicitly lock the change to chainIntegrity.reason text-content only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-02 | Information Disclosure (PII via log line) | ChainOfVouchesView.swift (composed a11y labels include party displayName) | mitigate | View-layer logging lock (Task 1 verify block: git grep -nE '(os_log|Logger\.|print\()' on ChainOfVouchesView.swift returns empty). CI wires the same gate per VALIDATION.md. |
| T-09.1-04 | Tampering (client-side trust derivation) | ChainOfVouchesView footer pill + tree-builder (NO derive* / compute* methods; consumes chain.integrity.verdict + chain.integrity.reason + node.role + node.displayName + node.verificationState VERBATIM) | mitigate | Footer pill apply(verdict:reason:) is a pure switch over the server-supplied verdict; tree-builder uses role-hierarchy table (UI-layer convention) and never reads edges[].fromPartyID for attribution (R9). The compromised-fixture snapshots verify the footer pill is the sole fraud signal. |
| T-09.1-01 | Tampering (color-ramp drift) | Footer pill verdict color mapping | mitigate | Footer pill consumes DS.Colors.caution + DS.Colors.destructive (existing DS tokens) and the .clean green tint is .systemGreen.withAlphaComponent(0.18) — NO switch over VerificationState; only over ChainIntegrity.Verdict. R12 interim gate (Task 1 verify) confirms ChainOfVouchesView is within the expected consumer set (the file may indirectly consume DS.Colors.Verification.color through RoleAvatarView OR may directly reference it; the gate's allowlist now includes the file). |
| T-09.1-04 | Tampering (R11 data-model freeze) | Fixture JSON edits in Task 3 (load-detail-VL-1001/1003/1005/1007.json) | mitigate | Edit is text-content-only on chainIntegrity.reason value; verified by R11 interim gate (Task 3 verify: git diff --name-only HEAD~5..HEAD -- 'validationLedger/Core/Load/' returns empty — fixtures live under validationLedgerTests/Networking/Fixtures/, NOT Core/Load). |
</threat_model>

<verification>
- Build: xcodebuild build exits 0 (Task 1 verify block).
- View-layer logging lock: zero os_log / Logger. / print( in ChainOfVouchesView.swift (Task 1 verify block).
- R12 interim gate: expected consumer allowlist now includes ChainOfVouchesView.swift alongside [VerificationBadgeView.swift, RoleAvatarView.swift]; ChainOfVouchesView may consume the helper INDIRECTLY through RoleAvatarView (Task 1 verify block).
- Tree-builder unit tests: 10 ChainOfVouchesViewTests + 6 ChainOfVouchesViewAccessibilityTests pass on iPhone 15 simulator (Task 2 verify block).
- R11 interim gate (Task 2 + Task 3 verify blocks): no Core/Load files modified.
- Snapshot suite: ≥10 ChainOfVouchesView attachments populate CI bundle (Task 3 verify block).
- Clean-fixture affirmative-cadence text: the 4 clean fixtures have chainIntegrity.reason matching "N vouch(es)?, no anonymous parties" exactly (Task 3 verify block).
</verification>

<success_criteria>
- [ ] validationLedger/Features/Loads/Detail/ChainOfVouchesView.swift exists; composes RoleAvatarView(.tree); uses LOCKED role-hierarchy parent table; renders per-row dashed Y-fork connectors via CAShapeLayer; folds the 3-tier footer pill (including the new green .clean tint).
- [ ] Tree-builder R3: depth-first canonical-role-order rows; siblings sorted by partyID alphabetical; one row per node.
- [ ] R9: VL-1009 both broker rows render as adjacent siblings under SHP with "VIA SHP" attribution.
- [ ] Pitfall 7: sparse-chain fallback works (shipper+carrier-only chain → carrier attributes "VIA SHP").
- [ ] R4: footer pill renders on every verdict tier (clean / caution / compromised); .clean uses green tint with affirmative-cadence reason.
- [ ] R7 (D-15 supersedure on iPhone): all rows at full alpha; no dashed-flagged-edge styling; no pulse halo.
- [ ] R6: container is_AccessibilityElement = false; each row is a leaf with .staticText traits and a composed `accessibilityLabel` ASSIGNED ONTO THE ROW INSTANCE by `buildRows(from:)` (root / non-root templates); footer pill is its own leaf with composed label.
- [ ] R11: zero files modified under validationLedger/Core/Load/; fixture text-only edits to chainIntegrity.reason on the 4 clean fixtures (NOT a data-model change per UI-SPEC line 187).
- [ ] R12: ChainOfVouchesView is within the expected allowlist for DS.Colors.Verification.color consumers — Task 1 may keep the indirection through RoleAvatarView (preferred) or add a direct call site; either is acceptable post-this-plan.
- [ ] All 3 test files pass on the iPhone 15 simulator lane (ChainOfVouchesViewTests + ChainOfVouchesViewAccessibilityTests + ChainOfVouchesViewSnapshotTests).
- [ ] Snapshot suite generates ≥10 attachments covering 5 fixtures × 2 widths + verdict-coverage extras.
- [ ] View-layer logging lock holds (no os_log / Logger. / print( in ChainOfVouchesView.swift).
- [ ] Project builds clean.
</success_criteria>

<output>
Create .planning/phases/09.1-chain-of-vouches-redesign-vertical-attribution-tree-everyone/09.1-04-SUMMARY.md when done.
</output>
</content>
</invoke>