---
phase: 07-load-domain-model-mock-contract
plan: 05
type: execute
wave: 4
depends_on: [07-02, 07-03]
files_modified:
  - validationLedgerTests/Networking/Fixtures/loads-list-shipper.json
  - validationLedgerTests/Networking/Fixtures/loads-list-broker.json
  - validationLedgerTests/Networking/Fixtures/loads-list-carrier.json
  - validationLedgerTests/Networking/Fixtures/loads-list-dispatch.json
  - validationLedgerTests/Networking/Fixtures/loads-list-factoring.json
  - validationLedgerTests/Networking/Fixtures/loads-list-empty.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1002.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1003.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1004.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1005.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1006.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1007.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1008.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1009.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1010.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1011.json
  - validationLedgerTests/Networking/Fixtures/load-detail-VL-1012.json
  - validationLedgerTests/Networking/Fixtures/load-action-success.json
  - validationLedgerTests/Networking/Fixtures/load-action-conflict-409.json
  - validationLedgerTests/Networking/Fixtures/load-action-validation-422.json
  - validationLedgerTests/Networking/Fixtures/load-action-server-error-500.json
  - validationLedgerTests/Load/LoadDomainDecodeTests.swift
  - validationLedgerTests/Load/ChainOfTrustDecodeTests.swift
  - validationLedgerTests/Load/LoadStateHistoryTests.swift
autonomous: true
requirements: [LOAD-01, LOAD-02]
must_haves:
  truths:
    - "Per-role load list fixtures exist for all 5 roles (D-11 shared-world: same VL- across role files)"
    - "12 named load detail fixtures exist covering every LoadStatus, all VerificationState values, all ChainIntegrity verdicts (D-12)"
    - "All 3 PROJECT.md fraud archetypes get a flagged-load fixture: VL-1009 double-broker, VL-1010 chameleon, VL-1011 factoring (D-13)"
    - "Action-outcome fixtures exist for success + 409 conflict + 422 validation + 500 server-error (D-14)"
    - "Every fixture decodes via APIClient.defaultDecoder() into LoadListEndpoint.Response / LoadDetailEndpoint.Response / LoadActionEndpoint.Response without error"
    - "Optional-field absence cases are present in at least 3 fixtures (Pitfall 5) — VL-1003 respondByAt:nil, VL-1006 chain.edges:[], VL-1005 target kycCompletedAt:nil"
    - "stateHistory ordering decodes correctly: events array preserves wire order with monotonic timestamps"
  artifacts:
    - path: validationLedgerTests/Networking/Fixtures/loads-list-broker.json
      provides: "broker's view of the shared 9-load list (VL-1001/1002/1003/1004/1005/1006/1008/1009/1010 per D-11)"
      contains: "VL-1009"
    - path: validationLedgerTests/Networking/Fixtures/load-detail-VL-1009.json
      provides: "double-/triple-brokered shipment fixture — flagged intermediary, compromised chain (D-13a)"
      contains: "compromised"
    - path: validationLedgerTests/Networking/Fixtures/load-detail-VL-1010.json
      provides: "chameleon-carrier fixture — flagged carrier, compromised chain (D-13b)"
    - path: validationLedgerTests/Networking/Fixtures/load-detail-VL-1011.json
      provides: "factoring-fraud fixture — flagged factoring on double-brokered chain (D-13c)"
    - path: validationLedgerTests/Networking/Fixtures/load-action-conflict-409.json
      provides: "409 stale-state error body (D-14)"
    - path: validationLedgerTests/Load/LoadDomainDecodeTests.swift
      provides: "per-fixture decode round-trip coverage across all 18 load fixtures"
      min_lines: 80
    - path: validationLedgerTests/Load/ChainOfTrustDecodeTests.swift
      provides: "3 fraud archetypes + clean + caution decode coverage"
      min_lines: 60
    - path: validationLedgerTests/Load/LoadStateHistoryTests.swift
      provides: "stateHistory ordering + LoadStatusEvent timestamp parsing"
      min_lines: 40
  key_links:
    - from: validationLedgerTests/Load/LoadDomainDecodeTests.swift
      to: FixtureLoader.loadFixture
      via: FixtureLoader resolves Networking/Fixtures bundle
      pattern: "FixtureLoader.loadFixture"
    - from: validationLedgerTests/Networking/Fixtures/load-detail-VL-1009.json
      to: ChainOfTrust.integrity.verdict == .compromised
      via: "chainOfTrust JSON path"
      pattern: "\"verdict\": ?\"compromised\""
---

<objective>
Author the full 22-fixture matrix per the D-12 named-load library and D-14 mechanical-failure set, register the new fixtures with the test target's Resources phase, and write the decode-round-trip test suites that validate every fixture decodes via `APIClient.defaultDecoder()` into the Plan 02 + Plan 03 Response types. The same fixtures are the "demo data" Phases 8-9 will render — quality matters beyond decode-passes.

Purpose: This plan delivers Roadmap SC #1 (every fixture decodes) and produces the named-load library that makes the v1.1 demo concrete. The three fraud-archetype fixtures (VL-1009, VL-1010, VL-1011) are not edge cases — they are the loads the trust graph exists to render. Plan 06 wires these fixtures into the `MockLoadFixtureRegistry` for the organic DEBUG tap-through.

Output: 22 JSON fixtures + 3 Swift Testing suites covering the decode contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md
@.planning/phases/07-load-domain-model-mock-contract/07-PATTERNS.md
@.planning/phases/07-load-domain-model-mock-contract/07-RESEARCH.md
@.planning/phases/07-load-domain-model-mock-contract/07-VALIDATION.md
@.planning/phases/07-load-domain-model-mock-contract/07-02-PLAN.md
@.planning/phases/07-load-domain-model-mock-contract/07-03-PLAN.md

<interfaces>
<!-- Plan 02 + Plan 03 outputs the fixtures must decode into. -->

From validationLedger/Core/Load/Load.swift (Plan 02):
- Load fields (wire keys under .convertFromSnakeCase): `id`, `reference_number`, `origin: LoadStop`, `destination: LoadStop`, `equipment`, `weight: Int`, `rate: Decimal` (encoded as a JSON number, e.g. `2450.00`), `pickup_at: Date` (ISO-8601 string), `deliver_at: Date`, `status: LoadStatus` (snake_case rawValue, e.g. `"in_transit"`), `state_history: [LoadStatusEvent]`, `respond_by_at: Date?`, `tender_eligibility: TenderEligibility?`
- LoadStop: `city, state, postal_code, country`
- TenderEligibility: `can_tender: Bool`, `disabled_reason: String?`

From validationLedger/Core/Load/LoadStatusEvent.swift (Plan 02):
- LoadStatusEvent: `status: LoadStatus`, `timestamp: Date`, `actor: LoadParty?`
- LoadParty: `party_id: String`, `role: String` (Role.rawValue), `display_name: String`

From validationLedger/Core/Load/ChainOfTrust.swift (Plan 02):
- ChainOfTrust: `nodes: [TrustNode]`, `edges: [TrustEdge]`, `integrity: ChainIntegrity` (wire key: `integrity` — bare, no CodingKey alias; matches the fixture content at Task 1 lines 184/204/215)
- TrustNode: `party_id`, `role`, `display_name`, `verification_state`, `kyc_completed_at: Date?`, `device_binding_status`, `usdot_number: String?`, `usdot_authority_status`, `prior_relationship_count: Int`
- TrustEdge: `edge_id`, `from_party_id`, `to_party_id`, `relationship_state`, `tender_ref: String?`
- ChainIntegrity: `verdict ("clean"|"caution"|"compromised")`, `reason`, `implicated_node_ids: [String]`, `implicated_edge_ids: [String]`

From validationLedger/Core/Load/VerificationState.swift (Plan 01) — fail-closed decoder:
- verified, pending, unverified, flagged (lowercase rawValue strings)

From validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift (Plan 03):
- Response: `{loads: [Load], next_cursor: String?}` (wire keys: `loads`, `next_cursor`)

From validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift (Plan 03):
- Response: `{load: Load, chain_of_trust: ChainOfTrust}` (wire keys: `load`, `chain_of_trust`)

From validationLedger/Core/Networking/Endpoints/LoadActionEndpoint.swift (Plan 03):
- Response: `{load: Load, chain_of_trust: ChainOfTrust}` (same shape as LoadDetail.Response — server returns updated Load + re-supplied chain)

From validationLedgerTests/Networking/FixtureLoader.swift (lines 11-27):
- `static func loadFixture(_ name: String) throws -> Data` — resolves `Bundle(for: …).url(forResource: name, withExtension: "json")` AND a subdirectory fallback at `Networking/Fixtures/{name}.json`.

From validationLedgerTests/Networking/Fixtures/kyc-status-rejected.json (richest existing nested fixture template):
- snake_case wire keys, ISO-8601 dates as strings (e.g. `"2026-04-22T12:01:00Z"`), explicit `null` for absent optionals inside arrays.

Wire-format conventions verified at validationLedger/Core/Networking/APIClient.swift:110-115:
- `JSONDecoder.keyDecodingStrategy = .convertFromSnakeCase`
- `JSONDecoder.dateDecodingStrategy = .iso8601`
- All Phase 7 fixtures and test decoders must use `APIClient.defaultDecoder()` (per Pattern 2 in PATTERNS.md "Shared Patterns").

From .planning/phases/07-load-domain-model-mock-contract/07-RESEARCH.md "Shared-World Fixture Roster (12 loads — D-10/11/12/13)":
- The 12 VL-IDs + their statuses, verification states, chain integrity verdicts, fraud archetypes, and which role lists each appears in. Exact table reproduced below for executor reference.

12 loads (VL-IDs + status + integrity):
- VL-1001 clean delivered (Anaheim→Atlanta) — all roles
- VL-1002 clean in_transit reefer (Salinas→Chicago) — shipper, broker, carrier, dispatch
- VL-1003 posted awaiting tender (Memphis→Dallas) — shipper, broker (respondByAt: null per Pitfall 5)
- VL-1004 active tender countdown (Phoenix→Denver, respondByAt: T+2h) — broker, carrier, dispatch
- VL-1005 expired tender reverted to posted (Newark→Charlotte; target carrier kycCompletedAt: null per Pitfall 5) — shipper, broker
- VL-1006 draft (Stockton→Portland) — shipper, broker (chain.edges: [] per Pitfall 5)
- VL-1007 dispatched (Detroit→Indianapolis) — carrier, dispatch
- VL-1008 posted, target carrier unverified (Houston→Tulsa) — broker (ACTION-07 driver)
- VL-1009 double-/triple-brokered in_transit (Long Beach→Phoenix; intermediary broker flagged, edge flagged) — broker, carrier, dispatch, factoring; integrity: compromised
- VL-1010 chameleon carrier accepted (Miami→Jacksonville; carrier flagged) — broker, carrier, dispatch; integrity: compromised
- VL-1011 factoring fraud on double-brokered (Atlanta→Birmingham, status: invoiced) — factoring; integrity: compromised
- VL-1012 funded delivered (San Diego→Tucson) — factoring

Per-role list contents (D-11 shared-world):
- loads-list-shipper.json: VL-1001, VL-1002, VL-1003, VL-1005, VL-1006
- loads-list-broker.json: VL-1001, VL-1002, VL-1003, VL-1004, VL-1005, VL-1006, VL-1008, VL-1009, VL-1010
- loads-list-carrier.json: VL-1001, VL-1002, VL-1004, VL-1007, VL-1009, VL-1010
- loads-list-dispatch.json: VL-1001, VL-1002, VL-1004, VL-1007, VL-1009, VL-1010
- loads-list-factoring.json: VL-1001, VL-1009, VL-1011, VL-1012
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 12 named load-detail JSON fixtures (D-12 + D-13)</name>
  <files>validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1002.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1003.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1004.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1005.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1006.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1007.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1008.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1009.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1010.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1011.json, validationLedgerTests/Networking/Fixtures/load-detail-VL-1012.json</files>
  <read_first>
    - validationLedgerTests/Networking/Fixtures/kyc-status-rejected.json (richest existing nested fixture — snake_case keys, ISO-8601 date strings, explicit `null` for absent in-array optionals)
    - validationLedger/Core/Load/Load.swift, Core/Load/ChainOfTrust.swift, Core/Load/LoadStatusEvent.swift, Core/Load/ChainIntegrity.swift, Core/Load/TenderEligibility (Plan 02 outputs — the decode targets every fixture must match)
    - validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift (Plan 03 — Response is `{load, chain_of_trust}`)
    - .planning/phases/07-load-domain-model-mock-contract/07-RESEARCH.md ("Shared-World Fixture Roster" — 12-load table + per-role list contents)
    - .planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md (D-10, D-11, D-12, D-13)
    - .planning/research/PITFALLS.md (Pitfall 5 — fixture matrix must include optional-field absence cases)
  </read_first>
  <action>
    Author 12 JSON fixture files, one per VL- load, at `validationLedgerTests/Networking/Fixtures/load-detail-VL-{id}.json`. Each fixture is a complete `LoadDetailEndpoint.Response` payload — top-level `{"load": {...}, "chain_of_trust": {...}}`.

    Wire-format rules (apply to every fixture):
    - **snake_case keys** for every field — `reference_number`, `state_history`, `chain_of_trust`, `verification_state`, `kyc_completed_at`, `usdot_number`, `usdot_authority_status`, `device_binding_status`, `prior_relationship_count`, `respond_by_at`, `tender_eligibility`, `can_tender`, `disabled_reason`, `next_cursor`, `party_id`, `from_party_id`, `to_party_id`, `edge_id`, `tender_ref`, `implicated_node_ids`, `implicated_edge_ids`, `display_name`.
    - **ISO-8601 dates as strings** — `"2026-04-15T08:30:00Z"`.
    - **rates as JSON numbers** — `2450.00` (not strings).
    - **explicit `null`** for absent optionals inside arrays or required-but-nullable wire positions; **omit the field entirely** for top-level absent optionals only if the existing v1.0 precedent supports it (otp-verify-success.json omits `kyc_status`). For Phase 7 fixtures, use explicit `null` consistently — clarity over precedent.

    Per-load content per the 12-load table in 07-RESEARCH.md "Shared-World Fixture Roster". Each fixture must include:
    1. `load.id` matching the filename VL-id (e.g. VL-1001).
    2. `load.reference_number` — a realistic short-form ref (e.g. "REF-1001-AA").
    3. `load.origin` + `load.destination` — the city pair from the table (LoadStop with city, state, postal_code, country).
    4. `load.equipment` — `"dry_van"` or `"reefer"` per the table.
    5. `load.weight` — integer pounds (e.g. 42500).
    6. `load.rate` — JSON number USD (e.g. 2450.00).
    7. `load.pickup_at` / `load.deliver_at` — ISO-8601 dates around 2026-04 timeframe.
    8. `load.status` — the LoadStatus rawValue from the table (snake_case wire form, e.g. `"in_transit"`, `"pod_captured"`).
    9. `load.state_history` — an array of `{status, timestamp, actor}` events in chronological order ending at the current status. At least 2 events on every non-draft load; the delivered/in_transit loads should have a full lifecycle history (draft→posted→tendered→accepted→dispatched→inTransit→delivered timeline where applicable).
    10. `load.respond_by_at` — ISO-8601 deadline ONLY for VL-1004 (active tender T+2h); `null` for every other load.
    11. `load.tender_eligibility` — for VL-1008 explicitly `{"can_tender": false, "disabled_reason": "Carrier identity not yet verified — Phase 5 KYC outstanding"}` (drives ACTION-07 hard-disable). For other loads either `{"can_tender": true, "disabled_reason": null}` OR `null` (test the Pitfall 5 absent-field case on at least 2 loads).
    12. `chain_of_trust.nodes` — TrustNode array; one per party in the load's chain (typically 3-5 parties: shipper, broker, carrier, dispatch, factoring). Each TrustNode carries: party_id, role, display_name, verification_state, kyc_completed_at, device_binding_status, usdot_number, usdot_authority_status, prior_relationship_count. For loads with a flagged party, set that node's `verification_state` to `"flagged"` (NOT `"unverified"` — the distinction is semantic per D-09).
    13. `chain_of_trust.edges` — TrustEdge array describing the handoff relationships (shipper→broker, broker→carrier, etc.). For VL-1009 the broker→carrier edge is flagged. For VL-1006 the edges array is `[]` (Pitfall 5 — draft loads have no chain yet).
    14. `chain_of_trust.integrity` — ChainIntegrity `{verdict, reason, implicated_node_ids, implicated_edge_ids}`. For VL-1009 / VL-1010 / VL-1011 verdict = `"compromised"` with a specific `reason` per the table (07-RESEARCH.md). For VL-1001-1008 + VL-1012 verdict = `"clean"` with `reason: ""` (or a short non-empty reason — the field is required). If you choose to include a `caution`-verdict load, ride it on VL-1005 (the expired-tender load) with `reason` describing pre-tender caution (planner discretion — 07-CONTEXT.md "Claude's Discretion").

    Special-case rules (NOT optional):
    - **VL-1003**: `respond_by_at: null` AND `tender_eligibility: null` — exercises top-level optional absence (Pitfall 5).
    - **VL-1005**: target carrier's TrustNode has `kyc_completed_at: null` — exercises in-array optional absence (Pitfall 5).
    - **VL-1006**: `chain_of_trust.edges: []` — exercises empty-array-not-missing-field (Pitfall 5).
    - **VL-1008**: `tender_eligibility = {"can_tender": false, "disabled_reason": "…"}` AND the target carrier TrustNode has `verification_state: "unverified"` — drives ACTION-07.
    - **VL-1009**: At least one node has `verification_state: "flagged"`, at least one edge has `relationship_state: "flagged"`, `integrity.verdict: "compromised"`, `integrity.implicated_node_ids` is non-empty, `integrity.implicated_edge_ids` is non-empty. The reason text contains the substring "broker" or "re-tender" (this is what the Phase 9 graph banner renders).
    - **VL-1010**: A carrier-role TrustNode has `verification_state: "flagged"`, `integrity.verdict: "compromised"`, `integrity.reason` contains "USDOT" or "authority" or "revocation".
    - **VL-1011**: A factoring-role TrustNode has `verification_state: "flagged"`, `integrity.verdict: "compromised"`, `integrity.reason` contains "factoring" or "invoice" or "clawback".

    Demo-data quality: party `display_name` fields read realistically — "Acme Logistics LLC", "FreightWise Brokerage", "Sunbelt Carriers Inc.", etc. (D-10: coherent narrative, not minimal mechanical coverage). The same display_name and party_id MUST be used for the same logical party across multiple VL- fixtures (D-11 shared world) — e.g. if "FreightWise Brokerage" appears in VL-1001 and VL-1003, both fixtures use party_id `"party-broker-freightwise"`.

    Add ALL 12 files to the test target's Resources build phase in Xcode (`validationLedger.xcodeproj/project.pbxproj`). The executor must update the project file so the fixtures are available to FixtureLoader at runtime. The simplest path: open Xcode, drag the new files into the validationLedgerTests group with target membership checked, OR edit pbxproj directly to add the file references and resource-phase entries — pick whichever is reliable. If pbxproj edit is error-prone, use Xcode UI and commit the resulting project.pbxproj diff. Verify by running the test command at the end: a `FixtureLoader.loadFixture` call must succeed.
  </action>
  <verify>
    <automated>ls validationLedgerTests/Networking/Fixtures/load-detail-VL-*.json | wc -l | tr -d ' '; for f in validationLedgerTests/Networking/Fixtures/load-detail-VL-{1001,1002,1003,1004,1005,1006,1007,1008,1009,1010,1011,1012}.json; do python3 -m json.tool "$f" > /dev/null && echo "OK $f" || echo "BAD $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: exactly 12 files matching `validationLedgerTests/Networking/Fixtures/load-detail-VL-{1001,1002,1003,1004,1005,1006,1007,1008,1009,1010,1011,1012}.json` (`ls … | wc -l` returns 12)
    - Each file is valid JSON: `python3 -m json.tool {file} > /dev/null` exits 0 for every fixture
    - Each fixture has top-level keys `load` and `chain_of_trust`
    - VL-1003 fixture: `respond_by_at` is the JSON literal `null` (verify: `grep -F '"respond_by_at": null' validationLedgerTests/Networking/Fixtures/load-detail-VL-1003.json`)
    - VL-1006 fixture: `chain_of_trust.edges` is `[]` (verify: `python3 -c "import json; d=json.load(open('validationLedgerTests/Networking/Fixtures/load-detail-VL-1006.json')); assert d['chain_of_trust']['edges']==[], d['chain_of_trust']['edges']"`)
    - VL-1008 fixture: `load.tender_eligibility.can_tender == false` (verify with python -c)
    - VL-1009 fixture: `chain_of_trust.integrity.verdict == "compromised"` AND at least one node has `verification_state == "flagged"` AND at least one edge has `relationship_state == "flagged"` (verify with python -c)
    - VL-1010 fixture: `chain_of_trust.integrity.verdict == "compromised"` AND at least one carrier-role node has `verification_state == "flagged"`
    - VL-1011 fixture: `chain_of_trust.integrity.verdict == "compromised"` AND at least one factoring-role node has `verification_state == "flagged"`
    - All 12 fixtures have at least one entry in `load.state_history`
    - All 12 fixtures are added to the test target's Resources build phase (`grep -c "load-detail-VL-" validationLedger.xcodeproj/project.pbxproj` returns at least 24 — each file appears in at least 2 pbxproj entries: file reference + build phase membership)
  </acceptance_criteria>
  <done>12 valid JSON fixtures exist with the special-case rules satisfied; all are registered in the test target Resources build phase; `python3 -m json.tool` validates each.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 5 per-role list fixtures + empty + 4 action-outcome fixtures (D-11, D-14)</name>
  <files>validationLedgerTests/Networking/Fixtures/loads-list-shipper.json, validationLedgerTests/Networking/Fixtures/loads-list-broker.json, validationLedgerTests/Networking/Fixtures/loads-list-carrier.json, validationLedgerTests/Networking/Fixtures/loads-list-dispatch.json, validationLedgerTests/Networking/Fixtures/loads-list-factoring.json, validationLedgerTests/Networking/Fixtures/loads-list-empty.json, validationLedgerTests/Networking/Fixtures/load-action-success.json, validationLedgerTests/Networking/Fixtures/load-action-conflict-409.json, validationLedgerTests/Networking/Fixtures/load-action-validation-422.json, validationLedgerTests/Networking/Fixtures/load-action-server-error-500.json</files>
  <read_first>
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json (Task 1 output — pull the same `load` payload for inclusion in the per-role list fixtures)
    - validationLedgerTests/Networking/Fixtures/kyc-submit-failure.json (existing error-body fixture template)
    - validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift (Plan 03 — Response is `{loads: [Load], next_cursor: String?}`)
    - validationLedger/Core/Networking/Endpoints/LoadActionEndpoint.swift (Plan 03 — success Response is `{load, chain_of_trust}`; the 4xx fixtures are error-body JSON the APIClient surfaces via NetworkError.httpError)
    - .planning/phases/07-load-domain-model-mock-contract/07-RESEARCH.md ("Shared-World Fixture Roster" + "Mechanical Failure Fixture HTTP Codes + Mock Threading")
    - .planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md (D-11, D-14)
  </read_first>
  <action>
    File set A — per-role list fixtures (D-11 shared-world derivation):

    Each `loads-list-{role}.json` is a LoadListEndpoint.Response: `{"loads": [Load, Load, …], "next_cursor": null}`. The `loads` array contains the FULL Load payloads (not just IDs) from the 12-load library, per the role-membership table:

    - `loads-list-shipper.json`: VL-1001, VL-1002, VL-1003, VL-1005, VL-1006 (5 loads)
    - `loads-list-broker.json`: VL-1001, VL-1002, VL-1003, VL-1004, VL-1005, VL-1006, VL-1008, VL-1009, VL-1010 (9 loads)
    - `loads-list-carrier.json`: VL-1001, VL-1002, VL-1004, VL-1007, VL-1009, VL-1010 (6 loads)
    - `loads-list-dispatch.json`: VL-1001, VL-1002, VL-1004, VL-1007, VL-1009, VL-1010 (6 loads — identical contents to carrier; D-06 collapses the roles but the fixture is still its own file for the role-in-path D-15 routing)
    - `loads-list-factoring.json`: VL-1001, VL-1009, VL-1011, VL-1012 (4 loads)

    For each role-list fixture, copy the `load` object from each VL- detail fixture (Task 1 output) into the `loads: [...]` array. The Load payload across the shared-world fixtures MUST be byte-identical for the same VL- (D-11) — a tool-script approach: read each load-detail-VL-*.json, extract `.load`, write the appropriate roles' list files concatenating those load objects. The chain_of_trust is NOT included in the list fixtures (it's only on the detail endpoint).

    `next_cursor` is `null` on every role-list fixture (v1.1 fixtures always return one page per 07-RESEARCH.md "Pagination").

    `loads-list-empty.json` — the empty-state mechanical fixture: `{"loads": [], "next_cursor": null}`.

    File set B — action-outcome fixtures (D-14 + 07-RESEARCH.md "Mechanical Failure Fixture HTTP Codes"):

    - `load-action-success.json`: a full LoadActionEndpoint.Response — `{"load": {…updated load…}, "chain_of_trust": {…re-supplied chain…}}`. Use VL-1004 as the source load (the active-tender load), with its status post-action — e.g. accepted (status: "accepted", state_history appended with an `{status: "accepted", timestamp: …, actor: carrier-party}` event). The chain_of_trust is the VL-1004 chain unchanged.
    - `load-action-conflict-409.json`: server error body for HTTP 409 — `{"error_code": "load.stale_state", "message": "This load has already been actioned by another party"}`. (NOT a Response — this is the body returned alongside HTTP status 409; tests register it with `registerForcedFailure(.http(statusCode: 409, body:))`.)
    - `load-action-validation-422.json`: `{"error_code": "load.invalid_transition", "message": "Cannot accept a load that is not currently tendered"}`.
    - `load-action-server-error-500.json`: `{"error_code": "server.internal", "message": "Service temporarily unavailable"}`.

    Add ALL 10 files to the test target's Resources build phase (same approach as Task 1 — Xcode UI drag with target membership, or pbxproj edit). Verify by `python3 -m json.tool` on each.
  </action>
  <verify>
    <automated>for f in validationLedgerTests/Networking/Fixtures/loads-list-{shipper,broker,carrier,dispatch,factoring,empty}.json validationLedgerTests/Networking/Fixtures/load-action-{success,conflict-409,validation-422,server-error-500}.json; do python3 -m json.tool "$f" > /dev/null && echo "OK $f" || echo "BAD $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: 5 per-role list files + `loads-list-empty.json` + 4 action-outcome files (10 total)
    - Each file is valid JSON
    - `loads-list-broker.json` contains 9 loads (verify: `python3 -c "import json; assert len(json.load(open('validationLedgerTests/Networking/Fixtures/loads-list-broker.json'))['loads'])==9"`)
    - `loads-list-broker.json` contains VL-1009 (the double-broker load) — `grep -c '"id": "VL-1009"' validationLedgerTests/Networking/Fixtures/loads-list-broker.json` returns at least 1
    - `loads-list-empty.json` has `loads: []` (verify with python -c)
    - All role-list fixtures have `next_cursor: null`
    - The `load` object for VL-1009 in `loads-list-broker.json` is byte-identical to the `.load` object in `load-detail-VL-1009.json` (D-11 shared-world consistency) — verify with python: `import json; b=json.load(open('loads-list-broker.json'))['loads']; d=json.load(open('load-detail-VL-1009.json'))['load']; assert any(l==d for l in b)`
    - `load-action-conflict-409.json` contains `"error_code": "load.stale_state"` (verify with grep)
    - `load-action-validation-422.json` contains `"error_code": "load.invalid_transition"`
    - All 10 fixtures are in the test target's Resources build phase (`grep -c "loads-list-\|load-action-" validationLedger.xcodeproj/project.pbxproj` returns at least 20)
  </acceptance_criteria>
  <done>10 list + action JSON fixtures exist, validate as JSON, satisfy D-11 shared-world consistency, and are registered in the test target Resources phase.</done>
</task>

<task type="auto">
  <name>Task 3: LoadDomainDecodeTests + ChainOfTrustDecodeTests + LoadStateHistoryTests</name>
  <files>validationLedgerTests/Load/LoadDomainDecodeTests.swift, validationLedgerTests/Load/ChainOfTrustDecodeTests.swift, validationLedgerTests/Load/LoadStateHistoryTests.swift</files>
  <read_first>
    - validationLedgerTests/Networking/DeviceChallengeEndpointTests.swift (fixture-decode test template — FixtureLoader + APIClient.defaultDecoder())
    - validationLedgerTests/Networking/FixtureLoader.swift (lines 11-27 — subdirectory fallback)
    - validationLedger/Core/Networking/APIClient.swift (lines 110-115 — defaultDecoder strategy)
    - validationLedger/Core/Networking/Endpoints/LoadDetailEndpoint.swift, LoadListEndpoint.swift, LoadActionEndpoint.swift (Plan 03 outputs — Response types the fixtures decode into)
    - validationLedger/Core/Load/Load.swift, LoadStatusEvent.swift, ChainOfTrust.swift, ChainIntegrity.swift (Plan 02 outputs)
    - All Task 1 + Task 2 fixture files (the source-of-truth this plan tests)
    - .planning/phases/07-load-domain-model-mock-contract/07-PATTERNS.md ("Test files" + "Fixture-loading via FixtureLoader" sections)
    - .planning/phases/07-load-domain-model-mock-contract/07-VALIDATION.md ("Wave 0 Requirements")
  </read_first>
  <action>
    Create 3 Swift Testing suites under `validationLedgerTests/Load/`. All three use `import Testing; import Foundation; @testable import validationLedger` and plain `@Suite` (no `.serialized` — none touch MockURLProtocol).

    File 1 — `LoadDomainDecodeTests.swift`. `@Suite("LoadDomainDecodeTests — every load + role + action fixture decodes via APIClient.defaultDecoder()")`.

    Test 1 — `@Test("Every load-detail-VL-*.json decodes into LoadDetailEndpoint.Response")` (parameterized via `@Test(arguments: …)` or a for-loop inside the body): iterate over `["VL-1001", "VL-1002", …, "VL-1012"]`; for each, `let data = try FixtureLoader.loadFixture("load-detail-\(id)")`; `let response = try APIClient.defaultDecoder().decode(LoadDetailEndpoint.Response.self, from: data)`; `#expect(response.load.id == id)`. Verifies SC #1 for all 12 detail fixtures.

    Test 2 — `@Test("Every loads-list-{role}.json decodes into LoadListEndpoint.Response with the expected count")`: parameterize over `[("shipper", 5), ("broker", 9), ("carrier", 6), ("dispatch", 6), ("factoring", 4)]`. For each, decode the fixture and assert `response.loads.count == expectedCount`. Plus `loads-list-empty.json` decoding to `response.loads.isEmpty`.

    Test 3 — `@Test("load-action-success.json decodes into LoadActionEndpoint.Response")`: decode the success fixture and assert `response.load.id` is non-empty and `response.chainOfTrust.nodes` is non-empty.

    Test 4 — `@Test("Action error-body fixtures parse as JSON (validate body shape; HTTP-status delivery is the mock layer's job)")`: for each of the 3 error fixtures (409 / 422 / 500), `let data = try FixtureLoader.loadFixture(…)`, `let json = try JSONSerialization.jsonObject(with: data) as? [String: Any]`, `#expect(json?["error_code"] is String)`, `#expect(json?["message"] is String)`. (NOT decoded into a typed error struct — the v1.0 NetworkError doesn't yet have a typed error-body type; the fixture exists for the Plan 04 forced-failure path to deliver as the body.)

    Test 5 — `@Test("LoadListEndpoint.Response.nextCursor is nil for every v1.1 fixture")`: across all 5 role lists + empty fixture, assert `response.nextCursor == nil`. Documents the D-16 paginated-envelope contract: fixtures always return one page in v1.1.

    File 2 — `ChainOfTrustDecodeTests.swift`. `@Suite("ChainOfTrustDecodeTests — fraud archetypes + clean decode (D-08, D-13)")`.

    Test 1 — `@Test("VL-1009 decodes a double-brokered chain with flagged intermediary node + flagged edge + compromised verdict")`: decode VL-1009 detail; `#expect(response.chainOfTrust.integrity.verdict == .compromised)`; `#expect(response.chainOfTrust.nodes.contains { $0.verificationState == .flagged })`; `#expect(response.chainOfTrust.edges.contains { $0.relationshipState == .flagged })`; `#expect(!response.chainOfTrust.integrity.implicatedNodeIDs.isEmpty)`; `#expect(!response.chainOfTrust.integrity.implicatedEdgeIDs.isEmpty)`.

    Test 2 — `@Test("VL-1010 decodes a chameleon-carrier chain with carrier-role flagged node + compromised verdict")`: decode VL-1010; `#expect(response.chainOfTrust.nodes.contains { $0.role == .carrier && $0.verificationState == .flagged })`; `#expect(response.chainOfTrust.integrity.verdict == .compromised)`.

    Test 3 — `@Test("VL-1011 decodes a factoring-fraud chain with factoring-role flagged node + compromised verdict")`: decode VL-1011; `#expect(response.chainOfTrust.nodes.contains { $0.role == .factoring && $0.verificationState == .flagged })`; `#expect(response.chainOfTrust.integrity.verdict == .compromised)`.

    Test 4 — `@Test("VL-1001 decodes a clean delivered chain — verdict .clean, no flagged nodes/edges")`: decode VL-1001; `#expect(response.chainOfTrust.integrity.verdict == .clean)`; `#expect(!response.chainOfTrust.nodes.contains { $0.verificationState == .flagged })`; `#expect(!response.chainOfTrust.edges.contains { $0.relationshipState == .flagged })`.

    Test 5 — `@Test("VL-1006 decodes a draft load with empty chain.edges (Pitfall 5 — empty-array-not-missing-field)")`: decode VL-1006; `#expect(response.chainOfTrust.edges.isEmpty)`.

    Test 6 — `@Test("VL-1005 decodes a TrustNode with kyc_completed_at: null (Pitfall 5 — in-array optional absence)")`: decode VL-1005; `#expect(response.chainOfTrust.nodes.contains { $0.kycCompletedAt == nil })`.

    Test 7 — `@Test("VL-1008 decodes target-carrier verification_state: unverified (drives ACTION-07 hard-disable)")`: decode VL-1008; `#expect(response.chainOfTrust.nodes.contains { $0.role == .carrier && $0.verificationState == .unverified })`; `#expect(response.load.tenderEligibility?.canTender == false)`.

    File 3 — `LoadStateHistoryTests.swift`. `@Suite("LoadStateHistoryTests — Load.stateHistory ordering + LoadStatusEvent decode (D-02)")`.

    Test 1 — `@Test("VL-1001 stateHistory decodes in monotonic timestamp order ending at .delivered")`: decode VL-1001 detail; assert `response.load.stateHistory.last?.status == .delivered`; assert `zip(stateHistory, stateHistory.dropFirst()).allSatisfy { $0.timestamp <= $1.timestamp }` (monotonic non-decreasing). This is the LOAD-06 timeline source-of-truth.

    Test 2 — `@Test("VL-1002 stateHistory contains the canonical in-transit progression (draft|posted)→tendered→accepted→dispatched→inTransit at minimum")`: decode VL-1002 detail; assert the stateHistory's status-sequence ends with `.inTransit` and contains all of [.posted, .tendered, .accepted, .dispatched, .inTransit] in order (allow gaps; just verify each appears once at the correct relative position).

    Test 3 — `@Test("LoadStatusEvent.actor decodes as nil when wire field is null (Pitfall 5 — in-array optional absence)")`: decode any load whose state_history contains at least one event with `"actor": null`. (Author this case in Task 1 if no fixture has it — VL-1006 draft is a candidate: the initial "draft" event has no actor since it's pre-counterparty.) Assert `response.load.stateHistory.first { $0.actor == nil } != nil` for that load.

    Test 4 — `@Test("stateHistory events preserve declared wire order (decoder does not re-sort)")`: hand-author an in-test JSON literal with stateHistory events out of monotonic timestamp order; decode; assert `response.load.stateHistory[0].timestamp > response.load.stateHistory[1].timestamp` (out-of-order is faithfully preserved). This documents: the client renders in array order; server is responsible for sorting.

    For all 3 test files: each test reads via `try FixtureLoader.loadFixture("load-detail-VL-…")` then decodes via `try APIClient.defaultDecoder().decode(LoadDetailEndpoint.Response.self, from: data)`. NEVER use a fresh `JSONDecoder()` — Pattern 2 in PATTERNS.md mandates `APIClient.defaultDecoder()` so any decoder drift surfaces in tests.

    Note for executor: if `APIClient.defaultDecoder()` is not exposed publicly, use `JSONDecoder()` with `keyDecodingStrategy = .convertFromSnakeCase` and `dateDecodingStrategy = .iso8601` — but grep `APIClient.swift` first; per PATTERNS.md it IS exposed as `public static func defaultDecoder()`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerTests/Load/LoadDomainDecodeTests -only-testing:validationLedgerTests/Load/ChainOfTrustDecodeTests -only-testing:validationLedgerTests/Load/LoadStateHistoryTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `validationLedgerTests/Load/LoadDomainDecodeTests.swift`, `validationLedgerTests/Load/ChainOfTrustDecodeTests.swift`, `validationLedgerTests/Load/LoadStateHistoryTests.swift`
    - Source: LoadDomainDecodeTests has at least 5 `@Test` methods iterating over the 12 load-detail fixtures, the 5 per-role list fixtures + empty, the success fixture, the 3 error fixtures
    - Source: ChainOfTrustDecodeTests has at least 7 `@Test` methods covering VL-1009/1010/1011 + clean (VL-1001) + Pitfall 5 cases (VL-1005 kyc null, VL-1006 empty edges, VL-1008 unverified)
    - Source: LoadStateHistoryTests has at least 4 `@Test` methods covering monotonic ordering, canonical progression, null actor, and wire-order preservation
    - Behavior: every load-detail-VL-*.json decodes via `APIClient.defaultDecoder().decode(LoadDetailEndpoint.Response.self, …)` without throwing (12/12)
    - Behavior: every loads-list-*.json decodes via `APIClient.defaultDecoder().decode(LoadListEndpoint.Response.self, …)` without throwing (6/6 including empty)
    - Behavior: VL-1009 decodes with `integrity.verdict == .compromised` AND a flagged node AND a flagged edge
    - Behavior: VL-1008 decodes with `load.tenderEligibility?.canTender == false`
    - Behavior: VL-1001 stateHistory ends with `.delivered` and timestamps are monotonic non-decreasing
    - Test command exits 0: scoped run of all 3 suites passes (~16 tests total)
  </acceptance_criteria>
  <done>3 decode-test suites pass; Roadmap SC #1 (every fixture decodes) is satisfied for the detail + list endpoints + the success action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON fixtures (git-tracked) → Decodable types | The fixtures are the contract; any "permissive" decoder change shows up in PR diff. Test fixtures defining trust state are the inverse of a fraud attack surface — they should be HARDER to read as benign than to read as malicious |
| Fixture file → test target Resources phase | A fixture without target membership fails silently in the test bundle with FixtureLoader returning a "file not found" error — surface this via the decode tests, never silently skip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-22 | Tampering | malicious fixture crafts permissive trust | mitigate | All fixtures are git-tracked and reviewed; permissive decoder changes surface in PR diff. Fixture authoring follows the named-load roster — deviations require updating 07-RESEARCH.md "Shared-World Fixture Roster" first. |
| T-07-23 | Spoofing | flagged-node fixture with weak verification_state="unverified" instead of "flagged" | mitigate | VL-1009/1010/1011 acceptance criteria mandate `verification_state == "flagged"` on the specific archetype party (Task 1 grep checks). The D-09 semantic distinction (unverified=not-yet-verified vs flagged=actively-suspicious) is preserved. |
| T-07-24 | Repudiation | stateHistory ordering tampered (e.g. delivered before posted) | mitigate | LoadStateHistoryTests Test 1 asserts monotonic timestamps; the server is the source of truth; client renders in array order (Test 4). Server-side enforcement of ordering is post-v1.1 (real backend); v1.1 trusts the fixture. |
| T-07-25 | Information Disclosure | PII in fixtures | accept | Fixtures use realistic-but-fake business names (Acme Logistics LLC, etc.) and public-record-equivalent fields (USDOT numbers, KYC completion dates). No SSN / DOB / document images. Documented in Task 1 demo-data-quality rule. |
| T-07-26 | Tampering | fixture file present on disk but missing from test target Resources phase | mitigate | Task 1 + Task 2 acceptance criteria include the pbxproj grep check ensuring the files are in the build phase; Task 3 tests would throw "fixture not found" if the membership were missing — surfaces immediately, not silently. |
</threat_model>

<verification>
- 22 JSON fixtures exist in `validationLedgerTests/Networking/Fixtures/` (12 detail + 5 role-list + 1 empty + 4 action-outcome).
- All 22 are valid JSON (`python3 -m json.tool` passes on each).
- All 22 are registered in the test target's Resources build phase (`project.pbxproj` contains at least 22 file references and at least 22 build-file entries — grep checks across Tasks 1 + 2).
- 3 new test suites pass on the simulator lane (scoped `xcodebuild test -only-testing` command exits 0).
- D-11 shared-world consistency: VL-1009's `load` object is byte-identical across `load-detail-VL-1009.json` and `loads-list-broker.json` (Task 2 acceptance criterion).
- All 3 D-13 fraud archetypes are present and recognizable from the test assertions (Task 3 Test 1-3 of ChainOfTrustDecodeTests).
- Pitfall 5 (optional-field absence) is exercised by VL-1003 (respond_by_at null), VL-1005 (kyc_completed_at null), VL-1006 (chain.edges []), VL-1008 (tender_eligibility populated).
</verification>

<success_criteria>
- Roadmap SC #1 is satisfied for the detail + list endpoints + success-action endpoint at fixture level (every fixture decodes via passing unit tests).
- D-12 12-load library exists; D-13 three fraud archetypes are flagged in their fixtures; D-14 four action-outcome fixtures exist.
- D-11 shared-world load consistency (same VL- across role files) is enforced by the byte-identity test in Task 2 acceptance criteria.
- LOAD-01 and LOAD-02 are extended with concrete fixture-level evidence; Plan 06 then wires fixtures into the DEBUG `MockLoadFixtureRegistry`.
</success_criteria>

<output>
Create `.planning/phases/07-load-domain-model-mock-contract/07-05-SUMMARY.md` recording: the 12-load library actually authored (any deviations from the 07-RESEARCH.md roster — e.g. if VL-1005 also carried a "caution" verdict per Claude's Discretion, document); the test target Resources phase update method (Xcode UI vs pbxproj direct edit); test results; any decoder-drift issues surfaced by the round-trip tests.
</output>
