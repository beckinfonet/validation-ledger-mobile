---
phase: 01-foundational-conventions-scaffolding
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - Package.swift
  - .gitignore
  - .swiftformat
  - docs/ci.md
  - docs/cert-rotation.md
  - docs/adr/0001-mvvm-c-memory-conventions.md
  - docs/adr/0002-role-coordinator-swap-pattern.md
  - docs/adr/0003-module-layout-and-target-strategy.md
autonomous: true
requirements:
  - STACK-01
  - STACK-04
  - FOUND-03
  - FOUND-05
  - CI-04
tags:
  - swiftpm
  - docs
  - adr
  - tooling

must_haves:
  truths:
    - "Package.swift declares Nuke 13.0.2 and SwiftLintPlugins 0.63.2 as the ONLY external dependencies — no Alamofire, KeychainAccess, XCoordinator, Sentry, Firebase, Amplitude, Mixpanel, Crashlytics"
    - "Package.swift uses `swift-tools-version: 6.0` (enables Swift Testing bundled with Xcode 16+)"
    - "docs/ci.md documents both pipelines (Simulator + Device) with triggers, runners, Xcode versions, and the known tradeoff"
    - "docs/cert-rotation.md exists as a skeleton (stub — full content Phase 2), contains '30-day' reference"
    - "docs/adr/0001-mvvm-c-memory-conventions.md codifies all 6 MVVM-C memory rules per RESEARCH.md Example 6"
    - "docs/adr/0002-role-coordinator-swap-pattern.md documents ARCH-06 root-swap decision"
    - "docs/adr/0003-module-layout-and-target-strategy.md records D-15 single-target choice + re-evaluation triggers"
    - ".swiftformat config file is in repo root (SwiftFormat config for STACK-02)"
    - ".gitignore excludes xcuserdata/, DerivedData/, .build/, *.xcuserstate"
  artifacts:
    - path: "Package.swift"
      provides: "SwiftPM dependency manifest pinning Nuke + SwiftLintPlugins"
      contains: "swift-tools-version: 6.0"
    - path: ".gitignore"
      provides: "Git exclusion of Xcode user state + DerivedData + .build"
      contains: "xcuserdata/"
    - path: ".swiftformat"
      provides: "SwiftFormat configuration"
    - path: "docs/ci.md"
      provides: "CI pipeline documentation (CI-04)"
      contains: "Simulator Pipeline"
    - path: "docs/cert-rotation.md"
      provides: "Cert rotation runbook skeleton (FOUND-05 Phase 1 scope)"
      contains: "STUB"
    - path: "docs/adr/0001-mvvm-c-memory-conventions.md"
      provides: "FOUND-03 memory conventions ADR"
      contains: "assign(to:on:) is BANNED"
    - path: "docs/adr/0002-role-coordinator-swap-pattern.md"
      provides: "ARCH-06 root-swap decision record"
      contains: "SceneDelegate"
    - path: "docs/adr/0003-module-layout-and-target-strategy.md"
      provides: "D-15 single-target architectural decision"
      contains: "single Xcode target"
  key_links:
    - from: "Package.swift"
      to: "Nuke 13.0.2"
      via: "SwiftPM .package(url:exact:)"
      pattern: "Nuke.*13\\.0\\.2"
    - from: "Package.swift"
      to: "SwiftLintPlugins 0.63.2"
      via: "SwiftPM .package(url:from:) + .plugin()"
      pattern: "SwiftLintPlugins"
    - from: "docs/ci.md"
      to: "docs/cert-rotation.md"
      via: "reference link"
    - from: "docs/adr/0001"
      to: "CLAUDE.md"
      via: "reference (FOUND-03 'documented in CLAUDE.md and enforced at review' — CLAUDE.md will link here)"
---

<objective>
Wave 1 parallel plan: Ship all repo-root configuration files and documentation artifacts that do NOT touch Swift source code. This includes SwiftPM manifest, .gitignore, SwiftFormat config, CI docs, cert-rotation skeleton, and 3 ADRs.

Purpose: These files are dependency-free — they don't import anything and nothing imports them. They can be written in parallel with Plan 03 (Core Swift services) and Plan 04 (UI/Resources/Roles) without file conflicts. Finishing them here unblocks Plan 06 (SwiftLint config will reference `Package.swift` for plugin resolution) and Plan 07 (CI workflows will reference `docs/ci.md`).

Output: 8 files committed to repo root and `docs/`. All static content (no runtime code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundational-conventions-scaffolding/01-CONTEXT.md
@.planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md
@.planning/phases/01-foundational-conventions-scaffolding/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- Research file contents to copy verbatim (do NOT re-derive): -->
<!-- - Package.swift: .planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md §Installation (lines 186-211) -->
<!-- - docs/adr/0001: RESEARCH.md Example 6 (lines 1228-1266) -->
<!-- - docs/ci.md: RESEARCH.md Example 4 (lines 1157-1203) -->
<!-- - docs/cert-rotation.md: RESEARCH.md Flag #5 (line 1474) + PATTERNS.md pattern (lines 1003-1018) -->

<!-- Dependency allowlist from CLAUDE.md + STACK-04: -->
<!-- ALLOWED (Phase 1): Nuke, SwiftLintPlugins -->
<!-- FORBIDDEN (STACK-04 + CLAUDE.md): Alamofire, KeychainAccess, XCoordinator, Sentry, Firebase, Crashlytics, Amplitude, Mixpanel, SDWebImage, Swinject, Resolver, RxSwift, Quick, Nimble, TrustKit -->

<!-- Swift Testing does NOT need a dep — bundled with Xcode 16+ when swift-tools-version is 6.0+ -->

<!-- CLAUDE.md constraints to reflect in docs: -->
<!-- - UIKit-first (SwiftUI permitted for non-critical surfaces only — N/A Phase 1) -->
<!-- - SwiftPM only (no CocoaPods, no Carthage) -->
<!-- - iOS 17.0 minimum -->
<!-- - Zero PII in analytics or crash logs -->
<!-- - Tokens in Keychain; keys in Secure Enclave -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ship Package.swift + .gitignore + .swiftformat (repo-root tooling configs)</name>
  <read_first>
    - .planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md §Installation (lines 186-211 — Package.swift verbatim) AND §Version Verification (lines 171-184 — version check commands)
    - .planning/phases/01-foundational-conventions-scaffolding/01-PATTERNS.md §Package.swift (lines 935-946) AND §`.gitignore` (lines 88-95) AND §.swiftformat (line 93)
    - .planning/research/STACK.md "TL;DR — The Pinned Stack" (top of file — versions table)
    - CLAUDE.md ("Pre-approved shortlist only; Phase 1 adds ONLY Nuke")
  </read_first>
  <action>
    **Step A — Create `Package.swift` at repo root (same directory as `validationLedger.xcodeproj`).** Write the following content verbatim (this is the exact block from RESEARCH.md lines 188-211):

    ```swift
    // swift-tools-version: 6.0
    // Package.swift — Validation Ledger iOS Client
    // Companion manifest to validationLedger.xcodeproj: declares SwiftPM external dependencies.
    // The .xcodeproj remains the source-of-truth for target structure (per D-15).
    //
    // Dependency allowlist per STACK-01 + STACK-04 + CLAUDE.md:
    //   - Nuke (images; pre-wired for later phases)
    //   - SwiftLintPlugins (lint enforcement)
    // FORBIDDEN: Alamofire, KeychainAccess, XCoordinator, Sentry, Firebase,
    //            Crashlytics, Amplitude, Mixpanel, SDWebImage.

    import PackageDescription

    let package = Package(
        name: "validationLedger",
        platforms: [
            .iOS(.v17)
        ],
        dependencies: [
            .package(url: "https://github.com/kean/Nuke.git",                    exact: "13.0.2"),
            .package(url: "https://github.com/SimplyDanny/SwiftLintPlugins.git", from: "0.63.2"),
        ]
    )
    ```

    **Important note on structure:** Per RESEARCH.md line 213, this is a COMPANION manifest to the Xcode project (path (a) in Assumption A5). Do NOT add `.target()` or `.testTarget()` blocks here — the `.xcodeproj` owns target structure per D-15. Xcode's "Add Package Dependencies" UI resolves this `Package.swift` to surface Nuke + SwiftLintPlugins for linking into the main target (handled by Plan 06 for the SwiftLint plugin, and Plan 04 pre-wires Nuke as a no-op dep on the app target).

    **Version verification (runtime check, not a test):** Before committing, run:
    ```
    curl -s https://api.github.com/repos/kean/Nuke/releases/latest | grep -E '"tag_name"|"published_at"'
    curl -s https://api.github.com/repos/SimplyDanny/SwiftLintPlugins/releases/latest | grep -E '"tag_name"|"published_at"'
    ```
    - If Nuke latest > 13.0.2: still pin to `13.0.2` per RESEARCH.md (versions may have advanced since 2026-04-20 research date; pinning locks per STACK-01).
    - If Nuke 13.0.2 is GONE from GitHub (yanked): STOP, surface as blocker, do NOT auto-bump. Per STACK.md "versions locked."

    **Step B — Create `.gitignore` at repo root.** Content:
    ```
    # Xcode
    xcuserdata/
    *.xcuserstate
    *.xcuserdatad/
    DerivedData/
    build/

    # SwiftPM
    .build/
    Package.resolved
    .swiftpm/

    # macOS
    .DS_Store

    # Generated
    *.hmap
    *.ipa
    *.dSYM.zip
    *.dSYM
    ```
    Note: `Package.resolved` is intentionally ignored per STACK.md (exact-pinned `.package(url:exact:)` makes `Package.resolved` advisory, not authoritative). Alternative: commit `Package.resolved` for extra reproducibility — planner selects *ignore* here matching the "pinned via `exact:`" pattern. If the team later wants reproducibility proof, remove the `Package.resolved` line.

    **Step C — Create `.swiftformat` at repo root.** Content (SwiftFormat 0.61.0 defaults with minor additions for Swift 6 readiness):
    ```
    # .swiftformat — Validation Ledger iOS Client
    # SwiftFormat 0.61.0 (nicklockwood). See docs: https://github.com/nicklockwood/SwiftFormat
    #
    # Philosophy: defaults + conservative additions. Pre-commit hook (installed by Plan 06)
    # runs `swiftformat --lint` on staged files; `swiftformat` (auto-fix) is developer choice.

    # Swift version target (matches STACK.md Swift 5.9 floor)
    --swiftversion 5.9

    # Exclude generated + vendor + test fixtures
    --exclude .build,DerivedData,build,xcuserdata

    # Sensible defaults (most are SwiftFormat defaults; listing for docs)
    --indent 4
    --indentcase false
    --trimwhitespace always
    --insertlines true
    --removelines true
    --wraparguments before-first
    --wrapparameters before-first
    --wrapcollections before-first
    --closingparen balanced
    --self init-only
    --importgrouping alpha
    ```

    **Commit message prefix for this task: `chore(01-02): pin SwiftPM deps + gitignore + swiftformat`**
  </action>
  <verify>
    <automated>test -f Package.swift && grep -q "13.0.2" Package.swift && grep -q "swift-tools-version: 6.0" Package.swift && test -f .gitignore && test -f .swiftformat</automated>
  </verify>
  <acceptance_criteria>
    1. `test -f Package.swift` exits 0
    2. `grep -q "swift-tools-version: 6.0" Package.swift` exits 0
    3. `grep -q 'Nuke.git",.*exact: "13.0.2"' Package.swift` exits 0
    4. `grep -q "SwiftLintPlugins.git" Package.swift` exits 0
    5. `grep -q "iOS(.v17)" Package.swift` exits 0
    6. `! grep -qE "(Alamofire|KeychainAccess|XCoordinator|Sentry|Firebase|Crashlytics|Amplitude|Mixpanel|SDWebImage|Swinject|Resolver|RxSwift|TrustKit)" Package.swift` exits 0 (FORBIDDEN dep allowlist enforcement — STACK-04)
    7. `test -f .gitignore && grep -q "xcuserdata/" .gitignore && grep -q "DerivedData/" .gitignore && grep -q ".build/" .gitignore` exits 0
    8. `test -f .swiftformat && grep -q -- "--swiftversion 5.9" .swiftformat` exits 0
    9. `swift package describe --type json 2>/dev/null | grep -q "validationLedger"` — validates Package.swift parses. (If `swift package` isn't available on the dev machine due to toolchain mismatch, fallback: open Xcode, File → Add Package Dependencies → select Package.swift, confirm resolution succeeds.)
  </acceptance_criteria>
  <done>
    - Package.swift parses (swift package CLI or Xcode UI resolves)
    - .gitignore, .swiftformat are committed
    - No FORBIDDEN dependencies appear in Package.swift (enforced by acceptance #6)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ship docs/ci.md + docs/cert-rotation.md skeleton</name>
  <read_first>
    - .planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md Example 4 (lines 1157-1203 — docs/ci.md verbatim) AND Flag #5 (lines 1474-1478 — cert-rotation skeleton rationale) AND Flag #2 (line 1449 — Xcode version docs gap)
    - .planning/phases/01-foundational-conventions-scaffolding/01-PATTERNS.md §docs/ci.md (lines 988-994) AND §docs/cert-rotation.md (lines 997-1018)
    - .planning/phases/01-foundational-conventions-scaffolding/01-CONTEXT.md D-01..D-06 (lines 29-38 — CI decisions that docs/ci.md documents)
    - .planning/research/PITFALLS.md (Pitfall 3 — cert rotation self-brick)
  </read_first>
  <action>
    **Step A — Create `docs/ci.md`.** Copy verbatim from RESEARCH.md Example 4 (lines 1157-1203) and add the Flag #2 clarification (Xcode version dev vs CI). Full content:

    ```markdown
    # CI Pipelines

    > Documents the two CI pipelines per FOUND-04 + CI-04.
    > Workflow YAML lives at `.github/workflows/ci-simulator.yml` and `.github/workflows/ci-device.yml` (created by Plan 07).

    ## Overview

    Two pipelines run:
    - **Simulator** (every PR) — `.github/workflows/ci-simulator.yml`
    - **Device** (every merge to main + security-path PR) — `.github/workflows/ci-device.yml`

    ## Xcode Version Policy

    - **Dev machine:** Xcode 26.4 (current stable locally).
    - **CI:** pins Xcode 16.4 via `sudo xcode-select -s /Applications/Xcode_16.4.app` in workflow.
    - **Reason:** CI uses the floor version so Swift Testing (bundled in Xcode 16+) + iOS 17 SDK availability are both guaranteed without surprises. `macos-latest` GitHub runner image typically ships Xcode 16.x + 26.x; pinning removes ambiguity. Dev machines get newer Xcode for iteration speed.

    ## Simulator Pipeline

    **Trigger:** `pull_request` on `main`
    **Runner:** `macos-latest` (GitHub-hosted) — D-01
    **Xcode:** floor 16.4 (bundled iOS 17 SDK + Swift Testing)
    **Steps:**
    1. Checkout
    2. Select Xcode 16.4
    3. Install iOS 17 simulator runtime (fallback — image usually has it)
    4. Restore SwiftPM cache
    5. SwiftLint `--strict` (fail-fast before tests)
    6. `xcodebuild test -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -enableCodeCoverage YES -only-testing:validationLedgerTests/Logging -only-testing:validationLedgerTests/Storage -only-testing:validationLedgerTests/Networking -only-testing:validationLedgerTests/Auth -only-testing:validationLedgerTests/Navigation -only-testing:validationLedgerTests/Roles`
    7. `PrivacyInfo.xcprivacy` presence check (grep-able script per D-21 / FOUND-06)
    8. Coverage parser: `Core/` ≥ 70% (CI-01)

    **Excluded:** Secure Enclave + biometric + App Attest tests (D-03 — require real hardware). These live in `validationLedgerDeviceTests/` and are gated out by the `-only-testing:` flags above.
    **Coverage target:** ≥70% on `Core/` (CI-01).

    ## Device Pipeline

    **Trigger:** `push` to `main` OR `pull_request` touching security paths (D-05)
    - Security paths: `validationLedger/Core/Auth/**`, `validationLedger/Core/KeyStore/**`, `validationLedger/Core/Identity/**`, `validationLedger/Core/Networking/CertificatePinning/**`
    **Runner:** self-hosted MacBook with connected iPhone (D-04)
    **Runner labels:** `[self-hosted, macOS, device]`
    **Xcode:** 26.4 (dev machine's installed Xcode)
    **Steps:**
    1. Checkout
    2. `xcodebuild test -destination 'platform=iOS,id=$DEVICE_UDID' -only-testing:validationLedgerDeviceTests`
    3. The single Phase-1 smoke test (D-06):
       - `#expect(SecureEnclave.isAvailable == true)` — fails on iPad without SEP, iPhone 5s and older
       - Keychain write → read → delete round-trip on a test key

    **Self-hosted runner setup:** See `docs/adr/0003-module-layout-and-target-strategy.md` for the M2-boundary re-evaluation trigger; see GitHub Actions docs for runner labeling.

    ## Known Trade-off

    Self-hosted runner occupies the dev MacBook for ~5–15 min per run. Re-evaluate at M2 boundary (see `.planning/STATE.md` Blockers/Concerns). Migration path: dedicated Mac mini or MacStadium when a second engineer joins or when device CI starts blocking merges in practice.

    ## Secrets

    | Secret | Where Stored | Used In |
    |--------|--------------|---------|
    | `DEVICE_UDID` | GitHub Actions repo secret | `ci-device.yml` |

    No API keys, signing certs, or provisioning profiles live in CI for Phase 1 (TestFlight submission is M5).

    ## Related

    - `docs/cert-rotation.md` — cert rotation runbook (skeleton in Phase 1; full content Phase 2)
    - `docs/adr/0003-module-layout-and-target-strategy.md` — single-target decision + re-evaluation triggers
    - `.planning/research/PITFALLS.md` — CI-related pitfalls P8, P20
    ```

    **Step B — Create `docs/cert-rotation.md` (skeleton only per Flag #5).** Content:

    ```markdown
    # Cert Rotation Runbook

    **Status:** STUB — full content ships in Phase 2 alongside SEC-01 cert pinning implementation.
    **Phase 1 scope:** Reserves the path + documents the 30-day rotation pattern at outline depth per PITFALLS.md P3.

    ## Why This File Exists in Phase 1

    FOUND-05 maps to Phase 1 in ROADMAP.md traceability, but the full runbook depends on Phase 2's `PinningSessionDelegate` implementation (the skeleton-only version ships in Phase 1 Plan 03 under `Core/Networking/CertificatePinning/`). This file is committed now so the path is reserved and the 30-day rotation pattern is captured at outline depth — CRITICAL for preventing self-brick DoS (PITFALLS.md P3).

    ## 30-Day Rotation Window (outline)

    1. **Dual-pin deployment:** Backup SPKI hash is deployed alongside the primary SPKI hash in every release. Both are valid simultaneously.
    2. **Primary approaches expiry (≥30 days before):**
       - Rotate the primary pin in source (`Core/Networking/CertificatePinning/PinningSessionDelegate.swift`) to the next-gen cert's SPKI hash.
       - Backup pin remains as fallback.
       - Ship the release; wait for TestFlight adoption baseline (target ≥ 95% active installs).
    3. **Primary expires:**
       - Old backup pin becomes the new primary.
       - Next-gen cert's SPKI hash moves to backup slot.
       - Ship the release.
    4. **Emergency revoke path (if primary cert is compromised before rotation window):**
       - Phase 2 deliverable — full step-by-step procedure + user-facing messaging.

    ## Phase 2 To-Do (FOUND-05 full scope)

    - Step-by-step script-driven rotation (commands with exact file paths, commit messages, review checklist)
    - Backup-pin source-of-truth decision (separate file? embedded in release build-config? TBD in Phase 2 context gather)
    - Rollback procedure if a rotation ships a bad pin
    - CI check: SwiftLint custom rule or unit test that fails if only one SPKI hash is declared (both must be present)
    - Integration with TestFlight adoption analytics (when available — note this may slip to M2 if crash-vendor not chosen)

    ## Related

    - `.planning/research/PITFALLS.md` — P3 (cert pinning without rotation = self-brick DoS)
    - `.planning/REQUIREMENTS.md` — FOUND-05, SEC-01
    - `docs/ci.md` — Device CI security-path filter includes `Core/Networking/CertificatePinning/**`
    ```

    **Commit message prefix for this task: `docs(01-02): add CI docs + cert-rotation skeleton`**
  </action>
  <verify>
    <automated>test -f docs/ci.md && test -f docs/cert-rotation.md && grep -q "Simulator Pipeline" docs/ci.md && grep -q "Device Pipeline" docs/ci.md && grep -q "STUB" docs/cert-rotation.md && grep -q "30-day" docs/cert-rotation.md</automated>
  </verify>
  <acceptance_criteria>
    1. `test -f docs/ci.md` exits 0
    2. `grep -q "Simulator Pipeline" docs/ci.md` exits 0
    3. `grep -q "Device Pipeline" docs/ci.md` exits 0
    4. `grep -q "Xcode 16.4" docs/ci.md` exits 0 (Flag #2 version policy documented)
    5. `grep -q "Xcode 26.4" docs/ci.md` exits 0 (dev vs CI split documented)
    6. `grep -q "self-hosted.*macOS.*device" docs/ci.md` exits 0 (runner labels documented)
    7. `grep -q "DEVICE_UDID" docs/ci.md` exits 0 (secret documented)
    8. `test -f docs/cert-rotation.md` exits 0
    9. `grep -q "STUB" docs/cert-rotation.md` exits 0 (explicit skeleton marker)
    10. `grep -q "30-day" docs/cert-rotation.md` exits 0 (Success Criterion from VALIDATION.md FOUND-05 row)
    11. `grep -q "Phase 2" docs/cert-rotation.md` exits 0 (deferral rationale present)
  </acceptance_criteria>
  <done>
    - `docs/ci.md` covers both pipelines with triggers, runners, Xcode version policy (Flag #2), and known tradeoff
    - `docs/cert-rotation.md` is a usable skeleton — reserves the path, captures 30-day pattern, defers detail to Phase 2 explicitly
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Ship 3 ADRs (0001 MVVM-C memory, 0002 role-coordinator-swap, 0003 module-layout)</name>
  <read_first>
    - .planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md Example 6 (lines 1228-1266 — ADR 0001 verbatim)
    - .planning/phases/01-foundational-conventions-scaffolding/01-PATTERNS.md §ADR 0001 (lines 949-959) AND §ADR 0002 (lines 962-971) AND §ADR 0003 (lines 975-985)
    - .planning/research/PITFALLS.md P5 (MVVM-C retain cycles — the rationale body of ADR 0001)
    - .planning/research/ARCHITECTURE.md (Pattern 1 MVVM-C with Initializer DI — background for ADR 0001; Amendment #3 root-swap at SceneDelegate — background for ADR 0002)
    - .planning/phases/01-foundational-conventions-scaffolding/01-CONTEXT.md D-10 (line 56 — root-swap), D-15 (line 69 — single target), D-18 (lines 72-77 — ADR layout)
  </read_first>
  <action>
    Create directory `docs/adr/` and ship three ADRs. All three follow the same structure (Status / Date / Supersedes / Context / Decision / Consequences / Related) per D-18.

    **Step A — `docs/adr/0001-mvvm-c-memory-conventions.md`** — COPY VERBATIM from RESEARCH.md Example 6 (lines 1230-1266). Full content:

    ```markdown
    # ADR 0001: MVVM-C Memory Conventions

    **Status:** Accepted
    **Date:** 2026-04-20
    **Supersedes:** None

    ## Context

    MVVM + Coordinators + Combine have four canonical leak patterns (`.planning/research/PITFALLS.md` P5).
    Undetected retain cycles compound through M2–M3 and force a feature freeze to fix.

    ## Decision

    The following rules are enforced at code review for every ViewModel + Coordinator pair:

    1. **Weak back-references:** `ViewModel` holds `weak var coordinator: CoordinatorProtocol?`.
       Coordinators hold children strongly, parent weakly.
    2. **`[weak self]` in every `sink` closure** followed by `guard let self else { return }`.
    3. **`assign(to:on:)` is BANNED.** Use explicit `.sink { [weak self] in self?.property = $0 }`
       or a custom `assignWeak(to:on:)` extension.
    4. **Every `Task` created in a ViewModel is stored and cancelled in `deinit`.**
    5. **`@Published` writes from background threads are forbidden.** Use `@MainActor` on VMs
       OR `.receive(on: DispatchQueue.main)` before assignment.
    6. **DEBUG `assert(cancellables.isEmpty)` in VM `deinit`** — makes leaks visible in console.

    ## Consequences

    - Code review cost: +5 min per VM PR; pays back in weeks of non-leak-hunting.
    - No runtime performance impact.
    - SwiftLint regex for `.assign(to:` can be added later; sink-without-weak is high false-positive.

    ## Related

    - `.planning/research/PITFALLS.md` P5
    - `.planning/research/ARCHITECTURE.md` Pattern 1 (MVVM-C with Initializer DI)
    - `CLAUDE.md` (this project's instructions link here for FOUND-03 compliance)
    - `REQUIREMENTS.md` FOUND-03
    ```

    **Step B — `docs/adr/0002-role-coordinator-swap-pattern.md`** — NOVEL content per PATTERNS.md §ADR 0002. Full content:

    ```markdown
    # ADR 0002: Role Coordinator Swap Pattern

    **Status:** Accepted
    **Date:** 2026-04-20
    **Supersedes:** None

    ## Context

    `Roles/RoleCoordinator` must swap the app's root coordinator when the active role changes (ARCH-06). Five roles exist: Shipper, Broker, Carrier, Dispatch, Factoring (per TechStack.md §4). Role changes must not leak state between roles — a security-sensitive app cannot risk, for example, a Shipper's in-memory cache being visible to a Carrier session.

    Two viable implementations exist:
    1. **Mutate `TabBarCoordinator` children:** swap the child VCs of a shared `UITabBarController`. Lightweight, no re-init cost.
    2. **Recreate the root at `SceneDelegate`:** on role change, allocate a fresh `AppContainer` + `AppCoordinator`, assign to `window.rootViewController`. Heavier, but state isolation is deterministic via ARC.

    `.planning/research/ARCHITECTURE.md` Amendment #3 and `.planning/research/PITFALLS.md` Anti-Pattern 4 recommend option 2.

    ## Decision

    **Root-swap at `SceneDelegate` level (option 2).** Implementation:

    ```swift
    func presentRoot(_ phase: AppPhase) {
        let container = AppContainer(env: .current)
        let coordinator = AppCoordinator(container: container, phase: phase)
        self.appCoordinator = coordinator          // single strong reference
        self.window?.rootViewController = coordinator.rootViewController
        // Previous coordinator tree is orphaned; ARC deallocates on next runloop.
    }
    ```

    - Fresh `AppContainer` per role change — no shared state between roles.
    - `SceneDelegate` holds exactly one strong reference (`appCoordinator`). Assignment to a new value drops the old tree deterministically.
    - **Abrupt replace** — no cross-dissolve animation. This is a dev affordance in Phase 1 (exercised via DevMenu shake gesture per D-07) and becomes a product flow in Phase 3 (AUTH-04 logout → phone-entry screen).

    ## Consequences

    - **Phase 1 cost:** Near-zero. Services in the container are cheap (loggers + stub keystore + protocol stubs).
    - **Phase 2+ cost:** Audit required. When `AppContainer` holds real `NetworkClient` (with URLSession caches), real `KeyStoreProtocol` (with Secure Enclave key handles), the re-create cost grows. If any service becomes expensive to re-create, add a migration step (pass the service to the new container) OR split the container into `SessionScope` (swap on role change) + `AppScope` (persistent across role changes).
    - **Testing:** Role-swap must be unit-testable via `RoleCoordinator` protocol contract tests (Plan 03 or Plan 05 writes these under `validationLedgerTests/Roles/RoleCoordinatorTests.swift`).
    - **Animation:** No cross-dissolve. Users see an instant swap. This is acceptable for a security-first product (the role change is an explicit dev action; users in Phase 3+ should rarely see role swaps — role is established at account creation).

    ## Related

    - `.planning/research/ARCHITECTURE.md` Amendment #3 (root-swap at SceneDelegate)
    - `.planning/research/PITFALLS.md` Anti-Pattern 4 (TabBar child mutation leaks state)
    - `CONTEXT.md` D-07 (full dev-menu demo), D-10 (root-swap mechanism)
    - `REQUIREMENTS.md` ARCH-06
    ```

    **Step C — `docs/adr/0003-module-layout-and-target-strategy.md`** — NOVEL content per PATTERNS.md §ADR 0003. Full content:

    ```markdown
    # ADR 0003: Module Layout and Target Strategy

    **Status:** Accepted
    **Date:** 2026-04-20
    **Supersedes:** None

    ## Context

    `TechStack.md §3.2` lays out the module structure: `App/`, `Core/{Networking, Auth, KeyStore, Identity, Storage, Logging, Analytics, Security, AIKit, Navigation}`, `Features/{Onboarding, Loads, BOL, Scanner, Assistant, Profile, Settings}`, `Roles/`, `UI/`, `Resources/`.

    Two viable targeting strategies exist:
    1. **Single Xcode target with directory groups.** `Features/X` and `Features/Y` live in the same target; "don't import across features" is a convention enforced by review + lint.
    2. **Per-Feature local SwiftPM packages.** Each `Features/X` is its own module with a `Package.swift`; cross-feature imports are compile-time errors.

    Research findings (`.planning/research/SUMMARY.md`): "no payoff on single-module M1." Per-Feature SPM packages add build-time cost + tooling complexity with zero value when the codebase has < 15 Features.

    ## Decision

    **Single Xcode target with directory groups (option 1) for Phase 1 and onward, re-evaluated at M2 boundary.**

    - The app target is `validationLedger` (single PBXNativeTarget).
    - Source code is organized in directory groups per the TechStack.md §3.2 layout.
    - Cross-feature isolation (ARCH-05) is enforced by **SwiftLint custom rule `no_cross_feature_import`** (Plan 06 ships the rule) — NOT by SPM package boundaries.
    - External SwiftPM dependencies (Nuke, SwiftLintPlugins) are declared in the companion `Package.swift` at the repo root (see ADR-TBD or Plan 02 for format).

    ## Consequences

    - **Build speed (Phase 1):** Fast. Single module compiles as one unit.
    - **Isolation:** Weaker than compile-enforced. A developer CAN write `import Features_Loads` from `Features/BOL/` if Features were modules, but since they are NOT modules, that statement is meaningless — lint rule `no_cross_feature_import` triggers zero Phase 1 violations (future-proofing for when Features become modules).
    - **Testing cost:** Single test target (`validationLedgerTests`) covers all `Core/` + any Feature code that lands in Phase 3+.
    - **Refactor cost if we later split:** Medium. Moving a Feature to its own SPM package requires adding a `Package.swift`, declaring types `public`, updating imports in consumers. ~1–2 engineer-days per Feature.

    ## Re-evaluation Triggers

    Revisit this decision when ANY of:
    1. The codebase crosses **~15 Features** (compile-time linting becomes valuable).
    2. ARCH-05 lint violations start slipping through review (lint rule is insufficient — compile-enforce needed).
    3. A second engineer joins and parallel feature work causes merge conflicts in shared-type files (module boundaries enforce isolation).
    4. Build time for the single target exceeds ~60s cold (modularization buys parallel compilation).
    5. M2 boundary checkpoint — planner should explicitly re-ask at that time.

    ## Related

    - `CONTEXT.md` D-15 (single Xcode target decision), D-18 (ADR layout)
    - `.planning/research/SUMMARY.md` ("no payoff on single-module M1")
    - `REQUIREMENTS.md` ARCH-03, ARCH-05
    - `.planning/research/ARCHITECTURE.md` §Recommended Project Structure
    ```

    **Commit message prefix for this task: `docs(01-02): add ADRs 0001-0003`**
  </action>
  <verify>
    <automated>test -f docs/adr/0001-mvvm-c-memory-conventions.md && test -f docs/adr/0002-role-coordinator-swap-pattern.md && test -f docs/adr/0003-module-layout-and-target-strategy.md && grep -q "assign(to:on:) is BANNED" docs/adr/0001-mvvm-c-memory-conventions.md</automated>
  </verify>
  <acceptance_criteria>
    1. `test -d docs/adr` exits 0
    2. `test -f docs/adr/0001-mvvm-c-memory-conventions.md` exits 0
    3. `grep -q "Status:.*Accepted" docs/adr/0001-mvvm-c-memory-conventions.md` exits 0
    4. `grep -q "assign(to:on:) is BANNED" docs/adr/0001-mvvm-c-memory-conventions.md` exits 0 (RULE 3 — critical enforcement signal)
    5. `grep -q "weak var coordinator" docs/adr/0001-mvvm-c-memory-conventions.md` exits 0 (RULE 1)
    6. `grep -c "^[0-9]\. " docs/adr/0001-mvvm-c-memory-conventions.md` returns ≥ 6 (all 6 numbered rules present)
    7. `test -f docs/adr/0002-role-coordinator-swap-pattern.md` exits 0
    8. `grep -q "SceneDelegate" docs/adr/0002-role-coordinator-swap-pattern.md` exits 0
    9. `grep -q "Fresh AppContainer" docs/adr/0002-role-coordinator-swap-pattern.md || grep -q "fresh .AppContainer." docs/adr/0002-role-coordinator-swap-pattern.md` exits 0
    10. `grep -q "Abrupt replace" docs/adr/0002-role-coordinator-swap-pattern.md` exits 0 (D-10 distinction)
    11. `test -f docs/adr/0003-module-layout-and-target-strategy.md` exits 0
    12. `grep -q "Single Xcode target" docs/adr/0003-module-layout-and-target-strategy.md` exits 0 (D-15 decision)
    13. `grep -q "Re-evaluation Triggers" docs/adr/0003-module-layout-and-target-strategy.md` exits 0 (D-15 trigger section per PATTERNS.md line 984)
    14. `grep -q "15 Features" docs/adr/0003-module-layout-and-target-strategy.md` exits 0 (one of the re-eval triggers per CONTEXT.md line 191)
  </acceptance_criteria>
  <done>
    - All three ADRs exist with Status=Accepted, Date, Context, Decision, Consequences, Related sections
    - ADR 0001 contains all 6 memory rules (satisfies FOUND-03 via D-18)
    - ADR 0002 documents the abrupt-replace decision (D-10) + dealloc contract
    - ADR 0003 states single-target decision (D-15) + at least 4 re-evaluation triggers
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependency declaration | `Package.swift` is the authoritative list of external code entering the trust boundary. Adding a dep here = expanding the attack surface. |
| Documentation integrity | ADRs are immutable once Accepted (per D-18). They serve as authority for code-review decisions; tampering would invalidate FOUND-03 / ARCH-06 enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Package.swift — unauthorized analytics/crash SDK addition | mitigate | Acceptance Criterion #6 greps for FORBIDDEN dep names; SwiftLint rule `ban_userdefaults_tokens` (Plan 06) doesn't cover this, but the CI step in Plan 07 can add `grep -E "(Sentry\|Firebase\|...)" Package.swift` returning empty. |
| T-02-02 | Information Disclosure | Package.resolved committed with unintended secrets | accept | Mitigated by `.gitignore` entry for `Package.resolved`; if team later opts to commit for reproducibility, verify no secrets (SwiftPM resolved files contain only URLs + revisions). |
| T-02-03 | Repudiation | ADR edited without superseding record | mitigate | D-18 process + Status:Accepted marker + file naming (NNNN-title.md). Enforced by code review, not by tooling. |
| T-02-04 | Denial of Service | Cert rotation runbook missing → self-brick on cert expiry | mitigate (partial — Phase 1 skeleton only) | `docs/cert-rotation.md` reserves the path and captures 30-day pattern outline. Full runbook content is a Phase 2 deliverable (Flag #5). Accepted as Phase 1 scope. |
</threat_model>

<verification>
1. `git status` — all 8 files staged with no unrelated changes
2. `swift package describe --type json 2>/dev/null | grep -q validationLedger` OR Xcode "Resolve Package Dependencies" succeeds
3. All 3 ADRs have valid Accepted status and required sections
4. `grep -E "(Sentry|Firebase|Crashlytics|Amplitude|Mixpanel|Alamofire|KeychainAccess|SDWebImage|XCoordinator|Swinject|Resolver|RxSwift|TrustKit)" Package.swift` returns empty (STACK-04 enforcement — mitigates T-02-01)
</verification>

<success_criteria>
- Package.swift + .gitignore + .swiftformat committed at repo root
- docs/ci.md + docs/cert-rotation.md committed under docs/
- docs/adr/0001, 0002, 0003 committed under docs/adr/
- All acceptance criteria in Tasks 1, 2, 3 pass
- No FORBIDDEN dependency names appear in Package.swift
- Plan 06 can reference `Package.swift` for SwiftLintPlugins resolution
- Plan 07 can reference `docs/ci.md` for pipeline specs
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundational-conventions-scaffolding/01-02-SUMMARY.md` capturing:
- Exact versions pinned in Package.swift (Nuke tag, SwiftLintPlugins tag) — note any divergence from RESEARCH.md (which recorded 13.0.2 / 0.63.2 on 2026-04-20)
- List of 8 files created with their one-line purpose
- Confirmation that `swift package describe` or Xcode package-resolution succeeded
- Any deviations from RESEARCH.md verbatim content (should be zero for Package.swift + ADR 0001 + docs/ci.md; minor additions allowed for ADRs 0002/0003 which are NOVEL)
</output>
