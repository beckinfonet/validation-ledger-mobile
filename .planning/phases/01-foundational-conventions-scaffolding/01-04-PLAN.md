---
phase: 01-foundational-conventions-scaffolding
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - validationLedger/Roles/Role.swift
  - validationLedger/Roles/RoleCoordinator.swift
  - validationLedger/Roles/Shipper/ShipperTabBarController.swift
  - validationLedger/Roles/Broker/BrokerTabBarController.swift
  - validationLedger/Roles/Carrier/CarrierTabBarController.swift
  - validationLedger/Roles/Dispatch/DispatchTabBarController.swift
  - validationLedger/Roles/Factoring/FactoringTabBarController.swift
  - validationLedger/UI/DesignSystem/Colors.swift
  - validationLedger/UI/DesignSystem/Spacing.swift
  - validationLedger/UI/DesignSystem/Typography.swift
  - validationLedger/Features/Onboarding/.gitkeep
  - validationLedger/Features/Loads/.gitkeep
  - validationLedger/Features/BOL/.gitkeep
  - validationLedger/Features/Scanner/.gitkeep
  - validationLedger/Features/Assistant/.gitkeep
  - validationLedger/Features/Profile/.gitkeep
  - validationLedger/Features/Settings/.gitkeep
  - validationLedger/Resources/PrivacyInfo.xcprivacy
  - validationLedger/Resources/Localizable.strings
  - validationLedger/App/Info.plist
  - validationLedgerTests/Roles/RoleCoordinatorTests.swift
autonomous: true
requirements:
  - ARCH-03
  - ARCH-06
  - FOUND-06
  - SEC-02
  - STACK-04
tags:
  - ui
  - roles
  - resources
  - privacy-manifest
  - ats

must_haves:
  truths:
    - "Role enum has all 5 cases in TechStack.md §4 order (shipper, broker, carrier, dispatch, factoring)"
    - "RoleCoordinator protocol exists in Core/ as the ARCH-06 contract for role swap"
    - "Each of 5 TabBarController subclasses has its TechStack.md §4 tabs — no deviation, no content"
    - "PrivacyInfo.xcprivacy declares NSPrivacyAccessedAPICategoryUserDefaults reason CA92.1, empty 3rd-party SDK list, zero tracking"
    - "PrivacyInfo.xcprivacy is in Copy Bundle Resources (target membership ON) — Plan 07 CI step will grep the built .app to prove it"
    - "Info.plist is ATS-strict: NSAllowsArbitraryLoads=false, no NSExceptionDomains entries"
    - "UI/DesignSystem has 3 token files (Colors, Spacing, Typography) — skeletons only"
    - "Features/ has 7 empty placeholder directories (ARCH-03 module layout)"
    - "RoleCoordinatorTests verify the protocol contract (each TabBarController creates with the correct number of tabs + correct titles)"
  artifacts:
    - path: "validationLedger/Roles/Role.swift"
      provides: "The Role enum (5 cases)"
      contains: "case shipper"
    - path: "validationLedger/Roles/RoleCoordinator.swift"
      provides: "ARCH-06 protocol contract for role-specific root coordinators"
      contains: "protocol RoleCoordinator"
    - path: "validationLedger/Roles/Shipper/ShipperTabBarController.swift"
      provides: "Shipper role tab bar: Loads, Brokers, BOL, Assistant"
      contains: "Brokers"
    - path: "validationLedger/Roles/Broker/BrokerTabBarController.swift"
      provides: "Broker role tab bar: Loads, Carriers, Network, Assistant"
      contains: "Network"
    - path: "validationLedger/Roles/Carrier/CarrierTabBarController.swift"
      provides: "Carrier role tab bar: Loads, Drivers, Documents, Assistant"
      contains: "Drivers"
    - path: "validationLedger/Roles/Dispatch/DispatchTabBarController.swift"
      provides: "Dispatch role tab bar: Loads, Fleet, Drivers, Assistant"
      contains: "Fleet"
    - path: "validationLedger/Roles/Factoring/FactoringTabBarController.swift"
      provides: "Factoring role tab bar: Invoices, Carriers, Chain, Assistant"
      contains: "Invoices"
    - path: "validationLedger/Resources/PrivacyInfo.xcprivacy"
      provides: "FOUND-06 privacy manifest (required-reason APIs, zero 3rd-party SDKs, no tracking)"
      contains: "CA92.1"
    - path: "validationLedger/App/Info.plist"
      provides: "SEC-02 ATS-strict configuration"
      contains: "NSAllowsArbitraryLoads"
    - path: "validationLedgerTests/Roles/RoleCoordinatorTests.swift"
      provides: "Contract tests for the 5 TabBarController subclasses"
      contains: "@Suite"
  key_links:
    - from: "PrivacyInfo.xcprivacy"
      to: "Copy Bundle Resources build phase"
      via: "Xcode Target Membership checkbox"
      pattern: "PrivacyInfo\\.xcprivacy"
    - from: "Info.plist"
      to: "NSAppTransportSecurity dict"
      via: "plist entry"
      pattern: "NSAllowsArbitraryLoads.*false"
    - from: "Role enum"
      to: "each TabBarController subclass"
      via: "1:1 mapping — 5 roles, 5 subclasses"
---

<objective>
Wave 1 parallel plan: Ship UI scaffolding, Resources (privacy manifest + Info.plist ATS), and the 5-role tab bar scaffolding. This lands the D-07/D-08/D-09 "full dev-menu demo" Phase-1 depth decision and the SEC-02 + FOUND-06 privacy/transport-security baseline.

Purpose: Plan 05 needs the `Role` enum to build SceneDelegate's `AppPhase.role(_:)` case and the 5 TabBarController types to instantiate. Plan 07 needs `PrivacyInfo.xcprivacy` in Copy Bundle Resources so the CI grep check passes. The UI/DesignSystem skeletons unblock any VC that wants a color constant later.

Output: ~15 Swift/plist/gitkeep files establishing the module layout, role scaffolding, and resource bundle. No behavior changes user-visibly in Phase 1 — this is scaffolding. Plan 05 wires the roles to a DEBUG-only DevMenu swap. Phase 3 wires them to real OTP flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundational-conventions-scaffolding/01-CONTEXT.md
@.planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md
@.planning/phases/01-foundational-conventions-scaffolding/01-PATTERNS.md
@TechStack.md
@CLAUDE.md

<interfaces>
<!-- D-09 tab inventory (COPY EXACTLY — no additions, no reordering, no merging): -->
<!-- Shipper:   Loads,    Brokers,  BOL,       Assistant -->
<!-- Broker:    Loads,    Carriers, Network,   Assistant -->
<!-- Carrier:   Loads,    Drivers,  Documents, Assistant -->
<!-- Dispatch:  Loads,    Fleet,    Drivers,   Assistant -->
<!-- Factoring: Invoices, Carriers, Chain,     Assistant -->

<!-- Source of truth: TechStack.md §4 role table + CONTEXT.md D-09 -->

<!-- D-21 PrivacyInfo.xcprivacy Phase-1 content (COPY EXACTLY — RESEARCH.md Pattern 5 sample lines 614-638): -->
<!--   NSPrivacyAccessedAPITypes: one entry — NSPrivacyAccessedAPICategoryUserDefaults reason CA92.1 -->
<!--   NSPrivacyCollectedDataTypes: empty array -->
<!--   NSPrivacyTracking: false -->
<!--   NSPrivacyTrackingDomains: empty array -->

<!-- SEC-02 Info.plist ATS block (from RESEARCH.md Example 3 lines 1138-1151): -->
<!--   NSAppTransportSecurity dict with NSAllowsArbitraryLoads=false + variants all false -->
<!--   NO NSExceptionDomains entry -->

<!-- D-15: single Xcode target — all files belong to the validationLedger target; tests belong to validationLedgerTests target. -->

<!-- SF Symbol choices for tab icons — planner's discretion per PATTERNS.md line 674, but stay consistent within the spec: -->
<!-- Examples chosen: -->
<!--   Loads → "shippingbox" -->
<!--   Brokers → "person.2" -->
<!--   BOL → "doc.text" -->
<!--   Assistant → "sparkles" -->
<!--   Carriers → "truck.box" (fallback "truck.box.fill" or "shippingbox.fill") -->
<!--   Network → "point.3.connected.trianglepath.dotted" (fallback "network") -->
<!--   Drivers → "person.badge.key" (fallback "person.circle") -->
<!--   Documents → "doc.on.doc" -->
<!--   Fleet → "car.2" -->
<!--   Invoices → "doc.text.magnifyingglass" -->
<!--   Chain → "link" -->
<!-- If a chosen SF Symbol is not available at iOS 17 (verify with Apple's SF Symbols app), substitute; record in summary. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ship Role enum + RoleCoordinator protocol + 5 TabBarController subclasses with RoleCoordinatorTests</name>
  <read_first>
    - .planning/phases/01-foundational-conventions-scaffolding/01-PATTERNS.md §Roles/Role.swift (lines 606-622) AND §Roles/{RoleName}TabBarController.swift (lines 626-674)
    - .planning/phases/01-foundational-conventions-scaffolding/01-CONTEXT.md D-07 (line 41), D-08 (lines 42-49), D-09 (lines 50-55) AND REQUIREMENTS.md SHELL-02 (line 69 for M1 tab inventory context; Phase 3 scope)
    - TechStack.md §4 (lines 98-108 — the authoritative 5-role tab inventory)
    - .planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md §Architectural Responsibility Map row "5 role TabBarController subclasses" (line 131)
  </read_first>
  <action>
    **Step A — `validationLedger/Roles/Role.swift`** (VERBATIM from PATTERNS.md lines 614-622):

    ```swift
    // validationLedger/Roles/Role.swift
    // The 5 M1 roles per TechStack.md §4. Role is established at account creation
    // (backend-assigned) and drives the root coordinator swap at SceneDelegate level (D-10).

    import Foundation

    public enum Role: String, CaseIterable, Sendable {
        case shipper
        case broker
        case carrier
        case dispatch
        case factoring
    }

    public extension Role {
        /// User-facing display name (uppercased first letter).
        var displayName: String {
            switch self {
            case .shipper:   return "Shipper"
            case .broker:    return "Broker"
            case .carrier:   return "Carrier"
            case .dispatch:  return "Dispatch"
            case .factoring: return "Factoring"
            }
        }
    }
    ```

    **Step B — `validationLedger/Roles/RoleCoordinator.swift`** (ARCH-06 protocol contract):

    ```swift
    // validationLedger/Roles/RoleCoordinator.swift
    // ARCH-06: protocol that each role's root coordinator conforms to.
    // Concrete role coordinators live in later phases; Phase 1 ships only the
    // UITabBarController subclasses (one per role) because that is what the
    // DevMenu swap (D-07) needs to demonstrate end-to-end.
    //
    // SceneDelegate (Plan 05) uses this protocol to present a new root when
    // the active role changes; the abrupt-replace semantics are ADR 0002.

    import UIKit

    public protocol RoleCoordinator: AnyObject {
        var role: Role { get }
        /// The root view controller installed as window.rootViewController.
        var rootViewController: UIViewController { get }
    }
    ```

    **Step C — Ship all 5 TabBarControllers.** They all follow the same pattern (PATTERNS.md lines 633-660). Tab inventories MUST match D-09 exactly.

    `validationLedger/Roles/Shipper/ShipperTabBarController.swift`:

    ```swift
    // validationLedger/Roles/Shipper/ShipperTabBarController.swift
    import UIKit

    final class ShipperTabBarController: UITabBarController, RoleCoordinator {
        let role: Role = .shipper
        var rootViewController: UIViewController { self }

        override func viewDidLoad() {
            super.viewDidLoad()
            viewControllers = [
                Self.makeTab(title: "Loads",     systemImage: "shippingbox"),
                Self.makeTab(title: "Brokers",   systemImage: "person.2"),
                Self.makeTab(title: "BOL",       systemImage: "doc.text"),
                Self.makeTab(title: "Assistant", systemImage: "sparkles"),
            ]
        }

        static func makeTab(title: String, systemImage: String) -> UIViewController {
            let vc = UIViewController()
            vc.title = title
            vc.tabBarItem = UITabBarItem(
                title: title,
                image: UIImage(systemName: systemImage),
                selectedImage: nil
            )
            vc.view.backgroundColor = .systemBackground
            return vc
        }
    }
    ```

    `validationLedger/Roles/Broker/BrokerTabBarController.swift`:

    ```swift
    import UIKit

    final class BrokerTabBarController: UITabBarController, RoleCoordinator {
        let role: Role = .broker
        var rootViewController: UIViewController { self }

        override func viewDidLoad() {
            super.viewDidLoad()
            viewControllers = [
                ShipperTabBarController.makeTab(title: "Loads",     systemImage: "shippingbox"),
                ShipperTabBarController.makeTab(title: "Carriers",  systemImage: "truck.box"),
                ShipperTabBarController.makeTab(title: "Network",   systemImage: "point.3.connected.trianglepath.dotted"),
                ShipperTabBarController.makeTab(title: "Assistant", systemImage: "sparkles"),
            ]
        }
    }
    ```

    `validationLedger/Roles/Carrier/CarrierTabBarController.swift`:

    ```swift
    import UIKit

    final class CarrierTabBarController: UITabBarController, RoleCoordinator {
        let role: Role = .carrier
        var rootViewController: UIViewController { self }

        override func viewDidLoad() {
            super.viewDidLoad()
            viewControllers = [
                ShipperTabBarController.makeTab(title: "Loads",     systemImage: "shippingbox"),
                ShipperTabBarController.makeTab(title: "Drivers",   systemImage: "person.badge.key"),
                ShipperTabBarController.makeTab(title: "Documents", systemImage: "doc.on.doc"),
                ShipperTabBarController.makeTab(title: "Assistant", systemImage: "sparkles"),
            ]
        }
    }
    ```

    `validationLedger/Roles/Dispatch/DispatchTabBarController.swift`:

    ```swift
    import UIKit

    final class DispatchTabBarController: UITabBarController, RoleCoordinator {
        let role: Role = .dispatch
        var rootViewController: UIViewController { self }

        override func viewDidLoad() {
            super.viewDidLoad()
            viewControllers = [
                ShipperTabBarController.makeTab(title: "Loads",     systemImage: "shippingbox"),
                ShipperTabBarController.makeTab(title: "Fleet",     systemImage: "car.2"),
                ShipperTabBarController.makeTab(title: "Drivers",   systemImage: "person.badge.key"),
                ShipperTabBarController.makeTab(title: "Assistant", systemImage: "sparkles"),
            ]
        }
    }
    ```

    `validationLedger/Roles/Factoring/FactoringTabBarController.swift`:

    ```swift
    import UIKit

    final class FactoringTabBarController: UITabBarController, RoleCoordinator {
        let role: Role = .factoring
        var rootViewController: UIViewController { self }

        override func viewDidLoad() {
            super.viewDidLoad()
            viewControllers = [
                ShipperTabBarController.makeTab(title: "Invoices",  systemImage: "doc.text.magnifyingglass"),
                ShipperTabBarController.makeTab(title: "Carriers",  systemImage: "truck.box"),
                ShipperTabBarController.makeTab(title: "Chain",     systemImage: "link"),
                ShipperTabBarController.makeTab(title: "Assistant", systemImage: "sparkles"),
            ]
        }
    }
    ```

    **Style note:** Reusing `ShipperTabBarController.makeTab(...)` across the 5 subclasses is intentional — it avoids duplicate helper code. If a future phase wants the helper free-standing, promote it to a `TabFactory` or extension on `UITabBarController`.

    **Step D — `validationLedgerTests/Roles/RoleCoordinatorTests.swift`** (Phase-1 contract tests — instantiation + tab count + title verification):

    ```swift
    // validationLedgerTests/Roles/RoleCoordinatorTests.swift
    import Testing
    import UIKit
    @testable import validationLedger

    @Suite("RoleCoordinator — 5 TabBarController contract (ARCH-06, D-07..D-09)")
    @MainActor
    struct RoleCoordinatorTests {
        @Test("ShipperTabBarController has tabs: Loads, Brokers, BOL, Assistant")
        func shipperTabs() {
            let vc = ShipperTabBarController()
            vc.loadViewIfNeeded()
            let titles = (vc.viewControllers ?? []).map { $0.title }
            #expect(titles == ["Loads", "Brokers", "BOL", "Assistant"])
            #expect(vc.role == .shipper)
        }

        @Test("BrokerTabBarController has tabs: Loads, Carriers, Network, Assistant")
        func brokerTabs() {
            let vc = BrokerTabBarController()
            vc.loadViewIfNeeded()
            let titles = (vc.viewControllers ?? []).map { $0.title }
            #expect(titles == ["Loads", "Carriers", "Network", "Assistant"])
            #expect(vc.role == .broker)
        }

        @Test("CarrierTabBarController has tabs: Loads, Drivers, Documents, Assistant")
        func carrierTabs() {
            let vc = CarrierTabBarController()
            vc.loadViewIfNeeded()
            let titles = (vc.viewControllers ?? []).map { $0.title }
            #expect(titles == ["Loads", "Drivers", "Documents", "Assistant"])
            #expect(vc.role == .carrier)
        }

        @Test("DispatchTabBarController has tabs: Loads, Fleet, Drivers, Assistant")
        func dispatchTabs() {
            let vc = DispatchTabBarController()
            vc.loadViewIfNeeded()
            let titles = (vc.viewControllers ?? []).map { $0.title }
            #expect(titles == ["Loads", "Fleet", "Drivers", "Assistant"])
            #expect(vc.role == .dispatch)
        }

        @Test("FactoringTabBarController has tabs: Invoices, Carriers, Chain, Assistant")
        func factoringTabs() {
            let vc = FactoringTabBarController()
            vc.loadViewIfNeeded()
            let titles = (vc.viewControllers ?? []).map { $0.title }
            #expect(titles == ["Invoices", "Carriers", "Chain", "Assistant"])
            #expect(vc.role == .factoring)
        }

        @Test("Role.allCases has all 5 roles in TechStack.md §4 order")
        func roleOrder() {
            #expect(Role.allCases == [.shipper, .broker, .carrier, .dispatch, .factoring])
        }
    }
    ```

    **Verification:**
    ```
    xcodebuild test \
        -project validationLedger.xcodeproj \
        -scheme validationLedger \
        -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' \
        -only-testing:validationLedgerTests/Roles/RoleCoordinatorTests
    ```

    **Commit message: `feat(01-04): Roles — enum + protocol + 5 TabBarControllers + tests`**
  </action>
  <verify>
    <automated>xcodebuild test -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerTests/Roles/RoleCoordinatorTests</automated>
  </verify>
  <acceptance_criteria>
    1. `test -f validationLedger/Roles/Role.swift && grep -c "^    case " validationLedger/Roles/Role.swift` returns ≥ 5
    2. `grep -q "case shipper" validationLedger/Roles/Role.swift && grep -q "case broker" validationLedger/Roles/Role.swift && grep -q "case carrier" validationLedger/Roles/Role.swift && grep -q "case dispatch" validationLedger/Roles/Role.swift && grep -q "case factoring" validationLedger/Roles/Role.swift` exits 0
    3. `test -f validationLedger/Roles/RoleCoordinator.swift && grep -q "protocol RoleCoordinator" validationLedger/Roles/RoleCoordinator.swift` exits 0
    4. All 5 TabBarController files exist: `for role in Shipper Broker Carrier Dispatch Factoring; do test -f "validationLedger/Roles/$role/${role}TabBarController.swift" || exit 1; done; exit 0`
    5. `grep -q "\"Brokers\"" validationLedger/Roles/Shipper/ShipperTabBarController.swift` exits 0 (D-09 Shipper tab)
    6. `grep -q "\"Network\"" validationLedger/Roles/Broker/BrokerTabBarController.swift` exits 0 (D-09 Broker tab)
    7. `grep -q "\"Fleet\"" validationLedger/Roles/Dispatch/DispatchTabBarController.swift` exits 0 (D-09 Dispatch tab)
    8. `grep -q "\"Invoices\"" validationLedger/Roles/Factoring/FactoringTabBarController.swift` exits 0 (D-09 Factoring tab)
    9. `grep -q "\"Documents\"" validationLedger/Roles/Carrier/CarrierTabBarController.swift` exits 0 (D-09 Carrier tab)
    10. `grep -q "Chain" validationLedger/Roles/Factoring/FactoringTabBarController.swift` exits 0
    11. `xcodebuild test ... -only-testing:validationLedgerTests/Roles/RoleCoordinatorTests` exits 0 (all 6 tests green)
  </acceptance_criteria>
  <done>
    - Role enum ships with 5 cases in exact TechStack.md §4 order
    - RoleCoordinator protocol ships with `role` + `rootViewController`
    - 5 TabBarControllers each conform to RoleCoordinator and set their 4 D-09 tabs
    - 6 RoleCoordinatorTests green
    - No content in placeholder tab VCs (just title + .systemBackground per PATTERNS.md line 673)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ship UI/DesignSystem tokens + Features/ placeholder directories</name>
  <read_first>
    - .planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md §Recommended Project Structure (lines 359-364 — UI/DesignSystem files; lines 338-345 — Features/ placeholders)
    - .planning/phases/01-foundational-conventions-scaffolding/01-PATTERNS.md §Design System (lines 67-71) AND §Features (lines 122-125)
    - REQUIREMENTS.md ARCH-03 (module layout — `Features/{Onboarding, Loads, BOL, Scanner, Assistant, Profile, Settings}` empty placeholders)
  </read_first>
  <action>
    **Step A — `validationLedger/UI/DesignSystem/Colors.swift`** (minimal per RESEARCH.md line 361):

    ```swift
    // validationLedger/UI/DesignSystem/Colors.swift
    // Phase-1 design system color tokens. Skeleton only — expanded in Phase 3+.
    // All colors default to iOS system semantic colors so dark-mode + Dynamic
    // Type support is automatic.

    import UIKit

    public enum DS {
        public enum Colors {
            public static let primary:    UIColor = .systemBlue
            public static let background: UIColor = .systemBackground
            public static let surface:    UIColor = .secondarySystemBackground
            public static let label:      UIColor = .label
            public static let labelSecondary: UIColor = .secondaryLabel
            public static let separator:  UIColor = .separator
        }
    }
    ```

    **Step B — `validationLedger/UI/DesignSystem/Spacing.swift`:**

    ```swift
    // validationLedger/UI/DesignSystem/Spacing.swift
    // Phase-1 spacing scale. 4pt base grid — iPad + iPhone Dynamic Type friendly.

    import UIKit

    public extension DS {
        enum Spacing {
            public static let xs:  CGFloat = 4
            public static let sm:  CGFloat = 8
            public static let md:  CGFloat = 16
            public static let lg:  CGFloat = 24
            public static let xl:  CGFloat = 32
            public static let xxl: CGFloat = 48
        }
    }
    ```

    **Step C — `validationLedger/UI/DesignSystem/Typography.swift`:**

    ```swift
    // validationLedger/UI/DesignSystem/Typography.swift
    // Phase-1 typography tokens. Uses UIFont.preferredFont(forTextStyle:) so
    // Dynamic Type support is automatic (TechStack.md §6 Accessibility).

    import UIKit

    public extension DS {
        enum Typography {
            public static var largeTitle: UIFont { .preferredFont(forTextStyle: .largeTitle) }
            public static var title1:     UIFont { .preferredFont(forTextStyle: .title1) }
            public static var title2:     UIFont { .preferredFont(forTextStyle: .title2) }
            public static var headline:   UIFont { .preferredFont(forTextStyle: .headline) }
            public static var body:       UIFont { .preferredFont(forTextStyle: .body) }
            public static var callout:    UIFont { .preferredFont(forTextStyle: .callout) }
            public static var footnote:   UIFont { .preferredFont(forTextStyle: .footnote) }
            public static var caption:    UIFont { .preferredFont(forTextStyle: .caption1) }
        }
    }
    ```

    **Step D — Features/ empty placeholders (ARCH-03 module layout):**
    Create 7 `.gitkeep` files to track the empty directories:
    ```
    mkdir -p validationLedger/Features/Onboarding
    mkdir -p validationLedger/Features/Loads
    mkdir -p validationLedger/Features/BOL
    mkdir -p validationLedger/Features/Scanner
    mkdir -p validationLedger/Features/Assistant
    mkdir -p validationLedger/Features/Profile
    mkdir -p validationLedger/Features/Settings
    touch validationLedger/Features/Onboarding/.gitkeep
    touch validationLedger/Features/Loads/.gitkeep
    touch validationLedger/Features/BOL/.gitkeep
    touch validationLedger/Features/Scanner/.gitkeep
    touch validationLedger/Features/Assistant/.gitkeep
    touch validationLedger/Features/Profile/.gitkeep
    touch validationLedger/Features/Settings/.gitkeep
    ```

    **Xcode group registration:** Add `UI/DesignSystem/` and `Features/*/` to the Xcode project as groups (not just folders) so IDE navigation works. Easiest path: Xcode sidebar → right-click `validationLedger` → Add Files to "validationLedger" → select folders with "Create groups" (NOT "Create folder references"). Verify the `.gitkeep` placeholders appear as `Files` members (not compilable).

    **Commit message: `feat(01-04): UI/DesignSystem tokens + Features/ placeholders`**
  </action>
  <verify>
    <automated>test -f validationLedger/UI/DesignSystem/Colors.swift && test -f validationLedger/UI/DesignSystem/Spacing.swift && test -f validationLedger/UI/DesignSystem/Typography.swift && test -f validationLedger/Features/Onboarding/.gitkeep && test -f validationLedger/Features/Loads/.gitkeep && test -f validationLedger/Features/BOL/.gitkeep && test -f validationLedger/Features/Scanner/.gitkeep && test -f validationLedger/Features/Assistant/.gitkeep && test -f validationLedger/Features/Profile/.gitkeep && test -f validationLedger/Features/Settings/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    1. All 3 UI/DesignSystem files exist and contain `public enum DS` or `public extension DS` (namespace alignment)
    2. All 7 Features/ directories exist with `.gitkeep` tracking
    3. Files compile as members of the main `validationLedger` target (verified by Task 1's xcodebuild test succeeding — tests compile the app)
    4. `grep -q "preferredFont" validationLedger/UI/DesignSystem/Typography.swift` exits 0 (Dynamic Type support wired)
  </acceptance_criteria>
  <done>
    - DesignSystem tokens exist as skeleton-only (expanded in later phases)
    - Features/ placeholders track in git so Plan 05's AppContainer / AppCoordinator can reference them conceptually (no imports needed yet; empty dirs just hold the path)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Ship PrivacyInfo.xcprivacy + Localizable.strings + Info.plist (ATS-strict)</name>
  <read_first>
    - .planning/phases/01-foundational-conventions-scaffolding/01-RESEARCH.md §Pattern 5 PrivacyInfo sample (lines 614-638 VERBATIM), §Pitfall P14 (lines 958-967 — Copy Bundle Resources requirement), Example 3 Info.plist ATS block (lines 1138-1151)
    - .planning/phases/01-foundational-conventions-scaffolding/01-PATTERNS.md §Resources/PrivacyInfo.xcprivacy (lines 678-714 VERBATIM), §App/Info.plist (lines 21)
    - .planning/phases/01-foundational-conventions-scaffolding/01-CONTEXT.md D-21 (line 85)
    - REQUIREMENTS.md FOUND-06 (line 20), SEC-02 (line 89), STACK-04 (line 38 — empty 3rd-party SDK list)
  </read_first>
  <action>
    **Step A — `validationLedger/Resources/PrivacyInfo.xcprivacy`** (COPY VERBATIM from RESEARCH.md Pattern 5 lines 615-638):

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <key>NSPrivacyAccessedAPITypes</key>
        <array>
            <dict>
                <key>NSPrivacyAccessedAPIType</key>
                <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
                <key>NSPrivacyAccessedAPITypeReasons</key>
                <array>
                    <string>CA92.1</string>
                </array>
            </dict>
        </array>
        <key>NSPrivacyCollectedDataTypes</key>
        <array/>
        <key>NSPrivacyTracking</key>
        <false/>
        <key>NSPrivacyTrackingDomains</key>
        <array/>
    </dict>
    </plist>
    ```

    **CRITICAL — Copy Bundle Resources membership (Pitfall P14):**
    Open `validationLedger.xcodeproj` in Xcode. Select `PrivacyInfo.xcprivacy` in the sidebar. In File Inspector (right panel, first tab):
    - Target Membership: **CHECK** `validationLedger` (the app target)
    - Target Membership: **UNCHECK** the test targets (`validationLedgerTests`, `validationLedgerUITests`, `validationLedgerDeviceTests`)

    Then in the `validationLedger` target's Build Phases tab:
    - Expand "Copy Bundle Resources"
    - Confirm `PrivacyInfo.xcprivacy` appears in the list.
    - If NOT: click "+" → add `PrivacyInfo.xcprivacy`.

    **Verify post-build:** After Plan 05 ships AppDelegate and the first successful simulator build:
    ```
    BUILD_DIR=$(xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -showBuildSettings 2>/dev/null | awk '/CONFIGURATION_BUILD_DIR/ {print $3; exit}')
    test -f "$BUILD_DIR/validationLedger.app/PrivacyInfo.xcprivacy" && echo OK || echo MISSING
    ```
    Plan 07's CI workflow runs this grep as a post-build step.

    **Step B — `validationLedger/Resources/Localizable.strings`** (empty English file per RESEARCH.md line 367):

    ```
    /*
      Localizable.strings
      validationLedger — English (en)

      Phase 1 ships this file empty so TechStack.md §6 "English v1; string catalog
      structured for Spanish in v2" is reserved. User-facing strings land starting
      in Phase 3 (OTP entry screens).
    */
    ```

    Add as Target Membership on `validationLedger` (Copy Bundle Resources) so bundle localization is wired. Xcode will create the `en.lproj/` subdirectory on build if it doesn't exist; alternatively, explicitly create `validationLedger/Resources/en.lproj/Localizable.strings` (recommended for clarity).

    **Step C — `validationLedger/App/Info.plist`** — ATS-strict per SEC-02 + UIKit scene manifest.

    Plan 01 Task 2 either kept the existing Info.plist OR set the build setting to point to a new physical path. This task WRITES or UPDATES the physical file at `validationLedger/App/Info.plist`. Content:

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
        <!-- Basic metadata -->
        <key>CFBundleDevelopmentRegion</key>
        <string>$(DEVELOPMENT_LANGUAGE)</string>
        <key>CFBundleExecutable</key>
        <string>$(EXECUTABLE_NAME)</string>
        <key>CFBundleIdentifier</key>
        <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
        <key>CFBundleInfoDictionaryVersion</key>
        <string>6.0</string>
        <key>CFBundleName</key>
        <string>$(PRODUCT_NAME)</string>
        <key>CFBundlePackageType</key>
        <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
        <key>CFBundleShortVersionString</key>
        <string>1.0</string>
        <key>CFBundleVersion</key>
        <string>1</string>
        <key>LSRequiresIPhoneOS</key>
        <true/>

        <!-- iOS 17 / iPhone + iPad -->
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
        <key>UISupportedInterfaceOrientations</key>
        <array>
            <string>UIInterfaceOrientationPortrait</string>
            <string>UIInterfaceOrientationLandscapeLeft</string>
            <string>UIInterfaceOrientationLandscapeRight</string>
        </array>
        <key>UISupportedInterfaceOrientations~ipad</key>
        <array>
            <string>UIInterfaceOrientationPortrait</string>
            <string>UIInterfaceOrientationPortraitUpsideDown</string>
            <string>UIInterfaceOrientationLandscapeLeft</string>
            <string>UIInterfaceOrientationLandscapeRight</string>
        </array>

        <!-- Scene manifest — UIKit scene lifecycle (Plan 05 wires SceneDelegate class name) -->
        <key>UIApplicationSceneManifest</key>
        <dict>
            <key>UIApplicationSupportsMultipleScenes</key>
            <false/>
            <key>UISceneConfigurations</key>
            <dict>
                <key>UIWindowSceneSessionRoleApplication</key>
                <array>
                    <dict>
                        <key>UISceneConfigurationName</key>
                        <string>Default Configuration</string>
                        <key>UISceneDelegateClassName</key>
                        <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
                    </dict>
                </array>
            </dict>
        </dict>

        <!-- SEC-02: App Transport Security — strict, no exceptions -->
        <key>NSAppTransportSecurity</key>
        <dict>
            <key>NSAllowsArbitraryLoads</key>
            <false/>
            <key>NSAllowsArbitraryLoadsForMedia</key>
            <false/>
            <key>NSAllowsArbitraryLoadsInWebContent</key>
            <false/>
            <key>NSAllowsLocalNetworking</key>
            <false/>
            <!-- NO NSExceptionDomains — SEC-02 requires zero exceptions -->
        </dict>
    </dict>
    </plist>
    ```

    **Important notes:**
    - The scene delegate is wired by classname here: `$(PRODUCT_MODULE_NAME).SceneDelegate`. Plan 05 ships the actual SceneDelegate.swift file. If the class name is different (e.g., nested), update this string.
    - NO `NSExceptionDomains` entry. If the team later legitimately needs to hit a plaintext dev endpoint, the correct path is a Debug-configuration Info.plist (not a production override).
    - NO location usage description yet (GEO-* is Phase 3). When Phase 3 adds `CLLocationManager`, it adds `NSLocationWhenInUseUsageDescription` here.
    - NO camera / Face ID usage descriptions yet (KYC is Phase 5). Those go in `Info.plist` when needed.

    **Update build settings (Xcode target → Build Settings) to point to this file:**
    - `INFOPLIST_FILE` = `validationLedger/App/Info.plist`
    - `INFOPLIST_KEY_UILaunchStoryboardName` = "" (empty — we use no launch storyboard in Phase 1; Plan 05 handles first-run via SceneDelegate)

    **Commit message: `feat(01-04): Resources/{PrivacyInfo.xcprivacy,Localizable.strings} + App/Info.plist (ATS-strict)`**
  </action>
  <verify>
    <automated>test -f validationLedger/Resources/PrivacyInfo.xcprivacy && grep -q "CA92.1" validationLedger/Resources/PrivacyInfo.xcprivacy && test -f validationLedger/App/Info.plist && grep -q "NSAllowsArbitraryLoads" validationLedger/App/Info.plist && ! grep -q "NSExceptionDomains" validationLedger/App/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    1. `test -f validationLedger/Resources/PrivacyInfo.xcprivacy` exits 0
    2. `grep -q "NSPrivacyAccessedAPICategoryUserDefaults" validationLedger/Resources/PrivacyInfo.xcprivacy` exits 0
    3. `grep -q "CA92.1" validationLedger/Resources/PrivacyInfo.xcprivacy` exits 0
    4. `grep -q "<key>NSPrivacyTracking</key>" validationLedger/Resources/PrivacyInfo.xcprivacy && grep -A1 "<key>NSPrivacyTracking</key>" validationLedger/Resources/PrivacyInfo.xcprivacy | grep -q "<false/>"` exits 0
    5. `grep -q "<key>NSPrivacyCollectedDataTypes</key>" validationLedger/Resources/PrivacyInfo.xcprivacy && grep -A1 "<key>NSPrivacyCollectedDataTypes</key>" validationLedger/Resources/PrivacyInfo.xcprivacy | grep -q "<array/>"` exits 0 (empty — no data collection in Phase 1)
    6. `test -f validationLedger/App/Info.plist` exits 0
    7. `grep -q "NSAllowsArbitraryLoads" validationLedger/App/Info.plist && grep -A1 "NSAllowsArbitraryLoads</key>" validationLedger/App/Info.plist | grep -q "<false/>"` exits 0 (SEC-02)
    8. `! grep -q "NSExceptionDomains" validationLedger/App/Info.plist` exits 0 (SEC-02 — zero exceptions)
    9. `grep -q "UISceneDelegateClassName" validationLedger/App/Info.plist && grep -q "SceneDelegate" validationLedger/App/Info.plist` exits 0 (scene manifest wired for Plan 05)
    10. `grep -q "UISupportedInterfaceOrientations~ipad" validationLedger/App/Info.plist` exits 0 (CLAUDE.md "iPad must render natively, not just scale")
    11. `xcodebuild -project validationLedger.xcodeproj -showBuildSettings | grep -q "INFOPLIST_FILE = validationLedger/App/Info.plist"` exits 0 (build setting points to our file)
    12. Target Membership verification (manual but documented): open project.pbxproj and confirm `PrivacyInfo.xcprivacy` appears in `PBXResourcesBuildPhase` for the validationLedger target — `grep -q "PrivacyInfo.xcprivacy" validationLedger.xcodeproj/project.pbxproj`
    13. `test -f validationLedger/Resources/Localizable.strings || test -f validationLedger/Resources/en.lproj/Localizable.strings` exits 0
  </acceptance_criteria>
  <done>
    - PrivacyInfo.xcprivacy exists with D-21 Phase-1 content, is in Copy Bundle Resources (Pitfall P14 preempted)
    - Localizable.strings skeleton exists (English, empty)
    - Info.plist is ATS-strict with no exception domains (SEC-02 satisfied)
    - Info.plist wires SceneDelegate class name (so Plan 05's SceneDelegate is discovered on launch)
    - iPad orientation support declared (CLAUDE.md constraint)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → app | Info.plist ATS-strict is the first line of defense — every HTTPS connection is validated by iOS before the app sees it. Without ATS-strict, an MitM attacker with a valid-looking cert on a hostile Wi-Fi could spoof traffic. |
| App Store review → build | PrivacyInfo.xcprivacy is validated at submission (ITMS-91053). A missing manifest blocks the release pipeline. |
| Bundle → running app | Resources in Copy Bundle Resources are trusted (signed by the code-signing identity). PrivacyInfo.xcprivacy NOT in Copy Bundle Resources = missing from signed bundle = App Store rejection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure + Tampering | Plaintext HTTP traffic (ATS disabled) | mitigate | Info.plist has `NSAllowsArbitraryLoads=false` + all 4 variants false + no `NSExceptionDomains`. Acceptance Criteria #7-8 grep-enforce this. |
| T-04-02 | N/A (Submission DoS) | PrivacyInfo.xcprivacy missing from `.app` bundle | mitigate | Pitfall P14 — file created AND added to Copy Bundle Resources. Plan 07 CI workflow adds a post-build grep check that fails the build if the manifest is absent from `$APP_PATH`. |
| T-04-03 | Information Disclosure | 3rd-party tracking SDK declared (Phase 1 shouldn't have any) | mitigate | PrivacyInfo.xcprivacy has empty `NSPrivacyCollectedDataTypes` array + `NSPrivacyTracking=false` + empty `NSPrivacyTrackingDomains`. STACK-04 enforcement. |
| T-04-04 | N/A (Spec drift) | Role tab inventory deviates from TechStack.md §4 | mitigate | RoleCoordinatorTests (Task 1) asserts exact tab titles per role. D-09 says "no deviation" — tests enforce. |
| T-04-05 | Elevation of Privilege | UISceneDelegateClassName in Info.plist points to wrong class | mitigate (partial) | Scene manifest references `$(PRODUCT_MODULE_NAME).SceneDelegate` — Plan 05 ships a class with that exact name. If class doesn't exist at launch, app crashes fast (not a stealth failure). |
</threat_model>

<verification>
1. `xcodebuild test -only-testing:validationLedgerTests/Roles/RoleCoordinatorTests` — all 6 tests green
2. `xcodebuild -project validationLedger.xcodeproj -scheme validationLedger build 2>&1 | tail -20` — still fails due to missing @main (Plan 05 fixes), but resource compilation (PrivacyInfo, Info.plist) succeeds
3. Info.plist contains exactly one `<key>NSAllowsArbitraryLoads</key>` followed by `<false/>`
4. PrivacyInfo.xcprivacy structure matches RESEARCH.md sample verbatim
</verification>

<success_criteria>
- 5 TabBarControllers + Role enum + RoleCoordinator protocol compile (verified by RoleCoordinatorTests passing)
- PrivacyInfo.xcprivacy in Copy Bundle Resources
- Info.plist ATS-strict
- UI/DesignSystem scaffolding ready
- Features/ empty directories tracked in git (ARCH-03 layout compliance)
- Plan 05 can now instantiate any of the 5 TabBarControllers and install via SceneDelegate
- Plan 07 can add the CI PrivacyInfo grep step expecting the file to be in the built .app
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundational-conventions-scaffolding/01-04-SUMMARY.md` capturing:
- List of all 21 files created (1 Role enum + 1 protocol + 5 TabBarControllers + 3 UI tokens + 7 Features/.gitkeep + 1 PrivacyInfo + 1 Localizable + 1 Info.plist + 1 RoleCoordinatorTests)
- SF Symbol choices used per tab (document any substitutions if the researcher's first pick was unavailable at iOS 17)
- Confirmation that PrivacyInfo.xcprivacy appears in project.pbxproj Resources build phase (grep proof)
- Confirmation that Info.plist `UISceneDelegateClassName` is `$(PRODUCT_MODULE_NAME).SceneDelegate` (so Plan 05's class auto-wires)
- Confirmation that iPad-native orientation support is declared
- Results: `RoleCoordinatorTests 6/6 passing`
</output>
