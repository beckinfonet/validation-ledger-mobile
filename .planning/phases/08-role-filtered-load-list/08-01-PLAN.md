---
phase: 08-role-filtered-load-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - validationLedger/Core/Load/LoadListItem.swift
  - validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift
  - validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift
  - validationLedgerTests/Networking/Fixtures/loads-list-broker.json
  - validationLedgerTests/Networking/Fixtures/loads-list-shipper.json
  - validationLedgerTests/Networking/Fixtures/loads-list-carrier.json
  - validationLedgerTests/Networking/Fixtures/loads-list-dispatch.json
  - validationLedgerTests/Networking/Fixtures/loads-list-factoring.json
  - validationLedgerTests/Networking/Fixtures/loads-list-empty.json
  - validationLedgerTests/Networking/Fixtures/loads-list-degraded-counterparty.json
  - validationLedgerTests/Loads/LoadListEnvelopeDecodeTests.swift
  - validationLedgerTests/Support/UIKitSnapshot.swift
autonomous: true
requirements:
  - LOAD-03
  - LOAD-04
  - TRUST-02
threat_refs:
  - T-08-01
  - T-08-02
  - T-08-03
  - T-08-04
user_setup: []

must_haves:
  truths:
    - "GET /loads/{role-rawValue} returns a paginated envelope of LoadListItem rows for each of the 5 roles"
    - "D-01: The per-row counterparty TrustNode is server-projected — already role-resolved server-side (Broker→carrier, Carrier→broker, Shipper→broker, Dispatch→broker, Factoring→carrier); iOS never selects which TrustNode to render. D-02: the envelope is the new LoadListItem value type. D-03: nil counterparty decodes cleanly via decodeIfPresent (fail-closed UNVERIFIED render)."
    - "D-04: Phase 8 owns the contract extension AND the fixture-data authoring — the 6 existing role fixtures AND the inline MockLoadFixtureRegistry.listPayloads carry the new envelope shape in lockstep"
    - "A new loads-list-degraded-counterparty.json fixture drives the fail-closed UI test path (one row nil counterparty + one row flagged counterparty) — second half of D-04."
    - "D-05: LoadListEndpoint.Response.nextCursor is decoded only — no infinite-scroll observer, no prefetch, no Load more button; the VM stores it but never reads it."
    - "A hand-rolled UIView->UIImage snapshot helper is available for Wave 2/3 snapshot tests with zero new SwiftPM dependencies"
  artifacts:
    - path: "validationLedger/Core/Load/LoadListItem.swift"
      provides: "The LoadListItem envelope value type (D-02)"
      contains: "public struct LoadListItem: Decodable, Sendable"
    - path: "validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift"
      provides: "Response.loads typed as [LoadListItem] (was [Load])"
      contains: "public let loads: [LoadListItem]"
    - path: "validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift"
      provides: "Inline listPayloads carries the envelope shape + optional registerForDegradedDemo() lane (DEBUG)"
      contains: "registerForDegradedDemo"
    - path: "validationLedgerTests/Networking/Fixtures/loads-list-broker.json"
      provides: "Envelope-wrapped broker list with carrier displayed_counterparty per row"
      contains: "displayed_counterparty"
    - path: "validationLedgerTests/Networking/Fixtures/loads-list-degraded-counterparty.json"
      provides: "Degraded edge fixture with one null and one flagged counterparty row"
      contains: "displayed_counterparty"
    - path: "validationLedgerTests/Loads/LoadListEnvelopeDecodeTests.swift"
      provides: "Envelope + nil + unknown-state + shared-world decode assertions"
      contains: "LoadListEnvelopeDecodeTests"
    - path: "validationLedgerTests/Support/UIKitSnapshot.swift"
      provides: "Hand-rolled UIView->UIImage snapshot helper (UIGraphicsImageRenderer + XCTAttachment) — Wave 0"
      contains: "UIKitSnapshot"
  key_links:
    - from: "validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift"
      to: "validationLedger/Core/Load/LoadListItem.swift"
      via: "Response.loads element type"
      pattern: "loads: \\[LoadListItem\\]"
    - from: "validationLedger/Core/Load/LoadListItem.swift"
      to: "validationLedger/Core/Load/ChainOfTrust.swift"
      via: "displayedCounterparty: TrustNode?"
      pattern: "displayedCounterparty:\\s*TrustNode\\?"
    - from: "validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift"
      to: "validationLedgerTests/Networking/Fixtures/loads-list-broker.json"
      via: "lockstep authoritative copy of envelope payload"
      pattern: "displayed_counterparty"
---

<objective>
Land the Phase 8 wire-format extension (D-01..D-04) and the Wave 0 test infrastructure that every downstream Phase 8 plan depends on. Specifically:

1. Introduce the `LoadListItem { load: Load; displayedCounterparty: TrustNode? }` envelope (D-02).
2. Change `LoadListEndpoint.Response.loads` from `[Load]` to `[LoadListItem]`.
3. Re-wrap the 6 existing role/empty list fixtures AND the inline `MockLoadFixtureRegistry.listPayloads` authoritative copy in lockstep.
4. Add the new `loads-list-degraded-counterparty.json` fixture and a DEBUG-only `MockLoadFixtureRegistry.registerForDegradedDemo()` lane.
5. Write the `LoadListEnvelopeDecodeTests` suite (envelope, fail-closed nil, unknown-verification-state degrade, snake_case bridge, shared-world consistency).
6. Land the hand-rolled `UIKitSnapshot` helper that Plans 02, 04, and 05 consume (Wave 0 dependency for all snapshot tests).

Purpose: every downstream plan reads against this contract; nothing else in Phase 8 can compile or assert without it.
Output: 1 new NEW envelope type, 1 modified endpoint, 1 modified registry, 6 modified fixtures, 1 new fixture, 1 new test suite, 1 new snapshot helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-role-filtered-load-list/08-CONTEXT.md
@.planning/phases/08-role-filtered-load-list/08-RESEARCH.md
@.planning/phases/08-role-filtered-load-list/08-PATTERNS.md
@.planning/phases/08-role-filtered-load-list/08-UI-SPEC.md
@.planning/phases/08-role-filtered-load-list/08-VALIDATION.md
@.planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md
@./CLAUDE.md

<interfaces>
Phase 7 contracts this plan reads against (verified from source). DO NOT re-explore; use these directly.

From validationLedger/Core/Load/Load.swift (UNCHANGED — nested under `load` in the envelope):
```swift
public struct Load: Decodable, Sendable {
    public let id: String
    public let referenceNumber: String
    public let origin: Address
    public let destination: Address
    public let equipment: String
    public let weight: Int
    public let rate: Decimal
    public let pickupAt: Date
    public let deliverAt: Date
    public let status: LoadStatus
    public let stateHistory: [LoadStatusEvent]
    public let respondByAt: Date?
    public let tenderEligibility: TenderEligibility?
}
```

From validationLedger/Core/Load/ChainOfTrust.swift (UNCHANGED — the type displayedCounterparty carries):
```swift
public struct TrustNode: Decodable, Sendable {
    public let partyID: String     // wire form: "party_id"  (CodingKeys handles partyID acronym bridge)
    public let role: Role          // wire form: "role"
    public let displayName: String // wire form: "display_name"
    public let verificationState: VerificationState  // wire form: "verification_state"
    public let kycCompletedAt: Date?
    public let deviceBindingStatus: DeviceBindingStatus
    public let usdotNumber: String?
    public let usdotAuthorityStatus: USDOTAuthorityStatus
    public let priorRelationshipCount: Int

    private enum CodingKeys: String, CodingKey {
        case partyID = "partyId"   // .convertFromSnakeCase produces partyId, not partyID
        case role, displayName, verificationState
        case kycCompletedAt, deviceBindingStatus
        case usdotNumber, usdotAuthorityStatus
        case priorRelationshipCount
    }
}
```

From validationLedger/Core/Load/VerificationState.swift (Phase 7 D-09 — fail-closed):
```swift
public enum VerificationState: String, Decodable, Sendable {
    case verified, pending, unverified, flagged
    public init(from decoder: Decoder) throws {
        let raw = try decoder.singleValueContainer().decode(String.self)
        self = VerificationState(rawValue: raw) ?? .unverified  // fail-closed on unknown
    }
}
```

From validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift (CURRENT — lines 36–48 are the change site):
```swift
public struct Response: Decodable, Sendable {
    public let loads: [Load]       // ← change this to [LoadListItem]
    public let nextCursor: String? // unchanged
}
```

From validationLedger/Core/Networking/APIClient.swift (verified existing decoder config):
```swift
// APIClient.defaultDecoder() sets:
//   keyDecodingStrategy = .convertFromSnakeCase
//   dateDecodingStrategy = .iso8601
// Therefore: displayed_counterparty -> displayedCounterparty is automatic; LoadListItem needs ZERO CodingKeys.
```

From validationLedgerTests/KYC/KYCThumbnailTests.swift:32-42 (snapshot helper precedent):
```swift
let renderer = UIGraphicsImageRenderer(size: size)
let image = renderer.image { ctx in ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — Hand-rolled UIView->UIImage snapshot helper</name>
  <files>validationLedgerTests/Support/UIKitSnapshot.swift</files>
  <read_first>
    - validationLedgerTests/KYC/KYCThumbnailTests.swift (lines 32–42 — `UIGraphicsImageRenderer` precedent for image rendering with `renderer.image { ctx in ... }`)
    - .planning/phases/08-role-filtered-load-list/08-RESEARCH.md (section `## Validation Architecture` § "Wave 0 Gaps" — pins the hand-rolled approach; section `## Assumptions Log` A2 — confirms no new SwiftPM dep)
    - .planning/phases/08-role-filtered-load-list/08-VALIDATION.md (Wave 0 Requirements bullet 1 — pins target file path)
    - CLAUDE.md (STACK-04 / SwiftPM-only — no new SwiftPM dependency may be added)
  </read_first>
  <action>
    Create `validationLedgerTests/Support/UIKitSnapshot.swift` exposing an `enum UIKitSnapshot` namespace with:
    - `static func image(of view: UIView, size: CGSize) -> UIImage` — sets `view.bounds = CGRect(origin: .zero, size:)`, calls `view.layoutIfNeeded()`, draws via `UIGraphicsImageRenderer(size:)` + `view.layer.render(in: ctx.cgContext)`.
    - `static func attach(_ image: UIImage, name: String, to testCase: XCTestCase, lifetime: XCTAttachment.Lifetime = .keepAlways)` — wraps the rendered image into an `XCTAttachment` and calls `testCase.add(_:)`.
    Mark `@testable import validationLedger` is NOT needed here — pure UIKit, no app symbols. The file imports `UIKit` and `XCTest` only.
    Add a file-level doc comment naming this as the app-wide snapshot baseline mechanism (precedent for Phase 8+); reference KYCThumbnailTests.swift line 34. No new SwiftPM dep is added (STACK-04 / CLAUDE.md pre-approved-list rule honored).
    Place under `validationLedgerTests/Support/` directory — create that directory if it doesn't exist (PBXFileSystemSynchronizedRootGroup auto-discovers files; no project.pbxproj edit needed).
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerTests/KYCThumbnailTests test -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    File `validationLedgerTests/Support/UIKitSnapshot.swift` exists; `grep -c '^enum UIKitSnapshot' validationLedgerTests/Support/UIKitSnapshot.swift == 1`; `grep -c 'UIGraphicsImageRenderer' validationLedgerTests/Support/UIKitSnapshot.swift >= 1`; existing test suite still builds (regression smoke).
  </done>
</task>

<task type="auto">
  <name>Task 2: Introduce LoadListItem envelope + change LoadListEndpoint.Response.loads element type</name>
  <files>
    validationLedger/Core/Load/LoadListItem.swift,
    validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift
  </files>
  <read_first>
    - validationLedger/Core/Load/Load.swift (the type nested under `load` in the envelope — confirm `public struct Load: Decodable, Sendable`)
    - validationLedger/Core/Load/ChainOfTrust.swift (lines 79–133 — TrustNode definition + CodingKeys; confirm TrustNode is Decodable, Sendable and that its `partyID` acronym bridge is already handled inside TrustNode itself)
    - validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift (lines 36–48 — current `Response.loads: [Load]` declaration to mutate)
    - validationLedger/Core/Networking/APIClient.swift (verify `defaultDecoder()` sets `.convertFromSnakeCase` + `.iso8601` so no explicit CodingKeys on LoadListItem is needed)
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "3. LoadListItem.swift" — pinned shape + § "4. LoadListEndpoint.swift" — pinned change)
    - .planning/phases/08-role-filtered-load-list/08-CONTEXT.md (D-02 + D-03 — locked decisions)
  </read_first>
  <action>
    A. NEW `validationLedger/Core/Load/LoadListItem.swift`:
       - File-level doc comment: cite "Phase 8 D-02 — server-projected list-row envelope"; explicitly document the D-03 fail-closed nil semantic (synthesized `decodeIfPresent` for the Optional handles JSON null + missing key); state that `LoadListItem` adds ZERO explicit `CodingKey` enums (D-02 / `convertFromSnakeCase` handles `displayed_counterparty` -> `displayedCounterparty`); cite Phase 7 D-09 / D-18 ("iOS never selects which TrustNode to render").
       - `import Foundation`
       - `public struct LoadListItem: Decodable, Sendable` with EXACTLY two public-let properties:
         - `public let load: Load`
         - `public let displayedCounterparty: TrustNode?`
       - Do NOT add a custom `init(from:)` — rely on the synthesized initializer + `decodeIfPresent` (per RESEARCH Pitfall 3 + Assumption A5).
       - Do NOT add CodingKeys — `convertFromSnakeCase` handles `displayed_counterparty` -> `displayedCounterparty` transparently (RESEARCH Pitfall 2 confirms `partyID` is the ONLY acronym field in TrustNode; LoadListItem itself has no acronym fields).

    B. MODIFY `validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift`:
       - Change the single declaration `public let loads: [Load]` -> `public let loads: [LoadListItem]` (lines 36–48 region).
       - Update the doc comment on `loads` to call out the envelope: "Phase 8 D-02: each row wraps a `Load` under `load:` and carries the server-projected `displayedCounterparty: TrustNode?` (nil is fail-closed UNVERIFIED per D-03). `LoadDetailEndpoint.Response.load: Load` is UNTOUCHED — list vs detail are cleanly separated by the envelope."
       - Leave `public let nextCursor: String?` untouched (D-05: decoded only).
       - Leave every other declaration / struct / endpoint conformance byte-identical.

    Per T-08-03 (Decoder safety): no custom `init(from:)`; rely on synthesized `decodeIfPresent`. Per T-08-04 (PII): the new type carries no logging.
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' build -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    `grep -c 'public let displayedCounterparty: TrustNode?' validationLedger/Core/Load/LoadListItem.swift == 1`; `grep -c 'public let load: Load$' validationLedger/Core/Load/LoadListItem.swift == 1`; `grep -c 'public let loads: \[LoadListItem\]' validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift == 1`; `grep -c 'public let loads: \[Load\]' validationLedger/Core/Networking/Endpoints/LoadListEndpoint.swift == 0`; `grep -c 'CodingKeys' validationLedger/Core/Load/LoadListItem.swift == 0`; `grep -c 'init(from' validationLedger/Core/Load/LoadListItem.swift == 0`; project builds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-wrap all 6 list fixtures + add degraded-counterparty fixture + lockstep MockLoadFixtureRegistry inline payloads + DEBUG demo lane</name>
  <files>
    validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift,
    validationLedgerTests/Networking/Fixtures/loads-list-broker.json,
    validationLedgerTests/Networking/Fixtures/loads-list-shipper.json,
    validationLedgerTests/Networking/Fixtures/loads-list-carrier.json,
    validationLedgerTests/Networking/Fixtures/loads-list-dispatch.json,
    validationLedgerTests/Networking/Fixtures/loads-list-factoring.json,
    validationLedgerTests/Networking/Fixtures/loads-list-empty.json,
    validationLedgerTests/Networking/Fixtures/loads-list-degraded-counterparty.json
  </files>
  <behavior>
    - After re-wrap: every existing row's pre-Phase-8 fields nest under a `"load": {...}` object; each row also has a `"displayed_counterparty": {...}` (or `null`) sibling.
    - Per-role counterparty role (D-04 + Phase 7 D-11 shared-world): broker fixture rows carry carrier counterparties; shipper rows carry broker; carrier rows carry broker; dispatch rows carry broker; factoring rows carry carrier; empty fixture is `{"loads": [], "next_cursor": null}` (envelope-shape passthrough — zero rows, decodes cleanly).
    - Shared-world invariant: load `VL-1001` (or whichever ID is shared across role lists) carries the role-appropriate counterparty in each role file (broker sees its carrier; shipper sees its broker; etc.); the counterparty TrustNode reuses the `partyID` + `displayName` + `verificationState` from `load-detail-VL-1001.json`'s `chain_of_trust.nodes`.
    - Degraded fixture: at least one row with `"displayed_counterparty": null`; at least one row with `"displayed_counterparty.verification_state": "flagged"`; the rest of the rows are valid envelope-wrapped rows for contrast.
    - `MockLoadFixtureRegistry.swift` inline `listPayloads` table is rewritten in lockstep with the JSON files (the per-file AUTHORITATIVE COPY banner at lines 30–47 demands lockstep edits — PATTERNS.md §5).
    - DEBUG demo lane: a new `static func registerForDegradedDemo()` exists inside the existing `#if DEBUG ... #endif` block; it is NOT called from `registerAppDefaults()` (RESEARCH Open Question 2 ratifies test-only); it registers the degraded fixture via the existing path-suffix dispatch mechanism (sentinel role suffix `"degraded"` — `/loads/degraded` — chosen because the existing dispatcher uses path-suffix matching, no query parsing).
  </behavior>
  <read_first>
    - validationLedgerTests/Networking/Fixtures/loads-list-broker.json (current top-level shape — `{ "loads": [ {flat Load fields}, ... ], "next_cursor": null }`)
    - validationLedgerTests/Networking/Fixtures/loads-list-empty.json (current 4-line file — `{ "loads": [], "next_cursor": null }`)
    - validationLedgerTests/Networking/Fixtures/load-detail-VL-1001.json (source of the canonical TrustNode shape for `displayed_counterparty` — see `chain_of_trust.nodes` for the field set: party_id, role, display_name, verification_state, kyc_completed_at, device_binding_status, usdot_number, usdot_authority_status, prior_relationship_count)
    - validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift (lines 27–47 AUTHORITATIVE COPY banner; lines 78 + last line `#if DEBUG`/`#endif`; lines 89–129 register block; lines 161+ `listPayloads` table — these are the structural references)
    - validationLedger/Core/Load/ChainOfTrust.swift (lines 79–133 — TrustNode field set + CodingKeys; CONFIRMS `party_id` wire form vs `partyID` Swift name; the other fields are clean snake_case<->camelCase via `convertFromSnakeCase`)
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "9. loads-list-{role}.json" — the wrap template + the per-role counterparty role table + shared-world consistency rule; § "5. MockLoadFixtureRegistry.swift" — the lockstep edit rule)
    - .planning/phases/08-role-filtered-load-list/08-CONTEXT.md (D-04 — fixture authoring; "Specifics" — fraud archetype VL-#### should surface .flagged badge in broker fixture)
    - .planning/phases/07-load-domain-model-mock-contract/07-CONTEXT.md (D-11 shared-world; D-13 fraud archetypes — double-brokering / chameleon / factoring-fraud each get a distinct flagged load)
  </read_first>
  <action>
    A. For each of the 6 list fixture JSON files in `validationLedgerTests/Networking/Fixtures/`:
       - For `loads-list-empty.json`: leave the file as `{"loads": [], "next_cursor": null}` (zero rows, envelope-shape passthrough).
       - For the 5 role files: rewrite every row by nesting the existing Load fields under a `"load": {...}` key, then adding `"displayed_counterparty": {...}` (or `null` if intentional — e.g. a draft/posted load with no counterparty yet) as a sibling.
         - The `displayed_counterparty` TrustNode shape: `{"party_id", "role", "display_name", "verification_state", "kyc_completed_at", "device_binding_status", "usdot_number", "usdot_authority_status", "prior_relationship_count"}` (snake_case wire form; verify against `load-detail-VL-1001.json` `chain_of_trust.nodes[]`).
         - Per-role role assignment: broker -> carrier; shipper -> broker; carrier -> broker; dispatch -> broker; factoring -> carrier.
         - Shared-world rule (Phase 7 D-11): for each load ID that appears in multiple role files, reuse the SAME upstream/downstream party TrustNodes from `load-detail-{ID}.json`. The broker fixture's `VL-1001` carries its carrier (Acme Trucking Inc.); the same `VL-1001` in `loads-list-shipper.json` carries its broker (FreightWise) — same `party_id`, same `display_name`.
         - Per CONTEXT.md §Specifics: the double-brokered fraud-archetype load (Phase 7 D-13) in `loads-list-broker.json` MUST carry its carrier with `"verification_state": "flagged"` so the demo flow surfaces the fraud signal on the row (before the user taps into Phase 9 detail).
       - Keep `"next_cursor": null` (or whatever the file currently has) untouched.
    B. NEW `validationLedgerTests/Networking/Fixtures/loads-list-degraded-counterparty.json`:
       - 3 rows minimum. Use synthetic party names (Acme Trucking Inc., FreightWise Brokerage Inc., etc. — already in the existing fixture corpus). Required structural traits:
         - Row 1: `"displayed_counterparty": null` (drives D-03 fail-closed render).
         - Row 2: `"displayed_counterparty": {... "verification_state": "flagged"}` (drives flagged-badge render path).
         - Row 3: valid envelope-wrapped row with a `verification_state: "verified"` counterparty for visual contrast.
       - Envelope shape matches the 5 role files exactly.
       - `"next_cursor": null`.
    C. MODIFY `validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift`:
       - LOCKSTEP REWRITE of the inline `listPayloads: [String: Data]` table (lines 161+) — every payload string is rewritten to match the new envelope shape exactly. The AUTHORITATIVE COPY banner at lines 30–47 demands this; the planner / executor MUST apply the JSON edit to both the .json file AND the inline `Data(#""" ... """#.utf8)` literal for each of the 5 role keys + empty.
       - Do NOT touch the `register { request in ... }` dispatcher block at lines 89–99 — the path-suffix routing is correct; only the response payload data changes.
       - ADD `static func registerForDegradedDemo()` inside the `#if DEBUG ... #endif` block (the whole enum is already DEBUG-only). The function registers ONE handler that dispatches `request.url?.path == "/loads/degraded"` to the new degraded payload (inline-Data form of `loads-list-degraded-counterparty.json`). Function is NEVER called from `registerAppDefaults()`; it is called only by the Wave 1 + downstream unit tests that need the degraded scenario (RESEARCH Open Question 2 — test-only).
       - Add a doc comment block on `registerForDegradedDemo()` citing CONTEXT.md D-04 second half and RESEARCH §Discretion (sentinel-role suffix path chosen because the existing dispatcher is path-suffix matched, not query-parsed).

    Per T-08-04 (PII): the fixtures contain synthetic party names only; no real customer data. Per T-08-03 (Decoder safety): every `displayed_counterparty` field is either a TrustNode-shaped object or JSON `null`; no malformed shapes are intentionally introduced.

    NOTE: This task is large (8 files), so execute as one atomic commit (the AUTHORITATIVE COPY banner mandates lockstep — partial edits leave the registry desynced from the on-disk fixtures and ALL endpoint tests would fail mid-edit). Apply the wrap one file at a time but commit as a single transaction.
  </behavior>
  <action>
    See `<behavior>` block above — the full execution recipe lives there because every step is testable as a structural assertion (envelope-wrapped rows have `load` key + `displayed_counterparty` key; per-role role assignment is deterministic; shared-world TrustNodes are byte-identical across role files; lockstep registry edit means `grep -c 'displayed_counterparty' MockLoadFixtureRegistry.swift > 0`).
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerTests/Networking/LoadEndpointsTests test -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    For each of `loads-list-{broker,shipper,carrier,dispatch,factoring}.json`: `grep -v '^#' file | grep -c '"load":' >= 1` AND `grep -v '^#' file | grep -c '"displayed_counterparty"' >= 1`; `loads-list-empty.json` decodes to `{"loads": [], "next_cursor": null}` (envelope-shape passthrough); `loads-list-degraded-counterparty.json` contains BOTH `"displayed_counterparty": null` AND `"verification_state": "flagged"` (`grep -c '"displayed_counterparty": null' loads-list-degraded-counterparty.json >= 1` AND `grep -c '"verification_state": "flagged"' loads-list-degraded-counterparty.json >= 1`); `grep -c 'registerForDegradedDemo' validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift >= 1`; `grep -c 'displayed_counterparty' validationLedger/Core/Networking/Mock/MockLoadFixtureRegistry.swift >= 5` (every inline role payload was rewritten); existing `LoadEndpointsTests` suite still compiles and the Response decode tests still pass against the envelope (the test file may need an update for the element-type change — handled in Task 4 below).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: LoadListEnvelopeDecodeTests — envelope + fail-closed nil + unknown-state degrade + shared-world cross-fixture invariant</name>
  <files>validationLedgerTests/Loads/LoadListEnvelopeDecodeTests.swift</files>
  <behavior>
    Test 1 — `envelopeWrapsLoadAndCounterpartyForBrokerList`: register `loads-list-broker.json` via `FixtureLoader` + `MockURLProtocol.registerFixture(...)`; call the endpoint via `APIClient.request(LoadListEndpoint(role: .broker))`; assert `response.loads.count > 0`, `response.loads[0].load` is a non-nil `Load` aggregate, `response.loads[0].displayedCounterparty?.role == .carrier` (broker sees carrier counterparty per D-04 + Phase 7 D-11).
    Test 2 — `decodeIfPresentHandlesNullCounterparty`: feed a synthetic JSON string `{"loads": [{"load": {<minimal valid Load>}, "displayed_counterparty": null}], "next_cursor": null}` into `JSONDecoder()` configured with the same `defaultDecoder()` settings as `APIClient`; assert the single row's `displayedCounterparty == nil` (D-03 fail-closed via synthesized `decodeIfPresent`).
    Test 3 — `decodeIfPresentHandlesMissingCounterpartyKey`: same as Test 2 but with the `"displayed_counterparty"` key OMITTED from the JSON; assert the row decodes successfully and `displayedCounterparty == nil` (synthesized `decodeIfPresent` handles both null AND missing key — RESEARCH Pitfall 3).
    Test 4 — `unknownVerificationStateDegradesToUnverified`: synthesize JSON with `"displayed_counterparty.verification_state": "compromised"` (an unknown enum value); assert that the row decodes successfully and `displayedCounterparty?.verificationState == .unverified` (Phase 7 D-09 fail-closed VerificationState decoder degrades unknown to .unverified at the Phase 7 contract layer).
    Test 5 — `degradedFixtureCarriesOneNullAndOneFlagged`: load `loads-list-degraded-counterparty.json` via FixtureLoader; assert that at least one row has `displayedCounterparty == nil` AND at least one row has `displayedCounterparty?.verificationState == .flagged`.
    Test 6 — `sharedWorldInvariantBrokerVsShipperSeesDifferentRoleCounterpartyForSameLoadID`: load `loads-list-broker.json` AND `loads-list-shipper.json`; find a shared load ID present in both; assert the broker file's row has `displayedCounterparty?.role == .carrier` AND the shipper file's row (same load ID) has `displayedCounterparty?.role == .broker` (Phase 7 D-11 shared-world).
    Test 7 — `nextCursorRoundTripsAcrossSnakeCaseBridge`: synthesize JSON with `"next_cursor": "abc-123"`; assert `response.nextCursor == "abc-123"` (D-05 + `.convertFromSnakeCase` proof).
    Test 8 — `nilNextCursorDecodesToNil`: synthesize JSON with `"next_cursor": null`; assert `response.nextCursor == nil`.
    Test 9 — `emptyEnvelopeFixtureDecodesToZeroRows`: load `loads-list-empty.json`; assert `response.loads.isEmpty == true`.

    Per T-08-03 (Decoder safety): Test 4 locks the fail-closed VerificationState behavior at the LoadListItem layer. Per T-08-04 (PII): tests use only synthetic party names from the existing fixture corpus.
  </behavior>
  <read_first>
    - validationLedgerTests/Networking/MockURLProtocolLatencyTests.swift (lines 26–60 — Swift Testing `@Suite(.serialized)` pattern + `MockURLProtocol.reset()` per-test discipline; also confirm the import set `import Testing`/`import Foundation`/`@testable import validationLedger`)
    - validationLedgerTests/Networking/FixtureLoader.swift (test-bundle JSON loader — confirm the API surface: `FixtureLoader.data(named: "loads-list-broker") -> Data`)
    - validationLedgerTests/Load/VerificationStateDecoderTests.swift (the Phase 7 fail-closed unknown-value decode test — structural precedent for Test 4)
    - validationLedger/Core/Networking/APIClient.swift (`defaultDecoder()` — confirm it returns a `JSONDecoder` with `.convertFromSnakeCase` + `.iso8601` so the synthesized-JSON tests can reuse the same configuration)
    - validationLedger/Core/Load/LoadListItem.swift (the envelope type written in Task 2 — exercise its surface)
    - .planning/phases/08-role-filtered-load-list/08-PATTERNS.md (§ "12. LoadListEndpointDecodeTests.swift" — the test-coverage matrix)
    - .planning/phases/08-role-filtered-load-list/08-RESEARCH.md (§ "Pitfall 3" — null vs missing key both -> nil; § "Pitfall 2" — partyID acronym bridge is in TrustNode already; § "Security Domain" Known Threat #5 — unknown verification_state superstring lock)
  </read_first>
  <action>
    Create `validationLedgerTests/Loads/LoadListEnvelopeDecodeTests.swift`. Use Swift Testing per STACK-03 (`import Testing` + `@Suite(.serialized)` + `@Test`). The `.serialized` attribute is REQUIRED because each test mutates the global `MockURLProtocol` handler registry (precedent: every Phase 7 endpoint test that touches the registry).
    Per-test setup pattern: `MockURLProtocol.reset()` + `MockURLProtocol.resetFailureHandlers()` at top of body; `defer { MockURLProtocol.reset(); MockURLProtocol.resetFailureHandlers() }` for cleanup.
    Implement the 9 tests enumerated in `<behavior>` above. Where a test does NOT need MockURLProtocol (Tests 2, 3, 4, 7, 8 — pure synthesized-JSON decodes), use a private helper `decode<T: Decodable>(_ json: String) throws -> T` that constructs a `JSONDecoder` with `keyDecodingStrategy = .convertFromSnakeCase` and `dateDecodingStrategy = .iso8601` (matches `APIClient.defaultDecoder()` configuration), then calls `try decoder.decode(T.self, from: Data(json.utf8))`.
    Where a test DOES use MockURLProtocol (Tests 1, 5, 6, 9), register the fixture via `MockURLProtocol.registerFixture(for: LoadListEndpoint.self, ...)` (Phase 7 surface) and call through `APIClient.request(...)`.
    Use `#expect(...)` for assertions (Swift Testing surface, not XCTAssert).
  </action>
  <verify>
    <automated>xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' -only-testing:validationLedgerTests/LoadListEnvelopeDecodeTests test -parallel-testing-enabled NO</automated>
  </verify>
  <done>
    File `validationLedgerTests/Loads/LoadListEnvelopeDecodeTests.swift` exists; `grep -c '@Test' validationLedgerTests/Loads/LoadListEnvelopeDecodeTests.swift >= 9`; `grep -c '\.serialized' validationLedgerTests/Loads/LoadListEnvelopeDecodeTests.swift >= 1`; the suite passes in the scoped simulator-lane invocation above (exit code 0); the shared-world invariant Test 6 passes (proving the Task 3 fixture edits got the per-role counterparty roles right).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture JSON / wire format -> iOS Decodable types | Malformed or hostile JSON from the mock (or eventual live backend) crosses into the LoadListItem decoder. Phase 7 D-09 already mitigates unknown enum values at the VerificationState layer; this plan extends the boundary to the new envelope shape. |
| MockLoadFixtureRegistry inline payloads <-> on-disk fixture files | Lockstep edit boundary — divergence here is a tampering vector (an attacker who can edit ONE side but not both could in principle desync runtime behavior from test expectations). The AUTHORITATIVE COPY banner enforces lockstep; CI test coverage catches drift. |
| Snapshot helper -> XCTAttachment lifecycle | Snapshot rendered images may end up in CI artifacts. Mitigation: synthetic party names only (CLAUDE.md zero-PII constraint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `LoadListEndpoint.Response.loads` element type change | accept | Source-incompatible diff; the only call site is born in Phase 8 (no downstream consumers exist outside this phase). Verified via grep across `validationLedger/` before edit. |
| T-08-02 | Information disclosure | Fixture JSON files in `validationLedgerTests/Networking/Fixtures/` | mitigate | Synthetic party names only ("Acme Trucking Inc.", "FreightWise Brokerage Inc."). PROJECT.md PII discipline: never inject real customer names into fixtures. Verified per RESEARCH §Security Domain Known-Threat-5. |
| T-08-03 | Tampering / Decoder safety | `LoadListItem` Decodable surface | mitigate | NO custom `init(from:)`; rely on synthesized initializer. `decodeIfPresent` handles JSON `null` + missing key for `displayedCounterparty` (D-03 fail-closed). Unknown enum values on `verification_state` collapse to `.unverified` at the Phase 7 VerificationState decoder layer (Phase 7 D-09). Locked by Tests 2, 3, 4 in Task 4. |
| T-08-04 | Information disclosure (PII) | All new files in this plan | mitigate | No logging in `LoadListItem` (pure value type); fixtures contain synthetic data only; the snapshot helper is structural with no string content. CLAUDE.md "Zero PII in analytics or crash logs" — verified by inspection. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | NO new SwiftPM dependencies introduced in this plan (CLAUDE.md SwiftPM-only + STACK-04 zero-new-SDK rule); every framework used is iOS-bundled. Package legitimacy gate is vacuously satisfied. |
</threat_model>

<verification>
1. Build green: `xcodebuild -project validationLedger.xcodeproj -scheme validationLedger -destination 'platform=iOS Simulator,name=iPhone 15,OS=17.5' build -parallel-testing-enabled NO` exits 0.
2. New decode suite green: scoped `-only-testing:validationLedgerTests/LoadListEnvelopeDecodeTests` passes (exit 0).
3. Regression smoke: scoped `-only-testing:validationLedgerTests/Networking/LoadEndpointsTests` still passes against the envelope-wrapped fixtures (the existing test file may need a one-line update for the element-type change — apply if needed during Task 4).
4. Shared-world invariant locked: Test 6 in Task 4 passes (proving broker/shipper per-role counterparty roles are wired correctly in Task 3).
5. Wave 0 snapshot helper available: file exists at `validationLedgerTests/Support/UIKitSnapshot.swift` (downstream plans depend on this).
</verification>

<success_criteria>
- LoadListItem envelope exists and is the element type of LoadListEndpoint.Response.loads.
- All 6 existing fixtures + 1 new degraded fixture + the inline MockLoadFixtureRegistry.listPayloads carry the envelope shape in lockstep.
- Per-role counterparty role assignment matches D-04 (broker->carrier, shipper->broker, carrier->broker, dispatch->broker, factoring->carrier).
- Shared-world consistency holds: a load ID appearing in multiple role files carries the role-appropriate counterparty in each.
- `LoadListEnvelopeDecodeTests` exits green with >= 9 tests covering envelope, fail-closed nil, unknown-state degrade, shared-world, next_cursor round-trip, empty envelope.
- `UIKitSnapshot.swift` is available for Plans 02 / 04 / 05.
- Zero new SwiftPM dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/08-role-filtered-load-list/08-01-SUMMARY.md` when done. Record:
- LoadListItem signature, the LoadListEndpoint change, the 7 fixture file paths + the registry inline-payload lockstep edit, the `loads-list-degraded-counterparty.json` field selection, the `registerForDegradedDemo()` lane signature + sentinel-role suffix choice, the UIKitSnapshot API surface, and any open question that surfaced (e.g. the existing `LoadEndpointsTests.swift` update if any).
</output>
